///////////////////////////////////////////////////////////////////////////
// This file was generated with svd2cpp, source file was RP2350_regs.svd
// DO NOT EDIT - CHANGES MIGHT BE OVERWRITTEN !!
///////////////////////////////////////////////////////////////////////////
//
#include "bitfield_defs.h"

// vendor: Raspberry Pi
// name: RP2350
// series: RP
// version: 0.1
// description: 
// Dual Cortex-M33 or Hazard3 processors at 150MHz 520kB on-chip SRAM, in 10 independent banks Extended low-power sleep states with optional SRAM retention: as low as 10uA DVDD 8kB of one-time-programmable storage (OTP) Up to 16MB of external QSPI flash/PSRAM via dedicated QSPI bus Additional 16MB flash/PSRAM accessible via optional second chip-select On-chip switched-mode power supply to generate core voltage Low-quiescent-current LDO mode can be enabled for sleep states 2x on-chip PLLs for internal or external clock generation GPIOs are 5V-tolerant (powered), and 3.3V-failsafe (unpowered) Security features: Optional boot signing, enforced by on-chip mask ROM, with key fingerprint in OTP Protected OTP storage for optional boot decryption key Global bus filtering based on Arm or RISC-V security/privilege levels Peripherals, GPIOs and DMA channels individually assignable to security domains Hardware mitigations for fault injection attacks Hardware SHA-256 accelerator Peripherals: 2x UARTs 2x SPI controllers 2x I2C controllers 24x PWM channels USB 1.1 controller and PHY, with host and device support 12x PIO state machines 1x HSTX peripheral
// width: 32
// size: 32
// resetMask: 0xffffffff
// resetValue: 0x00000000
// access: read-write
// licenseText: 
// Copyright (c) 2024 Raspberry Pi Ltd. SPDX-License-Identifier: BSD-3-Clause
// cpu
//     name: CM33
//     revision: r1p0
//     endian: little
//     mpuPresent: true
//     fpuPresent: true
//     sauNumRegions: 8
//     nvicPrioBits: 4
//     vtorPresent: 1
//     dspPresent: 1
//     vendorSystickConfig: false
//     deviceNumInterrupts: 52
// addressUnitBits: 8

namespace _RESETS_  {

    // Reset value: 0x1fffffff
    BEGIN_TYPE(RESET_t, uint32_t)
        ADD_BITFIELD_RW(USBCTRL, 28, 1)
        ADD_BITFIELD_RW(UART1, 27, 1)
        ADD_BITFIELD_RW(UART0, 26, 1)
        ADD_BITFIELD_RW(TRNG, 25, 1)
        ADD_BITFIELD_RW(TIMER1, 24, 1)
        ADD_BITFIELD_RW(TIMER0, 23, 1)
        ADD_BITFIELD_RW(TBMAN, 22, 1)
        ADD_BITFIELD_RW(SYSINFO, 21, 1)
        ADD_BITFIELD_RW(SYSCFG, 20, 1)
        ADD_BITFIELD_RW(SPI1, 19, 1)
        ADD_BITFIELD_RW(SPI0, 18, 1)
        ADD_BITFIELD_RW(SHA256, 17, 1)
        ADD_BITFIELD_RW(PWM, 16, 1)
        ADD_BITFIELD_RW(PLL_USB, 15, 1)
        ADD_BITFIELD_RW(PLL_SYS, 14, 1)
        ADD_BITFIELD_RW(PIO2, 13, 1)
        ADD_BITFIELD_RW(PIO1, 12, 1)
        ADD_BITFIELD_RW(PIO0, 11, 1)
        ADD_BITFIELD_RW(PADS_QSPI, 10, 1)
        ADD_BITFIELD_RW(PADS_BANK0, 9, 1)
        ADD_BITFIELD_RW(JTAG, 8, 1)
        ADD_BITFIELD_RW(IO_QSPI, 7, 1)
        ADD_BITFIELD_RW(IO_BANK0, 6, 1)
        ADD_BITFIELD_RW(I2C1, 5, 1)
        ADD_BITFIELD_RW(I2C0, 4, 1)
        ADD_BITFIELD_RW(HSTX, 3, 1)
        ADD_BITFIELD_RW(DMA, 2, 1)
        ADD_BITFIELD_RW(BUSCTRL, 1, 1)
        ADD_BITFIELD_RW(ADC, 0, 1)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(WDSEL_t, uint32_t)
        ADD_BITFIELD_RW(USBCTRL, 28, 1)
        ADD_BITFIELD_RW(UART1, 27, 1)
        ADD_BITFIELD_RW(UART0, 26, 1)
        ADD_BITFIELD_RW(TRNG, 25, 1)
        ADD_BITFIELD_RW(TIMER1, 24, 1)
        ADD_BITFIELD_RW(TIMER0, 23, 1)
        ADD_BITFIELD_RW(TBMAN, 22, 1)
        ADD_BITFIELD_RW(SYSINFO, 21, 1)
        ADD_BITFIELD_RW(SYSCFG, 20, 1)
        ADD_BITFIELD_RW(SPI1, 19, 1)
        ADD_BITFIELD_RW(SPI0, 18, 1)
        ADD_BITFIELD_RW(SHA256, 17, 1)
        ADD_BITFIELD_RW(PWM, 16, 1)
        ADD_BITFIELD_RW(PLL_USB, 15, 1)
        ADD_BITFIELD_RW(PLL_SYS, 14, 1)
        ADD_BITFIELD_RW(PIO2, 13, 1)
        ADD_BITFIELD_RW(PIO1, 12, 1)
        ADD_BITFIELD_RW(PIO0, 11, 1)
        ADD_BITFIELD_RW(PADS_QSPI, 10, 1)
        ADD_BITFIELD_RW(PADS_BANK0, 9, 1)
        ADD_BITFIELD_RW(JTAG, 8, 1)
        ADD_BITFIELD_RW(IO_QSPI, 7, 1)
        ADD_BITFIELD_RW(IO_BANK0, 6, 1)
        ADD_BITFIELD_RW(I2C1, 5, 1)
        ADD_BITFIELD_RW(I2C0, 4, 1)
        ADD_BITFIELD_RW(HSTX, 3, 1)
        ADD_BITFIELD_RW(DMA, 2, 1)
        ADD_BITFIELD_RW(BUSCTRL, 1, 1)
        ADD_BITFIELD_RW(ADC, 0, 1)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(RESET_DONE_t, uint32_t)
        ADD_BITFIELD_RO(USBCTRL, 28, 1)
        ADD_BITFIELD_RO(UART1, 27, 1)
        ADD_BITFIELD_RO(UART0, 26, 1)
        ADD_BITFIELD_RO(TRNG, 25, 1)
        ADD_BITFIELD_RO(TIMER1, 24, 1)
        ADD_BITFIELD_RO(TIMER0, 23, 1)
        ADD_BITFIELD_RO(TBMAN, 22, 1)
        ADD_BITFIELD_RO(SYSINFO, 21, 1)
        ADD_BITFIELD_RO(SYSCFG, 20, 1)
        ADD_BITFIELD_RO(SPI1, 19, 1)
        ADD_BITFIELD_RO(SPI0, 18, 1)
        ADD_BITFIELD_RO(SHA256, 17, 1)
        ADD_BITFIELD_RO(PWM, 16, 1)
        ADD_BITFIELD_RO(PLL_USB, 15, 1)
        ADD_BITFIELD_RO(PLL_SYS, 14, 1)
        ADD_BITFIELD_RO(PIO2, 13, 1)
        ADD_BITFIELD_RO(PIO1, 12, 1)
        ADD_BITFIELD_RO(PIO0, 11, 1)
        ADD_BITFIELD_RO(PADS_QSPI, 10, 1)
        ADD_BITFIELD_RO(PADS_BANK0, 9, 1)
        ADD_BITFIELD_RO(JTAG, 8, 1)
        ADD_BITFIELD_RO(IO_QSPI, 7, 1)
        ADD_BITFIELD_RO(IO_BANK0, 6, 1)
        ADD_BITFIELD_RO(I2C1, 5, 1)
        ADD_BITFIELD_RO(I2C0, 4, 1)
        ADD_BITFIELD_RO(HSTX, 3, 1)
        ADD_BITFIELD_RO(DMA, 2, 1)
        ADD_BITFIELD_RO(BUSCTRL, 1, 1)
        ADD_BITFIELD_RO(ADC, 0, 1)
    END_TYPE()

    struct RESETS_t {
        RESET_t                       RESET;
        WDSEL_t                       WDSEL;
        RESET_DONE_t                  RESET_DONE;
    };

    static RESETS_t & RESETS = (*(RESETS_t *)0x40020000);
    static RESETS_t & RESETS_XOR = (*(RESETS_t *)0x40021000);
    static RESETS_t & RESETS_SET = (*(RESETS_t *)0x40022000);
    static RESETS_t & RESETS_CLR = (*(RESETS_t *)0x40023000);

} // _RESETS_

namespace _PSM_  {

    // Force block out of reset (i.e. power it on)
    // Reset value: 0x00000000
    BEGIN_TYPE(FRCE_ON_t, uint32_t)
        ADD_BITFIELD_RW(PROC1, 24, 1)
        ADD_BITFIELD_RW(PROC0, 23, 1)
        ADD_BITFIELD_RW(ACCESSCTRL, 22, 1)
        ADD_BITFIELD_RW(SIO, 21, 1)
        ADD_BITFIELD_RW(XIP, 20, 1)
        ADD_BITFIELD_RW(SRAM9, 19, 1)
        ADD_BITFIELD_RW(SRAM8, 18, 1)
        ADD_BITFIELD_RW(SRAM7, 17, 1)
        ADD_BITFIELD_RW(SRAM6, 16, 1)
        ADD_BITFIELD_RW(SRAM5, 15, 1)
        ADD_BITFIELD_RW(SRAM4, 14, 1)
        ADD_BITFIELD_RW(SRAM3, 13, 1)
        ADD_BITFIELD_RW(SRAM2, 12, 1)
        ADD_BITFIELD_RW(SRAM1, 11, 1)
        ADD_BITFIELD_RW(SRAM0, 10, 1)
        ADD_BITFIELD_RW(BOOTRAM, 9, 1)
        ADD_BITFIELD_RW(ROM, 8, 1)
        ADD_BITFIELD_RW(BUSFABRIC, 7, 1)
        ADD_BITFIELD_RW(PSM_READY, 6, 1)
        ADD_BITFIELD_RW(CLOCKS, 5, 1)
        ADD_BITFIELD_RW(RESETS, 4, 1)
        ADD_BITFIELD_RW(XOSC, 3, 1)
        ADD_BITFIELD_RW(ROSC, 2, 1)
        ADD_BITFIELD_RW(OTP, 1, 1)
        ADD_BITFIELD_RW(PROC_COLD, 0, 1)
    END_TYPE()

    // Force into reset (i.e. power it off)
    // Reset value: 0x00000000
    BEGIN_TYPE(FRCE_OFF_t, uint32_t)
        ADD_BITFIELD_RW(PROC1, 24, 1)
        ADD_BITFIELD_RW(PROC0, 23, 1)
        ADD_BITFIELD_RW(ACCESSCTRL, 22, 1)
        ADD_BITFIELD_RW(SIO, 21, 1)
        ADD_BITFIELD_RW(XIP, 20, 1)
        ADD_BITFIELD_RW(SRAM9, 19, 1)
        ADD_BITFIELD_RW(SRAM8, 18, 1)
        ADD_BITFIELD_RW(SRAM7, 17, 1)
        ADD_BITFIELD_RW(SRAM6, 16, 1)
        ADD_BITFIELD_RW(SRAM5, 15, 1)
        ADD_BITFIELD_RW(SRAM4, 14, 1)
        ADD_BITFIELD_RW(SRAM3, 13, 1)
        ADD_BITFIELD_RW(SRAM2, 12, 1)
        ADD_BITFIELD_RW(SRAM1, 11, 1)
        ADD_BITFIELD_RW(SRAM0, 10, 1)
        ADD_BITFIELD_RW(BOOTRAM, 9, 1)
        ADD_BITFIELD_RW(ROM, 8, 1)
        ADD_BITFIELD_RW(BUSFABRIC, 7, 1)
        ADD_BITFIELD_RW(PSM_READY, 6, 1)
        ADD_BITFIELD_RW(CLOCKS, 5, 1)
        ADD_BITFIELD_RW(RESETS, 4, 1)
        ADD_BITFIELD_RW(XOSC, 3, 1)
        ADD_BITFIELD_RW(ROSC, 2, 1)
        ADD_BITFIELD_RW(OTP, 1, 1)
        ADD_BITFIELD_RW(PROC_COLD, 0, 1)
    END_TYPE()

    // Set to 1 if the watchdog should reset this
    // Reset value: 0x00000000
    BEGIN_TYPE(WDSEL_t, uint32_t)
        ADD_BITFIELD_RW(PROC1, 24, 1)
        ADD_BITFIELD_RW(PROC0, 23, 1)
        ADD_BITFIELD_RW(ACCESSCTRL, 22, 1)
        ADD_BITFIELD_RW(SIO, 21, 1)
        ADD_BITFIELD_RW(XIP, 20, 1)
        ADD_BITFIELD_RW(SRAM9, 19, 1)
        ADD_BITFIELD_RW(SRAM8, 18, 1)
        ADD_BITFIELD_RW(SRAM7, 17, 1)
        ADD_BITFIELD_RW(SRAM6, 16, 1)
        ADD_BITFIELD_RW(SRAM5, 15, 1)
        ADD_BITFIELD_RW(SRAM4, 14, 1)
        ADD_BITFIELD_RW(SRAM3, 13, 1)
        ADD_BITFIELD_RW(SRAM2, 12, 1)
        ADD_BITFIELD_RW(SRAM1, 11, 1)
        ADD_BITFIELD_RW(SRAM0, 10, 1)
        ADD_BITFIELD_RW(BOOTRAM, 9, 1)
        ADD_BITFIELD_RW(ROM, 8, 1)
        ADD_BITFIELD_RW(BUSFABRIC, 7, 1)
        ADD_BITFIELD_RW(PSM_READY, 6, 1)
        ADD_BITFIELD_RW(CLOCKS, 5, 1)
        ADD_BITFIELD_RW(RESETS, 4, 1)
        ADD_BITFIELD_RW(XOSC, 3, 1)
        ADD_BITFIELD_RW(ROSC, 2, 1)
        ADD_BITFIELD_RW(OTP, 1, 1)
        ADD_BITFIELD_RW(PROC_COLD, 0, 1)
    END_TYPE()

    // Is the subsystem ready?
    // Reset value: 0x00000000
    BEGIN_TYPE(DONE_t, uint32_t)
        ADD_BITFIELD_RO(PROC1, 24, 1)
        ADD_BITFIELD_RO(PROC0, 23, 1)
        ADD_BITFIELD_RO(ACCESSCTRL, 22, 1)
        ADD_BITFIELD_RO(SIO, 21, 1)
        ADD_BITFIELD_RO(XIP, 20, 1)
        ADD_BITFIELD_RO(SRAM9, 19, 1)
        ADD_BITFIELD_RO(SRAM8, 18, 1)
        ADD_BITFIELD_RO(SRAM7, 17, 1)
        ADD_BITFIELD_RO(SRAM6, 16, 1)
        ADD_BITFIELD_RO(SRAM5, 15, 1)
        ADD_BITFIELD_RO(SRAM4, 14, 1)
        ADD_BITFIELD_RO(SRAM3, 13, 1)
        ADD_BITFIELD_RO(SRAM2, 12, 1)
        ADD_BITFIELD_RO(SRAM1, 11, 1)
        ADD_BITFIELD_RO(SRAM0, 10, 1)
        ADD_BITFIELD_RO(BOOTRAM, 9, 1)
        ADD_BITFIELD_RO(ROM, 8, 1)
        ADD_BITFIELD_RO(BUSFABRIC, 7, 1)
        ADD_BITFIELD_RO(PSM_READY, 6, 1)
        ADD_BITFIELD_RO(CLOCKS, 5, 1)
        ADD_BITFIELD_RO(RESETS, 4, 1)
        ADD_BITFIELD_RO(XOSC, 3, 1)
        ADD_BITFIELD_RO(ROSC, 2, 1)
        ADD_BITFIELD_RO(OTP, 1, 1)
        ADD_BITFIELD_RO(PROC_COLD, 0, 1)
    END_TYPE()

    struct PSM_t {
        FRCE_ON_t                     FRCE_ON;
        FRCE_OFF_t                    FRCE_OFF;
        WDSEL_t                       WDSEL;
        DONE_t                        DONE;
    };

    static PSM_t & PSM = (*(PSM_t *)0x40018000);
    static PSM_t & PSM_XOR = (*(PSM_t *)0x40019000);
    static PSM_t & PSM_SET = (*(PSM_t *)0x4001a000);
    static PSM_t & PSM_CLR = (*(PSM_t *)0x4001b000);

} // _PSM_

namespace _CLOCKS_  {

    // Clock control, can be changed on-the-fly (except for auxsrc)
    // Reset value: 0x00000000
    BEGIN_TYPE(CLK_GPOUT_CTRL_t, uint32_t)
        // clock generator is enabled
        ADD_BITFIELD_RO(ENABLED, 28, 1)
        // An edge on this signal shifts the phase of the output by 1 cycle of the input clock  This can be done at any time
        ADD_BITFIELD_RW(NUDGE, 20, 1)
        // This delays the enable signal by up to 3 cycles of the input clock  This must be set before the clock is enabled to have any effect
        ADD_BITFIELD_RW(PHASE, 16, 2)
        // Enables duty cycle correction for odd divisors, can be changed on-the-fly
        ADD_BITFIELD_RW(DC50, 12, 1)
        // Starts and stops the clock generator cleanly
        ADD_BITFIELD_RW(ENABLE, 11, 1)
        // Asynchronously kills the clock generator, enable must be set low before deasserting kill
        ADD_BITFIELD_RW(KILL, 10, 1)
        // Selects the auxiliary clock source, will glitch when switching
        ADD_BITFIELD_RW(AUXSRC, 5, 4)
    END_TYPE()

    static const uint32_t CLK_GPOUT_CTRL_AUXSRC__clksrc_pll_sys = 0;
    static const uint32_t CLK_GPOUT_CTRL_AUXSRC__clksrc_gpin0 = 1;
    static const uint32_t CLK_GPOUT_CTRL_AUXSRC__clksrc_gpin1 = 2;
    static const uint32_t CLK_GPOUT_CTRL_AUXSRC__clksrc_pll_usb = 3;
    static const uint32_t CLK_GPOUT_CTRL_AUXSRC__clksrc_pll_usb_primary_ref_opcg = 4;
    static const uint32_t CLK_GPOUT_CTRL_AUXSRC__rosc_clksrc = 5;
    static const uint32_t CLK_GPOUT_CTRL_AUXSRC__xosc_clksrc = 6;
    static const uint32_t CLK_GPOUT_CTRL_AUXSRC__lposc_clksrc = 7;
    static const uint32_t CLK_GPOUT_CTRL_AUXSRC__clk_sys = 8;
    static const uint32_t CLK_GPOUT_CTRL_AUXSRC__clk_usb = 9;
    static const uint32_t CLK_GPOUT_CTRL_AUXSRC__clk_adc = 10;
    static const uint32_t CLK_GPOUT_CTRL_AUXSRC__clk_ref = 11;
    static const uint32_t CLK_GPOUT_CTRL_AUXSRC__clk_peri = 12;
    static const uint32_t CLK_GPOUT_CTRL_AUXSRC__clk_hstx = 13;
    static const uint32_t CLK_GPOUT_CTRL_AUXSRC__otp_clk2fc = 14;

    // Reset value: 0x00010000
    BEGIN_TYPE(CLK_GPOUT_DIV_t, uint32_t)
        // Integer part of clock divisor, 0 -> max+1, can be changed on-the-fly
        ADD_BITFIELD_RW(INT, 16, 16)
        // Fractional component of the divisor, can be changed on-the-fly
        ADD_BITFIELD_RW(FRAC, 0, 16)
    END_TYPE()

    // Indicates which src is currently selected (one-hot)
    // Reset value: 0x00000001
    BEGIN_TYPE(CLK_GPOUT_SELECTED_t, uint32_t)
        // This slice does not have a glitchless mux (only the AUX_SRC field is present, not SRC) so this register is hardwired to 0x1.
        ADD_BITFIELD_RO(SELECTED, 0, 1)
    END_TYPE()

    // Clock control, can be changed on-the-fly (except for auxsrc)
    // Reset value: 0x00000000
    BEGIN_TYPE(CLK_REF_CTRL_t, uint32_t)
        // Selects the auxiliary clock source, will glitch when switching
        ADD_BITFIELD_RW(AUXSRC, 5, 2)
        // Selects the clock source glitchlessly, can be changed on-the-fly
        ADD_BITFIELD_RW(SRC, 0, 2)
    END_TYPE()

    static const uint32_t CLK_REF_CTRL_AUXSRC__clksrc_pll_usb = 0;
    static const uint32_t CLK_REF_CTRL_AUXSRC__clksrc_gpin0 = 1;
    static const uint32_t CLK_REF_CTRL_AUXSRC__clksrc_gpin1 = 2;
    static const uint32_t CLK_REF_CTRL_AUXSRC__clksrc_pll_usb_primary_ref_opcg = 3;
    static const uint32_t CLK_REF_CTRL_SRC__rosc_clksrc_ph = 0;
    static const uint32_t CLK_REF_CTRL_SRC__clksrc_clk_ref_aux = 1;
    static const uint32_t CLK_REF_CTRL_SRC__xosc_clksrc = 2;
    static const uint32_t CLK_REF_CTRL_SRC__lposc_clksrc = 3;

    // Reset value: 0x00010000
    BEGIN_TYPE(CLK_REF_DIV_t, uint32_t)
        // Integer part of clock divisor, 0 -> max+1, can be changed on-the-fly
        ADD_BITFIELD_RW(INT, 16, 8)
    END_TYPE()

    // Indicates which src is currently selected (one-hot)
    // Reset value: 0x00000001
    BEGIN_TYPE(CLK_REF_SELECTED_t, uint32_t)
        // The glitchless multiplexer does not switch instantaneously (to avoid glitches), so software should poll this register to wait for the switch to complete. This register contains one decoded bit for each of the clock sources enumerated in the CTRL SRC field. At most one of these bits will be set at any time, indicating that clock is currently present at the output of the glitchless mux. Whilst switching is in progress, this register may briefly show all-0s.
        ADD_BITFIELD_RO(CLK_REF_SELECTED, 0, 4)
    END_TYPE()

    // Clock control, can be changed on-the-fly (except for auxsrc)
    // Reset value: 0x00000000
    BEGIN_TYPE(CLK_SYS_CTRL_t, uint32_t)
        // Selects the auxiliary clock source, will glitch when switching
        ADD_BITFIELD_RW(AUXSRC, 5, 3)
        // Selects the clock source glitchlessly, can be changed on-the-fly
        ADD_BITFIELD_RW(SRC, 0, 1)
    END_TYPE()

    static const uint32_t CLK_SYS_CTRL_AUXSRC__clksrc_pll_sys = 0;
    static const uint32_t CLK_SYS_CTRL_AUXSRC__clksrc_pll_usb = 1;
    static const uint32_t CLK_SYS_CTRL_AUXSRC__rosc_clksrc = 2;
    static const uint32_t CLK_SYS_CTRL_AUXSRC__xosc_clksrc = 3;
    static const uint32_t CLK_SYS_CTRL_AUXSRC__clksrc_gpin0 = 4;
    static const uint32_t CLK_SYS_CTRL_AUXSRC__clksrc_gpin1 = 5;
    static const uint32_t CLK_SYS_CTRL_SRC__clk_ref = 0;
    static const uint32_t CLK_SYS_CTRL_SRC__clksrc_clk_sys_aux = 1;

    // Reset value: 0x00010000
    BEGIN_TYPE(CLK_SYS_DIV_t, uint32_t)
        // Integer part of clock divisor, 0 -> max+1, can be changed on-the-fly
        ADD_BITFIELD_RW(INT, 16, 16)
        // Fractional component of the divisor, can be changed on-the-fly
        ADD_BITFIELD_RW(FRAC, 0, 16)
    END_TYPE()

    // Indicates which src is currently selected (one-hot)
    // Reset value: 0x00000001
    BEGIN_TYPE(CLK_SYS_SELECTED_t, uint32_t)
        // The glitchless multiplexer does not switch instantaneously (to avoid glitches), so software should poll this register to wait for the switch to complete. This register contains one decoded bit for each of the clock sources enumerated in the CTRL SRC field. At most one of these bits will be set at any time, indicating that clock is currently present at the output of the glitchless mux. Whilst switching is in progress, this register may briefly show all-0s.
        ADD_BITFIELD_RO(CLK_SYS_SELECTED, 0, 2)
    END_TYPE()

    // Clock control, can be changed on-the-fly (except for auxsrc)
    // Reset value: 0x00000000
    BEGIN_TYPE(CLK_PERI_CTRL_t, uint32_t)
        // clock generator is enabled
        ADD_BITFIELD_RO(ENABLED, 28, 1)
        // Starts and stops the clock generator cleanly
        ADD_BITFIELD_RW(ENABLE, 11, 1)
        // Asynchronously kills the clock generator, enable must be set low before deasserting kill
        ADD_BITFIELD_RW(KILL, 10, 1)
        // Selects the auxiliary clock source, will glitch when switching
        ADD_BITFIELD_RW(AUXSRC, 5, 3)
    END_TYPE()

    static const uint32_t CLK_PERI_CTRL_AUXSRC__clk_sys = 0;
    static const uint32_t CLK_PERI_CTRL_AUXSRC__clksrc_pll_sys = 1;
    static const uint32_t CLK_PERI_CTRL_AUXSRC__clksrc_pll_usb = 2;
    static const uint32_t CLK_PERI_CTRL_AUXSRC__rosc_clksrc_ph = 3;
    static const uint32_t CLK_PERI_CTRL_AUXSRC__xosc_clksrc = 4;
    static const uint32_t CLK_PERI_CTRL_AUXSRC__clksrc_gpin0 = 5;
    static const uint32_t CLK_PERI_CTRL_AUXSRC__clksrc_gpin1 = 6;

    // Reset value: 0x00010000
    BEGIN_TYPE(CLK_PERI_DIV_t, uint32_t)
        // Integer part of clock divisor, 0 -> max+1, can be changed on-the-fly
        ADD_BITFIELD_RW(INT, 16, 2)
    END_TYPE()

    // Indicates which src is currently selected (one-hot)
    // Reset value: 0x00000001
    BEGIN_TYPE(CLK_PERI_SELECTED_t, uint32_t)
        // This slice does not have a glitchless mux (only the AUX_SRC field is present, not SRC) so this register is hardwired to 0x1.
        ADD_BITFIELD_RO(CLK_PERI_SELECTED, 0, 1)
    END_TYPE()

    // Clock control, can be changed on-the-fly (except for auxsrc)
    // Reset value: 0x00000000
    BEGIN_TYPE(CLK_HSTX_CTRL_t, uint32_t)
        // clock generator is enabled
        ADD_BITFIELD_RO(ENABLED, 28, 1)
        // An edge on this signal shifts the phase of the output by 1 cycle of the input clock  This can be done at any time
        ADD_BITFIELD_RW(NUDGE, 20, 1)
        // This delays the enable signal by up to 3 cycles of the input clock  This must be set before the clock is enabled to have any effect
        ADD_BITFIELD_RW(PHASE, 16, 2)
        // Starts and stops the clock generator cleanly
        ADD_BITFIELD_RW(ENABLE, 11, 1)
        // Asynchronously kills the clock generator, enable must be set low before deasserting kill
        ADD_BITFIELD_RW(KILL, 10, 1)
        // Selects the auxiliary clock source, will glitch when switching
        ADD_BITFIELD_RW(AUXSRC, 5, 3)
    END_TYPE()

    static const uint32_t CLK_HSTX_CTRL_AUXSRC__clk_sys = 0;
    static const uint32_t CLK_HSTX_CTRL_AUXSRC__clksrc_pll_sys = 1;
    static const uint32_t CLK_HSTX_CTRL_AUXSRC__clksrc_pll_usb = 2;
    static const uint32_t CLK_HSTX_CTRL_AUXSRC__clksrc_gpin0 = 3;
    static const uint32_t CLK_HSTX_CTRL_AUXSRC__clksrc_gpin1 = 4;

    // Reset value: 0x00010000
    BEGIN_TYPE(CLK_HSTX_DIV_t, uint32_t)
        // Integer part of clock divisor, 0 -> max+1, can be changed on-the-fly
        ADD_BITFIELD_RW(INT, 16, 2)
    END_TYPE()

    // Indicates which src is currently selected (one-hot)
    // Reset value: 0x00000001
    BEGIN_TYPE(CLK_HSTX_SELECTED_t, uint32_t)
        // This slice does not have a glitchless mux (only the AUX_SRC field is present, not SRC) so this register is hardwired to 0x1.
        ADD_BITFIELD_RO(CLK_HSTX_SELECTED, 0, 1)
    END_TYPE()

    // Clock control, can be changed on-the-fly (except for auxsrc)
    // Reset value: 0x00000000
    BEGIN_TYPE(CLK_USB_CTRL_t, uint32_t)
        // clock generator is enabled
        ADD_BITFIELD_RO(ENABLED, 28, 1)
        // An edge on this signal shifts the phase of the output by 1 cycle of the input clock  This can be done at any time
        ADD_BITFIELD_RW(NUDGE, 20, 1)
        // This delays the enable signal by up to 3 cycles of the input clock  This must be set before the clock is enabled to have any effect
        ADD_BITFIELD_RW(PHASE, 16, 2)
        // Starts and stops the clock generator cleanly
        ADD_BITFIELD_RW(ENABLE, 11, 1)
        // Asynchronously kills the clock generator, enable must be set low before deasserting kill
        ADD_BITFIELD_RW(KILL, 10, 1)
        // Selects the auxiliary clock source, will glitch when switching
        ADD_BITFIELD_RW(AUXSRC, 5, 3)
    END_TYPE()

    static const uint32_t CLK_USB_CTRL_AUXSRC__clksrc_pll_usb = 0;
    static const uint32_t CLK_USB_CTRL_AUXSRC__clksrc_pll_sys = 1;
    static const uint32_t CLK_USB_CTRL_AUXSRC__rosc_clksrc_ph = 2;
    static const uint32_t CLK_USB_CTRL_AUXSRC__xosc_clksrc = 3;
    static const uint32_t CLK_USB_CTRL_AUXSRC__clksrc_gpin0 = 4;
    static const uint32_t CLK_USB_CTRL_AUXSRC__clksrc_gpin1 = 5;

    // Reset value: 0x00010000
    BEGIN_TYPE(CLK_USB_DIV_t, uint32_t)
        // Integer part of clock divisor, 0 -> max+1, can be changed on-the-fly
        ADD_BITFIELD_RW(INT, 16, 4)
    END_TYPE()

    // Indicates which src is currently selected (one-hot)
    // Reset value: 0x00000001
    BEGIN_TYPE(CLK_USB_SELECTED_t, uint32_t)
        // This slice does not have a glitchless mux (only the AUX_SRC field is present, not SRC) so this register is hardwired to 0x1.
        ADD_BITFIELD_RO(CLK_USB_SELECTED, 0, 1)
    END_TYPE()

    // Clock control, can be changed on-the-fly (except for auxsrc)
    // Reset value: 0x00000000
    BEGIN_TYPE(CLK_ADC_CTRL_t, uint32_t)
        // clock generator is enabled
        ADD_BITFIELD_RO(ENABLED, 28, 1)
        // An edge on this signal shifts the phase of the output by 1 cycle of the input clock  This can be done at any time
        ADD_BITFIELD_RW(NUDGE, 20, 1)
        // This delays the enable signal by up to 3 cycles of the input clock  This must be set before the clock is enabled to have any effect
        ADD_BITFIELD_RW(PHASE, 16, 2)
        // Starts and stops the clock generator cleanly
        ADD_BITFIELD_RW(ENABLE, 11, 1)
        // Asynchronously kills the clock generator, enable must be set low before deasserting kill
        ADD_BITFIELD_RW(KILL, 10, 1)
        // Selects the auxiliary clock source, will glitch when switching
        ADD_BITFIELD_RW(AUXSRC, 5, 3)
    END_TYPE()

    static const uint32_t CLK_ADC_CTRL_AUXSRC__clksrc_pll_usb = 0;
    static const uint32_t CLK_ADC_CTRL_AUXSRC__clksrc_pll_sys = 1;
    static const uint32_t CLK_ADC_CTRL_AUXSRC__rosc_clksrc_ph = 2;
    static const uint32_t CLK_ADC_CTRL_AUXSRC__xosc_clksrc = 3;
    static const uint32_t CLK_ADC_CTRL_AUXSRC__clksrc_gpin0 = 4;
    static const uint32_t CLK_ADC_CTRL_AUXSRC__clksrc_gpin1 = 5;

    // Reset value: 0x00010000
    BEGIN_TYPE(CLK_ADC_DIV_t, uint32_t)
        // Integer part of clock divisor, 0 -> max+1, can be changed on-the-fly
        ADD_BITFIELD_RW(INT, 16, 4)
    END_TYPE()

    // Indicates which src is currently selected (one-hot)
    // Reset value: 0x00000001
    BEGIN_TYPE(CLK_ADC_SELECTED_t, uint32_t)
        // This slice does not have a glitchless mux (only the AUX_SRC field is present, not SRC) so this register is hardwired to 0x1.
        ADD_BITFIELD_RO(CLK_ADC_SELECTED, 0, 1)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(DFTCLK_XOSC_CTRL_t, uint32_t)
        ADD_BITFIELD_RW(SRC, 0, 2)
    END_TYPE()

    static const uint32_t DFTCLK_XOSC_CTRL_SRC__NULL = 0;
    static const uint32_t DFTCLK_XOSC_CTRL_SRC__clksrc_pll_usb_primary = 1;
    static const uint32_t DFTCLK_XOSC_CTRL_SRC__clksrc_gpin0 = 2;

    // Reset value: 0x00000000
    BEGIN_TYPE(DFTCLK_ROSC_CTRL_t, uint32_t)
        ADD_BITFIELD_RW(SRC, 0, 2)
    END_TYPE()

    static const uint32_t DFTCLK_ROSC_CTRL_SRC__NULL = 0;
    static const uint32_t DFTCLK_ROSC_CTRL_SRC__clksrc_pll_sys_primary_rosc = 1;
    static const uint32_t DFTCLK_ROSC_CTRL_SRC__clksrc_gpin1 = 2;

    // Reset value: 0x00000000
    BEGIN_TYPE(DFTCLK_LPOSC_CTRL_t, uint32_t)
        ADD_BITFIELD_RW(SRC, 0, 2)
    END_TYPE()

    static const uint32_t DFTCLK_LPOSC_CTRL_SRC__NULL = 0;
    static const uint32_t DFTCLK_LPOSC_CTRL_SRC__clksrc_pll_usb_primary_lposc = 1;
    static const uint32_t DFTCLK_LPOSC_CTRL_SRC__clksrc_gpin1 = 2;

    // Reset value: 0x000000ff
    BEGIN_TYPE(CLK_SYS_RESUS_CTRL_t, uint32_t)
        // For clearing the resus after the fault that triggered it has been corrected
        ADD_BITFIELD_RW(CLEAR, 16, 1)
        // Force a resus, for test purposes only
        ADD_BITFIELD_RW(FRCE, 12, 1)
        // Enable resus
        ADD_BITFIELD_RW(ENABLE, 8, 1)
        // This is expressed as a number of clk_ref cycles  and must be >= 2x clk_ref_freq/min_clk_tst_freq
        ADD_BITFIELD_RW(TIMEOUT, 0, 8)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(CLK_SYS_RESUS_STATUS_t, uint32_t)
        // Clock has been resuscitated, correct the error then send ctrl_clear=1
        ADD_BITFIELD_RO(RESUSSED, 0, 1)
    END_TYPE()

    // Reference clock frequency in kHz
    // Reset value: 0x00000000
    BEGIN_TYPE(FC0_REF_KHZ_t, uint32_t)
        ADD_BITFIELD_RW(FC0_REF_KHZ, 0, 20)
    END_TYPE()

    // Minimum pass frequency in kHz. This is optional. Set to 0 if you are not using the pass/fail flags
    // Reset value: 0x00000000
    BEGIN_TYPE(FC0_MIN_KHZ_t, uint32_t)
        ADD_BITFIELD_RW(FC0_MIN_KHZ, 0, 25)
    END_TYPE()

    // Maximum pass frequency in kHz. This is optional. Set to 0x1ffffff if you are not using the pass/fail flags
    // Reset value: 0x01ffffff
    BEGIN_TYPE(FC0_MAX_KHZ_t, uint32_t)
        ADD_BITFIELD_RW(FC0_MAX_KHZ, 0, 25)
    END_TYPE()

    // Delays the start of frequency counting to allow the mux to settle  Delay is measured in multiples of the reference clock period
    // Reset value: 0x00000001
    BEGIN_TYPE(FC0_DELAY_t, uint32_t)
        ADD_BITFIELD_RW(FC0_DELAY, 0, 3)
    END_TYPE()

    // The test interval is 0.98us * 2**interval, but let's call it 1us * 2**interval  The default gives a test interval of 250us
    // Reset value: 0x00000008
    BEGIN_TYPE(FC0_INTERVAL_t, uint32_t)
        ADD_BITFIELD_RW(FC0_INTERVAL, 0, 4)
    END_TYPE()

    // Clock sent to frequency counter, set to 0 when not required  Writing to this register initiates the frequency count
    // Reset value: 0x00000000
    BEGIN_TYPE(FC0_SRC_t, uint32_t)
        ADD_BITFIELD_RW(FC0_SRC, 0, 8)
    END_TYPE()

    static const uint32_t FC0_SRC_FC0_SRC__NULL = 0;
    static const uint32_t FC0_SRC_FC0_SRC__pll_sys_clksrc_primary = 1;
    static const uint32_t FC0_SRC_FC0_SRC__pll_usb_clksrc_primary = 2;
    static const uint32_t FC0_SRC_FC0_SRC__rosc_clksrc = 3;
    static const uint32_t FC0_SRC_FC0_SRC__rosc_clksrc_ph = 4;
    static const uint32_t FC0_SRC_FC0_SRC__xosc_clksrc = 5;
    static const uint32_t FC0_SRC_FC0_SRC__clksrc_gpin0 = 6;
    static const uint32_t FC0_SRC_FC0_SRC__clksrc_gpin1 = 7;
    static const uint32_t FC0_SRC_FC0_SRC__clk_ref = 8;
    static const uint32_t FC0_SRC_FC0_SRC__clk_sys = 9;
    static const uint32_t FC0_SRC_FC0_SRC__clk_peri = 10;
    static const uint32_t FC0_SRC_FC0_SRC__clk_usb = 11;
    static const uint32_t FC0_SRC_FC0_SRC__clk_adc = 12;
    static const uint32_t FC0_SRC_FC0_SRC__clk_hstx = 13;
    static const uint32_t FC0_SRC_FC0_SRC__lposc_clksrc = 14;
    static const uint32_t FC0_SRC_FC0_SRC__otp_clk2fc = 15;
    static const uint32_t FC0_SRC_FC0_SRC__pll_usb_clksrc_primary_dft = 16;

    // Frequency counter status
    // Reset value: 0x00000000
    BEGIN_TYPE(FC0_STATUS_t, uint32_t)
        // Test clock stopped during test
        ADD_BITFIELD_RO(DIED, 28, 1)
        // Test clock faster than expected, only valid when status_done=1
        ADD_BITFIELD_RO(FAST, 24, 1)
        // Test clock slower than expected, only valid when status_done=1
        ADD_BITFIELD_RO(SLOW, 20, 1)
        // Test failed
        ADD_BITFIELD_RO(FAIL, 16, 1)
        // Waiting for test clock to start
        ADD_BITFIELD_RO(WAITING, 12, 1)
        // Test running
        ADD_BITFIELD_RO(RUNNING, 8, 1)
        // Test complete
        ADD_BITFIELD_RO(DONE, 4, 1)
        // Test passed
        ADD_BITFIELD_RO(PASS, 0, 1)
    END_TYPE()

    // Result of frequency measurement, only valid when status_done=1
    // Reset value: 0x00000000
    BEGIN_TYPE(FC0_RESULT_t, uint32_t)
        ADD_BITFIELD_RO(KHZ, 5, 25)
        ADD_BITFIELD_RO(FRAC, 0, 5)
    END_TYPE()

    // enable clock in wake mode
    // Reset value: 0xffffffff
    BEGIN_TYPE(WAKE_EN0_t, uint32_t)
        ADD_BITFIELD_RW(CLK_SYS_SIO, 31, 1)
        ADD_BITFIELD_RW(CLK_SYS_SHA256, 30, 1)
        ADD_BITFIELD_RW(CLK_SYS_PSM, 29, 1)
        ADD_BITFIELD_RW(CLK_SYS_ROSC, 28, 1)
        ADD_BITFIELD_RW(CLK_SYS_ROM, 27, 1)
        ADD_BITFIELD_RW(CLK_SYS_RESETS, 26, 1)
        ADD_BITFIELD_RW(CLK_SYS_PWM, 25, 1)
        ADD_BITFIELD_RW(CLK_SYS_POWMAN, 24, 1)
        ADD_BITFIELD_RW(CLK_REF_POWMAN, 23, 1)
        ADD_BITFIELD_RW(CLK_SYS_PLL_USB, 22, 1)
        ADD_BITFIELD_RW(CLK_SYS_PLL_SYS, 21, 1)
        ADD_BITFIELD_RW(CLK_SYS_PIO2, 20, 1)
        ADD_BITFIELD_RW(CLK_SYS_PIO1, 19, 1)
        ADD_BITFIELD_RW(CLK_SYS_PIO0, 18, 1)
        ADD_BITFIELD_RW(CLK_SYS_PADS, 17, 1)
        ADD_BITFIELD_RW(CLK_SYS_OTP, 16, 1)
        ADD_BITFIELD_RW(CLK_REF_OTP, 15, 1)
        ADD_BITFIELD_RW(CLK_SYS_JTAG, 14, 1)
        ADD_BITFIELD_RW(CLK_SYS_IO, 13, 1)
        ADD_BITFIELD_RW(CLK_SYS_I2C1, 12, 1)
        ADD_BITFIELD_RW(CLK_SYS_I2C0, 11, 1)
        ADD_BITFIELD_RW(CLK_SYS_HSTX, 10, 1)
        ADD_BITFIELD_RW(CLK_HSTX, 9, 1)
        ADD_BITFIELD_RW(CLK_SYS_GLITCH_DETECTOR, 8, 1)
        ADD_BITFIELD_RW(CLK_SYS_DMA, 7, 1)
        ADD_BITFIELD_RW(CLK_SYS_BUSFABRIC, 6, 1)
        ADD_BITFIELD_RW(CLK_SYS_BUSCTRL, 5, 1)
        ADD_BITFIELD_RW(CLK_SYS_BOOTRAM, 4, 1)
        ADD_BITFIELD_RW(CLK_SYS_ADC, 3, 1)
        ADD_BITFIELD_RW(CLK_ADC, 2, 1)
        ADD_BITFIELD_RW(CLK_SYS_ACCESSCTRL, 1, 1)
        ADD_BITFIELD_RW(CLK_SYS_CLOCKS, 0, 1)
    END_TYPE()

    // enable clock in wake mode
    // Reset value: 0x7fffffff
    BEGIN_TYPE(WAKE_EN1_t, uint32_t)
        ADD_BITFIELD_RW(CLK_SYS_XOSC, 30, 1)
        ADD_BITFIELD_RW(CLK_SYS_XIP, 29, 1)
        ADD_BITFIELD_RW(CLK_SYS_WATCHDOG, 28, 1)
        ADD_BITFIELD_RW(CLK_USB, 27, 1)
        ADD_BITFIELD_RW(CLK_SYS_USBCTRL, 26, 1)
        ADD_BITFIELD_RW(CLK_SYS_UART1, 25, 1)
        ADD_BITFIELD_RW(CLK_PERI_UART1, 24, 1)
        ADD_BITFIELD_RW(CLK_SYS_UART0, 23, 1)
        ADD_BITFIELD_RW(CLK_PERI_UART0, 22, 1)
        ADD_BITFIELD_RW(CLK_SYS_TRNG, 21, 1)
        ADD_BITFIELD_RW(CLK_SYS_TIMER1, 20, 1)
        ADD_BITFIELD_RW(CLK_SYS_TIMER0, 19, 1)
        ADD_BITFIELD_RW(CLK_SYS_TICKS, 18, 1)
        ADD_BITFIELD_RW(CLK_REF_TICKS, 17, 1)
        ADD_BITFIELD_RW(CLK_SYS_TBMAN, 16, 1)
        ADD_BITFIELD_RW(CLK_SYS_SYSINFO, 15, 1)
        ADD_BITFIELD_RW(CLK_SYS_SYSCFG, 14, 1)
        ADD_BITFIELD_RW(CLK_SYS_SRAM9, 13, 1)
        ADD_BITFIELD_RW(CLK_SYS_SRAM8, 12, 1)
        ADD_BITFIELD_RW(CLK_SYS_SRAM7, 11, 1)
        ADD_BITFIELD_RW(CLK_SYS_SRAM6, 10, 1)
        ADD_BITFIELD_RW(CLK_SYS_SRAM5, 9, 1)
        ADD_BITFIELD_RW(CLK_SYS_SRAM4, 8, 1)
        ADD_BITFIELD_RW(CLK_SYS_SRAM3, 7, 1)
        ADD_BITFIELD_RW(CLK_SYS_SRAM2, 6, 1)
        ADD_BITFIELD_RW(CLK_SYS_SRAM1, 5, 1)
        ADD_BITFIELD_RW(CLK_SYS_SRAM0, 4, 1)
        ADD_BITFIELD_RW(CLK_SYS_SPI1, 3, 1)
        ADD_BITFIELD_RW(CLK_PERI_SPI1, 2, 1)
        ADD_BITFIELD_RW(CLK_SYS_SPI0, 1, 1)
        ADD_BITFIELD_RW(CLK_PERI_SPI0, 0, 1)
    END_TYPE()

    // enable clock in sleep mode
    // Reset value: 0xffffffff
    BEGIN_TYPE(SLEEP_EN0_t, uint32_t)
        ADD_BITFIELD_RW(CLK_SYS_SIO, 31, 1)
        ADD_BITFIELD_RW(CLK_SYS_SHA256, 30, 1)
        ADD_BITFIELD_RW(CLK_SYS_PSM, 29, 1)
        ADD_BITFIELD_RW(CLK_SYS_ROSC, 28, 1)
        ADD_BITFIELD_RW(CLK_SYS_ROM, 27, 1)
        ADD_BITFIELD_RW(CLK_SYS_RESETS, 26, 1)
        ADD_BITFIELD_RW(CLK_SYS_PWM, 25, 1)
        ADD_BITFIELD_RW(CLK_SYS_POWMAN, 24, 1)
        ADD_BITFIELD_RW(CLK_REF_POWMAN, 23, 1)
        ADD_BITFIELD_RW(CLK_SYS_PLL_USB, 22, 1)
        ADD_BITFIELD_RW(CLK_SYS_PLL_SYS, 21, 1)
        ADD_BITFIELD_RW(CLK_SYS_PIO2, 20, 1)
        ADD_BITFIELD_RW(CLK_SYS_PIO1, 19, 1)
        ADD_BITFIELD_RW(CLK_SYS_PIO0, 18, 1)
        ADD_BITFIELD_RW(CLK_SYS_PADS, 17, 1)
        ADD_BITFIELD_RW(CLK_SYS_OTP, 16, 1)
        ADD_BITFIELD_RW(CLK_REF_OTP, 15, 1)
        ADD_BITFIELD_RW(CLK_SYS_JTAG, 14, 1)
        ADD_BITFIELD_RW(CLK_SYS_IO, 13, 1)
        ADD_BITFIELD_RW(CLK_SYS_I2C1, 12, 1)
        ADD_BITFIELD_RW(CLK_SYS_I2C0, 11, 1)
        ADD_BITFIELD_RW(CLK_SYS_HSTX, 10, 1)
        ADD_BITFIELD_RW(CLK_HSTX, 9, 1)
        ADD_BITFIELD_RW(CLK_SYS_GLITCH_DETECTOR, 8, 1)
        ADD_BITFIELD_RW(CLK_SYS_DMA, 7, 1)
        ADD_BITFIELD_RW(CLK_SYS_BUSFABRIC, 6, 1)
        ADD_BITFIELD_RW(CLK_SYS_BUSCTRL, 5, 1)
        ADD_BITFIELD_RW(CLK_SYS_BOOTRAM, 4, 1)
        ADD_BITFIELD_RW(CLK_SYS_ADC, 3, 1)
        ADD_BITFIELD_RW(CLK_ADC, 2, 1)
        ADD_BITFIELD_RW(CLK_SYS_ACCESSCTRL, 1, 1)
        ADD_BITFIELD_RW(CLK_SYS_CLOCKS, 0, 1)
    END_TYPE()

    // enable clock in sleep mode
    // Reset value: 0x7fffffff
    BEGIN_TYPE(SLEEP_EN1_t, uint32_t)
        ADD_BITFIELD_RW(CLK_SYS_XOSC, 30, 1)
        ADD_BITFIELD_RW(CLK_SYS_XIP, 29, 1)
        ADD_BITFIELD_RW(CLK_SYS_WATCHDOG, 28, 1)
        ADD_BITFIELD_RW(CLK_USB, 27, 1)
        ADD_BITFIELD_RW(CLK_SYS_USBCTRL, 26, 1)
        ADD_BITFIELD_RW(CLK_SYS_UART1, 25, 1)
        ADD_BITFIELD_RW(CLK_PERI_UART1, 24, 1)
        ADD_BITFIELD_RW(CLK_SYS_UART0, 23, 1)
        ADD_BITFIELD_RW(CLK_PERI_UART0, 22, 1)
        ADD_BITFIELD_RW(CLK_SYS_TRNG, 21, 1)
        ADD_BITFIELD_RW(CLK_SYS_TIMER1, 20, 1)
        ADD_BITFIELD_RW(CLK_SYS_TIMER0, 19, 1)
        ADD_BITFIELD_RW(CLK_SYS_TICKS, 18, 1)
        ADD_BITFIELD_RW(CLK_REF_TICKS, 17, 1)
        ADD_BITFIELD_RW(CLK_SYS_TBMAN, 16, 1)
        ADD_BITFIELD_RW(CLK_SYS_SYSINFO, 15, 1)
        ADD_BITFIELD_RW(CLK_SYS_SYSCFG, 14, 1)
        ADD_BITFIELD_RW(CLK_SYS_SRAM9, 13, 1)
        ADD_BITFIELD_RW(CLK_SYS_SRAM8, 12, 1)
        ADD_BITFIELD_RW(CLK_SYS_SRAM7, 11, 1)
        ADD_BITFIELD_RW(CLK_SYS_SRAM6, 10, 1)
        ADD_BITFIELD_RW(CLK_SYS_SRAM5, 9, 1)
        ADD_BITFIELD_RW(CLK_SYS_SRAM4, 8, 1)
        ADD_BITFIELD_RW(CLK_SYS_SRAM3, 7, 1)
        ADD_BITFIELD_RW(CLK_SYS_SRAM2, 6, 1)
        ADD_BITFIELD_RW(CLK_SYS_SRAM1, 5, 1)
        ADD_BITFIELD_RW(CLK_SYS_SRAM0, 4, 1)
        ADD_BITFIELD_RW(CLK_SYS_SPI1, 3, 1)
        ADD_BITFIELD_RW(CLK_PERI_SPI1, 2, 1)
        ADD_BITFIELD_RW(CLK_SYS_SPI0, 1, 1)
        ADD_BITFIELD_RW(CLK_PERI_SPI0, 0, 1)
    END_TYPE()

    // indicates the state of the clock enable
    // Reset value: 0x00000000
    BEGIN_TYPE(ENABLED0_t, uint32_t)
        ADD_BITFIELD_RO(CLK_SYS_SIO, 31, 1)
        ADD_BITFIELD_RO(CLK_SYS_SHA256, 30, 1)
        ADD_BITFIELD_RO(CLK_SYS_PSM, 29, 1)
        ADD_BITFIELD_RO(CLK_SYS_ROSC, 28, 1)
        ADD_BITFIELD_RO(CLK_SYS_ROM, 27, 1)
        ADD_BITFIELD_RO(CLK_SYS_RESETS, 26, 1)
        ADD_BITFIELD_RO(CLK_SYS_PWM, 25, 1)
        ADD_BITFIELD_RO(CLK_SYS_POWMAN, 24, 1)
        ADD_BITFIELD_RO(CLK_REF_POWMAN, 23, 1)
        ADD_BITFIELD_RO(CLK_SYS_PLL_USB, 22, 1)
        ADD_BITFIELD_RO(CLK_SYS_PLL_SYS, 21, 1)
        ADD_BITFIELD_RO(CLK_SYS_PIO2, 20, 1)
        ADD_BITFIELD_RO(CLK_SYS_PIO1, 19, 1)
        ADD_BITFIELD_RO(CLK_SYS_PIO0, 18, 1)
        ADD_BITFIELD_RO(CLK_SYS_PADS, 17, 1)
        ADD_BITFIELD_RO(CLK_SYS_OTP, 16, 1)
        ADD_BITFIELD_RO(CLK_REF_OTP, 15, 1)
        ADD_BITFIELD_RO(CLK_SYS_JTAG, 14, 1)
        ADD_BITFIELD_RO(CLK_SYS_IO, 13, 1)
        ADD_BITFIELD_RO(CLK_SYS_I2C1, 12, 1)
        ADD_BITFIELD_RO(CLK_SYS_I2C0, 11, 1)
        ADD_BITFIELD_RO(CLK_SYS_HSTX, 10, 1)
        ADD_BITFIELD_RO(CLK_HSTX, 9, 1)
        ADD_BITFIELD_RO(CLK_SYS_GLITCH_DETECTOR, 8, 1)
        ADD_BITFIELD_RO(CLK_SYS_DMA, 7, 1)
        ADD_BITFIELD_RO(CLK_SYS_BUSFABRIC, 6, 1)
        ADD_BITFIELD_RO(CLK_SYS_BUSCTRL, 5, 1)
        ADD_BITFIELD_RO(CLK_SYS_BOOTRAM, 4, 1)
        ADD_BITFIELD_RO(CLK_SYS_ADC, 3, 1)
        ADD_BITFIELD_RO(CLK_ADC, 2, 1)
        ADD_BITFIELD_RO(CLK_SYS_ACCESSCTRL, 1, 1)
        ADD_BITFIELD_RO(CLK_SYS_CLOCKS, 0, 1)
    END_TYPE()

    // indicates the state of the clock enable
    // Reset value: 0x00000000
    BEGIN_TYPE(ENABLED1_t, uint32_t)
        ADD_BITFIELD_RO(CLK_SYS_XOSC, 30, 1)
        ADD_BITFIELD_RO(CLK_SYS_XIP, 29, 1)
        ADD_BITFIELD_RO(CLK_SYS_WATCHDOG, 28, 1)
        ADD_BITFIELD_RO(CLK_USB, 27, 1)
        ADD_BITFIELD_RO(CLK_SYS_USBCTRL, 26, 1)
        ADD_BITFIELD_RO(CLK_SYS_UART1, 25, 1)
        ADD_BITFIELD_RO(CLK_PERI_UART1, 24, 1)
        ADD_BITFIELD_RO(CLK_SYS_UART0, 23, 1)
        ADD_BITFIELD_RO(CLK_PERI_UART0, 22, 1)
        ADD_BITFIELD_RO(CLK_SYS_TRNG, 21, 1)
        ADD_BITFIELD_RO(CLK_SYS_TIMER1, 20, 1)
        ADD_BITFIELD_RO(CLK_SYS_TIMER0, 19, 1)
        ADD_BITFIELD_RO(CLK_SYS_TICKS, 18, 1)
        ADD_BITFIELD_RO(CLK_REF_TICKS, 17, 1)
        ADD_BITFIELD_RO(CLK_SYS_TBMAN, 16, 1)
        ADD_BITFIELD_RO(CLK_SYS_SYSINFO, 15, 1)
        ADD_BITFIELD_RO(CLK_SYS_SYSCFG, 14, 1)
        ADD_BITFIELD_RO(CLK_SYS_SRAM9, 13, 1)
        ADD_BITFIELD_RO(CLK_SYS_SRAM8, 12, 1)
        ADD_BITFIELD_RO(CLK_SYS_SRAM7, 11, 1)
        ADD_BITFIELD_RO(CLK_SYS_SRAM6, 10, 1)
        ADD_BITFIELD_RO(CLK_SYS_SRAM5, 9, 1)
        ADD_BITFIELD_RO(CLK_SYS_SRAM4, 8, 1)
        ADD_BITFIELD_RO(CLK_SYS_SRAM3, 7, 1)
        ADD_BITFIELD_RO(CLK_SYS_SRAM2, 6, 1)
        ADD_BITFIELD_RO(CLK_SYS_SRAM1, 5, 1)
        ADD_BITFIELD_RO(CLK_SYS_SRAM0, 4, 1)
        ADD_BITFIELD_RO(CLK_SYS_SPI1, 3, 1)
        ADD_BITFIELD_RO(CLK_PERI_SPI1, 2, 1)
        ADD_BITFIELD_RO(CLK_SYS_SPI0, 1, 1)
        ADD_BITFIELD_RO(CLK_PERI_SPI0, 0, 1)
    END_TYPE()

    // Raw Interrupts
    // Reset value: 0x00000000
    BEGIN_TYPE(INTR_t, uint32_t)
        ADD_BITFIELD_RO(CLK_SYS_RESUS, 0, 1)
    END_TYPE()

    // Interrupt Enable
    // Reset value: 0x00000000
    BEGIN_TYPE(INTE_t, uint32_t)
        ADD_BITFIELD_RW(CLK_SYS_RESUS, 0, 1)
    END_TYPE()

    // Interrupt Force
    // Reset value: 0x00000000
    BEGIN_TYPE(INTF_t, uint32_t)
        ADD_BITFIELD_RW(CLK_SYS_RESUS, 0, 1)
    END_TYPE()

    // Interrupt status after masking & forcing
    // Reset value: 0x00000000
    BEGIN_TYPE(INTS_t, uint32_t)
        ADD_BITFIELD_RO(CLK_SYS_RESUS, 0, 1)
    END_TYPE()

    struct CLOCKS_t {
        CLK_GPOUT_CTRL_t              CLK_GPOUT0_CTRL;
        CLK_GPOUT_DIV_t               CLK_GPOUT0_DIV;
        CLK_GPOUT_SELECTED_t          CLK_GPOUT0_SELECTED;
        CLK_GPOUT_CTRL_t              CLK_GPOUT1_CTRL;
        CLK_GPOUT_DIV_t               CLK_GPOUT1_DIV;
        CLK_GPOUT_SELECTED_t          CLK_GPOUT1_SELECTED;
        CLK_GPOUT_CTRL_t              CLK_GPOUT2_CTRL;
        CLK_GPOUT_DIV_t               CLK_GPOUT2_DIV;
        CLK_GPOUT_SELECTED_t          CLK_GPOUT2_SELECTED;
        CLK_GPOUT_CTRL_t              CLK_GPOUT3_CTRL;
        CLK_GPOUT_DIV_t               CLK_GPOUT3_DIV;
        CLK_GPOUT_SELECTED_t          CLK_GPOUT3_SELECTED;
        CLK_REF_CTRL_t                CLK_REF_CTRL;
        CLK_REF_DIV_t                 CLK_REF_DIV;
        CLK_REF_SELECTED_t            CLK_REF_SELECTED;
        CLK_SYS_CTRL_t                CLK_SYS_CTRL;
        CLK_SYS_DIV_t                 CLK_SYS_DIV;
        CLK_SYS_SELECTED_t            CLK_SYS_SELECTED;
        CLK_PERI_CTRL_t               CLK_PERI_CTRL;
        CLK_PERI_DIV_t                CLK_PERI_DIV;
        CLK_PERI_SELECTED_t           CLK_PERI_SELECTED;
        CLK_HSTX_CTRL_t               CLK_HSTX_CTRL;
        CLK_HSTX_DIV_t                CLK_HSTX_DIV;
        CLK_HSTX_SELECTED_t           CLK_HSTX_SELECTED;
        CLK_USB_CTRL_t                CLK_USB_CTRL;
        CLK_USB_DIV_t                 CLK_USB_DIV;
        CLK_USB_SELECTED_t            CLK_USB_SELECTED;
        CLK_ADC_CTRL_t                CLK_ADC_CTRL;
        CLK_ADC_DIV_t                 CLK_ADC_DIV;
        CLK_ADC_SELECTED_t            CLK_ADC_SELECTED;
        DFTCLK_XOSC_CTRL_t            DFTCLK_XOSC_CTRL;
        DFTCLK_ROSC_CTRL_t            DFTCLK_ROSC_CTRL;
        DFTCLK_LPOSC_CTRL_t           DFTCLK_LPOSC_CTRL;
        CLK_SYS_RESUS_CTRL_t          CLK_SYS_RESUS_CTRL;
        CLK_SYS_RESUS_STATUS_t        CLK_SYS_RESUS_STATUS;
        FC0_REF_KHZ_t                 FC0_REF_KHZ;
        FC0_MIN_KHZ_t                 FC0_MIN_KHZ;
        FC0_MAX_KHZ_t                 FC0_MAX_KHZ;
        FC0_DELAY_t                   FC0_DELAY;
        FC0_INTERVAL_t                FC0_INTERVAL;
        FC0_SRC_t                     FC0_SRC;
        FC0_STATUS_t                  FC0_STATUS;
        FC0_RESULT_t                  FC0_RESULT;
        WAKE_EN0_t                    WAKE_EN0;
        WAKE_EN1_t                    WAKE_EN1;
        SLEEP_EN0_t                   SLEEP_EN0;
        SLEEP_EN1_t                   SLEEP_EN1;
        ENABLED0_t                    ENABLED0;
        ENABLED1_t                    ENABLED1;
        INTR_t                        INTR;
        INTE_t                        INTE;
        INTF_t                        INTF;
        INTS_t                        INTS;
    };

    static CLOCKS_t & CLOCKS = (*(CLOCKS_t *)0x40010000);
    static CLOCKS_t & CLOCKS_XOR = (*(CLOCKS_t *)0x40011000);
    static CLOCKS_t & CLOCKS_SET = (*(CLOCKS_t *)0x40012000);
    static CLOCKS_t & CLOCKS_CLR = (*(CLOCKS_t *)0x40013000);

} // _CLOCKS_

namespace _TICKS_  {

    // Controls the tick generator
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC0_CTRL_t, uint32_t)
        // Is the tick generator running?
        ADD_BITFIELD_RO(RUNNING, 1, 1)
        // start / stop tick generation
        ADD_BITFIELD_RW(ENABLE, 0, 1)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(PROC0_CYCLES_t, uint32_t)
        // Total number of clk_tick cycles before the next tick.
        ADD_BITFIELD_RW(PROC0_CYCLES, 0, 9)
    END_TYPE()

    BEGIN_TYPE(PROC0_COUNT_t, uint32_t)
        // Count down timer: the remaining number clk_tick cycles before the next tick is generated.
        ADD_BITFIELD_RO(PROC0_COUNT, 0, 9)
    END_TYPE()

    // Controls the tick generator
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC1_CTRL_t, uint32_t)
        // Is the tick generator running?
        ADD_BITFIELD_RO(RUNNING, 1, 1)
        // start / stop tick generation
        ADD_BITFIELD_RW(ENABLE, 0, 1)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(PROC1_CYCLES_t, uint32_t)
        // Total number of clk_tick cycles before the next tick.
        ADD_BITFIELD_RW(PROC1_CYCLES, 0, 9)
    END_TYPE()

    BEGIN_TYPE(PROC1_COUNT_t, uint32_t)
        // Count down timer: the remaining number clk_tick cycles before the next tick is generated.
        ADD_BITFIELD_RO(PROC1_COUNT, 0, 9)
    END_TYPE()

    // Controls the tick generator
    // Reset value: 0x00000000
    BEGIN_TYPE(TIMER0_CTRL_t, uint32_t)
        // Is the tick generator running?
        ADD_BITFIELD_RO(RUNNING, 1, 1)
        // start / stop tick generation
        ADD_BITFIELD_RW(ENABLE, 0, 1)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(TIMER0_CYCLES_t, uint32_t)
        // Total number of clk_tick cycles before the next tick.
        ADD_BITFIELD_RW(TIMER0_CYCLES, 0, 9)
    END_TYPE()

    BEGIN_TYPE(TIMER0_COUNT_t, uint32_t)
        // Count down timer: the remaining number clk_tick cycles before the next tick is generated.
        ADD_BITFIELD_RO(TIMER0_COUNT, 0, 9)
    END_TYPE()

    // Controls the tick generator
    // Reset value: 0x00000000
    BEGIN_TYPE(TIMER1_CTRL_t, uint32_t)
        // Is the tick generator running?
        ADD_BITFIELD_RO(RUNNING, 1, 1)
        // start / stop tick generation
        ADD_BITFIELD_RW(ENABLE, 0, 1)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(TIMER1_CYCLES_t, uint32_t)
        // Total number of clk_tick cycles before the next tick.
        ADD_BITFIELD_RW(TIMER1_CYCLES, 0, 9)
    END_TYPE()

    BEGIN_TYPE(TIMER1_COUNT_t, uint32_t)
        // Count down timer: the remaining number clk_tick cycles before the next tick is generated.
        ADD_BITFIELD_RO(TIMER1_COUNT, 0, 9)
    END_TYPE()

    // Controls the tick generator
    // Reset value: 0x00000000
    BEGIN_TYPE(WATCHDOG_CTRL_t, uint32_t)
        // Is the tick generator running?
        ADD_BITFIELD_RO(RUNNING, 1, 1)
        // start / stop tick generation
        ADD_BITFIELD_RW(ENABLE, 0, 1)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(WATCHDOG_CYCLES_t, uint32_t)
        // Total number of clk_tick cycles before the next tick.
        ADD_BITFIELD_RW(WATCHDOG_CYCLES, 0, 9)
    END_TYPE()

    BEGIN_TYPE(WATCHDOG_COUNT_t, uint32_t)
        // Count down timer: the remaining number clk_tick cycles before the next tick is generated.
        ADD_BITFIELD_RO(WATCHDOG_COUNT, 0, 9)
    END_TYPE()

    // Controls the tick generator
    // Reset value: 0x00000000
    BEGIN_TYPE(RISCV_CTRL_t, uint32_t)
        // Is the tick generator running?
        ADD_BITFIELD_RO(RUNNING, 1, 1)
        // start / stop tick generation
        ADD_BITFIELD_RW(ENABLE, 0, 1)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(RISCV_CYCLES_t, uint32_t)
        // Total number of clk_tick cycles before the next tick.
        ADD_BITFIELD_RW(RISCV_CYCLES, 0, 9)
    END_TYPE()

    BEGIN_TYPE(RISCV_COUNT_t, uint32_t)
        // Count down timer: the remaining number clk_tick cycles before the next tick is generated.
        ADD_BITFIELD_RO(RISCV_COUNT, 0, 9)
    END_TYPE()

    struct TICKS_t {
        PROC0_CTRL_t                  PROC0_CTRL;
        PROC0_CYCLES_t                PROC0_CYCLES;
        PROC0_COUNT_t                 PROC0_COUNT;
        PROC1_CTRL_t                  PROC1_CTRL;
        PROC1_CYCLES_t                PROC1_CYCLES;
        PROC1_COUNT_t                 PROC1_COUNT;
        TIMER0_CTRL_t                 TIMER0_CTRL;
        TIMER0_CYCLES_t               TIMER0_CYCLES;
        TIMER0_COUNT_t                TIMER0_COUNT;
        TIMER1_CTRL_t                 TIMER1_CTRL;
        TIMER1_CYCLES_t               TIMER1_CYCLES;
        TIMER1_COUNT_t                TIMER1_COUNT;
        WATCHDOG_CTRL_t               WATCHDOG_CTRL;
        WATCHDOG_CYCLES_t             WATCHDOG_CYCLES;
        WATCHDOG_COUNT_t              WATCHDOG_COUNT;
        RISCV_CTRL_t                  RISCV_CTRL;
        RISCV_CYCLES_t                RISCV_CYCLES;
        RISCV_COUNT_t                 RISCV_COUNT;
    };

    static TICKS_t & TICKS = (*(TICKS_t *)0x40108000);
    static TICKS_t & TICKS_XOR = (*(TICKS_t *)0x40109000);
    static TICKS_t & TICKS_SET = (*(TICKS_t *)0x4010a000);
    static TICKS_t & TICKS_CLR = (*(TICKS_t *)0x4010b000);

} // _TICKS_

namespace _PADS_BANK0_  {

    // Voltage select. Per bank control
    // Reset value: 0x00000000
    BEGIN_TYPE(VOLTAGE_SELECT_t, uint32_t)
        ADD_BITFIELD_RW(VOLTAGE_SELECT, 0, 1)
    END_TYPE()

    // Set voltage to 3.3V (DVDD >= 2V5)
    static const uint32_t VOLTAGE_SELECT_VOLTAGE_SELECT__3v3 = 0;
    // Set voltage to 1.8V (DVDD <= 1V8)
    static const uint32_t VOLTAGE_SELECT_VOLTAGE_SELECT__1v8 = 1;

    // Reset value: 0x00000116
    BEGIN_TYPE(GPIO_t, uint32_t)
        // Pad isolation control. Remove this once the pad is configured by software.
        ADD_BITFIELD_RW(ISO, 8, 1)
        // Output disable. Has priority over output enable from peripherals
        ADD_BITFIELD_RW(OD, 7, 1)
        // Input enable
        ADD_BITFIELD_RW(IE, 6, 1)
        // Drive strength.
        ADD_BITFIELD_RW(DRIVE, 4, 2)
        // Pull up enable
        ADD_BITFIELD_RW(PUE, 3, 1)
        // Pull down enable
        ADD_BITFIELD_RW(PDE, 2, 1)
        // Enable schmitt trigger
        ADD_BITFIELD_RW(SCHMITT, 1, 1)
        // Slew rate control. 1 = Fast, 0 = Slow
        ADD_BITFIELD_RW(SLEWFAST, 0, 1)
    END_TYPE()

    static const uint32_t GPIO_DRIVE__2mA = 0;
    static const uint32_t GPIO_DRIVE__4mA = 1;
    static const uint32_t GPIO_DRIVE__8mA = 2;
    static const uint32_t GPIO_DRIVE__12mA = 3;

    // Reset value: 0x0000005a
    BEGIN_TYPE(SWCLK_t, uint32_t)
        // Pad isolation control. Remove this once the pad is configured by software.
        ADD_BITFIELD_RW(ISO, 8, 1)
        // Output disable. Has priority over output enable from peripherals
        ADD_BITFIELD_RW(OD, 7, 1)
        // Input enable
        ADD_BITFIELD_RW(IE, 6, 1)
        // Drive strength.
        ADD_BITFIELD_RW(DRIVE, 4, 2)
        // Pull up enable
        ADD_BITFIELD_RW(PUE, 3, 1)
        // Pull down enable
        ADD_BITFIELD_RW(PDE, 2, 1)
        // Enable schmitt trigger
        ADD_BITFIELD_RW(SCHMITT, 1, 1)
        // Slew rate control. 1 = Fast, 0 = Slow
        ADD_BITFIELD_RW(SLEWFAST, 0, 1)
    END_TYPE()

    static const uint32_t SWCLK_DRIVE__2mA = 0;
    static const uint32_t SWCLK_DRIVE__4mA = 1;
    static const uint32_t SWCLK_DRIVE__8mA = 2;
    static const uint32_t SWCLK_DRIVE__12mA = 3;

    // Reset value: 0x0000005a
    BEGIN_TYPE(SWD_t, uint32_t)
        // Pad isolation control. Remove this once the pad is configured by software.
        ADD_BITFIELD_RW(ISO, 8, 1)
        // Output disable. Has priority over output enable from peripherals
        ADD_BITFIELD_RW(OD, 7, 1)
        // Input enable
        ADD_BITFIELD_RW(IE, 6, 1)
        // Drive strength.
        ADD_BITFIELD_RW(DRIVE, 4, 2)
        // Pull up enable
        ADD_BITFIELD_RW(PUE, 3, 1)
        // Pull down enable
        ADD_BITFIELD_RW(PDE, 2, 1)
        // Enable schmitt trigger
        ADD_BITFIELD_RW(SCHMITT, 1, 1)
        // Slew rate control. 1 = Fast, 0 = Slow
        ADD_BITFIELD_RW(SLEWFAST, 0, 1)
    END_TYPE()

    static const uint32_t SWD_DRIVE__2mA = 0;
    static const uint32_t SWD_DRIVE__4mA = 1;
    static const uint32_t SWD_DRIVE__8mA = 2;
    static const uint32_t SWD_DRIVE__12mA = 3;

    struct PADS_BANK0_t {
        VOLTAGE_SELECT_t              VOLTAGE_SELECT;
        GPIO_t                        GPIO[48];
        SWCLK_t                       SWCLK;
        SWD_t                         SWD;
    };

    static PADS_BANK0_t & PADS_BANK0 = (*(PADS_BANK0_t *)0x40038000);
    static PADS_BANK0_t & PADS_BANK0_XOR = (*(PADS_BANK0_t *)0x40039000);
    static PADS_BANK0_t & PADS_BANK0_SET = (*(PADS_BANK0_t *)0x4003a000);
    static PADS_BANK0_t & PADS_BANK0_CLR = (*(PADS_BANK0_t *)0x4003b000);

} // _PADS_BANK0_

namespace _PADS_QSPI_  {

    // Voltage select. Per bank control
    // Reset value: 0x00000000
    BEGIN_TYPE(VOLTAGE_SELECT_t, uint32_t)
        ADD_BITFIELD_RW(VOLTAGE_SELECT, 0, 1)
    END_TYPE()

    // Set voltage to 3.3V (DVDD >= 2V5)
    static const uint32_t VOLTAGE_SELECT_VOLTAGE_SELECT__3v3 = 0;
    // Set voltage to 1.8V (DVDD <= 1V8)
    static const uint32_t VOLTAGE_SELECT_VOLTAGE_SELECT__1v8 = 1;

    // Reset value: 0x00000156
    BEGIN_TYPE(GPIO_QSPI_SCLK_t, uint32_t)
        // Pad isolation control. Remove this once the pad is configured by software.
        ADD_BITFIELD_RW(ISO, 8, 1)
        // Output disable. Has priority over output enable from peripherals
        ADD_BITFIELD_RW(OD, 7, 1)
        // Input enable
        ADD_BITFIELD_RW(IE, 6, 1)
        // Drive strength.
        ADD_BITFIELD_RW(DRIVE, 4, 2)
        // Pull up enable
        ADD_BITFIELD_RW(PUE, 3, 1)
        // Pull down enable
        ADD_BITFIELD_RW(PDE, 2, 1)
        // Enable schmitt trigger
        ADD_BITFIELD_RW(SCHMITT, 1, 1)
        // Slew rate control. 1 = Fast, 0 = Slow
        ADD_BITFIELD_RW(SLEWFAST, 0, 1)
    END_TYPE()

    static const uint32_t GPIO_QSPI_SCLK_DRIVE__2mA = 0;
    static const uint32_t GPIO_QSPI_SCLK_DRIVE__4mA = 1;
    static const uint32_t GPIO_QSPI_SCLK_DRIVE__8mA = 2;
    static const uint32_t GPIO_QSPI_SCLK_DRIVE__12mA = 3;

    // Reset value: 0x00000156
    BEGIN_TYPE(GPIO_QSPI_SD_t, uint32_t)
        // Pad isolation control. Remove this once the pad is configured by software.
        ADD_BITFIELD_RW(ISO, 8, 1)
        // Output disable. Has priority over output enable from peripherals
        ADD_BITFIELD_RW(OD, 7, 1)
        // Input enable
        ADD_BITFIELD_RW(IE, 6, 1)
        // Drive strength.
        ADD_BITFIELD_RW(DRIVE, 4, 2)
        // Pull up enable
        ADD_BITFIELD_RW(PUE, 3, 1)
        // Pull down enable
        ADD_BITFIELD_RW(PDE, 2, 1)
        // Enable schmitt trigger
        ADD_BITFIELD_RW(SCHMITT, 1, 1)
        // Slew rate control. 1 = Fast, 0 = Slow
        ADD_BITFIELD_RW(SLEWFAST, 0, 1)
    END_TYPE()

    static const uint32_t GPIO_QSPI_SD_DRIVE__2mA = 0;
    static const uint32_t GPIO_QSPI_SD_DRIVE__4mA = 1;
    static const uint32_t GPIO_QSPI_SD_DRIVE__8mA = 2;
    static const uint32_t GPIO_QSPI_SD_DRIVE__12mA = 3;

    // Reset value: 0x0000015a
    BEGIN_TYPE(GPIO_QSPI_SS_t, uint32_t)
        // Pad isolation control. Remove this once the pad is configured by software.
        ADD_BITFIELD_RW(ISO, 8, 1)
        // Output disable. Has priority over output enable from peripherals
        ADD_BITFIELD_RW(OD, 7, 1)
        // Input enable
        ADD_BITFIELD_RW(IE, 6, 1)
        // Drive strength.
        ADD_BITFIELD_RW(DRIVE, 4, 2)
        // Pull up enable
        ADD_BITFIELD_RW(PUE, 3, 1)
        // Pull down enable
        ADD_BITFIELD_RW(PDE, 2, 1)
        // Enable schmitt trigger
        ADD_BITFIELD_RW(SCHMITT, 1, 1)
        // Slew rate control. 1 = Fast, 0 = Slow
        ADD_BITFIELD_RW(SLEWFAST, 0, 1)
    END_TYPE()

    static const uint32_t GPIO_QSPI_SS_DRIVE__2mA = 0;
    static const uint32_t GPIO_QSPI_SS_DRIVE__4mA = 1;
    static const uint32_t GPIO_QSPI_SS_DRIVE__8mA = 2;
    static const uint32_t GPIO_QSPI_SS_DRIVE__12mA = 3;

    struct PADS_QSPI_t {
        VOLTAGE_SELECT_t              VOLTAGE_SELECT;
        GPIO_QSPI_SCLK_t              GPIO_QSPI_SCLK;
        GPIO_QSPI_SD_t                GPIO_QSPI_SD[4];
        GPIO_QSPI_SS_t                GPIO_QSPI_SS;
    };

    static PADS_QSPI_t & PADS_QSPI = (*(PADS_QSPI_t *)0x40040000);
    static PADS_QSPI_t & PADS_QSPI_XOR = (*(PADS_QSPI_t *)0x40041000);
    static PADS_QSPI_t & PADS_QSPI_SET = (*(PADS_QSPI_t *)0x40042000);
    static PADS_QSPI_t & PADS_QSPI_CLR = (*(PADS_QSPI_t *)0x40043000);

} // _PADS_QSPI_

namespace _IO_QSPI_  {

    // Reset value: 0x00000000
    BEGIN_TYPE(USBPHY_DP_STATUS_t, uint32_t)
        // interrupt to processors, after override is applied
        ADD_BITFIELD_RO(IRQTOPROC, 26, 1)
        // input signal from pad, before filtering and override are applied
        ADD_BITFIELD_RO(INFROMPAD, 17, 1)
        // output enable to pad after register override is applied
        ADD_BITFIELD_RO(OETOPAD, 13, 1)
        // output signal to pad after register override is applied
        ADD_BITFIELD_RO(OUTTOPAD, 9, 1)
    END_TYPE()

    // Reset value: 0x0000001f
    BEGIN_TYPE(USBPHY_DP_CTRL_t, uint32_t)
        ADD_BITFIELD_RW(IRQOVER, 28, 2)
        ADD_BITFIELD_RW(INOVER, 16, 2)
        ADD_BITFIELD_RW(OEOVER, 14, 2)
        ADD_BITFIELD_RW(OUTOVER, 12, 2)
        // 0-31 -> selects pin function according to the gpio table  31 == NULL
        ADD_BITFIELD_RW(FUNCSEL, 0, 5)
    END_TYPE()

    // don't invert the interrupt
    static const uint32_t USBPHY_DP_CTRL_IRQOVER__NORMAL = 0;
    // invert the interrupt
    static const uint32_t USBPHY_DP_CTRL_IRQOVER__INVERT = 1;
    // drive interrupt low
    static const uint32_t USBPHY_DP_CTRL_IRQOVER__LOW = 2;
    // drive interrupt high
    static const uint32_t USBPHY_DP_CTRL_IRQOVER__HIGH = 3;
    // don't invert the peri input
    static const uint32_t USBPHY_DP_CTRL_INOVER__NORMAL = 0;
    // invert the peri input
    static const uint32_t USBPHY_DP_CTRL_INOVER__INVERT = 1;
    // drive peri input low
    static const uint32_t USBPHY_DP_CTRL_INOVER__LOW = 2;
    // drive peri input high
    static const uint32_t USBPHY_DP_CTRL_INOVER__HIGH = 3;
    // drive output enable from peripheral signal selected by funcsel
    static const uint32_t USBPHY_DP_CTRL_OEOVER__NORMAL = 0;
    // drive output enable from inverse of peripheral signal selected by funcsel
    static const uint32_t USBPHY_DP_CTRL_OEOVER__INVERT = 1;
    // disable output
    static const uint32_t USBPHY_DP_CTRL_OEOVER__DISABLE = 2;
    // enable output
    static const uint32_t USBPHY_DP_CTRL_OEOVER__ENABLE = 3;
    // drive output from peripheral signal selected by funcsel
    static const uint32_t USBPHY_DP_CTRL_OUTOVER__NORMAL = 0;
    // drive output from inverse of peripheral signal selected by funcsel
    static const uint32_t USBPHY_DP_CTRL_OUTOVER__INVERT = 1;
    // drive output low
    static const uint32_t USBPHY_DP_CTRL_OUTOVER__LOW = 2;
    // drive output high
    static const uint32_t USBPHY_DP_CTRL_OUTOVER__HIGH = 3;
    static const uint32_t USBPHY_DP_CTRL_FUNCSEL__uart1_tx = 2;
    static const uint32_t USBPHY_DP_CTRL_FUNCSEL__i2c0_sda = 3;
    static const uint32_t USBPHY_DP_CTRL_FUNCSEL__siob_proc_56 = 5;
    static const uint32_t USBPHY_DP_CTRL_FUNCSEL__null = 31;

    // Reset value: 0x00000000
    BEGIN_TYPE(USBPHY_DM_STATUS_t, uint32_t)
        // interrupt to processors, after override is applied
        ADD_BITFIELD_RO(IRQTOPROC, 26, 1)
        // input signal from pad, before filtering and override are applied
        ADD_BITFIELD_RO(INFROMPAD, 17, 1)
        // output enable to pad after register override is applied
        ADD_BITFIELD_RO(OETOPAD, 13, 1)
        // output signal to pad after register override is applied
        ADD_BITFIELD_RO(OUTTOPAD, 9, 1)
    END_TYPE()

    // Reset value: 0x0000001f
    BEGIN_TYPE(USBPHY_DM_CTRL_t, uint32_t)
        ADD_BITFIELD_RW(IRQOVER, 28, 2)
        ADD_BITFIELD_RW(INOVER, 16, 2)
        ADD_BITFIELD_RW(OEOVER, 14, 2)
        ADD_BITFIELD_RW(OUTOVER, 12, 2)
        // 0-31 -> selects pin function according to the gpio table  31 == NULL
        ADD_BITFIELD_RW(FUNCSEL, 0, 5)
    END_TYPE()

    // don't invert the interrupt
    static const uint32_t USBPHY_DM_CTRL_IRQOVER__NORMAL = 0;
    // invert the interrupt
    static const uint32_t USBPHY_DM_CTRL_IRQOVER__INVERT = 1;
    // drive interrupt low
    static const uint32_t USBPHY_DM_CTRL_IRQOVER__LOW = 2;
    // drive interrupt high
    static const uint32_t USBPHY_DM_CTRL_IRQOVER__HIGH = 3;
    // don't invert the peri input
    static const uint32_t USBPHY_DM_CTRL_INOVER__NORMAL = 0;
    // invert the peri input
    static const uint32_t USBPHY_DM_CTRL_INOVER__INVERT = 1;
    // drive peri input low
    static const uint32_t USBPHY_DM_CTRL_INOVER__LOW = 2;
    // drive peri input high
    static const uint32_t USBPHY_DM_CTRL_INOVER__HIGH = 3;
    // drive output enable from peripheral signal selected by funcsel
    static const uint32_t USBPHY_DM_CTRL_OEOVER__NORMAL = 0;
    // drive output enable from inverse of peripheral signal selected by funcsel
    static const uint32_t USBPHY_DM_CTRL_OEOVER__INVERT = 1;
    // disable output
    static const uint32_t USBPHY_DM_CTRL_OEOVER__DISABLE = 2;
    // enable output
    static const uint32_t USBPHY_DM_CTRL_OEOVER__ENABLE = 3;
    // drive output from peripheral signal selected by funcsel
    static const uint32_t USBPHY_DM_CTRL_OUTOVER__NORMAL = 0;
    // drive output from inverse of peripheral signal selected by funcsel
    static const uint32_t USBPHY_DM_CTRL_OUTOVER__INVERT = 1;
    // drive output low
    static const uint32_t USBPHY_DM_CTRL_OUTOVER__LOW = 2;
    // drive output high
    static const uint32_t USBPHY_DM_CTRL_OUTOVER__HIGH = 3;
    static const uint32_t USBPHY_DM_CTRL_FUNCSEL__uart1_rx = 2;
    static const uint32_t USBPHY_DM_CTRL_FUNCSEL__i2c0_scl = 3;
    static const uint32_t USBPHY_DM_CTRL_FUNCSEL__siob_proc_57 = 5;
    static const uint32_t USBPHY_DM_CTRL_FUNCSEL__null = 31;

    // Reset value: 0x00000000
    BEGIN_TYPE(GPIO_QSPI_SCLK_STATUS_t, uint32_t)
        // interrupt to processors, after override is applied
        ADD_BITFIELD_RO(IRQTOPROC, 26, 1)
        // input signal from pad, before filtering and override are applied
        ADD_BITFIELD_RO(INFROMPAD, 17, 1)
        // output enable to pad after register override is applied
        ADD_BITFIELD_RO(OETOPAD, 13, 1)
        // output signal to pad after register override is applied
        ADD_BITFIELD_RO(OUTTOPAD, 9, 1)
    END_TYPE()

    // Reset value: 0x0000001f
    BEGIN_TYPE(GPIO_QSPI_SCLK_CTRL_t, uint32_t)
        ADD_BITFIELD_RW(IRQOVER, 28, 2)
        ADD_BITFIELD_RW(INOVER, 16, 2)
        ADD_BITFIELD_RW(OEOVER, 14, 2)
        ADD_BITFIELD_RW(OUTOVER, 12, 2)
        // 0-31 -> selects pin function according to the gpio table  31 == NULL
        ADD_BITFIELD_RW(FUNCSEL, 0, 5)
    END_TYPE()

    // don't invert the interrupt
    static const uint32_t GPIO_QSPI_SCLK_CTRL_IRQOVER__NORMAL = 0;
    // invert the interrupt
    static const uint32_t GPIO_QSPI_SCLK_CTRL_IRQOVER__INVERT = 1;
    // drive interrupt low
    static const uint32_t GPIO_QSPI_SCLK_CTRL_IRQOVER__LOW = 2;
    // drive interrupt high
    static const uint32_t GPIO_QSPI_SCLK_CTRL_IRQOVER__HIGH = 3;
    // don't invert the peri input
    static const uint32_t GPIO_QSPI_SCLK_CTRL_INOVER__NORMAL = 0;
    // invert the peri input
    static const uint32_t GPIO_QSPI_SCLK_CTRL_INOVER__INVERT = 1;
    // drive peri input low
    static const uint32_t GPIO_QSPI_SCLK_CTRL_INOVER__LOW = 2;
    // drive peri input high
    static const uint32_t GPIO_QSPI_SCLK_CTRL_INOVER__HIGH = 3;
    // drive output enable from peripheral signal selected by funcsel
    static const uint32_t GPIO_QSPI_SCLK_CTRL_OEOVER__NORMAL = 0;
    // drive output enable from inverse of peripheral signal selected by funcsel
    static const uint32_t GPIO_QSPI_SCLK_CTRL_OEOVER__INVERT = 1;
    // disable output
    static const uint32_t GPIO_QSPI_SCLK_CTRL_OEOVER__DISABLE = 2;
    // enable output
    static const uint32_t GPIO_QSPI_SCLK_CTRL_OEOVER__ENABLE = 3;
    // drive output from peripheral signal selected by funcsel
    static const uint32_t GPIO_QSPI_SCLK_CTRL_OUTOVER__NORMAL = 0;
    // drive output from inverse of peripheral signal selected by funcsel
    static const uint32_t GPIO_QSPI_SCLK_CTRL_OUTOVER__INVERT = 1;
    // drive output low
    static const uint32_t GPIO_QSPI_SCLK_CTRL_OUTOVER__LOW = 2;
    // drive output high
    static const uint32_t GPIO_QSPI_SCLK_CTRL_OUTOVER__HIGH = 3;
    static const uint32_t GPIO_QSPI_SCLK_CTRL_FUNCSEL__xip_sclk = 0;
    static const uint32_t GPIO_QSPI_SCLK_CTRL_FUNCSEL__uart1_cts = 2;
    static const uint32_t GPIO_QSPI_SCLK_CTRL_FUNCSEL__i2c1_sda = 3;
    static const uint32_t GPIO_QSPI_SCLK_CTRL_FUNCSEL__siob_proc_58 = 5;
    static const uint32_t GPIO_QSPI_SCLK_CTRL_FUNCSEL__uart1_tx = 11;
    static const uint32_t GPIO_QSPI_SCLK_CTRL_FUNCSEL__null = 31;

    // Reset value: 0x00000000
    BEGIN_TYPE(GPIO_QSPI_SS_STATUS_t, uint32_t)
        // interrupt to processors, after override is applied
        ADD_BITFIELD_RO(IRQTOPROC, 26, 1)
        // input signal from pad, before filtering and override are applied
        ADD_BITFIELD_RO(INFROMPAD, 17, 1)
        // output enable to pad after register override is applied
        ADD_BITFIELD_RO(OETOPAD, 13, 1)
        // output signal to pad after register override is applied
        ADD_BITFIELD_RO(OUTTOPAD, 9, 1)
    END_TYPE()

    // Reset value: 0x0000001f
    BEGIN_TYPE(GPIO_QSPI_SS_CTRL_t, uint32_t)
        ADD_BITFIELD_RW(IRQOVER, 28, 2)
        ADD_BITFIELD_RW(INOVER, 16, 2)
        ADD_BITFIELD_RW(OEOVER, 14, 2)
        ADD_BITFIELD_RW(OUTOVER, 12, 2)
        // 0-31 -> selects pin function according to the gpio table  31 == NULL
        ADD_BITFIELD_RW(FUNCSEL, 0, 5)
    END_TYPE()

    // don't invert the interrupt
    static const uint32_t GPIO_QSPI_SS_CTRL_IRQOVER__NORMAL = 0;
    // invert the interrupt
    static const uint32_t GPIO_QSPI_SS_CTRL_IRQOVER__INVERT = 1;
    // drive interrupt low
    static const uint32_t GPIO_QSPI_SS_CTRL_IRQOVER__LOW = 2;
    // drive interrupt high
    static const uint32_t GPIO_QSPI_SS_CTRL_IRQOVER__HIGH = 3;
    // don't invert the peri input
    static const uint32_t GPIO_QSPI_SS_CTRL_INOVER__NORMAL = 0;
    // invert the peri input
    static const uint32_t GPIO_QSPI_SS_CTRL_INOVER__INVERT = 1;
    // drive peri input low
    static const uint32_t GPIO_QSPI_SS_CTRL_INOVER__LOW = 2;
    // drive peri input high
    static const uint32_t GPIO_QSPI_SS_CTRL_INOVER__HIGH = 3;
    // drive output enable from peripheral signal selected by funcsel
    static const uint32_t GPIO_QSPI_SS_CTRL_OEOVER__NORMAL = 0;
    // drive output enable from inverse of peripheral signal selected by funcsel
    static const uint32_t GPIO_QSPI_SS_CTRL_OEOVER__INVERT = 1;
    // disable output
    static const uint32_t GPIO_QSPI_SS_CTRL_OEOVER__DISABLE = 2;
    // enable output
    static const uint32_t GPIO_QSPI_SS_CTRL_OEOVER__ENABLE = 3;
    // drive output from peripheral signal selected by funcsel
    static const uint32_t GPIO_QSPI_SS_CTRL_OUTOVER__NORMAL = 0;
    // drive output from inverse of peripheral signal selected by funcsel
    static const uint32_t GPIO_QSPI_SS_CTRL_OUTOVER__INVERT = 1;
    // drive output low
    static const uint32_t GPIO_QSPI_SS_CTRL_OUTOVER__LOW = 2;
    // drive output high
    static const uint32_t GPIO_QSPI_SS_CTRL_OUTOVER__HIGH = 3;
    static const uint32_t GPIO_QSPI_SS_CTRL_FUNCSEL__xip_ss_n_0 = 0;
    static const uint32_t GPIO_QSPI_SS_CTRL_FUNCSEL__uart1_rts = 2;
    static const uint32_t GPIO_QSPI_SS_CTRL_FUNCSEL__i2c1_scl = 3;
    static const uint32_t GPIO_QSPI_SS_CTRL_FUNCSEL__siob_proc_59 = 5;
    static const uint32_t GPIO_QSPI_SS_CTRL_FUNCSEL__uart1_rx = 11;
    static const uint32_t GPIO_QSPI_SS_CTRL_FUNCSEL__null = 31;

    // Reset value: 0x00000000
    BEGIN_TYPE(GPIO_QSPI_SD_STATUS_t, uint32_t)
        // interrupt to processors, after override is applied
        ADD_BITFIELD_RO(IRQTOPROC, 26, 1)
        // input signal from pad, before filtering and override are applied
        ADD_BITFIELD_RO(INFROMPAD, 17, 1)
        // output enable to pad after register override is applied
        ADD_BITFIELD_RO(OETOPAD, 13, 1)
        // output signal to pad after register override is applied
        ADD_BITFIELD_RO(OUTTOPAD, 9, 1)
    END_TYPE()

    // Reset value: 0x0000001f
    BEGIN_TYPE(GPIO_QSPI_SD_CTRL_t, uint32_t)
        ADD_BITFIELD_RW(IRQOVER, 28, 2)
        ADD_BITFIELD_RW(INOVER, 16, 2)
        ADD_BITFIELD_RW(OEOVER, 14, 2)
        ADD_BITFIELD_RW(OUTOVER, 12, 2)
        // 0-31 -> selects pin function according to the gpio table  31 == NULL
        ADD_BITFIELD_RW(FUNCSEL, 0, 5)
    END_TYPE()

    // don't invert the interrupt
    static const uint32_t GPIO_QSPI_SD_CTRL_IRQOVER__NORMAL = 0;
    // invert the interrupt
    static const uint32_t GPIO_QSPI_SD_CTRL_IRQOVER__INVERT = 1;
    // drive interrupt low
    static const uint32_t GPIO_QSPI_SD_CTRL_IRQOVER__LOW = 2;
    // drive interrupt high
    static const uint32_t GPIO_QSPI_SD_CTRL_IRQOVER__HIGH = 3;
    // don't invert the peri input
    static const uint32_t GPIO_QSPI_SD_CTRL_INOVER__NORMAL = 0;
    // invert the peri input
    static const uint32_t GPIO_QSPI_SD_CTRL_INOVER__INVERT = 1;
    // drive peri input low
    static const uint32_t GPIO_QSPI_SD_CTRL_INOVER__LOW = 2;
    // drive peri input high
    static const uint32_t GPIO_QSPI_SD_CTRL_INOVER__HIGH = 3;
    // drive output enable from peripheral signal selected by funcsel
    static const uint32_t GPIO_QSPI_SD_CTRL_OEOVER__NORMAL = 0;
    // drive output enable from inverse of peripheral signal selected by funcsel
    static const uint32_t GPIO_QSPI_SD_CTRL_OEOVER__INVERT = 1;
    // disable output
    static const uint32_t GPIO_QSPI_SD_CTRL_OEOVER__DISABLE = 2;
    // enable output
    static const uint32_t GPIO_QSPI_SD_CTRL_OEOVER__ENABLE = 3;
    // drive output from peripheral signal selected by funcsel
    static const uint32_t GPIO_QSPI_SD_CTRL_OUTOVER__NORMAL = 0;
    // drive output from inverse of peripheral signal selected by funcsel
    static const uint32_t GPIO_QSPI_SD_CTRL_OUTOVER__INVERT = 1;
    // drive output low
    static const uint32_t GPIO_QSPI_SD_CTRL_OUTOVER__LOW = 2;
    // drive output high
    static const uint32_t GPIO_QSPI_SD_CTRL_OUTOVER__HIGH = 3;
    static const uint32_t GPIO_QSPI_SD_CTRL_FUNCSEL__xip = 0;
    static const uint32_t GPIO_QSPI_SD_CTRL_FUNCSEL__uart = 2;
    static const uint32_t GPIO_QSPI_SD_CTRL_FUNCSEL__i2c = 3;
    static const uint32_t GPIO_QSPI_SD_CTRL_FUNCSEL__sio = 5;
    static const uint32_t GPIO_QSPI_SD_CTRL_FUNCSEL__uart_aux = 11;
    static const uint32_t GPIO_QSPI_SD_CTRL_FUNCSEL__null = 31;

    // Reset value: 0x00000000
    BEGIN_TYPE(IRQSUMMARY_PROC0_SECURE_t, uint32_t)
        ADD_BITFIELD_RO(GPIO_QSPI_SD3, 7, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD2, 6, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD1, 5, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD0, 4, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SS, 3, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SCLK, 2, 1)
        ADD_BITFIELD_RO(USBPHY_DM, 1, 1)
        ADD_BITFIELD_RO(USBPHY_DP, 0, 1)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(IRQSUMMARY_PROC0_NONSECURE_t, uint32_t)
        ADD_BITFIELD_RO(GPIO_QSPI_SD3, 7, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD2, 6, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD1, 5, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD0, 4, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SS, 3, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SCLK, 2, 1)
        ADD_BITFIELD_RO(USBPHY_DM, 1, 1)
        ADD_BITFIELD_RO(USBPHY_DP, 0, 1)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(IRQSUMMARY_PROC1_SECURE_t, uint32_t)
        ADD_BITFIELD_RO(GPIO_QSPI_SD3, 7, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD2, 6, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD1, 5, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD0, 4, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SS, 3, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SCLK, 2, 1)
        ADD_BITFIELD_RO(USBPHY_DM, 1, 1)
        ADD_BITFIELD_RO(USBPHY_DP, 0, 1)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(IRQSUMMARY_PROC1_NONSECURE_t, uint32_t)
        ADD_BITFIELD_RO(GPIO_QSPI_SD3, 7, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD2, 6, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD1, 5, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD0, 4, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SS, 3, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SCLK, 2, 1)
        ADD_BITFIELD_RO(USBPHY_DM, 1, 1)
        ADD_BITFIELD_RO(USBPHY_DP, 0, 1)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(IRQSUMMARY_DORMANT_WAKE_SECURE_t, uint32_t)
        ADD_BITFIELD_RO(GPIO_QSPI_SD3, 7, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD2, 6, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD1, 5, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD0, 4, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SS, 3, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SCLK, 2, 1)
        ADD_BITFIELD_RO(USBPHY_DM, 1, 1)
        ADD_BITFIELD_RO(USBPHY_DP, 0, 1)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(IRQSUMMARY_DORMANT_WAKE_NONSECURE_t, uint32_t)
        ADD_BITFIELD_RO(GPIO_QSPI_SD3, 7, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD2, 6, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD1, 5, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD0, 4, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SS, 3, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SCLK, 2, 1)
        ADD_BITFIELD_RO(USBPHY_DM, 1, 1)
        ADD_BITFIELD_RO(USBPHY_DP, 0, 1)
    END_TYPE()

    // Raw Interrupts
    // Reset value: 0x00000000
    BEGIN_TYPE(INTR_t, uint32_t)
        ADD_BITFIELD_RW(GPIO_QSPI_SD3_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD3_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD3_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD3_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD2_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD2_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD2_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD2_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD1_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD1_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD1_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD1_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD0_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD0_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD0_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD0_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SS_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SS_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SS_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SS_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SCLK_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SCLK_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SCLK_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SCLK_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(USBPHY_DM_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(USBPHY_DM_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RO(USBPHY_DM_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RO(USBPHY_DM_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(USBPHY_DP_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(USBPHY_DP_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RO(USBPHY_DP_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RO(USBPHY_DP_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Enable for proc0
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC0_INTE_t, uint32_t)
        ADD_BITFIELD_RW(GPIO_QSPI_SD3_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD3_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD3_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD3_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD2_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD2_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD2_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD2_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD1_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD1_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD1_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD1_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD0_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD0_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD0_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD0_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SS_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SS_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SS_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SS_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SCLK_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SCLK_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SCLK_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SCLK_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(USBPHY_DM_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(USBPHY_DM_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(USBPHY_DM_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(USBPHY_DM_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(USBPHY_DP_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(USBPHY_DP_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(USBPHY_DP_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(USBPHY_DP_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Force for proc0
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC0_INTF_t, uint32_t)
        ADD_BITFIELD_RW(GPIO_QSPI_SD3_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD3_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD3_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD3_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD2_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD2_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD2_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD2_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD1_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD1_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD1_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD1_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD0_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD0_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD0_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD0_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SS_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SS_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SS_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SS_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SCLK_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SCLK_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SCLK_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SCLK_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(USBPHY_DM_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(USBPHY_DM_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(USBPHY_DM_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(USBPHY_DM_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(USBPHY_DP_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(USBPHY_DP_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(USBPHY_DP_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(USBPHY_DP_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt status after masking & forcing for proc0
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC0_INTS_t, uint32_t)
        ADD_BITFIELD_RO(GPIO_QSPI_SD3_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD3_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD3_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD3_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD2_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD2_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD2_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD2_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD1_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD1_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD1_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD1_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD0_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD0_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD0_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD0_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SS_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SS_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SS_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SS_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SCLK_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SCLK_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SCLK_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SCLK_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RO(USBPHY_DM_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RO(USBPHY_DM_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RO(USBPHY_DM_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RO(USBPHY_DM_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RO(USBPHY_DP_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RO(USBPHY_DP_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RO(USBPHY_DP_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RO(USBPHY_DP_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Enable for proc1
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC1_INTE_t, uint32_t)
        ADD_BITFIELD_RW(GPIO_QSPI_SD3_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD3_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD3_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD3_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD2_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD2_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD2_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD2_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD1_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD1_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD1_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD1_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD0_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD0_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD0_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD0_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SS_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SS_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SS_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SS_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SCLK_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SCLK_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SCLK_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SCLK_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(USBPHY_DM_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(USBPHY_DM_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(USBPHY_DM_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(USBPHY_DM_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(USBPHY_DP_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(USBPHY_DP_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(USBPHY_DP_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(USBPHY_DP_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Force for proc1
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC1_INTF_t, uint32_t)
        ADD_BITFIELD_RW(GPIO_QSPI_SD3_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD3_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD3_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD3_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD2_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD2_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD2_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD2_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD1_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD1_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD1_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD1_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD0_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD0_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD0_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD0_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SS_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SS_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SS_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SS_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SCLK_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SCLK_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SCLK_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SCLK_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(USBPHY_DM_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(USBPHY_DM_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(USBPHY_DM_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(USBPHY_DM_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(USBPHY_DP_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(USBPHY_DP_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(USBPHY_DP_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(USBPHY_DP_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt status after masking & forcing for proc1
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC1_INTS_t, uint32_t)
        ADD_BITFIELD_RO(GPIO_QSPI_SD3_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD3_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD3_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD3_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD2_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD2_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD2_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD2_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD1_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD1_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD1_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD1_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD0_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD0_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD0_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD0_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SS_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SS_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SS_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SS_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SCLK_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SCLK_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SCLK_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SCLK_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RO(USBPHY_DM_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RO(USBPHY_DM_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RO(USBPHY_DM_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RO(USBPHY_DM_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RO(USBPHY_DP_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RO(USBPHY_DP_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RO(USBPHY_DP_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RO(USBPHY_DP_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Enable for dormant_wake
    // Reset value: 0x00000000
    BEGIN_TYPE(DORMANT_WAKE_INTE_t, uint32_t)
        ADD_BITFIELD_RW(GPIO_QSPI_SD3_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD3_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD3_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD3_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD2_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD2_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD2_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD2_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD1_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD1_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD1_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD1_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD0_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD0_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD0_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD0_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SS_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SS_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SS_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SS_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SCLK_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SCLK_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SCLK_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SCLK_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(USBPHY_DM_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(USBPHY_DM_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(USBPHY_DM_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(USBPHY_DM_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(USBPHY_DP_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(USBPHY_DP_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(USBPHY_DP_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(USBPHY_DP_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Force for dormant_wake
    // Reset value: 0x00000000
    BEGIN_TYPE(DORMANT_WAKE_INTF_t, uint32_t)
        ADD_BITFIELD_RW(GPIO_QSPI_SD3_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD3_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD3_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD3_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD2_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD2_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD2_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD2_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD1_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD1_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD1_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD1_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD0_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD0_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD0_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SD0_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SS_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SS_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SS_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SS_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SCLK_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SCLK_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SCLK_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO_QSPI_SCLK_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(USBPHY_DM_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(USBPHY_DM_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(USBPHY_DM_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(USBPHY_DM_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(USBPHY_DP_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(USBPHY_DP_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(USBPHY_DP_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(USBPHY_DP_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt status after masking & forcing for dormant_wake
    // Reset value: 0x00000000
    BEGIN_TYPE(DORMANT_WAKE_INTS_t, uint32_t)
        ADD_BITFIELD_RO(GPIO_QSPI_SD3_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD3_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD3_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD3_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD2_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD2_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD2_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD2_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD1_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD1_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD1_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD1_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD0_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD0_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD0_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SD0_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SS_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SS_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SS_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SS_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SCLK_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SCLK_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SCLK_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RO(GPIO_QSPI_SCLK_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RO(USBPHY_DM_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RO(USBPHY_DM_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RO(USBPHY_DM_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RO(USBPHY_DM_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RO(USBPHY_DP_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RO(USBPHY_DP_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RO(USBPHY_DP_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RO(USBPHY_DP_LEVEL_LOW, 0, 1)
    END_TYPE()

    struct IO_QSPI_t {
        USBPHY_DP_STATUS_t            USBPHY_DP_STATUS;
        USBPHY_DP_CTRL_t              USBPHY_DP_CTRL;
        USBPHY_DM_STATUS_t            USBPHY_DM_STATUS;
        USBPHY_DM_CTRL_t              USBPHY_DM_CTRL;
        GPIO_QSPI_SCLK_STATUS_t       GPIO_QSPI_SCLK_STATUS;
        GPIO_QSPI_SCLK_CTRL_t         GPIO_QSPI_SCLK_CTRL;
        GPIO_QSPI_SS_STATUS_t         GPIO_QSPI_SS_STATUS;
        GPIO_QSPI_SS_CTRL_t           GPIO_QSPI_SS_CTRL;
        GPIO_QSPI_SD_STATUS_t         GPIO_QSPI_SD0_STATUS;
        GPIO_QSPI_SD_CTRL_t           GPIO_QSPI_SD0_CTRL;
        GPIO_QSPI_SD_STATUS_t         GPIO_QSPI_SD1_STATUS;
        GPIO_QSPI_SD_CTRL_t           GPIO_QSPI_SD1_CTRL;
        GPIO_QSPI_SD_STATUS_t         GPIO_QSPI_SD2_STATUS;
        GPIO_QSPI_SD_CTRL_t           GPIO_QSPI_SD2_CTRL;
        GPIO_QSPI_SD_STATUS_t         GPIO_QSPI_SD3_STATUS;
        GPIO_QSPI_SD_CTRL_t           GPIO_QSPI_SD3_CTRL;
        uint32_t                      reserved0[112];
        IRQSUMMARY_PROC0_SECURE_t     IRQSUMMARY_PROC0_SECURE;
        IRQSUMMARY_PROC0_NONSECURE_t  IRQSUMMARY_PROC0_NONSECURE;
        IRQSUMMARY_PROC1_SECURE_t     IRQSUMMARY_PROC1_SECURE;
        IRQSUMMARY_PROC1_NONSECURE_t  IRQSUMMARY_PROC1_NONSECURE;
        IRQSUMMARY_DORMANT_WAKE_SECURE_t IRQSUMMARY_DORMANT_WAKE_SECURE;
        IRQSUMMARY_DORMANT_WAKE_NONSECURE_t IRQSUMMARY_DORMANT_WAKE_NONSECURE;
        INTR_t                        INTR;
        PROC0_INTE_t                  PROC0_INTE;
        PROC0_INTF_t                  PROC0_INTF;
        PROC0_INTS_t                  PROC0_INTS;
        PROC1_INTE_t                  PROC1_INTE;
        PROC1_INTF_t                  PROC1_INTF;
        PROC1_INTS_t                  PROC1_INTS;
        DORMANT_WAKE_INTE_t           DORMANT_WAKE_INTE;
        DORMANT_WAKE_INTF_t           DORMANT_WAKE_INTF;
        DORMANT_WAKE_INTS_t           DORMANT_WAKE_INTS;
    };

    static IO_QSPI_t & IO_QSPI = (*(IO_QSPI_t *)0x40030000);
    static IO_QSPI_t & IO_QSPI_XOR = (*(IO_QSPI_t *)0x40031000);
    static IO_QSPI_t & IO_QSPI_SET = (*(IO_QSPI_t *)0x40032000);
    static IO_QSPI_t & IO_QSPI_CLR = (*(IO_QSPI_t *)0x40033000);

} // _IO_QSPI_

namespace _IO_BANK0_  {

    // Reset value: 0x00000000
    BEGIN_TYPE(GPIO_STATUS_t, uint32_t)
        // interrupt to processors, after override is applied
        ADD_BITFIELD_RO(IRQTOPROC, 26, 1)
        // input signal from pad, before filtering and override are applied
        ADD_BITFIELD_RO(INFROMPAD, 17, 1)
        // output enable to pad after register override is applied
        ADD_BITFIELD_RO(OETOPAD, 13, 1)
        // output signal to pad after register override is applied
        ADD_BITFIELD_RO(OUTTOPAD, 9, 1)
    END_TYPE()

    // Reset value: 0x0000001f
    BEGIN_TYPE(GPIO_CTRL_t, uint32_t)
        ADD_BITFIELD_RW(IRQOVER, 28, 2)
        ADD_BITFIELD_RW(INOVER, 16, 2)
        ADD_BITFIELD_RW(OEOVER, 14, 2)
        ADD_BITFIELD_RW(OUTOVER, 12, 2)
        // 0-31 -> selects pin function according to the gpio table  31 == NULL
        ADD_BITFIELD_RW(FUNCSEL, 0, 5)
    END_TYPE()

    // don't invert the interrupt
    static const uint32_t GPIO_CTRL_IRQOVER__NORMAL = 0;
    // invert the interrupt
    static const uint32_t GPIO_CTRL_IRQOVER__INVERT = 1;
    // drive interrupt low
    static const uint32_t GPIO_CTRL_IRQOVER__LOW = 2;
    // drive interrupt high
    static const uint32_t GPIO_CTRL_IRQOVER__HIGH = 3;
    // don't invert the peri input
    static const uint32_t GPIO_CTRL_INOVER__NORMAL = 0;
    // invert the peri input
    static const uint32_t GPIO_CTRL_INOVER__INVERT = 1;
    // drive peri input low
    static const uint32_t GPIO_CTRL_INOVER__LOW = 2;
    // drive peri input high
    static const uint32_t GPIO_CTRL_INOVER__HIGH = 3;
    // drive output enable from peripheral signal selected by funcsel
    static const uint32_t GPIO_CTRL_OEOVER__NORMAL = 0;
    // drive output enable from inverse of peripheral signal selected by funcsel
    static const uint32_t GPIO_CTRL_OEOVER__INVERT = 1;
    // disable output
    static const uint32_t GPIO_CTRL_OEOVER__DISABLE = 2;
    // enable output
    static const uint32_t GPIO_CTRL_OEOVER__ENABLE = 3;
    // drive output from peripheral signal selected by funcsel
    static const uint32_t GPIO_CTRL_OUTOVER__NORMAL = 0;
    // drive output from inverse of peripheral signal selected by funcsel
    static const uint32_t GPIO_CTRL_OUTOVER__INVERT = 1;
    // drive output low
    static const uint32_t GPIO_CTRL_OUTOVER__LOW = 2;
    // drive output high
    static const uint32_t GPIO_CTRL_OUTOVER__HIGH = 3;
    static const uint32_t GPIO_CTRL_FUNCSEL__jtag = 0;
    static const uint32_t GPIO_CTRL_FUNCSEL__spi = 1;
    static const uint32_t GPIO_CTRL_FUNCSEL__uart = 2;
    static const uint32_t GPIO_CTRL_FUNCSEL__i2c = 3;
    static const uint32_t GPIO_CTRL_FUNCSEL__pwm = 4;
    static const uint32_t GPIO_CTRL_FUNCSEL__sio = 5;
    static const uint32_t GPIO_CTRL_FUNCSEL__pio0 = 6;
    static const uint32_t GPIO_CTRL_FUNCSEL__pio1 = 7;
    static const uint32_t GPIO_CTRL_FUNCSEL__pio2 = 8;
    static const uint32_t GPIO_CTRL_FUNCSEL__clock = 9;
    static const uint32_t GPIO_CTRL_FUNCSEL__usb = 10;
    static const uint32_t GPIO_CTRL_FUNCSEL__uart_aux = 11;
    static const uint32_t GPIO_CTRL_FUNCSEL__null = 31;

    // Reset value: 0x00000000
    BEGIN_TYPE(IRQSUMMARY_PROC0_SECURE0_t, uint32_t)
        ADD_BITFIELD_RO(GPIO31, 31, 1)
        ADD_BITFIELD_RO(GPIO30, 30, 1)
        ADD_BITFIELD_RO(GPIO29, 29, 1)
        ADD_BITFIELD_RO(GPIO28, 28, 1)
        ADD_BITFIELD_RO(GPIO27, 27, 1)
        ADD_BITFIELD_RO(GPIO26, 26, 1)
        ADD_BITFIELD_RO(GPIO25, 25, 1)
        ADD_BITFIELD_RO(GPIO24, 24, 1)
        ADD_BITFIELD_RO(GPIO23, 23, 1)
        ADD_BITFIELD_RO(GPIO22, 22, 1)
        ADD_BITFIELD_RO(GPIO21, 21, 1)
        ADD_BITFIELD_RO(GPIO20, 20, 1)
        ADD_BITFIELD_RO(GPIO19, 19, 1)
        ADD_BITFIELD_RO(GPIO18, 18, 1)
        ADD_BITFIELD_RO(GPIO17, 17, 1)
        ADD_BITFIELD_RO(GPIO16, 16, 1)
        ADD_BITFIELD_RO(GPIO15, 15, 1)
        ADD_BITFIELD_RO(GPIO14, 14, 1)
        ADD_BITFIELD_RO(GPIO13, 13, 1)
        ADD_BITFIELD_RO(GPIO12, 12, 1)
        ADD_BITFIELD_RO(GPIO11, 11, 1)
        ADD_BITFIELD_RO(GPIO10, 10, 1)
        ADD_BITFIELD_RO(GPIO9, 9, 1)
        ADD_BITFIELD_RO(GPIO8, 8, 1)
        ADD_BITFIELD_RO(GPIO7, 7, 1)
        ADD_BITFIELD_RO(GPIO6, 6, 1)
        ADD_BITFIELD_RO(GPIO5, 5, 1)
        ADD_BITFIELD_RO(GPIO4, 4, 1)
        ADD_BITFIELD_RO(GPIO3, 3, 1)
        ADD_BITFIELD_RO(GPIO2, 2, 1)
        ADD_BITFIELD_RO(GPIO1, 1, 1)
        ADD_BITFIELD_RO(GPIO0, 0, 1)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(IRQSUMMARY_PROC0_SECURE1_t, uint32_t)
        ADD_BITFIELD_RO(GPIO47, 15, 1)
        ADD_BITFIELD_RO(GPIO46, 14, 1)
        ADD_BITFIELD_RO(GPIO45, 13, 1)
        ADD_BITFIELD_RO(GPIO44, 12, 1)
        ADD_BITFIELD_RO(GPIO43, 11, 1)
        ADD_BITFIELD_RO(GPIO42, 10, 1)
        ADD_BITFIELD_RO(GPIO41, 9, 1)
        ADD_BITFIELD_RO(GPIO40, 8, 1)
        ADD_BITFIELD_RO(GPIO39, 7, 1)
        ADD_BITFIELD_RO(GPIO38, 6, 1)
        ADD_BITFIELD_RO(GPIO37, 5, 1)
        ADD_BITFIELD_RO(GPIO36, 4, 1)
        ADD_BITFIELD_RO(GPIO35, 3, 1)
        ADD_BITFIELD_RO(GPIO34, 2, 1)
        ADD_BITFIELD_RO(GPIO33, 1, 1)
        ADD_BITFIELD_RO(GPIO32, 0, 1)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(IRQSUMMARY_PROC0_NONSECURE0_t, uint32_t)
        ADD_BITFIELD_RO(GPIO31, 31, 1)
        ADD_BITFIELD_RO(GPIO30, 30, 1)
        ADD_BITFIELD_RO(GPIO29, 29, 1)
        ADD_BITFIELD_RO(GPIO28, 28, 1)
        ADD_BITFIELD_RO(GPIO27, 27, 1)
        ADD_BITFIELD_RO(GPIO26, 26, 1)
        ADD_BITFIELD_RO(GPIO25, 25, 1)
        ADD_BITFIELD_RO(GPIO24, 24, 1)
        ADD_BITFIELD_RO(GPIO23, 23, 1)
        ADD_BITFIELD_RO(GPIO22, 22, 1)
        ADD_BITFIELD_RO(GPIO21, 21, 1)
        ADD_BITFIELD_RO(GPIO20, 20, 1)
        ADD_BITFIELD_RO(GPIO19, 19, 1)
        ADD_BITFIELD_RO(GPIO18, 18, 1)
        ADD_BITFIELD_RO(GPIO17, 17, 1)
        ADD_BITFIELD_RO(GPIO16, 16, 1)
        ADD_BITFIELD_RO(GPIO15, 15, 1)
        ADD_BITFIELD_RO(GPIO14, 14, 1)
        ADD_BITFIELD_RO(GPIO13, 13, 1)
        ADD_BITFIELD_RO(GPIO12, 12, 1)
        ADD_BITFIELD_RO(GPIO11, 11, 1)
        ADD_BITFIELD_RO(GPIO10, 10, 1)
        ADD_BITFIELD_RO(GPIO9, 9, 1)
        ADD_BITFIELD_RO(GPIO8, 8, 1)
        ADD_BITFIELD_RO(GPIO7, 7, 1)
        ADD_BITFIELD_RO(GPIO6, 6, 1)
        ADD_BITFIELD_RO(GPIO5, 5, 1)
        ADD_BITFIELD_RO(GPIO4, 4, 1)
        ADD_BITFIELD_RO(GPIO3, 3, 1)
        ADD_BITFIELD_RO(GPIO2, 2, 1)
        ADD_BITFIELD_RO(GPIO1, 1, 1)
        ADD_BITFIELD_RO(GPIO0, 0, 1)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(IRQSUMMARY_PROC0_NONSECURE1_t, uint32_t)
        ADD_BITFIELD_RO(GPIO47, 15, 1)
        ADD_BITFIELD_RO(GPIO46, 14, 1)
        ADD_BITFIELD_RO(GPIO45, 13, 1)
        ADD_BITFIELD_RO(GPIO44, 12, 1)
        ADD_BITFIELD_RO(GPIO43, 11, 1)
        ADD_BITFIELD_RO(GPIO42, 10, 1)
        ADD_BITFIELD_RO(GPIO41, 9, 1)
        ADD_BITFIELD_RO(GPIO40, 8, 1)
        ADD_BITFIELD_RO(GPIO39, 7, 1)
        ADD_BITFIELD_RO(GPIO38, 6, 1)
        ADD_BITFIELD_RO(GPIO37, 5, 1)
        ADD_BITFIELD_RO(GPIO36, 4, 1)
        ADD_BITFIELD_RO(GPIO35, 3, 1)
        ADD_BITFIELD_RO(GPIO34, 2, 1)
        ADD_BITFIELD_RO(GPIO33, 1, 1)
        ADD_BITFIELD_RO(GPIO32, 0, 1)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(IRQSUMMARY_PROC1_SECURE0_t, uint32_t)
        ADD_BITFIELD_RO(GPIO31, 31, 1)
        ADD_BITFIELD_RO(GPIO30, 30, 1)
        ADD_BITFIELD_RO(GPIO29, 29, 1)
        ADD_BITFIELD_RO(GPIO28, 28, 1)
        ADD_BITFIELD_RO(GPIO27, 27, 1)
        ADD_BITFIELD_RO(GPIO26, 26, 1)
        ADD_BITFIELD_RO(GPIO25, 25, 1)
        ADD_BITFIELD_RO(GPIO24, 24, 1)
        ADD_BITFIELD_RO(GPIO23, 23, 1)
        ADD_BITFIELD_RO(GPIO22, 22, 1)
        ADD_BITFIELD_RO(GPIO21, 21, 1)
        ADD_BITFIELD_RO(GPIO20, 20, 1)
        ADD_BITFIELD_RO(GPIO19, 19, 1)
        ADD_BITFIELD_RO(GPIO18, 18, 1)
        ADD_BITFIELD_RO(GPIO17, 17, 1)
        ADD_BITFIELD_RO(GPIO16, 16, 1)
        ADD_BITFIELD_RO(GPIO15, 15, 1)
        ADD_BITFIELD_RO(GPIO14, 14, 1)
        ADD_BITFIELD_RO(GPIO13, 13, 1)
        ADD_BITFIELD_RO(GPIO12, 12, 1)
        ADD_BITFIELD_RO(GPIO11, 11, 1)
        ADD_BITFIELD_RO(GPIO10, 10, 1)
        ADD_BITFIELD_RO(GPIO9, 9, 1)
        ADD_BITFIELD_RO(GPIO8, 8, 1)
        ADD_BITFIELD_RO(GPIO7, 7, 1)
        ADD_BITFIELD_RO(GPIO6, 6, 1)
        ADD_BITFIELD_RO(GPIO5, 5, 1)
        ADD_BITFIELD_RO(GPIO4, 4, 1)
        ADD_BITFIELD_RO(GPIO3, 3, 1)
        ADD_BITFIELD_RO(GPIO2, 2, 1)
        ADD_BITFIELD_RO(GPIO1, 1, 1)
        ADD_BITFIELD_RO(GPIO0, 0, 1)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(IRQSUMMARY_PROC1_SECURE1_t, uint32_t)
        ADD_BITFIELD_RO(GPIO47, 15, 1)
        ADD_BITFIELD_RO(GPIO46, 14, 1)
        ADD_BITFIELD_RO(GPIO45, 13, 1)
        ADD_BITFIELD_RO(GPIO44, 12, 1)
        ADD_BITFIELD_RO(GPIO43, 11, 1)
        ADD_BITFIELD_RO(GPIO42, 10, 1)
        ADD_BITFIELD_RO(GPIO41, 9, 1)
        ADD_BITFIELD_RO(GPIO40, 8, 1)
        ADD_BITFIELD_RO(GPIO39, 7, 1)
        ADD_BITFIELD_RO(GPIO38, 6, 1)
        ADD_BITFIELD_RO(GPIO37, 5, 1)
        ADD_BITFIELD_RO(GPIO36, 4, 1)
        ADD_BITFIELD_RO(GPIO35, 3, 1)
        ADD_BITFIELD_RO(GPIO34, 2, 1)
        ADD_BITFIELD_RO(GPIO33, 1, 1)
        ADD_BITFIELD_RO(GPIO32, 0, 1)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(IRQSUMMARY_PROC1_NONSECURE0_t, uint32_t)
        ADD_BITFIELD_RO(GPIO31, 31, 1)
        ADD_BITFIELD_RO(GPIO30, 30, 1)
        ADD_BITFIELD_RO(GPIO29, 29, 1)
        ADD_BITFIELD_RO(GPIO28, 28, 1)
        ADD_BITFIELD_RO(GPIO27, 27, 1)
        ADD_BITFIELD_RO(GPIO26, 26, 1)
        ADD_BITFIELD_RO(GPIO25, 25, 1)
        ADD_BITFIELD_RO(GPIO24, 24, 1)
        ADD_BITFIELD_RO(GPIO23, 23, 1)
        ADD_BITFIELD_RO(GPIO22, 22, 1)
        ADD_BITFIELD_RO(GPIO21, 21, 1)
        ADD_BITFIELD_RO(GPIO20, 20, 1)
        ADD_BITFIELD_RO(GPIO19, 19, 1)
        ADD_BITFIELD_RO(GPIO18, 18, 1)
        ADD_BITFIELD_RO(GPIO17, 17, 1)
        ADD_BITFIELD_RO(GPIO16, 16, 1)
        ADD_BITFIELD_RO(GPIO15, 15, 1)
        ADD_BITFIELD_RO(GPIO14, 14, 1)
        ADD_BITFIELD_RO(GPIO13, 13, 1)
        ADD_BITFIELD_RO(GPIO12, 12, 1)
        ADD_BITFIELD_RO(GPIO11, 11, 1)
        ADD_BITFIELD_RO(GPIO10, 10, 1)
        ADD_BITFIELD_RO(GPIO9, 9, 1)
        ADD_BITFIELD_RO(GPIO8, 8, 1)
        ADD_BITFIELD_RO(GPIO7, 7, 1)
        ADD_BITFIELD_RO(GPIO6, 6, 1)
        ADD_BITFIELD_RO(GPIO5, 5, 1)
        ADD_BITFIELD_RO(GPIO4, 4, 1)
        ADD_BITFIELD_RO(GPIO3, 3, 1)
        ADD_BITFIELD_RO(GPIO2, 2, 1)
        ADD_BITFIELD_RO(GPIO1, 1, 1)
        ADD_BITFIELD_RO(GPIO0, 0, 1)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(IRQSUMMARY_PROC1_NONSECURE1_t, uint32_t)
        ADD_BITFIELD_RO(GPIO47, 15, 1)
        ADD_BITFIELD_RO(GPIO46, 14, 1)
        ADD_BITFIELD_RO(GPIO45, 13, 1)
        ADD_BITFIELD_RO(GPIO44, 12, 1)
        ADD_BITFIELD_RO(GPIO43, 11, 1)
        ADD_BITFIELD_RO(GPIO42, 10, 1)
        ADD_BITFIELD_RO(GPIO41, 9, 1)
        ADD_BITFIELD_RO(GPIO40, 8, 1)
        ADD_BITFIELD_RO(GPIO39, 7, 1)
        ADD_BITFIELD_RO(GPIO38, 6, 1)
        ADD_BITFIELD_RO(GPIO37, 5, 1)
        ADD_BITFIELD_RO(GPIO36, 4, 1)
        ADD_BITFIELD_RO(GPIO35, 3, 1)
        ADD_BITFIELD_RO(GPIO34, 2, 1)
        ADD_BITFIELD_RO(GPIO33, 1, 1)
        ADD_BITFIELD_RO(GPIO32, 0, 1)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(IRQSUMMARY_DORMANT_WAKE_SECURE0_t, uint32_t)
        ADD_BITFIELD_RO(GPIO31, 31, 1)
        ADD_BITFIELD_RO(GPIO30, 30, 1)
        ADD_BITFIELD_RO(GPIO29, 29, 1)
        ADD_BITFIELD_RO(GPIO28, 28, 1)
        ADD_BITFIELD_RO(GPIO27, 27, 1)
        ADD_BITFIELD_RO(GPIO26, 26, 1)
        ADD_BITFIELD_RO(GPIO25, 25, 1)
        ADD_BITFIELD_RO(GPIO24, 24, 1)
        ADD_BITFIELD_RO(GPIO23, 23, 1)
        ADD_BITFIELD_RO(GPIO22, 22, 1)
        ADD_BITFIELD_RO(GPIO21, 21, 1)
        ADD_BITFIELD_RO(GPIO20, 20, 1)
        ADD_BITFIELD_RO(GPIO19, 19, 1)
        ADD_BITFIELD_RO(GPIO18, 18, 1)
        ADD_BITFIELD_RO(GPIO17, 17, 1)
        ADD_BITFIELD_RO(GPIO16, 16, 1)
        ADD_BITFIELD_RO(GPIO15, 15, 1)
        ADD_BITFIELD_RO(GPIO14, 14, 1)
        ADD_BITFIELD_RO(GPIO13, 13, 1)
        ADD_BITFIELD_RO(GPIO12, 12, 1)
        ADD_BITFIELD_RO(GPIO11, 11, 1)
        ADD_BITFIELD_RO(GPIO10, 10, 1)
        ADD_BITFIELD_RO(GPIO9, 9, 1)
        ADD_BITFIELD_RO(GPIO8, 8, 1)
        ADD_BITFIELD_RO(GPIO7, 7, 1)
        ADD_BITFIELD_RO(GPIO6, 6, 1)
        ADD_BITFIELD_RO(GPIO5, 5, 1)
        ADD_BITFIELD_RO(GPIO4, 4, 1)
        ADD_BITFIELD_RO(GPIO3, 3, 1)
        ADD_BITFIELD_RO(GPIO2, 2, 1)
        ADD_BITFIELD_RO(GPIO1, 1, 1)
        ADD_BITFIELD_RO(GPIO0, 0, 1)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(IRQSUMMARY_DORMANT_WAKE_SECURE1_t, uint32_t)
        ADD_BITFIELD_RO(GPIO47, 15, 1)
        ADD_BITFIELD_RO(GPIO46, 14, 1)
        ADD_BITFIELD_RO(GPIO45, 13, 1)
        ADD_BITFIELD_RO(GPIO44, 12, 1)
        ADD_BITFIELD_RO(GPIO43, 11, 1)
        ADD_BITFIELD_RO(GPIO42, 10, 1)
        ADD_BITFIELD_RO(GPIO41, 9, 1)
        ADD_BITFIELD_RO(GPIO40, 8, 1)
        ADD_BITFIELD_RO(GPIO39, 7, 1)
        ADD_BITFIELD_RO(GPIO38, 6, 1)
        ADD_BITFIELD_RO(GPIO37, 5, 1)
        ADD_BITFIELD_RO(GPIO36, 4, 1)
        ADD_BITFIELD_RO(GPIO35, 3, 1)
        ADD_BITFIELD_RO(GPIO34, 2, 1)
        ADD_BITFIELD_RO(GPIO33, 1, 1)
        ADD_BITFIELD_RO(GPIO32, 0, 1)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(IRQSUMMARY_DORMANT_WAKE_NONSECURE0_t, uint32_t)
        ADD_BITFIELD_RO(GPIO31, 31, 1)
        ADD_BITFIELD_RO(GPIO30, 30, 1)
        ADD_BITFIELD_RO(GPIO29, 29, 1)
        ADD_BITFIELD_RO(GPIO28, 28, 1)
        ADD_BITFIELD_RO(GPIO27, 27, 1)
        ADD_BITFIELD_RO(GPIO26, 26, 1)
        ADD_BITFIELD_RO(GPIO25, 25, 1)
        ADD_BITFIELD_RO(GPIO24, 24, 1)
        ADD_BITFIELD_RO(GPIO23, 23, 1)
        ADD_BITFIELD_RO(GPIO22, 22, 1)
        ADD_BITFIELD_RO(GPIO21, 21, 1)
        ADD_BITFIELD_RO(GPIO20, 20, 1)
        ADD_BITFIELD_RO(GPIO19, 19, 1)
        ADD_BITFIELD_RO(GPIO18, 18, 1)
        ADD_BITFIELD_RO(GPIO17, 17, 1)
        ADD_BITFIELD_RO(GPIO16, 16, 1)
        ADD_BITFIELD_RO(GPIO15, 15, 1)
        ADD_BITFIELD_RO(GPIO14, 14, 1)
        ADD_BITFIELD_RO(GPIO13, 13, 1)
        ADD_BITFIELD_RO(GPIO12, 12, 1)
        ADD_BITFIELD_RO(GPIO11, 11, 1)
        ADD_BITFIELD_RO(GPIO10, 10, 1)
        ADD_BITFIELD_RO(GPIO9, 9, 1)
        ADD_BITFIELD_RO(GPIO8, 8, 1)
        ADD_BITFIELD_RO(GPIO7, 7, 1)
        ADD_BITFIELD_RO(GPIO6, 6, 1)
        ADD_BITFIELD_RO(GPIO5, 5, 1)
        ADD_BITFIELD_RO(GPIO4, 4, 1)
        ADD_BITFIELD_RO(GPIO3, 3, 1)
        ADD_BITFIELD_RO(GPIO2, 2, 1)
        ADD_BITFIELD_RO(GPIO1, 1, 1)
        ADD_BITFIELD_RO(GPIO0, 0, 1)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(IRQSUMMARY_DORMANT_WAKE_NONSECURE1_t, uint32_t)
        ADD_BITFIELD_RO(GPIO47, 15, 1)
        ADD_BITFIELD_RO(GPIO46, 14, 1)
        ADD_BITFIELD_RO(GPIO45, 13, 1)
        ADD_BITFIELD_RO(GPIO44, 12, 1)
        ADD_BITFIELD_RO(GPIO43, 11, 1)
        ADD_BITFIELD_RO(GPIO42, 10, 1)
        ADD_BITFIELD_RO(GPIO41, 9, 1)
        ADD_BITFIELD_RO(GPIO40, 8, 1)
        ADD_BITFIELD_RO(GPIO39, 7, 1)
        ADD_BITFIELD_RO(GPIO38, 6, 1)
        ADD_BITFIELD_RO(GPIO37, 5, 1)
        ADD_BITFIELD_RO(GPIO36, 4, 1)
        ADD_BITFIELD_RO(GPIO35, 3, 1)
        ADD_BITFIELD_RO(GPIO34, 2, 1)
        ADD_BITFIELD_RO(GPIO33, 1, 1)
        ADD_BITFIELD_RO(GPIO32, 0, 1)
    END_TYPE()

    // Raw Interrupts
    // Reset value: 0x00000000
    BEGIN_TYPE(INTR0_t, uint32_t)
        ADD_BITFIELD_RW(GPIO7_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO7_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RO(GPIO7_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RO(GPIO7_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO6_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO6_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RO(GPIO6_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RO(GPIO6_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO5_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO5_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RO(GPIO5_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RO(GPIO5_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO4_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO4_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RO(GPIO4_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RO(GPIO4_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO3_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO3_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RO(GPIO3_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RO(GPIO3_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO2_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO2_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RO(GPIO2_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RO(GPIO2_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO1_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO1_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RO(GPIO1_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RO(GPIO1_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO0_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO0_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RO(GPIO0_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RO(GPIO0_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Raw Interrupts
    // Reset value: 0x00000000
    BEGIN_TYPE(INTR1_t, uint32_t)
        ADD_BITFIELD_RW(GPIO15_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO15_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RO(GPIO15_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RO(GPIO15_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO14_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO14_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RO(GPIO14_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RO(GPIO14_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO13_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO13_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RO(GPIO13_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RO(GPIO13_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO12_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO12_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RO(GPIO12_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RO(GPIO12_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO11_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO11_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RO(GPIO11_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RO(GPIO11_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO10_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO10_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RO(GPIO10_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RO(GPIO10_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO9_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO9_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RO(GPIO9_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RO(GPIO9_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO8_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO8_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RO(GPIO8_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RO(GPIO8_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Raw Interrupts
    // Reset value: 0x00000000
    BEGIN_TYPE(INTR2_t, uint32_t)
        ADD_BITFIELD_RW(GPIO23_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO23_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RO(GPIO23_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RO(GPIO23_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO22_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO22_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RO(GPIO22_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RO(GPIO22_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO21_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO21_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RO(GPIO21_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RO(GPIO21_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO20_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO20_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RO(GPIO20_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RO(GPIO20_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO19_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO19_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RO(GPIO19_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RO(GPIO19_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO18_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO18_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RO(GPIO18_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RO(GPIO18_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO17_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO17_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RO(GPIO17_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RO(GPIO17_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO16_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO16_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RO(GPIO16_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RO(GPIO16_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Raw Interrupts
    // Reset value: 0x00000000
    BEGIN_TYPE(INTR3_t, uint32_t)
        ADD_BITFIELD_RW(GPIO31_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO31_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RO(GPIO31_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RO(GPIO31_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO30_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO30_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RO(GPIO30_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RO(GPIO30_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO29_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO29_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RO(GPIO29_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RO(GPIO29_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO28_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO28_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RO(GPIO28_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RO(GPIO28_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO27_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO27_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RO(GPIO27_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RO(GPIO27_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO26_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO26_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RO(GPIO26_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RO(GPIO26_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO25_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO25_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RO(GPIO25_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RO(GPIO25_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO24_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO24_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RO(GPIO24_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RO(GPIO24_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Raw Interrupts
    // Reset value: 0x00000000
    BEGIN_TYPE(INTR4_t, uint32_t)
        ADD_BITFIELD_RW(GPIO39_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO39_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RO(GPIO39_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RO(GPIO39_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO38_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO38_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RO(GPIO38_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RO(GPIO38_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO37_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO37_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RO(GPIO37_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RO(GPIO37_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO36_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO36_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RO(GPIO36_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RO(GPIO36_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO35_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO35_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RO(GPIO35_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RO(GPIO35_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO34_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO34_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RO(GPIO34_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RO(GPIO34_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO33_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO33_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RO(GPIO33_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RO(GPIO33_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO32_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO32_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RO(GPIO32_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RO(GPIO32_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Raw Interrupts
    // Reset value: 0x00000000
    BEGIN_TYPE(INTR5_t, uint32_t)
        ADD_BITFIELD_RW(GPIO47_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO47_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RO(GPIO47_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RO(GPIO47_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO46_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO46_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RO(GPIO46_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RO(GPIO46_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO45_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO45_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RO(GPIO45_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RO(GPIO45_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO44_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO44_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RO(GPIO44_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RO(GPIO44_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO43_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO43_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RO(GPIO43_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RO(GPIO43_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO42_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO42_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RO(GPIO42_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RO(GPIO42_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO41_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO41_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RO(GPIO41_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RO(GPIO41_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO40_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO40_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RO(GPIO40_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RO(GPIO40_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Enable for proc0
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC0_INTE0_t, uint32_t)
        ADD_BITFIELD_RW(GPIO7_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO7_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO7_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO7_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO6_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO6_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO6_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO6_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO5_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO5_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO5_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO5_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO4_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO4_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO4_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO4_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO3_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO3_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO3_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO3_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO2_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO2_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO2_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO2_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO1_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO1_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO1_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO1_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO0_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO0_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO0_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO0_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Enable for proc0
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC0_INTE1_t, uint32_t)
        ADD_BITFIELD_RW(GPIO15_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO15_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO15_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO15_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO14_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO14_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO14_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO14_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO13_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO13_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO13_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO13_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO12_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO12_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO12_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO12_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO11_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO11_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO11_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO11_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO10_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO10_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO10_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO10_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO9_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO9_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO9_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO9_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO8_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO8_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO8_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO8_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Enable for proc0
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC0_INTE2_t, uint32_t)
        ADD_BITFIELD_RW(GPIO23_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO23_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO23_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO23_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO22_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO22_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO22_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO22_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO21_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO21_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO21_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO21_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO20_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO20_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO20_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO20_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO19_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO19_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO19_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO19_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO18_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO18_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO18_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO18_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO17_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO17_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO17_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO17_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO16_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO16_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO16_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO16_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Enable for proc0
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC0_INTE3_t, uint32_t)
        ADD_BITFIELD_RW(GPIO31_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO31_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO31_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO31_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO30_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO30_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO30_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO30_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO29_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO29_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO29_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO29_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO28_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO28_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO28_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO28_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO27_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO27_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO27_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO27_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO26_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO26_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO26_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO26_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO25_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO25_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO25_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO25_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO24_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO24_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO24_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO24_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Enable for proc0
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC0_INTE4_t, uint32_t)
        ADD_BITFIELD_RW(GPIO39_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO39_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO39_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO39_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO38_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO38_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO38_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO38_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO37_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO37_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO37_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO37_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO36_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO36_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO36_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO36_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO35_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO35_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO35_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO35_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO34_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO34_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO34_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO34_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO33_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO33_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO33_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO33_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO32_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO32_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO32_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO32_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Enable for proc0
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC0_INTE5_t, uint32_t)
        ADD_BITFIELD_RW(GPIO47_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO47_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO47_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO47_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO46_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO46_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO46_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO46_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO45_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO45_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO45_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO45_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO44_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO44_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO44_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO44_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO43_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO43_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO43_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO43_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO42_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO42_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO42_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO42_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO41_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO41_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO41_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO41_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO40_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO40_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO40_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO40_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Force for proc0
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC0_INTF0_t, uint32_t)
        ADD_BITFIELD_RW(GPIO7_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO7_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO7_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO7_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO6_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO6_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO6_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO6_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO5_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO5_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO5_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO5_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO4_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO4_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO4_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO4_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO3_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO3_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO3_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO3_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO2_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO2_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO2_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO2_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO1_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO1_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO1_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO1_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO0_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO0_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO0_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO0_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Force for proc0
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC0_INTF1_t, uint32_t)
        ADD_BITFIELD_RW(GPIO15_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO15_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO15_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO15_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO14_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO14_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO14_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO14_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO13_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO13_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO13_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO13_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO12_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO12_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO12_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO12_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO11_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO11_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO11_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO11_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO10_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO10_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO10_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO10_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO9_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO9_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO9_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO9_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO8_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO8_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO8_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO8_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Force for proc0
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC0_INTF2_t, uint32_t)
        ADD_BITFIELD_RW(GPIO23_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO23_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO23_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO23_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO22_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO22_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO22_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO22_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO21_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO21_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO21_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO21_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO20_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO20_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO20_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO20_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO19_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO19_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO19_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO19_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO18_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO18_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO18_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO18_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO17_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO17_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO17_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO17_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO16_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO16_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO16_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO16_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Force for proc0
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC0_INTF3_t, uint32_t)
        ADD_BITFIELD_RW(GPIO31_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO31_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO31_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO31_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO30_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO30_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO30_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO30_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO29_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO29_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO29_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO29_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO28_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO28_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO28_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO28_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO27_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO27_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO27_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO27_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO26_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO26_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO26_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO26_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO25_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO25_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO25_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO25_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO24_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO24_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO24_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO24_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Force for proc0
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC0_INTF4_t, uint32_t)
        ADD_BITFIELD_RW(GPIO39_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO39_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO39_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO39_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO38_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO38_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO38_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO38_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO37_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO37_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO37_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO37_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO36_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO36_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO36_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO36_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO35_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO35_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO35_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO35_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO34_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO34_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO34_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO34_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO33_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO33_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO33_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO33_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO32_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO32_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO32_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO32_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Force for proc0
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC0_INTF5_t, uint32_t)
        ADD_BITFIELD_RW(GPIO47_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO47_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO47_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO47_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO46_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO46_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO46_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO46_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO45_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO45_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO45_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO45_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO44_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO44_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO44_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO44_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO43_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO43_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO43_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO43_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO42_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO42_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO42_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO42_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO41_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO41_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO41_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO41_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO40_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO40_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO40_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO40_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt status after masking & forcing for proc0
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC0_INTS0_t, uint32_t)
        ADD_BITFIELD_RO(GPIO7_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RO(GPIO7_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RO(GPIO7_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RO(GPIO7_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RO(GPIO6_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RO(GPIO6_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RO(GPIO6_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RO(GPIO6_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RO(GPIO5_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RO(GPIO5_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RO(GPIO5_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RO(GPIO5_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RO(GPIO4_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RO(GPIO4_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RO(GPIO4_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RO(GPIO4_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RO(GPIO3_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RO(GPIO3_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RO(GPIO3_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RO(GPIO3_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RO(GPIO2_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RO(GPIO2_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RO(GPIO2_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RO(GPIO2_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RO(GPIO1_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RO(GPIO1_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RO(GPIO1_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RO(GPIO1_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RO(GPIO0_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RO(GPIO0_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RO(GPIO0_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RO(GPIO0_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt status after masking & forcing for proc0
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC0_INTS1_t, uint32_t)
        ADD_BITFIELD_RO(GPIO15_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RO(GPIO15_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RO(GPIO15_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RO(GPIO15_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RO(GPIO14_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RO(GPIO14_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RO(GPIO14_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RO(GPIO14_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RO(GPIO13_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RO(GPIO13_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RO(GPIO13_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RO(GPIO13_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RO(GPIO12_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RO(GPIO12_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RO(GPIO12_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RO(GPIO12_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RO(GPIO11_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RO(GPIO11_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RO(GPIO11_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RO(GPIO11_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RO(GPIO10_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RO(GPIO10_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RO(GPIO10_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RO(GPIO10_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RO(GPIO9_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RO(GPIO9_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RO(GPIO9_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RO(GPIO9_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RO(GPIO8_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RO(GPIO8_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RO(GPIO8_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RO(GPIO8_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt status after masking & forcing for proc0
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC0_INTS2_t, uint32_t)
        ADD_BITFIELD_RO(GPIO23_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RO(GPIO23_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RO(GPIO23_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RO(GPIO23_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RO(GPIO22_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RO(GPIO22_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RO(GPIO22_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RO(GPIO22_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RO(GPIO21_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RO(GPIO21_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RO(GPIO21_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RO(GPIO21_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RO(GPIO20_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RO(GPIO20_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RO(GPIO20_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RO(GPIO20_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RO(GPIO19_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RO(GPIO19_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RO(GPIO19_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RO(GPIO19_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RO(GPIO18_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RO(GPIO18_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RO(GPIO18_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RO(GPIO18_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RO(GPIO17_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RO(GPIO17_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RO(GPIO17_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RO(GPIO17_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RO(GPIO16_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RO(GPIO16_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RO(GPIO16_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RO(GPIO16_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt status after masking & forcing for proc0
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC0_INTS3_t, uint32_t)
        ADD_BITFIELD_RO(GPIO31_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RO(GPIO31_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RO(GPIO31_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RO(GPIO31_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RO(GPIO30_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RO(GPIO30_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RO(GPIO30_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RO(GPIO30_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RO(GPIO29_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RO(GPIO29_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RO(GPIO29_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RO(GPIO29_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RO(GPIO28_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RO(GPIO28_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RO(GPIO28_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RO(GPIO28_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RO(GPIO27_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RO(GPIO27_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RO(GPIO27_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RO(GPIO27_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RO(GPIO26_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RO(GPIO26_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RO(GPIO26_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RO(GPIO26_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RO(GPIO25_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RO(GPIO25_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RO(GPIO25_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RO(GPIO25_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RO(GPIO24_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RO(GPIO24_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RO(GPIO24_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RO(GPIO24_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt status after masking & forcing for proc0
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC0_INTS4_t, uint32_t)
        ADD_BITFIELD_RO(GPIO39_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RO(GPIO39_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RO(GPIO39_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RO(GPIO39_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RO(GPIO38_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RO(GPIO38_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RO(GPIO38_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RO(GPIO38_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RO(GPIO37_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RO(GPIO37_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RO(GPIO37_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RO(GPIO37_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RO(GPIO36_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RO(GPIO36_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RO(GPIO36_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RO(GPIO36_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RO(GPIO35_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RO(GPIO35_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RO(GPIO35_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RO(GPIO35_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RO(GPIO34_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RO(GPIO34_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RO(GPIO34_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RO(GPIO34_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RO(GPIO33_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RO(GPIO33_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RO(GPIO33_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RO(GPIO33_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RO(GPIO32_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RO(GPIO32_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RO(GPIO32_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RO(GPIO32_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt status after masking & forcing for proc0
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC0_INTS5_t, uint32_t)
        ADD_BITFIELD_RO(GPIO47_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RO(GPIO47_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RO(GPIO47_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RO(GPIO47_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RO(GPIO46_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RO(GPIO46_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RO(GPIO46_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RO(GPIO46_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RO(GPIO45_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RO(GPIO45_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RO(GPIO45_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RO(GPIO45_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RO(GPIO44_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RO(GPIO44_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RO(GPIO44_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RO(GPIO44_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RO(GPIO43_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RO(GPIO43_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RO(GPIO43_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RO(GPIO43_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RO(GPIO42_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RO(GPIO42_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RO(GPIO42_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RO(GPIO42_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RO(GPIO41_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RO(GPIO41_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RO(GPIO41_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RO(GPIO41_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RO(GPIO40_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RO(GPIO40_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RO(GPIO40_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RO(GPIO40_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Enable for proc1
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC1_INTE0_t, uint32_t)
        ADD_BITFIELD_RW(GPIO7_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO7_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO7_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO7_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO6_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO6_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO6_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO6_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO5_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO5_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO5_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO5_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO4_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO4_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO4_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO4_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO3_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO3_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO3_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO3_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO2_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO2_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO2_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO2_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO1_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO1_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO1_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO1_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO0_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO0_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO0_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO0_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Enable for proc1
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC1_INTE1_t, uint32_t)
        ADD_BITFIELD_RW(GPIO15_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO15_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO15_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO15_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO14_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO14_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO14_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO14_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO13_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO13_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO13_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO13_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO12_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO12_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO12_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO12_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO11_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO11_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO11_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO11_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO10_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO10_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO10_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO10_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO9_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO9_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO9_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO9_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO8_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO8_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO8_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO8_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Enable for proc1
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC1_INTE2_t, uint32_t)
        ADD_BITFIELD_RW(GPIO23_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO23_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO23_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO23_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO22_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO22_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO22_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO22_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO21_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO21_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO21_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO21_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO20_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO20_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO20_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO20_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO19_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO19_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO19_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO19_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO18_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO18_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO18_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO18_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO17_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO17_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO17_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO17_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO16_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO16_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO16_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO16_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Enable for proc1
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC1_INTE3_t, uint32_t)
        ADD_BITFIELD_RW(GPIO31_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO31_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO31_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO31_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO30_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO30_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO30_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO30_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO29_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO29_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO29_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO29_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO28_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO28_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO28_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO28_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO27_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO27_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO27_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO27_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO26_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO26_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO26_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO26_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO25_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO25_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO25_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO25_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO24_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO24_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO24_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO24_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Enable for proc1
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC1_INTE4_t, uint32_t)
        ADD_BITFIELD_RW(GPIO39_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO39_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO39_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO39_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO38_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO38_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO38_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO38_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO37_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO37_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO37_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO37_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO36_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO36_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO36_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO36_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO35_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO35_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO35_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO35_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO34_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO34_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO34_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO34_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO33_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO33_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO33_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO33_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO32_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO32_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO32_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO32_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Enable for proc1
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC1_INTE5_t, uint32_t)
        ADD_BITFIELD_RW(GPIO47_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO47_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO47_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO47_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO46_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO46_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO46_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO46_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO45_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO45_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO45_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO45_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO44_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO44_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO44_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO44_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO43_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO43_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO43_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO43_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO42_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO42_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO42_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO42_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO41_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO41_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO41_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO41_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO40_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO40_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO40_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO40_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Force for proc1
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC1_INTF0_t, uint32_t)
        ADD_BITFIELD_RW(GPIO7_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO7_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO7_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO7_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO6_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO6_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO6_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO6_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO5_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO5_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO5_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO5_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO4_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO4_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO4_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO4_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO3_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO3_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO3_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO3_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO2_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO2_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO2_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO2_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO1_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO1_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO1_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO1_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO0_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO0_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO0_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO0_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Force for proc1
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC1_INTF1_t, uint32_t)
        ADD_BITFIELD_RW(GPIO15_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO15_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO15_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO15_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO14_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO14_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO14_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO14_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO13_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO13_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO13_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO13_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO12_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO12_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO12_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO12_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO11_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO11_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO11_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO11_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO10_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO10_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO10_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO10_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO9_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO9_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO9_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO9_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO8_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO8_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO8_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO8_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Force for proc1
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC1_INTF2_t, uint32_t)
        ADD_BITFIELD_RW(GPIO23_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO23_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO23_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO23_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO22_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO22_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO22_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO22_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO21_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO21_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO21_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO21_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO20_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO20_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO20_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO20_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO19_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO19_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO19_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO19_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO18_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO18_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO18_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO18_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO17_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO17_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO17_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO17_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO16_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO16_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO16_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO16_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Force for proc1
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC1_INTF3_t, uint32_t)
        ADD_BITFIELD_RW(GPIO31_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO31_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO31_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO31_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO30_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO30_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO30_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO30_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO29_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO29_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO29_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO29_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO28_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO28_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO28_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO28_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO27_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO27_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO27_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO27_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO26_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO26_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO26_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO26_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO25_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO25_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO25_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO25_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO24_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO24_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO24_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO24_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Force for proc1
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC1_INTF4_t, uint32_t)
        ADD_BITFIELD_RW(GPIO39_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO39_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO39_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO39_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO38_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO38_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO38_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO38_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO37_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO37_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO37_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO37_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO36_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO36_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO36_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO36_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO35_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO35_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO35_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO35_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO34_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO34_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO34_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO34_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO33_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO33_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO33_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO33_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO32_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO32_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO32_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO32_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Force for proc1
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC1_INTF5_t, uint32_t)
        ADD_BITFIELD_RW(GPIO47_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO47_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO47_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO47_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO46_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO46_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO46_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO46_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO45_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO45_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO45_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO45_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO44_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO44_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO44_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO44_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO43_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO43_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO43_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO43_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO42_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO42_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO42_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO42_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO41_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO41_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO41_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO41_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO40_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO40_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO40_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO40_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt status after masking & forcing for proc1
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC1_INTS0_t, uint32_t)
        ADD_BITFIELD_RO(GPIO7_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RO(GPIO7_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RO(GPIO7_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RO(GPIO7_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RO(GPIO6_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RO(GPIO6_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RO(GPIO6_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RO(GPIO6_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RO(GPIO5_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RO(GPIO5_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RO(GPIO5_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RO(GPIO5_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RO(GPIO4_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RO(GPIO4_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RO(GPIO4_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RO(GPIO4_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RO(GPIO3_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RO(GPIO3_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RO(GPIO3_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RO(GPIO3_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RO(GPIO2_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RO(GPIO2_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RO(GPIO2_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RO(GPIO2_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RO(GPIO1_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RO(GPIO1_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RO(GPIO1_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RO(GPIO1_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RO(GPIO0_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RO(GPIO0_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RO(GPIO0_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RO(GPIO0_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt status after masking & forcing for proc1
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC1_INTS1_t, uint32_t)
        ADD_BITFIELD_RO(GPIO15_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RO(GPIO15_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RO(GPIO15_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RO(GPIO15_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RO(GPIO14_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RO(GPIO14_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RO(GPIO14_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RO(GPIO14_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RO(GPIO13_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RO(GPIO13_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RO(GPIO13_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RO(GPIO13_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RO(GPIO12_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RO(GPIO12_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RO(GPIO12_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RO(GPIO12_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RO(GPIO11_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RO(GPIO11_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RO(GPIO11_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RO(GPIO11_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RO(GPIO10_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RO(GPIO10_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RO(GPIO10_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RO(GPIO10_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RO(GPIO9_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RO(GPIO9_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RO(GPIO9_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RO(GPIO9_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RO(GPIO8_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RO(GPIO8_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RO(GPIO8_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RO(GPIO8_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt status after masking & forcing for proc1
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC1_INTS2_t, uint32_t)
        ADD_BITFIELD_RO(GPIO23_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RO(GPIO23_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RO(GPIO23_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RO(GPIO23_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RO(GPIO22_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RO(GPIO22_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RO(GPIO22_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RO(GPIO22_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RO(GPIO21_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RO(GPIO21_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RO(GPIO21_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RO(GPIO21_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RO(GPIO20_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RO(GPIO20_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RO(GPIO20_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RO(GPIO20_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RO(GPIO19_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RO(GPIO19_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RO(GPIO19_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RO(GPIO19_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RO(GPIO18_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RO(GPIO18_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RO(GPIO18_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RO(GPIO18_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RO(GPIO17_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RO(GPIO17_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RO(GPIO17_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RO(GPIO17_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RO(GPIO16_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RO(GPIO16_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RO(GPIO16_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RO(GPIO16_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt status after masking & forcing for proc1
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC1_INTS3_t, uint32_t)
        ADD_BITFIELD_RO(GPIO31_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RO(GPIO31_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RO(GPIO31_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RO(GPIO31_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RO(GPIO30_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RO(GPIO30_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RO(GPIO30_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RO(GPIO30_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RO(GPIO29_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RO(GPIO29_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RO(GPIO29_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RO(GPIO29_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RO(GPIO28_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RO(GPIO28_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RO(GPIO28_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RO(GPIO28_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RO(GPIO27_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RO(GPIO27_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RO(GPIO27_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RO(GPIO27_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RO(GPIO26_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RO(GPIO26_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RO(GPIO26_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RO(GPIO26_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RO(GPIO25_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RO(GPIO25_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RO(GPIO25_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RO(GPIO25_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RO(GPIO24_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RO(GPIO24_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RO(GPIO24_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RO(GPIO24_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt status after masking & forcing for proc1
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC1_INTS4_t, uint32_t)
        ADD_BITFIELD_RO(GPIO39_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RO(GPIO39_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RO(GPIO39_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RO(GPIO39_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RO(GPIO38_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RO(GPIO38_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RO(GPIO38_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RO(GPIO38_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RO(GPIO37_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RO(GPIO37_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RO(GPIO37_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RO(GPIO37_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RO(GPIO36_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RO(GPIO36_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RO(GPIO36_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RO(GPIO36_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RO(GPIO35_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RO(GPIO35_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RO(GPIO35_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RO(GPIO35_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RO(GPIO34_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RO(GPIO34_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RO(GPIO34_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RO(GPIO34_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RO(GPIO33_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RO(GPIO33_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RO(GPIO33_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RO(GPIO33_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RO(GPIO32_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RO(GPIO32_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RO(GPIO32_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RO(GPIO32_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt status after masking & forcing for proc1
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC1_INTS5_t, uint32_t)
        ADD_BITFIELD_RO(GPIO47_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RO(GPIO47_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RO(GPIO47_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RO(GPIO47_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RO(GPIO46_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RO(GPIO46_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RO(GPIO46_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RO(GPIO46_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RO(GPIO45_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RO(GPIO45_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RO(GPIO45_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RO(GPIO45_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RO(GPIO44_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RO(GPIO44_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RO(GPIO44_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RO(GPIO44_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RO(GPIO43_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RO(GPIO43_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RO(GPIO43_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RO(GPIO43_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RO(GPIO42_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RO(GPIO42_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RO(GPIO42_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RO(GPIO42_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RO(GPIO41_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RO(GPIO41_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RO(GPIO41_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RO(GPIO41_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RO(GPIO40_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RO(GPIO40_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RO(GPIO40_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RO(GPIO40_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Enable for dormant_wake
    // Reset value: 0x00000000
    BEGIN_TYPE(DORMANT_WAKE_INTE0_t, uint32_t)
        ADD_BITFIELD_RW(GPIO7_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO7_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO7_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO7_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO6_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO6_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO6_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO6_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO5_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO5_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO5_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO5_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO4_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO4_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO4_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO4_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO3_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO3_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO3_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO3_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO2_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO2_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO2_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO2_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO1_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO1_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO1_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO1_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO0_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO0_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO0_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO0_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Enable for dormant_wake
    // Reset value: 0x00000000
    BEGIN_TYPE(DORMANT_WAKE_INTE1_t, uint32_t)
        ADD_BITFIELD_RW(GPIO15_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO15_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO15_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO15_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO14_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO14_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO14_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO14_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO13_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO13_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO13_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO13_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO12_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO12_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO12_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO12_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO11_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO11_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO11_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO11_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO10_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO10_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO10_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO10_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO9_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO9_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO9_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO9_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO8_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO8_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO8_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO8_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Enable for dormant_wake
    // Reset value: 0x00000000
    BEGIN_TYPE(DORMANT_WAKE_INTE2_t, uint32_t)
        ADD_BITFIELD_RW(GPIO23_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO23_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO23_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO23_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO22_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO22_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO22_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO22_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO21_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO21_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO21_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO21_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO20_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO20_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO20_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO20_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO19_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO19_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO19_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO19_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO18_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO18_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO18_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO18_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO17_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO17_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO17_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO17_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO16_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO16_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO16_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO16_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Enable for dormant_wake
    // Reset value: 0x00000000
    BEGIN_TYPE(DORMANT_WAKE_INTE3_t, uint32_t)
        ADD_BITFIELD_RW(GPIO31_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO31_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO31_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO31_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO30_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO30_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO30_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO30_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO29_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO29_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO29_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO29_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO28_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO28_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO28_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO28_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO27_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO27_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO27_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO27_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO26_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO26_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO26_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO26_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO25_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO25_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO25_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO25_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO24_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO24_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO24_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO24_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Enable for dormant_wake
    // Reset value: 0x00000000
    BEGIN_TYPE(DORMANT_WAKE_INTE4_t, uint32_t)
        ADD_BITFIELD_RW(GPIO39_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO39_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO39_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO39_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO38_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO38_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO38_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO38_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO37_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO37_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO37_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO37_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO36_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO36_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO36_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO36_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO35_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO35_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO35_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO35_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO34_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO34_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO34_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO34_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO33_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO33_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO33_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO33_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO32_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO32_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO32_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO32_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Enable for dormant_wake
    // Reset value: 0x00000000
    BEGIN_TYPE(DORMANT_WAKE_INTE5_t, uint32_t)
        ADD_BITFIELD_RW(GPIO47_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO47_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO47_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO47_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO46_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO46_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO46_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO46_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO45_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO45_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO45_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO45_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO44_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO44_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO44_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO44_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO43_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO43_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO43_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO43_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO42_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO42_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO42_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO42_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO41_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO41_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO41_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO41_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO40_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO40_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO40_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO40_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Force for dormant_wake
    // Reset value: 0x00000000
    BEGIN_TYPE(DORMANT_WAKE_INTF0_t, uint32_t)
        ADD_BITFIELD_RW(GPIO7_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO7_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO7_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO7_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO6_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO6_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO6_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO6_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO5_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO5_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO5_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO5_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO4_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO4_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO4_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO4_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO3_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO3_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO3_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO3_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO2_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO2_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO2_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO2_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO1_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO1_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO1_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO1_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO0_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO0_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO0_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO0_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Force for dormant_wake
    // Reset value: 0x00000000
    BEGIN_TYPE(DORMANT_WAKE_INTF1_t, uint32_t)
        ADD_BITFIELD_RW(GPIO15_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO15_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO15_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO15_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO14_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO14_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO14_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO14_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO13_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO13_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO13_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO13_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO12_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO12_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO12_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO12_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO11_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO11_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO11_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO11_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO10_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO10_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO10_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO10_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO9_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO9_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO9_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO9_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO8_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO8_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO8_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO8_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Force for dormant_wake
    // Reset value: 0x00000000
    BEGIN_TYPE(DORMANT_WAKE_INTF2_t, uint32_t)
        ADD_BITFIELD_RW(GPIO23_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO23_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO23_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO23_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO22_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO22_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO22_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO22_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO21_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO21_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO21_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO21_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO20_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO20_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO20_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO20_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO19_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO19_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO19_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO19_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO18_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO18_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO18_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO18_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO17_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO17_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO17_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO17_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO16_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO16_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO16_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO16_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Force for dormant_wake
    // Reset value: 0x00000000
    BEGIN_TYPE(DORMANT_WAKE_INTF3_t, uint32_t)
        ADD_BITFIELD_RW(GPIO31_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO31_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO31_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO31_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO30_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO30_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO30_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO30_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO29_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO29_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO29_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO29_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO28_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO28_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO28_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO28_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO27_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO27_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO27_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO27_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO26_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO26_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO26_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO26_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO25_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO25_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO25_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO25_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO24_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO24_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO24_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO24_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Force for dormant_wake
    // Reset value: 0x00000000
    BEGIN_TYPE(DORMANT_WAKE_INTF4_t, uint32_t)
        ADD_BITFIELD_RW(GPIO39_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO39_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO39_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO39_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO38_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO38_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO38_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO38_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO37_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO37_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO37_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO37_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO36_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO36_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO36_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO36_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO35_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO35_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO35_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO35_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO34_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO34_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO34_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO34_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO33_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO33_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO33_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO33_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO32_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO32_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO32_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO32_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt Force for dormant_wake
    // Reset value: 0x00000000
    BEGIN_TYPE(DORMANT_WAKE_INTF5_t, uint32_t)
        ADD_BITFIELD_RW(GPIO47_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RW(GPIO47_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RW(GPIO47_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RW(GPIO47_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RW(GPIO46_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RW(GPIO46_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RW(GPIO46_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RW(GPIO46_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RW(GPIO45_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RW(GPIO45_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RW(GPIO45_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RW(GPIO45_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RW(GPIO44_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RW(GPIO44_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RW(GPIO44_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RW(GPIO44_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RW(GPIO43_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RW(GPIO43_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RW(GPIO43_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RW(GPIO43_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RW(GPIO42_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RW(GPIO42_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RW(GPIO42_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RW(GPIO42_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RW(GPIO41_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RW(GPIO41_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RW(GPIO41_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RW(GPIO41_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RW(GPIO40_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RW(GPIO40_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RW(GPIO40_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RW(GPIO40_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt status after masking & forcing for dormant_wake
    // Reset value: 0x00000000
    BEGIN_TYPE(DORMANT_WAKE_INTS0_t, uint32_t)
        ADD_BITFIELD_RO(GPIO7_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RO(GPIO7_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RO(GPIO7_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RO(GPIO7_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RO(GPIO6_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RO(GPIO6_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RO(GPIO6_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RO(GPIO6_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RO(GPIO5_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RO(GPIO5_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RO(GPIO5_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RO(GPIO5_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RO(GPIO4_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RO(GPIO4_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RO(GPIO4_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RO(GPIO4_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RO(GPIO3_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RO(GPIO3_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RO(GPIO3_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RO(GPIO3_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RO(GPIO2_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RO(GPIO2_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RO(GPIO2_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RO(GPIO2_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RO(GPIO1_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RO(GPIO1_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RO(GPIO1_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RO(GPIO1_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RO(GPIO0_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RO(GPIO0_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RO(GPIO0_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RO(GPIO0_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt status after masking & forcing for dormant_wake
    // Reset value: 0x00000000
    BEGIN_TYPE(DORMANT_WAKE_INTS1_t, uint32_t)
        ADD_BITFIELD_RO(GPIO15_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RO(GPIO15_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RO(GPIO15_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RO(GPIO15_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RO(GPIO14_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RO(GPIO14_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RO(GPIO14_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RO(GPIO14_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RO(GPIO13_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RO(GPIO13_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RO(GPIO13_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RO(GPIO13_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RO(GPIO12_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RO(GPIO12_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RO(GPIO12_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RO(GPIO12_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RO(GPIO11_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RO(GPIO11_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RO(GPIO11_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RO(GPIO11_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RO(GPIO10_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RO(GPIO10_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RO(GPIO10_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RO(GPIO10_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RO(GPIO9_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RO(GPIO9_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RO(GPIO9_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RO(GPIO9_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RO(GPIO8_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RO(GPIO8_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RO(GPIO8_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RO(GPIO8_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt status after masking & forcing for dormant_wake
    // Reset value: 0x00000000
    BEGIN_TYPE(DORMANT_WAKE_INTS2_t, uint32_t)
        ADD_BITFIELD_RO(GPIO23_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RO(GPIO23_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RO(GPIO23_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RO(GPIO23_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RO(GPIO22_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RO(GPIO22_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RO(GPIO22_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RO(GPIO22_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RO(GPIO21_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RO(GPIO21_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RO(GPIO21_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RO(GPIO21_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RO(GPIO20_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RO(GPIO20_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RO(GPIO20_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RO(GPIO20_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RO(GPIO19_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RO(GPIO19_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RO(GPIO19_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RO(GPIO19_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RO(GPIO18_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RO(GPIO18_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RO(GPIO18_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RO(GPIO18_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RO(GPIO17_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RO(GPIO17_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RO(GPIO17_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RO(GPIO17_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RO(GPIO16_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RO(GPIO16_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RO(GPIO16_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RO(GPIO16_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt status after masking & forcing for dormant_wake
    // Reset value: 0x00000000
    BEGIN_TYPE(DORMANT_WAKE_INTS3_t, uint32_t)
        ADD_BITFIELD_RO(GPIO31_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RO(GPIO31_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RO(GPIO31_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RO(GPIO31_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RO(GPIO30_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RO(GPIO30_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RO(GPIO30_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RO(GPIO30_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RO(GPIO29_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RO(GPIO29_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RO(GPIO29_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RO(GPIO29_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RO(GPIO28_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RO(GPIO28_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RO(GPIO28_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RO(GPIO28_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RO(GPIO27_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RO(GPIO27_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RO(GPIO27_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RO(GPIO27_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RO(GPIO26_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RO(GPIO26_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RO(GPIO26_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RO(GPIO26_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RO(GPIO25_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RO(GPIO25_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RO(GPIO25_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RO(GPIO25_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RO(GPIO24_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RO(GPIO24_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RO(GPIO24_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RO(GPIO24_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt status after masking & forcing for dormant_wake
    // Reset value: 0x00000000
    BEGIN_TYPE(DORMANT_WAKE_INTS4_t, uint32_t)
        ADD_BITFIELD_RO(GPIO39_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RO(GPIO39_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RO(GPIO39_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RO(GPIO39_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RO(GPIO38_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RO(GPIO38_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RO(GPIO38_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RO(GPIO38_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RO(GPIO37_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RO(GPIO37_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RO(GPIO37_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RO(GPIO37_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RO(GPIO36_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RO(GPIO36_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RO(GPIO36_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RO(GPIO36_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RO(GPIO35_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RO(GPIO35_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RO(GPIO35_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RO(GPIO35_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RO(GPIO34_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RO(GPIO34_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RO(GPIO34_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RO(GPIO34_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RO(GPIO33_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RO(GPIO33_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RO(GPIO33_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RO(GPIO33_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RO(GPIO32_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RO(GPIO32_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RO(GPIO32_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RO(GPIO32_LEVEL_LOW, 0, 1)
    END_TYPE()

    // Interrupt status after masking & forcing for dormant_wake
    // Reset value: 0x00000000
    BEGIN_TYPE(DORMANT_WAKE_INTS5_t, uint32_t)
        ADD_BITFIELD_RO(GPIO47_EDGE_HIGH, 31, 1)
        ADD_BITFIELD_RO(GPIO47_EDGE_LOW, 30, 1)
        ADD_BITFIELD_RO(GPIO47_LEVEL_HIGH, 29, 1)
        ADD_BITFIELD_RO(GPIO47_LEVEL_LOW, 28, 1)
        ADD_BITFIELD_RO(GPIO46_EDGE_HIGH, 27, 1)
        ADD_BITFIELD_RO(GPIO46_EDGE_LOW, 26, 1)
        ADD_BITFIELD_RO(GPIO46_LEVEL_HIGH, 25, 1)
        ADD_BITFIELD_RO(GPIO46_LEVEL_LOW, 24, 1)
        ADD_BITFIELD_RO(GPIO45_EDGE_HIGH, 23, 1)
        ADD_BITFIELD_RO(GPIO45_EDGE_LOW, 22, 1)
        ADD_BITFIELD_RO(GPIO45_LEVEL_HIGH, 21, 1)
        ADD_BITFIELD_RO(GPIO45_LEVEL_LOW, 20, 1)
        ADD_BITFIELD_RO(GPIO44_EDGE_HIGH, 19, 1)
        ADD_BITFIELD_RO(GPIO44_EDGE_LOW, 18, 1)
        ADD_BITFIELD_RO(GPIO44_LEVEL_HIGH, 17, 1)
        ADD_BITFIELD_RO(GPIO44_LEVEL_LOW, 16, 1)
        ADD_BITFIELD_RO(GPIO43_EDGE_HIGH, 15, 1)
        ADD_BITFIELD_RO(GPIO43_EDGE_LOW, 14, 1)
        ADD_BITFIELD_RO(GPIO43_LEVEL_HIGH, 13, 1)
        ADD_BITFIELD_RO(GPIO43_LEVEL_LOW, 12, 1)
        ADD_BITFIELD_RO(GPIO42_EDGE_HIGH, 11, 1)
        ADD_BITFIELD_RO(GPIO42_EDGE_LOW, 10, 1)
        ADD_BITFIELD_RO(GPIO42_LEVEL_HIGH, 9, 1)
        ADD_BITFIELD_RO(GPIO42_LEVEL_LOW, 8, 1)
        ADD_BITFIELD_RO(GPIO41_EDGE_HIGH, 7, 1)
        ADD_BITFIELD_RO(GPIO41_EDGE_LOW, 6, 1)
        ADD_BITFIELD_RO(GPIO41_LEVEL_HIGH, 5, 1)
        ADD_BITFIELD_RO(GPIO41_LEVEL_LOW, 4, 1)
        ADD_BITFIELD_RO(GPIO40_EDGE_HIGH, 3, 1)
        ADD_BITFIELD_RO(GPIO40_EDGE_LOW, 2, 1)
        ADD_BITFIELD_RO(GPIO40_LEVEL_HIGH, 1, 1)
        ADD_BITFIELD_RO(GPIO40_LEVEL_LOW, 0, 1)
    END_TYPE()

    struct IO_BANK0_t {
        GPIO_STATUS_t                 GPIO0_STATUS;
        GPIO_CTRL_t                   GPIO0_CTRL;
        GPIO_STATUS_t                 GPIO1_STATUS;
        GPIO_CTRL_t                   GPIO1_CTRL;
        GPIO_STATUS_t                 GPIO2_STATUS;
        GPIO_CTRL_t                   GPIO2_CTRL;
        GPIO_STATUS_t                 GPIO3_STATUS;
        GPIO_CTRL_t                   GPIO3_CTRL;
        GPIO_STATUS_t                 GPIO4_STATUS;
        GPIO_CTRL_t                   GPIO4_CTRL;
        GPIO_STATUS_t                 GPIO5_STATUS;
        GPIO_CTRL_t                   GPIO5_CTRL;
        GPIO_STATUS_t                 GPIO6_STATUS;
        GPIO_CTRL_t                   GPIO6_CTRL;
        GPIO_STATUS_t                 GPIO7_STATUS;
        GPIO_CTRL_t                   GPIO7_CTRL;
        GPIO_STATUS_t                 GPIO8_STATUS;
        GPIO_CTRL_t                   GPIO8_CTRL;
        GPIO_STATUS_t                 GPIO9_STATUS;
        GPIO_CTRL_t                   GPIO9_CTRL;
        GPIO_STATUS_t                 GPIO10_STATUS;
        GPIO_CTRL_t                   GPIO10_CTRL;
        GPIO_STATUS_t                 GPIO11_STATUS;
        GPIO_CTRL_t                   GPIO11_CTRL;
        GPIO_STATUS_t                 GPIO12_STATUS;
        GPIO_CTRL_t                   GPIO12_CTRL;
        GPIO_STATUS_t                 GPIO13_STATUS;
        GPIO_CTRL_t                   GPIO13_CTRL;
        GPIO_STATUS_t                 GPIO14_STATUS;
        GPIO_CTRL_t                   GPIO14_CTRL;
        GPIO_STATUS_t                 GPIO15_STATUS;
        GPIO_CTRL_t                   GPIO15_CTRL;
        GPIO_STATUS_t                 GPIO16_STATUS;
        GPIO_CTRL_t                   GPIO16_CTRL;
        GPIO_STATUS_t                 GPIO17_STATUS;
        GPIO_CTRL_t                   GPIO17_CTRL;
        GPIO_STATUS_t                 GPIO18_STATUS;
        GPIO_CTRL_t                   GPIO18_CTRL;
        GPIO_STATUS_t                 GPIO19_STATUS;
        GPIO_CTRL_t                   GPIO19_CTRL;
        GPIO_STATUS_t                 GPIO20_STATUS;
        GPIO_CTRL_t                   GPIO20_CTRL;
        GPIO_STATUS_t                 GPIO21_STATUS;
        GPIO_CTRL_t                   GPIO21_CTRL;
        GPIO_STATUS_t                 GPIO22_STATUS;
        GPIO_CTRL_t                   GPIO22_CTRL;
        GPIO_STATUS_t                 GPIO23_STATUS;
        GPIO_CTRL_t                   GPIO23_CTRL;
        GPIO_STATUS_t                 GPIO24_STATUS;
        GPIO_CTRL_t                   GPIO24_CTRL;
        GPIO_STATUS_t                 GPIO25_STATUS;
        GPIO_CTRL_t                   GPIO25_CTRL;
        GPIO_STATUS_t                 GPIO26_STATUS;
        GPIO_CTRL_t                   GPIO26_CTRL;
        GPIO_STATUS_t                 GPIO27_STATUS;
        GPIO_CTRL_t                   GPIO27_CTRL;
        GPIO_STATUS_t                 GPIO28_STATUS;
        GPIO_CTRL_t                   GPIO28_CTRL;
        GPIO_STATUS_t                 GPIO29_STATUS;
        GPIO_CTRL_t                   GPIO29_CTRL;
        GPIO_STATUS_t                 GPIO30_STATUS;
        GPIO_CTRL_t                   GPIO30_CTRL;
        GPIO_STATUS_t                 GPIO31_STATUS;
        GPIO_CTRL_t                   GPIO31_CTRL;
        GPIO_STATUS_t                 GPIO32_STATUS;
        GPIO_CTRL_t                   GPIO32_CTRL;
        GPIO_STATUS_t                 GPIO33_STATUS;
        GPIO_CTRL_t                   GPIO33_CTRL;
        GPIO_STATUS_t                 GPIO34_STATUS;
        GPIO_CTRL_t                   GPIO34_CTRL;
        GPIO_STATUS_t                 GPIO35_STATUS;
        GPIO_CTRL_t                   GPIO35_CTRL;
        GPIO_STATUS_t                 GPIO36_STATUS;
        GPIO_CTRL_t                   GPIO36_CTRL;
        GPIO_STATUS_t                 GPIO37_STATUS;
        GPIO_CTRL_t                   GPIO37_CTRL;
        GPIO_STATUS_t                 GPIO38_STATUS;
        GPIO_CTRL_t                   GPIO38_CTRL;
        GPIO_STATUS_t                 GPIO39_STATUS;
        GPIO_CTRL_t                   GPIO39_CTRL;
        GPIO_STATUS_t                 GPIO40_STATUS;
        GPIO_CTRL_t                   GPIO40_CTRL;
        GPIO_STATUS_t                 GPIO41_STATUS;
        GPIO_CTRL_t                   GPIO41_CTRL;
        GPIO_STATUS_t                 GPIO42_STATUS;
        GPIO_CTRL_t                   GPIO42_CTRL;
        GPIO_STATUS_t                 GPIO43_STATUS;
        GPIO_CTRL_t                   GPIO43_CTRL;
        GPIO_STATUS_t                 GPIO44_STATUS;
        GPIO_CTRL_t                   GPIO44_CTRL;
        GPIO_STATUS_t                 GPIO45_STATUS;
        GPIO_CTRL_t                   GPIO45_CTRL;
        GPIO_STATUS_t                 GPIO46_STATUS;
        GPIO_CTRL_t                   GPIO46_CTRL;
        GPIO_STATUS_t                 GPIO47_STATUS;
        GPIO_CTRL_t                   GPIO47_CTRL;
        uint32_t                      reserved0[32];
        IRQSUMMARY_PROC0_SECURE0_t    IRQSUMMARY_PROC0_SECURE0;
        IRQSUMMARY_PROC0_SECURE1_t    IRQSUMMARY_PROC0_SECURE1;
        IRQSUMMARY_PROC0_NONSECURE0_t IRQSUMMARY_PROC0_NONSECURE0;
        IRQSUMMARY_PROC0_NONSECURE1_t IRQSUMMARY_PROC0_NONSECURE1;
        IRQSUMMARY_PROC1_SECURE0_t    IRQSUMMARY_PROC1_SECURE0;
        IRQSUMMARY_PROC1_SECURE1_t    IRQSUMMARY_PROC1_SECURE1;
        IRQSUMMARY_PROC1_NONSECURE0_t IRQSUMMARY_PROC1_NONSECURE0;
        IRQSUMMARY_PROC1_NONSECURE1_t IRQSUMMARY_PROC1_NONSECURE1;
        IRQSUMMARY_DORMANT_WAKE_SECURE0_t IRQSUMMARY_DORMANT_WAKE_SECURE0;
        IRQSUMMARY_DORMANT_WAKE_SECURE1_t IRQSUMMARY_DORMANT_WAKE_SECURE1;
        IRQSUMMARY_DORMANT_WAKE_NONSECURE0_t IRQSUMMARY_DORMANT_WAKE_NONSECURE0;
        IRQSUMMARY_DORMANT_WAKE_NONSECURE1_t IRQSUMMARY_DORMANT_WAKE_NONSECURE1;
        INTR0_t                       INTR0;
        INTR1_t                       INTR1;
        INTR2_t                       INTR2;
        INTR3_t                       INTR3;
        INTR4_t                       INTR4;
        INTR5_t                       INTR5;
        PROC0_INTE0_t                 PROC0_INTE0;
        PROC0_INTE1_t                 PROC0_INTE1;
        PROC0_INTE2_t                 PROC0_INTE2;
        PROC0_INTE3_t                 PROC0_INTE3;
        PROC0_INTE4_t                 PROC0_INTE4;
        PROC0_INTE5_t                 PROC0_INTE5;
        PROC0_INTF0_t                 PROC0_INTF0;
        PROC0_INTF1_t                 PROC0_INTF1;
        PROC0_INTF2_t                 PROC0_INTF2;
        PROC0_INTF3_t                 PROC0_INTF3;
        PROC0_INTF4_t                 PROC0_INTF4;
        PROC0_INTF5_t                 PROC0_INTF5;
        PROC0_INTS0_t                 PROC0_INTS0;
        PROC0_INTS1_t                 PROC0_INTS1;
        PROC0_INTS2_t                 PROC0_INTS2;
        PROC0_INTS3_t                 PROC0_INTS3;
        PROC0_INTS4_t                 PROC0_INTS4;
        PROC0_INTS5_t                 PROC0_INTS5;
        PROC1_INTE0_t                 PROC1_INTE0;
        PROC1_INTE1_t                 PROC1_INTE1;
        PROC1_INTE2_t                 PROC1_INTE2;
        PROC1_INTE3_t                 PROC1_INTE3;
        PROC1_INTE4_t                 PROC1_INTE4;
        PROC1_INTE5_t                 PROC1_INTE5;
        PROC1_INTF0_t                 PROC1_INTF0;
        PROC1_INTF1_t                 PROC1_INTF1;
        PROC1_INTF2_t                 PROC1_INTF2;
        PROC1_INTF3_t                 PROC1_INTF3;
        PROC1_INTF4_t                 PROC1_INTF4;
        PROC1_INTF5_t                 PROC1_INTF5;
        PROC1_INTS0_t                 PROC1_INTS0;
        PROC1_INTS1_t                 PROC1_INTS1;
        PROC1_INTS2_t                 PROC1_INTS2;
        PROC1_INTS3_t                 PROC1_INTS3;
        PROC1_INTS4_t                 PROC1_INTS4;
        PROC1_INTS5_t                 PROC1_INTS5;
        DORMANT_WAKE_INTE0_t          DORMANT_WAKE_INTE0;
        DORMANT_WAKE_INTE1_t          DORMANT_WAKE_INTE1;
        DORMANT_WAKE_INTE2_t          DORMANT_WAKE_INTE2;
        DORMANT_WAKE_INTE3_t          DORMANT_WAKE_INTE3;
        DORMANT_WAKE_INTE4_t          DORMANT_WAKE_INTE4;
        DORMANT_WAKE_INTE5_t          DORMANT_WAKE_INTE5;
        DORMANT_WAKE_INTF0_t          DORMANT_WAKE_INTF0;
        DORMANT_WAKE_INTF1_t          DORMANT_WAKE_INTF1;
        DORMANT_WAKE_INTF2_t          DORMANT_WAKE_INTF2;
        DORMANT_WAKE_INTF3_t          DORMANT_WAKE_INTF3;
        DORMANT_WAKE_INTF4_t          DORMANT_WAKE_INTF4;
        DORMANT_WAKE_INTF5_t          DORMANT_WAKE_INTF5;
        DORMANT_WAKE_INTS0_t          DORMANT_WAKE_INTS0;
        DORMANT_WAKE_INTS1_t          DORMANT_WAKE_INTS1;
        DORMANT_WAKE_INTS2_t          DORMANT_WAKE_INTS2;
        DORMANT_WAKE_INTS3_t          DORMANT_WAKE_INTS3;
        DORMANT_WAKE_INTS4_t          DORMANT_WAKE_INTS4;
        DORMANT_WAKE_INTS5_t          DORMANT_WAKE_INTS5;
    };

    static IO_BANK0_t & IO_BANK0 = (*(IO_BANK0_t *)0x40028000);
    static IO_BANK0_t & IO_BANK0_XOR = (*(IO_BANK0_t *)0x40029000);
    static IO_BANK0_t & IO_BANK0_SET = (*(IO_BANK0_t *)0x4002a000);
    static IO_BANK0_t & IO_BANK0_CLR = (*(IO_BANK0_t *)0x4002b000);

} // _IO_BANK0_

namespace _SYSINFO_  {

    // JEDEC JEP-106 compliant chip identifier.
    // Reset value: 0x00000001
    BEGIN_TYPE(CHIP_ID_t, uint32_t)
        ADD_BITFIELD_RO(REVISION, 28, 4)
        ADD_BITFIELD_RO(PART, 12, 16)
        ADD_BITFIELD_RO(MANUFACTURER, 1, 11)
        ADD_BITFIELD_RO(STOP_BIT, 0, 1)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(PACKAGE_SEL_t, uint32_t)
        ADD_BITFIELD_RO(PACKAGE_SEL, 0, 1)
    END_TYPE()

    // Platform register. Allows software to know what environment it is running in during pre-production development. Post-production, the PLATFORM is always ASIC, non-SIM.
    // Reset value: 0x00000000
    BEGIN_TYPE(PLATFORM_t, uint32_t)
        ADD_BITFIELD_RO(GATESIM, 4, 1)
        ADD_BITFIELD_RO(BATCHSIM, 3, 1)
        ADD_BITFIELD_RO(HDLSIM, 2, 1)
        ADD_BITFIELD_RO(ASIC, 1, 1)
        ADD_BITFIELD_RO(FPGA, 0, 1)
    END_TYPE()

    // Git hash of the chip source. Used to identify chip version.
    BEGIN_TYPE(GITREF_RP2350_t, uint32_t)
        ADD_BITFIELD_RO(GITREF_RP2350, 0, 32)
    END_TYPE()

    struct SYSINFO_t {
        CHIP_ID_t                     CHIP_ID;
        PACKAGE_SEL_t                 PACKAGE_SEL;
        PLATFORM_t                    PLATFORM;
        uint32_t                      reserved0[2];
        GITREF_RP2350_t               GITREF_RP2350;
    };

    static SYSINFO_t & SYSINFO = (*(SYSINFO_t *)0x40000000);
    static SYSINFO_t & SYSINFO_XOR = (*(SYSINFO_t *)0x40001000);
    static SYSINFO_t & SYSINFO_SET = (*(SYSINFO_t *)0x40002000);
    static SYSINFO_t & SYSINFO_CLR = (*(SYSINFO_t *)0x40003000);

} // _SYSINFO_

// SHA-256 hash function implementation
namespace _SHA256_  {

    // Control and status register
    // Reset value: 0x00001206
    BEGIN_TYPE(CSR_t, uint32_t)
        // Enable byte swapping of 32-bit values at the point they are committed to the SHA message scheduler. This block's bus interface assembles byte/halfword data into message words in little-endian order, so that DMAing the same buffer with different transfer sizes always gives the same result on a little-endian system like RP2350. However, when marshalling bytes into blocks, SHA expects that the first byte is the *most significant* in each message word. To resolve this, once the bus interface has accumulated 32 bits of data (either a word write, two halfword writes in little-endian order, or four byte writes in little-endian order) the final value can be byte-swapped before passing to the actual SHA core. This feature is enabled by default because using the SHA core to checksum byte buffers is expected to be more common than having preformatted SHA message words lying around.
        ADD_BITFIELD_RW(BSWAP, 12, 1)
        // Configure DREQ logic for the correct DMA data size. Must be configured before the DMA channel is triggered. The SHA-256 core's DREQ logic requests one entire block of data at once, since there is no FIFO, and data goes straight into the core's message schedule and digest hardware. Therefore, when transferring data with DMA, CSR_DMA_SIZE must be configured in advance so that the correct number of transfers can be requested per block.
        ADD_BITFIELD_RW(DMA_SIZE, 8, 2)
        // Set when a write occurs whilst the SHA-256 core is not ready for data (WDATA_RDY is low). Write one to clear.
        ADD_BITFIELD_RW(ERR_WDATA_NOT_RDY, 4, 1)
        // If 1, the SHA-256 checksum presented in registers SUM0 through SUM7 is currently valid. Goes low when WDATA is first written, then returns high once 16 words have been written and the digest of the current 512-bit block has subsequently completed.
        ADD_BITFIELD_RO(SUM_VLD, 2, 1)
        // If 1, the SHA-256 core is ready to accept more data through the WDATA register. After writing 16 words, this flag will go low for 57 cycles whilst the core completes its digest.
        ADD_BITFIELD_RO(WDATA_RDY, 1, 1)
        // Write 1 to prepare the SHA-256 core for a new checksum. The SUMx registers are initialised to the proper values (fractional bits of square roots of first 8 primes) and internal counters are cleared. This immediately forces WDATA_RDY and SUM_VLD high. START must be written before initiating a DMA transfer to the SHA-256 core, because the core will always request 16 transfers at a time (1 512-bit block). Additionally, the DMA channel should be configured for a multiple of 16 32-bit transfers.
        ADD_BITFIELD_WO(START, 0, 1)
    END_TYPE()

    static const uint32_t CSR_DMA_SIZE__8bit = 0;
    static const uint32_t CSR_DMA_SIZE__16bit = 1;
    static const uint32_t CSR_DMA_SIZE__32bit = 2;

    // Write data register
    // Reset value: 0x00000000
    BEGIN_TYPE(WDATA_t, uint32_t)
        // After pulsing START and writing 16 words of data to this register, WDATA_RDY will go low and the SHA-256 core will complete the digest of the current 512-bit block. Software is responsible for ensuring the data is correctly padded and terminated to a whole number of 512-bit blocks. After this, WDATA_RDY will return high, and more data can be written (if any). This register supports word, halfword and byte writes, so that DMA from non-word-aligned buffers can be supported. The total amount of data per block remains the same (16 words, 32 halfwords or 64 bytes) and byte/halfword transfers must not be mixed within a block.
        ADD_BITFIELD_WO(WDATA, 0, 32)
    END_TYPE()

    // 256-bit checksum result. Contents are undefined when CSR_SUM_VLD is 0.
    // Reset value: 0x00000000
    typedef uint32_t SUM_t;

    struct SHA256_t {
        CSR_t                         CSR;
        WDATA_t                       WDATA;
        SUM_t                         SUM[8];
    };

    static SHA256_t & SHA256 = (*(SHA256_t *)0x400f8000);
    static SHA256_t & SHA256_XOR = (*(SHA256_t *)0x400f9000);
    static SHA256_t & SHA256_SET = (*(SHA256_t *)0x400fa000);
    static SHA256_t & SHA256_CLR = (*(SHA256_t *)0x400fb000);

} // _SHA256_

// FIFO status and write access for HSTX
namespace _HSTX_FIFO_  {

    // FIFO status
    // Reset value: 0x00000000
    BEGIN_TYPE(STAT_t, uint32_t)
        // FIFO was written when full. Write 1 to clear.
        ADD_BITFIELD_RW(WOF, 10, 1)
        ADD_BITFIELD_RO(EMPTY, 9, 1)
        ADD_BITFIELD_RO(FULL, 8, 1)
        ADD_BITFIELD_RO(LEVEL, 0, 8)
    END_TYPE()

    // Write access to FIFO
    // Reset value: 0x00000000
    BEGIN_TYPE(FIFO_t, uint32_t)
        ADD_BITFIELD_WO(FIFO, 0, 32)
    END_TYPE()

    struct HSTX_FIFO_t {
        STAT_t                        STAT;
        FIFO_t                        FIFO;
    };

    static HSTX_FIFO_t & HSTX_FIFO = (*(HSTX_FIFO_t *)0x50600000);
    static HSTX_FIFO_t & HSTX_FIFO_XOR = (*(HSTX_FIFO_t *)0x50601000);
    static HSTX_FIFO_t & HSTX_FIFO_SET = (*(HSTX_FIFO_t *)0x50602000);
    static HSTX_FIFO_t & HSTX_FIFO_CLR = (*(HSTX_FIFO_t *)0x50603000);

} // _HSTX_FIFO_

// Control interface to HSTX. For FIFO write access and status, see the HSTX_FIFO register block.
namespace _HSTX_CTRL_  {

    // Reset value: 0x10050600
    BEGIN_TYPE(CSR_t, uint32_t)
        // Clock period of the generated clock, measured in HSTX clock cycles. Can be odd or even. The generated clock advances only on cycles where the shift register shifts. For example, a clkdiv of 5 would generate a complete output clock period for every 5 HSTX clocks (or every 10 half-clocks). A CLKDIV value of 0 is mapped to a period of 16 HSTX clock cycles.
        ADD_BITFIELD_RW(CLKDIV, 28, 4)
        // Set the initial phase of the generated clock. A CLKPHASE of 0 means the clock is initially low, and the first rising edge occurs after one half period of the generated clock (i.e. CLKDIV/2 cycles of clk_hstx). Incrementing CLKPHASE by 1 will advance the initial clock phase by one half clk_hstx period. For example, if CLKDIV=2 and CLKPHASE=1: * The clock will be initially low * The first rising edge will be 0.5 clk_hstx cycles after asserting first data * The first falling edge will be 1.5 clk_hstx cycles after asserting first data This configuration would be suitable for serialising at a bit rate of clk_hstx with a centre-aligned DDR clock. When the HSTX is halted by clearing CSR_EN, the clock generator will return to its initial phase as configured by the CLKPHASE field. Note CLKPHASE must be strictly less than double the value of CLKDIV (one full period), else its operation is undefined.
        ADD_BITFIELD_RW(CLKPHASE, 24, 4)
        // Number of times to shift the shift register before refilling it from the FIFO. (A count of how many times it has been shifted, *not* the total shift distance.) A register value of 0 means shift 32 times.
        ADD_BITFIELD_RW(N_SHIFTS, 16, 5)
        // How many bits to right-rotate the shift register by each cycle. The use of a rotate rather than a shift allows left shifts to be emulated, by subtracting the left-shift amount from 32. It also allows data to be repeated, when the product of SHIFT and N_SHIFTS is greater than 32.
        ADD_BITFIELD_RW(SHIFT, 8, 5)
        // Select which PIO to use for coupled mode operation.
        ADD_BITFIELD_RW(COUPLED_SEL, 5, 2)
        // Enable the PIO-to-HSTX 1:1 connection. The HSTX must be clocked *directly* from the system clock (not just from some other clock source of the same frequency) for this synchronous interface to function correctly. When COUPLED_MODE is set, BITx_SEL_P and SEL_N indices 24 through 31 will select bits from the 8-bit PIO-to-HSTX path, rather than shifter bits. Indices of 0 through 23 will still index the shift register as normal. The PIO outputs connected to the PIO-to-HSTX bus are those same outputs that would appear on the HSTX-capable pins if those pins' FUNCSELs were set to PIO instead of HSTX. For example, if HSTX is on GPIOs 12 through 19, then PIO outputs 12 through 19 are connected to the HSTX when coupled mode is engaged.
        ADD_BITFIELD_RW(COUPLED_MODE, 4, 1)
        // Enable the command expander. When 0, raw FIFO data is passed directly to the output shift register. When 1, the command expander can perform simple operations such as run length decoding on data between the FIFO and the shift register. Do not change CXPD_EN whilst EN is set. It's safe to set CXPD_EN simultaneously with setting EN.
        ADD_BITFIELD_RW(EXPAND_EN, 1, 1)
        // When EN is 1, the HSTX will shift out data as it appears in the FIFO. As long as there is data, the HSTX shift register will shift once per clock cycle, and the frequency of popping from the FIFO is determined by the ratio of SHIFT and SHIFT_THRESH. When EN is 0, the FIFO is not popped. The shift counter and clock generator are also reset to their initial state for as long as EN is low. Note the initial phase of the clock generator can be configured by the CLKPHASE field. Once the HSTX is enabled again, and data is pushed to the FIFO, the generated clock's first rising edge will be one half-period after the first data is launched.
        ADD_BITFIELD_RW(EN, 0, 1)
    END_TYPE()

    // Data control register for output bit 0
    // Reset value: 0x00000000
    BEGIN_TYPE(BIT_t, uint32_t)
        // Connect this output to the generated clock, rather than the data shift register. SEL_P and SEL_N are ignored if this bit is set, but INV can still be set to generate an antiphase clock.
        ADD_BITFIELD_RW(CLK, 17, 1)
        // Invert this data output (logical NOT)
        ADD_BITFIELD_RW(INV, 16, 1)
        // Shift register data bit select for the second half of the HSTX clock cycle
        ADD_BITFIELD_RW(SEL_N, 8, 5)
        // Shift register data bit select for the first half of the HSTX clock cycle
        ADD_BITFIELD_RW(SEL_P, 0, 5)
    END_TYPE()

    // Configure the optional shifter inside the command expander
    // Reset value: 0x01000100
    BEGIN_TYPE(EXPAND_SHIFT_t, uint32_t)
        // Number of times to consume from the shift register before refilling it from the FIFO, when the current command is an encoded data command (e.g. TMDS). A register value of 0 means shift 32 times.
        ADD_BITFIELD_RW(ENC_N_SHIFTS, 24, 5)
        // How many bits to right-rotate the shift register by each time data is pushed to the output shifter, when the current command is an encoded data command (e.g. TMDS).
        ADD_BITFIELD_RW(ENC_SHIFT, 16, 5)
        // Number of times to consume from the shift register before refilling it from the FIFO, when the current command is a raw data command. A register value of 0 means shift 32 times.
        ADD_BITFIELD_RW(RAW_N_SHIFTS, 8, 5)
        // How many bits to right-rotate the shift register by each time data is pushed to the output shifter, when the current command is a raw data command.
        ADD_BITFIELD_RW(RAW_SHIFT, 0, 5)
    END_TYPE()

    // Configure the optional TMDS encoder inside the command expander
    // Reset value: 0x00000000
    BEGIN_TYPE(EXPAND_TMDS_t, uint32_t)
        // Number of valid data bits for the lane 2 TMDS encoder, starting from bit 7 of the rotated data. Field values of 0 -> 7 encode counts of 1 -> 8 bits.
        ADD_BITFIELD_RW(L2_NBITS, 21, 3)
        // Right-rotate applied to the current shifter data before the lane 2 TMDS encoder.
        ADD_BITFIELD_RW(L2_ROT, 16, 5)
        // Number of valid data bits for the lane 1 TMDS encoder, starting from bit 7 of the rotated data. Field values of 0 -> 7 encode counts of 1 -> 8 bits.
        ADD_BITFIELD_RW(L1_NBITS, 13, 3)
        // Right-rotate applied to the current shifter data before the lane 1 TMDS encoder.
        ADD_BITFIELD_RW(L1_ROT, 8, 5)
        // Number of valid data bits for the lane 0 TMDS encoder, starting from bit 7 of the rotated data. Field values of 0 -> 7 encode counts of 1 -> 8 bits.
        ADD_BITFIELD_RW(L0_NBITS, 5, 3)
        // Right-rotate applied to the current shifter data before the lane 0 TMDS encoder.
        ADD_BITFIELD_RW(L0_ROT, 0, 5)
    END_TYPE()

    struct HSTX_CTRL_t {
        CSR_t                         CSR;
        BIT_t                         BIT[8];
        EXPAND_SHIFT_t                EXPAND_SHIFT;
        EXPAND_TMDS_t                 EXPAND_TMDS;
    };

    static HSTX_CTRL_t & HSTX_CTRL = (*(HSTX_CTRL_t *)0x400c0000);
    static HSTX_CTRL_t & HSTX_CTRL_XOR = (*(HSTX_CTRL_t *)0x400c1000);
    static HSTX_CTRL_t & HSTX_CTRL_SET = (*(HSTX_CTRL_t *)0x400c2000);
    static HSTX_CTRL_t & HSTX_CTRL_CLR = (*(HSTX_CTRL_t *)0x400c3000);

} // _HSTX_CTRL_

// Cortex-M33 EPPB vendor register block for RP2350
namespace _EPPB_  {

    // NMI mask for IRQs 0 through 31. This register is core-local, and is reset by a processor warm reset.
    // Reset value: 0x00000000
    BEGIN_TYPE(NMI_MASK0_t, uint32_t)
        ADD_BITFIELD_RW(NMI_MASK0, 0, 32)
    END_TYPE()

    // NMI mask for IRQs 0 though 51. This register is core-local, and is reset by a processor warm reset.
    // Reset value: 0x00000000
    BEGIN_TYPE(NMI_MASK1_t, uint32_t)
        ADD_BITFIELD_RW(NMI_MASK1, 0, 20)
    END_TYPE()

    // Nonstandard sleep control register
    // Reset value: 0x00000002
    BEGIN_TYPE(SLEEPCTRL_t, uint32_t)
        // Status signal from the processor's interrupt controller. Changes to WICENREQ are eventually reflected in WICENACK.
        ADD_BITFIELD_RO(WICENACK, 2, 1)
        // Request that the next processor deep sleep is a WIC sleep. After setting this bit, before sleeping, poll WICENACK to ensure the processor interrupt controller has acknowledged the change.
        ADD_BITFIELD_RW(WICENREQ, 1, 1)
        // By default, any processor sleep will deassert the system-level clock request. Reenabling the clocks incurs 5 cycles of additional latency on wakeup. Setting LIGHT_SLEEP to 1 keeps the clock request asserted during a normal sleep (Arm SCR.SLEEPDEEP = 0), for faster wakeup. Processor deep sleep (Arm SCR.SLEEPDEEP = 1) is not affected, and will always deassert the system-level clock request.
        ADD_BITFIELD_RW(LIGHT_SLEEP, 0, 1)
    END_TYPE()

    struct EPPB_t {
        NMI_MASK0_t                   NMI_MASK0;
        NMI_MASK1_t                   NMI_MASK1;
        SLEEPCTRL_t                   SLEEPCTRL;
    };

    static EPPB_t & EPPB = (*(EPPB_t *)0xe0080000);

} // _EPPB_

// TEAL registers accessible through the debug interface
namespace _PPB_  {

    // Provides the interface for generating Instrumentation packets
    // Reset value: 0x00000000
    BEGIN_TYPE(ITM_STIM_t, uint32_t)
        // Data to write to the Stimulus Port FIFO, for forwarding as an Instrumentation packet. The size of write access determines the type of Instrumentation packet generated.
        ADD_BITFIELD_RW(STIMULUS, 0, 32)
    END_TYPE()

    // Provide an individual enable bit for each ITM_STIM register
    // Reset value: 0x00000000
    BEGIN_TYPE(ITM_TER0_t, uint32_t)
        // For STIMENA[m] in ITM_TER*n, controls whether ITM_STIM(32*n + m) is enabled
        ADD_BITFIELD_RW(STIMENA, 0, 32)
    END_TYPE()

    // Controls which stimulus ports can be accessed by unprivileged code
    // Reset value: 0x00000000
    BEGIN_TYPE(ITM_TPR_t, uint32_t)
        // Bit mask to enable tracing on ITM stimulus ports
        ADD_BITFIELD_RW(PRIVMASK, 0, 4)
    END_TYPE()

    // Configures and controls transfers through the ITM interface
    // Reset value: 0x00000000
    BEGIN_TYPE(ITM_TCR_t, uint32_t)
        // Indicates whether the ITM is currently processing events
        ADD_BITFIELD_RO(BUSY, 23, 1)
        // Identifier for multi-source trace stream formatting. If multi-source trace is in use, the debugger must write a unique non-zero trace ID value to this field
        ADD_BITFIELD_RW(TRACEBUSID, 16, 7)
        // Defines how often the ITM generates a global timestamp, based on the global timestamp clock frequency, or disables generation of global timestamps
        ADD_BITFIELD_RW(GTSFREQ, 10, 2)
        // Local timestamp prescaler, used with the trace packet reference clock
        ADD_BITFIELD_RW(TSPRESCALE, 8, 2)
        // Stall the PE to guarantee delivery of Data Trace packets.
        ADD_BITFIELD_RW(STALLENA, 5, 1)
        // Enables asynchronous clocking of the timestamp counter
        ADD_BITFIELD_RW(SWOENA, 4, 1)
        // Enables forwarding of hardware event packet from the DWT unit to the ITM for output to the TPIU
        ADD_BITFIELD_RW(TXENA, 3, 1)
        // Enables Synchronization packet transmission for a synchronous TPIU
        ADD_BITFIELD_RW(SYNCENA, 2, 1)
        // Enables Local timestamp generation
        ADD_BITFIELD_RW(TSENA, 1, 1)
        // Enables the ITM
        ADD_BITFIELD_RW(ITMENA, 0, 1)
    END_TYPE()

    // Integration Mode: Read ATB Ready
    // Reset value: 0x00000000
    BEGIN_TYPE(INT_ATREADY_t, uint32_t)
        // A read of this bit returns the value of AFVALID
        ADD_BITFIELD_RO(AFVALID, 1, 1)
        // A read of this bit returns the value of ATREADY
        ADD_BITFIELD_RO(ATREADY, 0, 1)
    END_TYPE()

    // Integration Mode: Write ATB Valid
    // Reset value: 0x00000000
    BEGIN_TYPE(INT_ATVALID_t, uint32_t)
        // A write to this bit gives the value of AFREADY
        ADD_BITFIELD_RW(AFREADY, 1, 1)
        // A write to this bit gives the value of ATVALID
        ADD_BITFIELD_RW(ATREADY, 0, 1)
    END_TYPE()

    // Integration Mode Control Register
    // Reset value: 0x00000000
    BEGIN_TYPE(ITM_ITCTRL_t, uint32_t)
        // Integration mode enable bit - The possible values are:  0 - The trace unit is not in integration mode. 1 - The trace unit is in integration mode. This mode enables: A debug agent to perform topology detection. SoC test software to perform integration testing.
        ADD_BITFIELD_RW(IME, 0, 1)
    END_TYPE()

    // Provides CoreSight discovery information for the ITM
    // Reset value: 0x47701a01
    BEGIN_TYPE(ITM_DEVARCH_t, uint32_t)
        // Defines the architect of the component. Bits [31:28] are the JEP106 continuation code (JEP106 bank ID, minus 1) and bits [27:21] are the JEP106 ID code.
        ADD_BITFIELD_RO(ARCHITECT, 21, 11)
        // Defines that the DEVARCH register is present
        ADD_BITFIELD_RO(PRESENT, 20, 1)
        // Defines the architecture revision of the component
        ADD_BITFIELD_RO(REVISION, 16, 4)
        // Defines the architecture version of the component
        ADD_BITFIELD_RO(ARCHVER, 12, 4)
        // Defines the architecture of the component
        ADD_BITFIELD_RO(ARCHPART, 0, 12)
    END_TYPE()

    // Provides CoreSight discovery information for the ITM
    // Reset value: 0x00000043
    BEGIN_TYPE(ITM_DEVTYPE_t, uint32_t)
        // Component sub-type
        ADD_BITFIELD_RO(SUB, 4, 4)
        // Component major type
        ADD_BITFIELD_RO(MAJOR, 0, 4)
    END_TYPE()

    // Provides CoreSight discovery information for the ITM
    // Reset value: 0x00000004
    BEGIN_TYPE(ITM_PIDR4_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(SIZE, 4, 4)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(DES_2, 0, 4)
    END_TYPE()

    // Provides CoreSight discovery information for the ITM
    // Reset value: 0x00000000
    BEGIN_TYPE(ITM_PIDR5_t, uint32_t)
        ADD_BITFIELD_RW(ITM_PIDR5, 0, 32)
    END_TYPE()

    // Provides CoreSight discovery information for the ITM
    // Reset value: 0x00000000
    BEGIN_TYPE(ITM_PIDR6_t, uint32_t)
        ADD_BITFIELD_RW(ITM_PIDR6, 0, 32)
    END_TYPE()

    // Provides CoreSight discovery information for the ITM
    // Reset value: 0x00000000
    BEGIN_TYPE(ITM_PIDR7_t, uint32_t)
        ADD_BITFIELD_RW(ITM_PIDR7, 0, 32)
    END_TYPE()

    // Provides CoreSight discovery information for the ITM
    // Reset value: 0x00000021
    BEGIN_TYPE(ITM_PIDR0_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(PART_0, 0, 8)
    END_TYPE()

    // Provides CoreSight discovery information for the ITM
    // Reset value: 0x000000bd
    BEGIN_TYPE(ITM_PIDR1_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(DES_0, 4, 4)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(PART_1, 0, 4)
    END_TYPE()

    // Provides CoreSight discovery information for the ITM
    // Reset value: 0x0000000b
    BEGIN_TYPE(ITM_PIDR2_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(REVISION, 4, 4)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(JEDEC, 3, 1)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(DES_1, 0, 3)
    END_TYPE()

    // Provides CoreSight discovery information for the ITM
    // Reset value: 0x00000000
    BEGIN_TYPE(ITM_PIDR3_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(REVAND, 4, 4)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(CMOD, 0, 4)
    END_TYPE()

    // Provides CoreSight discovery information for the ITM
    // Reset value: 0x0000000d
    BEGIN_TYPE(ITM_CIDR0_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(PRMBL_0, 0, 8)
    END_TYPE()

    // Provides CoreSight discovery information for the ITM
    // Reset value: 0x00000090
    BEGIN_TYPE(ITM_CIDR1_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(CLASS, 4, 4)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(PRMBL_1, 0, 4)
    END_TYPE()

    // Provides CoreSight discovery information for the ITM
    // Reset value: 0x00000005
    BEGIN_TYPE(ITM_CIDR2_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(PRMBL_2, 0, 8)
    END_TYPE()

    // Provides CoreSight discovery information for the ITM
    // Reset value: 0x000000b1
    BEGIN_TYPE(ITM_CIDR3_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(PRMBL_3, 0, 8)
    END_TYPE()

    // Provides configuration and status information for the DWT unit, and used to control features of the unit
    // Reset value: 0x73741824
    BEGIN_TYPE(DWT_CTRL_t, uint32_t)
        // Number of DWT comparators implemented
        ADD_BITFIELD_RO(NUMCOMP, 28, 4)
        // Indicates whether the implementation does not support trace
        ADD_BITFIELD_RO(NOTRCPKT, 27, 1)
        // Reserved, RAZ
        ADD_BITFIELD_RO(NOEXTTRIG, 26, 1)
        // Indicates whether the implementation does not include a cycle counter
        ADD_BITFIELD_RO(NOCYCCNT, 25, 1)
        // Indicates whether the implementation does not include the profiling counters
        ADD_BITFIELD_RO(NOPRFCNT, 24, 1)
        // Controls whether the cycle counter is disabled in Secure state
        ADD_BITFIELD_RW(CYCDISS, 23, 1)
        // Enables Event Counter packet generation on POSTCNT underflow
        ADD_BITFIELD_RW(CYCEVTENA, 22, 1)
        // Enables DWT_FOLDCNT counter
        ADD_BITFIELD_RW(FOLDEVTENA, 21, 1)
        // Enables DWT_LSUCNT counter
        ADD_BITFIELD_RW(LSUEVTENA, 20, 1)
        // Enable DWT_SLEEPCNT counter
        ADD_BITFIELD_RW(SLEEPEVTENA, 19, 1)
        // Enables DWT_EXCCNT counter
        ADD_BITFIELD_RW(EXCEVTENA, 18, 1)
        // Enables DWT_CPICNT counter
        ADD_BITFIELD_RW(CPIEVTENA, 17, 1)
        // Enables generation of Exception Trace packets
        ADD_BITFIELD_RW(EXTTRCENA, 16, 1)
        // Enables use of POSTCNT counter as a timer for Periodic PC Sample packet generation
        ADD_BITFIELD_RW(PCSAMPLENA, 12, 1)
        // Selects the position of the synchronization packet counter tap on the CYCCNT counter. This determines the Synchronization packet rate
        ADD_BITFIELD_RW(SYNCTAP, 10, 2)
        // Selects the position of the POSTCNT tap on the CYCCNT counter
        ADD_BITFIELD_RW(CYCTAP, 9, 1)
        // Initial value for the POSTCNT counter
        ADD_BITFIELD_RW(POSTINIT, 5, 4)
        // Reload value for the POSTCNT counter
        ADD_BITFIELD_RW(POSTPRESET, 1, 4)
        // Enables CYCCNT
        ADD_BITFIELD_RW(CYCCNTENA, 0, 1)
    END_TYPE()

    // Shows or sets the value of the processor cycle counter, CYCCNT
    // Reset value: 0x00000000
    BEGIN_TYPE(DWT_CYCCNT_t, uint32_t)
        // Increments one on each processor clock cycle when DWT_CTRL.CYCCNTENA == 1 and DEMCR.TRCENA == 1. On overflow, CYCCNT wraps to zero
        ADD_BITFIELD_RW(CYCCNT, 0, 32)
    END_TYPE()

    // Counts the total cycles spent in exception processing
    // Reset value: 0x00000000
    BEGIN_TYPE(DWT_EXCCNT_t, uint32_t)
        // Counts one on each cycle when all of the following are true: - DWT_CTRL.EXCEVTENA == 1 and DEMCR.TRCENA == 1. - No instruction is executed, see DWT_CPICNT. - An exception-entry or exception-exit related operation is in progress. - Either SecureNoninvasiveDebugAllowed() == TRUE, or NS-Req for the operation is set to Non-secure and NoninvasiveDebugAllowed() == TRUE.
        ADD_BITFIELD_RW(EXCCNT, 0, 8)
    END_TYPE()

    // Increments on the additional cycles required to execute all load or store instructions
    // Reset value: 0x00000000
    BEGIN_TYPE(DWT_LSUCNT_t, uint32_t)
        // Counts one on each cycle when all of the following are true: - DWT_CTRL.LSUEVTENA == 1 and DEMCR.TRCENA == 1. - No instruction is executed, see DWT_CPICNT. - No exception-entry or exception-exit operation is in progress, see DWT_EXCCNT. - A load-store operation is in progress. - Either SecureNoninvasiveDebugAllowed() == TRUE, or NS-Req for the operation is set to Non-secure and NoninvasiveDebugAllowed() == TRUE.
        ADD_BITFIELD_RW(LSUCNT, 0, 8)
    END_TYPE()

    // Increments on the additional cycles required to execute all load or store instructions
    // Reset value: 0x00000000
    BEGIN_TYPE(DWT_FOLDCNT_t, uint32_t)
        // Counts on each cycle when all of the following are true: - DWT_CTRL.FOLDEVTENA == 1 and DEMCR.TRCENA == 1. - At least two instructions are executed, see DWT_CPICNT. - Either SecureNoninvasiveDebugAllowed() == TRUE, or the PE is in Non-secure state and NoninvasiveDebugAllowed() == TRUE. The counter is incremented by the number of instructions executed, minus one
        ADD_BITFIELD_RW(FOLDCNT, 0, 8)
    END_TYPE()

    // Provides a reference value for use by watchpoint comparator 0
    // Reset value: 0x00000000
    BEGIN_TYPE(DWT_COMP0_t, uint32_t)
        ADD_BITFIELD_RW(DWT_COMP0, 0, 32)
    END_TYPE()

    // Controls the operation of watchpoint comparator 0
    // Reset value: 0x58000000
    BEGIN_TYPE(DWT_FUNCTION0_t, uint32_t)
        // Identifies the capabilities for MATCH for comparator *n
        ADD_BITFIELD_RO(ID, 27, 5)
        // Set to 1 when the comparator matches
        ADD_BITFIELD_RO(MATCHED, 24, 1)
        // Defines the size of the object being watched for by Data Value and Data Address comparators
        ADD_BITFIELD_RW(DATAVSIZE, 10, 2)
        // Defines the action on a match. This field is ignored and the comparator generates no actions if it is disabled by MATCH
        ADD_BITFIELD_RW(ACTION, 4, 2)
        // Controls the type of match generated by this comparator
        ADD_BITFIELD_RW(MATCH, 0, 4)
    END_TYPE()

    // Provides a reference value for use by watchpoint comparator 1
    // Reset value: 0x00000000
    BEGIN_TYPE(DWT_COMP1_t, uint32_t)
        ADD_BITFIELD_RW(DWT_COMP1, 0, 32)
    END_TYPE()

    // Controls the operation of watchpoint comparator 1
    // Reset value: 0x89000828
    BEGIN_TYPE(DWT_FUNCTION1_t, uint32_t)
        // Identifies the capabilities for MATCH for comparator *n
        ADD_BITFIELD_RO(ID, 27, 5)
        // Set to 1 when the comparator matches
        ADD_BITFIELD_RO(MATCHED, 24, 1)
        // Defines the size of the object being watched for by Data Value and Data Address comparators
        ADD_BITFIELD_RW(DATAVSIZE, 10, 2)
        // Defines the action on a match. This field is ignored and the comparator generates no actions if it is disabled by MATCH
        ADD_BITFIELD_RW(ACTION, 4, 2)
        // Controls the type of match generated by this comparator
        ADD_BITFIELD_RW(MATCH, 0, 4)
    END_TYPE()

    // Provides a reference value for use by watchpoint comparator 2
    // Reset value: 0x00000000
    BEGIN_TYPE(DWT_COMP2_t, uint32_t)
        ADD_BITFIELD_RW(DWT_COMP2, 0, 32)
    END_TYPE()

    // Controls the operation of watchpoint comparator 2
    // Reset value: 0x50000000
    BEGIN_TYPE(DWT_FUNCTION2_t, uint32_t)
        // Identifies the capabilities for MATCH for comparator *n
        ADD_BITFIELD_RO(ID, 27, 5)
        // Set to 1 when the comparator matches
        ADD_BITFIELD_RO(MATCHED, 24, 1)
        // Defines the size of the object being watched for by Data Value and Data Address comparators
        ADD_BITFIELD_RW(DATAVSIZE, 10, 2)
        // Defines the action on a match. This field is ignored and the comparator generates no actions if it is disabled by MATCH
        ADD_BITFIELD_RW(ACTION, 4, 2)
        // Controls the type of match generated by this comparator
        ADD_BITFIELD_RW(MATCH, 0, 4)
    END_TYPE()

    // Provides a reference value for use by watchpoint comparator 3
    // Reset value: 0x00000000
    BEGIN_TYPE(DWT_COMP3_t, uint32_t)
        ADD_BITFIELD_RW(DWT_COMP3, 0, 32)
    END_TYPE()

    // Controls the operation of watchpoint comparator 3
    // Reset value: 0x20000800
    BEGIN_TYPE(DWT_FUNCTION3_t, uint32_t)
        // Identifies the capabilities for MATCH for comparator *n
        ADD_BITFIELD_RO(ID, 27, 5)
        // Set to 1 when the comparator matches
        ADD_BITFIELD_RO(MATCHED, 24, 1)
        // Defines the size of the object being watched for by Data Value and Data Address comparators
        ADD_BITFIELD_RW(DATAVSIZE, 10, 2)
        // Defines the action on a match. This field is ignored and the comparator generates no actions if it is disabled by MATCH
        ADD_BITFIELD_RW(ACTION, 4, 2)
        // Controls the type of match generated by this comparator
        ADD_BITFIELD_RW(MATCH, 0, 4)
    END_TYPE()

    // Provides CoreSight discovery information for the DWT
    // Reset value: 0x47701a02
    BEGIN_TYPE(DWT_DEVARCH_t, uint32_t)
        // Defines the architect of the component. Bits [31:28] are the JEP106 continuation code (JEP106 bank ID, minus 1) and bits [27:21] are the JEP106 ID code.
        ADD_BITFIELD_RO(ARCHITECT, 21, 11)
        // Defines that the DEVARCH register is present
        ADD_BITFIELD_RO(PRESENT, 20, 1)
        // Defines the architecture revision of the component
        ADD_BITFIELD_RO(REVISION, 16, 4)
        // Defines the architecture version of the component
        ADD_BITFIELD_RO(ARCHVER, 12, 4)
        // Defines the architecture of the component
        ADD_BITFIELD_RO(ARCHPART, 0, 12)
    END_TYPE()

    // Provides CoreSight discovery information for the DWT
    // Reset value: 0x00000000
    BEGIN_TYPE(DWT_DEVTYPE_t, uint32_t)
        // Component sub-type
        ADD_BITFIELD_RO(SUB, 4, 4)
        // Component major type
        ADD_BITFIELD_RO(MAJOR, 0, 4)
    END_TYPE()

    // Provides CoreSight discovery information for the DWT
    // Reset value: 0x00000004
    BEGIN_TYPE(DWT_PIDR4_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(SIZE, 4, 4)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(DES_2, 0, 4)
    END_TYPE()

    // Provides CoreSight discovery information for the DWT
    // Reset value: 0x00000000
    BEGIN_TYPE(DWT_PIDR5_t, uint32_t)
        ADD_BITFIELD_RW(DWT_PIDR5, 0, 32)
    END_TYPE()

    // Provides CoreSight discovery information for the DWT
    // Reset value: 0x00000000
    BEGIN_TYPE(DWT_PIDR6_t, uint32_t)
        ADD_BITFIELD_RW(DWT_PIDR6, 0, 32)
    END_TYPE()

    // Provides CoreSight discovery information for the DWT
    // Reset value: 0x00000000
    BEGIN_TYPE(DWT_PIDR7_t, uint32_t)
        ADD_BITFIELD_RW(DWT_PIDR7, 0, 32)
    END_TYPE()

    // Provides CoreSight discovery information for the DWT
    // Reset value: 0x00000021
    BEGIN_TYPE(DWT_PIDR0_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(PART_0, 0, 8)
    END_TYPE()

    // Provides CoreSight discovery information for the DWT
    // Reset value: 0x000000bd
    BEGIN_TYPE(DWT_PIDR1_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(DES_0, 4, 4)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(PART_1, 0, 4)
    END_TYPE()

    // Provides CoreSight discovery information for the DWT
    // Reset value: 0x0000000b
    BEGIN_TYPE(DWT_PIDR2_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(REVISION, 4, 4)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(JEDEC, 3, 1)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(DES_1, 0, 3)
    END_TYPE()

    // Provides CoreSight discovery information for the DWT
    // Reset value: 0x00000000
    BEGIN_TYPE(DWT_PIDR3_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(REVAND, 4, 4)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(CMOD, 0, 4)
    END_TYPE()

    // Provides CoreSight discovery information for the DWT
    // Reset value: 0x0000000d
    BEGIN_TYPE(DWT_CIDR0_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(PRMBL_0, 0, 8)
    END_TYPE()

    // Provides CoreSight discovery information for the DWT
    // Reset value: 0x00000090
    BEGIN_TYPE(DWT_CIDR1_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(CLASS, 4, 4)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(PRMBL_1, 0, 4)
    END_TYPE()

    // Provides CoreSight discovery information for the DWT
    // Reset value: 0x00000005
    BEGIN_TYPE(DWT_CIDR2_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(PRMBL_2, 0, 8)
    END_TYPE()

    // Provides CoreSight discovery information for the DWT
    // Reset value: 0x000000b1
    BEGIN_TYPE(DWT_CIDR3_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(PRMBL_3, 0, 8)
    END_TYPE()

    // Provides FPB implementation information, and the global enable for the FPB unit
    // Reset value: 0x60005580
    BEGIN_TYPE(FP_CTRL_t, uint32_t)
        // Flash Patch and Breakpoint Unit architecture revision
        ADD_BITFIELD_RO(REV, 28, 4)
        // Indicates the number of implemented instruction address comparators. Zero indicates no Instruction Address comparators are implemented. The Instruction Address comparators are numbered from 0 to NUM_CODE - 1
        ADD_BITFIELD_RO(NUM_CODE_14_12_, 12, 3)
        // Indicates the number of implemented literal address comparators. The Literal Address comparators are numbered from NUM_CODE to NUM_CODE + NUM_LIT - 1
        ADD_BITFIELD_RO(NUM_LIT, 8, 4)
        // Indicates the number of implemented instruction address comparators. Zero indicates no Instruction Address comparators are implemented. The Instruction Address comparators are numbered from 0 to NUM_CODE - 1
        ADD_BITFIELD_RO(NUM_CODE_7_4_, 4, 4)
        // Writes to the FP_CTRL are ignored unless KEY is concurrently written to one
        ADD_BITFIELD_RW(KEY, 1, 1)
        // Enables the FPB
        ADD_BITFIELD_RW(ENABLE, 0, 1)
    END_TYPE()

    // Indicates whether the implementation supports Flash Patch remap and, if it does, holds the target address for remap
    // Reset value: 0x00000000
    BEGIN_TYPE(FP_REMAP_t, uint32_t)
        // Indicates whether the FPB unit supports the Flash Patch remap function
        ADD_BITFIELD_RO(RMPSPT, 29, 1)
        // Holds the bits[28:5] of the Flash Patch remap address
        ADD_BITFIELD_RO(REMAP, 5, 24)
    END_TYPE()

    // Holds an address for comparison. The effect of the match depends on the configuration of the FPB and whether the comparator is an instruction address comparator or a literal address comparator
    // Reset value: 0x00000000
    BEGIN_TYPE(FP_COMP_t, uint32_t)
        // Selects between flashpatch and breakpoint functionality
        ADD_BITFIELD_RW(BE, 0, 1)
    END_TYPE()

    // Provides CoreSight discovery information for the FPB
    // Reset value: 0x47701a03
    BEGIN_TYPE(FP_DEVARCH_t, uint32_t)
        // Defines the architect of the component. Bits [31:28] are the JEP106 continuation code (JEP106 bank ID, minus 1) and bits [27:21] are the JEP106 ID code.
        ADD_BITFIELD_RO(ARCHITECT, 21, 11)
        // Defines that the DEVARCH register is present
        ADD_BITFIELD_RO(PRESENT, 20, 1)
        // Defines the architecture revision of the component
        ADD_BITFIELD_RO(REVISION, 16, 4)
        // Defines the architecture version of the component
        ADD_BITFIELD_RO(ARCHVER, 12, 4)
        // Defines the architecture of the component
        ADD_BITFIELD_RO(ARCHPART, 0, 12)
    END_TYPE()

    // Provides CoreSight discovery information for the FPB
    // Reset value: 0x00000000
    BEGIN_TYPE(FP_DEVTYPE_t, uint32_t)
        // Component sub-type
        ADD_BITFIELD_RO(SUB, 4, 4)
        // Component major type
        ADD_BITFIELD_RO(MAJOR, 0, 4)
    END_TYPE()

    // Provides CoreSight discovery information for the FP
    // Reset value: 0x00000004
    BEGIN_TYPE(FP_PIDR4_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(SIZE, 4, 4)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(DES_2, 0, 4)
    END_TYPE()

    // Provides CoreSight discovery information for the FP
    // Reset value: 0x00000000
    BEGIN_TYPE(FP_PIDR5_t, uint32_t)
        ADD_BITFIELD_RW(FP_PIDR5, 0, 32)
    END_TYPE()

    // Provides CoreSight discovery information for the FP
    // Reset value: 0x00000000
    BEGIN_TYPE(FP_PIDR6_t, uint32_t)
        ADD_BITFIELD_RW(FP_PIDR6, 0, 32)
    END_TYPE()

    // Provides CoreSight discovery information for the FP
    // Reset value: 0x00000000
    BEGIN_TYPE(FP_PIDR7_t, uint32_t)
        ADD_BITFIELD_RW(FP_PIDR7, 0, 32)
    END_TYPE()

    // Provides CoreSight discovery information for the FP
    // Reset value: 0x00000021
    BEGIN_TYPE(FP_PIDR0_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(PART_0, 0, 8)
    END_TYPE()

    // Provides CoreSight discovery information for the FP
    // Reset value: 0x000000bd
    BEGIN_TYPE(FP_PIDR1_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(DES_0, 4, 4)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(PART_1, 0, 4)
    END_TYPE()

    // Provides CoreSight discovery information for the FP
    // Reset value: 0x0000000b
    BEGIN_TYPE(FP_PIDR2_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(REVISION, 4, 4)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(JEDEC, 3, 1)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(DES_1, 0, 3)
    END_TYPE()

    // Provides CoreSight discovery information for the FP
    // Reset value: 0x00000000
    BEGIN_TYPE(FP_PIDR3_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(REVAND, 4, 4)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(CMOD, 0, 4)
    END_TYPE()

    // Provides CoreSight discovery information for the FP
    // Reset value: 0x0000000d
    BEGIN_TYPE(FP_CIDR0_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(PRMBL_0, 0, 8)
    END_TYPE()

    // Provides CoreSight discovery information for the FP
    // Reset value: 0x00000090
    BEGIN_TYPE(FP_CIDR1_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(CLASS, 4, 4)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(PRMBL_1, 0, 4)
    END_TYPE()

    // Provides CoreSight discovery information for the FP
    // Reset value: 0x00000005
    BEGIN_TYPE(FP_CIDR2_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(PRMBL_2, 0, 8)
    END_TYPE()

    // Provides CoreSight discovery information for the FP
    // Reset value: 0x000000b1
    BEGIN_TYPE(FP_CIDR3_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(PRMBL_3, 0, 8)
    END_TYPE()

    // Provides information about the interrupt controller
    // Reset value: 0x00000001
    BEGIN_TYPE(ICTR_t, uint32_t)
        // Indicates the number of the highest implemented register in each of the NVIC control register sets, or in the case of NVIC_IPR*n, 4×INTLINESNUM
        ADD_BITFIELD_RO(INTLINESNUM, 0, 4)
    END_TYPE()

    // Provides IMPLEMENTATION DEFINED configuration and control options
    // Reset value: 0x00000000
    BEGIN_TYPE(ACTLR_t, uint32_t)
        // External Exclusives Allowed with no MPU
        ADD_BITFIELD_RW(EXTEXCLALL, 29, 1)
        // Disable ATB Flush
        ADD_BITFIELD_RW(DISITMATBFLUSH, 12, 1)
        // Disable FPU exception outputs
        ADD_BITFIELD_RW(FPEXCODIS, 10, 1)
        // Disable out-of-order FP instruction completion
        ADD_BITFIELD_RW(DISOOFP, 9, 1)
        // Disable dual-issue.
        ADD_BITFIELD_RW(DISFOLD, 2, 1)
        // Disable dual-issue.
        ADD_BITFIELD_RW(DISMCYCINT, 0, 1)
    END_TYPE()

    // Use the SysTick Control and Status Register to enable the SysTick features.
    // Reset value: 0x00000000
    BEGIN_TYPE(SYST_CSR_t, uint32_t)
        // Returns 1 if timer counted to 0 since last time this was read. Clears on read by application or debugger.
        ADD_BITFIELD_RO(COUNTFLAG, 16, 1)
        // SysTick clock source. Always reads as one if SYST_CALIB reports NOREF.  Selects the SysTick timer clock source:  0 = External reference clock.  1 = Processor clock.
        ADD_BITFIELD_RW(CLKSOURCE, 2, 1)
        // Enables SysTick exception request:  0 = Counting down to zero does not assert the SysTick exception request.  1 = Counting down to zero to asserts the SysTick exception request.
        ADD_BITFIELD_RW(TICKINT, 1, 1)
        // Enable SysTick counter:  0 = Counter disabled.  1 = Counter enabled.
        ADD_BITFIELD_RW(ENABLE, 0, 1)
    END_TYPE()

    // Use the SysTick Reload Value Register to specify the start value to load into the current value register when the counter reaches 0. It can be any value between 0 and 0x00FFFFFF. A start value of 0 is possible, but has no effect because the SysTick interrupt and COUNTFLAG are activated when counting from 1 to 0. The reset value of this register is UNKNOWN.  To generate a multi-shot timer with a period of N processor clock cycles, use a RELOAD value of N-1. For example, if the SysTick interrupt is required every 100 clock pulses, set RELOAD to 99.
    // Reset value: 0x00000000
    BEGIN_TYPE(SYST_RVR_t, uint32_t)
        // Value to load into the SysTick Current Value Register when the counter reaches 0.
        ADD_BITFIELD_RW(RELOAD, 0, 24)
    END_TYPE()

    // Use the SysTick Current Value Register to find the current value in the register. The reset value of this register is UNKNOWN.
    // Reset value: 0x00000000
    BEGIN_TYPE(SYST_CVR_t, uint32_t)
        // Reads return the current value of the SysTick counter. This register is write-clear. Writing to it with any value clears the register to 0. Clearing this register also clears the COUNTFLAG bit of the SysTick Control and Status Register.
        ADD_BITFIELD_RW(CURRENT, 0, 24)
    END_TYPE()

    // Use the SysTick Calibration Value Register to enable software to scale to any required speed using divide and multiply.
    // Reset value: 0x00000000
    BEGIN_TYPE(SYST_CALIB_t, uint32_t)
        // If reads as 1, the Reference clock is not provided - the CLKSOURCE bit of the SysTick Control and Status register will be forced to 1 and cannot be cleared to 0.
        ADD_BITFIELD_RO(NOREF, 31, 1)
        // If reads as 1, the calibration value for 10ms is inexact (due to clock frequency).
        ADD_BITFIELD_RO(SKEW, 30, 1)
        // An optional Reload value to be used for 10ms (100Hz) timing, subject to system clock skew errors. If the value reads as 0, the calibration value is not known.
        ADD_BITFIELD_RO(TENMS, 0, 24)
    END_TYPE()

    // Enables or reads the enabled state of each group of 32 interrupts
    // Reset value: 0x00000000
    BEGIN_TYPE(NVIC_ISER0_t, uint32_t)
        // For SETENA[m] in NVIC_ISER*n, indicates whether interrupt 32*n + m is enabled
        ADD_BITFIELD_RW(SETENA, 0, 32)
    END_TYPE()

    // Enables or reads the enabled state of each group of 32 interrupts
    // Reset value: 0x00000000
    BEGIN_TYPE(NVIC_ISER1_t, uint32_t)
        // For SETENA[m] in NVIC_ISER*n, indicates whether interrupt 32*n + m is enabled
        ADD_BITFIELD_RW(SETENA, 0, 32)
    END_TYPE()

    // Clears or reads the enabled state of each group of 32 interrupts
    // Reset value: 0x00000000
    BEGIN_TYPE(NVIC_ICER0_t, uint32_t)
        // For CLRENA[m] in NVIC_ICER*n, indicates whether interrupt 32*n + m is enabled
        ADD_BITFIELD_RW(CLRENA, 0, 32)
    END_TYPE()

    // Clears or reads the enabled state of each group of 32 interrupts
    // Reset value: 0x00000000
    BEGIN_TYPE(NVIC_ICER1_t, uint32_t)
        // For CLRENA[m] in NVIC_ICER*n, indicates whether interrupt 32*n + m is enabled
        ADD_BITFIELD_RW(CLRENA, 0, 32)
    END_TYPE()

    // Enables or reads the pending state of each group of 32 interrupts
    // Reset value: 0x00000000
    BEGIN_TYPE(NVIC_ISPR0_t, uint32_t)
        // For SETPEND[m] in NVIC_ISPR*n, indicates whether interrupt 32*n + m is pending
        ADD_BITFIELD_RW(SETPEND, 0, 32)
    END_TYPE()

    // Enables or reads the pending state of each group of 32 interrupts
    // Reset value: 0x00000000
    BEGIN_TYPE(NVIC_ISPR1_t, uint32_t)
        // For SETPEND[m] in NVIC_ISPR*n, indicates whether interrupt 32*n + m is pending
        ADD_BITFIELD_RW(SETPEND, 0, 32)
    END_TYPE()

    // Clears or reads the pending state of each group of 32 interrupts
    // Reset value: 0x00000000
    BEGIN_TYPE(NVIC_ICPR0_t, uint32_t)
        // For CLRPEND[m] in NVIC_ICPR*n, indicates whether interrupt 32*n + m is pending
        ADD_BITFIELD_RW(CLRPEND, 0, 32)
    END_TYPE()

    // Clears or reads the pending state of each group of 32 interrupts
    // Reset value: 0x00000000
    BEGIN_TYPE(NVIC_ICPR1_t, uint32_t)
        // For CLRPEND[m] in NVIC_ICPR*n, indicates whether interrupt 32*n + m is pending
        ADD_BITFIELD_RW(CLRPEND, 0, 32)
    END_TYPE()

    // For each group of 32 interrupts, shows the active state of each interrupt
    // Reset value: 0x00000000
    BEGIN_TYPE(NVIC_IABR0_t, uint32_t)
        // For ACTIVE[m] in NVIC_IABR*n, indicates the active state for interrupt 32*n+m
        ADD_BITFIELD_RW(ACTIVE, 0, 32)
    END_TYPE()

    // For each group of 32 interrupts, shows the active state of each interrupt
    // Reset value: 0x00000000
    BEGIN_TYPE(NVIC_IABR1_t, uint32_t)
        // For ACTIVE[m] in NVIC_IABR*n, indicates the active state for interrupt 32*n+m
        ADD_BITFIELD_RW(ACTIVE, 0, 32)
    END_TYPE()

    // For each group of 32 interrupts, determines whether each interrupt targets Non-secure or Secure state
    // Reset value: 0x00000000
    BEGIN_TYPE(NVIC_ITNS0_t, uint32_t)
        // For ITNS[m] in NVIC_ITNS*n, `IAAMO the target Security state for interrupt 32*n+m
        ADD_BITFIELD_RW(ITNS, 0, 32)
    END_TYPE()

    // For each group of 32 interrupts, determines whether each interrupt targets Non-secure or Secure state
    // Reset value: 0x00000000
    BEGIN_TYPE(NVIC_ITNS1_t, uint32_t)
        // For ITNS[m] in NVIC_ITNS*n, `IAAMO the target Security state for interrupt 32*n+m
        ADD_BITFIELD_RW(ITNS, 0, 32)
    END_TYPE()

    // Sets or reads interrupt priorities
    // Reset value: 0x00000000
    BEGIN_TYPE(NVIC_IPR_t, uint32_t)
        // For register NVIC_IPRn, the priority of interrupt number 4*n+3, or RES0 if the PE does not implement this interrupt
        ADD_BITFIELD_RW(PRI_N3, 28, 4)
        // For register NVIC_IPRn, the priority of interrupt number 4*n+2, or RES0 if the PE does not implement this interrupt
        ADD_BITFIELD_RW(PRI_N2, 20, 4)
        // For register NVIC_IPRn, the priority of interrupt number 4*n+1, or RES0 if the PE does not implement this interrupt
        ADD_BITFIELD_RW(PRI_N1, 12, 4)
        // For register NVIC_IPRn, the priority of interrupt number 4*n+0, or RES0 if the PE does not implement this interrupt
        ADD_BITFIELD_RW(PRI_N0, 4, 4)
    END_TYPE()

    // Provides identification information for the PE, including an implementer code for the device and a device ID number
    // Reset value: 0x411fd210
    BEGIN_TYPE(CPUID_t, uint32_t)
        // This field must hold an implementer code that has been assigned by ARM
        ADD_BITFIELD_RO(IMPLEMENTER, 24, 8)
        // IMPLEMENTATION DEFINED variant number. Typically, this field is used to distinguish between different product variants, or major revisions of a product
        ADD_BITFIELD_RO(VARIANT, 20, 4)
        // Defines the Architecture implemented by the PE
        ADD_BITFIELD_RO(ARCHITECTURE, 16, 4)
        // IMPLEMENTATION DEFINED primary part number for the device
        ADD_BITFIELD_RO(PARTNO, 4, 12)
        // IMPLEMENTATION DEFINED revision number for the device
        ADD_BITFIELD_RO(REVISION, 0, 4)
    END_TYPE()

    // Controls and provides status information for NMI, PendSV, SysTick and interrupts
    // Reset value: 0x00000000
    BEGIN_TYPE(ICSR_t, uint32_t)
        // Indicates whether the NMI exception is pending
        ADD_BITFIELD_RO(PENDNMISET, 31, 1)
        // Allows the NMI exception pend state to be cleared
        ADD_BITFIELD_RW(PENDNMICLR, 30, 1)
        // Indicates whether the PendSV `FTSSS exception is pending
        ADD_BITFIELD_RO(PENDSVSET, 28, 1)
        // Allows the PendSV exception pend state to be cleared `FTSSS
        ADD_BITFIELD_RW(PENDSVCLR, 27, 1)
        // Indicates whether the SysTick `FTSSS exception is pending
        ADD_BITFIELD_RO(PENDSTSET, 26, 1)
        // Allows the SysTick exception pend state to be cleared `FTSSS
        ADD_BITFIELD_RW(PENDSTCLR, 25, 1)
        // Controls whether in a single SysTick implementation, the SysTick is Secure or Non-secure
        ADD_BITFIELD_RW(STTNS, 24, 1)
        // Indicates whether a pending exception will be serviced on exit from debug halt state
        ADD_BITFIELD_RO(ISRPREEMPT, 23, 1)
        // Indicates whether an external interrupt, generated by the NVIC, is pending
        ADD_BITFIELD_RO(ISRPENDING, 22, 1)
        // The exception number of the highest priority pending and enabled interrupt
        ADD_BITFIELD_RO(VECTPENDING, 12, 9)
        // In Handler mode, indicates whether there is more than one active exception
        ADD_BITFIELD_RO(RETTOBASE, 11, 1)
        // The exception number of the current executing exception
        ADD_BITFIELD_RO(VECTACTIVE, 0, 9)
    END_TYPE()

    // The VTOR indicates the offset of the vector table base address from memory address 0x00000000.
    // Reset value: 0x00000000
    BEGIN_TYPE(VTOR_t, uint32_t)
        // Vector table base offset field. It contains bits[31:7] of the offset of the table base from the bottom of the memory map.
        ADD_BITFIELD_RW(TBLOFF, 7, 25)
    END_TYPE()

    // Use the Application Interrupt and Reset Control Register to: determine data endianness, clear all active state information from debug halt mode, request a system reset.
    // Reset value: 0x00000000
    BEGIN_TYPE(AIRCR_t, uint32_t)
        // Register key:  Reads as Unknown  On writes, write 0x05FA to VECTKEY, otherwise the write is ignored.
        ADD_BITFIELD_RW(VECTKEY, 16, 16)
        // Data endianness implemented:  0 = Little-endian.
        ADD_BITFIELD_RO(ENDIANESS, 15, 1)
        // Prioritize Secure exceptions. The value of this bit defines whether Secure exception priority boosting is enabled.  0	Priority ranges of Secure and Non-secure exceptions are identical.  1	Non-secure exceptions are de-prioritized.
        ADD_BITFIELD_RW(PRIS, 14, 1)
        // BusFault, HardFault, and NMI Non-secure enable.  0	BusFault, HardFault, and NMI are Secure.  1	BusFault and NMI are Non-secure and exceptions can target Non-secure HardFault.
        ADD_BITFIELD_RW(BFHFNMINS, 13, 1)
        // Interrupt priority grouping field. This field determines the split of group priority from subpriority.  See https://developer.arm.com/documentation/100235/0004/the-cortex-m33-peripherals/system-control-block/application-interrupt-and-reset-control-register?lang=en
        ADD_BITFIELD_RW(PRIGROUP, 8, 3)
        // System reset request, Secure state only.  0	SYSRESETREQ functionality is available to both Security states.  1 SYSRESETREQ functionality is only available to Secure state.
        ADD_BITFIELD_RW(SYSRESETREQS, 3, 1)
        // Writing 1 to this bit causes the SYSRESETREQ signal to the outer system to be asserted to request a reset. The intention is to force a large system reset of all major components except for debug. The C_HALT bit in the DHCSR is cleared as a result of the system reset requested. The debugger does not lose contact with the device.
        ADD_BITFIELD_RW(SYSRESETREQ, 2, 1)
        // Clears all active state information for fixed and configurable exceptions. This bit: is self-clearing, can only be set by the DAP when the core is halted.  When set: clears all active exception status of the processor, forces a return to Thread mode, forces an IPSR of 0. A debugger must re-initialize the stack.
        ADD_BITFIELD_RW(VECTCLRACTIVE, 1, 1)
    END_TYPE()

    // System Control Register. Use the System Control Register for power-management functions: signal to the system when the processor can enter a low power state, control how the processor enters and exits low power states.
    // Reset value: 0x00000000
    BEGIN_TYPE(SCR_t, uint32_t)
        // Send Event on Pending bit:  0 = Only enabled interrupts or events can wakeup the processor, disabled interrupts are excluded.  1 = Enabled events and all interrupts, including disabled interrupts, can wakeup the processor.  When an event or interrupt becomes pending, the event signal wakes up the processor from WFE. If the  processor is not waiting for an event, the event is registered and affects the next WFE.  The processor also wakes up on execution of an SEV instruction or an external event.
        ADD_BITFIELD_RW(SEVONPEND, 4, 1)
        // 0 SLEEPDEEP is available to both security states  1 SLEEPDEEP is only available to Secure state
        ADD_BITFIELD_RW(SLEEPDEEPS, 3, 1)
        // Controls whether the processor uses sleep or deep sleep as its low power mode:  0 = Sleep.  1 = Deep sleep.
        ADD_BITFIELD_RW(SLEEPDEEP, 2, 1)
        // Indicates sleep-on-exit when returning from Handler mode to Thread mode:  0 = Do not sleep when returning to Thread mode.  1 = Enter sleep, or deep sleep, on return from an ISR to Thread mode.  Setting this bit to 1 enables an interrupt driven application to avoid returning to an empty main application.
        ADD_BITFIELD_RW(SLEEPONEXIT, 1, 1)
    END_TYPE()

    // Sets or returns configuration and control data
    // Reset value: 0x00000201
    BEGIN_TYPE(CCR_t, uint32_t)
        // Enables program flow prediction `FTSSS
        ADD_BITFIELD_RO(BP, 18, 1)
        // This is a global enable bit for instruction caches in the selected Security state
        ADD_BITFIELD_RO(IC, 17, 1)
        // Enables data caching of all data accesses to Normal memory `FTSSS
        ADD_BITFIELD_RO(DC, 16, 1)
        // Controls the effect of a stack limit violation while executing at a requested priority less than 0
        ADD_BITFIELD_RW(STKOFHFNMIGN, 10, 1)
        // Reserved, RES1
        ADD_BITFIELD_RO(RES1, 9, 1)
        // Determines the effect of precise BusFaults on handlers running at a requested priority less than 0
        ADD_BITFIELD_RW(BFHFNMIGN, 8, 1)
        // Controls the generation of a DIVBYZERO UsageFault when attempting to perform integer division by zero
        ADD_BITFIELD_RW(DIV_0_TRP, 4, 1)
        // Controls the trapping of unaligned word or halfword accesses
        ADD_BITFIELD_RW(UNALIGN_TRP, 3, 1)
        // Determines whether unprivileged accesses are permitted to pend interrupts via the STIR
        ADD_BITFIELD_RW(USERSETMPEND, 1, 1)
        // Reserved, RES1
        ADD_BITFIELD_RO(RES1_1, 0, 1)
    END_TYPE()

    // Sets or returns priority for system handlers 4 - 7
    // Reset value: 0x00000000
    BEGIN_TYPE(SHPR1_t, uint32_t)
        // Priority of system handler 7, SecureFault
        ADD_BITFIELD_RW(PRI_7_3, 29, 3)
        // Priority of system handler 6, SecureFault
        ADD_BITFIELD_RW(PRI_6_3, 21, 3)
        // Priority of system handler 5, SecureFault
        ADD_BITFIELD_RW(PRI_5_3, 13, 3)
        // Priority of system handler 4, SecureFault
        ADD_BITFIELD_RW(PRI_4_3, 5, 3)
    END_TYPE()

    // Sets or returns priority for system handlers 8 - 11
    // Reset value: 0x00000000
    BEGIN_TYPE(SHPR2_t, uint32_t)
        // Priority of system handler 11, SecureFault
        ADD_BITFIELD_RW(PRI_11_3, 29, 3)
        // Reserved, RES0
        ADD_BITFIELD_RO(PRI_10, 16, 8)
        // Reserved, RES0
        ADD_BITFIELD_RO(PRI_9, 8, 8)
        // Reserved, RES0
        ADD_BITFIELD_RO(PRI_8, 0, 8)
    END_TYPE()

    // Sets or returns priority for system handlers 12 - 15
    // Reset value: 0x00000000
    BEGIN_TYPE(SHPR3_t, uint32_t)
        // Priority of system handler 15, SecureFault
        ADD_BITFIELD_RW(PRI_15_3, 29, 3)
        // Priority of system handler 14, SecureFault
        ADD_BITFIELD_RW(PRI_14_3, 21, 3)
        // Reserved, RES0
        ADD_BITFIELD_RO(PRI_13, 8, 8)
        // Priority of system handler 12, SecureFault
        ADD_BITFIELD_RW(PRI_12_3, 5, 3)
    END_TYPE()

    // Provides access to the active and pending status of system exceptions
    // Reset value: 0x00000000
    BEGIN_TYPE(SHCSR_t, uint32_t)
        // `IAAMO the pending state of the HardFault exception `CTTSSS
        ADD_BITFIELD_RW(HARDFAULTPENDED, 21, 1)
        // `IAAMO the pending state of the SecureFault exception
        ADD_BITFIELD_RW(SECUREFAULTPENDED, 20, 1)
        // `DW the SecureFault exception is enabled
        ADD_BITFIELD_RW(SECUREFAULTENA, 19, 1)
        // `DW the UsageFault exception is enabled `FTSSS
        ADD_BITFIELD_RW(USGFAULTENA, 18, 1)
        // `DW the BusFault exception is enabled
        ADD_BITFIELD_RW(BUSFAULTENA, 17, 1)
        // `DW the MemManage exception is enabled `FTSSS
        ADD_BITFIELD_RW(MEMFAULTENA, 16, 1)
        // `IAAMO the pending state of the SVCall exception `FTSSS
        ADD_BITFIELD_RW(SVCALLPENDED, 15, 1)
        // `IAAMO the pending state of the BusFault exception
        ADD_BITFIELD_RW(BUSFAULTPENDED, 14, 1)
        // `IAAMO the pending state of the MemManage exception `FTSSS
        ADD_BITFIELD_RW(MEMFAULTPENDED, 13, 1)
        // The UsageFault exception is banked between Security states, `IAAMO the pending state of the UsageFault exception `FTSSS
        ADD_BITFIELD_RW(USGFAULTPENDED, 12, 1)
        // `IAAMO the active state of the SysTick exception `FTSSS
        ADD_BITFIELD_RW(SYSTICKACT, 11, 1)
        // `IAAMO the active state of the PendSV exception `FTSSS
        ADD_BITFIELD_RW(PENDSVACT, 10, 1)
        // `IAAMO the active state of the DebugMonitor exception
        ADD_BITFIELD_RW(MONITORACT, 8, 1)
        // `IAAMO the active state of the SVCall exception `FTSSS
        ADD_BITFIELD_RW(SVCALLACT, 7, 1)
        // `IAAMO the active state of the NMI exception
        ADD_BITFIELD_RW(NMIACT, 5, 1)
        // `IAAMO the active state of the SecureFault exception
        ADD_BITFIELD_RW(SECUREFAULTACT, 4, 1)
        // `IAAMO the active state of the UsageFault exception `FTSSS
        ADD_BITFIELD_RW(USGFAULTACT, 3, 1)
        // Indicates and allows limited modification of the active state of the HardFault exception `FTSSS
        ADD_BITFIELD_RW(HARDFAULTACT, 2, 1)
        // `IAAMO the active state of the BusFault exception
        ADD_BITFIELD_RW(BUSFAULTACT, 1, 1)
        // `IAAMO the active state of the MemManage exception `FTSSS
        ADD_BITFIELD_RW(MEMFAULTACT, 0, 1)
    END_TYPE()

    // Contains the three Configurable Fault Status Registers. 31:16 UFSR: Provides information on UsageFault exceptions 15:8 BFSR: Provides information on BusFault exceptions 7:0 MMFSR: Provides information on MemManage exceptions
    // Reset value: 0x00000000
    BEGIN_TYPE(CFSR_t, uint32_t)
        // Sticky flag indicating whether an integer division by zero error has occurred
        ADD_BITFIELD_RW(UFSR_DIVBYZERO, 25, 1)
        // Sticky flag indicating whether an unaligned access error has occurred
        ADD_BITFIELD_RW(UFSR_UNALIGNED, 24, 1)
        // Sticky flag indicating whether a stack overflow error has occurred
        ADD_BITFIELD_RW(UFSR_STKOF, 20, 1)
        // Sticky flag indicating whether a coprocessor disabled or not present error has occurred
        ADD_BITFIELD_RW(UFSR_NOCP, 19, 1)
        // Sticky flag indicating whether an integrity check error has occurred
        ADD_BITFIELD_RW(UFSR_INVPC, 18, 1)
        // Sticky flag indicating whether an EPSR.T or EPSR.IT validity error has occurred
        ADD_BITFIELD_RW(UFSR_INVSTATE, 17, 1)
        // Sticky flag indicating whether an undefined instruction error has occurred
        ADD_BITFIELD_RW(UFSR_UNDEFINSTR, 16, 1)
        // Indicates validity of the contents of the BFAR register
        ADD_BITFIELD_RW(BFSR_BFARVALID, 15, 1)
        // Records whether a BusFault occurred during FP lazy state preservation
        ADD_BITFIELD_RW(BFSR_LSPERR, 13, 1)
        // Records whether a derived BusFault occurred during exception entry stacking
        ADD_BITFIELD_RW(BFSR_STKERR, 12, 1)
        // Records whether a derived BusFault occurred during exception return unstacking
        ADD_BITFIELD_RW(BFSR_UNSTKERR, 11, 1)
        // Records whether an imprecise data access error has occurred
        ADD_BITFIELD_RW(BFSR_IMPRECISERR, 10, 1)
        // Records whether a precise data access error has occurred
        ADD_BITFIELD_RW(BFSR_PRECISERR, 9, 1)
        // Records whether a BusFault on an instruction prefetch has occurred
        ADD_BITFIELD_RW(BFSR_IBUSERR, 8, 1)
        // Provides information on MemManage exceptions
        ADD_BITFIELD_RW(MMFSR, 0, 8)
    END_TYPE()

    // Shows the cause of any HardFaults
    // Reset value: 0x00000000
    BEGIN_TYPE(HFSR_t, uint32_t)
        // Indicates when a Debug event has occurred
        ADD_BITFIELD_RW(DEBUGEVT, 31, 1)
        // Indicates that a fault with configurable priority has been escalated to a HardFault exception, because it could not be made active, because of priority, or because it was disabled
        ADD_BITFIELD_RW(FORCED, 30, 1)
        // Indicates when a fault has occurred because of a vector table read error on exception processing
        ADD_BITFIELD_RW(VECTTBL, 1, 1)
    END_TYPE()

    // Shows which debug event occurred
    // Reset value: 0x00000000
    BEGIN_TYPE(DFSR_t, uint32_t)
        // Sticky flag indicating whether an External debug request debug event has occurred
        ADD_BITFIELD_RW(EXTERNAL, 4, 1)
        // Sticky flag indicating whether a Vector catch debug event has occurred
        ADD_BITFIELD_RW(VCATCH, 3, 1)
        // Sticky flag indicating whether a Watchpoint debug event has occurred
        ADD_BITFIELD_RW(DWTTRAP, 2, 1)
        // Sticky flag indicating whether a Breakpoint debug event has occurred
        ADD_BITFIELD_RW(BKPT, 1, 1)
        // Sticky flag indicating that a Halt request debug event or Step debug event has occurred
        ADD_BITFIELD_RW(HALTED, 0, 1)
    END_TYPE()

    // Shows the address of the memory location that caused an MPU fault
    // Reset value: 0x00000000
    BEGIN_TYPE(MMFAR_t, uint32_t)
        // This register is updated with the address of a location that produced a MemManage fault. The MMFSR shows the cause of the fault, and whether this field is valid. This field is valid only when MMFSR.MMARVALID is set, otherwise it is UNKNOWN
        ADD_BITFIELD_RW(ADDRESS, 0, 32)
    END_TYPE()

    // Shows the address associated with a precise data access BusFault
    // Reset value: 0x00000000
    BEGIN_TYPE(BFAR_t, uint32_t)
        // This register is updated with the address of a location that produced a BusFault. The BFSR shows the reason for the fault. This field is valid only when BFSR.BFARVALID is set, otherwise it is UNKNOWN
        ADD_BITFIELD_RW(ADDRESS, 0, 32)
    END_TYPE()

    // Gives top-level information about the instruction set supported by the PE
    // Reset value: 0x00000030
    BEGIN_TYPE(ID_PFR0_t, uint32_t)
        // T32 instruction set support
        ADD_BITFIELD_RO(STATE1, 4, 4)
        // A32 instruction set support
        ADD_BITFIELD_RO(STATE0, 0, 4)
    END_TYPE()

    // Gives information about the programmers' model and Extensions support
    // Reset value: 0x00000520
    BEGIN_TYPE(ID_PFR1_t, uint32_t)
        // Identifies support for the M-Profile programmers' model support
        ADD_BITFIELD_RO(MPROGMOD, 8, 4)
        // Identifies whether the Security Extension is implemented
        ADD_BITFIELD_RO(SECURITY, 4, 4)
    END_TYPE()

    // Provides top level information about the debug system
    // Reset value: 0x00200000
    BEGIN_TYPE(ID_DFR0_t, uint32_t)
        // Indicates the supported M-profile debug architecture
        ADD_BITFIELD_RO(MPROFDBG, 20, 4)
    END_TYPE()

    // Provides information about the IMPLEMENTATION DEFINED features of the PE
    // Reset value: 0x00000000
    BEGIN_TYPE(ID_AFR0_t, uint32_t)
        // IMPLEMENTATION DEFINED meaning
        ADD_BITFIELD_RO(IMPDEF3, 12, 4)
        // IMPLEMENTATION DEFINED meaning
        ADD_BITFIELD_RO(IMPDEF2, 8, 4)
        // IMPLEMENTATION DEFINED meaning
        ADD_BITFIELD_RO(IMPDEF1, 4, 4)
        // IMPLEMENTATION DEFINED meaning
        ADD_BITFIELD_RO(IMPDEF0, 0, 4)
    END_TYPE()

    // Provides information about the implemented memory model and memory management support
    // Reset value: 0x00101f40
    BEGIN_TYPE(ID_MMFR0_t, uint32_t)
        // Indicates support for Auxiliary Control Registers
        ADD_BITFIELD_RO(AUXREG, 20, 4)
        // Indicates support for tightly coupled memories (TCMs)
        ADD_BITFIELD_RO(TCM, 16, 4)
        // Indicates the number of shareability levels implemented
        ADD_BITFIELD_RO(SHARELVL, 12, 4)
        // Indicates the outermost shareability domain implemented
        ADD_BITFIELD_RO(OUTERSHR, 8, 4)
        // Indicates support for the protected memory system architecture (PMSA)
        ADD_BITFIELD_RO(PMSA, 4, 4)
    END_TYPE()

    // Provides information about the implemented memory model and memory management support
    // Reset value: 0x00000000
    BEGIN_TYPE(ID_MMFR1_t, uint32_t)
        ADD_BITFIELD_RW(ID_MMFR1, 0, 32)
    END_TYPE()

    // Provides information about the implemented memory model and memory management support
    // Reset value: 0x01000000
    BEGIN_TYPE(ID_MMFR2_t, uint32_t)
        // Indicates the support for Wait For Interrupt (WFI) stalling
        ADD_BITFIELD_RO(WFISTALL, 24, 4)
    END_TYPE()

    // Provides information about the implemented memory model and memory management support
    // Reset value: 0x00000000
    BEGIN_TYPE(ID_MMFR3_t, uint32_t)
        // Indicates the supported branch predictor maintenance
        ADD_BITFIELD_RO(BPMAINT, 8, 4)
        // Indicates the supported cache maintenance operations by set/way
        ADD_BITFIELD_RO(CMAINTSW, 4, 4)
        // Indicates the supported cache maintenance operations by address
        ADD_BITFIELD_RO(CMAINTVA, 0, 4)
    END_TYPE()

    // Provides information about the instruction set implemented by the PE
    // Reset value: 0x08092300
    BEGIN_TYPE(ID_ISAR0_t, uint32_t)
        // Indicates the supported Divide instructions
        ADD_BITFIELD_RO(DIVIDE, 24, 4)
        // Indicates the implemented Debug instructions
        ADD_BITFIELD_RO(DEBUG, 20, 4)
        // Indicates the supported Coprocessor instructions
        ADD_BITFIELD_RO(COPROC, 16, 4)
        // Indicates the supported combined Compare and Branch instructions
        ADD_BITFIELD_RO(CMPBRANCH, 12, 4)
        // Indicates the supported bit field instructions
        ADD_BITFIELD_RO(BITFIELD, 8, 4)
        // Indicates the supported bit count instructions
        ADD_BITFIELD_RO(BITCOUNT, 4, 4)
    END_TYPE()

    // Provides information about the instruction set implemented by the PE
    // Reset value: 0x05725000
    BEGIN_TYPE(ID_ISAR1_t, uint32_t)
        // Indicates the implemented Interworking instructions
        ADD_BITFIELD_RO(INTERWORK, 24, 4)
        // Indicates the implemented for data-processing instructions with long immediates
        ADD_BITFIELD_RO(IMMEDIATE, 20, 4)
        // Indicates the implemented If-Then instructions
        ADD_BITFIELD_RO(IFTHEN, 16, 4)
        // Indicates the implemented Extend instructions
        ADD_BITFIELD_RO(EXTEND, 12, 4)
    END_TYPE()

    // Provides information about the instruction set implemented by the PE
    // Reset value: 0x30173426
    BEGIN_TYPE(ID_ISAR2_t, uint32_t)
        // Indicates the implemented Reversal instructions
        ADD_BITFIELD_RO(REVERSAL, 28, 4)
        // Indicates the implemented advanced unsigned Multiply instructions
        ADD_BITFIELD_RO(MULTU, 20, 4)
        // Indicates the implemented advanced signed Multiply instructions
        ADD_BITFIELD_RO(MULTS, 16, 4)
        // Indicates the implemented additional Multiply instructions
        ADD_BITFIELD_RO(MULT, 12, 4)
        // Indicates the support for interruptible multi-access instructions
        ADD_BITFIELD_RO(MULTIACCESSINT, 8, 4)
        // Indicates the implemented Memory Hint instructions
        ADD_BITFIELD_RO(MEMHINT, 4, 4)
        // Indicates the implemented additional load/store instructions
        ADD_BITFIELD_RO(LOADSTORE, 0, 4)
    END_TYPE()

    // Provides information about the instruction set implemented by the PE
    // Reset value: 0x07895729
    BEGIN_TYPE(ID_ISAR3_t, uint32_t)
        // Indicates the implemented true NOP instructions
        ADD_BITFIELD_RO(TRUENOP, 24, 4)
        // Indicates the support for T32 non flag-setting MOV instructions
        ADD_BITFIELD_RO(T32COPY, 20, 4)
        // Indicates the implemented Table Branch instructions
        ADD_BITFIELD_RO(TABBRANCH, 16, 4)
        // Used in conjunction with ID_ISAR4.SynchPrim_frac to indicate the implemented Synchronization Primitive instructions
        ADD_BITFIELD_RO(SYNCHPRIM, 12, 4)
        // Indicates the implemented SVC instructions
        ADD_BITFIELD_RO(SVC, 8, 4)
        // Indicates the implemented SIMD instructions
        ADD_BITFIELD_RO(SIMD, 4, 4)
        // Indicates the implemented saturating instructions
        ADD_BITFIELD_RO(SATURATE, 0, 4)
    END_TYPE()

    // Provides information about the instruction set implemented by the PE
    // Reset value: 0x01310132
    BEGIN_TYPE(ID_ISAR4_t, uint32_t)
        // Indicates the implemented M profile instructions to modify the PSRs
        ADD_BITFIELD_RO(PSR_M, 24, 4)
        // Used in conjunction with ID_ISAR3.SynchPrim to indicate the implemented Synchronization Primitive instructions
        ADD_BITFIELD_RO(SYNCPRIM_FRAC, 20, 4)
        // Indicates the implemented Barrier instructions
        ADD_BITFIELD_RO(BARRIER, 16, 4)
        // Indicates the support for writeback addressing modes
        ADD_BITFIELD_RO(WRITEBACK, 8, 4)
        // Indicates the support for writeback addressing modes
        ADD_BITFIELD_RO(WITHSHIFTS, 4, 4)
        // Indicates the implemented unprivileged instructions
        ADD_BITFIELD_RO(UNPRIV, 0, 4)
    END_TYPE()

    // Provides information about the instruction set implemented by the PE
    // Reset value: 0x00000000
    BEGIN_TYPE(ID_ISAR5_t, uint32_t)
        ADD_BITFIELD_RW(ID_ISAR5, 0, 32)
    END_TYPE()

    // Provides information about the architecture of the caches. CTR is RES0 if CLIDR is zero.
    // Reset value: 0x8000c000
    BEGIN_TYPE(CTR_t, uint32_t)
        // Reserved, RES1
        ADD_BITFIELD_RO(RES1, 31, 1)
        // Log2 of the number of words of the maximum size of memory that can be overwritten as a result of the eviction of a cache entry that has had a memory location in it modified
        ADD_BITFIELD_RO(CWG, 24, 4)
        // Log2 of the number of words of the maximum size of the reservation granule that has been implemented for the Load-Exclusive and Store-Exclusive instructions
        ADD_BITFIELD_RO(ERG, 20, 4)
        // Log2 of the number of words in the smallest cache line of all the data caches and unified caches that are controlled by the PE
        ADD_BITFIELD_RO(DMINLINE, 16, 4)
        // Reserved, RES1
        ADD_BITFIELD_RO(RES1_1, 14, 2)
        // Log2 of the number of words in the smallest cache line of all the instruction caches that are controlled by the PE
        ADD_BITFIELD_RO(IMINLINE, 0, 4)
    END_TYPE()

    // Specifies the access privileges for coprocessors and the FP Extension
    // Reset value: 0x00000000
    BEGIN_TYPE(CPACR_t, uint32_t)
        // The value in this field is ignored. If the implementation does not include the FP Extension, this field is RAZ/WI. If the value of this bit is not programmed to the same value as the CP10 field, then the value is UNKNOWN
        ADD_BITFIELD_RW(CP11, 22, 2)
        // Defines the access rights for the floating-point functionality
        ADD_BITFIELD_RW(CP10, 20, 2)
        // Controls access privileges for coprocessor 7
        ADD_BITFIELD_RW(CP7, 14, 2)
        // Controls access privileges for coprocessor 6
        ADD_BITFIELD_RW(CP6, 12, 2)
        // Controls access privileges for coprocessor 5
        ADD_BITFIELD_RW(CP5, 10, 2)
        // Controls access privileges for coprocessor 4
        ADD_BITFIELD_RW(CP4, 8, 2)
        // Controls access privileges for coprocessor 3
        ADD_BITFIELD_RW(CP3, 6, 2)
        // Controls access privileges for coprocessor 2
        ADD_BITFIELD_RW(CP2, 4, 2)
        // Controls access privileges for coprocessor 1
        ADD_BITFIELD_RW(CP1, 2, 2)
        // Controls access privileges for coprocessor 0
        ADD_BITFIELD_RW(CP0, 0, 2)
    END_TYPE()

    // Defines the Non-secure access permissions for both the FP Extension and coprocessors CP0 to CP7
    // Reset value: 0x00000000
    BEGIN_TYPE(NSACR_t, uint32_t)
        // Enables Non-secure access to the Floating-point Extension
        ADD_BITFIELD_RW(CP11, 11, 1)
        // Enables Non-secure access to the Floating-point Extension
        ADD_BITFIELD_RW(CP10, 10, 1)
        // Enables Non-secure access to coprocessor CP7
        ADD_BITFIELD_RW(CP7, 7, 1)
        // Enables Non-secure access to coprocessor CP6
        ADD_BITFIELD_RW(CP6, 6, 1)
        // Enables Non-secure access to coprocessor CP5
        ADD_BITFIELD_RW(CP5, 5, 1)
        // Enables Non-secure access to coprocessor CP4
        ADD_BITFIELD_RW(CP4, 4, 1)
        // Enables Non-secure access to coprocessor CP3
        ADD_BITFIELD_RW(CP3, 3, 1)
        // Enables Non-secure access to coprocessor CP2
        ADD_BITFIELD_RW(CP2, 2, 1)
        // Enables Non-secure access to coprocessor CP1
        ADD_BITFIELD_RW(CP1, 1, 1)
        // Enables Non-secure access to coprocessor CP0
        ADD_BITFIELD_RW(CP0, 0, 1)
    END_TYPE()

    // The MPU Type Register indicates how many regions the MPU `FTSSS supports
    // Reset value: 0x00000800
    BEGIN_TYPE(MPU_TYPE_t, uint32_t)
        // Number of regions supported by the MPU
        ADD_BITFIELD_RO(DREGION, 8, 8)
        // Indicates support for separate instructions and data address regions
        ADD_BITFIELD_RO(SEPARATE, 0, 1)
    END_TYPE()

    // Enables the MPU and, when the MPU is enabled, controls whether the default memory map is enabled as a background region for privileged accesses, and whether the MPU is enabled for HardFaults, NMIs, and exception handlers when FAULTMASK is set to 1
    // Reset value: 0x00000000
    BEGIN_TYPE(MPU_CTRL_t, uint32_t)
        // Controls whether the default memory map is enabled for privileged software
        ADD_BITFIELD_RW(PRIVDEFENA, 2, 1)
        // Controls whether handlers executing with priority less than 0 access memory with the MPU enabled or disabled. This applies to HardFaults, NMIs, and exception handlers when FAULTMASK is set to 1
        ADD_BITFIELD_RW(HFNMIENA, 1, 1)
        // Enables the MPU
        ADD_BITFIELD_RW(ENABLE, 0, 1)
    END_TYPE()

    // Selects the region currently accessed by MPU_RBAR and MPU_RLAR
    // Reset value: 0x00000000
    BEGIN_TYPE(MPU_RNR_t, uint32_t)
        // Indicates the memory region accessed by MPU_RBAR and MPU_RLAR
        ADD_BITFIELD_RW(REGION, 0, 3)
    END_TYPE()

    // Provides indirect read and write access to the base address of the currently selected MPU region `FTSSS
    // Reset value: 0x00000000
    BEGIN_TYPE(MPU_RBAR_t, uint32_t)
        // Contains bits [31:5] of the lower inclusive limit of the selected MPU memory region. This value is zero extended to provide the base address to be checked against
        ADD_BITFIELD_RW(BASE, 5, 27)
        // Defines the Shareability domain of this region for Normal memory
        ADD_BITFIELD_RW(SH, 3, 2)
        // Defines the access permissions for this region
        ADD_BITFIELD_RW(AP, 1, 2)
        // Defines whether code can be executed from this region
        ADD_BITFIELD_RW(XN, 0, 1)
    END_TYPE()

    // Provides indirect read and write access to the limit address of the currently selected MPU region `FTSSS
    // Reset value: 0x00000000
    BEGIN_TYPE(MPU_RLAR_t, uint32_t)
        // Contains bits [31:5] of the upper inclusive limit of the selected MPU memory region. This value is postfixed with 0x1F to provide the limit address to be checked against
        ADD_BITFIELD_RW(LIMIT, 5, 27)
        // Associates a set of attributes in the MPU_MAIR0 and MPU_MAIR1 fields
        ADD_BITFIELD_RW(ATTRINDX, 1, 3)
        // Region enable
        ADD_BITFIELD_RW(EN, 0, 1)
    END_TYPE()

    // Provides indirect read and write access to the base address of the MPU region selected by MPU_RNR[7:2]:(1[1:0]) `FTSSS
    // Reset value: 0x00000000
    BEGIN_TYPE(MPU_RBAR_A1_t, uint32_t)
        // Contains bits [31:5] of the lower inclusive limit of the selected MPU memory region. This value is zero extended to provide the base address to be checked against
        ADD_BITFIELD_RW(BASE, 5, 27)
        // Defines the Shareability domain of this region for Normal memory
        ADD_BITFIELD_RW(SH, 3, 2)
        // Defines the access permissions for this region
        ADD_BITFIELD_RW(AP, 1, 2)
        // Defines whether code can be executed from this region
        ADD_BITFIELD_RW(XN, 0, 1)
    END_TYPE()

    // Provides indirect read and write access to the limit address of the currently selected MPU region selected by MPU_RNR[7:2]:(1[1:0]) `FTSSS
    // Reset value: 0x00000000
    BEGIN_TYPE(MPU_RLAR_A1_t, uint32_t)
        // Contains bits [31:5] of the upper inclusive limit of the selected MPU memory region. This value is postfixed with 0x1F to provide the limit address to be checked against
        ADD_BITFIELD_RW(LIMIT, 5, 27)
        // Associates a set of attributes in the MPU_MAIR0 and MPU_MAIR1 fields
        ADD_BITFIELD_RW(ATTRINDX, 1, 3)
        // Region enable
        ADD_BITFIELD_RW(EN, 0, 1)
    END_TYPE()

    // Provides indirect read and write access to the base address of the MPU region selected by MPU_RNR[7:2]:(2[1:0]) `FTSSS
    // Reset value: 0x00000000
    BEGIN_TYPE(MPU_RBAR_A2_t, uint32_t)
        // Contains bits [31:5] of the lower inclusive limit of the selected MPU memory region. This value is zero extended to provide the base address to be checked against
        ADD_BITFIELD_RW(BASE, 5, 27)
        // Defines the Shareability domain of this region for Normal memory
        ADD_BITFIELD_RW(SH, 3, 2)
        // Defines the access permissions for this region
        ADD_BITFIELD_RW(AP, 1, 2)
        // Defines whether code can be executed from this region
        ADD_BITFIELD_RW(XN, 0, 1)
    END_TYPE()

    // Provides indirect read and write access to the limit address of the currently selected MPU region selected by MPU_RNR[7:2]:(2[1:0]) `FTSSS
    // Reset value: 0x00000000
    BEGIN_TYPE(MPU_RLAR_A2_t, uint32_t)
        // Contains bits [31:5] of the upper inclusive limit of the selected MPU memory region. This value is postfixed with 0x1F to provide the limit address to be checked against
        ADD_BITFIELD_RW(LIMIT, 5, 27)
        // Associates a set of attributes in the MPU_MAIR0 and MPU_MAIR1 fields
        ADD_BITFIELD_RW(ATTRINDX, 1, 3)
        // Region enable
        ADD_BITFIELD_RW(EN, 0, 1)
    END_TYPE()

    // Provides indirect read and write access to the base address of the MPU region selected by MPU_RNR[7:2]:(3[1:0]) `FTSSS
    // Reset value: 0x00000000
    BEGIN_TYPE(MPU_RBAR_A3_t, uint32_t)
        // Contains bits [31:5] of the lower inclusive limit of the selected MPU memory region. This value is zero extended to provide the base address to be checked against
        ADD_BITFIELD_RW(BASE, 5, 27)
        // Defines the Shareability domain of this region for Normal memory
        ADD_BITFIELD_RW(SH, 3, 2)
        // Defines the access permissions for this region
        ADD_BITFIELD_RW(AP, 1, 2)
        // Defines whether code can be executed from this region
        ADD_BITFIELD_RW(XN, 0, 1)
    END_TYPE()

    // Provides indirect read and write access to the limit address of the currently selected MPU region selected by MPU_RNR[7:2]:(3[1:0]) `FTSSS
    // Reset value: 0x00000000
    BEGIN_TYPE(MPU_RLAR_A3_t, uint32_t)
        // Contains bits [31:5] of the upper inclusive limit of the selected MPU memory region. This value is postfixed with 0x1F to provide the limit address to be checked against
        ADD_BITFIELD_RW(LIMIT, 5, 27)
        // Associates a set of attributes in the MPU_MAIR0 and MPU_MAIR1 fields
        ADD_BITFIELD_RW(ATTRINDX, 1, 3)
        // Region enable
        ADD_BITFIELD_RW(EN, 0, 1)
    END_TYPE()

    // Along with MPU_MAIR1, provides the memory attribute encodings corresponding to the AttrIndex values
    // Reset value: 0x00000000
    BEGIN_TYPE(MPU_MAIR0_t, uint32_t)
        // Memory attribute encoding for MPU regions with an AttrIndex of 3
        ADD_BITFIELD_RW(ATTR3, 24, 8)
        // Memory attribute encoding for MPU regions with an AttrIndex of 2
        ADD_BITFIELD_RW(ATTR2, 16, 8)
        // Memory attribute encoding for MPU regions with an AttrIndex of 1
        ADD_BITFIELD_RW(ATTR1, 8, 8)
        // Memory attribute encoding for MPU regions with an AttrIndex of 0
        ADD_BITFIELD_RW(ATTR0, 0, 8)
    END_TYPE()

    // Along with MPU_MAIR0, provides the memory attribute encodings corresponding to the AttrIndex values
    // Reset value: 0x00000000
    BEGIN_TYPE(MPU_MAIR1_t, uint32_t)
        // Memory attribute encoding for MPU regions with an AttrIndex of 7
        ADD_BITFIELD_RW(ATTR7, 24, 8)
        // Memory attribute encoding for MPU regions with an AttrIndex of 6
        ADD_BITFIELD_RW(ATTR6, 16, 8)
        // Memory attribute encoding for MPU regions with an AttrIndex of 5
        ADD_BITFIELD_RW(ATTR5, 8, 8)
        // Memory attribute encoding for MPU regions with an AttrIndex of 4
        ADD_BITFIELD_RW(ATTR4, 0, 8)
    END_TYPE()

    // Allows enabling of the Security Attribution Unit
    // Reset value: 0x00000000
    BEGIN_TYPE(SAU_CTRL_t, uint32_t)
        // When SAU_CTRL.ENABLE is 0 this bit controls if the memory is marked as Non-secure or Secure
        ADD_BITFIELD_RW(ALLNS, 1, 1)
        // Enables the SAU
        ADD_BITFIELD_RW(ENABLE, 0, 1)
    END_TYPE()

    // Indicates the number of regions implemented by the Security Attribution Unit
    // Reset value: 0x00000008
    BEGIN_TYPE(SAU_TYPE_t, uint32_t)
        // The number of implemented SAU regions
        ADD_BITFIELD_RO(SREGION, 0, 8)
    END_TYPE()

    // Selects the region currently accessed by SAU_RBAR and SAU_RLAR
    // Reset value: 0x00000000
    BEGIN_TYPE(SAU_RNR_t, uint32_t)
        // Indicates the SAU region accessed by SAU_RBAR and SAU_RLAR
        ADD_BITFIELD_RW(REGION, 0, 8)
    END_TYPE()

    // Provides indirect read and write access to the base address of the currently selected SAU region
    // Reset value: 0x00000000
    BEGIN_TYPE(SAU_RBAR_t, uint32_t)
        // Holds bits [31:5] of the base address for the selected SAU region
        ADD_BITFIELD_RW(BADDR, 5, 27)
    END_TYPE()

    // Provides indirect read and write access to the limit address of the currently selected SAU region
    // Reset value: 0x00000000
    BEGIN_TYPE(SAU_RLAR_t, uint32_t)
        // Holds bits [31:5] of the limit address for the selected SAU region
        ADD_BITFIELD_RW(LADDR, 5, 27)
        // Controls whether Non-secure state is permitted to execute an SG instruction from this region
        ADD_BITFIELD_RW(NSC, 1, 1)
        // SAU region enable
        ADD_BITFIELD_RW(ENABLE, 0, 1)
    END_TYPE()

    // Provides information about any security related faults
    // Reset value: 0x00000000
    BEGIN_TYPE(SFSR_t, uint32_t)
        // Sticky flag indicating that an error occurred during lazy state activation or deactivation
        ADD_BITFIELD_RW(LSERR, 7, 1)
        // This bit is set when the SFAR register contains a valid value. As with similar fields, such as BFSR.BFARVALID and MMFSR.MMARVALID, this bit can be cleared by other exceptions, such as BusFault
        ADD_BITFIELD_RW(SFARVALID, 6, 1)
        // Stick flag indicating that an SAU or IDAU violation occurred during the lazy preservation of floating-point state
        ADD_BITFIELD_RW(LSPERR, 5, 1)
        // Sticky flag indicating that an exception was raised due to a branch that was not flagged as being domain crossing causing a transition from Secure to Non-secure memory
        ADD_BITFIELD_RW(INVTRAN, 4, 1)
        // Sticky flag indicating that an attempt was made to access parts of the address space that are marked as Secure with NS-Req for the transaction set to Non-secure. This bit is not set if the violation occurred during lazy state preservation. See LSPERR
        ADD_BITFIELD_RW(AUVIOL, 3, 1)
        // This can be caused by EXC_RETURN.DCRS being set to 0 when returning from an exception in the Non-secure state, or by EXC_RETURN.ES being set to 1 when returning from an exception in the Non-secure state
        ADD_BITFIELD_RW(INVER, 2, 1)
        // This bit is set if the integrity signature in an exception stack frame is found to be invalid during the unstacking operation
        ADD_BITFIELD_RW(INVIS, 1, 1)
        // This bit is set if a function call from the Non-secure state or exception targets a non-SG instruction in the Secure state. This bit is also set if the target address is a SG instruction, but there is no matching SAU/IDAU region with the NSC flag set
        ADD_BITFIELD_RW(INVEP, 0, 1)
    END_TYPE()

    // Shows the address of the memory location that caused a Security violation
    // Reset value: 0x00000000
    BEGIN_TYPE(SFAR_t, uint32_t)
        // The address of an access that caused a attribution unit violation. This field is only valid when SFSR.SFARVALID is set. This allows the actual flip flops associated with this register to be shared with other fault address registers. If an implementation chooses to share the storage in this way, care must be taken to not leak Secure address information to the Non-secure state. One way of achieving this is to share the SFAR register with the MMFAR_S register, which is not accessible to the Non-secure state
        ADD_BITFIELD_RW(ADDRESS, 0, 32)
    END_TYPE()

    // Controls halting debug
    // Reset value: 0x00000000
    BEGIN_TYPE(DHCSR_t, uint32_t)
        // Indicates the PE has processed a request to clear DHCSR.C_HALT to 0. That is, either a write to DHCSR that clears DHCSR.C_HALT from 1 to 0, or an External Restart Request
        ADD_BITFIELD_RO(S_RESTART_ST, 26, 1)
        // Indicates whether the PE has been reset since the last read of the DHCSR
        ADD_BITFIELD_RO(S_RESET_ST, 25, 1)
        // Set to 1 every time the PE retires one of more instructions
        ADD_BITFIELD_RO(S_RETIRE_ST, 24, 1)
        // Indicates whether Secure invasive debug is allowed
        ADD_BITFIELD_RO(S_SDE, 20, 1)
        // Indicates whether the PE is in Lockup state
        ADD_BITFIELD_RO(S_LOCKUP, 19, 1)
        // Indicates whether the PE is sleeping
        ADD_BITFIELD_RO(S_SLEEP, 18, 1)
        // Indicates whether the PE is in Debug state
        ADD_BITFIELD_RO(S_HALT, 17, 1)
        // Handshake flag to transfers through the DCRDR
        ADD_BITFIELD_RO(S_REGRDY, 16, 1)
        // Allow imprecise entry to Debug state
        ADD_BITFIELD_RW(C_SNAPSTALL, 5, 1)
        // When debug is enabled, the debugger can write to this bit to mask PendSV, SysTick and external configurable interrupts
        ADD_BITFIELD_RW(C_MASKINTS, 3, 1)
        // Enable single instruction step
        ADD_BITFIELD_RW(C_STEP, 2, 1)
        // PE enter Debug state halt request
        ADD_BITFIELD_RW(C_HALT, 1, 1)
        // Enable Halting debug
        ADD_BITFIELD_RW(C_DEBUGEN, 0, 1)
    END_TYPE()

    // With the DCRDR, provides debug access to the general-purpose registers, special-purpose registers, and the FP extension registers. A write to the DCRSR specifies the register to transfer, whether the transfer is a read or write, and starts the transfer
    // Reset value: 0x00000000
    BEGIN_TYPE(DCRSR_t, uint32_t)
        // Specifies the access type for the transfer
        ADD_BITFIELD_RW(REGWNR, 16, 1)
        // Specifies the general-purpose register, special-purpose register, or FP register to transfer
        ADD_BITFIELD_RW(REGSEL, 0, 7)
    END_TYPE()

    // With the DCRSR, provides debug access to the general-purpose registers, special-purpose registers, and the FP Extension registers. If the Main Extension is implemented, it can also be used for message passing between an external debugger and a debug agent running on the PE
    // Reset value: 0x00000000
    BEGIN_TYPE(DCRDR_t, uint32_t)
        // Provides debug access for reading and writing the general-purpose registers, special-purpose registers, and Floating-point Extension registers
        ADD_BITFIELD_RW(DBGTMP, 0, 32)
    END_TYPE()

    // Manages vector catch behavior and DebugMonitor handling when debugging
    // Reset value: 0x00000000
    BEGIN_TYPE(DEMCR_t, uint32_t)
        // Global enable for all DWT and ITM features
        ADD_BITFIELD_RW(TRCENA, 24, 1)
        // Indicates whether the DebugMonitor targets the Secure or the Non-secure state and whether debug events are allowed in Secure state
        ADD_BITFIELD_RO(SDME, 20, 1)
        // DebugMonitor semaphore bit
        ADD_BITFIELD_RW(MON_REQ, 19, 1)
        // Enable DebugMonitor stepping
        ADD_BITFIELD_RW(MON_STEP, 18, 1)
        // Sets or clears the pending state of the DebugMonitor exception
        ADD_BITFIELD_RW(MON_PEND, 17, 1)
        // Enable the DebugMonitor exception
        ADD_BITFIELD_RW(MON_EN, 16, 1)
        // SecureFault exception halting debug vector catch enable
        ADD_BITFIELD_RW(VC_SFERR, 11, 1)
        // HardFault exception halting debug vector catch enable
        ADD_BITFIELD_RW(VC_HARDERR, 10, 1)
        // Enable halting debug vector catch for faults during exception entry and return
        ADD_BITFIELD_RW(VC_INTERR, 9, 1)
        // BusFault exception halting debug vector catch enable
        ADD_BITFIELD_RW(VC_BUSERR, 8, 1)
        // Enable halting debug trap on a UsageFault exception caused by a state information error, for example an Undefined Instruction exception
        ADD_BITFIELD_RW(VC_STATERR, 7, 1)
        // Enable halting debug trap on a UsageFault exception caused by a checking error, for example an alignment check error
        ADD_BITFIELD_RW(VC_CHKERR, 6, 1)
        // Enable halting debug trap on a UsageFault caused by an access to a coprocessor
        ADD_BITFIELD_RW(VC_NOCPERR, 5, 1)
        // Enable halting debug trap on a MemManage exception
        ADD_BITFIELD_RW(VC_MMERR, 4, 1)
        // Enable Reset Vector Catch. This causes a warm reset to halt a running system
        ADD_BITFIELD_RW(VC_CORERESET, 0, 1)
    END_TYPE()

    // Provides control and status information for Secure debug
    // Reset value: 0x00000000
    BEGIN_TYPE(DSCSR_t, uint32_t)
        // Writes to the CDS bit are ignored unless CDSKEY is concurrently written to zero
        ADD_BITFIELD_RW(CDSKEY, 17, 1)
        // This field indicates the current Security state of the processor
        ADD_BITFIELD_RW(CDS, 16, 1)
        // If SBRSELEN is 1 this bit selects whether the Non-secure or the Secure version of the memory-mapped Banked registers are accessible to the debugger
        ADD_BITFIELD_RW(SBRSEL, 1, 1)
        // Controls whether the SBRSEL field or the current Security state of the processor selects which version of the memory-mapped Banked registers are accessed to the debugger
        ADD_BITFIELD_RW(SBRSELEN, 0, 1)
    END_TYPE()

    // Provides a mechanism for software to generate an interrupt
    // Reset value: 0x00000000
    BEGIN_TYPE(STIR_t, uint32_t)
        // Indicates the interrupt to be pended. The value written is (ExceptionNumber - 16)
        ADD_BITFIELD_RW(INTID, 0, 9)
    END_TYPE()

    // Holds control data for the Floating-point extension
    // Reset value: 0x20000472
    BEGIN_TYPE(FPCCR_t, uint32_t)
        // When this bit is set to 1, execution of a floating-point instruction sets the CONTROL.FPCA bit to 1
        ADD_BITFIELD_RW(ASPEN, 31, 1)
        // Enables lazy context save of floating-point state
        ADD_BITFIELD_RW(LSPEN, 30, 1)
        // This bit controls whether the LSPEN bit is writeable from the Non-secure state
        ADD_BITFIELD_RW(LSPENS, 29, 1)
        // Clear floating-point caller saved registers on exception return
        ADD_BITFIELD_RW(CLRONRET, 28, 1)
        // This bit controls whether the CLRONRET bit is writeable from the Non-secure state
        ADD_BITFIELD_RW(CLRONRETS, 27, 1)
        // Treat floating-point registers as Secure enable
        ADD_BITFIELD_RW(TS, 26, 1)
        // Indicates whether the software executing when the PE allocated the floating-point stack frame was able to set the UsageFault exception to pending
        ADD_BITFIELD_RW(UFRDY, 10, 1)
        // This bit is banked between the Security states and indicates whether the floating-point context violates the stack pointer limit that was active when lazy state preservation was activated. SPLIMVIOL modifies the lazy floating-point state preservation behavior
        ADD_BITFIELD_RW(SPLIMVIOL, 9, 1)
        // Indicates whether the software executing when the PE allocated the floating-point stack frame was able to set the DebugMonitor exception to pending
        ADD_BITFIELD_RW(MONRDY, 8, 1)
        // Indicates whether the software executing when the PE allocated the floating-point stack frame was able to set the SecureFault exception to pending. This bit is only present in the Secure version of the register, and behaves as RAZ/WI when accessed from the Non-secure state
        ADD_BITFIELD_RW(SFRDY, 7, 1)
        // Indicates whether the software executing when the PE allocated the floating-point stack frame was able to set the BusFault exception to pending
        ADD_BITFIELD_RW(BFRDY, 6, 1)
        // Indicates whether the software executing when the PE allocated the floating-point stack frame was able to set the MemManage exception to pending
        ADD_BITFIELD_RW(MMRDY, 5, 1)
        // Indicates whether the software executing when the PE allocated the floating-point stack frame was able to set the HardFault exception to pending
        ADD_BITFIELD_RW(HFRDY, 4, 1)
        // Indicates the PE mode when it allocated the floating-point stack frame
        ADD_BITFIELD_RW(THREAD, 3, 1)
        // Security status of the floating-point context. This bit is only present in the Secure version of the register, and behaves as RAZ/WI when accessed from the Non-secure state. This bit is updated whenever lazy state preservation is activated, or when a floating-point instruction is executed
        ADD_BITFIELD_RW(S, 2, 1)
        // Indicates the privilege level of the software executing when the PE allocated the floating-point stack frame
        ADD_BITFIELD_RW(USER, 1, 1)
        // Indicates whether lazy preservation of the floating-point state is active
        ADD_BITFIELD_RW(LSPACT, 0, 1)
    END_TYPE()

    // Holds the location of the unpopulated floating-point register space allocated on an exception stack frame
    // Reset value: 0x00000000
    BEGIN_TYPE(FPCAR_t, uint32_t)
        // The location of the unpopulated floating-point register space allocated on an exception stack frame
        ADD_BITFIELD_RW(ADDRESS, 3, 29)
    END_TYPE()

    // Holds the default values for the floating-point status control data that the PE assigns to the FPSCR when it creates a new floating-point context
    // Reset value: 0x00000000
    BEGIN_TYPE(FPDSCR_t, uint32_t)
        // Default value for FPSCR.AHP
        ADD_BITFIELD_RW(AHP, 26, 1)
        // Default value for FPSCR.DN
        ADD_BITFIELD_RW(DN, 25, 1)
        // Default value for FPSCR.FZ
        ADD_BITFIELD_RW(FZ, 24, 1)
        // Default value for FPSCR.RMode
        ADD_BITFIELD_RW(RMODE, 22, 2)
    END_TYPE()

    // Describes the features provided by the Floating-point Extension
    // Reset value: 0x60540601
    BEGIN_TYPE(MVFR0_t, uint32_t)
        // Indicates the rounding modes supported by the FP Extension
        ADD_BITFIELD_RO(FPROUND, 28, 4)
        // Indicates the support for FP square root operations
        ADD_BITFIELD_RO(FPSQRT, 20, 4)
        // Indicates the support for FP divide operations
        ADD_BITFIELD_RO(FPDIVIDE, 16, 4)
        // Indicates support for FP double-precision operations
        ADD_BITFIELD_RO(FPDP, 8, 4)
        // Indicates support for FP single-precision operations
        ADD_BITFIELD_RO(FPSP, 4, 4)
        // Indicates size of FP register file
        ADD_BITFIELD_RO(SIMDREG, 0, 4)
    END_TYPE()

    // Describes the features provided by the Floating-point Extension
    // Reset value: 0x85000089
    BEGIN_TYPE(MVFR1_t, uint32_t)
        // Indicates whether the FP Extension implements the fused multiply accumulate instructions
        ADD_BITFIELD_RO(FMAC, 28, 4)
        // Indicates whether the FP Extension implements half-precision FP conversion instructions
        ADD_BITFIELD_RO(FPHP, 24, 4)
        // Indicates whether the FP hardware implementation supports NaN propagation
        ADD_BITFIELD_RO(FPDNAN, 4, 4)
        // Indicates whether subnormals are always flushed-to-zero
        ADD_BITFIELD_RO(FPFTZ, 0, 4)
    END_TYPE()

    // Describes the features provided by the Floating-point Extension
    // Reset value: 0x00000060
    BEGIN_TYPE(MVFR2_t, uint32_t)
        // Indicates support for miscellaneous FP features
        ADD_BITFIELD_RO(FPMISC, 4, 4)
    END_TYPE()

    // Provides CoreSight discovery information for the SCS
    // Reset value: 0x47702a04
    BEGIN_TYPE(DDEVARCH_t, uint32_t)
        // Defines the architect of the component. Bits [31:28] are the JEP106 continuation code (JEP106 bank ID, minus 1) and bits [27:21] are the JEP106 ID code.
        ADD_BITFIELD_RO(ARCHITECT, 21, 11)
        // Defines that the DEVARCH register is present
        ADD_BITFIELD_RO(PRESENT, 20, 1)
        // Defines the architecture revision of the component
        ADD_BITFIELD_RO(REVISION, 16, 4)
        // Defines the architecture version of the component
        ADD_BITFIELD_RO(ARCHVER, 12, 4)
        // Defines the architecture of the component
        ADD_BITFIELD_RO(ARCHPART, 0, 12)
    END_TYPE()

    // Provides CoreSight discovery information for the SCS
    // Reset value: 0x00000000
    BEGIN_TYPE(DDEVTYPE_t, uint32_t)
        // Component sub-type
        ADD_BITFIELD_RO(SUB, 4, 4)
        // CoreSight major type
        ADD_BITFIELD_RO(MAJOR, 0, 4)
    END_TYPE()

    // Provides CoreSight discovery information for the SCS
    // Reset value: 0x00000004
    BEGIN_TYPE(DPIDR4_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(SIZE, 4, 4)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(DES_2, 0, 4)
    END_TYPE()

    // Provides CoreSight discovery information for the SCS
    // Reset value: 0x00000000
    BEGIN_TYPE(DPIDR5_t, uint32_t)
        ADD_BITFIELD_RW(DPIDR5, 0, 32)
    END_TYPE()

    // Provides CoreSight discovery information for the SCS
    // Reset value: 0x00000000
    BEGIN_TYPE(DPIDR6_t, uint32_t)
        ADD_BITFIELD_RW(DPIDR6, 0, 32)
    END_TYPE()

    // Provides CoreSight discovery information for the SCS
    // Reset value: 0x00000000
    BEGIN_TYPE(DPIDR7_t, uint32_t)
        ADD_BITFIELD_RW(DPIDR7, 0, 32)
    END_TYPE()

    // Provides CoreSight discovery information for the SCS
    // Reset value: 0x00000021
    BEGIN_TYPE(DPIDR0_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(PART_0, 0, 8)
    END_TYPE()

    // Provides CoreSight discovery information for the SCS
    // Reset value: 0x000000bd
    BEGIN_TYPE(DPIDR1_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(DES_0, 4, 4)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(PART_1, 0, 4)
    END_TYPE()

    // Provides CoreSight discovery information for the SCS
    // Reset value: 0x0000000b
    BEGIN_TYPE(DPIDR2_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(REVISION, 4, 4)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(JEDEC, 3, 1)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(DES_1, 0, 3)
    END_TYPE()

    // Provides CoreSight discovery information for the SCS
    // Reset value: 0x00000000
    BEGIN_TYPE(DPIDR3_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(REVAND, 4, 4)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(CMOD, 0, 4)
    END_TYPE()

    // Provides CoreSight discovery information for the SCS
    // Reset value: 0x0000000d
    BEGIN_TYPE(DCIDR0_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(PRMBL_0, 0, 8)
    END_TYPE()

    // Provides CoreSight discovery information for the SCS
    // Reset value: 0x00000090
    BEGIN_TYPE(DCIDR1_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(CLASS, 4, 4)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(PRMBL_1, 0, 4)
    END_TYPE()

    // Provides CoreSight discovery information for the SCS
    // Reset value: 0x00000005
    BEGIN_TYPE(DCIDR2_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(PRMBL_2, 0, 8)
    END_TYPE()

    // Provides CoreSight discovery information for the SCS
    // Reset value: 0x000000b1
    BEGIN_TYPE(DCIDR3_t, uint32_t)
        // See CoreSight Architecture Specification
        ADD_BITFIELD_RO(PRMBL_3, 0, 8)
    END_TYPE()

    // Programming Control Register
    // Reset value: 0x00000000
    BEGIN_TYPE(TRCPRGCTLR_t, uint32_t)
        // Trace Unit Enable
        ADD_BITFIELD_RW(EN, 0, 1)
    END_TYPE()

    // The TRCSTATR indicates the ETM-Teal status
    // Reset value: 0x00000000
    BEGIN_TYPE(TRCSTATR_t, uint32_t)
        // Indicates whether the ETM-Teal registers are stable and can be read
        ADD_BITFIELD_RO(PMSTABLE, 1, 1)
        // Indicates that the trace unit is inactive
        ADD_BITFIELD_RO(IDLE, 0, 1)
    END_TYPE()

    // The TRCCONFIGR sets the basic tracing options for the trace unit
    // Reset value: 0x00000000
    BEGIN_TYPE(TRCCONFIGR_t, uint32_t)
        // Return stack enable
        ADD_BITFIELD_RW(RS, 12, 1)
        // Global timestamp tracing
        ADD_BITFIELD_RW(TS, 11, 1)
        // Conditional instruction tracing
        ADD_BITFIELD_RW(COND, 5, 6)
        // Cycle counting in instruction trace
        ADD_BITFIELD_RW(CCI, 4, 1)
        // Branch broadcast mode
        ADD_BITFIELD_RW(BB, 3, 1)
    END_TYPE()

    // The TRCEVENTCTL0R controls the tracing of events in the trace stream. The events also drive the ETM-Teal external outputs.
    // Reset value: 0x00000000
    BEGIN_TYPE(TRCEVENTCTL0R_t, uint32_t)
        // Selects the resource type for event 1
        ADD_BITFIELD_RW(TYPE1, 15, 1)
        // Selects the resource number, based on the value of TYPE1: When TYPE1 is 0, selects a single selected resource from 0-15 defined by SEL1[2:0].  When TYPE1 is 1, selects a Boolean combined resource pair from 0-7 defined by SEL1[2:0]
        ADD_BITFIELD_RW(SEL1, 8, 3)
        // Selects the resource type for event 0
        ADD_BITFIELD_RW(TYPE0, 7, 1)
        // Selects the resource number, based on the value of TYPE0: When TYPE1 is 0, selects a single selected resource from 0-15 defined by SEL0[2:0].  When TYPE1 is 1, selects a Boolean combined resource pair from 0-7 defined by SEL0[2:0]
        ADD_BITFIELD_RW(SEL0, 0, 3)
    END_TYPE()

    // The TRCEVENTCTL1R controls how the events selected by TRCEVENTCTL0R behave
    // Reset value: 0x00000000
    BEGIN_TYPE(TRCEVENTCTL1R_t, uint32_t)
        // Low power state behavior override
        ADD_BITFIELD_RW(LPOVERRIDE, 12, 1)
        // ATB enabled
        ADD_BITFIELD_RW(ATB, 11, 1)
        // One bit per event, to enable generation of an event element in the instruction trace stream when the selected event occurs
        ADD_BITFIELD_RW(INSTEN1, 1, 1)
        // One bit per event, to enable generation of an event element in the instruction trace stream when the selected event occurs
        ADD_BITFIELD_RW(INSTEN0, 0, 1)
    END_TYPE()

    // The TRCSTALLCTLR enables ETM-Teal to stall the processor if the ETM-Teal FIFO goes over the programmed level to minimize risk of overflow
    // Reset value: 0x00000000
    BEGIN_TYPE(TRCSTALLCTLR_t, uint32_t)
        // Reserved, RES0
        ADD_BITFIELD_RO(INSTPRIORITY, 10, 1)
        // Stall processor based on instruction trace buffer space
        ADD_BITFIELD_RW(ISTALL, 8, 1)
        // Threshold at which stalling becomes active. This provides four levels. This level can be varied to optimize the level of invasion caused by stalling, balanced against the risk of a FIFO overflow
        ADD_BITFIELD_RW(LEVEL, 2, 2)
    END_TYPE()

    // The TRCTSCTLR controls the insertion of global timestamps into the trace stream. A timestamp is always inserted into the instruction trace stream
    // Reset value: 0x00000000
    BEGIN_TYPE(TRCTSCTLR_t, uint32_t)
        // Selects the resource type for event 0
        ADD_BITFIELD_RW(TYPE0, 7, 1)
        // Selects the resource number, based on the value of TYPE0: When TYPE1 is 0, selects a single selected resource from 0-15 defined by SEL0[2:0].  When TYPE1 is 1, selects a Boolean combined resource pair from 0-7 defined by SEL0[2:0]
        ADD_BITFIELD_RW(SEL0, 0, 2)
    END_TYPE()

    // The TRCSYNCPR specifies the period of trace synchronization of the trace streams. TRCSYNCPR defines a number of bytes of trace between requests for trace synchronization. This value is always a power of two
    // Reset value: 0x0000000a
    BEGIN_TYPE(TRCSYNCPR_t, uint32_t)
        // Defines the number of bytes of trace between trace synchronization requests as a total of the number of bytes generated by the instruction stream. The number of bytes is 2N where N is the value of this field: - A value of zero disables these periodic trace synchronization requests, but does not disable other trace synchronization requests.  - The minimum value that can be programmed, other than zero, is 8, providing a minimum trace synchronization period of 256 bytes.  - The maximum value is 20, providing a maximum trace synchronization period of 2^20 bytes
        ADD_BITFIELD_RO(PERIOD, 0, 5)
    END_TYPE()

    // The TRCCCCTLR sets the threshold value for instruction trace cycle counting. The threshold represents the minimum interval between cycle count trace packets
    // Reset value: 0x00000000
    BEGIN_TYPE(TRCCCCTLR_t, uint32_t)
        // Instruction trace cycle count threshold
        ADD_BITFIELD_RW(THRESHOLD, 0, 12)
    END_TYPE()

    // The TRCVICTLR controls instruction trace filtering
    // Reset value: 0x00000000
    BEGIN_TYPE(TRCVICTLR_t, uint32_t)
        // In Secure state, each bit controls whether instruction tracing is enabled for the corresponding exception level
        ADD_BITFIELD_RW(EXLEVEL_S3, 19, 1)
        // In Secure state, each bit controls whether instruction tracing is enabled for the corresponding exception level
        ADD_BITFIELD_RW(EXLEVEL_S0, 16, 1)
        // Selects whether a system error exception must always be traced
        ADD_BITFIELD_RW(TRCERR, 11, 1)
        // Selects whether a reset exception must always be traced
        ADD_BITFIELD_RW(TRCRESET, 10, 1)
        // Indicates the current status of the start/stop logic
        ADD_BITFIELD_RW(SSSTATUS, 9, 1)
        // Selects the resource type for event 0
        ADD_BITFIELD_RW(TYPE0, 7, 1)
        // Selects the resource number, based on the value of TYPE0: When TYPE1 is 0, selects a single selected resource from 0-15 defined by SEL0[2:0].  When TYPE1 is 1, selects a Boolean combined resource pair from 0-7 defined by SEL0[2:0]
        ADD_BITFIELD_RW(SEL0, 0, 2)
    END_TYPE()

    // The TRCCNTRLDVR defines the reload value for the reduced function counter
    // Reset value: 0x00000000
    BEGIN_TYPE(TRCCNTRLDVR0_t, uint32_t)
        // Defines the reload value for the counter. This value is loaded into the counter each time the reload event occurs
        ADD_BITFIELD_RW(VALUE, 0, 16)
    END_TYPE()

    // TRCIDR8
    // Reset value: 0x00000000
    BEGIN_TYPE(TRCIDR8_t, uint32_t)
        // reads as `ImpDef
        ADD_BITFIELD_RO(MAXSPEC, 0, 32)
    END_TYPE()

    // TRCIDR9
    // Reset value: 0x00000000
    BEGIN_TYPE(TRCIDR9_t, uint32_t)
        // reads as `ImpDef
        ADD_BITFIELD_RO(NUMP0KEY, 0, 32)
    END_TYPE()

    // TRCIDR10
    // Reset value: 0x00000000
    BEGIN_TYPE(TRCIDR10_t, uint32_t)
        // reads as `ImpDef
        ADD_BITFIELD_RO(NUMP1KEY, 0, 32)
    END_TYPE()

    // TRCIDR11
    // Reset value: 0x00000000
    BEGIN_TYPE(TRCIDR11_t, uint32_t)
        // reads as `ImpDef
        ADD_BITFIELD_RO(NUMP1SPC, 0, 32)
    END_TYPE()

    // TRCIDR12
    // Reset value: 0x00000001
    BEGIN_TYPE(TRCIDR12_t, uint32_t)
        // reads as `ImpDef
        ADD_BITFIELD_RO(NUMCONDKEY, 0, 32)
    END_TYPE()

    // TRCIDR13
    // Reset value: 0x00000000
    BEGIN_TYPE(TRCIDR13_t, uint32_t)
        // reads as `ImpDef
        ADD_BITFIELD_RO(NUMCONDSPC, 0, 32)
    END_TYPE()

    // The TRCIMSPEC shows the presence of any IMPLEMENTATION SPECIFIC features, and enables any features that are provided
    // Reset value: 0x00000000
    BEGIN_TYPE(TRCIMSPEC_t, uint32_t)
        // Reserved, RES0
        ADD_BITFIELD_RO(SUPPORT, 0, 4)
    END_TYPE()

    // TRCIDR0
    // Reset value: 0x280006e1
    BEGIN_TYPE(TRCIDR0_t, uint32_t)
        // reads as `ImpDef
        ADD_BITFIELD_RO(COMMOPT, 29, 1)
        // reads as `ImpDef
        ADD_BITFIELD_RO(TSSIZE, 24, 5)
        // reads as `ImpDef
        ADD_BITFIELD_RO(TRCEXDATA, 17, 1)
        // reads as `ImpDef
        ADD_BITFIELD_RO(QSUPP, 15, 2)
        // reads as `ImpDef
        ADD_BITFIELD_RO(QFILT, 14, 1)
        // reads as `ImpDef
        ADD_BITFIELD_RO(CONDTYPE, 12, 2)
        // reads as `ImpDef
        ADD_BITFIELD_RO(NUMEVENT, 10, 2)
        // reads as `ImpDef
        ADD_BITFIELD_RO(RETSTACK, 9, 1)
        // reads as `ImpDef
        ADD_BITFIELD_RO(TRCCCI, 7, 1)
        // reads as `ImpDef
        ADD_BITFIELD_RO(TRCCOND, 6, 1)
        // reads as `ImpDef
        ADD_BITFIELD_RO(TRCBB, 5, 1)
        // reads as `ImpDef
        ADD_BITFIELD_RO(TRCDATA, 3, 2)
        // reads as `ImpDef
        ADD_BITFIELD_RO(INSTP0, 1, 2)
        // Reserved, RES1
        ADD_BITFIELD_RO(RES1, 0, 1)
    END_TYPE()

    // TRCIDR1
    // Reset value: 0x4100f421
    BEGIN_TYPE(TRCIDR1_t, uint32_t)
        // reads as `ImpDef
        ADD_BITFIELD_RO(DESIGNER, 24, 8)
        // Reserved, RES1
        ADD_BITFIELD_RO(RES1, 12, 4)
        // reads as 0b0100
        ADD_BITFIELD_RO(TRCARCHMAJ, 8, 4)
        // reads as 0b0000
        ADD_BITFIELD_RO(TRCARCHMIN, 4, 4)
        // reads as `ImpDef
        ADD_BITFIELD_RO(REVISION, 0, 4)
    END_TYPE()

    // TRCIDR2
    // Reset value: 0x00000004
    BEGIN_TYPE(TRCIDR2_t, uint32_t)
        // reads as `ImpDef
        ADD_BITFIELD_RO(CCSIZE, 25, 4)
        // reads as `ImpDef
        ADD_BITFIELD_RO(DVSIZE, 20, 5)
        // reads as `ImpDef
        ADD_BITFIELD_RO(DASIZE, 15, 5)
        // reads as `ImpDef
        ADD_BITFIELD_RO(VMIDSIZE, 10, 5)
        // reads as `ImpDef
        ADD_BITFIELD_RO(CIDSIZE, 5, 5)
        // reads as `ImpDef
        ADD_BITFIELD_RO(IASIZE, 0, 5)
    END_TYPE()

    // TRCIDR3
    // Reset value: 0x0f090004
    BEGIN_TYPE(TRCIDR3_t, uint32_t)
        // reads as `ImpDef
        ADD_BITFIELD_RO(NOOVERFLOW, 31, 1)
        // reads as `ImpDef
        ADD_BITFIELD_RO(NUMPROC, 28, 3)
        // reads as `ImpDef
        ADD_BITFIELD_RO(SYSSTALL, 27, 1)
        // reads as `ImpDef
        ADD_BITFIELD_RO(STALLCTL, 26, 1)
        // reads as `ImpDef
        ADD_BITFIELD_RO(SYNCPR, 25, 1)
        // reads as `ImpDef
        ADD_BITFIELD_RO(TRCERR, 24, 1)
        // reads as `ImpDef
        ADD_BITFIELD_RO(EXLEVEL_NS, 20, 4)
        // reads as `ImpDef
        ADD_BITFIELD_RO(EXLEVEL_S, 16, 4)
        // reads as `ImpDef
        ADD_BITFIELD_RO(CCITMIN, 0, 12)
    END_TYPE()

    // TRCIDR4
    // Reset value: 0x00114000
    BEGIN_TYPE(TRCIDR4_t, uint32_t)
        // reads as `ImpDef
        ADD_BITFIELD_RO(NUMVMIDC, 28, 4)
        // reads as `ImpDef
        ADD_BITFIELD_RO(NUMCIDC, 24, 4)
        // reads as `ImpDef
        ADD_BITFIELD_RO(NUMSSCC, 20, 4)
        // reads as `ImpDef
        ADD_BITFIELD_RO(NUMRSPAIR, 16, 4)
        // reads as `ImpDef
        ADD_BITFIELD_RO(NUMPC, 12, 4)
        // reads as `ImpDef
        ADD_BITFIELD_RO(SUPPDAC, 8, 1)
        // reads as `ImpDef
        ADD_BITFIELD_RO(NUMDVC, 4, 4)
        // reads as `ImpDef
        ADD_BITFIELD_RO(NUMACPAIRS, 0, 4)
    END_TYPE()

    // TRCIDR5
    // Reset value: 0x90c70004
    BEGIN_TYPE(TRCIDR5_t, uint32_t)
        // reads as `ImpDef
        ADD_BITFIELD_RO(REDFUNCNTR, 31, 1)
        // reads as `ImpDef
        ADD_BITFIELD_RO(NUMCNTR, 28, 3)
        // reads as `ImpDef
        ADD_BITFIELD_RO(NUMSEQSTATE, 25, 3)
        // reads as `ImpDef
        ADD_BITFIELD_RO(LPOVERRIDE, 23, 1)
        // reads as `ImpDef
        ADD_BITFIELD_RO(ATBTRIG, 22, 1)
        // reads as 0x07
        ADD_BITFIELD_RO(TRACEIDSIZE, 16, 6)
        // reads as `ImpDef
        ADD_BITFIELD_RO(NUMEXTINSEL, 9, 3)
        // reads as `ImpDef
        ADD_BITFIELD_RO(NUMEXTIN, 0, 9)
    END_TYPE()

    // TRCIDR6
    // Reset value: 0x00000000
    BEGIN_TYPE(TRCIDR6_t, uint32_t)
        ADD_BITFIELD_RW(TRCIDR6, 0, 32)
    END_TYPE()

    // TRCIDR7
    // Reset value: 0x00000000
    BEGIN_TYPE(TRCIDR7_t, uint32_t)
        ADD_BITFIELD_RW(TRCIDR7, 0, 32)
    END_TYPE()

    // The TRCRSCTLR controls the trace resources
    // Reset value: 0x00000000
    BEGIN_TYPE(TRCRSCTLR2_t, uint32_t)
        // Inverts the result of a combined pair of resources.  This bit is only implemented on the lower register for a pair of resource selectors
        ADD_BITFIELD_RW(PAIRINV, 21, 1)
        // Inverts the selected resources
        ADD_BITFIELD_RW(INV, 20, 1)
        // Selects a group of resource
        ADD_BITFIELD_RW(GROUP, 16, 3)
        // Selects one or more resources from the wanted group. One bit is provided per resource from the group
        ADD_BITFIELD_RW(SELECT, 0, 8)
    END_TYPE()

    // The TRCRSCTLR controls the trace resources
    // Reset value: 0x00000000
    BEGIN_TYPE(TRCRSCTLR3_t, uint32_t)
        // Inverts the result of a combined pair of resources.  This bit is only implemented on the lower register for a pair of resource selectors
        ADD_BITFIELD_RW(PAIRINV, 21, 1)
        // Inverts the selected resources
        ADD_BITFIELD_RW(INV, 20, 1)
        // Selects a group of resource
        ADD_BITFIELD_RW(GROUP, 16, 3)
        // Selects one or more resources from the wanted group. One bit is provided per resource from the group
        ADD_BITFIELD_RW(SELECT, 0, 8)
    END_TYPE()

    // Controls the corresponding single-shot comparator resource
    // Reset value: 0x00000000
    BEGIN_TYPE(TRCSSCSR_t, uint32_t)
        // Single-shot status bit. Indicates if any of the comparators, that TRCSSCCRn.SAC or TRCSSCCRn.ARC selects, have matched
        ADD_BITFIELD_RW(STATUS, 31, 1)
        // Reserved, RES1
        ADD_BITFIELD_RO(PC, 3, 1)
        // Reserved, RES0
        ADD_BITFIELD_RO(DV, 2, 1)
        // Reserved, RES0
        ADD_BITFIELD_RO(DA, 1, 1)
        // Reserved, RES0
        ADD_BITFIELD_RO(INST, 0, 1)
    END_TYPE()

    // Selects the PE comparator inputs for Single-shot control
    // Reset value: 0x00000000
    BEGIN_TYPE(TRCSSPCICR_t, uint32_t)
        // Selects one or more PE comparator inputs for Single-shot control.  TRCIDR4.NUMPC defines the size of the PC field.  1 bit is provided for each implemented PE comparator input.  For example, if bit[1] == 1 this selects PE comparator input 1 for Single-shot control
        ADD_BITFIELD_RW(PC, 0, 4)
    END_TYPE()

    // Requests the system to provide power to the trace unit
    // Reset value: 0x00000000
    BEGIN_TYPE(TRCPDCR_t, uint32_t)
        // Powerup request bit:
        ADD_BITFIELD_RW(PU, 3, 1)
    END_TYPE()

    // Returns the following information about the trace unit: - OS Lock status.  - Core power domain status.  - Power interruption status
    // Reset value: 0x00000003
    BEGIN_TYPE(TRCPDSR_t, uint32_t)
        // OS Lock status bit:
        ADD_BITFIELD_RO(OSLK, 5, 1)
        // Sticky powerdown status bit. Indicates whether the trace register state is valid:
        ADD_BITFIELD_RO(STICKYPD, 1, 1)
        // Power status bit:
        ADD_BITFIELD_RO(POWER, 0, 1)
    END_TYPE()

    // Trace Integration ATB Identification Register
    // Reset value: 0x00000000
    BEGIN_TYPE(TRCITATBIDR_t, uint32_t)
        // Trace ID
        ADD_BITFIELD_RW(ID, 0, 7)
    END_TYPE()

    // Trace Integration Instruction ATB In Register
    // Reset value: 0x00000000
    BEGIN_TYPE(TRCITIATBINR_t, uint32_t)
        // Integration Mode instruction AFVALIDM in
        ADD_BITFIELD_RW(AFVALIDM, 1, 1)
        // Integration Mode instruction ATREADYM in
        ADD_BITFIELD_RW(ATREADYM, 0, 1)
    END_TYPE()

    // Trace Integration Instruction ATB Out Register
    // Reset value: 0x00000000
    BEGIN_TYPE(TRCITIATBOUTR_t, uint32_t)
        // Integration Mode instruction AFREADY out
        ADD_BITFIELD_RW(AFREADY, 1, 1)
        // Integration Mode instruction ATVALID out
        ADD_BITFIELD_RW(ATVALID, 0, 1)
    END_TYPE()

    // Claim Tag Set Register
    // Reset value: 0x0000000f
    BEGIN_TYPE(TRCCLAIMSET_t, uint32_t)
        // When a write to one of these bits occurs, with the value:
        ADD_BITFIELD_RW(SET3, 3, 1)
        // When a write to one of these bits occurs, with the value:
        ADD_BITFIELD_RW(SET2, 2, 1)
        // When a write to one of these bits occurs, with the value:
        ADD_BITFIELD_RW(SET1, 1, 1)
        // When a write to one of these bits occurs, with the value:
        ADD_BITFIELD_RW(SET0, 0, 1)
    END_TYPE()

    // Claim Tag Clear Register
    // Reset value: 0x00000000
    BEGIN_TYPE(TRCCLAIMCLR_t, uint32_t)
        // When a write to one of these bits occurs, with the value:
        ADD_BITFIELD_RW(CLR3, 3, 1)
        // When a write to one of these bits occurs, with the value:
        ADD_BITFIELD_RW(CLR2, 2, 1)
        // When a write to one of these bits occurs, with the value:
        ADD_BITFIELD_RW(CLR1, 1, 1)
        // When a write to one of these bits occurs, with the value:
        ADD_BITFIELD_RW(CLR0, 0, 1)
    END_TYPE()

    // Returns the level of tracing that the trace unit can support
    // Reset value: 0x00000000
    BEGIN_TYPE(TRCAUTHSTATUS_t, uint32_t)
        // Indicates whether the system enables the trace unit to support Secure non-invasive debug:
        ADD_BITFIELD_RO(SNID, 6, 2)
        // Indicates whether the trace unit supports Secure invasive debug:
        ADD_BITFIELD_RO(SID, 4, 2)
        // Indicates whether the system enables the trace unit to support Non-secure non-invasive debug:
        ADD_BITFIELD_RO(NSNID, 2, 2)
        // Indicates whether the trace unit supports Non-secure invasive debug:
        ADD_BITFIELD_RO(NSID, 0, 2)
    END_TYPE()

    // TRCDEVARCH
    // Reset value: 0x47724a13
    BEGIN_TYPE(TRCDEVARCH_t, uint32_t)
        // reads as 0b01000111011
        ADD_BITFIELD_RO(ARCHITECT, 21, 11)
        // reads as 0b1
        ADD_BITFIELD_RO(PRESENT, 20, 1)
        // reads as 0b0000
        ADD_BITFIELD_RO(REVISION, 16, 4)
        // reads as 0b0100101000010011
        ADD_BITFIELD_RO(ARCHID, 0, 16)
    END_TYPE()

    // TRCDEVID
    // Reset value: 0x00000000
    BEGIN_TYPE(TRCDEVID_t, uint32_t)
        ADD_BITFIELD_RW(TRCDEVID, 0, 32)
    END_TYPE()

    // TRCDEVTYPE
    // Reset value: 0x00000013
    BEGIN_TYPE(TRCDEVTYPE_t, uint32_t)
        // reads as 0b0001
        ADD_BITFIELD_RO(SUB, 4, 4)
        // reads as 0b0011
        ADD_BITFIELD_RO(MAJOR, 0, 4)
    END_TYPE()

    // TRCPIDR4
    // Reset value: 0x00000004
    BEGIN_TYPE(TRCPIDR4_t, uint32_t)
        // reads as `ImpDef
        ADD_BITFIELD_RO(SIZE, 4, 4)
        // reads as `ImpDef
        ADD_BITFIELD_RO(DES_2, 0, 4)
    END_TYPE()

    // TRCPIDR5
    // Reset value: 0x00000000
    BEGIN_TYPE(TRCPIDR5_t, uint32_t)
        ADD_BITFIELD_RW(TRCPIDR5, 0, 32)
    END_TYPE()

    // TRCPIDR6
    // Reset value: 0x00000000
    BEGIN_TYPE(TRCPIDR6_t, uint32_t)
        ADD_BITFIELD_RW(TRCPIDR6, 0, 32)
    END_TYPE()

    // TRCPIDR7
    // Reset value: 0x00000000
    BEGIN_TYPE(TRCPIDR7_t, uint32_t)
        ADD_BITFIELD_RW(TRCPIDR7, 0, 32)
    END_TYPE()

    // TRCPIDR0
    // Reset value: 0x00000021
    BEGIN_TYPE(TRCPIDR0_t, uint32_t)
        // reads as `ImpDef
        ADD_BITFIELD_RO(PART_0, 0, 8)
    END_TYPE()

    // TRCPIDR1
    // Reset value: 0x000000bd
    BEGIN_TYPE(TRCPIDR1_t, uint32_t)
        // reads as `ImpDef
        ADD_BITFIELD_RO(DES_0, 4, 4)
        // reads as `ImpDef
        ADD_BITFIELD_RO(PART_0, 0, 4)
    END_TYPE()

    // TRCPIDR2
    // Reset value: 0x0000002b
    BEGIN_TYPE(TRCPIDR2_t, uint32_t)
        // reads as `ImpDef
        ADD_BITFIELD_RO(REVISION, 4, 4)
        // reads as 0b1
        ADD_BITFIELD_RO(JEDEC, 3, 1)
        // reads as `ImpDef
        ADD_BITFIELD_RO(DES_0, 0, 3)
    END_TYPE()

    // TRCPIDR3
    // Reset value: 0x00000000
    BEGIN_TYPE(TRCPIDR3_t, uint32_t)
        // reads as `ImpDef
        ADD_BITFIELD_RO(REVAND, 4, 4)
        // reads as `ImpDef
        ADD_BITFIELD_RO(CMOD, 0, 4)
    END_TYPE()

    // TRCCIDR0
    // Reset value: 0x0000000d
    BEGIN_TYPE(TRCCIDR0_t, uint32_t)
        // reads as 0b00001101
        ADD_BITFIELD_RO(PRMBL_0, 0, 8)
    END_TYPE()

    // TRCCIDR1
    // Reset value: 0x00000090
    BEGIN_TYPE(TRCCIDR1_t, uint32_t)
        // reads as 0b1001
        ADD_BITFIELD_RO(CLASS, 4, 4)
        // reads as 0b0000
        ADD_BITFIELD_RO(PRMBL_1, 0, 4)
    END_TYPE()

    // TRCCIDR2
    // Reset value: 0x00000005
    BEGIN_TYPE(TRCCIDR2_t, uint32_t)
        // reads as 0b00000101
        ADD_BITFIELD_RO(PRMBL_2, 0, 8)
    END_TYPE()

    // TRCCIDR3
    // Reset value: 0x000000b1
    BEGIN_TYPE(TRCCIDR3_t, uint32_t)
        // reads as 0b10110001
        ADD_BITFIELD_RO(PRMBL_3, 0, 8)
    END_TYPE()

    // CTI Control Register
    // Reset value: 0x00000000
    BEGIN_TYPE(CTICONTROL_t, uint32_t)
        // Enables or disables the CTI
        ADD_BITFIELD_RW(GLBEN, 0, 1)
    END_TYPE()

    // CTI Interrupt Acknowledge Register
    // Reset value: 0x00000000
    BEGIN_TYPE(CTIINTACK_t, uint32_t)
        // Acknowledges the corresponding ctitrigout output. There is one bit of the register for each ctitrigout output. When a 1 is written to a bit in this register, the corresponding ctitrigout is acknowledged, causing it to be cleared.
        ADD_BITFIELD_RW(INTACK, 0, 8)
    END_TYPE()

    // CTI Application Trigger Set Register
    // Reset value: 0x00000000
    BEGIN_TYPE(CTIAPPSET_t, uint32_t)
        // Setting a bit HIGH generates a channel event for the selected channel. There is one bit of the register for each channel
        ADD_BITFIELD_RW(APPSET, 0, 4)
    END_TYPE()

    // CTI Application Trigger Clear Register
    // Reset value: 0x00000000
    BEGIN_TYPE(CTIAPPCLEAR_t, uint32_t)
        // Sets the corresponding bits in the CTIAPPSET to 0. There is one bit of the register for each channel.
        ADD_BITFIELD_RW(APPCLEAR, 0, 4)
    END_TYPE()

    // CTI Application Pulse Register
    // Reset value: 0x00000000
    BEGIN_TYPE(CTIAPPPULSE_t, uint32_t)
        // Setting a bit HIGH generates a channel event pulse for the selected channel. There is one bit of the register for each channel.
        ADD_BITFIELD_RW(APPULSE, 0, 4)
    END_TYPE()

    // CTI Trigger to Channel Enable Registers
    // Reset value: 0x00000000
    BEGIN_TYPE(CTIINEN_t, uint32_t)
        // Enables a cross trigger event to the corresponding channel when a ctitrigin input is activated. There is one bit of the field for each of the four channels
        ADD_BITFIELD_RW(TRIGINEN, 0, 4)
    END_TYPE()

    // CTI Trigger to Channel Enable Registers
    // Reset value: 0x00000000
    BEGIN_TYPE(CTIOUTEN_t, uint32_t)
        // Enables a cross trigger event to ctitrigout when the corresponding channel is activated. There is one bit of the field for each of the four channels.
        ADD_BITFIELD_RW(TRIGOUTEN, 0, 4)
    END_TYPE()

    // CTI Trigger to Channel Enable Registers
    // Reset value: 0x00000000
    BEGIN_TYPE(CTITRIGINSTATUS_t, uint32_t)
        // Shows the status of the ctitrigin inputs. There is one bit of the field for each trigger input.Because the register provides a view of the raw ctitrigin inputs, the reset value is UNKNOWN.
        ADD_BITFIELD_RO(TRIGINSTATUS, 0, 8)
    END_TYPE()

    // CTI Trigger In Status Register
    // Reset value: 0x00000000
    BEGIN_TYPE(CTITRIGOUTSTATUS_t, uint32_t)
        // Shows the status of the ctitrigout outputs. There is one bit of the field for each trigger output.
        ADD_BITFIELD_RO(TRIGOUTSTATUS, 0, 8)
    END_TYPE()

    // CTI Channel In Status Register
    // Reset value: 0x00000000
    BEGIN_TYPE(CTICHINSTATUS_t, uint32_t)
        // Shows the status of the ctichout outputs. There is one bit of the field for each channel output
        ADD_BITFIELD_RO(CTICHOUTSTATUS, 0, 4)
    END_TYPE()

    // Enable CTI Channel Gate register
    // Reset value: 0x0000000f
    BEGIN_TYPE(CTIGATE_t, uint32_t)
        // Enable ctichout3. Set to 0 to disable channel propagation.
        ADD_BITFIELD_RW(CTIGATEEN3, 3, 1)
        // Enable ctichout2. Set to 0 to disable channel propagation.
        ADD_BITFIELD_RW(CTIGATEEN2, 2, 1)
        // Enable ctichout1. Set to 0 to disable channel propagation.
        ADD_BITFIELD_RW(CTIGATEEN1, 1, 1)
        // Enable ctichout0. Set to 0 to disable channel propagation.
        ADD_BITFIELD_RW(CTIGATEEN0, 0, 1)
    END_TYPE()

    // External Multiplexer Control register
    // Reset value: 0x00000000
    BEGIN_TYPE(ASICCTL_t, uint32_t)
        ADD_BITFIELD_RW(ASICCTL, 0, 32)
    END_TYPE()

    // Integration Test Channel Output register
    // Reset value: 0x00000000
    BEGIN_TYPE(ITCHOUT_t, uint32_t)
        // Sets the value of the ctichout outputs
        ADD_BITFIELD_RW(CTCHOUT, 0, 4)
    END_TYPE()

    // Integration Test Trigger Output register
    // Reset value: 0x00000000
    BEGIN_TYPE(ITTRIGOUT_t, uint32_t)
        // Sets the value of the ctitrigout outputs
        ADD_BITFIELD_RW(CTTRIGOUT, 0, 8)
    END_TYPE()

    // Integration Test Channel Input register
    // Reset value: 0x00000000
    BEGIN_TYPE(ITCHIN_t, uint32_t)
        // Reads the value of the ctichin inputs.
        ADD_BITFIELD_RO(CTCHIN, 0, 4)
    END_TYPE()

    // Integration Mode Control register
    // Reset value: 0x00000000
    BEGIN_TYPE(ITCTRL_t, uint32_t)
        // Integration Mode Enable
        ADD_BITFIELD_RW(IME, 0, 1)
    END_TYPE()

    // Device Architecture register
    // Reset value: 0x47701a14
    BEGIN_TYPE(DEVARCH_t, uint32_t)
        // Indicates the component architect
        ADD_BITFIELD_RO(ARCHITECT, 21, 11)
        // Indicates whether the DEVARCH register is present
        ADD_BITFIELD_RO(PRESENT, 20, 1)
        // Indicates the architecture revision
        ADD_BITFIELD_RO(REVISION, 16, 4)
        // Indicates the component
        ADD_BITFIELD_RO(ARCHID, 0, 16)
    END_TYPE()

    // Device Configuration register
    // Reset value: 0x00040800
    BEGIN_TYPE(DEVID_t, uint32_t)
        // Number of ECT channels available
        ADD_BITFIELD_RO(NUMCH, 16, 4)
        // Number of ECT triggers available.
        ADD_BITFIELD_RO(NUMTRIG, 8, 8)
        // Indicates the number of multiplexers available on Trigger Inputs and Trigger Outputs that are using asicctl. The default value of 0b00000 indicates that no multiplexing is present. This value of this bit depends on the Verilog define EXTMUXNUM that you must change accordingly.
        ADD_BITFIELD_RO(EXTMUXNUM, 0, 5)
    END_TYPE()

    // Device Type Identifier register
    // Reset value: 0x00000014
    BEGIN_TYPE(DEVTYPE_t, uint32_t)
        // Sub-classification of the type of the debug component as specified in the ARM Architecture Specification within the major classification as specified in the MAJOR field.
        ADD_BITFIELD_RO(SUB, 4, 4)
        // Major classification of the type of the debug component as specified in the ARM Architecture Specification for this debug and trace component.
        ADD_BITFIELD_RO(MAJOR, 0, 4)
    END_TYPE()

    // CoreSight Peripheral ID4
    // Reset value: 0x00000004
    BEGIN_TYPE(PIDR4_t, uint32_t)
        // Always 0b0000. Indicates that the device only occupies 4KB of memory
        ADD_BITFIELD_RO(SIZE, 4, 4)
        // Together, PIDR1.DES_0, PIDR2.DES_1, and PIDR4.DES_2 identify the designer of the component.
        ADD_BITFIELD_RO(DES_2, 0, 4)
    END_TYPE()

    // CoreSight Peripheral ID5
    // Reset value: 0x00000000
    BEGIN_TYPE(PIDR5_t, uint32_t)
        ADD_BITFIELD_RW(PIDR5, 0, 32)
    END_TYPE()

    // CoreSight Peripheral ID6
    // Reset value: 0x00000000
    BEGIN_TYPE(PIDR6_t, uint32_t)
        ADD_BITFIELD_RW(PIDR6, 0, 32)
    END_TYPE()

    // CoreSight Peripheral ID7
    // Reset value: 0x00000000
    BEGIN_TYPE(PIDR7_t, uint32_t)
        ADD_BITFIELD_RW(PIDR7, 0, 32)
    END_TYPE()

    // CoreSight Peripheral ID0
    // Reset value: 0x00000021
    BEGIN_TYPE(PIDR0_t, uint32_t)
        // Bits[7:0] of the 12-bit part number of the component. The designer of the component assigns this part number.
        ADD_BITFIELD_RO(PART_0, 0, 8)
    END_TYPE()

    // CoreSight Peripheral ID1
    // Reset value: 0x000000bd
    BEGIN_TYPE(PIDR1_t, uint32_t)
        // Together, PIDR1.DES_0, PIDR2.DES_1, and PIDR4.DES_2 identify the designer of the component.
        ADD_BITFIELD_RO(DES_0, 4, 4)
        // Bits[11:8] of the 12-bit part number of the component. The designer of the component assigns this part number.
        ADD_BITFIELD_RO(PART_1, 0, 4)
    END_TYPE()

    // CoreSight Peripheral ID2
    // Reset value: 0x0000000b
    BEGIN_TYPE(PIDR2_t, uint32_t)
        // This device is at r1p0
        ADD_BITFIELD_RO(REVISION, 4, 4)
        // Always 1. Indicates that the JEDEC-assigned designer ID is used.
        ADD_BITFIELD_RO(JEDEC, 3, 1)
        // Together, PIDR1.DES_0, PIDR2.DES_1, and PIDR4.DES_2 identify the designer of the component.
        ADD_BITFIELD_RO(DES_1, 0, 3)
    END_TYPE()

    // CoreSight Peripheral ID3
    // Reset value: 0x00000000
    BEGIN_TYPE(PIDR3_t, uint32_t)
        // Indicates minor errata fixes specific to the revision of the component being used, for example metal fixes after implementation. In most cases, this field is 0b0000. ARM recommends that the component designers ensure that a metal fix can change this field if required, for example, by driving it from registers that reset to 0b0000.
        ADD_BITFIELD_RO(REVAND, 4, 4)
        // Customer Modified. Indicates whether the customer has modified the behavior of the component. In most cases, this field is 0b0000. Customers change this value when they make authorized modifications to this component.
        ADD_BITFIELD_RO(CMOD, 0, 4)
    END_TYPE()

    // CoreSight Component ID0
    // Reset value: 0x0000000d
    BEGIN_TYPE(CIDR0_t, uint32_t)
        // Preamble[0]. Contains bits[7:0] of the component identification code
        ADD_BITFIELD_RO(PRMBL_0, 0, 8)
    END_TYPE()

    // CoreSight Component ID1
    // Reset value: 0x00000090
    BEGIN_TYPE(CIDR1_t, uint32_t)
        // Class of the component, for example, whether the component is a ROM table or a generic CoreSight component. Contains bits[15:12] of the component identification code.
        ADD_BITFIELD_RO(CLASS, 4, 4)
        // Preamble[1]. Contains bits[11:8] of the component identification code.
        ADD_BITFIELD_RO(PRMBL_1, 0, 4)
    END_TYPE()

    // CoreSight Component ID2
    // Reset value: 0x00000005
    BEGIN_TYPE(CIDR2_t, uint32_t)
        // Preamble[2]. Contains bits[23:16] of the component identification code.
        ADD_BITFIELD_RO(PRMBL_2, 0, 8)
    END_TYPE()

    // CoreSight Component ID3
    // Reset value: 0x000000b1
    BEGIN_TYPE(CIDR3_t, uint32_t)
        // Preamble[3]. Contains bits[31:24] of the component identification code.
        ADD_BITFIELD_RO(PRMBL_3, 0, 8)
    END_TYPE()

    struct PPB_t {
        ITM_STIM_t                    ITM_STIM[32];
        uint32_t                      reserved0[864];
        ITM_TER0_t                    ITM_TER0;
        uint32_t                      reserved1[15];
        ITM_TPR_t                     ITM_TPR;
        uint32_t                      reserved2[15];
        ITM_TCR_t                     ITM_TCR;
        uint32_t                      reserved3[27];
        INT_ATREADY_t                 INT_ATREADY;
        uint32_t                      reserved4;
        INT_ATVALID_t                 INT_ATVALID;
        uint32_t                      reserved5;
        ITM_ITCTRL_t                  ITM_ITCTRL;
        uint32_t                      reserved6[46];
        ITM_DEVARCH_t                 ITM_DEVARCH;
        uint32_t                      reserved7[3];
        ITM_DEVTYPE_t                 ITM_DEVTYPE;
        ITM_PIDR4_t                   ITM_PIDR4;
        ITM_PIDR5_t                   ITM_PIDR5;
        ITM_PIDR6_t                   ITM_PIDR6;
        ITM_PIDR7_t                   ITM_PIDR7;
        ITM_PIDR0_t                   ITM_PIDR0;
        ITM_PIDR1_t                   ITM_PIDR1;
        ITM_PIDR2_t                   ITM_PIDR2;
        ITM_PIDR3_t                   ITM_PIDR3;
        ITM_CIDR0_t                   ITM_CIDR0;
        ITM_CIDR1_t                   ITM_CIDR1;
        ITM_CIDR2_t                   ITM_CIDR2;
        ITM_CIDR3_t                   ITM_CIDR3;
        DWT_CTRL_t                    DWT_CTRL;
        DWT_CYCCNT_t                  DWT_CYCCNT;
        uint32_t                      reserved8;
        DWT_EXCCNT_t                  DWT_EXCCNT;
        uint32_t                      reserved9;
        DWT_LSUCNT_t                  DWT_LSUCNT;
        DWT_FOLDCNT_t                 DWT_FOLDCNT;
        uint32_t                      reserved10;
        DWT_COMP0_t                   DWT_COMP0;
        uint32_t                      reserved11;
        DWT_FUNCTION0_t               DWT_FUNCTION0;
        uint32_t                      reserved12;
        DWT_COMP1_t                   DWT_COMP1;
        uint32_t                      reserved13;
        DWT_FUNCTION1_t               DWT_FUNCTION1;
        uint32_t                      reserved14;
        DWT_COMP2_t                   DWT_COMP2;
        uint32_t                      reserved15;
        DWT_FUNCTION2_t               DWT_FUNCTION2;
        uint32_t                      reserved16;
        DWT_COMP3_t                   DWT_COMP3;
        uint32_t                      reserved17;
        DWT_FUNCTION3_t               DWT_FUNCTION3;
        uint32_t                      reserved18[984];
        DWT_DEVARCH_t                 DWT_DEVARCH;
        uint32_t                      reserved19[3];
        DWT_DEVTYPE_t                 DWT_DEVTYPE;
        DWT_PIDR4_t                   DWT_PIDR4;
        DWT_PIDR5_t                   DWT_PIDR5;
        DWT_PIDR6_t                   DWT_PIDR6;
        DWT_PIDR7_t                   DWT_PIDR7;
        DWT_PIDR0_t                   DWT_PIDR0;
        DWT_PIDR1_t                   DWT_PIDR1;
        DWT_PIDR2_t                   DWT_PIDR2;
        DWT_PIDR3_t                   DWT_PIDR3;
        DWT_CIDR0_t                   DWT_CIDR0;
        DWT_CIDR1_t                   DWT_CIDR1;
        DWT_CIDR2_t                   DWT_CIDR2;
        DWT_CIDR3_t                   DWT_CIDR3;
        FP_CTRL_t                     FP_CTRL;
        FP_REMAP_t                    FP_REMAP;
        FP_COMP_t                     FP_COMP[8];
        uint32_t                      reserved20[997];
        FP_DEVARCH_t                  FP_DEVARCH;
        uint32_t                      reserved21[3];
        FP_DEVTYPE_t                  FP_DEVTYPE;
        FP_PIDR4_t                    FP_PIDR4;
        FP_PIDR5_t                    FP_PIDR5;
        FP_PIDR6_t                    FP_PIDR6;
        FP_PIDR7_t                    FP_PIDR7;
        FP_PIDR0_t                    FP_PIDR0;
        FP_PIDR1_t                    FP_PIDR1;
        FP_PIDR2_t                    FP_PIDR2;
        FP_PIDR3_t                    FP_PIDR3;
        FP_CIDR0_t                    FP_CIDR0;
        FP_CIDR1_t                    FP_CIDR1;
        FP_CIDR2_t                    FP_CIDR2;
        FP_CIDR3_t                    FP_CIDR3;
        uint32_t                      reserved22[11265];
        ICTR_t                        ICTR;
        ACTLR_t                       ACTLR;
        uint32_t                      reserved23;
        SYST_CSR_t                    SYST_CSR;
        SYST_RVR_t                    SYST_RVR;
        SYST_CVR_t                    SYST_CVR;
        SYST_CALIB_t                  SYST_CALIB;
        uint32_t                      reserved24[56];
        NVIC_ISER0_t                  NVIC_ISER0;
        NVIC_ISER1_t                  NVIC_ISER1;
        uint32_t                      reserved25[30];
        NVIC_ICER0_t                  NVIC_ICER0;
        NVIC_ICER1_t                  NVIC_ICER1;
        uint32_t                      reserved26[30];
        NVIC_ISPR0_t                  NVIC_ISPR0;
        NVIC_ISPR1_t                  NVIC_ISPR1;
        uint32_t                      reserved27[30];
        NVIC_ICPR0_t                  NVIC_ICPR0;
        NVIC_ICPR1_t                  NVIC_ICPR1;
        uint32_t                      reserved28[30];
        NVIC_IABR0_t                  NVIC_IABR0;
        NVIC_IABR1_t                  NVIC_IABR1;
        uint32_t                      reserved29[30];
        NVIC_ITNS0_t                  NVIC_ITNS0;
        NVIC_ITNS1_t                  NVIC_ITNS1;
        uint32_t                      reserved30[30];
        NVIC_IPR_t                    NVIC_IPR[16];
        uint32_t                      reserved31[560];
        CPUID_t                       CPUID;
        ICSR_t                        ICSR;
        VTOR_t                        VTOR;
        AIRCR_t                       AIRCR;
        SCR_t                         SCR;
        CCR_t                         CCR;
        SHPR1_t                       SHPR1;
        SHPR2_t                       SHPR2;
        SHPR3_t                       SHPR3;
        SHCSR_t                       SHCSR;
        CFSR_t                        CFSR;
        HFSR_t                        HFSR;
        DFSR_t                        DFSR;
        MMFAR_t                       MMFAR;
        BFAR_t                        BFAR;
        uint32_t                      reserved32;
        ID_PFR0_t                     ID_PFR0;
        ID_PFR1_t                     ID_PFR1;
        ID_DFR0_t                     ID_DFR0;
        ID_AFR0_t                     ID_AFR0;
        ID_MMFR0_t                    ID_MMFR0;
        ID_MMFR1_t                    ID_MMFR1;
        ID_MMFR2_t                    ID_MMFR2;
        ID_MMFR3_t                    ID_MMFR3;
        ID_ISAR0_t                    ID_ISAR0;
        ID_ISAR1_t                    ID_ISAR1;
        ID_ISAR2_t                    ID_ISAR2;
        ID_ISAR3_t                    ID_ISAR3;
        ID_ISAR4_t                    ID_ISAR4;
        ID_ISAR5_t                    ID_ISAR5;
        uint32_t                      reserved33;
        CTR_t                         CTR;
        uint32_t                      reserved34[2];
        CPACR_t                       CPACR;
        NSACR_t                       NSACR;
        MPU_TYPE_t                    MPU_TYPE;
        MPU_CTRL_t                    MPU_CTRL;
        MPU_RNR_t                     MPU_RNR;
        MPU_RBAR_t                    MPU_RBAR;
        MPU_RLAR_t                    MPU_RLAR;
        MPU_RBAR_A1_t                 MPU_RBAR_A1;
        MPU_RLAR_A1_t                 MPU_RLAR_A1;
        MPU_RBAR_A2_t                 MPU_RBAR_A2;
        MPU_RLAR_A2_t                 MPU_RLAR_A2;
        MPU_RBAR_A3_t                 MPU_RBAR_A3;
        MPU_RLAR_A3_t                 MPU_RLAR_A3;
        uint32_t                      reserved35;
        MPU_MAIR0_t                   MPU_MAIR0;
        MPU_MAIR1_t                   MPU_MAIR1;
        uint32_t                      reserved36[2];
        SAU_CTRL_t                    SAU_CTRL;
        SAU_TYPE_t                    SAU_TYPE;
        SAU_RNR_t                     SAU_RNR;
        SAU_RBAR_t                    SAU_RBAR;
        SAU_RLAR_t                    SAU_RLAR;
        SFSR_t                        SFSR;
        SFAR_t                        SFAR;
        uint32_t                      reserved37;
        DHCSR_t                       DHCSR;
        DCRSR_t                       DCRSR;
        DCRDR_t                       DCRDR;
        DEMCR_t                       DEMCR;
        uint32_t                      reserved38[2];
        DSCSR_t                       DSCSR;
        uint32_t                      reserved39[61];
        STIR_t                        STIR;
        uint32_t                      reserved40[12];
        FPCCR_t                       FPCCR;
        FPCAR_t                       FPCAR;
        FPDSCR_t                      FPDSCR;
        MVFR0_t                       MVFR0;
        MVFR1_t                       MVFR1;
        MVFR2_t                       MVFR2;
        uint32_t                      reserved41[28];
        DDEVARCH_t                    DDEVARCH;
        uint32_t                      reserved42[3];
        DDEVTYPE_t                    DDEVTYPE;
        DPIDR4_t                      DPIDR4;
        DPIDR5_t                      DPIDR5;
        DPIDR6_t                      DPIDR6;
        DPIDR7_t                      DPIDR7;
        DPIDR0_t                      DPIDR0;
        DPIDR1_t                      DPIDR1;
        DPIDR2_t                      DPIDR2;
        DPIDR3_t                      DPIDR3;
        DCIDR0_t                      DCIDR0;
        DCIDR1_t                      DCIDR1;
        DCIDR2_t                      DCIDR2;
        DCIDR3_t                      DCIDR3;
        uint32_t                      reserved43[51201];
        TRCPRGCTLR_t                  TRCPRGCTLR;
        uint32_t                      reserved44;
        TRCSTATR_t                    TRCSTATR;
        TRCCONFIGR_t                  TRCCONFIGR;
        uint32_t                      reserved45[3];
        TRCEVENTCTL0R_t               TRCEVENTCTL0R;
        TRCEVENTCTL1R_t               TRCEVENTCTL1R;
        uint32_t                      reserved46;
        TRCSTALLCTLR_t                TRCSTALLCTLR;
        TRCTSCTLR_t                   TRCTSCTLR;
        TRCSYNCPR_t                   TRCSYNCPR;
        TRCCCCTLR_t                   TRCCCCTLR;
        uint32_t                      reserved47[17];
        TRCVICTLR_t                   TRCVICTLR;
        uint32_t                      reserved48[47];
        TRCCNTRLDVR0_t                TRCCNTRLDVR0;
        uint32_t                      reserved49[15];
        TRCIDR8_t                     TRCIDR8;
        TRCIDR9_t                     TRCIDR9;
        TRCIDR10_t                    TRCIDR10;
        TRCIDR11_t                    TRCIDR11;
        TRCIDR12_t                    TRCIDR12;
        TRCIDR13_t                    TRCIDR13;
        uint32_t                      reserved50[10];
        TRCIMSPEC_t                   TRCIMSPEC;
        uint32_t                      reserved51[7];
        TRCIDR0_t                     TRCIDR0;
        TRCIDR1_t                     TRCIDR1;
        TRCIDR2_t                     TRCIDR2;
        TRCIDR3_t                     TRCIDR3;
        TRCIDR4_t                     TRCIDR4;
        TRCIDR5_t                     TRCIDR5;
        TRCIDR6_t                     TRCIDR6;
        TRCIDR7_t                     TRCIDR7;
        uint32_t                      reserved52[2];
        TRCRSCTLR2_t                  TRCRSCTLR2;
        TRCRSCTLR3_t                  TRCRSCTLR3;
        uint32_t                      reserved53[36];
        TRCSSCSR_t                    TRCSSCSR;
        uint32_t                      reserved54[7];
        TRCSSPCICR_t                  TRCSSPCICR;
        uint32_t                      reserved55[19];
        TRCPDCR_t                     TRCPDCR;
        TRCPDSR_t                     TRCPDSR;
        uint32_t                      reserved56[755];
        TRCITATBIDR_t                 TRCITATBIDR;
        uint32_t                      reserved57[3];
        TRCITIATBINR_t                TRCITIATBINR;
        uint32_t                      reserved58;
        TRCITIATBOUTR_t               TRCITIATBOUTR;
        uint32_t                      reserved59[40];
        TRCCLAIMSET_t                 TRCCLAIMSET;
        TRCCLAIMCLR_t                 TRCCLAIMCLR;
        uint32_t                      reserved60[4];
        TRCAUTHSTATUS_t               TRCAUTHSTATUS;
        TRCDEVARCH_t                  TRCDEVARCH;
        uint32_t                      reserved61[2];
        TRCDEVID_t                    TRCDEVID;
        TRCDEVTYPE_t                  TRCDEVTYPE;
        TRCPIDR4_t                    TRCPIDR4;
        TRCPIDR5_t                    TRCPIDR5;
        TRCPIDR6_t                    TRCPIDR6;
        TRCPIDR7_t                    TRCPIDR7;
        TRCPIDR0_t                    TRCPIDR0;
        TRCPIDR1_t                    TRCPIDR1;
        TRCPIDR2_t                    TRCPIDR2;
        TRCPIDR3_t                    TRCPIDR3;
        TRCCIDR0_t                    TRCCIDR0;
        TRCCIDR1_t                    TRCCIDR1;
        TRCCIDR2_t                    TRCCIDR2;
        TRCCIDR3_t                    TRCCIDR3;
        CTICONTROL_t                  CTICONTROL;
        uint32_t                      reserved62[3];
        CTIINTACK_t                   CTIINTACK;
        CTIAPPSET_t                   CTIAPPSET;
        CTIAPPCLEAR_t                 CTIAPPCLEAR;
        CTIAPPPULSE_t                 CTIAPPPULSE;
        CTIINEN_t                     CTIINEN[8];
        uint32_t                      reserved63[24];
        CTIOUTEN_t                    CTIOUTEN[8];
        uint32_t                      reserved64[28];
        CTITRIGINSTATUS_t             CTITRIGINSTATUS;
        CTITRIGOUTSTATUS_t            CTITRIGOUTSTATUS;
        CTICHINSTATUS_t               CTICHINSTATUS;
        uint32_t                      reserved65;
        CTIGATE_t                     CTIGATE;
        ASICCTL_t                     ASICCTL;
        uint32_t                      reserved66[871];
        ITCHOUT_t                     ITCHOUT;
        ITTRIGOUT_t                   ITTRIGOUT;
        uint32_t                      reserved67[2];
        ITCHIN_t                      ITCHIN;
        uint32_t                      reserved68[2];
        ITCTRL_t                      ITCTRL;
        uint32_t                      reserved69[46];
        DEVARCH_t                     DEVARCH;
        uint32_t                      reserved70[2];
        DEVID_t                       DEVID;
        DEVTYPE_t                     DEVTYPE;
        PIDR4_t                       PIDR4;
        PIDR5_t                       PIDR5;
        PIDR6_t                       PIDR6;
        PIDR7_t                       PIDR7;
        PIDR0_t                       PIDR0;
        PIDR1_t                       PIDR1;
        PIDR2_t                       PIDR2;
        PIDR3_t                       PIDR3;
        CIDR0_t                       CIDR0;
        CIDR1_t                       CIDR1;
        CIDR2_t                       CIDR2;
        CIDR3_t                       CIDR3;
    };

    static PPB_t & PPB = (*(PPB_t *)0xe0000000);

} // _PPB_

namespace _PPB_NS_  {

    static _PPB_::PPB_t & PPB_NS = (*(_PPB_::PPB_t *)0xe0020000);

} // _PPB_NS_

// QSPI Memory Interface. Provides a memory-mapped interface to up to two SPI/DSPI/QSPI flash or PSRAM devices. Also provides a serial interface for programming and configuration of the external device.
namespace _QMI_  {

    // Control and status for direct serial mode Direct serial mode allows the processor to send and receive raw serial frames, for programming, configuration and control of the external memory devices. Only SPI mode 0 (CPOL=0 CPHA=0) is supported.
    // Reset value: 0x01800000
    BEGIN_TYPE(DIRECT_CSR_t, uint32_t)
        // Delay the read data sample timing, in units of one half of a system clock cycle. (Not necessarily half of an SCK cycle.)
        ADD_BITFIELD_RW(RXDELAY, 30, 2)
        // Clock divisor for direct serial mode. Divisors of 1..255 are encoded directly, and the maximum divisor of 256 is encoded by a value of CLKDIV=0. The clock divisor can be changed on-the-fly by software, without halting or otherwise coordinating with the serial interface. The serial interface will sample the latest clock divisor each time it begins the transmission of a new byte.
        ADD_BITFIELD_RW(CLKDIV, 22, 8)
        // Current level of DIRECT_RX FIFO
        ADD_BITFIELD_RO(RXLEVEL, 18, 3)
        // When 1, the DIRECT_RX FIFO is currently full. The serial interface will be stalled until data is popped; the interface will not begin a new serial frame when the DIRECT_TX FIFO is empty or the DIRECT_RX FIFO is full.
        ADD_BITFIELD_RO(RXFULL, 17, 1)
        // When 1, the DIRECT_RX FIFO is currently empty. If the processor attempts to read more data, the FIFO state is not affected, but the value returned to the processor is undefined.
        ADD_BITFIELD_RO(RXEMPTY, 16, 1)
        // Current level of DIRECT_TX FIFO
        ADD_BITFIELD_RO(TXLEVEL, 12, 3)
        // When 1, the DIRECT_TX FIFO is currently empty. Unless the processor pushes more data, transmission will stop and BUSY will go low once the current 8-bit serial frame completes.
        ADD_BITFIELD_RO(TXEMPTY, 11, 1)
        // When 1, the DIRECT_TX FIFO is currently full. If the processor tries to write more data, that data will be ignored.
        ADD_BITFIELD_RO(TXFULL, 10, 1)
        // When 1, automatically assert the CS1n chip select line whenever the BUSY flag is set.
        ADD_BITFIELD_RW(AUTO_CS1N, 7, 1)
        // When 1, automatically assert the CS0n chip select line whenever the BUSY flag is set.
        ADD_BITFIELD_RW(AUTO_CS0N, 6, 1)
        // When 1, assert (i.e. drive low) the CS1n chip select line. Note that this applies even when DIRECT_CSR_EN is 0.
        ADD_BITFIELD_RW(ASSERT_CS1N, 3, 1)
        // When 1, assert (i.e. drive low) the CS0n chip select line. Note that this applies even when DIRECT_CSR_EN is 0.
        ADD_BITFIELD_RW(ASSERT_CS0N, 2, 1)
        // Direct mode busy flag. If 1, data is currently being shifted in/out (or would be if the interface were not stalled on the RX FIFO), and the chip select must not yet be deasserted. The busy flag will also be set to 1 if a memory-mapped transfer is still in progress when direct mode is enabled. Direct mode blocks new memory-mapped transfers, but can't halt a transfer that is already in progress. If there is a chance that memory-mapped transfers may be in progress, the busy flag should be polled for 0 before asserting the chip select. (In practice you will usually discover this timing condition through other means, because any subsequent memory-mapped transfers when direct mode is enabled will return bus errors, which are difficult to ignore.)
        ADD_BITFIELD_RO(BUSY, 1, 1)
        // Enable direct mode. In direct mode, software controls the chip select lines, and can perform direct SPI transfers by pushing data to the DIRECT_TX FIFO, and popping the same amount of data from the DIRECT_RX FIFO. Memory-mapped accesses will generate bus errors when direct serial mode is enabled.
        ADD_BITFIELD_RW(EN, 0, 1)
    END_TYPE()

    // Transmit FIFO for direct mode
    // Reset value: 0x00000000
    BEGIN_TYPE(DIRECT_TX_t, uint32_t)
        // Inhibit the RX FIFO push that would correspond to this TX FIFO entry. Useful to avoid garbage appearing in the RX FIFO when pushing the command at the beginning of a SPI transfer.
        ADD_BITFIELD_WO(NOPUSH, 20, 1)
        // Output enable (active-high). For single width (SPI), this field is ignored, and SD0 is always set to output, with SD1 always set to input. For dual and quad width (DSPI/QSPI), this sets whether the relevant SDx pads are set to output whilst transferring this FIFO record. In this case the command/address should have OE set, and the data transfer should have OE set or clear depending on the direction of the transfer.
        ADD_BITFIELD_WO(OE, 19, 1)
        // Data width. If 0, hardware will transmit the 8 LSBs of the DIRECT_TX DATA field, and return an 8-bit value in the 8 LSBs of DIRECT_RX. If 1, the full 16-bit width is used. 8-bit and 16-bit transfers can be mixed freely.
        ADD_BITFIELD_WO(DWIDTH, 18, 1)
        // Configure whether this FIFO record is transferred with single/dual/quad interface width (0/1/2). Different widths can be mixed freely.
        ADD_BITFIELD_WO(IWIDTH, 16, 2)
        // Data pushed here will be clocked out falling edges of SCK (or before the very first rising edge of SCK, if this is the first pulse). For each byte clocked out, the interface will simultaneously sample one byte, on rising edges of SCK, and push this to the DIRECT_RX FIFO. For 16-bit data, the least-significant byte is transmitted first.
        ADD_BITFIELD_WO(DATA, 0, 16)
    END_TYPE()

    // Single width
    static const uint32_t DIRECT_TX_IWIDTH__S = 0;
    // Dual width
    static const uint32_t DIRECT_TX_IWIDTH__D = 1;
    // Quad width
    static const uint32_t DIRECT_TX_IWIDTH__Q = 2;

    // Receive FIFO for direct mode
    // Reset value: 0x00000000
    BEGIN_TYPE(DIRECT_RX_t, uint32_t)
        // With each byte clocked out on the serial interface, one byte will simultaneously be clocked in, and will appear in this FIFO. The serial interface will stall when this FIFO is full, to avoid dropping data. When 16-bit data is pushed into the TX FIFO, the corresponding RX FIFO push will also contain 16 bits of data. The least-significant byte is the first one received.
        ADD_BITFIELD_RO(DIRECT_RX, 0, 16)
    END_TYPE()

    // Timing configuration register for memory address window 0.
    // Reset value: 0x40000004
    BEGIN_TYPE(M0_TIMING_t, uint32_t)
        // Chip select cooldown period. When a memory transfer finishes, the chip select remains asserted for 64 x COOLDOWN system clock cycles, plus half an SCK clock period (rounded up for odd SCK divisors). After this cooldown expires, the chip select is always deasserted to save power. If the next memory access arrives within the cooldown period, the QMI may be able to append more SCK cycles to the currently ongoing SPI transfer, rather than starting a new transfer. This reduces access latency and increases bus throughput. Specifically, the next access must be in the same direction (read/write), access the same memory window (chip select 0/1), and follow sequentially the address of the last transfer. If any of these are false, the new access will first deassert the chip select, then begin a new transfer. If COOLDOWN is 0, the address alignment configured by PAGEBREAK has been reached, or the total chip select assertion limit MAX_SELECT has been reached, the cooldown period is skipped, and the chip select will always be deasserted one half SCK period after the transfer finishes.
        ADD_BITFIELD_RW(COOLDOWN, 30, 2)
        // When page break is enabled, chip select will automatically deassert when crossing certain power-of-2-aligned address boundaries. The next access will always begin a new read/write SPI burst, even if the address of the next access follows in sequence with the last access before the page boundary. Some flash and PSRAM devices forbid crossing page boundaries with a single read/write transfer, or restrict the operating frequency for transfers that do cross page a boundary. This option allows the QMI to safely support those devices. This field has no effect when COOLDOWN is disabled.
        ADD_BITFIELD_RW(PAGEBREAK, 28, 2)
        // Add up to one additional system clock cycle of setup between chip select assertion and the first rising edge of SCK. The default setup time is one half SCK period, which is usually sufficient except for very high SCK frequencies with some flash devices.
        ADD_BITFIELD_RW(SELECT_SETUP, 25, 1)
        // Add up to three additional system clock cycles of active hold between the last falling edge of SCK and the deassertion of this window's chip select. The default hold time is one system clock cycle. Note that flash datasheets usually give chip select active hold time from the last *rising* edge of SCK, and so even zero hold from the last falling edge would be safe. Note that this is a minimum hold time guaranteed by the QMI: the actual chip select active hold may be slightly longer for read transfers with low clock divisors and/or high sample delays. Specifically, if the point two cycles after the last RX data sample is later than the last SCK falling edge, then the hold time is measured from *this* point. Note also that, in case the final SCK pulse is masked to save energy (true for non-DTR reads when COOLDOWN is disabled or PAGE_BREAK is reached), all of QMI's timing logic behaves as though the clock pulse were still present. The SELECT_HOLD time is applied from the point where the last SCK falling edge would be if the clock pulse were not masked.
        ADD_BITFIELD_RW(SELECT_HOLD, 23, 2)
        // Enforce a maximum assertion duration for this window's chip select, in units of 64 system clock cycles. If 0, the QMI is permitted to keep the chip select asserted indefinitely when servicing sequential memory accesses (see COOLDOWN). This feature is required to meet timing constraints of PSRAM devices, which specify a maximum chip select assertion so they can perform DRAM refresh cycles. See also MIN_DESELECT, which can enforce a minimum deselect time. If a memory access is in progress at the time MAX_SELECT is reached, the QMI will wait for the access to complete before deasserting the chip select. This additional time must be accounted for to calculate a safe MAX_SELECT value. In the worst case, this may be a fully-formed serial transfer, including command prefix and address, with a data payload as large as one cache line.
        ADD_BITFIELD_RW(MAX_SELECT, 17, 6)
        // After this window's chip select is deasserted, it remains deasserted for half an SCK cycle (rounded up to an integer number of system clock cycles), plus MIN_DESELECT additional system clock cycles, before the QMI reasserts either chip select pin. Nonzero values may be required for PSRAM devices which enforce a longer minimum CS deselect time, so that they can perform internal DRAM refresh cycles whilst deselected.
        ADD_BITFIELD_RW(MIN_DESELECT, 12, 5)
        // Delay the read data sample timing, in units of one half of a system clock cycle. (Not necessarily half of an SCK cycle.) An RXDELAY of 0 means the sample is captured at the SDI input registers simultaneously with the rising edge of SCK launched from the SCK output register. At higher SCK frequencies, RXDELAY may need to be increased to account for the round trip delay of the pads, and the clock-to-Q delay of the QSPI memory device.
        ADD_BITFIELD_RW(RXDELAY, 8, 3)
        // Clock divisor. Odd and even divisors are supported. Defines the SCK clock period in units of 1 system clock cycle. Divisors 1..255 are encoded directly, and a divisor of 256 is encoded with a value of CLKDIV=0. The clock divisor can be changed on-the-fly, even when the QMI is currently accessing memory in this address window. All other parameters must only be changed when the QMI is idle. If software is increasing CLKDIV in anticipation of an increase in the system clock frequency, a dummy access to either memory window (and appropriate processor barriers/fences) must be inserted after the Mx_TIMING write to ensure the SCK divisor change is in effect _before_ the system clock is changed.
        ADD_BITFIELD_RW(CLKDIV, 0, 8)
    END_TYPE()

    // No page boundary is enforced
    static const uint32_t M0_TIMING_PAGEBREAK__NONE = 0;
    // Break bursts crossing a 256-byte page boundary
    static const uint32_t M0_TIMING_PAGEBREAK__256 = 1;
    // Break bursts crossing a 1024-byte quad-page boundary
    static const uint32_t M0_TIMING_PAGEBREAK__1024 = 2;
    // Break bursts crossing a 4096-byte sector boundary
    static const uint32_t M0_TIMING_PAGEBREAK__4096 = 3;

    // Read transfer format configuration for memory address window 0. Configure the bus width of each transfer phase individually, and configure the length or presence of the command prefix, command suffix and dummy/turnaround transfer phases. Only 24-bit addresses are supported. The reset value of the M0_RFMT register is configured to support a basic 03h serial read transfer with no additional configuration.
    // Reset value: 0x00001000
    BEGIN_TYPE(M0_RFMT_t, uint32_t)
        // Enable double transfer rate (DTR) for read commands: address, suffix and read data phases are active on both edges of SCK. SDO data is launched centre-aligned on each SCK edge, and SDI data is captured on the SCK edge that follows its launch. DTR is implemented by halving the clock rate; SCK has a period of 2 x CLK_DIV throughout the transfer. The prefix and dummy phases are still single transfer rate. If the suffix is quad-width, it must be 0 or 8 bits in length, to ensure an even number of SCK edges.
        ADD_BITFIELD_RW(DTR, 28, 1)
        // Length of dummy phase between command suffix and data phase, in units of 4 bits. (i.e. 1 cycle for quad width, 2 for dual, 4 for single)
        ADD_BITFIELD_RW(DUMMY_LEN, 16, 3)
        // Length of post-address command suffix, in units of 4 bits. (i.e. 1 cycle for quad width, 2 for dual, 4 for single) Only values of 0 and 8 bits are supported.
        ADD_BITFIELD_RW(SUFFIX_LEN, 14, 2)
        // Length of command prefix, in units of 8 bits. (i.e. 2 cycles for quad width, 4 for dual, 8 for single)
        ADD_BITFIELD_RW(PREFIX_LEN, 12, 1)
        // The width used for the data transfer
        ADD_BITFIELD_RW(DATA_WIDTH, 8, 2)
        // The width used for the dummy phase, if any. If width is single, SD0/MOSI is held asserted low during the dummy phase, and SD1...SD3 are tristated. If width is dual/quad, all IOs are tristated during the dummy phase.
        ADD_BITFIELD_RW(DUMMY_WIDTH, 6, 2)
        // The width used for the post-address command suffix, if any
        ADD_BITFIELD_RW(SUFFIX_WIDTH, 4, 2)
        // The transfer width used for the address. The address phase always transfers 24 bits in total.
        ADD_BITFIELD_RW(ADDR_WIDTH, 2, 2)
        // The transfer width used for the command prefix, if any
        ADD_BITFIELD_RW(PREFIX_WIDTH, 0, 2)
    END_TYPE()

    // No dummy phase
    static const uint32_t M0_RFMT_DUMMY_LEN__NONE = 0;
    // 4 dummy bits
    static const uint32_t M0_RFMT_DUMMY_LEN__4 = 1;
    // 8 dummy bits
    static const uint32_t M0_RFMT_DUMMY_LEN__8 = 2;
    // 12 dummy bits
    static const uint32_t M0_RFMT_DUMMY_LEN__12 = 3;
    // 16 dummy bits
    static const uint32_t M0_RFMT_DUMMY_LEN__16 = 4;
    // 20 dummy bits
    static const uint32_t M0_RFMT_DUMMY_LEN__20 = 5;
    // 24 dummy bits
    static const uint32_t M0_RFMT_DUMMY_LEN__24 = 6;
    // 28 dummy bits
    static const uint32_t M0_RFMT_DUMMY_LEN__28 = 7;
    // No suffix
    static const uint32_t M0_RFMT_SUFFIX_LEN__NONE = 0;
    // 8-bit suffix
    static const uint32_t M0_RFMT_SUFFIX_LEN__8 = 2;
    // No prefix
    static const uint32_t M0_RFMT_PREFIX_LEN__NONE = 0;
    // 8-bit prefix
    static const uint32_t M0_RFMT_PREFIX_LEN__8 = 1;
    // Single width
    static const uint32_t M0_RFMT_DATA_WIDTH__S = 0;
    // Dual width
    static const uint32_t M0_RFMT_DATA_WIDTH__D = 1;
    // Quad width
    static const uint32_t M0_RFMT_DATA_WIDTH__Q = 2;
    // Single width
    static const uint32_t M0_RFMT_DUMMY_WIDTH__S = 0;
    // Dual width
    static const uint32_t M0_RFMT_DUMMY_WIDTH__D = 1;
    // Quad width
    static const uint32_t M0_RFMT_DUMMY_WIDTH__Q = 2;
    // Single width
    static const uint32_t M0_RFMT_SUFFIX_WIDTH__S = 0;
    // Dual width
    static const uint32_t M0_RFMT_SUFFIX_WIDTH__D = 1;
    // Quad width
    static const uint32_t M0_RFMT_SUFFIX_WIDTH__Q = 2;
    // Single width
    static const uint32_t M0_RFMT_ADDR_WIDTH__S = 0;
    // Dual width
    static const uint32_t M0_RFMT_ADDR_WIDTH__D = 1;
    // Quad width
    static const uint32_t M0_RFMT_ADDR_WIDTH__Q = 2;
    // Single width
    static const uint32_t M0_RFMT_PREFIX_WIDTH__S = 0;
    // Dual width
    static const uint32_t M0_RFMT_PREFIX_WIDTH__D = 1;
    // Quad width
    static const uint32_t M0_RFMT_PREFIX_WIDTH__Q = 2;

    // Command constants used for reads from memory address window 0. The reset value of the M0_RCMD register is configured to support a basic 03h serial read transfer with no additional configuration.
    // Reset value: 0x0000a003
    BEGIN_TYPE(M0_RCMD_t, uint32_t)
        // The command suffix bits following the address, if Mx_RFMT_SUFFIX_LEN is nonzero.
        ADD_BITFIELD_RW(SUFFIX, 8, 8)
        // The command prefix bits to prepend on each new transfer, if Mx_RFMT_PREFIX_LEN is nonzero.
        ADD_BITFIELD_RW(PREFIX, 0, 8)
    END_TYPE()

    // Write transfer format configuration for memory address window 0. Configure the bus width of each transfer phase individually, and configure the length or presence of the command prefix, command suffix and dummy/turnaround transfer phases. Only 24-bit addresses are supported. The reset value of the M0_WFMT register is configured to support a basic 02h serial write transfer. However, writes to this window must first be enabled via the XIP_CTRL_WRITABLE_M0 bit, as XIP memory is read-only by default.
    // Reset value: 0x00001000
    BEGIN_TYPE(M0_WFMT_t, uint32_t)
        // Enable double transfer rate (DTR) for write commands: address, suffix and write data phases are active on both edges of SCK. SDO data is launched centre-aligned on each SCK edge, and SDI data is captured on the SCK edge that follows its launch. DTR is implemented by halving the clock rate; SCK has a period of 2 x CLK_DIV throughout the transfer. The prefix and dummy phases are still single transfer rate. If the suffix is quad-width, it must be 0 or 8 bits in length, to ensure an even number of SCK edges.
        ADD_BITFIELD_RW(DTR, 28, 1)
        // Length of dummy phase between command suffix and data phase, in units of 4 bits. (i.e. 1 cycle for quad width, 2 for dual, 4 for single)
        ADD_BITFIELD_RW(DUMMY_LEN, 16, 3)
        // Length of post-address command suffix, in units of 4 bits. (i.e. 1 cycle for quad width, 2 for dual, 4 for single) Only values of 0 and 8 bits are supported.
        ADD_BITFIELD_RW(SUFFIX_LEN, 14, 2)
        // Length of command prefix, in units of 8 bits. (i.e. 2 cycles for quad width, 4 for dual, 8 for single)
        ADD_BITFIELD_RW(PREFIX_LEN, 12, 1)
        // The width used for the data transfer
        ADD_BITFIELD_RW(DATA_WIDTH, 8, 2)
        // The width used for the dummy phase, if any. If width is single, SD0/MOSI is held asserted low during the dummy phase, and SD1...SD3 are tristated. If width is dual/quad, all IOs are tristated during the dummy phase.
        ADD_BITFIELD_RW(DUMMY_WIDTH, 6, 2)
        // The width used for the post-address command suffix, if any
        ADD_BITFIELD_RW(SUFFIX_WIDTH, 4, 2)
        // The transfer width used for the address. The address phase always transfers 24 bits in total.
        ADD_BITFIELD_RW(ADDR_WIDTH, 2, 2)
        // The transfer width used for the command prefix, if any
        ADD_BITFIELD_RW(PREFIX_WIDTH, 0, 2)
    END_TYPE()

    // No dummy phase
    static const uint32_t M0_WFMT_DUMMY_LEN__NONE = 0;
    // 4 dummy bits
    static const uint32_t M0_WFMT_DUMMY_LEN__4 = 1;
    // 8 dummy bits
    static const uint32_t M0_WFMT_DUMMY_LEN__8 = 2;
    // 12 dummy bits
    static const uint32_t M0_WFMT_DUMMY_LEN__12 = 3;
    // 16 dummy bits
    static const uint32_t M0_WFMT_DUMMY_LEN__16 = 4;
    // 20 dummy bits
    static const uint32_t M0_WFMT_DUMMY_LEN__20 = 5;
    // 24 dummy bits
    static const uint32_t M0_WFMT_DUMMY_LEN__24 = 6;
    // 28 dummy bits
    static const uint32_t M0_WFMT_DUMMY_LEN__28 = 7;
    // No suffix
    static const uint32_t M0_WFMT_SUFFIX_LEN__NONE = 0;
    // 8-bit suffix
    static const uint32_t M0_WFMT_SUFFIX_LEN__8 = 2;
    // No prefix
    static const uint32_t M0_WFMT_PREFIX_LEN__NONE = 0;
    // 8-bit prefix
    static const uint32_t M0_WFMT_PREFIX_LEN__8 = 1;
    // Single width
    static const uint32_t M0_WFMT_DATA_WIDTH__S = 0;
    // Dual width
    static const uint32_t M0_WFMT_DATA_WIDTH__D = 1;
    // Quad width
    static const uint32_t M0_WFMT_DATA_WIDTH__Q = 2;
    // Single width
    static const uint32_t M0_WFMT_DUMMY_WIDTH__S = 0;
    // Dual width
    static const uint32_t M0_WFMT_DUMMY_WIDTH__D = 1;
    // Quad width
    static const uint32_t M0_WFMT_DUMMY_WIDTH__Q = 2;
    // Single width
    static const uint32_t M0_WFMT_SUFFIX_WIDTH__S = 0;
    // Dual width
    static const uint32_t M0_WFMT_SUFFIX_WIDTH__D = 1;
    // Quad width
    static const uint32_t M0_WFMT_SUFFIX_WIDTH__Q = 2;
    // Single width
    static const uint32_t M0_WFMT_ADDR_WIDTH__S = 0;
    // Dual width
    static const uint32_t M0_WFMT_ADDR_WIDTH__D = 1;
    // Quad width
    static const uint32_t M0_WFMT_ADDR_WIDTH__Q = 2;
    // Single width
    static const uint32_t M0_WFMT_PREFIX_WIDTH__S = 0;
    // Dual width
    static const uint32_t M0_WFMT_PREFIX_WIDTH__D = 1;
    // Quad width
    static const uint32_t M0_WFMT_PREFIX_WIDTH__Q = 2;

    // Command constants used for writes to memory address window 0. The reset value of the M0_WCMD register is configured to support a basic 02h serial write transfer with no additional configuration.
    // Reset value: 0x0000a002
    BEGIN_TYPE(M0_WCMD_t, uint32_t)
        // The command suffix bits following the address, if Mx_WFMT_SUFFIX_LEN is nonzero.
        ADD_BITFIELD_RW(SUFFIX, 8, 8)
        // The command prefix bits to prepend on each new transfer, if Mx_WFMT_PREFIX_LEN is nonzero.
        ADD_BITFIELD_RW(PREFIX, 0, 8)
    END_TYPE()

    // Timing configuration register for memory address window 1.
    // Reset value: 0x40000004
    BEGIN_TYPE(M1_TIMING_t, uint32_t)
        // Chip select cooldown period. When a memory transfer finishes, the chip select remains asserted for 64 x COOLDOWN system clock cycles, plus half an SCK clock period (rounded up for odd SCK divisors). After this cooldown expires, the chip select is always deasserted to save power. If the next memory access arrives within the cooldown period, the QMI may be able to append more SCK cycles to the currently ongoing SPI transfer, rather than starting a new transfer. This reduces access latency and increases bus throughput. Specifically, the next access must be in the same direction (read/write), access the same memory window (chip select 0/1), and follow sequentially the address of the last transfer. If any of these are false, the new access will first deassert the chip select, then begin a new transfer. If COOLDOWN is 0, the address alignment configured by PAGEBREAK has been reached, or the total chip select assertion limit MAX_SELECT has been reached, the cooldown period is skipped, and the chip select will always be deasserted one half SCK period after the transfer finishes.
        ADD_BITFIELD_RW(COOLDOWN, 30, 2)
        // When page break is enabled, chip select will automatically deassert when crossing certain power-of-2-aligned address boundaries. The next access will always begin a new read/write SPI burst, even if the address of the next access follows in sequence with the last access before the page boundary. Some flash and PSRAM devices forbid crossing page boundaries with a single read/write transfer, or restrict the operating frequency for transfers that do cross page a boundary. This option allows the QMI to safely support those devices. This field has no effect when COOLDOWN is disabled.
        ADD_BITFIELD_RW(PAGEBREAK, 28, 2)
        // Add up to one additional system clock cycle of setup between chip select assertion and the first rising edge of SCK. The default setup time is one half SCK period, which is usually sufficient except for very high SCK frequencies with some flash devices.
        ADD_BITFIELD_RW(SELECT_SETUP, 25, 1)
        // Add up to three additional system clock cycles of active hold between the last falling edge of SCK and the deassertion of this window's chip select. The default hold time is one system clock cycle. Note that flash datasheets usually give chip select active hold time from the last *rising* edge of SCK, and so even zero hold from the last falling edge would be safe. Note that this is a minimum hold time guaranteed by the QMI: the actual chip select active hold may be slightly longer for read transfers with low clock divisors and/or high sample delays. Specifically, if the point two cycles after the last RX data sample is later than the last SCK falling edge, then the hold time is measured from *this* point. Note also that, in case the final SCK pulse is masked to save energy (true for non-DTR reads when COOLDOWN is disabled or PAGE_BREAK is reached), all of QMI's timing logic behaves as though the clock pulse were still present. The SELECT_HOLD time is applied from the point where the last SCK falling edge would be if the clock pulse were not masked.
        ADD_BITFIELD_RW(SELECT_HOLD, 23, 2)
        // Enforce a maximum assertion duration for this window's chip select, in units of 64 system clock cycles. If 0, the QMI is permitted to keep the chip select asserted indefinitely when servicing sequential memory accesses (see COOLDOWN). This feature is required to meet timing constraints of PSRAM devices, which specify a maximum chip select assertion so they can perform DRAM refresh cycles. See also MIN_DESELECT, which can enforce a minimum deselect time. If a memory access is in progress at the time MAX_SELECT is reached, the QMI will wait for the access to complete before deasserting the chip select. This additional time must be accounted for to calculate a safe MAX_SELECT value. In the worst case, this may be a fully-formed serial transfer, including command prefix and address, with a data payload as large as one cache line.
        ADD_BITFIELD_RW(MAX_SELECT, 17, 6)
        // After this window's chip select is deasserted, it remains deasserted for half an SCK cycle (rounded up to an integer number of system clock cycles), plus MIN_DESELECT additional system clock cycles, before the QMI reasserts either chip select pin. Nonzero values may be required for PSRAM devices which enforce a longer minimum CS deselect time, so that they can perform internal DRAM refresh cycles whilst deselected.
        ADD_BITFIELD_RW(MIN_DESELECT, 12, 5)
        // Delay the read data sample timing, in units of one half of a system clock cycle. (Not necessarily half of an SCK cycle.) An RXDELAY of 0 means the sample is captured at the SDI input registers simultaneously with the rising edge of SCK launched from the SCK output register. At higher SCK frequencies, RXDELAY may need to be increased to account for the round trip delay of the pads, and the clock-to-Q delay of the QSPI memory device.
        ADD_BITFIELD_RW(RXDELAY, 8, 3)
        // Clock divisor. Odd and even divisors are supported. Defines the SCK clock period in units of 1 system clock cycle. Divisors 1..255 are encoded directly, and a divisor of 256 is encoded with a value of CLKDIV=0. The clock divisor can be changed on-the-fly, even when the QMI is currently accessing memory in this address window. All other parameters must only be changed when the QMI is idle. If software is increasing CLKDIV in anticipation of an increase in the system clock frequency, a dummy access to either memory window (and appropriate processor barriers/fences) must be inserted after the Mx_TIMING write to ensure the SCK divisor change is in effect _before_ the system clock is changed.
        ADD_BITFIELD_RW(CLKDIV, 0, 8)
    END_TYPE()

    // No page boundary is enforced
    static const uint32_t M1_TIMING_PAGEBREAK__NONE = 0;
    // Break bursts crossing a 256-byte page boundary
    static const uint32_t M1_TIMING_PAGEBREAK__256 = 1;
    // Break bursts crossing a 1024-byte quad-page boundary
    static const uint32_t M1_TIMING_PAGEBREAK__1024 = 2;
    // Break bursts crossing a 4096-byte sector boundary
    static const uint32_t M1_TIMING_PAGEBREAK__4096 = 3;

    // Read transfer format configuration for memory address window 1. Configure the bus width of each transfer phase individually, and configure the length or presence of the command prefix, command suffix and dummy/turnaround transfer phases. Only 24-bit addresses are supported. The reset value of the M1_RFMT register is configured to support a basic 03h serial read transfer with no additional configuration.
    // Reset value: 0x00001000
    BEGIN_TYPE(M1_RFMT_t, uint32_t)
        // Enable double transfer rate (DTR) for read commands: address, suffix and read data phases are active on both edges of SCK. SDO data is launched centre-aligned on each SCK edge, and SDI data is captured on the SCK edge that follows its launch. DTR is implemented by halving the clock rate; SCK has a period of 2 x CLK_DIV throughout the transfer. The prefix and dummy phases are still single transfer rate. If the suffix is quad-width, it must be 0 or 8 bits in length, to ensure an even number of SCK edges.
        ADD_BITFIELD_RW(DTR, 28, 1)
        // Length of dummy phase between command suffix and data phase, in units of 4 bits. (i.e. 1 cycle for quad width, 2 for dual, 4 for single)
        ADD_BITFIELD_RW(DUMMY_LEN, 16, 3)
        // Length of post-address command suffix, in units of 4 bits. (i.e. 1 cycle for quad width, 2 for dual, 4 for single) Only values of 0 and 8 bits are supported.
        ADD_BITFIELD_RW(SUFFIX_LEN, 14, 2)
        // Length of command prefix, in units of 8 bits. (i.e. 2 cycles for quad width, 4 for dual, 8 for single)
        ADD_BITFIELD_RW(PREFIX_LEN, 12, 1)
        // The width used for the data transfer
        ADD_BITFIELD_RW(DATA_WIDTH, 8, 2)
        // The width used for the dummy phase, if any. If width is single, SD0/MOSI is held asserted low during the dummy phase, and SD1...SD3 are tristated. If width is dual/quad, all IOs are tristated during the dummy phase.
        ADD_BITFIELD_RW(DUMMY_WIDTH, 6, 2)
        // The width used for the post-address command suffix, if any
        ADD_BITFIELD_RW(SUFFIX_WIDTH, 4, 2)
        // The transfer width used for the address. The address phase always transfers 24 bits in total.
        ADD_BITFIELD_RW(ADDR_WIDTH, 2, 2)
        // The transfer width used for the command prefix, if any
        ADD_BITFIELD_RW(PREFIX_WIDTH, 0, 2)
    END_TYPE()

    // No dummy phase
    static const uint32_t M1_RFMT_DUMMY_LEN__NONE = 0;
    // 4 dummy bits
    static const uint32_t M1_RFMT_DUMMY_LEN__4 = 1;
    // 8 dummy bits
    static const uint32_t M1_RFMT_DUMMY_LEN__8 = 2;
    // 12 dummy bits
    static const uint32_t M1_RFMT_DUMMY_LEN__12 = 3;
    // 16 dummy bits
    static const uint32_t M1_RFMT_DUMMY_LEN__16 = 4;
    // 20 dummy bits
    static const uint32_t M1_RFMT_DUMMY_LEN__20 = 5;
    // 24 dummy bits
    static const uint32_t M1_RFMT_DUMMY_LEN__24 = 6;
    // 28 dummy bits
    static const uint32_t M1_RFMT_DUMMY_LEN__28 = 7;
    // No suffix
    static const uint32_t M1_RFMT_SUFFIX_LEN__NONE = 0;
    // 8-bit suffix
    static const uint32_t M1_RFMT_SUFFIX_LEN__8 = 2;
    // No prefix
    static const uint32_t M1_RFMT_PREFIX_LEN__NONE = 0;
    // 8-bit prefix
    static const uint32_t M1_RFMT_PREFIX_LEN__8 = 1;
    // Single width
    static const uint32_t M1_RFMT_DATA_WIDTH__S = 0;
    // Dual width
    static const uint32_t M1_RFMT_DATA_WIDTH__D = 1;
    // Quad width
    static const uint32_t M1_RFMT_DATA_WIDTH__Q = 2;
    // Single width
    static const uint32_t M1_RFMT_DUMMY_WIDTH__S = 0;
    // Dual width
    static const uint32_t M1_RFMT_DUMMY_WIDTH__D = 1;
    // Quad width
    static const uint32_t M1_RFMT_DUMMY_WIDTH__Q = 2;
    // Single width
    static const uint32_t M1_RFMT_SUFFIX_WIDTH__S = 0;
    // Dual width
    static const uint32_t M1_RFMT_SUFFIX_WIDTH__D = 1;
    // Quad width
    static const uint32_t M1_RFMT_SUFFIX_WIDTH__Q = 2;
    // Single width
    static const uint32_t M1_RFMT_ADDR_WIDTH__S = 0;
    // Dual width
    static const uint32_t M1_RFMT_ADDR_WIDTH__D = 1;
    // Quad width
    static const uint32_t M1_RFMT_ADDR_WIDTH__Q = 2;
    // Single width
    static const uint32_t M1_RFMT_PREFIX_WIDTH__S = 0;
    // Dual width
    static const uint32_t M1_RFMT_PREFIX_WIDTH__D = 1;
    // Quad width
    static const uint32_t M1_RFMT_PREFIX_WIDTH__Q = 2;

    // Command constants used for reads from memory address window 1. The reset value of the M1_RCMD register is configured to support a basic 03h serial read transfer with no additional configuration.
    // Reset value: 0x0000a003
    BEGIN_TYPE(M1_RCMD_t, uint32_t)
        // The command suffix bits following the address, if Mx_RFMT_SUFFIX_LEN is nonzero.
        ADD_BITFIELD_RW(SUFFIX, 8, 8)
        // The command prefix bits to prepend on each new transfer, if Mx_RFMT_PREFIX_LEN is nonzero.
        ADD_BITFIELD_RW(PREFIX, 0, 8)
    END_TYPE()

    // Write transfer format configuration for memory address window 1. Configure the bus width of each transfer phase individually, and configure the length or presence of the command prefix, command suffix and dummy/turnaround transfer phases. Only 24-bit addresses are supported. The reset value of the M1_WFMT register is configured to support a basic 02h serial write transfer. However, writes to this window must first be enabled via the XIP_CTRL_WRITABLE_M1 bit, as XIP memory is read-only by default.
    // Reset value: 0x00001000
    BEGIN_TYPE(M1_WFMT_t, uint32_t)
        // Enable double transfer rate (DTR) for write commands: address, suffix and write data phases are active on both edges of SCK. SDO data is launched centre-aligned on each SCK edge, and SDI data is captured on the SCK edge that follows its launch. DTR is implemented by halving the clock rate; SCK has a period of 2 x CLK_DIV throughout the transfer. The prefix and dummy phases are still single transfer rate. If the suffix is quad-width, it must be 0 or 8 bits in length, to ensure an even number of SCK edges.
        ADD_BITFIELD_RW(DTR, 28, 1)
        // Length of dummy phase between command suffix and data phase, in units of 4 bits. (i.e. 1 cycle for quad width, 2 for dual, 4 for single)
        ADD_BITFIELD_RW(DUMMY_LEN, 16, 3)
        // Length of post-address command suffix, in units of 4 bits. (i.e. 1 cycle for quad width, 2 for dual, 4 for single) Only values of 0 and 8 bits are supported.
        ADD_BITFIELD_RW(SUFFIX_LEN, 14, 2)
        // Length of command prefix, in units of 8 bits. (i.e. 2 cycles for quad width, 4 for dual, 8 for single)
        ADD_BITFIELD_RW(PREFIX_LEN, 12, 1)
        // The width used for the data transfer
        ADD_BITFIELD_RW(DATA_WIDTH, 8, 2)
        // The width used for the dummy phase, if any. If width is single, SD0/MOSI is held asserted low during the dummy phase, and SD1...SD3 are tristated. If width is dual/quad, all IOs are tristated during the dummy phase.
        ADD_BITFIELD_RW(DUMMY_WIDTH, 6, 2)
        // The width used for the post-address command suffix, if any
        ADD_BITFIELD_RW(SUFFIX_WIDTH, 4, 2)
        // The transfer width used for the address. The address phase always transfers 24 bits in total.
        ADD_BITFIELD_RW(ADDR_WIDTH, 2, 2)
        // The transfer width used for the command prefix, if any
        ADD_BITFIELD_RW(PREFIX_WIDTH, 0, 2)
    END_TYPE()

    // No dummy phase
    static const uint32_t M1_WFMT_DUMMY_LEN__NONE = 0;
    // 4 dummy bits
    static const uint32_t M1_WFMT_DUMMY_LEN__4 = 1;
    // 8 dummy bits
    static const uint32_t M1_WFMT_DUMMY_LEN__8 = 2;
    // 12 dummy bits
    static const uint32_t M1_WFMT_DUMMY_LEN__12 = 3;
    // 16 dummy bits
    static const uint32_t M1_WFMT_DUMMY_LEN__16 = 4;
    // 20 dummy bits
    static const uint32_t M1_WFMT_DUMMY_LEN__20 = 5;
    // 24 dummy bits
    static const uint32_t M1_WFMT_DUMMY_LEN__24 = 6;
    // 28 dummy bits
    static const uint32_t M1_WFMT_DUMMY_LEN__28 = 7;
    // No suffix
    static const uint32_t M1_WFMT_SUFFIX_LEN__NONE = 0;
    // 8-bit suffix
    static const uint32_t M1_WFMT_SUFFIX_LEN__8 = 2;
    // No prefix
    static const uint32_t M1_WFMT_PREFIX_LEN__NONE = 0;
    // 8-bit prefix
    static const uint32_t M1_WFMT_PREFIX_LEN__8 = 1;
    // Single width
    static const uint32_t M1_WFMT_DATA_WIDTH__S = 0;
    // Dual width
    static const uint32_t M1_WFMT_DATA_WIDTH__D = 1;
    // Quad width
    static const uint32_t M1_WFMT_DATA_WIDTH__Q = 2;
    // Single width
    static const uint32_t M1_WFMT_DUMMY_WIDTH__S = 0;
    // Dual width
    static const uint32_t M1_WFMT_DUMMY_WIDTH__D = 1;
    // Quad width
    static const uint32_t M1_WFMT_DUMMY_WIDTH__Q = 2;
    // Single width
    static const uint32_t M1_WFMT_SUFFIX_WIDTH__S = 0;
    // Dual width
    static const uint32_t M1_WFMT_SUFFIX_WIDTH__D = 1;
    // Quad width
    static const uint32_t M1_WFMT_SUFFIX_WIDTH__Q = 2;
    // Single width
    static const uint32_t M1_WFMT_ADDR_WIDTH__S = 0;
    // Dual width
    static const uint32_t M1_WFMT_ADDR_WIDTH__D = 1;
    // Quad width
    static const uint32_t M1_WFMT_ADDR_WIDTH__Q = 2;
    // Single width
    static const uint32_t M1_WFMT_PREFIX_WIDTH__S = 0;
    // Dual width
    static const uint32_t M1_WFMT_PREFIX_WIDTH__D = 1;
    // Quad width
    static const uint32_t M1_WFMT_PREFIX_WIDTH__Q = 2;

    // Command constants used for writes to memory address window 1. The reset value of the M1_WCMD register is configured to support a basic 02h serial write transfer with no additional configuration.
    // Reset value: 0x0000a002
    BEGIN_TYPE(M1_WCMD_t, uint32_t)
        // The command suffix bits following the address, if Mx_WFMT_SUFFIX_LEN is nonzero.
        ADD_BITFIELD_RW(SUFFIX, 8, 8)
        // The command prefix bits to prepend on each new transfer, if Mx_WFMT_PREFIX_LEN is nonzero.
        ADD_BITFIELD_RW(PREFIX, 0, 8)
    END_TYPE()

    // Configure address translation for XIP virtual addresses 0x000000 through 0x3fffff (a 4 MiB window starting at +0 MiB). Address translation allows a program image to be executed in place at multiple physical flash addresses (for example, a double-buffered flash image for over-the-air updates), without the overhead of position-independent code. At reset, the address translation registers are initialised to an identity mapping, so that they can be ignored if address translation is not required. Note that the XIP cache is fully virtually addressed, so a cache flush is required after changing the address translation.
    // Reset value: 0x04000000
    BEGIN_TYPE(ATRANS0_t, uint32_t)
        // Translation aperture size for this virtual address range, in units of 4 kiB (one flash sector). Bits 21:12 of the virtual address are compared to SIZE. Offsets greater than SIZE return a bus error, and do not cause a QSPI access.
        ADD_BITFIELD_RW(SIZE, 16, 11)
        // Physical address base for this virtual address range, in units of 4 kiB (one flash sector). Taking a 24-bit virtual address, firstly bits 23:22 (the two MSBs) are masked to zero, and then BASE is added to bits 23:12 (the upper 12 bits) to form the physical address. Translation wraps on a 16 MiB boundary.
        ADD_BITFIELD_RW(BASE, 0, 12)
    END_TYPE()

    // Configure address translation for XIP virtual addresses 0x400000 through 0x7fffff (a 4 MiB window starting at +4 MiB). Address translation allows a program image to be executed in place at multiple physical flash addresses (for example, a double-buffered flash image for over-the-air updates), without the overhead of position-independent code. At reset, the address translation registers are initialised to an identity mapping, so that they can be ignored if address translation is not required. Note that the XIP cache is fully virtually addressed, so a cache flush is required after changing the address translation.
    // Reset value: 0x04000400
    BEGIN_TYPE(ATRANS1_t, uint32_t)
        // Translation aperture size for this virtual address range, in units of 4 kiB (one flash sector). Bits 21:12 of the virtual address are compared to SIZE. Offsets greater than SIZE return a bus error, and do not cause a QSPI access.
        ADD_BITFIELD_RW(SIZE, 16, 11)
        // Physical address base for this virtual address range, in units of 4 kiB (one flash sector). Taking a 24-bit virtual address, firstly bits 23:22 (the two MSBs) are masked to zero, and then BASE is added to bits 23:12 (the upper 12 bits) to form the physical address. Translation wraps on a 16 MiB boundary.
        ADD_BITFIELD_RW(BASE, 0, 12)
    END_TYPE()

    // Configure address translation for XIP virtual addresses 0x800000 through 0xbfffff (a 4 MiB window starting at +8 MiB). Address translation allows a program image to be executed in place at multiple physical flash addresses (for example, a double-buffered flash image for over-the-air updates), without the overhead of position-independent code. At reset, the address translation registers are initialised to an identity mapping, so that they can be ignored if address translation is not required. Note that the XIP cache is fully virtually addressed, so a cache flush is required after changing the address translation.
    // Reset value: 0x04000800
    BEGIN_TYPE(ATRANS2_t, uint32_t)
        // Translation aperture size for this virtual address range, in units of 4 kiB (one flash sector). Bits 21:12 of the virtual address are compared to SIZE. Offsets greater than SIZE return a bus error, and do not cause a QSPI access.
        ADD_BITFIELD_RW(SIZE, 16, 11)
        // Physical address base for this virtual address range, in units of 4 kiB (one flash sector). Taking a 24-bit virtual address, firstly bits 23:22 (the two MSBs) are masked to zero, and then BASE is added to bits 23:12 (the upper 12 bits) to form the physical address. Translation wraps on a 16 MiB boundary.
        ADD_BITFIELD_RW(BASE, 0, 12)
    END_TYPE()

    // Configure address translation for XIP virtual addresses 0xc00000 through 0xffffff (a 4 MiB window starting at +12 MiB). Address translation allows a program image to be executed in place at multiple physical flash addresses (for example, a double-buffered flash image for over-the-air updates), without the overhead of position-independent code. At reset, the address translation registers are initialised to an identity mapping, so that they can be ignored if address translation is not required. Note that the XIP cache is fully virtually addressed, so a cache flush is required after changing the address translation.
    // Reset value: 0x04000c00
    BEGIN_TYPE(ATRANS3_t, uint32_t)
        // Translation aperture size for this virtual address range, in units of 4 kiB (one flash sector). Bits 21:12 of the virtual address are compared to SIZE. Offsets greater than SIZE return a bus error, and do not cause a QSPI access.
        ADD_BITFIELD_RW(SIZE, 16, 11)
        // Physical address base for this virtual address range, in units of 4 kiB (one flash sector). Taking a 24-bit virtual address, firstly bits 23:22 (the two MSBs) are masked to zero, and then BASE is added to bits 23:12 (the upper 12 bits) to form the physical address. Translation wraps on a 16 MiB boundary.
        ADD_BITFIELD_RW(BASE, 0, 12)
    END_TYPE()

    // Configure address translation for XIP virtual addresses 0x1000000 through 0x13fffff (a 4 MiB window starting at +16 MiB). Address translation allows a program image to be executed in place at multiple physical flash addresses (for example, a double-buffered flash image for over-the-air updates), without the overhead of position-independent code. At reset, the address translation registers are initialised to an identity mapping, so that they can be ignored if address translation is not required. Note that the XIP cache is fully virtually addressed, so a cache flush is required after changing the address translation.
    // Reset value: 0x04000000
    BEGIN_TYPE(ATRANS4_t, uint32_t)
        // Translation aperture size for this virtual address range, in units of 4 kiB (one flash sector). Bits 21:12 of the virtual address are compared to SIZE. Offsets greater than SIZE return a bus error, and do not cause a QSPI access.
        ADD_BITFIELD_RW(SIZE, 16, 11)
        // Physical address base for this virtual address range, in units of 4 kiB (one flash sector). Taking a 24-bit virtual address, firstly bits 23:22 (the two MSBs) are masked to zero, and then BASE is added to bits 23:12 (the upper 12 bits) to form the physical address. Translation wraps on a 16 MiB boundary.
        ADD_BITFIELD_RW(BASE, 0, 12)
    END_TYPE()

    // Configure address translation for XIP virtual addresses 0x1400000 through 0x17fffff (a 4 MiB window starting at +20 MiB). Address translation allows a program image to be executed in place at multiple physical flash addresses (for example, a double-buffered flash image for over-the-air updates), without the overhead of position-independent code. At reset, the address translation registers are initialised to an identity mapping, so that they can be ignored if address translation is not required. Note that the XIP cache is fully virtually addressed, so a cache flush is required after changing the address translation.
    // Reset value: 0x04000400
    BEGIN_TYPE(ATRANS5_t, uint32_t)
        // Translation aperture size for this virtual address range, in units of 4 kiB (one flash sector). Bits 21:12 of the virtual address are compared to SIZE. Offsets greater than SIZE return a bus error, and do not cause a QSPI access.
        ADD_BITFIELD_RW(SIZE, 16, 11)
        // Physical address base for this virtual address range, in units of 4 kiB (one flash sector). Taking a 24-bit virtual address, firstly bits 23:22 (the two MSBs) are masked to zero, and then BASE is added to bits 23:12 (the upper 12 bits) to form the physical address. Translation wraps on a 16 MiB boundary.
        ADD_BITFIELD_RW(BASE, 0, 12)
    END_TYPE()

    // Configure address translation for XIP virtual addresses 0x1800000 through 0x1bfffff (a 4 MiB window starting at +24 MiB). Address translation allows a program image to be executed in place at multiple physical flash addresses (for example, a double-buffered flash image for over-the-air updates), without the overhead of position-independent code. At reset, the address translation registers are initialised to an identity mapping, so that they can be ignored if address translation is not required. Note that the XIP cache is fully virtually addressed, so a cache flush is required after changing the address translation.
    // Reset value: 0x04000800
    BEGIN_TYPE(ATRANS6_t, uint32_t)
        // Translation aperture size for this virtual address range, in units of 4 kiB (one flash sector). Bits 21:12 of the virtual address are compared to SIZE. Offsets greater than SIZE return a bus error, and do not cause a QSPI access.
        ADD_BITFIELD_RW(SIZE, 16, 11)
        // Physical address base for this virtual address range, in units of 4 kiB (one flash sector). Taking a 24-bit virtual address, firstly bits 23:22 (the two MSBs) are masked to zero, and then BASE is added to bits 23:12 (the upper 12 bits) to form the physical address. Translation wraps on a 16 MiB boundary.
        ADD_BITFIELD_RW(BASE, 0, 12)
    END_TYPE()

    // Configure address translation for XIP virtual addresses 0x1c00000 through 0x1ffffff (a 4 MiB window starting at +28 MiB). Address translation allows a program image to be executed in place at multiple physical flash addresses (for example, a double-buffered flash image for over-the-air updates), without the overhead of position-independent code. At reset, the address translation registers are initialised to an identity mapping, so that they can be ignored if address translation is not required. Note that the XIP cache is fully virtually addressed, so a cache flush is required after changing the address translation.
    // Reset value: 0x04000c00
    BEGIN_TYPE(ATRANS7_t, uint32_t)
        // Translation aperture size for this virtual address range, in units of 4 kiB (one flash sector). Bits 21:12 of the virtual address are compared to SIZE. Offsets greater than SIZE return a bus error, and do not cause a QSPI access.
        ADD_BITFIELD_RW(SIZE, 16, 11)
        // Physical address base for this virtual address range, in units of 4 kiB (one flash sector). Taking a 24-bit virtual address, firstly bits 23:22 (the two MSBs) are masked to zero, and then BASE is added to bits 23:12 (the upper 12 bits) to form the physical address. Translation wraps on a 16 MiB boundary.
        ADD_BITFIELD_RW(BASE, 0, 12)
    END_TYPE()

    struct QMI_t {
        DIRECT_CSR_t                  DIRECT_CSR;
        DIRECT_TX_t                   DIRECT_TX;
        DIRECT_RX_t                   DIRECT_RX;
        M0_TIMING_t                   M0_TIMING;
        M0_RFMT_t                     M0_RFMT;
        M0_RCMD_t                     M0_RCMD;
        M0_WFMT_t                     M0_WFMT;
        M0_WCMD_t                     M0_WCMD;
        M1_TIMING_t                   M1_TIMING;
        M1_RFMT_t                     M1_RFMT;
        M1_RCMD_t                     M1_RCMD;
        M1_WFMT_t                     M1_WFMT;
        M1_WCMD_t                     M1_WCMD;
        ATRANS0_t                     ATRANS0;
        ATRANS1_t                     ATRANS1;
        ATRANS2_t                     ATRANS2;
        ATRANS3_t                     ATRANS3;
        ATRANS4_t                     ATRANS4;
        ATRANS5_t                     ATRANS5;
        ATRANS6_t                     ATRANS6;
        ATRANS7_t                     ATRANS7;
    };

    static QMI_t & QMI = (*(QMI_t *)0x400d0000);
    static QMI_t & QMI_XOR = (*(QMI_t *)0x400d1000);
    static QMI_t & QMI_SET = (*(QMI_t *)0x400d2000);
    static QMI_t & QMI_CLR = (*(QMI_t *)0x400d3000);

} // _QMI_

// QSPI flash execute-in-place block
namespace _XIP_CTRL_  {

    // Cache control register. Read-only from a Non-secure context.
    // Reset value: 0x00000083
    BEGIN_TYPE(CTRL_t, uint32_t)
        // If 1, enable writes to XIP memory window 1 (addresses 0x11000000 through 0x11ffffff, and their uncached mirrors). If 0, this region is read-only. XIP memory is *read-only by default*. This bit must be set to enable writes if a RAM device is attached on QSPI chip select 1. The default read-only behaviour avoids two issues with writing to a read-only QSPI device (e.g. flash). First, a write will initially appear to succeed due to caching, but the data will eventually be lost when the written line is evicted, causing unpredictable behaviour. Second, when a written line is evicted, it will cause a write command to be issued to the flash, which can break the flash out of its continuous read mode. After this point, flash reads will return garbage. This is a security concern, as it allows Non-secure software to break Secure flash reads if it has permission to write to any flash address. Note the read-only behaviour is implemented by downgrading writes to reads, so writes will still cause allocation of an address, but have no other effect.
        ADD_BITFIELD_RW(WRITABLE_M1, 11, 1)
        // If 1, enable writes to XIP memory window 0 (addresses 0x10000000 through 0x10ffffff, and their uncached mirrors). If 0, this region is read-only. XIP memory is *read-only by default*. This bit must be set to enable writes if a RAM device is attached on QSPI chip select 0. The default read-only behaviour avoids two issues with writing to a read-only QSPI device (e.g. flash). First, a write will initially appear to succeed due to caching, but the data will eventually be lost when the written line is evicted, causing unpredictable behaviour. Second, when a written line is evicted, it will cause a write command to be issued to the flash, which can break the flash out of its continuous read mode. After this point, flash reads will return garbage. This is a security concern, as it allows Non-secure software to break Secure flash reads if it has permission to write to any flash address. Note the read-only behaviour is implemented by downgrading writes to reads, so writes will still cause allocation of an address, but have no other effect.
        ADD_BITFIELD_RW(WRITABLE_M0, 10, 1)
        // When 1, route all cached+Secure accesses to way 0 of the cache, and route all cached+Non-secure accesses to way 1 of the cache. This partitions the cache into two half-sized direct-mapped regions, such that Non-secure code can not observe cache line state changes caused by Secure execution. A full cache flush is required when changing the value of SPLIT_WAYS. The flush should be performed whilst SPLIT_WAYS is 0, so that both cache ways are accessible for invalidation.
        ADD_BITFIELD_RW(SPLIT_WAYS, 9, 1)
        // When 0, Non-secure accesses to the cache maintenance address window (addr[27] == 1, addr[26] == 0) will generate a bus error. When 1, Non-secure accesses can perform cache maintenance operations by writing to the cache maintenance address window. Cache maintenance operations may be used to corrupt Secure data by invalidating cache lines inappropriately, or map Secure content into a Non-secure region by pinning cache lines. Therefore this bit should generally be set to 0, unless Secure code is not using the cache. Care should also be taken to clear the cache data memory and tag memory before granting maintenance operations to Non-secure code.
        ADD_BITFIELD_RW(MAINT_NONSEC, 8, 1)
        // When 1, Non-secure accesses to the uncached, untranslated window (addr[27:26] == 3) will generate a bus error.
        ADD_BITFIELD_RW(NO_UNTRANSLATED_NONSEC, 7, 1)
        // When 1, Secure accesses to the uncached, untranslated window (addr[27:26] == 3) will generate a bus error.
        ADD_BITFIELD_RW(NO_UNTRANSLATED_SEC, 6, 1)
        // When 1, Non-secure accesses to the uncached window (addr[27:26] == 1) will generate a bus error. This may reduce the number of SAU/MPU/PMP regions required to protect flash contents. Note this does not disable access to the uncached, untranslated window -- see NO_UNTRANSLATED_SEC.
        ADD_BITFIELD_RW(NO_UNCACHED_NONSEC, 5, 1)
        // When 1, Secure accesses to the uncached window (addr[27:26] == 1) will generate a bus error. This may reduce the number of SAU/MPU/PMP regions required to protect flash contents. Note this does not disable access to the uncached, untranslated window -- see NO_UNTRANSLATED_SEC.
        ADD_BITFIELD_RW(NO_UNCACHED_SEC, 4, 1)
        // When 1, the cache memories are powered down. They retain state, but can not be accessed. This reduces static power dissipation. Writing 1 to this bit forces CTRL_EN_SECURE and CTRL_EN_NONSECURE to 0, i.e. the cache cannot be enabled when powered down.
        ADD_BITFIELD_RW(POWER_DOWN, 3, 1)
        // When 1, enable the cache for Non-secure accesses. When enabled, Non-secure XIP accesses to the cached (addr[26] == 0) window will query the cache, and QSPI accesses are performed only if the requested data is not present. When disabled, Secure access ignore the cache contents, and always access the QSPI interface. Accesses to the uncached (addr[26] == 1) window will never query the cache, irrespective of this bit.
        ADD_BITFIELD_RW(EN_NONSECURE, 1, 1)
        // When 1, enable the cache for Secure accesses. When enabled, Secure XIP accesses to the cached (addr[26] == 0) window will query the cache, and QSPI accesses are performed only if the requested data is not present. When disabled, Secure access ignore the cache contents, and always access the QSPI interface. Accesses to the uncached (addr[26] == 1) window will never query the cache, irrespective of this bit. There is no cache-as-SRAM address window. Cache lines are allocated for SRAM-like use by individually pinning them, and keeping the cache enabled.
        ADD_BITFIELD_RW(EN_SECURE, 0, 1)
    END_TYPE()

    // Reset value: 0x00000002
    BEGIN_TYPE(STAT_t, uint32_t)
        // When 1, indicates the XIP streaming FIFO is completely full.  The streaming FIFO is 2 entries deep, so the full and empty  flag allow its level to be ascertained.
        ADD_BITFIELD_RO(FIFO_FULL, 2, 1)
        // When 1, indicates the XIP streaming FIFO is completely empty.
        ADD_BITFIELD_RO(FIFO_EMPTY, 1, 1)
    END_TYPE()

    // Cache Hit counter
    // Reset value: 0x00000000
    BEGIN_TYPE(CTR_HIT_t, uint32_t)
        // A 32 bit saturating counter that increments upon each cache hit,  i.e. when an XIP access is serviced directly from cached data.  Write any value to clear.
        ADD_BITFIELD_RW(CTR_HIT, 0, 32)
    END_TYPE()

    // Cache Access counter
    // Reset value: 0x00000000
    BEGIN_TYPE(CTR_ACC_t, uint32_t)
        // A 32 bit saturating counter that increments upon each XIP access,  whether the cache is hit or not. This includes noncacheable accesses.  Write any value to clear.
        ADD_BITFIELD_RW(CTR_ACC, 0, 32)
    END_TYPE()

    // FIFO stream address
    // Reset value: 0x00000000
    BEGIN_TYPE(STREAM_ADDR_t, uint32_t)
        // The address of the next word to be streamed from flash to the streaming FIFO.  Increments automatically after each flash access.  Write the initial access address here before starting a streaming read.
        ADD_BITFIELD_RW(STREAM_ADDR, 2, 30)
    END_TYPE()

    // FIFO stream control
    // Reset value: 0x00000000
    BEGIN_TYPE(STREAM_CTR_t, uint32_t)
        // Write a nonzero value to start a streaming read. This will then  progress in the background, using flash idle cycles to transfer  a linear data block from flash to the streaming FIFO.  Decrements automatically (1 at a time) as the stream  progresses, and halts on reaching 0.  Write 0 to halt an in-progress stream, and discard any in-flight  read, so that a new stream can immediately be started (after  draining the FIFO and reinitialising STREAM_ADDR)
        ADD_BITFIELD_RW(STREAM_CTR, 0, 22)
    END_TYPE()

    // FIFO stream data
    // Reset value: 0x00000000
    BEGIN_TYPE(STREAM_FIFO_t, uint32_t)
        // Streamed data is buffered here, for retrieval by the system DMA.  This FIFO can also be accessed via the XIP_AUX slave, to avoid exposing  the DMA to bus stalls caused by other XIP traffic.
        ADD_BITFIELD_RO(STREAM_FIFO, 0, 32)
    END_TYPE()

    struct XIP_CTRL_t {
        CTRL_t                        CTRL;
        uint32_t                      reserved0;
        STAT_t                        STAT;
        CTR_HIT_t                     CTR_HIT;
        CTR_ACC_t                     CTR_ACC;
        STREAM_ADDR_t                 STREAM_ADDR;
        STREAM_CTR_t                  STREAM_CTR;
        STREAM_FIFO_t                 STREAM_FIFO;
    };

    static XIP_CTRL_t & XIP_CTRL = (*(XIP_CTRL_t *)0x400c8000);
    static XIP_CTRL_t & XIP_CTRL_XOR = (*(XIP_CTRL_t *)0x400c9000);
    static XIP_CTRL_t & XIP_CTRL_SET = (*(XIP_CTRL_t *)0x400ca000);
    static XIP_CTRL_t & XIP_CTRL_CLR = (*(XIP_CTRL_t *)0x400cb000);

} // _XIP_CTRL_

// Auxiliary DMA access to XIP FIFOs, via fast AHB bus access
namespace _XIP_AUX_  {

    // Read the XIP stream FIFO (fast bus access to XIP_CTRL_STREAM_FIFO)
    // Reset value: 0x00000000
    BEGIN_TYPE(STREAM_t, uint32_t)
        ADD_BITFIELD_RO(STREAM, 0, 32)
    END_TYPE()

    // Write to the QMI direct-mode TX FIFO (fast bus access to QMI_DIRECT_TX)
    // Reset value: 0x00000000
    BEGIN_TYPE(QMI_DIRECT_TX_t, uint32_t)
        // Inhibit the RX FIFO push that would correspond to this TX FIFO entry. Useful to avoid garbage appearing in the RX FIFO when pushing the command at the beginning of a SPI transfer.
        ADD_BITFIELD_WO(NOPUSH, 20, 1)
        // Output enable (active-high). For single width (SPI), this field is ignored, and SD0 is always set to output, with SD1 always set to input. For dual and quad width (DSPI/QSPI), this sets whether the relevant SDx pads are set to output whilst transferring this FIFO record. In this case the command/address should have OE set, and the data transfer should have OE set or clear depending on the direction of the transfer.
        ADD_BITFIELD_WO(OE, 19, 1)
        // Data width. If 0, hardware will transmit the 8 LSBs of the DIRECT_TX DATA field, and return an 8-bit value in the 8 LSBs of DIRECT_RX. If 1, the full 16-bit width is used. 8-bit and 16-bit transfers can be mixed freely.
        ADD_BITFIELD_WO(DWIDTH, 18, 1)
        // Configure whether this FIFO record is transferred with single/dual/quad interface width (0/1/2). Different widths can be mixed freely.
        ADD_BITFIELD_WO(IWIDTH, 16, 2)
        // Data pushed here will be clocked out falling edges of SCK (or before the very first rising edge of SCK, if this is the first pulse). For each byte clocked out, the interface will simultaneously sample one byte, on rising edges of SCK, and push this to the DIRECT_RX FIFO. For 16-bit data, the least-significant byte is transmitted first.
        ADD_BITFIELD_WO(DATA, 0, 16)
    END_TYPE()

    // Single width
    static const uint32_t QMI_DIRECT_TX_IWIDTH__S = 0;
    // Dual width
    static const uint32_t QMI_DIRECT_TX_IWIDTH__D = 1;
    // Quad width
    static const uint32_t QMI_DIRECT_TX_IWIDTH__Q = 2;

    // Read from the QMI direct-mode RX FIFO (fast bus access to QMI_DIRECT_RX)
    // Reset value: 0x00000000
    BEGIN_TYPE(QMI_DIRECT_RX_t, uint32_t)
        // With each byte clocked out on the serial interface, one byte will simultaneously be clocked in, and will appear in this FIFO. The serial interface will stall when this FIFO is full, to avoid dropping data. When 16-bit data is pushed into the TX FIFO, the corresponding RX FIFO push will also contain 16 bits of data. The least-significant byte is the first one received.
        ADD_BITFIELD_RO(QMI_DIRECT_RX, 0, 16)
    END_TYPE()

    struct XIP_AUX_t {
        STREAM_t                      STREAM;
        QMI_DIRECT_TX_t               QMI_DIRECT_TX;
        QMI_DIRECT_RX_t               QMI_DIRECT_RX;
    };

    static XIP_AUX_t & XIP_AUX = (*(XIP_AUX_t *)0x50500000);
    static XIP_AUX_t & XIP_AUX_XOR = (*(XIP_AUX_t *)0x50501000);
    static XIP_AUX_t & XIP_AUX_SET = (*(XIP_AUX_t *)0x50502000);
    static XIP_AUX_t & XIP_AUX_CLR = (*(XIP_AUX_t *)0x50503000);

} // _XIP_AUX_

// Register block for various chip control signals
namespace _SYSCFG_  {

    // Configuration for processors
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC_CONFIG_t, uint32_t)
        // Indication that proc1 has halted
        ADD_BITFIELD_RO(PROC1_HALTED, 1, 1)
        // Indication that proc0 has halted
        ADD_BITFIELD_RO(PROC0_HALTED, 0, 1)
    END_TYPE()

    // For each bit, if 1, bypass the input synchronizer between that GPIO  and the GPIO input register in the SIO. The input synchronizers should  generally be unbypassed, to avoid injecting metastabilities into processors.  If you're feeling brave, you can bypass to save two cycles of input  latency. This register applies to GPIO 0...31.
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC_IN_SYNC_BYPASS_t, uint32_t)
        ADD_BITFIELD_RW(GPIO, 0, 32)
    END_TYPE()

    // For each bit, if 1, bypass the input synchronizer between that GPIO  and the GPIO input register in the SIO. The input synchronizers should  generally be unbypassed, to avoid injecting metastabilities into processors.  If you're feeling brave, you can bypass to save two cycles of input  latency. This register applies to GPIO 32...47. USB GPIO 56..57 QSPI GPIO 58..63
    // Reset value: 0x00000000
    BEGIN_TYPE(PROC_IN_SYNC_BYPASS_HI_t, uint32_t)
        ADD_BITFIELD_RW(QSPI_SD, 28, 4)
        ADD_BITFIELD_RW(QSPI_CSN, 27, 1)
        ADD_BITFIELD_RW(QSPI_SCK, 26, 1)
        ADD_BITFIELD_RW(USB_DM, 25, 1)
        ADD_BITFIELD_RW(USB_DP, 24, 1)
        ADD_BITFIELD_RW(GPIO, 0, 16)
    END_TYPE()

    // Directly control the chip SWD debug port
    // Reset value: 0x00000006
    BEGIN_TYPE(DBGFORCE_t, uint32_t)
        // Attach chip debug port to syscfg controls, and disconnect it from external SWD pads.
        ADD_BITFIELD_RW(ATTACH, 3, 1)
        // Directly drive SWCLK, if ATTACH is set
        ADD_BITFIELD_RW(SWCLK, 2, 1)
        // Directly drive SWDIO input, if ATTACH is set
        ADD_BITFIELD_RW(SWDI, 1, 1)
        // Observe the value of SWDIO output.
        ADD_BITFIELD_RO(SWDO, 0, 1)
    END_TYPE()

    // Control PD pins to memories.  Set high to put memories to a low power state. In this state the memories will retain contents but not be accessible  Use with caution
    // Reset value: 0x00000000
    BEGIN_TYPE(MEMPOWERDOWN_t, uint32_t)
        ADD_BITFIELD_RW(BOOTRAM, 12, 1)
        ADD_BITFIELD_RW(ROM, 11, 1)
        ADD_BITFIELD_RW(USB, 10, 1)
        ADD_BITFIELD_RW(SRAM9, 9, 1)
        ADD_BITFIELD_RW(SRAM8, 8, 1)
        ADD_BITFIELD_RW(SRAM7, 7, 1)
        ADD_BITFIELD_RW(SRAM6, 6, 1)
        ADD_BITFIELD_RW(SRAM5, 5, 1)
        ADD_BITFIELD_RW(SRAM4, 4, 1)
        ADD_BITFIELD_RW(SRAM3, 3, 1)
        ADD_BITFIELD_RW(SRAM2, 2, 1)
        ADD_BITFIELD_RW(SRAM1, 1, 1)
        ADD_BITFIELD_RW(SRAM0, 0, 1)
    END_TYPE()

    // Auxiliary system control register
    // Reset value: 0x00000000
    BEGIN_TYPE(AUXCTRL_t, uint32_t)
        // * Bits 7:2: Reserved * Bit 1: When clear, the LPOSC output is XORed into the TRNG ROSC output as an additional, uncorrelated entropy source. When set, this behaviour is disabled. * Bit 0: Force POWMAN clock to switch to LPOSC, by asserting its WDRESET input. This must be set before initiating a watchdog reset of the RSM from a stage that includes CLOCKS, if POWMAN is running from clk_ref at the point that the watchdog reset takes place. Otherwise, the short pulse generated on clk_ref by the reset of the CLOCKS block may affect POWMAN register state.
        ADD_BITFIELD_RW(AUXCTRL, 0, 8)
    END_TYPE()

    struct SYSCFG_t {
        PROC_CONFIG_t                 PROC_CONFIG;
        PROC_IN_SYNC_BYPASS_t         PROC_IN_SYNC_BYPASS;
        PROC_IN_SYNC_BYPASS_HI_t      PROC_IN_SYNC_BYPASS_HI;
        DBGFORCE_t                    DBGFORCE;
        MEMPOWERDOWN_t                MEMPOWERDOWN;
        AUXCTRL_t                     AUXCTRL;
    };

    static SYSCFG_t & SYSCFG = (*(SYSCFG_t *)0x40008000);
    static SYSCFG_t & SYSCFG_XOR = (*(SYSCFG_t *)0x40009000);
    static SYSCFG_t & SYSCFG_SET = (*(SYSCFG_t *)0x4000a000);
    static SYSCFG_t & SYSCFG_CLR = (*(SYSCFG_t *)0x4000b000);

} // _SYSCFG_

// Controls the crystal oscillator
namespace _XOSC_  {

    // Crystal Oscillator Control
    // Reset value: 0x00000000
    BEGIN_TYPE(CTRL_t, uint32_t)
        // On power-up this field is initialised to DISABLE and the chip runs from the ROSC.  If the chip has subsequently been programmed to run from the XOSC then setting this field to DISABLE may lock-up the chip. If  this is a concern then run the clk_ref from the ROSC and enable the clk_sys RESUS feature.  The 12-bit code is intended to give some protection against accidental writes. An invalid setting will retain the previous value. The actual value being used can be read from STATUS_ENABLED
        ADD_BITFIELD_RW(ENABLE, 12, 12)
        // The 12-bit code is intended to give some protection against accidental writes. An invalid setting will retain the previous value. The actual value being used can be read from STATUS_FREQ_RANGE
        ADD_BITFIELD_RW(FREQ_RANGE, 0, 12)
    END_TYPE()

    static const uint32_t CTRL_ENABLE__DISABLE = 3358;
    static const uint32_t CTRL_ENABLE__ENABLE = 4011;
    static const uint32_t CTRL_FREQ_RANGE__1_15MHZ = 2720;
    static const uint32_t CTRL_FREQ_RANGE__10_30MHZ = 2721;
    static const uint32_t CTRL_FREQ_RANGE__25_60MHZ = 2722;
    static const uint32_t CTRL_FREQ_RANGE__40_100MHZ = 2723;

    // Crystal Oscillator Status
    // Reset value: 0x00000000
    BEGIN_TYPE(STATUS_t, uint32_t)
        // Oscillator is running and stable
        ADD_BITFIELD_RO(STABLE, 31, 1)
        // An invalid value has been written to CTRL_ENABLE or CTRL_FREQ_RANGE or DORMANT
        ADD_BITFIELD_RW(BADWRITE, 24, 1)
        // Oscillator is enabled but not necessarily running and stable, resets to 0
        ADD_BITFIELD_RO(ENABLED, 12, 1)
        // The current frequency range setting
        ADD_BITFIELD_RO(FREQ_RANGE, 0, 2)
    END_TYPE()

    static const uint32_t STATUS_FREQ_RANGE__1_15MHZ = 0;
    static const uint32_t STATUS_FREQ_RANGE__10_30MHZ = 1;
    static const uint32_t STATUS_FREQ_RANGE__25_60MHZ = 2;
    static const uint32_t STATUS_FREQ_RANGE__40_100MHZ = 3;

    // Crystal Oscillator pause control
    BEGIN_TYPE(DORMANT_t, uint32_t)
        // This is used to save power by pausing the XOSC  On power-up this field is initialised to WAKE  An invalid write will also select WAKE  Warning: stop the PLLs before selecting dormant mode  Warning: setup the irq before selecting dormant mode
        ADD_BITFIELD_RW(DORMANT, 0, 32)
    END_TYPE()

    static const uint32_t DORMANT_DORMANT__dormant = 1668246881;
    static const uint32_t DORMANT_DORMANT__WAKE = 2002873189;

    // Controls the startup delay
    // Reset value: 0x00000000
    BEGIN_TYPE(STARTUP_t, uint32_t)
        // Multiplies the startup_delay by 4, just in case. The reset value is controlled by a mask-programmable tiecell and is provided in case we are booting from XOSC and the default startup delay is insufficient. The reset value is 0x0.
        ADD_BITFIELD_RW(X4, 20, 1)
        // in multiples of 256*xtal_period. The reset value of 0xc4 corresponds to approx 50 000 cycles.
        ADD_BITFIELD_RW(DELAY, 0, 14)
    END_TYPE()

    // A down counter running at the xosc frequency which counts to zero and stops.  Can be used for short software pauses when setting up time sensitive hardware.  To start the counter, write a non-zero value. Reads will return 1 while the count is running and 0 when it has finished.  Minimum count value is 4. Count values <4 will be treated as count value =4.  Note that synchronisation to the register clock domain costs 2 register clock cycles and the counter cannot compensate for that.
    // Reset value: 0x00000000
    BEGIN_TYPE(COUNT_t, uint32_t)
        ADD_BITFIELD_RW(COUNT, 0, 16)
    END_TYPE()

    struct XOSC_t {
        CTRL_t                        CTRL;
        STATUS_t                      STATUS;
        DORMANT_t                     DORMANT;
        STARTUP_t                     STARTUP;
        COUNT_t                       COUNT;
    };

    static XOSC_t & XOSC = (*(XOSC_t *)0x40048000);
    static XOSC_t & XOSC_XOR = (*(XOSC_t *)0x40049000);
    static XOSC_t & XOSC_SET = (*(XOSC_t *)0x4004a000);
    static XOSC_t & XOSC_CLR = (*(XOSC_t *)0x4004b000);

} // _XOSC_

namespace _PLL_SYS_  {

    // Control and Status  GENERAL CONSTRAINTS:  Reference clock frequency min=5MHz, max=800MHz  Feedback divider min=16, max=320  VCO frequency min=750MHz, max=1600MHz
    // Reset value: 0x00000001
    BEGIN_TYPE(CS_t, uint32_t)
        // PLL is locked
        ADD_BITFIELD_RO(LOCK, 31, 1)
        // PLL is not locked  Ideally this is cleared when PLL lock is seen and this should never normally be set
        ADD_BITFIELD_RW(LOCK_N, 30, 1)
        // Passes the reference clock to the output instead of the divided VCO. The VCO continues to run so the user can switch between the reference clock and the divided VCO but the output will glitch when doing so.
        ADD_BITFIELD_RW(BYPASS, 8, 1)
        // Divides the PLL input reference clock.  Behaviour is undefined for div=0.  PLL output will be unpredictable during refdiv changes, wait for lock=1 before using it.
        ADD_BITFIELD_RW(REFDIV, 0, 6)
    END_TYPE()

    // Controls the PLL power modes.
    // Reset value: 0x0000002d
    BEGIN_TYPE(PWR_t, uint32_t)
        // PLL VCO powerdown  To save power set high when PLL output not required or bypass=1.
        ADD_BITFIELD_RW(VCOPD, 5, 1)
        // PLL post divider powerdown  To save power set high when PLL output not required or bypass=1.
        ADD_BITFIELD_RW(POSTDIVPD, 3, 1)
        // PLL DSM powerdown  Nothing is achieved by setting this low.
        ADD_BITFIELD_RW(DSMPD, 2, 1)
        // PLL powerdown  To save power set high when PLL output not required.
        ADD_BITFIELD_RW(PD, 0, 1)
    END_TYPE()

    // Feedback divisor  (note: this PLL does not support fractional division)
    // Reset value: 0x00000000
    BEGIN_TYPE(FBDIV_INT_t, uint32_t)
        // see ctrl reg description for constraints
        ADD_BITFIELD_RW(FBDIV_INT, 0, 12)
    END_TYPE()

    // Controls the PLL post dividers for the primary output  (note: this PLL does not have a secondary output)  the primary output is driven from VCO divided by postdiv1*postdiv2
    // Reset value: 0x00077000
    BEGIN_TYPE(PRIM_t, uint32_t)
        // divide by 1-7
        ADD_BITFIELD_RW(POSTDIV1, 16, 3)
        // divide by 1-7
        ADD_BITFIELD_RW(POSTDIV2, 12, 3)
    END_TYPE()

    // Raw Interrupts
    // Reset value: 0x00000000
    BEGIN_TYPE(INTR_t, uint32_t)
        ADD_BITFIELD_RW(LOCK_N_STICKY, 0, 1)
    END_TYPE()

    // Interrupt Enable
    // Reset value: 0x00000000
    BEGIN_TYPE(INTE_t, uint32_t)
        ADD_BITFIELD_RW(LOCK_N_STICKY, 0, 1)
    END_TYPE()

    // Interrupt Force
    // Reset value: 0x00000000
    BEGIN_TYPE(INTF_t, uint32_t)
        ADD_BITFIELD_RW(LOCK_N_STICKY, 0, 1)
    END_TYPE()

    // Interrupt status after masking & forcing
    // Reset value: 0x00000000
    BEGIN_TYPE(INTS_t, uint32_t)
        ADD_BITFIELD_RO(LOCK_N_STICKY, 0, 1)
    END_TYPE()

    struct PLL_SYS_t {
        CS_t                          CS;
        PWR_t                         PWR;
        FBDIV_INT_t                   FBDIV_INT;
        PRIM_t                        PRIM;
        INTR_t                        INTR;
        INTE_t                        INTE;
        INTF_t                        INTF;
        INTS_t                        INTS;
    };

    static PLL_SYS_t & PLL_SYS = (*(PLL_SYS_t *)0x40050000);
    static PLL_SYS_t & PLL_SYS_XOR = (*(PLL_SYS_t *)0x40051000);
    static PLL_SYS_t & PLL_SYS_SET = (*(PLL_SYS_t *)0x40052000);
    static PLL_SYS_t & PLL_SYS_CLR = (*(PLL_SYS_t *)0x40053000);

} // _PLL_SYS_

namespace _PLL_USB_  {

    static _PLL_SYS_::PLL_SYS_t & PLL_USB = (*(_PLL_SYS_::PLL_SYS_t *)0x40058000);
    static _PLL_SYS_::PLL_SYS_t & PLL_USB_XOR = (*(_PLL_SYS_::PLL_SYS_t *)0x40059000);
    static _PLL_SYS_::PLL_SYS_t & PLL_USB_SET = (*(_PLL_SYS_::PLL_SYS_t *)0x4005a000);
    static _PLL_SYS_::PLL_SYS_t & PLL_USB_CLR = (*(_PLL_SYS_::PLL_SYS_t *)0x4005b000);

} // _PLL_USB_

// Hardware access control registers
namespace _ACCESSCTRL_  {

    // Once a LOCK bit is written to 1, ACCESSCTRL silently ignores writes from that master. LOCK is writable only by a Secure, Privileged processor or debugger. LOCK bits are only writable when their value is zero. Once set, they can never be cleared, except by a full reset of ACCESSCTRL Setting the LOCK bit does not affect whether an access raises a bus error. Unprivileged writes, or writes from the DMA, will continue to raise bus errors. All other accesses will continue not to.
    // Reset value: 0x00000004
    BEGIN_TYPE(LOCK_t, uint32_t)
        ADD_BITFIELD_RW(DEBUG, 3, 1)
        ADD_BITFIELD_RO(DMA, 2, 1)
        ADD_BITFIELD_RW(CORE1, 1, 1)
        ADD_BITFIELD_RW(CORE0, 0, 1)
    END_TYPE()

    // Force core 1's bus accesses to always be Non-secure, no matter the core's internal state. Useful for schemes where one core is designated as the Non-secure core, since some peripherals may filter individual registers internally based on security state but not on master ID.
    // Reset value: 0x00000000
    BEGIN_TYPE(FORCE_CORE_NS_t, uint32_t)
        ADD_BITFIELD_RW(CORE1, 1, 1)
    END_TYPE()

    // Write 1 to reset all ACCESSCTRL configuration, except for the LOCK and FORCE_CORE_NS registers. This bit is used in the RP2350 bootrom to quickly restore ACCESSCTRL to a known state during the boot path. Note that, like all registers in ACCESSCTRL, this register is not writable when the writer's corresponding LOCK bit is set, therefore a master which has been locked out of ACCESSCTRL can not use the CFGRESET register to disturb its contents.
    // Reset value: 0x00000000
    BEGIN_TYPE(CFGRESET_t, uint32_t)
        ADD_BITFIELD_WO(CFGRESET, 0, 1)
    END_TYPE()

    // Control whether GPIO0...31 are accessible to Non-secure code. Writable only by a Secure, Privileged processor or debugger. 0 -> Secure access only 1 -> Secure + Non-secure access
    // Reset value: 0x00000000
    BEGIN_TYPE(GPIO_NSMASK0_t, uint32_t)
        ADD_BITFIELD_RW(GPIO_NSMASK0, 0, 32)
    END_TYPE()

    // Control whether GPIO32..47 are accessible to Non-secure code, and whether QSPI and USB bitbang are accessible through the Non-secure SIO. Writable only by a Secure, Privileged processor or debugger.
    // Reset value: 0x00000000
    BEGIN_TYPE(GPIO_NSMASK1_t, uint32_t)
        ADD_BITFIELD_RW(QSPI_SD, 28, 4)
        ADD_BITFIELD_RW(QSPI_CSN, 27, 1)
        ADD_BITFIELD_RW(QSPI_SCK, 26, 1)
        ADD_BITFIELD_RW(USB_DM, 25, 1)
        ADD_BITFIELD_RW(USB_DP, 24, 1)
        ADD_BITFIELD_RW(GPIO, 0, 16)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access ROM, and at what security/privilege levels they can do so. Defaults to fully open access. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000ff
    BEGIN_TYPE(ROM_t, uint32_t)
        // If 1, ROM can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, ROM can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, ROM can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, ROM can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, ROM can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, ROM can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, ROM can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, ROM can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access XIP_MAIN, and at what security/privilege levels they can do so. Defaults to fully open access. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000ff
    BEGIN_TYPE(XIP_MAIN_t, uint32_t)
        // If 1, XIP_MAIN can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, XIP_MAIN can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, XIP_MAIN can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, XIP_MAIN can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, XIP_MAIN can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, XIP_MAIN can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, XIP_MAIN can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, XIP_MAIN can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access SRAM0, and at what security/privilege levels they can do so. Defaults to fully open access. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000ff
    BEGIN_TYPE(SRAM0_t, uint32_t)
        // If 1, SRAM0 can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, SRAM0 can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, SRAM0 can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, SRAM0 can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, SRAM0 can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, SRAM0 can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, SRAM0 can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, SRAM0 can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access SRAM1, and at what security/privilege levels they can do so. Defaults to fully open access. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000ff
    BEGIN_TYPE(SRAM1_t, uint32_t)
        // If 1, SRAM1 can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, SRAM1 can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, SRAM1 can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, SRAM1 can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, SRAM1 can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, SRAM1 can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, SRAM1 can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, SRAM1 can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access SRAM2, and at what security/privilege levels they can do so. Defaults to fully open access. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000ff
    BEGIN_TYPE(SRAM2_t, uint32_t)
        // If 1, SRAM2 can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, SRAM2 can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, SRAM2 can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, SRAM2 can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, SRAM2 can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, SRAM2 can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, SRAM2 can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, SRAM2 can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access SRAM3, and at what security/privilege levels they can do so. Defaults to fully open access. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000ff
    BEGIN_TYPE(SRAM3_t, uint32_t)
        // If 1, SRAM3 can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, SRAM3 can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, SRAM3 can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, SRAM3 can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, SRAM3 can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, SRAM3 can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, SRAM3 can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, SRAM3 can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access SRAM4, and at what security/privilege levels they can do so. Defaults to fully open access. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000ff
    BEGIN_TYPE(SRAM4_t, uint32_t)
        // If 1, SRAM4 can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, SRAM4 can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, SRAM4 can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, SRAM4 can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, SRAM4 can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, SRAM4 can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, SRAM4 can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, SRAM4 can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access SRAM5, and at what security/privilege levels they can do so. Defaults to fully open access. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000ff
    BEGIN_TYPE(SRAM5_t, uint32_t)
        // If 1, SRAM5 can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, SRAM5 can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, SRAM5 can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, SRAM5 can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, SRAM5 can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, SRAM5 can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, SRAM5 can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, SRAM5 can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access SRAM6, and at what security/privilege levels they can do so. Defaults to fully open access. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000ff
    BEGIN_TYPE(SRAM6_t, uint32_t)
        // If 1, SRAM6 can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, SRAM6 can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, SRAM6 can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, SRAM6 can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, SRAM6 can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, SRAM6 can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, SRAM6 can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, SRAM6 can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access SRAM7, and at what security/privilege levels they can do so. Defaults to fully open access. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000ff
    BEGIN_TYPE(SRAM7_t, uint32_t)
        // If 1, SRAM7 can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, SRAM7 can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, SRAM7 can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, SRAM7 can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, SRAM7 can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, SRAM7 can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, SRAM7 can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, SRAM7 can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access SRAM8, and at what security/privilege levels they can do so. Defaults to fully open access. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000ff
    BEGIN_TYPE(SRAM8_t, uint32_t)
        // If 1, SRAM8 can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, SRAM8 can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, SRAM8 can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, SRAM8 can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, SRAM8 can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, SRAM8 can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, SRAM8 can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, SRAM8 can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access SRAM9, and at what security/privilege levels they can do so. Defaults to fully open access. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000ff
    BEGIN_TYPE(SRAM9_t, uint32_t)
        // If 1, SRAM9 can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, SRAM9 can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, SRAM9 can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, SRAM9 can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, SRAM9 can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, SRAM9 can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, SRAM9 can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, SRAM9 can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access DMA, and at what security/privilege levels they can do so. Defaults to Secure access from any master. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000fc
    BEGIN_TYPE(DMA_t, uint32_t)
        // If 1, DMA can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, DMA can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, DMA can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, DMA can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, DMA can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, DMA can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, DMA can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, DMA can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access USBCTRL, and at what security/privilege levels they can do so. Defaults to Secure access from any master. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000fc
    BEGIN_TYPE(USBCTRL_t, uint32_t)
        // If 1, USBCTRL can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, USBCTRL can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, USBCTRL can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, USBCTRL can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, USBCTRL can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, USBCTRL can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, USBCTRL can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, USBCTRL can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access PIO0, and at what security/privilege levels they can do so. Defaults to Secure access from any master. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000fc
    BEGIN_TYPE(PIO0_t, uint32_t)
        // If 1, PIO0 can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, PIO0 can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, PIO0 can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, PIO0 can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, PIO0 can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, PIO0 can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, PIO0 can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, PIO0 can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access PIO1, and at what security/privilege levels they can do so. Defaults to Secure access from any master. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000fc
    BEGIN_TYPE(PIO1_t, uint32_t)
        // If 1, PIO1 can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, PIO1 can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, PIO1 can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, PIO1 can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, PIO1 can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, PIO1 can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, PIO1 can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, PIO1 can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access PIO2, and at what security/privilege levels they can do so. Defaults to Secure access from any master. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000fc
    BEGIN_TYPE(PIO2_t, uint32_t)
        // If 1, PIO2 can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, PIO2 can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, PIO2 can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, PIO2 can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, PIO2 can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, PIO2 can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, PIO2 can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, PIO2 can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access CORESIGHT_TRACE, and at what security/privilege levels they can do so. Defaults to Secure, Privileged processor or debug access only. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000b8
    BEGIN_TYPE(CORESIGHT_TRACE_t, uint32_t)
        // If 1, CORESIGHT_TRACE can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, CORESIGHT_TRACE can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, CORESIGHT_TRACE can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, CORESIGHT_TRACE can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, CORESIGHT_TRACE can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, CORESIGHT_TRACE can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, CORESIGHT_TRACE can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, CORESIGHT_TRACE can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access CORESIGHT_PERIPH, and at what security/privilege levels they can do so. Defaults to Secure, Privileged processor or debug access only. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000b8
    BEGIN_TYPE(CORESIGHT_PERIPH_t, uint32_t)
        // If 1, CORESIGHT_PERIPH can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, CORESIGHT_PERIPH can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, CORESIGHT_PERIPH can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, CORESIGHT_PERIPH can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, CORESIGHT_PERIPH can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, CORESIGHT_PERIPH can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, CORESIGHT_PERIPH can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, CORESIGHT_PERIPH can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access SYSINFO, and at what security/privilege levels they can do so. Defaults to fully open access. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000ff
    BEGIN_TYPE(SYSINFO_t, uint32_t)
        // If 1, SYSINFO can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, SYSINFO can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, SYSINFO can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, SYSINFO can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, SYSINFO can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, SYSINFO can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, SYSINFO can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, SYSINFO can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access RESETS, and at what security/privilege levels they can do so. Defaults to Secure access from any master. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000fc
    BEGIN_TYPE(RESETS_t, uint32_t)
        // If 1, RESETS can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, RESETS can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, RESETS can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, RESETS can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, RESETS can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, RESETS can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, RESETS can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, RESETS can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access IO_BANK0, and at what security/privilege levels they can do so. Defaults to Secure access from any master. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000fc
    BEGIN_TYPE(IO_BANK0_t, uint32_t)
        // If 1, IO_BANK0 can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, IO_BANK0 can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, IO_BANK0 can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, IO_BANK0 can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, IO_BANK0 can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, IO_BANK0 can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, IO_BANK0 can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, IO_BANK0 can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access IO_BANK1, and at what security/privilege levels they can do so. Defaults to Secure access from any master. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000fc
    BEGIN_TYPE(IO_BANK1_t, uint32_t)
        // If 1, IO_BANK1 can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, IO_BANK1 can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, IO_BANK1 can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, IO_BANK1 can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, IO_BANK1 can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, IO_BANK1 can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, IO_BANK1 can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, IO_BANK1 can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access PADS_BANK0, and at what security/privilege levels they can do so. Defaults to Secure access from any master. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000fc
    BEGIN_TYPE(PADS_BANK0_t, uint32_t)
        // If 1, PADS_BANK0 can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, PADS_BANK0 can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, PADS_BANK0 can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, PADS_BANK0 can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, PADS_BANK0 can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, PADS_BANK0 can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, PADS_BANK0 can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, PADS_BANK0 can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access PADS_QSPI, and at what security/privilege levels they can do so. Defaults to Secure access from any master. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000fc
    BEGIN_TYPE(PADS_QSPI_t, uint32_t)
        // If 1, PADS_QSPI can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, PADS_QSPI can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, PADS_QSPI can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, PADS_QSPI can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, PADS_QSPI can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, PADS_QSPI can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, PADS_QSPI can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, PADS_QSPI can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access BUSCTRL, and at what security/privilege levels they can do so. Defaults to Secure access from any master. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000fc
    BEGIN_TYPE(BUSCTRL_t, uint32_t)
        // If 1, BUSCTRL can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, BUSCTRL can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, BUSCTRL can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, BUSCTRL can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, BUSCTRL can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, BUSCTRL can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, BUSCTRL can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, BUSCTRL can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access ADC0, and at what security/privilege levels they can do so. Defaults to Secure access from any master. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000fc
    BEGIN_TYPE(ADC0_t, uint32_t)
        // If 1, ADC0 can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, ADC0 can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, ADC0 can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, ADC0 can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, ADC0 can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, ADC0 can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, ADC0 can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, ADC0 can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access HSTX, and at what security/privilege levels they can do so. Defaults to Secure access from any master. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000fc
    BEGIN_TYPE(HSTX_t, uint32_t)
        // If 1, HSTX can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, HSTX can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, HSTX can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, HSTX can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, HSTX can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, HSTX can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, HSTX can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, HSTX can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access I2C0, and at what security/privilege levels they can do so. Defaults to Secure access from any master. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000fc
    BEGIN_TYPE(I2C0_t, uint32_t)
        // If 1, I2C0 can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, I2C0 can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, I2C0 can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, I2C0 can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, I2C0 can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, I2C0 can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, I2C0 can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, I2C0 can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access I2C1, and at what security/privilege levels they can do so. Defaults to Secure access from any master. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000fc
    BEGIN_TYPE(I2C1_t, uint32_t)
        // If 1, I2C1 can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, I2C1 can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, I2C1 can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, I2C1 can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, I2C1 can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, I2C1 can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, I2C1 can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, I2C1 can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access PWM, and at what security/privilege levels they can do so. Defaults to Secure access from any master. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000fc
    BEGIN_TYPE(PWM_t, uint32_t)
        // If 1, PWM can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, PWM can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, PWM can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, PWM can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, PWM can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, PWM can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, PWM can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, PWM can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access SPI0, and at what security/privilege levels they can do so. Defaults to Secure access from any master. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000fc
    BEGIN_TYPE(SPI0_t, uint32_t)
        // If 1, SPI0 can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, SPI0 can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, SPI0 can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, SPI0 can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, SPI0 can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, SPI0 can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, SPI0 can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, SPI0 can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access SPI1, and at what security/privilege levels they can do so. Defaults to Secure access from any master. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000fc
    BEGIN_TYPE(SPI1_t, uint32_t)
        // If 1, SPI1 can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, SPI1 can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, SPI1 can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, SPI1 can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, SPI1 can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, SPI1 can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, SPI1 can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, SPI1 can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access TIMER0, and at what security/privilege levels they can do so. Defaults to Secure access from any master. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000fc
    BEGIN_TYPE(TIMER0_t, uint32_t)
        // If 1, TIMER0 can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, TIMER0 can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, TIMER0 can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, TIMER0 can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, TIMER0 can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, TIMER0 can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, TIMER0 can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, TIMER0 can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access TIMER1, and at what security/privilege levels they can do so. Defaults to Secure access from any master. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000fc
    BEGIN_TYPE(TIMER1_t, uint32_t)
        // If 1, TIMER1 can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, TIMER1 can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, TIMER1 can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, TIMER1 can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, TIMER1 can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, TIMER1 can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, TIMER1 can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, TIMER1 can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access UART0, and at what security/privilege levels they can do so. Defaults to Secure access from any master. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000fc
    BEGIN_TYPE(UART0_t, uint32_t)
        // If 1, UART0 can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, UART0 can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, UART0 can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, UART0 can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, UART0 can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, UART0 can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, UART0 can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, UART0 can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access UART1, and at what security/privilege levels they can do so. Defaults to Secure access from any master. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000fc
    BEGIN_TYPE(UART1_t, uint32_t)
        // If 1, UART1 can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, UART1 can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, UART1 can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, UART1 can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, UART1 can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, UART1 can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, UART1 can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, UART1 can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access OTP, and at what security/privilege levels they can do so. Defaults to Secure access from any master. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000fc
    BEGIN_TYPE(OTP_t, uint32_t)
        // If 1, OTP can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, OTP can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, OTP can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, OTP can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, OTP can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, OTP can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, OTP can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, OTP can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access TBMAN, and at what security/privilege levels they can do so. Defaults to Secure access from any master. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000fc
    BEGIN_TYPE(TBMAN_t, uint32_t)
        // If 1, TBMAN can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, TBMAN can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, TBMAN can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, TBMAN can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, TBMAN can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, TBMAN can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, TBMAN can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, TBMAN can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access POWMAN, and at what security/privilege levels they can do so. Defaults to Secure, Privileged processor or debug access only. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000b8
    BEGIN_TYPE(POWMAN_t, uint32_t)
        // If 1, POWMAN can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, POWMAN can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, POWMAN can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, POWMAN can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, POWMAN can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, POWMAN can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, POWMAN can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, POWMAN can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access TRNG, and at what security/privilege levels they can do so. Defaults to Secure, Privileged processor or debug access only. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000b8
    BEGIN_TYPE(TRNG_t, uint32_t)
        // If 1, TRNG can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, TRNG can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, TRNG can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, TRNG can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, TRNG can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, TRNG can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, TRNG can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, TRNG can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access SHA256, and at what security/privilege levels they can do so. Defaults to Secure, Privileged access only. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000f8
    BEGIN_TYPE(SHA256_t, uint32_t)
        // If 1, SHA256 can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, SHA256 can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, SHA256 can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, SHA256 can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, SHA256 can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, SHA256 can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, SHA256 can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, SHA256 can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access SYSCFG, and at what security/privilege levels they can do so. Defaults to Secure, Privileged processor or debug access only. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000b8
    BEGIN_TYPE(SYSCFG_t, uint32_t)
        // If 1, SYSCFG can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, SYSCFG can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, SYSCFG can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, SYSCFG can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, SYSCFG can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, SYSCFG can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, SYSCFG can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, SYSCFG can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access CLOCKS, and at what security/privilege levels they can do so. Defaults to Secure, Privileged processor or debug access only. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000b8
    BEGIN_TYPE(CLOCKS_t, uint32_t)
        // If 1, CLOCKS can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, CLOCKS can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, CLOCKS can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, CLOCKS can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, CLOCKS can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, CLOCKS can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, CLOCKS can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, CLOCKS can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access XOSC, and at what security/privilege levels they can do so. Defaults to Secure, Privileged processor or debug access only. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000b8
    BEGIN_TYPE(XOSC_t, uint32_t)
        // If 1, XOSC can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, XOSC can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, XOSC can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, XOSC can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, XOSC can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, XOSC can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, XOSC can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, XOSC can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access ROSC, and at what security/privilege levels they can do so. Defaults to Secure, Privileged processor or debug access only. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000b8
    BEGIN_TYPE(ROSC_t, uint32_t)
        // If 1, ROSC can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, ROSC can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, ROSC can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, ROSC can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, ROSC can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, ROSC can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, ROSC can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, ROSC can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access PLL_SYS, and at what security/privilege levels they can do so. Defaults to Secure, Privileged processor or debug access only. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000b8
    BEGIN_TYPE(PLL_SYS_t, uint32_t)
        // If 1, PLL_SYS can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, PLL_SYS can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, PLL_SYS can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, PLL_SYS can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, PLL_SYS can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, PLL_SYS can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, PLL_SYS can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, PLL_SYS can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access PLL_USB, and at what security/privilege levels they can do so. Defaults to Secure, Privileged processor or debug access only. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000b8
    BEGIN_TYPE(PLL_USB_t, uint32_t)
        // If 1, PLL_USB can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, PLL_USB can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, PLL_USB can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, PLL_USB can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, PLL_USB can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, PLL_USB can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, PLL_USB can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, PLL_USB can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access TICKS, and at what security/privilege levels they can do so. Defaults to Secure, Privileged processor or debug access only. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000b8
    BEGIN_TYPE(TICKS_t, uint32_t)
        // If 1, TICKS can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, TICKS can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, TICKS can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, TICKS can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, TICKS can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, TICKS can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, TICKS can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, TICKS can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access WATCHDOG, and at what security/privilege levels they can do so. Defaults to Secure, Privileged processor or debug access only. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000b8
    BEGIN_TYPE(WATCHDOG_t, uint32_t)
        // If 1, WATCHDOG can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, WATCHDOG can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, WATCHDOG can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, WATCHDOG can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, WATCHDOG can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, WATCHDOG can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, WATCHDOG can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, WATCHDOG can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access RSM, and at what security/privilege levels they can do so. Defaults to Secure, Privileged processor or debug access only. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000b8
    BEGIN_TYPE(RSM_t, uint32_t)
        // If 1, RSM can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, RSM can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, RSM can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, RSM can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, RSM can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, RSM can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, RSM can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, RSM can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access XIP_CTRL, and at what security/privilege levels they can do so. Defaults to Secure, Privileged processor or debug access only. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000b8
    BEGIN_TYPE(XIP_CTRL_t, uint32_t)
        // If 1, XIP_CTRL can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, XIP_CTRL can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, XIP_CTRL can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, XIP_CTRL can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, XIP_CTRL can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, XIP_CTRL can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, XIP_CTRL can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, XIP_CTRL can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access XIP_QMI, and at what security/privilege levels they can do so. Defaults to Secure, Privileged processor or debug access only. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000b8
    BEGIN_TYPE(XIP_QMI_t, uint32_t)
        // If 1, XIP_QMI can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, XIP_QMI can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, XIP_QMI can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, XIP_QMI can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, XIP_QMI can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, XIP_QMI can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, XIP_QMI can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, XIP_QMI can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    // Control whether debugger, DMA, core 0 and core 1 can access XIP_AUX, and at what security/privilege levels they can do so. Defaults to Secure, Privileged access only. This register is writable only from a Secure, Privileged processor or debugger, with the exception of the NSU bit, which becomes Non-secure-Privileged-writable when the NSP bit is set.
    // Reset value: 0x000000f8
    BEGIN_TYPE(XIP_AUX_t, uint32_t)
        // If 1, XIP_AUX can be accessed by the debugger, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DBG, 7, 1)
        // If 1, XIP_AUX can be accessed by the DMA, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(DMA, 6, 1)
        // If 1, XIP_AUX can be accessed by core 1, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE1, 5, 1)
        // If 1, XIP_AUX can be accessed by core 0, at security/privilege levels permitted by SP/NSP/SU/NSU in this register.
        ADD_BITFIELD_RW(CORE0, 4, 1)
        // If 1, XIP_AUX can be accessed from a Secure, Privileged context.
        ADD_BITFIELD_RW(SP, 3, 1)
        // If 1, and SP is also set, XIP_AUX can be accessed from a Secure, Unprivileged context.
        ADD_BITFIELD_RW(SU, 2, 1)
        // If 1, XIP_AUX can be accessed from a Non-secure, Privileged context.
        ADD_BITFIELD_RW(NSP, 1, 1)
        // If 1, and NSP is also set, XIP_AUX can be accessed from a Non-secure, Unprivileged context. This bit is writable from a Non-secure, Privileged context, if and only if the NSP bit is set.
        ADD_BITFIELD_RW(NSU, 0, 1)
    END_TYPE()

    struct ACCESSCTRL_t {
        LOCK_t                        LOCK;
        FORCE_CORE_NS_t               FORCE_CORE_NS;
        CFGRESET_t                    CFGRESET;
        GPIO_NSMASK0_t                GPIO_NSMASK0;
        GPIO_NSMASK1_t                GPIO_NSMASK1;
        ROM_t                         ROM;
        XIP_MAIN_t                    XIP_MAIN;
        SRAM0_t                       SRAM0;
        SRAM1_t                       SRAM1;
        SRAM2_t                       SRAM2;
        SRAM3_t                       SRAM3;
        SRAM4_t                       SRAM4;
        SRAM5_t                       SRAM5;
        SRAM6_t                       SRAM6;
        SRAM7_t                       SRAM7;
        SRAM8_t                       SRAM8;
        SRAM9_t                       SRAM9;
        DMA_t                         DMA;
        USBCTRL_t                     USBCTRL;
        PIO0_t                        PIO0;
        PIO1_t                        PIO1;
        PIO2_t                        PIO2;
        CORESIGHT_TRACE_t             CORESIGHT_TRACE;
        CORESIGHT_PERIPH_t            CORESIGHT_PERIPH;
        SYSINFO_t                     SYSINFO;
        RESETS_t                      RESETS;
        IO_BANK0_t                    IO_BANK0;
        IO_BANK1_t                    IO_BANK1;
        PADS_BANK0_t                  PADS_BANK0;
        PADS_QSPI_t                   PADS_QSPI;
        BUSCTRL_t                     BUSCTRL;
        ADC0_t                        ADC0;
        HSTX_t                        HSTX;
        I2C0_t                        I2C0;
        I2C1_t                        I2C1;
        PWM_t                         PWM;
        SPI0_t                        SPI0;
        SPI1_t                        SPI1;
        TIMER0_t                      TIMER0;
        TIMER1_t                      TIMER1;
        UART0_t                       UART0;
        UART1_t                       UART1;
        OTP_t                         OTP;
        TBMAN_t                       TBMAN;
        POWMAN_t                      POWMAN;
        TRNG_t                        TRNG;
        SHA256_t                      SHA256;
        SYSCFG_t                      SYSCFG;
        CLOCKS_t                      CLOCKS;
        XOSC_t                        XOSC;
        ROSC_t                        ROSC;
        PLL_SYS_t                     PLL_SYS;
        PLL_USB_t                     PLL_USB;
        TICKS_t                       TICKS;
        WATCHDOG_t                    WATCHDOG;
        RSM_t                         RSM;
        XIP_CTRL_t                    XIP_CTRL;
        XIP_QMI_t                     XIP_QMI;
        XIP_AUX_t                     XIP_AUX;
    };

    static ACCESSCTRL_t & ACCESSCTRL = (*(ACCESSCTRL_t *)0x40060000);
    static ACCESSCTRL_t & ACCESSCTRL_XOR = (*(ACCESSCTRL_t *)0x40061000);
    static ACCESSCTRL_t & ACCESSCTRL_SET = (*(ACCESSCTRL_t *)0x40062000);
    static ACCESSCTRL_t & ACCESSCTRL_CLR = (*(ACCESSCTRL_t *)0x40063000);

} // _ACCESSCTRL_

namespace _UART0_  {

    // Data Register, UARTDR
    // Reset value: 0x00000000
    BEGIN_TYPE(UARTDR_t, uint32_t)
        // Overrun error. This bit is set to 1 if data is received and the receive FIFO is already full. This is cleared to 0 once there is an empty space in the FIFO and a new character can be written to it.
        ADD_BITFIELD_RO(OE, 11, 1)
        // Break error. This bit is set to 1 if a break condition was detected, indicating that the received data input was held LOW for longer than a full-word transmission time (defined as start, data, parity and stop bits). In FIFO mode, this error is associated with the character at the top of the FIFO. When a break occurs, only one 0 character is loaded into the FIFO. The next character is only enabled after the receive data input goes to a 1 (marking state), and the next valid start bit is received.
        ADD_BITFIELD_RO(BE, 10, 1)
        // Parity error. When set to 1, it indicates that the parity of the received data character does not match the parity that the EPS and SPS bits in the Line Control Register, UARTLCR_H. In FIFO mode, this error is associated with the character at the top of the FIFO.
        ADD_BITFIELD_RO(PE, 9, 1)
        // Framing error. When set to 1, it indicates that the received character did not have a valid stop bit (a valid stop bit is 1). In FIFO mode, this error is associated with the character at the top of the FIFO.
        ADD_BITFIELD_RO(FE, 8, 1)
        // Receive (read) data character. Transmit (write) data character.
        ADD_BITFIELD_RW(DATA, 0, 8)
    END_TYPE()

    // Receive Status Register/Error Clear Register, UARTRSR/UARTECR
    // Reset value: 0x00000000
    BEGIN_TYPE(UARTRSR_t, uint32_t)
        // Overrun error. This bit is set to 1 if data is received and the FIFO is already full. This bit is cleared to 0 by a write to UARTECR. The FIFO contents remain valid because no more data is written when the FIFO is full, only the contents of the shift register are overwritten. The CPU must now read the data, to empty the FIFO.
        ADD_BITFIELD_RW(OE, 3, 1)
        // Break error. This bit is set to 1 if a break condition was detected, indicating that the received data input was held LOW for longer than a full-word transmission time (defined as start, data, parity, and stop bits). This bit is cleared to 0 after a write to UARTECR. In FIFO mode, this error is associated with the character at the top of the FIFO. When a break occurs, only one 0 character is loaded into the FIFO. The next character is only enabled after the receive data input goes to a 1 (marking state) and the next valid start bit is received.
        ADD_BITFIELD_RW(BE, 2, 1)
        // Parity error. When set to 1, it indicates that the parity of the received data character does not match the parity that the EPS and SPS bits in the Line Control Register, UARTLCR_H. This bit is cleared to 0 by a write to UARTECR. In FIFO mode, this error is associated with the character at the top of the FIFO.
        ADD_BITFIELD_RW(PE, 1, 1)
        // Framing error. When set to 1, it indicates that the received character did not have a valid stop bit (a valid stop bit is 1). This bit is cleared to 0 by a write to UARTECR. In FIFO mode, this error is associated with the character at the top of the FIFO.
        ADD_BITFIELD_RW(FE, 0, 1)
    END_TYPE()

    // Flag Register, UARTFR
    // Reset value: 0x00000090
    BEGIN_TYPE(UARTFR_t, uint32_t)
        // Ring indicator. This bit is the complement of the UART ring indicator, nUARTRI, modem status input. That is, the bit is 1 when nUARTRI is LOW.
        ADD_BITFIELD_RO(RI, 8, 1)
        // Transmit FIFO empty. The meaning of this bit depends on the state of the FEN bit in the Line Control Register, UARTLCR_H. If the FIFO is disabled, this bit is set when the transmit holding register is empty. If the FIFO is enabled, the TXFE bit is set when the transmit FIFO is empty. This bit does not indicate if there is data in the transmit shift register.
        ADD_BITFIELD_RO(TXFE, 7, 1)
        // Receive FIFO full. The meaning of this bit depends on the state of the FEN bit in the UARTLCR_H Register. If the FIFO is disabled, this bit is set when the receive holding register is full. If the FIFO is enabled, the RXFF bit is set when the receive FIFO is full.
        ADD_BITFIELD_RO(RXFF, 6, 1)
        // Transmit FIFO full. The meaning of this bit depends on the state of the FEN bit in the UARTLCR_H Register. If the FIFO is disabled, this bit is set when the transmit holding register is full. If the FIFO is enabled, the TXFF bit is set when the transmit FIFO is full.
        ADD_BITFIELD_RO(TXFF, 5, 1)
        // Receive FIFO empty. The meaning of this bit depends on the state of the FEN bit in the UARTLCR_H Register. If the FIFO is disabled, this bit is set when the receive holding register is empty. If the FIFO is enabled, the RXFE bit is set when the receive FIFO is empty.
        ADD_BITFIELD_RO(RXFE, 4, 1)
        // UART busy. If this bit is set to 1, the UART is busy transmitting data. This bit remains set until the complete byte, including all the stop bits, has been sent from the shift register. This bit is set as soon as the transmit FIFO becomes non-empty, regardless of whether the UART is enabled or not.
        ADD_BITFIELD_RO(BUSY, 3, 1)
        // Data carrier detect. This bit is the complement of the UART data carrier detect, nUARTDCD, modem status input. That is, the bit is 1 when nUARTDCD is LOW.
        ADD_BITFIELD_RO(DCD, 2, 1)
        // Data set ready. This bit is the complement of the UART data set ready, nUARTDSR, modem status input. That is, the bit is 1 when nUARTDSR is LOW.
        ADD_BITFIELD_RO(DSR, 1, 1)
        // Clear to send. This bit is the complement of the UART clear to send, nUARTCTS, modem status input. That is, the bit is 1 when nUARTCTS is LOW.
        ADD_BITFIELD_RO(CTS, 0, 1)
    END_TYPE()

    // IrDA Low-Power Counter Register, UARTILPR
    // Reset value: 0x00000000
    BEGIN_TYPE(UARTILPR_t, uint32_t)
        // 8-bit low-power divisor value. These bits are cleared to 0 at reset.
        ADD_BITFIELD_RW(ILPDVSR, 0, 8)
    END_TYPE()

    // Integer Baud Rate Register, UARTIBRD
    // Reset value: 0x00000000
    BEGIN_TYPE(UARTIBRD_t, uint32_t)
        // The integer baud rate divisor. These bits are cleared to 0 on reset.
        ADD_BITFIELD_RW(BAUD_DIVINT, 0, 16)
    END_TYPE()

    // Fractional Baud Rate Register, UARTFBRD
    // Reset value: 0x00000000
    BEGIN_TYPE(UARTFBRD_t, uint32_t)
        // The fractional baud rate divisor. These bits are cleared to 0 on reset.
        ADD_BITFIELD_RW(BAUD_DIVFRAC, 0, 6)
    END_TYPE()

    // Line Control Register, UARTLCR_H
    // Reset value: 0x00000000
    BEGIN_TYPE(UARTLCR_H_t, uint32_t)
        // Stick parity select. 0 = stick parity is disabled 1 = either: * if the EPS bit is 0 then the parity bit is transmitted and checked as a 1 * if the EPS bit is 1 then the parity bit is transmitted and checked as a 0. This bit has no effect when the PEN bit disables parity checking and generation.
        ADD_BITFIELD_RW(SPS, 7, 1)
        // Word length. These bits indicate the number of data bits transmitted or received in a frame as follows: b11 = 8 bits b10 = 7 bits b01 = 6 bits b00 = 5 bits.
        ADD_BITFIELD_RW(WLEN, 5, 2)
        // Enable FIFOs: 0 = FIFOs are disabled (character mode) that is, the FIFOs become 1-byte-deep holding registers 1 = transmit and receive FIFO buffers are enabled (FIFO mode).
        ADD_BITFIELD_RW(FEN, 4, 1)
        // Two stop bits select. If this bit is set to 1, two stop bits are transmitted at the end of the frame. The receive logic does not check for two stop bits being received.
        ADD_BITFIELD_RW(STP2, 3, 1)
        // Even parity select. Controls the type of parity the UART uses during transmission and reception: 0 = odd parity. The UART generates or checks for an odd number of 1s in the data and parity bits. 1 = even parity. The UART generates or checks for an even number of 1s in the data and parity bits. This bit has no effect when the PEN bit disables parity checking and generation.
        ADD_BITFIELD_RW(EPS, 2, 1)
        // Parity enable: 0 = parity is disabled and no parity bit added to the data frame 1 = parity checking and generation is enabled.
        ADD_BITFIELD_RW(PEN, 1, 1)
        // Send break. If this bit is set to 1, a low-level is continually output on the UARTTXD output, after completing transmission of the current character. For the proper execution of the break command, the software must set this bit for at least two complete frames. For normal use, this bit must be cleared to 0.
        ADD_BITFIELD_RW(BRK, 0, 1)
    END_TYPE()

    // Control Register, UARTCR
    // Reset value: 0x00000300
    BEGIN_TYPE(UARTCR_t, uint32_t)
        // CTS hardware flow control enable. If this bit is set to 1, CTS hardware flow control is enabled. Data is only transmitted when the nUARTCTS signal is asserted.
        ADD_BITFIELD_RW(CTSEN, 15, 1)
        // RTS hardware flow control enable. If this bit is set to 1, RTS hardware flow control is enabled. Data is only requested when there is space in the receive FIFO for it to be received.
        ADD_BITFIELD_RW(RTSEN, 14, 1)
        // This bit is the complement of the UART Out2 (nUARTOut2) modem status output. That is, when the bit is programmed to a 1, the output is 0. For DTE this can be used as Ring Indicator (RI).
        ADD_BITFIELD_RW(OUT2, 13, 1)
        // This bit is the complement of the UART Out1 (nUARTOut1) modem status output. That is, when the bit is programmed to a 1 the output is 0. For DTE this can be used as Data Carrier Detect (DCD).
        ADD_BITFIELD_RW(OUT1, 12, 1)
        // Request to send. This bit is the complement of the UART request to send, nUARTRTS, modem status output. That is, when the bit is programmed to a 1 then nUARTRTS is LOW.
        ADD_BITFIELD_RW(RTS, 11, 1)
        // Data transmit ready. This bit is the complement of the UART data transmit ready, nUARTDTR, modem status output. That is, when the bit is programmed to a 1 then nUARTDTR is LOW.
        ADD_BITFIELD_RW(DTR, 10, 1)
        // Receive enable. If this bit is set to 1, the receive section of the UART is enabled. Data reception occurs for either UART signals or SIR signals depending on the setting of the SIREN bit. When the UART is disabled in the middle of reception, it completes the current character before stopping.
        ADD_BITFIELD_RW(RXE, 9, 1)
        // Transmit enable. If this bit is set to 1, the transmit section of the UART is enabled. Data transmission occurs for either UART signals, or SIR signals depending on the setting of the SIREN bit. When the UART is disabled in the middle of transmission, it completes the current character before stopping.
        ADD_BITFIELD_RW(TXE, 8, 1)
        // Loopback enable. If this bit is set to 1 and the SIREN bit is set to 1 and the SIRTEST bit in the Test Control Register, UARTTCR is set to 1, then the nSIROUT path is inverted, and fed through to the SIRIN path. The SIRTEST bit in the test register must be set to 1 to override the normal half-duplex SIR operation. This must be the requirement for accessing the test registers during normal operation, and SIRTEST must be cleared to 0 when loopback testing is finished. This feature reduces the amount of external coupling required during system test. If this bit is set to 1, and the SIRTEST bit is set to 0, the UARTTXD path is fed through to the UARTRXD path. In either SIR mode or UART mode, when this bit is set, the modem outputs are also fed through to the modem inputs. This bit is cleared to 0 on reset, to disable loopback.
        ADD_BITFIELD_RW(LBE, 7, 1)
        // SIR low-power IrDA mode. This bit selects the IrDA encoding mode. If this bit is cleared to 0, low-level bits are transmitted as an active high pulse with a width of 3 / 16th of the bit period. If this bit is set to 1, low-level bits are transmitted with a pulse width that is 3 times the period of the IrLPBaud16 input signal, regardless of the selected bit rate. Setting this bit uses less power, but might reduce transmission distances.
        ADD_BITFIELD_RW(SIRLP, 2, 1)
        // SIR enable: 0 = IrDA SIR ENDEC is disabled. nSIROUT remains LOW (no light pulse generated), and signal transitions on SIRIN have no effect. 1 = IrDA SIR ENDEC is enabled. Data is transmitted and received on nSIROUT and SIRIN. UARTTXD remains HIGH, in the marking state. Signal transitions on UARTRXD or modem status inputs have no effect. This bit has no effect if the UARTEN bit disables the UART.
        ADD_BITFIELD_RW(SIREN, 1, 1)
        // UART enable: 0 = UART is disabled. If the UART is disabled in the middle of transmission or reception, it completes the current character before stopping. 1 = the UART is enabled. Data transmission and reception occurs for either UART signals or SIR signals depending on the setting of the SIREN bit.
        ADD_BITFIELD_RW(UARTEN, 0, 1)
    END_TYPE()

    // Interrupt FIFO Level Select Register, UARTIFLS
    // Reset value: 0x00000012
    BEGIN_TYPE(UARTIFLS_t, uint32_t)
        // Receive interrupt FIFO level select. The trigger points for the receive interrupt are as follows: b000 = Receive FIFO becomes >= 1 / 8 full b001 = Receive FIFO becomes >= 1 / 4 full b010 = Receive FIFO becomes >= 1 / 2 full b011 = Receive FIFO becomes >= 3 / 4 full b100 = Receive FIFO becomes >= 7 / 8 full b101-b111 = reserved.
        ADD_BITFIELD_RW(RXIFLSEL, 3, 3)
        // Transmit interrupt FIFO level select. The trigger points for the transmit interrupt are as follows: b000 = Transmit FIFO becomes <= 1 / 8 full b001 = Transmit FIFO becomes <= 1 / 4 full b010 = Transmit FIFO becomes <= 1 / 2 full b011 = Transmit FIFO becomes <= 3 / 4 full b100 = Transmit FIFO becomes <= 7 / 8 full b101-b111 = reserved.
        ADD_BITFIELD_RW(TXIFLSEL, 0, 3)
    END_TYPE()

    // Interrupt Mask Set/Clear Register, UARTIMSC
    // Reset value: 0x00000000
    BEGIN_TYPE(UARTIMSC_t, uint32_t)
        // Overrun error interrupt mask. A read returns the current mask for the UARTOEINTR interrupt. On a write of 1, the mask of the UARTOEINTR interrupt is set. A write of 0 clears the mask.
        ADD_BITFIELD_RW(OEIM, 10, 1)
        // Break error interrupt mask. A read returns the current mask for the UARTBEINTR interrupt. On a write of 1, the mask of the UARTBEINTR interrupt is set. A write of 0 clears the mask.
        ADD_BITFIELD_RW(BEIM, 9, 1)
        // Parity error interrupt mask. A read returns the current mask for the UARTPEINTR interrupt. On a write of 1, the mask of the UARTPEINTR interrupt is set. A write of 0 clears the mask.
        ADD_BITFIELD_RW(PEIM, 8, 1)
        // Framing error interrupt mask. A read returns the current mask for the UARTFEINTR interrupt. On a write of 1, the mask of the UARTFEINTR interrupt is set. A write of 0 clears the mask.
        ADD_BITFIELD_RW(FEIM, 7, 1)
        // Receive timeout interrupt mask. A read returns the current mask for the UARTRTINTR interrupt. On a write of 1, the mask of the UARTRTINTR interrupt is set. A write of 0 clears the mask.
        ADD_BITFIELD_RW(RTIM, 6, 1)
        // Transmit interrupt mask. A read returns the current mask for the UARTTXINTR interrupt. On a write of 1, the mask of the UARTTXINTR interrupt is set. A write of 0 clears the mask.
        ADD_BITFIELD_RW(TXIM, 5, 1)
        // Receive interrupt mask. A read returns the current mask for the UARTRXINTR interrupt. On a write of 1, the mask of the UARTRXINTR interrupt is set. A write of 0 clears the mask.
        ADD_BITFIELD_RW(RXIM, 4, 1)
        // nUARTDSR modem interrupt mask. A read returns the current mask for the UARTDSRINTR interrupt. On a write of 1, the mask of the UARTDSRINTR interrupt is set. A write of 0 clears the mask.
        ADD_BITFIELD_RW(DSRMIM, 3, 1)
        // nUARTDCD modem interrupt mask. A read returns the current mask for the UARTDCDINTR interrupt. On a write of 1, the mask of the UARTDCDINTR interrupt is set. A write of 0 clears the mask.
        ADD_BITFIELD_RW(DCDMIM, 2, 1)
        // nUARTCTS modem interrupt mask. A read returns the current mask for the UARTCTSINTR interrupt. On a write of 1, the mask of the UARTCTSINTR interrupt is set. A write of 0 clears the mask.
        ADD_BITFIELD_RW(CTSMIM, 1, 1)
        // nUARTRI modem interrupt mask. A read returns the current mask for the UARTRIINTR interrupt. On a write of 1, the mask of the UARTRIINTR interrupt is set. A write of 0 clears the mask.
        ADD_BITFIELD_RW(RIMIM, 0, 1)
    END_TYPE()

    // Raw Interrupt Status Register, UARTRIS
    // Reset value: 0x00000000
    BEGIN_TYPE(UARTRIS_t, uint32_t)
        // Overrun error interrupt status. Returns the raw interrupt state of the UARTOEINTR interrupt.
        ADD_BITFIELD_RO(OERIS, 10, 1)
        // Break error interrupt status. Returns the raw interrupt state of the UARTBEINTR interrupt.
        ADD_BITFIELD_RO(BERIS, 9, 1)
        // Parity error interrupt status. Returns the raw interrupt state of the UARTPEINTR interrupt.
        ADD_BITFIELD_RO(PERIS, 8, 1)
        // Framing error interrupt status. Returns the raw interrupt state of the UARTFEINTR interrupt.
        ADD_BITFIELD_RO(FERIS, 7, 1)
        // Receive timeout interrupt status. Returns the raw interrupt state of the UARTRTINTR interrupt. a
        ADD_BITFIELD_RO(RTRIS, 6, 1)
        // Transmit interrupt status. Returns the raw interrupt state of the UARTTXINTR interrupt.
        ADD_BITFIELD_RO(TXRIS, 5, 1)
        // Receive interrupt status. Returns the raw interrupt state of the UARTRXINTR interrupt.
        ADD_BITFIELD_RO(RXRIS, 4, 1)
        // nUARTDSR modem interrupt status. Returns the raw interrupt state of the UARTDSRINTR interrupt.
        ADD_BITFIELD_RO(DSRRMIS, 3, 1)
        // nUARTDCD modem interrupt status. Returns the raw interrupt state of the UARTDCDINTR interrupt.
        ADD_BITFIELD_RO(DCDRMIS, 2, 1)
        // nUARTCTS modem interrupt status. Returns the raw interrupt state of the UARTCTSINTR interrupt.
        ADD_BITFIELD_RO(CTSRMIS, 1, 1)
        // nUARTRI modem interrupt status. Returns the raw interrupt state of the UARTRIINTR interrupt.
        ADD_BITFIELD_RO(RIRMIS, 0, 1)
    END_TYPE()

    // Masked Interrupt Status Register, UARTMIS
    // Reset value: 0x00000000
    BEGIN_TYPE(UARTMIS_t, uint32_t)
        // Overrun error masked interrupt status. Returns the masked interrupt state of the UARTOEINTR interrupt.
        ADD_BITFIELD_RO(OEMIS, 10, 1)
        // Break error masked interrupt status. Returns the masked interrupt state of the UARTBEINTR interrupt.
        ADD_BITFIELD_RO(BEMIS, 9, 1)
        // Parity error masked interrupt status. Returns the masked interrupt state of the UARTPEINTR interrupt.
        ADD_BITFIELD_RO(PEMIS, 8, 1)
        // Framing error masked interrupt status. Returns the masked interrupt state of the UARTFEINTR interrupt.
        ADD_BITFIELD_RO(FEMIS, 7, 1)
        // Receive timeout masked interrupt status. Returns the masked interrupt state of the UARTRTINTR interrupt.
        ADD_BITFIELD_RO(RTMIS, 6, 1)
        // Transmit masked interrupt status. Returns the masked interrupt state of the UARTTXINTR interrupt.
        ADD_BITFIELD_RO(TXMIS, 5, 1)
        // Receive masked interrupt status. Returns the masked interrupt state of the UARTRXINTR interrupt.
        ADD_BITFIELD_RO(RXMIS, 4, 1)
        // nUARTDSR modem masked interrupt status. Returns the masked interrupt state of the UARTDSRINTR interrupt.
        ADD_BITFIELD_RO(DSRMMIS, 3, 1)
        // nUARTDCD modem masked interrupt status. Returns the masked interrupt state of the UARTDCDINTR interrupt.
        ADD_BITFIELD_RO(DCDMMIS, 2, 1)
        // nUARTCTS modem masked interrupt status. Returns the masked interrupt state of the UARTCTSINTR interrupt.
        ADD_BITFIELD_RO(CTSMMIS, 1, 1)
        // nUARTRI modem masked interrupt status. Returns the masked interrupt state of the UARTRIINTR interrupt.
        ADD_BITFIELD_RO(RIMMIS, 0, 1)
    END_TYPE()

    // Interrupt Clear Register, UARTICR
    // Reset value: 0x00000000
    BEGIN_TYPE(UARTICR_t, uint32_t)
        // Overrun error interrupt clear. Clears the UARTOEINTR interrupt.
        ADD_BITFIELD_RW(OEIC, 10, 1)
        // Break error interrupt clear. Clears the UARTBEINTR interrupt.
        ADD_BITFIELD_RW(BEIC, 9, 1)
        // Parity error interrupt clear. Clears the UARTPEINTR interrupt.
        ADD_BITFIELD_RW(PEIC, 8, 1)
        // Framing error interrupt clear. Clears the UARTFEINTR interrupt.
        ADD_BITFIELD_RW(FEIC, 7, 1)
        // Receive timeout interrupt clear. Clears the UARTRTINTR interrupt.
        ADD_BITFIELD_RW(RTIC, 6, 1)
        // Transmit interrupt clear. Clears the UARTTXINTR interrupt.
        ADD_BITFIELD_RW(TXIC, 5, 1)
        // Receive interrupt clear. Clears the UARTRXINTR interrupt.
        ADD_BITFIELD_RW(RXIC, 4, 1)
        // nUARTDSR modem interrupt clear. Clears the UARTDSRINTR interrupt.
        ADD_BITFIELD_RW(DSRMIC, 3, 1)
        // nUARTDCD modem interrupt clear. Clears the UARTDCDINTR interrupt.
        ADD_BITFIELD_RW(DCDMIC, 2, 1)
        // nUARTCTS modem interrupt clear. Clears the UARTCTSINTR interrupt.
        ADD_BITFIELD_RW(CTSMIC, 1, 1)
        // nUARTRI modem interrupt clear. Clears the UARTRIINTR interrupt.
        ADD_BITFIELD_RW(RIMIC, 0, 1)
    END_TYPE()

    // DMA Control Register, UARTDMACR
    // Reset value: 0x00000000
    BEGIN_TYPE(UARTDMACR_t, uint32_t)
        // DMA on error. If this bit is set to 1, the DMA receive request outputs, UARTRXDMASREQ or UARTRXDMABREQ, are disabled when the UART error interrupt is asserted.
        ADD_BITFIELD_RW(DMAONERR, 2, 1)
        // Transmit DMA enable. If this bit is set to 1, DMA for the transmit FIFO is enabled.
        ADD_BITFIELD_RW(TXDMAE, 1, 1)
        // Receive DMA enable. If this bit is set to 1, DMA for the receive FIFO is enabled.
        ADD_BITFIELD_RW(RXDMAE, 0, 1)
    END_TYPE()

    // UARTPeriphID0 Register
    // Reset value: 0x00000011
    BEGIN_TYPE(UARTPERIPHID0_t, uint32_t)
        // These bits read back as 0x11
        ADD_BITFIELD_RO(PARTNUMBER0, 0, 8)
    END_TYPE()

    // UARTPeriphID1 Register
    // Reset value: 0x00000010
    BEGIN_TYPE(UARTPERIPHID1_t, uint32_t)
        // These bits read back as 0x1
        ADD_BITFIELD_RO(DESIGNER0, 4, 4)
        // These bits read back as 0x0
        ADD_BITFIELD_RO(PARTNUMBER1, 0, 4)
    END_TYPE()

    // UARTPeriphID2 Register
    // Reset value: 0x00000034
    BEGIN_TYPE(UARTPERIPHID2_t, uint32_t)
        // This field depends on the revision of the UART: r1p0 0x0 r1p1 0x1 r1p3 0x2 r1p4 0x2 r1p5 0x3
        ADD_BITFIELD_RO(REVISION, 4, 4)
        // These bits read back as 0x4
        ADD_BITFIELD_RO(DESIGNER1, 0, 4)
    END_TYPE()

    // UARTPeriphID3 Register
    // Reset value: 0x00000000
    BEGIN_TYPE(UARTPERIPHID3_t, uint32_t)
        // These bits read back as 0x00
        ADD_BITFIELD_RO(CONFIGURATION, 0, 8)
    END_TYPE()

    // UARTPCellID0 Register
    // Reset value: 0x0000000d
    BEGIN_TYPE(UARTPCELLID0_t, uint32_t)
        // These bits read back as 0x0D
        ADD_BITFIELD_RO(UARTPCELLID0, 0, 8)
    END_TYPE()

    // UARTPCellID1 Register
    // Reset value: 0x000000f0
    BEGIN_TYPE(UARTPCELLID1_t, uint32_t)
        // These bits read back as 0xF0
        ADD_BITFIELD_RO(UARTPCELLID1, 0, 8)
    END_TYPE()

    // UARTPCellID2 Register
    // Reset value: 0x00000005
    BEGIN_TYPE(UARTPCELLID2_t, uint32_t)
        // These bits read back as 0x05
        ADD_BITFIELD_RO(UARTPCELLID2, 0, 8)
    END_TYPE()

    // UARTPCellID3 Register
    // Reset value: 0x000000b1
    BEGIN_TYPE(UARTPCELLID3_t, uint32_t)
        // These bits read back as 0xB1
        ADD_BITFIELD_RO(UARTPCELLID3, 0, 8)
    END_TYPE()

    struct UART0_t {
        UARTDR_t                      UARTDR;
        UARTRSR_t                     UARTRSR;
        uint32_t                      reserved0[4];
        UARTFR_t                      UARTFR;
        uint32_t                      reserved1;
        UARTILPR_t                    UARTILPR;
        UARTIBRD_t                    UARTIBRD;
        UARTFBRD_t                    UARTFBRD;
        UARTLCR_H_t                   UARTLCR_H;
        UARTCR_t                      UARTCR;
        UARTIFLS_t                    UARTIFLS;
        UARTIMSC_t                    UARTIMSC;
        UARTRIS_t                     UARTRIS;
        UARTMIS_t                     UARTMIS;
        UARTICR_t                     UARTICR;
        UARTDMACR_t                   UARTDMACR;
        uint32_t                      reserved2[997];
        UARTPERIPHID0_t               UARTPERIPHID0;
        UARTPERIPHID1_t               UARTPERIPHID1;
        UARTPERIPHID2_t               UARTPERIPHID2;
        UARTPERIPHID3_t               UARTPERIPHID3;
        UARTPCELLID0_t                UARTPCELLID0;
        UARTPCELLID1_t                UARTPCELLID1;
        UARTPCELLID2_t                UARTPCELLID2;
        UARTPCELLID3_t                UARTPCELLID3;
    };

    static UART0_t & UART0 = (*(UART0_t *)0x40070000);
    static UART0_t & UART0_XOR = (*(UART0_t *)0x40071000);
    static UART0_t & UART0_SET = (*(UART0_t *)0x40072000);
    static UART0_t & UART0_CLR = (*(UART0_t *)0x40073000);

} // _UART0_

namespace _UART1_  {

    static _UART0_::UART0_t & UART1 = (*(_UART0_::UART0_t *)0x40078000);
    static _UART0_::UART0_t & UART1_XOR = (*(_UART0_::UART0_t *)0x40079000);
    static _UART0_::UART0_t & UART1_SET = (*(_UART0_::UART0_t *)0x4007a000);
    static _UART0_::UART0_t & UART1_CLR = (*(_UART0_::UART0_t *)0x4007b000);

} // _UART1_

namespace _ROSC_  {

    // Ring Oscillator control
    // Reset value: 0x00000aa0
    BEGIN_TYPE(CTRL_t, uint32_t)
        // On power-up this field is initialised to ENABLE  The system clock must be switched to another source before setting this field to DISABLE otherwise the chip will lock up  The 12-bit code is intended to give some protection against accidental writes. An invalid setting will enable the oscillator.
        ADD_BITFIELD_RW(ENABLE, 12, 12)
        // Controls the number of delay stages in the ROSC ring  LOW uses stages 0 to 7  MEDIUM uses stages 2 to 7  HIGH uses stages 4 to 7  TOOHIGH uses stages 6 to 7 and should not be used because its frequency exceeds design specifications  The clock output will not glitch when changing the range up one step at a time  The clock output will glitch when changing the range down  Note: the values here are gray coded which is why HIGH comes before TOOHIGH
        ADD_BITFIELD_RW(FREQ_RANGE, 0, 12)
    END_TYPE()

    static const uint32_t CTRL_ENABLE__DISABLE = 3358;
    static const uint32_t CTRL_ENABLE__ENABLE = 4011;
    static const uint32_t CTRL_FREQ_RANGE__LOW = 4004;
    static const uint32_t CTRL_FREQ_RANGE__MEDIUM = 4005;
    static const uint32_t CTRL_FREQ_RANGE__HIGH = 4007;
    static const uint32_t CTRL_FREQ_RANGE__TOOHIGH = 4006;

    // The FREQA & FREQB registers control the frequency by controlling the drive strength of each stage  The drive strength has 4 levels determined by the number of bits set  Increasing the number of bits set increases the drive strength and increases the oscillation frequency  0 bits set is the default drive strength  1 bit set doubles the drive strength  2 bits set triples drive strength  3 bits set quadruples drive strength  For frequency randomisation set both DS0_RANDOM=1 & DS1_RANDOM=1
    // Reset value: 0x00000000
    BEGIN_TYPE(FREQA_t, uint32_t)
        // Set to 0x9696 to apply the settings  Any other value in this field will set all drive strengths to 0
        ADD_BITFIELD_RW(PASSWD, 16, 16)
        // Stage 3 drive strength
        ADD_BITFIELD_RW(DS3, 12, 3)
        // Stage 2 drive strength
        ADD_BITFIELD_RW(DS2, 8, 3)
        // Randomises the stage 1 drive strength
        ADD_BITFIELD_RW(DS1_RANDOM, 7, 1)
        // Stage 1 drive strength
        ADD_BITFIELD_RW(DS1, 4, 3)
        // Randomises the stage 0 drive strength
        ADD_BITFIELD_RW(DS0_RANDOM, 3, 1)
        // Stage 0 drive strength
        ADD_BITFIELD_RW(DS0, 0, 3)
    END_TYPE()

    static const uint32_t FREQA_PASSWD__PASS = 38550;

    // For a detailed description see freqa register
    // Reset value: 0x00000000
    BEGIN_TYPE(FREQB_t, uint32_t)
        // Set to 0x9696 to apply the settings  Any other value in this field will set all drive strengths to 0
        ADD_BITFIELD_RW(PASSWD, 16, 16)
        // Stage 7 drive strength
        ADD_BITFIELD_RW(DS7, 12, 3)
        // Stage 6 drive strength
        ADD_BITFIELD_RW(DS6, 8, 3)
        // Stage 5 drive strength
        ADD_BITFIELD_RW(DS5, 4, 3)
        // Stage 4 drive strength
        ADD_BITFIELD_RW(DS4, 0, 3)
    END_TYPE()

    static const uint32_t FREQB_PASSWD__PASS = 38550;

    // Loads a value to the LFSR randomiser
    // Reset value: 0x3f04b16d
    BEGIN_TYPE(RANDOM_t, uint32_t)
        ADD_BITFIELD_RW(SEED, 0, 32)
    END_TYPE()

    // Ring Oscillator pause control
    BEGIN_TYPE(DORMANT_t, uint32_t)
        // This is used to save power by pausing the ROSC  On power-up this field is initialised to WAKE  An invalid write will also select WAKE  Warning: setup the irq before selecting dormant mode
        ADD_BITFIELD_RW(DORMANT, 0, 32)
    END_TYPE()

    static const uint32_t DORMANT_DORMANT__dormant = 1668246881;
    static const uint32_t DORMANT_DORMANT__WAKE = 2002873189;

    // Controls the output divider
    BEGIN_TYPE(DIV_t, uint32_t)
        // set to 0xaa00 + div where  div = 0 divides by 128  div = 1-127 divides by div  any other value sets div=128  this register resets to div=32
        ADD_BITFIELD_RW(DIV, 0, 16)
    END_TYPE()

    static const uint32_t DIV_DIV__PASS = 43520;

    // Controls the phase shifted output
    // Reset value: 0x00000008
    BEGIN_TYPE(PHASE_t, uint32_t)
        // set to 0xaa  any other value enables the output with shift=0
        ADD_BITFIELD_RW(PASSWD, 4, 8)
        // enable the phase-shifted output  this can be changed on-the-fly
        ADD_BITFIELD_RW(ENABLE, 3, 1)
        // invert the phase-shifted output  this is ignored when div=1
        ADD_BITFIELD_RW(FLIP, 2, 1)
        // phase shift the phase-shifted output by SHIFT input clocks  this can be changed on-the-fly  must be set to 0 before setting div=1
        ADD_BITFIELD_RW(SHIFT, 0, 2)
    END_TYPE()

    // Ring Oscillator Status
    // Reset value: 0x00000000
    BEGIN_TYPE(STATUS_t, uint32_t)
        // Oscillator is running and stable
        ADD_BITFIELD_RO(STABLE, 31, 1)
        // An invalid value has been written to CTRL_ENABLE or CTRL_FREQ_RANGE or FREQA or FREQB or DIV or PHASE or DORMANT
        ADD_BITFIELD_RW(BADWRITE, 24, 1)
        // post-divider is running  this resets to 0 but transitions to 1 during chip startup
        ADD_BITFIELD_RO(DIV_RUNNING, 16, 1)
        // Oscillator is enabled but not necessarily running and stable  this resets to 0 but transitions to 1 during chip startup
        ADD_BITFIELD_RO(ENABLED, 12, 1)
    END_TYPE()

    // This just reads the state of the oscillator output so randomness is compromised if the ring oscillator is stopped or run at a harmonic of the bus frequency
    // Reset value: 0x00000001
    BEGIN_TYPE(RANDOMBIT_t, uint32_t)
        ADD_BITFIELD_RO(RANDOMBIT, 0, 1)
    END_TYPE()

    // A down counter running at the ROSC frequency which counts to zero and stops.  To start the counter write a non-zero value.  Can be used for short software pauses when setting up time sensitive hardware.
    // Reset value: 0x00000000
    BEGIN_TYPE(COUNT_t, uint32_t)
        ADD_BITFIELD_RW(COUNT, 0, 16)
    END_TYPE()

    struct ROSC_t {
        CTRL_t                        CTRL;
        FREQA_t                       FREQA;
        FREQB_t                       FREQB;
        RANDOM_t                      RANDOM;
        DORMANT_t                     DORMANT;
        DIV_t                         DIV;
        PHASE_t                       PHASE;
        STATUS_t                      STATUS;
        RANDOMBIT_t                   RANDOMBIT;
        COUNT_t                       COUNT;
    };

    static ROSC_t & ROSC = (*(ROSC_t *)0x400e8000);
    static ROSC_t & ROSC_XOR = (*(ROSC_t *)0x400e9000);
    static ROSC_t & ROSC_SET = (*(ROSC_t *)0x400ea000);
    static ROSC_t & ROSC_CLR = (*(ROSC_t *)0x400eb000);

} // _ROSC_

// Controls vreg, bor, lposc, chip resets & xosc startup, powman and provides scratch register for general use and for bootcode use
namespace _POWMAN_  {

    // Indicates a bad password has been used
    // Reset value: 0x00000000
    BEGIN_TYPE(BADPASSWD_t, uint32_t)
        ADD_BITFIELD_RW(BADPASSWD, 0, 1)
    END_TYPE()

    // Voltage Regulator Control
    // Reset value: 0x00008050
    BEGIN_TYPE(VREG_CTRL_t, uint32_t)
        // returns the regulator to its startup settings  0 - reset  1 - not reset (default)
        ADD_BITFIELD_RW(RST_N, 15, 1)
        // unlocks the VREG control interface after power up  0 - Locked (default)  1 - Unlocked  It cannot be relocked when it is unlocked.
        ADD_BITFIELD_RW(UNLOCK, 13, 1)
        // isolates the VREG control interface  0 - not isolated (default)  1 - isolated
        ADD_BITFIELD_RW(ISOLATE, 12, 1)
        // 0=not disabled, 1=enabled
        ADD_BITFIELD_RW(DISABLE_VOLTAGE_LIMIT, 8, 1)
        // high temperature protection threshold  regulator power transistors are disabled when junction temperature exceeds threshold  000 - 100C  001 - 105C  010 - 110C  011 - 115C  100 - 120C  101 - 125C  110 - 135C  111 - 150C
        ADD_BITFIELD_RW(HT_TH, 4, 3)
    END_TYPE()

    // Voltage Regulator Status
    // Reset value: 0x00000000
    BEGIN_TYPE(VREG_STS_t, uint32_t)
        // output regulation status  0=not in regulation, 1=in regulation
        ADD_BITFIELD_RO(VOUT_OK, 4, 1)
        // startup status  0=startup complete, 1=starting up
        ADD_BITFIELD_RO(STARTUP, 0, 1)
    END_TYPE()

    // Voltage Regulator Settings
    // Reset value: 0x000000b0
    BEGIN_TYPE(VREG_t, uint32_t)
        // regulator state is being updated  writes to the vreg register will be ignored when this field is set
        ADD_BITFIELD_RO(UPDATE_IN_PROGRESS, 15, 1)
        // output voltage select  the regulator output voltage is limited to 1.3V unless the voltage limit  is disabled using the disable_voltage_limit field in the vreg_ctrl register  00000 - 0.55V  00001 - 0.60V  00010 - 0.65V  00011 - 0.70V  00100 - 0.75V  00101 - 0.80V  00110 - 0.85V  00111 - 0.90V  01000 - 0.95V  01001 - 1.00V  01010 - 1.05V  01011 - 1.10V (default)  01100 - 1.15V  01101 - 1.20V  01110 - 1.25V  01111 - 1.30V  10000 - 1.35V  10001 - 1.40V  10010 - 1.50V  10011 - 1.60V  10100 - 1.65V  10101 - 1.70V  10110 - 1.80V  10111 - 1.90V  11000 - 2.00V  11001 - 2.35V  11010 - 2.50V  11011 - 2.65V  11100 - 2.80V  11101 - 3.00V  11110 - 3.15V  11111 - 3.30V
        ADD_BITFIELD_RW(VSEL, 4, 5)
        // high impedance mode select  0=not in high impedance mode, 1=in high impedance mode
        ADD_BITFIELD_RW(HIZ, 1, 1)
    END_TYPE()

    // Voltage Regulator Low Power Entry Settings
    // Reset value: 0x000000b4
    BEGIN_TYPE(VREG_LP_ENTRY_t, uint32_t)
        // output voltage select  the regulator output voltage is limited to 1.3V unless the voltage limit  is disabled using the disable_voltage_limit field in the vreg_ctrl register  00000 - 0.55V  00001 - 0.60V  00010 - 0.65V  00011 - 0.70V  00100 - 0.75V  00101 - 0.80V  00110 - 0.85V  00111 - 0.90V  01000 - 0.95V  01001 - 1.00V  01010 - 1.05V  01011 - 1.10V (default)  01100 - 1.15V  01101 - 1.20V  01110 - 1.25V  01111 - 1.30V  10000 - 1.35V  10001 - 1.40V  10010 - 1.50V  10011 - 1.60V  10100 - 1.65V  10101 - 1.70V  10110 - 1.80V  10111 - 1.90V  11000 - 2.00V  11001 - 2.35V  11010 - 2.50V  11011 - 2.65V  11100 - 2.80V  11101 - 3.00V  11110 - 3.15V  11111 - 3.30V
        ADD_BITFIELD_RW(VSEL, 4, 5)
        // selects either normal (switching) mode or low power (linear) mode  low power mode can only be selected for output voltages up to 1.3V  0 = normal mode (switching)  1 = low power mode (linear)
        ADD_BITFIELD_RW(MODE, 2, 1)
        // high impedance mode select  0=not in high impedance mode, 1=in high impedance mode
        ADD_BITFIELD_RW(HIZ, 1, 1)
    END_TYPE()

    // Voltage Regulator Low Power Exit Settings
    // Reset value: 0x000000b0
    BEGIN_TYPE(VREG_LP_EXIT_t, uint32_t)
        // output voltage select  the regulator output voltage is limited to 1.3V unless the voltage limit  is disabled using the disable_voltage_limit field in the vreg_ctrl register  00000 - 0.55V  00001 - 0.60V  00010 - 0.65V  00011 - 0.70V  00100 - 0.75V  00101 - 0.80V  00110 - 0.85V  00111 - 0.90V  01000 - 0.95V  01001 - 1.00V  01010 - 1.05V  01011 - 1.10V (default)  01100 - 1.15V  01101 - 1.20V  01110 - 1.25V  01111 - 1.30V  10000 - 1.35V  10001 - 1.40V  10010 - 1.50V  10011 - 1.60V  10100 - 1.65V  10101 - 1.70V  10110 - 1.80V  10111 - 1.90V  11000 - 2.00V  11001 - 2.35V  11010 - 2.50V  11011 - 2.65V  11100 - 2.80V  11101 - 3.00V  11110 - 3.15V  11111 - 3.30V
        ADD_BITFIELD_RW(VSEL, 4, 5)
        // selects either normal (switching) mode or low power (linear) mode  low power mode can only be selected for output voltages up to 1.3V  0 = normal mode (switching)  1 = low power mode (linear)
        ADD_BITFIELD_RW(MODE, 2, 1)
        // high impedance mode select  0=not in high impedance mode, 1=in high impedance mode
        ADD_BITFIELD_RW(HIZ, 1, 1)
    END_TYPE()

    // Brown-out Detection Control
    // Reset value: 0x00000000
    BEGIN_TYPE(BOD_CTRL_t, uint32_t)
        // isolates the brown-out detection control interface  0 - not isolated (default)  1 - isolated
        ADD_BITFIELD_RW(ISOLATE, 12, 1)
    END_TYPE()

    // Brown-out Detection Settings
    // Reset value: 0x000000b1
    BEGIN_TYPE(BOD_t, uint32_t)
        // threshold select  00000 - 0.473V  00001 - 0.516V  00010 - 0.559V  00011 - 0.602V  00100 - 0.645VS  00101 - 0.688V  00110 - 0.731V  00111 - 0.774V  01000 - 0.817V  01001 - 0.860V (default)  01010 - 0.903V  01011 - 0.946V  01100 - 0.989V  01101 - 1.032V  01110 - 1.075V  01111 - 1.118V  10000 - 1.161  10001 - 1.204V
        ADD_BITFIELD_RW(VSEL, 4, 5)
        // enable brown-out detection  0=not enabled, 1=enabled
        ADD_BITFIELD_RW(EN, 0, 1)
    END_TYPE()

    // Brown-out Detection Low Power Entry Settings
    // Reset value: 0x000000b0
    BEGIN_TYPE(BOD_LP_ENTRY_t, uint32_t)
        // threshold select  00000 - 0.473V  00001 - 0.516V  00010 - 0.559V  00011 - 0.602V  00100 - 0.645VS  00101 - 0.688V  00110 - 0.731V  00111 - 0.774V  01000 - 0.817V  01001 - 0.860V (default)  01010 - 0.903V  01011 - 0.946V  01100 - 0.989V  01101 - 1.032V  01110 - 1.075V  01111 - 1.118V  10000 - 1.161  10001 - 1.204V
        ADD_BITFIELD_RW(VSEL, 4, 5)
        // enable brown-out detection  0=not enabled, 1=enabled
        ADD_BITFIELD_RW(EN, 0, 1)
    END_TYPE()

    // Brown-out Detection Low Power Exit Settings
    // Reset value: 0x000000b1
    BEGIN_TYPE(BOD_LP_EXIT_t, uint32_t)
        // threshold select  00000 - 0.473V  00001 - 0.516V  00010 - 0.559V  00011 - 0.602V  00100 - 0.645VS  00101 - 0.688V  00110 - 0.731V  00111 - 0.774V  01000 - 0.817V  01001 - 0.860V (default)  01010 - 0.903V  01011 - 0.946V  01100 - 0.989V  01101 - 1.032V  01110 - 1.075V  01111 - 1.118V  10000 - 1.161  10001 - 1.204V
        ADD_BITFIELD_RW(VSEL, 4, 5)
        // enable brown-out detection  0=not enabled, 1=enabled
        ADD_BITFIELD_RW(EN, 0, 1)
    END_TYPE()

    // Low power oscillator control register.
    // Reset value: 0x00000203
    BEGIN_TYPE(LPOSC_t, uint32_t)
        // Frequency trim - the trim step is typically 1% of the reset frequency, but can be up to 3%
        ADD_BITFIELD_RW(TRIM, 4, 6)
        // This feature has been removed
        ADD_BITFIELD_RW(MODE, 0, 2)
    END_TYPE()

    // Chip reset control and status
    // Reset value: 0x00000000
    BEGIN_TYPE(CHIP_RESET_t, uint32_t)
        // Last reset was a watchdog timeout which was configured to reset the power-on state machine  This resets:  double_tap flag    no  DP                 no  RPAP               no  rescue_flag        no  timer              no  powman             no  swcore             no  psm                yes  and does not change the power state
        ADD_BITFIELD_RO(HAD_WATCHDOG_RESET_RSM, 28, 1)
        // Last reset was a system reset from the hazard debugger  This resets:  double_tap flag    no  DP                 no  RPAP               no  rescue_flag        no  timer              no  powman             no  swcore             no  psm                yes  and does not change the power state
        ADD_BITFIELD_RO(HAD_HZD_SYS_RESET_REQ, 27, 1)
        // Last reset was due to a power supply glitch  This resets:  double_tap flag    no  DP                 no  RPAP               no  rescue_flag        no  timer              no  powman             no  swcore             no  psm                yes  and does not change the power state
        ADD_BITFIELD_RO(HAD_GLITCH_DETECT, 26, 1)
        // Last reset was a switched core powerdown  This resets:  double_tap flag    no  DP                 no  RPAP               no  rescue_flag        no  timer              no  powman             no  swcore             yes  psm                yes  then starts the power sequencer
        ADD_BITFIELD_RO(HAD_SWCORE_PD, 25, 1)
        // Last reset was a watchdog timeout which was configured to reset the switched-core  This resets:  double_tap flag    no  DP                 no  RPAP               no  rescue_flag        no  timer              no  powman             no  swcore             yes  psm                yes  then starts the power sequencer
        ADD_BITFIELD_RO(HAD_WATCHDOG_RESET_SWCORE, 24, 1)
        // Last reset was a watchdog timeout which was configured to reset the power manager  This resets:  double_tap flag    no  DP                 no  RPAP               no  rescue_flag        no  timer              yes  powman             yes  swcore             yes  psm                yes  then starts the power sequencer
        ADD_BITFIELD_RO(HAD_WATCHDOG_RESET_POWMAN, 23, 1)
        // Last reset was a watchdog timeout which was configured to reset the power manager asynchronously  This resets:  double_tap flag    no  DP                 no  RPAP               no  rescue_flag        no  timer              yes  powman             yes  swcore             yes  psm                yes  then starts the power sequencer
        ADD_BITFIELD_RO(HAD_WATCHDOG_RESET_POWMAN_ASYNC, 22, 1)
        // Last reset was a rescue reset from the debugger  This resets:  double_tap flag    no  DP                 no  RPAP               no  rescue_flag        no, it sets this flag  timer              yes  powman             yes  swcore             yes  psm                yes  then starts the power sequencer
        ADD_BITFIELD_RO(HAD_RESCUE, 21, 1)
        // Last reset was an reset request from the arm debugger  This resets:  double_tap flag    no  DP                 no  RPAP               no  rescue_flag        yes  timer              yes  powman             yes  swcore             yes  psm                yes  then starts the power sequencer
        ADD_BITFIELD_RO(HAD_DP_RESET_REQ, 19, 1)
        // Last reset was from the RUN pin  This resets:  double_tap flag    no  DP                 yes  RPAP               yes  rescue_flag        yes  timer              yes  powman             yes  swcore             yes  psm                yes  then starts the power sequencer
        ADD_BITFIELD_RO(HAD_RUN_LOW, 18, 1)
        // Last reset was from the brown-out detection block  This resets:  double_tap flag    yes  DP                 yes  RPAP               yes  rescue_flag        yes  timer              yes  powman             yes  swcore             yes  psm                yes  then starts the power sequencer
        ADD_BITFIELD_RO(HAD_BOR, 17, 1)
        // Last reset was from the power-on reset  This resets:  double_tap flag    yes  DP                 yes  RPAP               yes  rescue_flag        yes  timer              yes  powman             yes  swcore             yes  psm                yes  then starts the power sequencer
        ADD_BITFIELD_RO(HAD_POR, 16, 1)
        // This is set by a rescue reset from the RP-AP.  Its purpose is to halt before the bootrom before booting from flash in order to recover from a boot lock-up.  The debugger can then attach once the bootrom has been halted and flash some working code that does not lock up.
        ADD_BITFIELD_RW(RESCUE_FLAG, 4, 1)
        // This flag is set by double-tapping RUN. It tells bootcode to go into the bootloader.
        ADD_BITFIELD_RW(DOUBLE_TAP, 0, 1)
    END_TYPE()

    // Allows a watchdog reset to reset the internal state of powman in addition to the power-on state machine (PSM).  Note that powman ignores watchdog resets that do not select at least the CLOCKS stage or earlier stages in the PSM. If using these bits, it's recommended to set PSM_WDSEL to all-ones in addition to the desired bits in this register. Failing to select CLOCKS or earlier will result in the POWMAN_WDSEL register having no effect.
    // Reset value: 0x00000000
    BEGIN_TYPE(WDSEL_t, uint32_t)
        // If set to 1, a watchdog reset will run the full power-on state machine (PSM) sequence  From a user perspective it is the same as setting RSM_WDSEL_PROC_COLD  From a hardware debug perspective it has the same effect as a reset from a glitch detector
        ADD_BITFIELD_RW(RESET_RSM, 12, 1)
        // If set to 1, a watchdog reset will reset the switched core power domain and run the full power-on state machine (PSM) sequence  From a user perspective it is the same as setting RSM_WDSEL_PROC_COLD  From a hardware debug perspective it has the same effect as a power-on reset for the switched core power domain
        ADD_BITFIELD_RW(RESET_SWCORE, 8, 1)
        // If set to 1, a watchdog reset will restore powman defaults, reset the timer, reset the switched core power domain  and run the full power-on state machine (PSM) sequence  This relies on clk_ref running. Use reset_powman_async if that may not be true
        ADD_BITFIELD_RW(RESET_POWMAN, 4, 1)
        // If set to 1, a watchdog reset will restore powman defaults, reset the timer,  reset the switched core domain and run the full power-on state machine (PSM) sequence  This does not rely on clk_ref running
        ADD_BITFIELD_RW(RESET_POWMAN_ASYNC, 0, 1)
    END_TYPE()

    // For configuration of the power sequencer  Writes are ignored while POWMAN_STATE_CHANGING=1
    // Reset value: 0x001011f0
    BEGIN_TYPE(SEQ_CFG_t, uint32_t)
        // 0 indicates the POWMAN clock is running from the low power oscillator (32kHz)  1 indicates the POWMAN clock is running from the reference clock (2-50MHz)
        ADD_BITFIELD_RO(USING_FAST_POWCK, 20, 1)
        // Indicates the brown-out detector (BOD) mode  0 = BOD high power mode which is the default  1 = BOD low power mode
        ADD_BITFIELD_RO(USING_BOD_LP, 17, 1)
        // Indicates the voltage regulator (VREG) mode  0 = VREG high power mode which is the default  1 = VREG low power mode
        ADD_BITFIELD_RO(USING_VREG_LP, 16, 1)
        // selects the reference clock (clk_ref) as the source of the POWMAN clock when switched-core is powered. The POWMAN clock always switches to the slow clock (lposc) when switched-core is powered down because the fast clock stops running.  0 always run the POWMAN clock from the slow clock (lposc)  1 run the POWMAN clock from the fast clock when available  This setting takes effect when a power up sequence is next run
        ADD_BITFIELD_RW(USE_FAST_POWCK, 12, 1)
        // Set to 0 to stop the low power osc when the switched-core is powered down, which is unwise if using it to clock the timer  This setting takes effect when the swcore is next powered down
        ADD_BITFIELD_RW(RUN_LPOSC_IN_LP, 8, 1)
        // Set to 0 to prevent automatic switching to bod high power mode when switched-core is powered up  This setting takes effect when the swcore is next powered up
        ADD_BITFIELD_RW(USE_BOD_HP, 7, 1)
        // Set to 0 to prevent automatic switching to bod low power mode when switched-core is powered down  This setting takes effect when the swcore is next powered down
        ADD_BITFIELD_RW(USE_BOD_LP, 6, 1)
        // Set to 0 to prevent automatic switching to vreg high power mode when switched-core is powered up  This setting takes effect when the swcore is next powered up
        ADD_BITFIELD_RW(USE_VREG_HP, 5, 1)
        // Set to 0 to prevent automatic switching to vreg low power mode when switched-core is powered down  This setting takes effect when the swcore is next powered down
        ADD_BITFIELD_RW(USE_VREG_LP, 4, 1)
        // Specifies the power state of SRAM0 when powering up swcore from a low power state (P1.xxx) to a high power state (P0.0xx).  0=power-up  1=no change
        ADD_BITFIELD_RW(HW_PWRUP_SRAM0, 1, 1)
        // Specifies the power state of SRAM1 when powering up swcore from a low power state (P1.xxx) to a high power state (P0.0xx).  0=power-up  1=no change
        ADD_BITFIELD_RW(HW_PWRUP_SRAM1, 0, 1)
    END_TYPE()

    // This register controls the power state of the 4 power domains.  The current power state is indicated in POWMAN_STATE_CURRENT which is read-only.  To change the state, write to POWMAN_STATE_REQ.  The coding of POWMAN_STATE_CURRENT & POWMAN_STATE_REQ corresponds to the power states  defined in the datasheet:  bit 3 = SWCORE  bit 2 = XIP cache  bit 1 = SRAM0  bit 0 = SRAM1  0 = powered up  1 = powered down  When POWMAN_STATE_REQ is written, the POWMAN_STATE_WAITING flag is set while the Power Manager determines what is required. If an invalid transition is requested the Power Manager will still register the request in POWMAN_STATE_REQ but will also set the POWMAN_BAD_REQ flag. It will then implement the power-up requests and ignore the power down requests. To do nothing would risk entering an unrecoverable lock-up state. Invalid requests are: any combination of power up and power down requests any request that results in swcore boing powered and xip unpowered If the request is to power down the switched-core domain then POWMAN_STATE_WAITING stays active until the processors halt. During this time the POWMAN_STATE_REQ field can be re-written to change or cancel the request. When the power state transition begins the POWMAN_STATE_WAITING_flag is cleared, the POWMAN_STATE_CHANGING flag is set and POWMAN register writes are ignored until the transition completes.
    // Reset value: 0x0000000f
    BEGIN_TYPE(STATE_t, uint32_t)
        ADD_BITFIELD_RO(CHANGING, 13, 1)
        ADD_BITFIELD_RO(WAITING, 12, 1)
        // Bad hardware initiated state request. Went back to state 0 (i.e. everything powered up)
        ADD_BITFIELD_RO(BAD_HW_REQ, 11, 1)
        // Bad software initiated state request. No action taken.
        ADD_BITFIELD_RO(BAD_SW_REQ, 10, 1)
        // Request ignored because of a pending pwrup request. See current_pwrup_req. Note this blocks powering up AND powering down.
        ADD_BITFIELD_RW(PWRUP_WHILE_WAITING, 9, 1)
        ADD_BITFIELD_RW(REQ_IGNORED, 8, 1)
        ADD_BITFIELD_RW(REQ, 4, 4)
        ADD_BITFIELD_RO(CURRENT, 0, 4)
    END_TYPE()

    // Reset value: 0x00000040
    BEGIN_TYPE(POW_FASTDIV_t, uint32_t)
        // divides the POWMAN clock to provide a tick for the delay module and state machines  when clk_pow is running from the slow clock it is not divided  when clk_pow is running from the fast clock it is divided by tick_div
        ADD_BITFIELD_RW(POW_FASTDIV, 0, 11)
    END_TYPE()

    // power state machine delays
    // Reset value: 0x00002011
    BEGIN_TYPE(POW_DELAY_t, uint32_t)
        // timing between the sram0 and sram1 power state machine steps  measured in units of the powman tick period (>=1us), 0 gives a delay of 1 unit
        ADD_BITFIELD_RW(SRAM_STEP, 8, 8)
        // timing between the xip power state machine steps  measured in units of the lposc period, 0 gives a delay of 1 unit
        ADD_BITFIELD_RW(XIP_STEP, 4, 4)
        // timing between the swcore power state machine steps  measured in units of the lposc period, 0 gives a delay of 1 unit
        ADD_BITFIELD_RW(SWCORE_STEP, 0, 4)
    END_TYPE()

    // Configures a gpio as a power mode aware control output
    // Reset value: 0x0000003f
    BEGIN_TYPE(EXT_CTRL0_t, uint32_t)
        // output level when exiting the low power state
        ADD_BITFIELD_RW(LP_EXIT_STATE, 14, 1)
        // output level when entering the low power state
        ADD_BITFIELD_RW(LP_ENTRY_STATE, 13, 1)
        ADD_BITFIELD_RW(INIT_STATE, 12, 1)
        ADD_BITFIELD_RW(INIT, 8, 1)
        // selects from gpio 0->30  set to 31 to disable this feature
        ADD_BITFIELD_RW(GPIO_SELECT, 0, 6)
    END_TYPE()

    // Configures a gpio as a power mode aware control output
    // Reset value: 0x0000003f
    BEGIN_TYPE(EXT_CTRL1_t, uint32_t)
        // output level when exiting the low power state
        ADD_BITFIELD_RW(LP_EXIT_STATE, 14, 1)
        // output level when entering the low power state
        ADD_BITFIELD_RW(LP_ENTRY_STATE, 13, 1)
        ADD_BITFIELD_RW(INIT_STATE, 12, 1)
        ADD_BITFIELD_RW(INIT, 8, 1)
        // selects from gpio 0->30  set to 31 to disable this feature
        ADD_BITFIELD_RW(GPIO_SELECT, 0, 6)
    END_TYPE()

    // Select a GPIO to use as a time reference, the source can be used to drive the low power clock at 32kHz, or to provide a 1ms tick to the timer, or provide a 1Hz tick to the timer. The tick selection is controlled by the POWMAN_TIMER register.
    // Reset value: 0x00000000
    BEGIN_TYPE(EXT_TIME_REF_t, uint32_t)
        // Use the selected GPIO to drive the 32kHz low power clock, in place of LPOSC. This field must only be written when POWMAN_TIMER_RUN=0
        ADD_BITFIELD_RW(DRIVE_LPCK, 4, 1)
        // 0 ->  gpio12  1 ->  gpio20  2 ->  gpio14  3 ->  gpio22
        ADD_BITFIELD_RW(SOURCE_SEL, 0, 2)
    END_TYPE()

    // Informs the AON Timer of the integer component of the clock frequency when running off the LPOSC.
    // Reset value: 0x00000020
    BEGIN_TYPE(LPOSC_FREQ_KHZ_INT_t, uint32_t)
        // Integer component of the LPOSC or GPIO clock source frequency in kHz. Default = 32 This field must only be written when POWMAN_TIMER_RUN=0 or POWMAN_TIMER_USING_XOSC=1
        ADD_BITFIELD_RW(LPOSC_FREQ_KHZ_INT, 0, 6)
    END_TYPE()

    // Informs the AON Timer of the fractional component of the clock frequency when running off the LPOSC.
    // Reset value: 0x0000c49c
    BEGIN_TYPE(LPOSC_FREQ_KHZ_FRAC_t, uint32_t)
        // Fractional component of the LPOSC or GPIO clock source frequency in kHz. Default = 0.768 This field must only be written when POWMAN_TIMER_RUN=0 or POWMAN_TIMER_USING_XOSC=1
        ADD_BITFIELD_RW(LPOSC_FREQ_KHZ_FRAC, 0, 16)
    END_TYPE()

    // Informs the AON Timer of the integer component of the clock frequency when running off the XOSC.
    // Reset value: 0x00002ee0
    BEGIN_TYPE(XOSC_FREQ_KHZ_INT_t, uint32_t)
        // Integer component of the XOSC frequency in kHz. Default = 12000 Must be >1 This field must only be written when POWMAN_TIMER_RUN=0 or POWMAN_TIMER_USING_XOSC=0
        ADD_BITFIELD_RW(XOSC_FREQ_KHZ_INT, 0, 16)
    END_TYPE()

    // Informs the AON Timer of the fractional component of the clock frequency when running off the XOSC.
    // Reset value: 0x00000000
    BEGIN_TYPE(XOSC_FREQ_KHZ_FRAC_t, uint32_t)
        // Fractional component of the XOSC frequency in kHz. This field must only be written when POWMAN_TIMER_RUN=0 or POWMAN_TIMER_USING_XOSC=0
        ADD_BITFIELD_RW(XOSC_FREQ_KHZ_FRAC, 0, 16)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(SET_TIME_63TO48_t, uint32_t)
        // For setting the time, do not use for reading the time, use POWMAN_READ_TIME_UPPER and POWMAN_READ_TIME_LOWER. This field must only be written when POWMAN_TIMER_RUN=0
        ADD_BITFIELD_RW(SET_TIME_63TO48, 0, 16)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(SET_TIME_47TO32_t, uint32_t)
        // For setting the time, do not use for reading the time, use POWMAN_READ_TIME_UPPER and POWMAN_READ_TIME_LOWER. This field must only be written when POWMAN_TIMER_RUN=0
        ADD_BITFIELD_RW(SET_TIME_47TO32, 0, 16)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(SET_TIME_31TO16_t, uint32_t)
        // For setting the time, do not use for reading the time, use POWMAN_READ_TIME_UPPER and POWMAN_READ_TIME_LOWER. This field must only be written when POWMAN_TIMER_RUN=0
        ADD_BITFIELD_RW(SET_TIME_31TO16, 0, 16)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(SET_TIME_15TO0_t, uint32_t)
        // For setting the time, do not use for reading the time, use POWMAN_READ_TIME_UPPER and POWMAN_READ_TIME_LOWER. This field must only be written when POWMAN_TIMER_RUN=0
        ADD_BITFIELD_RW(SET_TIME_15TO0, 0, 16)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(READ_TIME_UPPER_t, uint32_t)
        // For reading bits 63:32 of the timer. When reading all 64 bits it is possible for the LOWER count to rollover during the read. It is recommended to read UPPER, then LOWER, then re-read UPPER and, if it has changed, re-read LOWER.
        ADD_BITFIELD_RO(READ_TIME_UPPER, 0, 32)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(READ_TIME_LOWER_t, uint32_t)
        // For reading bits 31:0 of the timer.
        ADD_BITFIELD_RO(READ_TIME_LOWER, 0, 32)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(ALARM_TIME_63TO48_t, uint32_t)
        // This field must only be written when POWMAN_ALARM_ENAB=0
        ADD_BITFIELD_RW(ALARM_TIME_63TO48, 0, 16)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(ALARM_TIME_47TO32_t, uint32_t)
        // This field must only be written when POWMAN_ALARM_ENAB=0
        ADD_BITFIELD_RW(ALARM_TIME_47TO32, 0, 16)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(ALARM_TIME_31TO16_t, uint32_t)
        // This field must only be written when POWMAN_ALARM_ENAB=0
        ADD_BITFIELD_RW(ALARM_TIME_31TO16, 0, 16)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(ALARM_TIME_15TO0_t, uint32_t)
        // This field must only be written when POWMAN_ALARM_ENAB=0
        ADD_BITFIELD_RW(ALARM_TIME_15TO0, 0, 16)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(TIMER_t, uint32_t)
        // Timer is synchronised to a 1hz gpio source
        ADD_BITFIELD_RO(USING_GPIO_1HZ, 19, 1)
        // Timer is running from a 1khz gpio source
        ADD_BITFIELD_RO(USING_GPIO_1KHZ, 18, 1)
        // Timer is running from lposc
        ADD_BITFIELD_RO(USING_LPOSC, 17, 1)
        // Timer is running from xosc
        ADD_BITFIELD_RO(USING_XOSC, 16, 1)
        // Selects the gpio source as the reference for the sec counter. The msec counter will continue to use the lposc or xosc reference.
        ADD_BITFIELD_RW(USE_GPIO_1HZ, 13, 1)
        // switch to gpio as the source of the 1kHz timer tick
        ADD_BITFIELD_WO(USE_GPIO_1KHZ, 10, 1)
        // switch to xosc as the source of the 1kHz timer tick
        ADD_BITFIELD_WO(USE_XOSC, 9, 1)
        // Switch to lposc as the source of the 1kHz timer tick
        ADD_BITFIELD_WO(USE_LPOSC, 8, 1)
        // Alarm has fired. Write to 1 to clear the alarm.
        ADD_BITFIELD_RW(ALARM, 6, 1)
        // Alarm wakes the chip from low power mode
        ADD_BITFIELD_RW(PWRUP_ON_ALARM, 5, 1)
        // Enables the alarm. The alarm must be disabled while writing the alarm time.
        ADD_BITFIELD_RW(ALARM_ENAB, 4, 1)
        // Clears the timer, does not disable the timer and does not affect the alarm. This control can be written at any time.
        ADD_BITFIELD_WO(CLEAR, 2, 1)
        // Timer enable. Setting this bit causes the timer to begin counting up from its current value. Clearing this bit stops the timer from counting. Before enabling the timer, set the POWMAN_LPOSC_FREQ* and POWMAN_XOSC_FREQ* registers to configure the count rate, and initialise the current time by writing to SET_TIME_63TO48 through SET_TIME_15TO0. You must not write to the SET_TIME_x registers when the timer is running. Once configured, start the timer by setting POWMAN_TIMER_RUN=1. This will start the timer running from the LPOSC. When the XOSC is available switch the reference clock to XOSC then select it as the timer clock by setting POWMAN_TIMER_USE_XOSC=1
        ADD_BITFIELD_RW(RUN, 1, 1)
        // Control whether Non-secure software can write to the timer registers. All other registers are hardwired to be inaccessible to Non-secure.
        ADD_BITFIELD_RW(NONSEC_WRITE, 0, 1)
    END_TYPE()

    // 4 GPIO powerup events can be configured to wake the chip up from a low power state.  The pwrups are level/edge sensitive and can be set to trigger on a high/rising or low/falling event  The number of gpios available depends on the package option. An invalid selection will be ignored  source = 0 selects gpio0  .  .  source = 47 selects gpio47  source = 48 selects qspi_ss  source = 49 selects qspi_sd0  source = 50 selects qspi_sd1  source = 51 selects qspi_sd2  source = 52 selects qspi_sd3  source = 53 selects qspi_sclk  level  = 0 triggers the pwrup when the source is low  level  = 1 triggers the pwrup when the source is high
    // Reset value: 0x0000003f
    BEGIN_TYPE(PWRUP0_t, uint32_t)
        // Value of selected gpio pin (only if enable == 1)
        ADD_BITFIELD_RO(RAW_STATUS, 10, 1)
        // Status of gpio wakeup. Write to 1 to clear a latched edge detect.
        ADD_BITFIELD_RW(STATUS, 9, 1)
        // Edge or level detect. Edge will detect a 0 to 1 transition (or 1 to 0 transition). Level will detect a 1 or 0. Both types of event get latched into the current_pwrup_req register.
        ADD_BITFIELD_RW(MODE, 8, 1)
        ADD_BITFIELD_RW(DIRECTION, 7, 1)
        // Set to 1 to enable the wakeup source. Set to 0 to disable the wakeup source and clear a pending wakeup event.  If using edge detect a latched edge needs to be cleared by writing 1 to the status register also.
        ADD_BITFIELD_RW(ENABLE, 6, 1)
        ADD_BITFIELD_RW(SOURCE, 0, 6)
    END_TYPE()

    static const uint32_t PWRUP0_MODE__level = 0;
    static const uint32_t PWRUP0_MODE__edge = 1;
    static const uint32_t PWRUP0_DIRECTION__low_falling = 0;
    static const uint32_t PWRUP0_DIRECTION__high_rising = 1;

    // 4 GPIO powerup events can be configured to wake the chip up from a low power state.  The pwrups are level/edge sensitive and can be set to trigger on a high/rising or low/falling event  The number of gpios available depends on the package option. An invalid selection will be ignored  source = 0 selects gpio0  .  .  source = 47 selects gpio47  source = 48 selects qspi_ss  source = 49 selects qspi_sd0  source = 50 selects qspi_sd1  source = 51 selects qspi_sd2  source = 52 selects qspi_sd3  source = 53 selects qspi_sclk  level  = 0 triggers the pwrup when the source is low  level  = 1 triggers the pwrup when the source is high
    // Reset value: 0x0000003f
    BEGIN_TYPE(PWRUP1_t, uint32_t)
        // Value of selected gpio pin (only if enable == 1)
        ADD_BITFIELD_RO(RAW_STATUS, 10, 1)
        // Status of gpio wakeup. Write to 1 to clear a latched edge detect.
        ADD_BITFIELD_RW(STATUS, 9, 1)
        // Edge or level detect. Edge will detect a 0 to 1 transition (or 1 to 0 transition). Level will detect a 1 or 0. Both types of event get latched into the current_pwrup_req register.
        ADD_BITFIELD_RW(MODE, 8, 1)
        ADD_BITFIELD_RW(DIRECTION, 7, 1)
        // Set to 1 to enable the wakeup source. Set to 0 to disable the wakeup source and clear a pending wakeup event.  If using edge detect a latched edge needs to be cleared by writing 1 to the status register also.
        ADD_BITFIELD_RW(ENABLE, 6, 1)
        ADD_BITFIELD_RW(SOURCE, 0, 6)
    END_TYPE()

    static const uint32_t PWRUP1_MODE__level = 0;
    static const uint32_t PWRUP1_MODE__edge = 1;
    static const uint32_t PWRUP1_DIRECTION__low_falling = 0;
    static const uint32_t PWRUP1_DIRECTION__high_rising = 1;

    // 4 GPIO powerup events can be configured to wake the chip up from a low power state.  The pwrups are level/edge sensitive and can be set to trigger on a high/rising or low/falling event  The number of gpios available depends on the package option. An invalid selection will be ignored  source = 0 selects gpio0  .  .  source = 47 selects gpio47  source = 48 selects qspi_ss  source = 49 selects qspi_sd0  source = 50 selects qspi_sd1  source = 51 selects qspi_sd2  source = 52 selects qspi_sd3  source = 53 selects qspi_sclk  level  = 0 triggers the pwrup when the source is low  level  = 1 triggers the pwrup when the source is high
    // Reset value: 0x0000003f
    BEGIN_TYPE(PWRUP2_t, uint32_t)
        // Value of selected gpio pin (only if enable == 1)
        ADD_BITFIELD_RO(RAW_STATUS, 10, 1)
        // Status of gpio wakeup. Write to 1 to clear a latched edge detect.
        ADD_BITFIELD_RW(STATUS, 9, 1)
        // Edge or level detect. Edge will detect a 0 to 1 transition (or 1 to 0 transition). Level will detect a 1 or 0. Both types of event get latched into the current_pwrup_req register.
        ADD_BITFIELD_RW(MODE, 8, 1)
        ADD_BITFIELD_RW(DIRECTION, 7, 1)
        // Set to 1 to enable the wakeup source. Set to 0 to disable the wakeup source and clear a pending wakeup event.  If using edge detect a latched edge needs to be cleared by writing 1 to the status register also.
        ADD_BITFIELD_RW(ENABLE, 6, 1)
        ADD_BITFIELD_RW(SOURCE, 0, 6)
    END_TYPE()

    static const uint32_t PWRUP2_MODE__level = 0;
    static const uint32_t PWRUP2_MODE__edge = 1;
    static const uint32_t PWRUP2_DIRECTION__low_falling = 0;
    static const uint32_t PWRUP2_DIRECTION__high_rising = 1;

    // 4 GPIO powerup events can be configured to wake the chip up from a low power state.  The pwrups are level/edge sensitive and can be set to trigger on a high/rising or low/falling event  The number of gpios available depends on the package option. An invalid selection will be ignored  source = 0 selects gpio0  .  .  source = 47 selects gpio47  source = 48 selects qspi_ss  source = 49 selects qspi_sd0  source = 50 selects qspi_sd1  source = 51 selects qspi_sd2  source = 52 selects qspi_sd3  source = 53 selects qspi_sclk  level  = 0 triggers the pwrup when the source is low  level  = 1 triggers the pwrup when the source is high
    // Reset value: 0x0000003f
    BEGIN_TYPE(PWRUP3_t, uint32_t)
        // Value of selected gpio pin (only if enable == 1)
        ADD_BITFIELD_RO(RAW_STATUS, 10, 1)
        // Status of gpio wakeup. Write to 1 to clear a latched edge detect.
        ADD_BITFIELD_RW(STATUS, 9, 1)
        // Edge or level detect. Edge will detect a 0 to 1 transition (or 1 to 0 transition). Level will detect a 1 or 0. Both types of event get latched into the current_pwrup_req register.
        ADD_BITFIELD_RW(MODE, 8, 1)
        ADD_BITFIELD_RW(DIRECTION, 7, 1)
        // Set to 1 to enable the wakeup source. Set to 0 to disable the wakeup source and clear a pending wakeup event.  If using edge detect a latched edge needs to be cleared by writing 1 to the status register also.
        ADD_BITFIELD_RW(ENABLE, 6, 1)
        ADD_BITFIELD_RW(SOURCE, 0, 6)
    END_TYPE()

    static const uint32_t PWRUP3_MODE__level = 0;
    static const uint32_t PWRUP3_MODE__edge = 1;
    static const uint32_t PWRUP3_DIRECTION__low_falling = 0;
    static const uint32_t PWRUP3_DIRECTION__high_rising = 1;

    // Indicates current powerup request state  pwrup events can be cleared by removing the enable from the pwrup register. The alarm pwrup req can be cleared by clearing timer.alarm_enab  0 = chip reset, for the source of the last reset see POWMAN_CHIP_RESET  1 = pwrup0  2 = pwrup1  3 = pwrup2  4 = pwrup3  5 = coresight_pwrup  6 = alarm_pwrup
    // Reset value: 0x00000000
    BEGIN_TYPE(CURRENT_PWRUP_REQ_t, uint32_t)
        ADD_BITFIELD_RO(CURRENT_PWRUP_REQ, 0, 7)
    END_TYPE()

    // Indicates which pwrup source triggered the last switched-core power up  0 = chip reset, for the source of the last reset see POWMAN_CHIP_RESET  1 = pwrup0  2 = pwrup1  3 = pwrup2  4 = pwrup3  5 = coresight_pwrup  6 = alarm_pwrup
    // Reset value: 0x00000000
    BEGIN_TYPE(LAST_SWCORE_PWRUP_t, uint32_t)
        ADD_BITFIELD_RO(LAST_SWCORE_PWRUP, 0, 7)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(DBG_PWRCFG_t, uint32_t)
        // Ignore pwrup req from debugger. If pwrup req is asserted then this will prevent power down and set powerdown blocked. Set ignore to stop paying attention to pwrup_req
        ADD_BITFIELD_RW(IGNORE, 0, 1)
    END_TYPE()

    // Tell the bootrom to ignore the BOOT0..3 registers following the next RSM reset (e.g. the next core power down/up). If an early boot stage has soft-locked some OTP pages in order to protect their contents from later stages, there is a risk that Secure code running at a later stage can unlock the pages by powering the core up and down. This register can be used to ensure that the bootloader runs as normal on the next power up, preventing Secure code at a later stage from accessing OTP in its unlocked state. Should be used in conjunction with the OTP BOOTDIS register.
    // Reset value: 0x00000000
    BEGIN_TYPE(BOOTDIS_t, uint32_t)
        // This flag always ORs writes into its current contents. It can be set but not cleared by software. The BOOTDIS_NEXT bit is OR'd into the BOOTDIS_NOW bit when the core is powered down. Simultaneously, the BOOTDIS_NEXT bit is cleared. Setting this bit means that the BOOT0..3 registers will be ignored following the next reset of the RSM by powman. This flag should be set by an early boot stage that has soft-locked OTP pages, to prevent later stages from unlocking it by power cycling.
        ADD_BITFIELD_RW(NEXT, 1, 1)
        // When powman resets the RSM, the current value of BOOTDIS_NEXT is OR'd into BOOTDIS_NOW, and BOOTDIS_NEXT is cleared. The bootrom checks this flag before reading the BOOT0..3 registers. If it is set, the bootrom clears it, and ignores the BOOT registers. This prevents Secure software from diverting the boot path before a bootloader has had the chance to soft lock OTP pages containing sensitive data.
        ADD_BITFIELD_RW(NOW, 0, 1)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(DBGCONFIG_t, uint32_t)
        // Configure DP instance ID for SWD multidrop selection.  Recommend that this is NOT changed until you require debug access in multi-chip environment
        ADD_BITFIELD_RW(DP_INSTID, 0, 4)
    END_TYPE()

    // Scratch register. Information persists in low power mode
    // Reset value: 0x00000000
    typedef uint32_t SCRATCH_t;

    // Scratch register. Information persists in low power mode
    // Reset value: 0x00000000
    typedef uint32_t BOOT_t;

    // Raw Interrupts
    // Reset value: 0x00000000
    BEGIN_TYPE(INTR_t, uint32_t)
        // Source is state.pwrup_while_waiting
        ADD_BITFIELD_RO(PWRUP_WHILE_WAITING, 3, 1)
        // Source is state.req_ignored
        ADD_BITFIELD_RO(STATE_REQ_IGNORED, 2, 1)
        ADD_BITFIELD_RO(TIMER, 1, 1)
        ADD_BITFIELD_RW(VREG_OUTPUT_LOW, 0, 1)
    END_TYPE()

    // Interrupt Enable
    // Reset value: 0x00000000
    BEGIN_TYPE(INTE_t, uint32_t)
        // Source is state.pwrup_while_waiting
        ADD_BITFIELD_RW(PWRUP_WHILE_WAITING, 3, 1)
        // Source is state.req_ignored
        ADD_BITFIELD_RW(STATE_REQ_IGNORED, 2, 1)
        ADD_BITFIELD_RW(TIMER, 1, 1)
        ADD_BITFIELD_RW(VREG_OUTPUT_LOW, 0, 1)
    END_TYPE()

    // Interrupt Force
    // Reset value: 0x00000000
    BEGIN_TYPE(INTF_t, uint32_t)
        // Source is state.pwrup_while_waiting
        ADD_BITFIELD_RW(PWRUP_WHILE_WAITING, 3, 1)
        // Source is state.req_ignored
        ADD_BITFIELD_RW(STATE_REQ_IGNORED, 2, 1)
        ADD_BITFIELD_RW(TIMER, 1, 1)
        ADD_BITFIELD_RW(VREG_OUTPUT_LOW, 0, 1)
    END_TYPE()

    // Interrupt status after masking & forcing
    // Reset value: 0x00000000
    BEGIN_TYPE(INTS_t, uint32_t)
        // Source is state.pwrup_while_waiting
        ADD_BITFIELD_RO(PWRUP_WHILE_WAITING, 3, 1)
        // Source is state.req_ignored
        ADD_BITFIELD_RO(STATE_REQ_IGNORED, 2, 1)
        ADD_BITFIELD_RO(TIMER, 1, 1)
        ADD_BITFIELD_RO(VREG_OUTPUT_LOW, 0, 1)
    END_TYPE()

    struct POWMAN_t {
        BADPASSWD_t                   BADPASSWD;
        VREG_CTRL_t                   VREG_CTRL;
        VREG_STS_t                    VREG_STS;
        VREG_t                        VREG;
        VREG_LP_ENTRY_t               VREG_LP_ENTRY;
        VREG_LP_EXIT_t                VREG_LP_EXIT;
        BOD_CTRL_t                    BOD_CTRL;
        BOD_t                         BOD;
        BOD_LP_ENTRY_t                BOD_LP_ENTRY;
        BOD_LP_EXIT_t                 BOD_LP_EXIT;
        LPOSC_t                       LPOSC;
        CHIP_RESET_t                  CHIP_RESET;
        WDSEL_t                       WDSEL;
        SEQ_CFG_t                     SEQ_CFG;
        STATE_t                       STATE;
        POW_FASTDIV_t                 POW_FASTDIV;
        POW_DELAY_t                   POW_DELAY;
        EXT_CTRL0_t                   EXT_CTRL0;
        EXT_CTRL1_t                   EXT_CTRL1;
        EXT_TIME_REF_t                EXT_TIME_REF;
        LPOSC_FREQ_KHZ_INT_t          LPOSC_FREQ_KHZ_INT;
        LPOSC_FREQ_KHZ_FRAC_t         LPOSC_FREQ_KHZ_FRAC;
        XOSC_FREQ_KHZ_INT_t           XOSC_FREQ_KHZ_INT;
        XOSC_FREQ_KHZ_FRAC_t          XOSC_FREQ_KHZ_FRAC;
        SET_TIME_63TO48_t             SET_TIME_63TO48;
        SET_TIME_47TO32_t             SET_TIME_47TO32;
        SET_TIME_31TO16_t             SET_TIME_31TO16;
        SET_TIME_15TO0_t              SET_TIME_15TO0;
        READ_TIME_UPPER_t             READ_TIME_UPPER;
        READ_TIME_LOWER_t             READ_TIME_LOWER;
        ALARM_TIME_63TO48_t           ALARM_TIME_63TO48;
        ALARM_TIME_47TO32_t           ALARM_TIME_47TO32;
        ALARM_TIME_31TO16_t           ALARM_TIME_31TO16;
        ALARM_TIME_15TO0_t            ALARM_TIME_15TO0;
        TIMER_t                       TIMER;
        PWRUP0_t                      PWRUP0;
        PWRUP1_t                      PWRUP1;
        PWRUP2_t                      PWRUP2;
        PWRUP3_t                      PWRUP3;
        CURRENT_PWRUP_REQ_t           CURRENT_PWRUP_REQ;
        LAST_SWCORE_PWRUP_t           LAST_SWCORE_PWRUP;
        DBG_PWRCFG_t                  DBG_PWRCFG;
        BOOTDIS_t                     BOOTDIS;
        DBGCONFIG_t                   DBGCONFIG;
        SCRATCH_t                     SCRATCH[8];
        BOOT_t                        BOOT[4];
        INTR_t                        INTR;
        INTE_t                        INTE;
        INTF_t                        INTF;
        INTS_t                        INTS;
    };

    static POWMAN_t & POWMAN = (*(POWMAN_t *)0x40100000);
    static POWMAN_t & POWMAN_XOR = (*(POWMAN_t *)0x40101000);
    static POWMAN_t & POWMAN_SET = (*(POWMAN_t *)0x40102000);
    static POWMAN_t & POWMAN_CLR = (*(POWMAN_t *)0x40103000);

} // _POWMAN_

namespace _WATCHDOG_  {

    // Watchdog control  The rst_wdsel register determines which subsystems are reset when the watchdog is triggered.  The watchdog can be triggered in software.
    // Reset value: 0x07000000
    BEGIN_TYPE(CTRL_t, uint32_t)
        // Trigger a watchdog reset
        ADD_BITFIELD_WO(TRIGGER, 31, 1)
        // When not enabled the watchdog timer is paused
        ADD_BITFIELD_RW(ENABLE, 30, 1)
        // Pause the watchdog timer when processor 1 is in debug mode
        ADD_BITFIELD_RW(PAUSE_DBG1, 26, 1)
        // Pause the watchdog timer when processor 0 is in debug mode
        ADD_BITFIELD_RW(PAUSE_DBG0, 25, 1)
        // Pause the watchdog timer when JTAG is accessing the bus fabric
        ADD_BITFIELD_RW(PAUSE_JTAG, 24, 1)
        // Indicates the time in usec before a watchdog reset will be triggered
        ADD_BITFIELD_RO(TIME, 0, 24)
    END_TYPE()

    // Load the watchdog timer. The maximum setting is 0xffffff which corresponds to approximately 16 seconds.
    // Reset value: 0x00000000
    BEGIN_TYPE(LOAD_t, uint32_t)
        ADD_BITFIELD_WO(LOAD, 0, 24)
    END_TYPE()

    // Logs the reason for the last reset. Both bits are zero for the case of a hardware reset. Additionally, as of RP2350, a debugger warm reset of either core (SYSRESETREQ or hartreset) will also clear the watchdog reason register, so that software loaded under the debugger following a watchdog timeout will not continue to see the timeout condition.
    // Reset value: 0x00000000
    BEGIN_TYPE(REASON_t, uint32_t)
        ADD_BITFIELD_RO(FORCE, 1, 1)
        ADD_BITFIELD_RO(TIMER, 0, 1)
    END_TYPE()

    // Scratch register. Information persists through soft reset of the chip.
    // Reset value: 0x00000000
    typedef uint32_t SCRATCH_t;

    struct WATCHDOG_t {
        CTRL_t                        CTRL;
        LOAD_t                        LOAD;
        REASON_t                      REASON;
        SCRATCH_t                     SCRATCH[8];
    };

    static WATCHDOG_t & WATCHDOG = (*(WATCHDOG_t *)0x400d8000);
    static WATCHDOG_t & WATCHDOG_XOR = (*(WATCHDOG_t *)0x400d9000);
    static WATCHDOG_t & WATCHDOG_SET = (*(WATCHDOG_t *)0x400da000);
    static WATCHDOG_t & WATCHDOG_CLR = (*(WATCHDOG_t *)0x400db000);

} // _WATCHDOG_

// DMA with separate read and write masters
namespace _DMA_  {

    // DMA Channel 0 Read Address pointer
    // Reset value: 0x00000000
    typedef uint32_t CH_READ_ADDR_t;

    // DMA Channel 0 Write Address pointer
    // Reset value: 0x00000000
    typedef uint32_t CH_WRITE_ADDR_t;

    // DMA Channel 0 Transfer Count
    // Reset value: 0x00000000
    BEGIN_TYPE(CH_TRANS_COUNT_t, uint32_t)
        // When MODE is 0x0, the transfer count decrements with each transfer until 0, and then the channel triggers the next channel indicated by CTRL_CHAIN_TO. When MODE is 0x1, the transfer count decrements with each transfer until 0, and then the channel re-triggers itself, in addition to the trigger indicated by CTRL_CHAIN_TO. This is useful for e.g. an endless ring-buffer DMA with periodic interrupts. When MODE is 0xf, the transfer count does not decrement. The DMA channel performs an endless sequence of transfers, never triggering other channels or raising interrupts, until an ABORT is raised. All other values are reserved.
        ADD_BITFIELD_RW(MODE, 28, 4)
        // 28-bit transfer count (256 million transfers maximum). Program the number of bus transfers a channel will perform before halting. Note that, if transfers are larger than one byte in size, this is not equal to the number of bytes transferred (see CTRL_DATA_SIZE). When the channel is active, reading this register shows the number of transfers remaining, updating automatically each time a write transfer completes. Writing this register sets the RELOAD value for the transfer counter. Each time this channel is triggered, the RELOAD value is copied into the live transfer counter. The channel can be started multiple times, and will perform the same number of transfers each time, as programmed by most recent write. The RELOAD value can be observed at CHx_DBG_TCR. If TRANS_COUNT is used as a trigger, the written value is used immediately as the length of the new transfer sequence, as well as being written to RELOAD.
        ADD_BITFIELD_RW(COUNT, 0, 28)
    END_TYPE()

    static const uint32_t CH_TRANS_COUNT_MODE__NORMAL = 0;
    static const uint32_t CH_TRANS_COUNT_MODE__TRIGGER_SELF = 1;
    static const uint32_t CH_TRANS_COUNT_MODE__ENDLESS = 15;

    // DMA Channel 0 Control and Status
    // Reset value: 0x00000000
    BEGIN_TYPE(CH_CTRL_TRIG_t, uint32_t)
        // Logical OR of the READ_ERROR and WRITE_ERROR flags. The channel halts when it encounters any bus error, and always raises its channel IRQ flag.
        ADD_BITFIELD_RO(AHB_ERROR, 31, 1)
        // If 1, the channel received a read bus error. Write one to clear.  READ_ADDR shows the approximate address where the bus error was encountered (will not be earlier, or more than 3 transfers later)
        ADD_BITFIELD_RW(READ_ERROR, 30, 1)
        // If 1, the channel received a write bus error. Write one to clear.  WRITE_ADDR shows the approximate address where the bus error was encountered (will not be earlier, or more than 5 transfers later)
        ADD_BITFIELD_RW(WRITE_ERROR, 29, 1)
        // This flag goes high when the channel starts a new transfer sequence, and low when the last transfer of that sequence completes. Clearing EN while BUSY is high pauses the channel, and BUSY will stay high while paused. To terminate a sequence early (and clear the BUSY flag), see CHAN_ABORT.
        ADD_BITFIELD_RO(BUSY, 26, 1)
        // If 1, this channel's data transfers are visible to the sniff hardware, and each transfer will advance the state of the checksum. This only applies if the sniff hardware is enabled, and has this channel selected. This allows checksum to be enabled or disabled on a per-control- block basis.
        ADD_BITFIELD_RW(SNIFF_EN, 25, 1)
        // Apply byte-swap transformation to DMA data.  For byte data, this has no effect. For halfword data, the two bytes of each halfword are swapped. For word data, the four bytes of each word are swapped to reverse order.
        ADD_BITFIELD_RW(BSWAP, 24, 1)
        // In QUIET mode, the channel does not generate IRQs at the end of every transfer block. Instead, an IRQ is raised when NULL is written to a trigger register, indicating the end of a control block chain. This reduces the number of interrupts to be serviced by the CPU when transferring a DMA chain of many small control blocks.
        ADD_BITFIELD_RW(IRQ_QUIET, 23, 1)
        // Select a Transfer Request signal.  The channel uses the transfer request signal to pace its data transfer rate. Sources for TREQ signals are internal (TIMERS) or external (DREQ, a Data Request from the system).  0x0 to 0x3a -> select DREQ n as TREQ
        ADD_BITFIELD_RW(TREQ_SEL, 17, 6)
        // When this channel completes, it will trigger the channel indicated by CHAIN_TO. Disable by setting CHAIN_TO = _(this channel)_. Note this field resets to 0, so channels 1 and above will chain to channel 0 by default. Set this field to avoid this behaviour.
        ADD_BITFIELD_RW(CHAIN_TO, 13, 4)
        // Select whether RING_SIZE applies to read or write addresses.  If 0, read addresses are wrapped on a (1 << RING_SIZE) boundary. If 1, write addresses are wrapped.
        ADD_BITFIELD_RW(RING_SEL, 12, 1)
        // Size of address wrap region. If 0, don't wrap. For values n > 0, only the lower n bits of the address will change. This wraps the address on a (1 << n) byte boundary, facilitating access to naturally-aligned ring buffers. Ring sizes between 2 and 32768 bytes are possible. This can apply to either read or write addresses, based on value of RING_SEL.
        ADD_BITFIELD_RW(RING_SIZE, 8, 4)
        // If 1, and INCR_WRITE is 1, the write address is decremented rather than incremented with each transfer. If 1, and INCR_WRITE is 0, this otherwise-unused combination causes the write address to be incremented by twice the transfer size, i.e. skipping over alternate addresses.
        ADD_BITFIELD_RW(INCR_WRITE_REV, 7, 1)
        // If 1, the write address increments with each transfer. If 0, each write is directed to the same, initial address. Generally this should be disabled for memory-to-peripheral transfers.
        ADD_BITFIELD_RW(INCR_WRITE, 6, 1)
        // If 1, and INCR_READ is 1, the read address is decremented rather than incremented with each transfer. If 1, and INCR_READ is 0, this otherwise-unused combination causes the read address to be incremented by twice the transfer size, i.e. skipping over alternate addresses.
        ADD_BITFIELD_RW(INCR_READ_REV, 5, 1)
        // If 1, the read address increments with each transfer. If 0, each read is directed to the same, initial address. Generally this should be disabled for peripheral-to-memory transfers.
        ADD_BITFIELD_RW(INCR_READ, 4, 1)
        // Set the size of each bus transfer (byte/halfword/word). READ_ADDR and WRITE_ADDR advance by this amount (1/2/4 bytes) with each transfer.
        ADD_BITFIELD_RW(DATA_SIZE, 2, 2)
        // HIGH_PRIORITY gives a channel preferential treatment in issue scheduling: in each scheduling round, all high priority channels are considered first, and then only a single low priority channel, before returning to the high priority channels. This only affects the order in which the DMA schedules channels. The DMA's bus priority is not changed. If the DMA is not saturated then a low priority channel will see no loss of throughput.
        ADD_BITFIELD_RW(HIGH_PRIORITY, 1, 1)
        // DMA Channel Enable.  When 1, the channel will respond to triggering events, which will cause it to become BUSY and start transferring data. When 0, the channel will ignore triggers, stop issuing transfers, and pause the current transfer sequence (i.e. BUSY will remain high if already high)
        ADD_BITFIELD_RW(EN, 0, 1)
    END_TYPE()

    // Select PIO0's TX FIFO 0 as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PIO0_TX0 = 0;
    // Select PIO0's TX FIFO 1 as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PIO0_TX1 = 1;
    // Select PIO0's TX FIFO 2 as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PIO0_TX2 = 2;
    // Select PIO0's TX FIFO 3 as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PIO0_TX3 = 3;
    // Select PIO0's RX FIFO 0 as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PIO0_RX0 = 4;
    // Select PIO0's RX FIFO 1 as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PIO0_RX1 = 5;
    // Select PIO0's RX FIFO 2 as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PIO0_RX2 = 6;
    // Select PIO0's RX FIFO 3 as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PIO0_RX3 = 7;
    // Select PIO1's TX FIFO 0 as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PIO1_TX0 = 8;
    // Select PIO1's TX FIFO 1 as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PIO1_TX1 = 9;
    // Select PIO1's TX FIFO 2 as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PIO1_TX2 = 10;
    // Select PIO1's TX FIFO 3 as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PIO1_TX3 = 11;
    // Select PIO1's RX FIFO 0 as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PIO1_RX0 = 12;
    // Select PIO1's RX FIFO 1 as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PIO1_RX1 = 13;
    // Select PIO1's RX FIFO 2 as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PIO1_RX2 = 14;
    // Select PIO1's RX FIFO 3 as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PIO1_RX3 = 15;
    // Select PIO2's TX FIFO 0 as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PIO2_TX0 = 16;
    // Select PIO2's TX FIFO 1 as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PIO2_TX1 = 17;
    // Select PIO2's TX FIFO 2 as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PIO2_TX2 = 18;
    // Select PIO2's TX FIFO 3 as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PIO2_TX3 = 19;
    // Select PIO2's RX FIFO 0 as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PIO2_RX0 = 20;
    // Select PIO2's RX FIFO 1 as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PIO2_RX1 = 21;
    // Select PIO2's RX FIFO 2 as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PIO2_RX2 = 22;
    // Select PIO2's RX FIFO 3 as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PIO2_RX3 = 23;
    // Select SPI0's TX FIFO as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__SPI0_TX = 24;
    // Select SPI0's RX FIFO as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__SPI0_RX = 25;
    // Select SPI1's TX FIFO as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__SPI1_TX = 26;
    // Select SPI1's RX FIFO as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__SPI1_RX = 27;
    // Select UART0's TX FIFO as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__UART0_TX = 28;
    // Select UART0's RX FIFO as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__UART0_RX = 29;
    // Select UART1's TX FIFO as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__UART1_TX = 30;
    // Select UART1's RX FIFO as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__UART1_RX = 31;
    // Select PWM Counter 0's Wrap Value as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PWM_WRAP0 = 32;
    // Select PWM Counter 1's Wrap Value as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PWM_WRAP1 = 33;
    // Select PWM Counter 2's Wrap Value as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PWM_WRAP2 = 34;
    // Select PWM Counter 3's Wrap Value as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PWM_WRAP3 = 35;
    // Select PWM Counter 4's Wrap Value as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PWM_WRAP4 = 36;
    // Select PWM Counter 5's Wrap Value as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PWM_WRAP5 = 37;
    // Select PWM Counter 6's Wrap Value as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PWM_WRAP6 = 38;
    // Select PWM Counter 7's Wrap Value as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PWM_WRAP7 = 39;
    // Select PWM Counter 8's Wrap Value as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PWM_WRAP8 = 40;
    // Select PWM Counter 9's Wrap Value as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PWM_WRAP9 = 41;
    // Select PWM Counter 0's Wrap Value as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PWM_WRAP10 = 42;
    // Select PWM Counter 1's Wrap Value as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PWM_WRAP11 = 43;
    // Select I2C0's TX FIFO as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__I2C0_TX = 44;
    // Select I2C0's RX FIFO as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__I2C0_RX = 45;
    // Select I2C1's TX FIFO as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__I2C1_TX = 46;
    // Select I2C1's RX FIFO as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__I2C1_RX = 47;
    // Select the ADC as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__ADC = 48;
    // Select the XIP Streaming FIFO as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__XIP_STREAM = 49;
    // Select XIP_QMITX as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__XIP_QMITX = 50;
    // Select XIP_QMIRX as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__XIP_QMIRX = 51;
    // Select HSTX as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__HSTX = 52;
    // Select CORESIGHT as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__CORESIGHT = 53;
    // Select SHA256 as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__SHA256 = 54;
    // Select Timer 0 as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__TIMER0 = 59;
    // Select Timer 1 as TREQ
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__TIMER1 = 60;
    // Select Timer 2 as TREQ (Optional)
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__TIMER2 = 61;
    // Select Timer 3 as TREQ (Optional)
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__TIMER3 = 62;
    // Permanent request, for unpaced transfers.
    static const uint32_t CH_CTRL_TRIG_TREQ_SEL__PERMANENT = 63;
    static const uint32_t CH_CTRL_TRIG_RING_SIZE__RING_NONE = 0;
    static const uint32_t CH_CTRL_TRIG_DATA_SIZE__SIZE_BYTE = 0;
    static const uint32_t CH_CTRL_TRIG_DATA_SIZE__SIZE_HALFWORD = 1;
    static const uint32_t CH_CTRL_TRIG_DATA_SIZE__SIZE_WORD = 2;

    // Alias for channel 0 CTRL register
    typedef uint32_t CH_AL1_CTRL_t;

    // Alias for channel 0 READ_ADDR register
    typedef uint32_t CH_AL1_READ_ADDR_t;

    // Alias for channel 0 WRITE_ADDR register
    typedef uint32_t CH_AL1_WRITE_ADDR_t;

    // Alias for channel 0 TRANS_COUNT register  This is a trigger register (0xc). Writing a nonzero value will  reload the channel counter and start the channel.
    typedef uint32_t CH_AL1_TRANS_COUNT_TRIG_t;

    // Alias for channel 0 CTRL register
    typedef uint32_t CH_AL2_CTRL_t;

    // Alias for channel 0 TRANS_COUNT register
    typedef uint32_t CH_AL2_TRANS_COUNT_t;

    // Alias for channel 0 READ_ADDR register
    typedef uint32_t CH_AL2_READ_ADDR_t;

    // Alias for channel 0 WRITE_ADDR register  This is a trigger register (0xc). Writing a nonzero value will  reload the channel counter and start the channel.
    typedef uint32_t CH_AL2_WRITE_ADDR_TRIG_t;

    // Alias for channel 0 CTRL register
    typedef uint32_t CH_AL3_CTRL_t;

    // Alias for channel 0 WRITE_ADDR register
    typedef uint32_t CH_AL3_WRITE_ADDR_t;

    // Alias for channel 0 TRANS_COUNT register
    typedef uint32_t CH_AL3_TRANS_COUNT_t;

    // Alias for channel 0 READ_ADDR register  This is a trigger register (0xc). Writing a nonzero value will  reload the channel counter and start the channel.
    typedef uint32_t CH_AL3_READ_ADDR_TRIG_t;

    // Interrupt Status (raw)
    // Reset value: 0x00000000
    BEGIN_TYPE(INTR_t, uint32_t)
        // Raw interrupt status for DMA Channels 0..15. Bit n corresponds to channel n. Ignores any masking or forcing. Channel interrupts can be cleared by writing a bit mask to INTR or INTS0/1/2/3. Channel interrupts can be routed to either of four system-level IRQs based on INTE0, INTE1, INTE2 and INTE3. The multiple system-level interrupts might be used to allow NVIC IRQ preemption for more time-critical channels, to spread IRQ load across different cores, or to target IRQs to different security domains. It is also valid to ignore the multiple IRQs, and just use INTE0/INTS0/IRQ 0. If this register is accessed at a security/privilege level less than that of a given channel (as defined by that channel's SECCFG_CHx register), then that channel's interrupt status will read as 0, ignore writes.
        ADD_BITFIELD_RW(INTR, 0, 16)
    END_TYPE()

    // Interrupt Enables for IRQ 0
    // Reset value: 0x00000000
    BEGIN_TYPE(INTE0_t, uint32_t)
        // Set bit n to pass interrupts from channel n to DMA IRQ 0. Note this bit has no effect if the channel security/privilege level, defined by SECCFG_CHx, is greater than the IRQ security/privilege defined by SECCFG_IRQ0.
        ADD_BITFIELD_RW(INTE0, 0, 16)
    END_TYPE()

    // Force Interrupts
    // Reset value: 0x00000000
    BEGIN_TYPE(INTF0_t, uint32_t)
        // Write 1s to force the corresponding bits in INTS0. The interrupt remains asserted until INTF0 is cleared.
        ADD_BITFIELD_RW(INTF0, 0, 16)
    END_TYPE()

    // Interrupt Status for IRQ 0
    // Reset value: 0x00000000
    BEGIN_TYPE(INTS0_t, uint32_t)
        // Indicates active channel interrupt requests which are currently causing IRQ 0 to be asserted.  Channel interrupts can be cleared by writing a bit mask here. Channels with a security/privilege (SECCFG_CHx) greater SECCFG_IRQ0) read as 0 in this register, and ignore writes.
        ADD_BITFIELD_RW(INTS0, 0, 16)
    END_TYPE()

    // Interrupt Status (raw)
    // Reset value: 0x00000000
    BEGIN_TYPE(INTR1_t, uint32_t)
        // Raw interrupt status for DMA Channels 0..15. Bit n corresponds to channel n. Ignores any masking or forcing. Channel interrupts can be cleared by writing a bit mask to INTR or INTS0/1/2/3. Channel interrupts can be routed to either of four system-level IRQs based on INTE0, INTE1, INTE2 and INTE3. The multiple system-level interrupts might be used to allow NVIC IRQ preemption for more time-critical channels, to spread IRQ load across different cores, or to target IRQs to different security domains. It is also valid to ignore the multiple IRQs, and just use INTE0/INTS0/IRQ 0. If this register is accessed at a security/privilege level less than that of a given channel (as defined by that channel's SECCFG_CHx register), then that channel's interrupt status will read as 0, ignore writes.
        ADD_BITFIELD_RW(INTR1, 0, 16)
    END_TYPE()

    // Interrupt Enables for IRQ 1
    // Reset value: 0x00000000
    BEGIN_TYPE(INTE1_t, uint32_t)
        // Set bit n to pass interrupts from channel n to DMA IRQ 1. Note this bit has no effect if the channel security/privilege level, defined by SECCFG_CHx, is greater than the IRQ security/privilege defined by SECCFG_IRQ1.
        ADD_BITFIELD_RW(INTE1, 0, 16)
    END_TYPE()

    // Force Interrupts
    // Reset value: 0x00000000
    BEGIN_TYPE(INTF1_t, uint32_t)
        // Write 1s to force the corresponding bits in INTS1. The interrupt remains asserted until INTF1 is cleared.
        ADD_BITFIELD_RW(INTF1, 0, 16)
    END_TYPE()

    // Interrupt Status for IRQ 1
    // Reset value: 0x00000000
    BEGIN_TYPE(INTS1_t, uint32_t)
        // Indicates active channel interrupt requests which are currently causing IRQ 1 to be asserted.  Channel interrupts can be cleared by writing a bit mask here. Channels with a security/privilege (SECCFG_CHx) greater SECCFG_IRQ1) read as 0 in this register, and ignore writes.
        ADD_BITFIELD_RW(INTS1, 0, 16)
    END_TYPE()

    // Interrupt Status (raw)
    // Reset value: 0x00000000
    BEGIN_TYPE(INTR2_t, uint32_t)
        // Raw interrupt status for DMA Channels 0..15. Bit n corresponds to channel n. Ignores any masking or forcing. Channel interrupts can be cleared by writing a bit mask to INTR or INTS0/1/2/3. Channel interrupts can be routed to either of four system-level IRQs based on INTE0, INTE1, INTE2 and INTE3. The multiple system-level interrupts might be used to allow NVIC IRQ preemption for more time-critical channels, to spread IRQ load across different cores, or to target IRQs to different security domains. It is also valid to ignore the multiple IRQs, and just use INTE0/INTS0/IRQ 0. If this register is accessed at a security/privilege level less than that of a given channel (as defined by that channel's SECCFG_CHx register), then that channel's interrupt status will read as 0, ignore writes.
        ADD_BITFIELD_RW(INTR2, 0, 16)
    END_TYPE()

    // Interrupt Enables for IRQ 2
    // Reset value: 0x00000000
    BEGIN_TYPE(INTE2_t, uint32_t)
        // Set bit n to pass interrupts from channel n to DMA IRQ 2. Note this bit has no effect if the channel security/privilege level, defined by SECCFG_CHx, is greater than the IRQ security/privilege defined by SECCFG_IRQ2.
        ADD_BITFIELD_RW(INTE2, 0, 16)
    END_TYPE()

    // Force Interrupts
    // Reset value: 0x00000000
    BEGIN_TYPE(INTF2_t, uint32_t)
        // Write 1s to force the corresponding bits in INTS2. The interrupt remains asserted until INTF2 is cleared.
        ADD_BITFIELD_RW(INTF2, 0, 16)
    END_TYPE()

    // Interrupt Status for IRQ 2
    // Reset value: 0x00000000
    BEGIN_TYPE(INTS2_t, uint32_t)
        // Indicates active channel interrupt requests which are currently causing IRQ 2 to be asserted.  Channel interrupts can be cleared by writing a bit mask here. Channels with a security/privilege (SECCFG_CHx) greater SECCFG_IRQ2) read as 0 in this register, and ignore writes.
        ADD_BITFIELD_RW(INTS2, 0, 16)
    END_TYPE()

    // Interrupt Status (raw)
    // Reset value: 0x00000000
    BEGIN_TYPE(INTR3_t, uint32_t)
        // Raw interrupt status for DMA Channels 0..15. Bit n corresponds to channel n. Ignores any masking or forcing. Channel interrupts can be cleared by writing a bit mask to INTR or INTS0/1/2/3. Channel interrupts can be routed to either of four system-level IRQs based on INTE0, INTE1, INTE2 and INTE3. The multiple system-level interrupts might be used to allow NVIC IRQ preemption for more time-critical channels, to spread IRQ load across different cores, or to target IRQs to different security domains. It is also valid to ignore the multiple IRQs, and just use INTE0/INTS0/IRQ 0. If this register is accessed at a security/privilege level less than that of a given channel (as defined by that channel's SECCFG_CHx register), then that channel's interrupt status will read as 0, ignore writes.
        ADD_BITFIELD_RW(INTR3, 0, 16)
    END_TYPE()

    // Interrupt Enables for IRQ 3
    // Reset value: 0x00000000
    BEGIN_TYPE(INTE3_t, uint32_t)
        // Set bit n to pass interrupts from channel n to DMA IRQ 3. Note this bit has no effect if the channel security/privilege level, defined by SECCFG_CHx, is greater than the IRQ security/privilege defined by SECCFG_IRQ3.
        ADD_BITFIELD_RW(INTE3, 0, 16)
    END_TYPE()

    // Force Interrupts
    // Reset value: 0x00000000
    BEGIN_TYPE(INTF3_t, uint32_t)
        // Write 1s to force the corresponding bits in INTS3. The interrupt remains asserted until INTF3 is cleared.
        ADD_BITFIELD_RW(INTF3, 0, 16)
    END_TYPE()

    // Interrupt Status for IRQ 3
    // Reset value: 0x00000000
    BEGIN_TYPE(INTS3_t, uint32_t)
        // Indicates active channel interrupt requests which are currently causing IRQ 3 to be asserted.  Channel interrupts can be cleared by writing a bit mask here. Channels with a security/privilege (SECCFG_CHx) greater SECCFG_IRQ3) read as 0 in this register, and ignore writes.
        ADD_BITFIELD_RW(INTS3, 0, 16)
    END_TYPE()

    // Pacing (X/Y) fractional timer  The pacing timer produces TREQ assertions at a rate set by ((X/Y) * sys_clk). This equation is evaluated every sys_clk cycles and therefore can only generate TREQs at a rate of 1 per sys_clk (i.e. permanent TREQ) or less.
    // Reset value: 0x00000000
    BEGIN_TYPE(TIMER_t, uint32_t)
        // Pacing Timer Dividend. Specifies the X value for the (X/Y) fractional timer.
        ADD_BITFIELD_RW(X, 16, 16)
        // Pacing Timer Divisor. Specifies the Y value for the (X/Y) fractional timer.
        ADD_BITFIELD_RW(Y, 0, 16)
    END_TYPE()

    // Trigger one or more channels simultaneously
    // Reset value: 0x00000000
    BEGIN_TYPE(MULTI_CHAN_TRIGGER_t, uint32_t)
        // Each bit in this register corresponds to a DMA channel. Writing a 1 to the relevant bit is the same as writing to that channel's trigger register; the channel will start if it is currently enabled and not already busy.
        ADD_BITFIELD_WO(MULTI_CHAN_TRIGGER, 0, 16)
    END_TYPE()

    // Sniffer Control
    // Reset value: 0x00000000
    BEGIN_TYPE(SNIFF_CTRL_t, uint32_t)
        // If set, the result appears inverted (bitwise complement) when read. This does not affect the way the checksum is calculated; the result is transformed on-the-fly between the result register and the bus.
        ADD_BITFIELD_RW(OUT_INV, 11, 1)
        // If set, the result appears bit-reversed when read. This does not affect the way the checksum is calculated; the result is transformed on-the-fly between the result register and the bus.
        ADD_BITFIELD_RW(OUT_REV, 10, 1)
        // Locally perform a byte reverse on the sniffed data, before feeding into checksum. Note that the sniff hardware is downstream of the DMA channel byteswap performed in the read master: if channel CTRL_BSWAP and SNIFF_CTRL_BSWAP are both enabled, their effects cancel from the sniffer's point of view.
        ADD_BITFIELD_RW(BSWAP, 9, 1)
        ADD_BITFIELD_RW(CALC, 5, 4)
        // DMA channel for Sniffer to observe
        ADD_BITFIELD_RW(DMACH, 1, 4)
        // Enable sniffer
        ADD_BITFIELD_RW(EN, 0, 1)
    END_TYPE()

    // Calculate a CRC-32 (IEEE802.3 polynomial)
    static const uint32_t SNIFF_CTRL_CALC__CRC32 = 0;
    // Calculate a CRC-32 (IEEE802.3 polynomial) with bit reversed data
    static const uint32_t SNIFF_CTRL_CALC__CRC32R = 1;
    // Calculate a CRC-16-CCITT
    static const uint32_t SNIFF_CTRL_CALC__CRC16 = 2;
    // Calculate a CRC-16-CCITT with bit reversed data
    static const uint32_t SNIFF_CTRL_CALC__CRC16R = 3;
    // XOR reduction over all data. == 1 if the total 1 population count is odd.
    static const uint32_t SNIFF_CTRL_CALC__EVEN = 14;
    // Calculate a simple 32-bit checksum (addition with a 32 bit accumulator)
    static const uint32_t SNIFF_CTRL_CALC__SUM = 15;

    // Data accumulator for sniff hardware
    // Reset value: 0x00000000
    BEGIN_TYPE(SNIFF_DATA_t, uint32_t)
        // Write an initial seed value here before starting a DMA transfer on the channel indicated by SNIFF_CTRL_DMACH. The hardware will update this register each time it observes a read from the indicated channel. Once the channel completes, the final result can be read from this register.
        ADD_BITFIELD_RW(SNIFF_DATA, 0, 32)
    END_TYPE()

    // Debug RAF, WAF, TDF levels
    // Reset value: 0x00000000
    BEGIN_TYPE(FIFO_LEVELS_t, uint32_t)
        // Current Read-Address-FIFO fill level
        ADD_BITFIELD_RO(RAF_LVL, 16, 8)
        // Current Write-Address-FIFO fill level
        ADD_BITFIELD_RO(WAF_LVL, 8, 8)
        // Current Transfer-Data-FIFO fill level
        ADD_BITFIELD_RO(TDF_LVL, 0, 8)
    END_TYPE()

    // Abort an in-progress transfer sequence on one or more channels
    // Reset value: 0x00000000
    BEGIN_TYPE(CHAN_ABORT_t, uint32_t)
        // Each bit corresponds to a channel. Writing a 1 aborts whatever transfer sequence is in progress on that channel. The bit will remain high until any in-flight transfers have been flushed through the address and data FIFOs. After writing, this register must be polled until it returns all-zero. Until this point, it is unsafe to restart the channel.
        ADD_BITFIELD_WO(CHAN_ABORT, 0, 16)
    END_TYPE()

    // The number of channels this DMA instance is equipped with. This DMA supports up to 16 hardware channels, but can be configured with as few as one, to minimise silicon area.
    BEGIN_TYPE(N_CHANNELS_t, uint32_t)
        ADD_BITFIELD_RO(N_CHANNELS, 0, 5)
    END_TYPE()

    // Security configuration for channel 0. Control whether this channel performs Secure/Non-secure and Privileged/Unprivileged bus accesses. If this channel generates bus accesses of some security level, an access of at least that level (in the order S+P > S+U > NS+P > NS+U) is required to program, trigger, abort, check the status of, interrupt on or acknowledge the interrupt of this channel. This register automatically locks down (becomes read-only) once software starts to configure the channel. This register is world-readable, but is writable only from a Secure, Privileged context.
    // Reset value: 0x00000003
    BEGIN_TYPE(SECCFG_CH_t, uint32_t)
        // LOCK is 0 at reset, and is set to 1 automatically upon a successful write to this channel's control registers. That is, a write to CTRL, READ_ADDR, WRITE_ADDR, TRANS_COUNT and their aliases. Once its LOCK bit is set, this register becomes read-only. A failed write, for example due to the write's privilege being lower than that specified in the channel's SECCFG register, will not set the LOCK bit.
        ADD_BITFIELD_RW(LOCK, 2, 1)
        // Secure channel. If 1, this channel performs Secure bus accesses. If 0, it performs Non-secure bus accesses. If 1, this channel is controllable only from a Secure context.
        ADD_BITFIELD_RW(S, 1, 1)
        // Privileged channel. If 1, this channel performs Privileged bus accesses. If 0, it performs Unprivileged bus accesses. If 1, this channel is controllable only from a Privileged context of the same Secure/Non-secure level, or any context of a higher Secure/Non-secure level.
        ADD_BITFIELD_RW(P, 0, 1)
    END_TYPE()

    // Security configuration for IRQ 0. Control whether the IRQ permits configuration by Non-secure/Unprivileged contexts, and whether it can observe Secure/Privileged channel interrupt flags.
    // Reset value: 0x00000003
    BEGIN_TYPE(SECCFG_IRQ_t, uint32_t)
        // Secure IRQ. If 1, this IRQ's control registers can only be accessed from a Secure context. If 0, this IRQ's control registers can be accessed from a Non-secure context, but Secure channels (as per SECCFG_CHx) are masked from the IRQ status, and this IRQ's registers can not be used to acknowledge the channel interrupts of Secure channels.
        ADD_BITFIELD_RW(S, 1, 1)
        // Privileged IRQ. If 1, this IRQ's control registers can only be accessed from a Privileged context. If 0, this IRQ's control registers can be accessed from an Unprivileged context, but Privileged channels (as per SECCFG_CHx) are masked from the IRQ status, and this IRQ's registers can not be used to acknowledge the channel interrupts of Privileged channels.
        ADD_BITFIELD_RW(P, 0, 1)
    END_TYPE()

    // Miscellaneous security configuration
    // Reset value: 0x000003ff
    BEGIN_TYPE(SECCFG_MISC_t, uint32_t)
        // If 1, the TIMER3 register is only accessible from a Secure context, and timer DREQ 3 is only visible to Secure channels.
        ADD_BITFIELD_RW(TIMER3_S, 9, 1)
        // If 1, the TIMER3 register is only accessible from a Privileged (or more Secure) context, and timer DREQ 3 is only visible to Privileged (or more Secure) channels.
        ADD_BITFIELD_RW(TIMER3_P, 8, 1)
        // If 1, the TIMER2 register is only accessible from a Secure context, and timer DREQ 2 is only visible to Secure channels.
        ADD_BITFIELD_RW(TIMER2_S, 7, 1)
        // If 1, the TIMER2 register is only accessible from a Privileged (or more Secure) context, and timer DREQ 2 is only visible to Privileged (or more Secure) channels.
        ADD_BITFIELD_RW(TIMER2_P, 6, 1)
        // If 1, the TIMER1 register is only accessible from a Secure context, and timer DREQ 1 is only visible to Secure channels.
        ADD_BITFIELD_RW(TIMER1_S, 5, 1)
        // If 1, the TIMER1 register is only accessible from a Privileged (or more Secure) context, and timer DREQ 1 is only visible to Privileged (or more Secure) channels.
        ADD_BITFIELD_RW(TIMER1_P, 4, 1)
        // If 1, the TIMER0 register is only accessible from a Secure context, and timer DREQ 0 is only visible to Secure channels.
        ADD_BITFIELD_RW(TIMER0_S, 3, 1)
        // If 1, the TIMER0 register is only accessible from a Privileged (or more Secure) context, and timer DREQ 0 is only visible to Privileged (or more Secure) channels.
        ADD_BITFIELD_RW(TIMER0_P, 2, 1)
        // If 1, the sniffer can see data transfers from Secure channels, and can itself only be accessed from a Secure context. If 0, the sniffer can be accessed from either a Secure or Non-secure context, but can not see data transfers of Secure channels.
        ADD_BITFIELD_RW(SNIFF_S, 1, 1)
        // If 1, the sniffer can see data transfers from Privileged channels, and can itself only be accessed from a privileged context, or from a Secure context when SNIFF_S is 0. If 0, the sniffer can be accessed from either a Privileged or Unprivileged context (with sufficient security level) but can not see transfers from Privileged channels.
        ADD_BITFIELD_RW(SNIFF_P, 0, 1)
    END_TYPE()

    // Control register for DMA MPU. Accessible only from a Privileged context.
    // Reset value: 0x00000000
    BEGIN_TYPE(MPU_CTRL_t, uint32_t)
        // By default, when a region's S bit is clear, Non-secure-Privileged reads can see the region's base address and limit address. Set this bit to make the addresses appear as 0 to Non-secure reads, even when the region is Non-secure, to avoid leaking information about the processor SAU map.
        ADD_BITFIELD_RW(NS_HIDE_ADDR, 3, 1)
        // Determine whether an address not covered by an active MPU region is Secure (1) or Non-secure (0)
        ADD_BITFIELD_RW(S, 2, 1)
        // Determine whether an address not covered by an active MPU region is Privileged (1) or Unprivileged (0)
        ADD_BITFIELD_RW(P, 1, 1)
    END_TYPE()

    // Base address register for MPU region 0. Writable only from a Secure, Privileged context.
    // Reset value: 0x00000000
    BEGIN_TYPE(MPU_BAR_t, uint32_t)
        // This MPU region matches addresses where addr[31:5] (the 27 most significant bits) are greater than or equal to BAR_ADDR, and less than or equal to LAR_ADDR. Readable from any Privileged context, if and only if this region's S bit is clear, and MPU_CTRL_NS_HIDE_ADDR is clear. Otherwise readable only from a Secure, Privileged context.
        ADD_BITFIELD_RW(ADDR, 5, 27)
    END_TYPE()

    // Limit address register for MPU region 0. Writable only from a Secure, Privileged context, with the exception of the P bit.
    // Reset value: 0x00000000
    BEGIN_TYPE(MPU_LAR_t, uint32_t)
        // Limit address bits 31:5. Readable from any Privileged context, if and only if this region's S bit is clear, and MPU_CTRL_NS_HIDE_ADDR is clear. Otherwise readable only from a Secure, Privileged context.
        ADD_BITFIELD_RW(ADDR, 5, 27)
        // Determines the Secure/Non-secure (=1/0) status of addresses matching this region, if this region is enabled.
        ADD_BITFIELD_RW(S, 2, 1)
        // Determines the Privileged/Unprivileged (=1/0) status of addresses matching this region, if this region is enabled. Writable from any Privileged context, if and only if the S bit is clear. Otherwise, writable only from a Secure, Privileged context.
        ADD_BITFIELD_RW(P, 1, 1)
        // Region enable. If 1, any address within range specified by the base address (BAR_ADDR) and limit address (LAR_ADDR) has the attributes specified by S and P.
        ADD_BITFIELD_RW(EN, 0, 1)
    END_TYPE()

    // Read: get channel DREQ counter (i.e. how many accesses the DMA expects it can perform on the peripheral without overflow/underflow. Write any value: clears the counter, and cause channel to re-initiate DREQ handshake.
    // Reset value: 0x00000000
    BEGIN_TYPE(CH_DBG_CTDREQ_t, uint32_t)
        ADD_BITFIELD_RW(DBG_CTDREQ, 0, 6)
    END_TYPE()

    // Read to get channel TRANS_COUNT reload value, i.e. the length of the next transfer
    // Reset value: 0x00000000
    BEGIN_TYPE(CH_DBG_TCR_t, uint32_t)
        ADD_BITFIELD_RO(DBG_TCR, 0, 32)
    END_TYPE()

    struct DMA_t {
        CH_READ_ADDR_t                CH0_READ_ADDR;
        CH_WRITE_ADDR_t               CH0_WRITE_ADDR;
        CH_TRANS_COUNT_t              CH0_TRANS_COUNT;
        CH_CTRL_TRIG_t                CH0_CTRL_TRIG;
        CH_AL1_CTRL_t                 CH0_AL1_CTRL;
        CH_AL1_READ_ADDR_t            CH0_AL1_READ_ADDR;
        CH_AL1_WRITE_ADDR_t           CH0_AL1_WRITE_ADDR;
        CH_AL1_TRANS_COUNT_TRIG_t     CH0_AL1_TRANS_COUNT_TRIG;
        CH_AL2_CTRL_t                 CH0_AL2_CTRL;
        CH_AL2_TRANS_COUNT_t          CH0_AL2_TRANS_COUNT;
        CH_AL2_READ_ADDR_t            CH0_AL2_READ_ADDR;
        CH_AL2_WRITE_ADDR_TRIG_t      CH0_AL2_WRITE_ADDR_TRIG;
        CH_AL3_CTRL_t                 CH0_AL3_CTRL;
        CH_AL3_WRITE_ADDR_t           CH0_AL3_WRITE_ADDR;
        CH_AL3_TRANS_COUNT_t          CH0_AL3_TRANS_COUNT;
        CH_AL3_READ_ADDR_TRIG_t       CH0_AL3_READ_ADDR_TRIG;
        CH_READ_ADDR_t                CH1_READ_ADDR;
        CH_WRITE_ADDR_t               CH1_WRITE_ADDR;
        CH_TRANS_COUNT_t              CH1_TRANS_COUNT;
        CH_CTRL_TRIG_t                CH1_CTRL_TRIG;
        CH_AL1_CTRL_t                 CH1_AL1_CTRL;
        CH_AL1_READ_ADDR_t            CH1_AL1_READ_ADDR;
        CH_AL1_WRITE_ADDR_t           CH1_AL1_WRITE_ADDR;
        CH_AL1_TRANS_COUNT_TRIG_t     CH1_AL1_TRANS_COUNT_TRIG;
        CH_AL2_CTRL_t                 CH1_AL2_CTRL;
        CH_AL2_TRANS_COUNT_t          CH1_AL2_TRANS_COUNT;
        CH_AL2_READ_ADDR_t            CH1_AL2_READ_ADDR;
        CH_AL2_WRITE_ADDR_TRIG_t      CH1_AL2_WRITE_ADDR_TRIG;
        CH_AL3_CTRL_t                 CH1_AL3_CTRL;
        CH_AL3_WRITE_ADDR_t           CH1_AL3_WRITE_ADDR;
        CH_AL3_TRANS_COUNT_t          CH1_AL3_TRANS_COUNT;
        CH_AL3_READ_ADDR_TRIG_t       CH1_AL3_READ_ADDR_TRIG;
        CH_READ_ADDR_t                CH2_READ_ADDR;
        CH_WRITE_ADDR_t               CH2_WRITE_ADDR;
        CH_TRANS_COUNT_t              CH2_TRANS_COUNT;
        CH_CTRL_TRIG_t                CH2_CTRL_TRIG;
        CH_AL1_CTRL_t                 CH2_AL1_CTRL;
        CH_AL1_READ_ADDR_t            CH2_AL1_READ_ADDR;
        CH_AL1_WRITE_ADDR_t           CH2_AL1_WRITE_ADDR;
        CH_AL1_TRANS_COUNT_TRIG_t     CH2_AL1_TRANS_COUNT_TRIG;
        CH_AL2_CTRL_t                 CH2_AL2_CTRL;
        CH_AL2_TRANS_COUNT_t          CH2_AL2_TRANS_COUNT;
        CH_AL2_READ_ADDR_t            CH2_AL2_READ_ADDR;
        CH_AL2_WRITE_ADDR_TRIG_t      CH2_AL2_WRITE_ADDR_TRIG;
        CH_AL3_CTRL_t                 CH2_AL3_CTRL;
        CH_AL3_WRITE_ADDR_t           CH2_AL3_WRITE_ADDR;
        CH_AL3_TRANS_COUNT_t          CH2_AL3_TRANS_COUNT;
        CH_AL3_READ_ADDR_TRIG_t       CH2_AL3_READ_ADDR_TRIG;
        CH_READ_ADDR_t                CH3_READ_ADDR;
        CH_WRITE_ADDR_t               CH3_WRITE_ADDR;
        CH_TRANS_COUNT_t              CH3_TRANS_COUNT;
        CH_CTRL_TRIG_t                CH3_CTRL_TRIG;
        CH_AL1_CTRL_t                 CH3_AL1_CTRL;
        CH_AL1_READ_ADDR_t            CH3_AL1_READ_ADDR;
        CH_AL1_WRITE_ADDR_t           CH3_AL1_WRITE_ADDR;
        CH_AL1_TRANS_COUNT_TRIG_t     CH3_AL1_TRANS_COUNT_TRIG;
        CH_AL2_CTRL_t                 CH3_AL2_CTRL;
        CH_AL2_TRANS_COUNT_t          CH3_AL2_TRANS_COUNT;
        CH_AL2_READ_ADDR_t            CH3_AL2_READ_ADDR;
        CH_AL2_WRITE_ADDR_TRIG_t      CH3_AL2_WRITE_ADDR_TRIG;
        CH_AL3_CTRL_t                 CH3_AL3_CTRL;
        CH_AL3_WRITE_ADDR_t           CH3_AL3_WRITE_ADDR;
        CH_AL3_TRANS_COUNT_t          CH3_AL3_TRANS_COUNT;
        CH_AL3_READ_ADDR_TRIG_t       CH3_AL3_READ_ADDR_TRIG;
        CH_READ_ADDR_t                CH4_READ_ADDR;
        CH_WRITE_ADDR_t               CH4_WRITE_ADDR;
        CH_TRANS_COUNT_t              CH4_TRANS_COUNT;
        CH_CTRL_TRIG_t                CH4_CTRL_TRIG;
        CH_AL1_CTRL_t                 CH4_AL1_CTRL;
        CH_AL1_READ_ADDR_t            CH4_AL1_READ_ADDR;
        CH_AL1_WRITE_ADDR_t           CH4_AL1_WRITE_ADDR;
        CH_AL1_TRANS_COUNT_TRIG_t     CH4_AL1_TRANS_COUNT_TRIG;
        CH_AL2_CTRL_t                 CH4_AL2_CTRL;
        CH_AL2_TRANS_COUNT_t          CH4_AL2_TRANS_COUNT;
        CH_AL2_READ_ADDR_t            CH4_AL2_READ_ADDR;
        CH_AL2_WRITE_ADDR_TRIG_t      CH4_AL2_WRITE_ADDR_TRIG;
        CH_AL3_CTRL_t                 CH4_AL3_CTRL;
        CH_AL3_WRITE_ADDR_t           CH4_AL3_WRITE_ADDR;
        CH_AL3_TRANS_COUNT_t          CH4_AL3_TRANS_COUNT;
        CH_AL3_READ_ADDR_TRIG_t       CH4_AL3_READ_ADDR_TRIG;
        CH_READ_ADDR_t                CH5_READ_ADDR;
        CH_WRITE_ADDR_t               CH5_WRITE_ADDR;
        CH_TRANS_COUNT_t              CH5_TRANS_COUNT;
        CH_CTRL_TRIG_t                CH5_CTRL_TRIG;
        CH_AL1_CTRL_t                 CH5_AL1_CTRL;
        CH_AL1_READ_ADDR_t            CH5_AL1_READ_ADDR;
        CH_AL1_WRITE_ADDR_t           CH5_AL1_WRITE_ADDR;
        CH_AL1_TRANS_COUNT_TRIG_t     CH5_AL1_TRANS_COUNT_TRIG;
        CH_AL2_CTRL_t                 CH5_AL2_CTRL;
        CH_AL2_TRANS_COUNT_t          CH5_AL2_TRANS_COUNT;
        CH_AL2_READ_ADDR_t            CH5_AL2_READ_ADDR;
        CH_AL2_WRITE_ADDR_TRIG_t      CH5_AL2_WRITE_ADDR_TRIG;
        CH_AL3_CTRL_t                 CH5_AL3_CTRL;
        CH_AL3_WRITE_ADDR_t           CH5_AL3_WRITE_ADDR;
        CH_AL3_TRANS_COUNT_t          CH5_AL3_TRANS_COUNT;
        CH_AL3_READ_ADDR_TRIG_t       CH5_AL3_READ_ADDR_TRIG;
        CH_READ_ADDR_t                CH6_READ_ADDR;
        CH_WRITE_ADDR_t               CH6_WRITE_ADDR;
        CH_TRANS_COUNT_t              CH6_TRANS_COUNT;
        CH_CTRL_TRIG_t                CH6_CTRL_TRIG;
        CH_AL1_CTRL_t                 CH6_AL1_CTRL;
        CH_AL1_READ_ADDR_t            CH6_AL1_READ_ADDR;
        CH_AL1_WRITE_ADDR_t           CH6_AL1_WRITE_ADDR;
        CH_AL1_TRANS_COUNT_TRIG_t     CH6_AL1_TRANS_COUNT_TRIG;
        CH_AL2_CTRL_t                 CH6_AL2_CTRL;
        CH_AL2_TRANS_COUNT_t          CH6_AL2_TRANS_COUNT;
        CH_AL2_READ_ADDR_t            CH6_AL2_READ_ADDR;
        CH_AL2_WRITE_ADDR_TRIG_t      CH6_AL2_WRITE_ADDR_TRIG;
        CH_AL3_CTRL_t                 CH6_AL3_CTRL;
        CH_AL3_WRITE_ADDR_t           CH6_AL3_WRITE_ADDR;
        CH_AL3_TRANS_COUNT_t          CH6_AL3_TRANS_COUNT;
        CH_AL3_READ_ADDR_TRIG_t       CH6_AL3_READ_ADDR_TRIG;
        CH_READ_ADDR_t                CH7_READ_ADDR;
        CH_WRITE_ADDR_t               CH7_WRITE_ADDR;
        CH_TRANS_COUNT_t              CH7_TRANS_COUNT;
        CH_CTRL_TRIG_t                CH7_CTRL_TRIG;
        CH_AL1_CTRL_t                 CH7_AL1_CTRL;
        CH_AL1_READ_ADDR_t            CH7_AL1_READ_ADDR;
        CH_AL1_WRITE_ADDR_t           CH7_AL1_WRITE_ADDR;
        CH_AL1_TRANS_COUNT_TRIG_t     CH7_AL1_TRANS_COUNT_TRIG;
        CH_AL2_CTRL_t                 CH7_AL2_CTRL;
        CH_AL2_TRANS_COUNT_t          CH7_AL2_TRANS_COUNT;
        CH_AL2_READ_ADDR_t            CH7_AL2_READ_ADDR;
        CH_AL2_WRITE_ADDR_TRIG_t      CH7_AL2_WRITE_ADDR_TRIG;
        CH_AL3_CTRL_t                 CH7_AL3_CTRL;
        CH_AL3_WRITE_ADDR_t           CH7_AL3_WRITE_ADDR;
        CH_AL3_TRANS_COUNT_t          CH7_AL3_TRANS_COUNT;
        CH_AL3_READ_ADDR_TRIG_t       CH7_AL3_READ_ADDR_TRIG;
        CH_READ_ADDR_t                CH8_READ_ADDR;
        CH_WRITE_ADDR_t               CH8_WRITE_ADDR;
        CH_TRANS_COUNT_t              CH8_TRANS_COUNT;
        CH_CTRL_TRIG_t                CH8_CTRL_TRIG;
        CH_AL1_CTRL_t                 CH8_AL1_CTRL;
        CH_AL1_READ_ADDR_t            CH8_AL1_READ_ADDR;
        CH_AL1_WRITE_ADDR_t           CH8_AL1_WRITE_ADDR;
        CH_AL1_TRANS_COUNT_TRIG_t     CH8_AL1_TRANS_COUNT_TRIG;
        CH_AL2_CTRL_t                 CH8_AL2_CTRL;
        CH_AL2_TRANS_COUNT_t          CH8_AL2_TRANS_COUNT;
        CH_AL2_READ_ADDR_t            CH8_AL2_READ_ADDR;
        CH_AL2_WRITE_ADDR_TRIG_t      CH8_AL2_WRITE_ADDR_TRIG;
        CH_AL3_CTRL_t                 CH8_AL3_CTRL;
        CH_AL3_WRITE_ADDR_t           CH8_AL3_WRITE_ADDR;
        CH_AL3_TRANS_COUNT_t          CH8_AL3_TRANS_COUNT;
        CH_AL3_READ_ADDR_TRIG_t       CH8_AL3_READ_ADDR_TRIG;
        CH_READ_ADDR_t                CH9_READ_ADDR;
        CH_WRITE_ADDR_t               CH9_WRITE_ADDR;
        CH_TRANS_COUNT_t              CH9_TRANS_COUNT;
        CH_CTRL_TRIG_t                CH9_CTRL_TRIG;
        CH_AL1_CTRL_t                 CH9_AL1_CTRL;
        CH_AL1_READ_ADDR_t            CH9_AL1_READ_ADDR;
        CH_AL1_WRITE_ADDR_t           CH9_AL1_WRITE_ADDR;
        CH_AL1_TRANS_COUNT_TRIG_t     CH9_AL1_TRANS_COUNT_TRIG;
        CH_AL2_CTRL_t                 CH9_AL2_CTRL;
        CH_AL2_TRANS_COUNT_t          CH9_AL2_TRANS_COUNT;
        CH_AL2_READ_ADDR_t            CH9_AL2_READ_ADDR;
        CH_AL2_WRITE_ADDR_TRIG_t      CH9_AL2_WRITE_ADDR_TRIG;
        CH_AL3_CTRL_t                 CH9_AL3_CTRL;
        CH_AL3_WRITE_ADDR_t           CH9_AL3_WRITE_ADDR;
        CH_AL3_TRANS_COUNT_t          CH9_AL3_TRANS_COUNT;
        CH_AL3_READ_ADDR_TRIG_t       CH9_AL3_READ_ADDR_TRIG;
        CH_READ_ADDR_t                CH10_READ_ADDR;
        CH_WRITE_ADDR_t               CH10_WRITE_ADDR;
        CH_TRANS_COUNT_t              CH10_TRANS_COUNT;
        CH_CTRL_TRIG_t                CH10_CTRL_TRIG;
        CH_AL1_CTRL_t                 CH10_AL1_CTRL;
        CH_AL1_READ_ADDR_t            CH10_AL1_READ_ADDR;
        CH_AL1_WRITE_ADDR_t           CH10_AL1_WRITE_ADDR;
        CH_AL1_TRANS_COUNT_TRIG_t     CH10_AL1_TRANS_COUNT_TRIG;
        CH_AL2_CTRL_t                 CH10_AL2_CTRL;
        CH_AL2_TRANS_COUNT_t          CH10_AL2_TRANS_COUNT;
        CH_AL2_READ_ADDR_t            CH10_AL2_READ_ADDR;
        CH_AL2_WRITE_ADDR_TRIG_t      CH10_AL2_WRITE_ADDR_TRIG;
        CH_AL3_CTRL_t                 CH10_AL3_CTRL;
        CH_AL3_WRITE_ADDR_t           CH10_AL3_WRITE_ADDR;
        CH_AL3_TRANS_COUNT_t          CH10_AL3_TRANS_COUNT;
        CH_AL3_READ_ADDR_TRIG_t       CH10_AL3_READ_ADDR_TRIG;
        CH_READ_ADDR_t                CH11_READ_ADDR;
        CH_WRITE_ADDR_t               CH11_WRITE_ADDR;
        CH_TRANS_COUNT_t              CH11_TRANS_COUNT;
        CH_CTRL_TRIG_t                CH11_CTRL_TRIG;
        CH_AL1_CTRL_t                 CH11_AL1_CTRL;
        CH_AL1_READ_ADDR_t            CH11_AL1_READ_ADDR;
        CH_AL1_WRITE_ADDR_t           CH11_AL1_WRITE_ADDR;
        CH_AL1_TRANS_COUNT_TRIG_t     CH11_AL1_TRANS_COUNT_TRIG;
        CH_AL2_CTRL_t                 CH11_AL2_CTRL;
        CH_AL2_TRANS_COUNT_t          CH11_AL2_TRANS_COUNT;
        CH_AL2_READ_ADDR_t            CH11_AL2_READ_ADDR;
        CH_AL2_WRITE_ADDR_TRIG_t      CH11_AL2_WRITE_ADDR_TRIG;
        CH_AL3_CTRL_t                 CH11_AL3_CTRL;
        CH_AL3_WRITE_ADDR_t           CH11_AL3_WRITE_ADDR;
        CH_AL3_TRANS_COUNT_t          CH11_AL3_TRANS_COUNT;
        CH_AL3_READ_ADDR_TRIG_t       CH11_AL3_READ_ADDR_TRIG;
        CH_READ_ADDR_t                CH12_READ_ADDR;
        CH_WRITE_ADDR_t               CH12_WRITE_ADDR;
        CH_TRANS_COUNT_t              CH12_TRANS_COUNT;
        CH_CTRL_TRIG_t                CH12_CTRL_TRIG;
        CH_AL1_CTRL_t                 CH12_AL1_CTRL;
        CH_AL1_READ_ADDR_t            CH12_AL1_READ_ADDR;
        CH_AL1_WRITE_ADDR_t           CH12_AL1_WRITE_ADDR;
        CH_AL1_TRANS_COUNT_TRIG_t     CH12_AL1_TRANS_COUNT_TRIG;
        CH_AL2_CTRL_t                 CH12_AL2_CTRL;
        CH_AL2_TRANS_COUNT_t          CH12_AL2_TRANS_COUNT;
        CH_AL2_READ_ADDR_t            CH12_AL2_READ_ADDR;
        CH_AL2_WRITE_ADDR_TRIG_t      CH12_AL2_WRITE_ADDR_TRIG;
        CH_AL3_CTRL_t                 CH12_AL3_CTRL;
        CH_AL3_WRITE_ADDR_t           CH12_AL3_WRITE_ADDR;
        CH_AL3_TRANS_COUNT_t          CH12_AL3_TRANS_COUNT;
        CH_AL3_READ_ADDR_TRIG_t       CH12_AL3_READ_ADDR_TRIG;
        CH_READ_ADDR_t                CH13_READ_ADDR;
        CH_WRITE_ADDR_t               CH13_WRITE_ADDR;
        CH_TRANS_COUNT_t              CH13_TRANS_COUNT;
        CH_CTRL_TRIG_t                CH13_CTRL_TRIG;
        CH_AL1_CTRL_t                 CH13_AL1_CTRL;
        CH_AL1_READ_ADDR_t            CH13_AL1_READ_ADDR;
        CH_AL1_WRITE_ADDR_t           CH13_AL1_WRITE_ADDR;
        CH_AL1_TRANS_COUNT_TRIG_t     CH13_AL1_TRANS_COUNT_TRIG;
        CH_AL2_CTRL_t                 CH13_AL2_CTRL;
        CH_AL2_TRANS_COUNT_t          CH13_AL2_TRANS_COUNT;
        CH_AL2_READ_ADDR_t            CH13_AL2_READ_ADDR;
        CH_AL2_WRITE_ADDR_TRIG_t      CH13_AL2_WRITE_ADDR_TRIG;
        CH_AL3_CTRL_t                 CH13_AL3_CTRL;
        CH_AL3_WRITE_ADDR_t           CH13_AL3_WRITE_ADDR;
        CH_AL3_TRANS_COUNT_t          CH13_AL3_TRANS_COUNT;
        CH_AL3_READ_ADDR_TRIG_t       CH13_AL3_READ_ADDR_TRIG;
        CH_READ_ADDR_t                CH14_READ_ADDR;
        CH_WRITE_ADDR_t               CH14_WRITE_ADDR;
        CH_TRANS_COUNT_t              CH14_TRANS_COUNT;
        CH_CTRL_TRIG_t                CH14_CTRL_TRIG;
        CH_AL1_CTRL_t                 CH14_AL1_CTRL;
        CH_AL1_READ_ADDR_t            CH14_AL1_READ_ADDR;
        CH_AL1_WRITE_ADDR_t           CH14_AL1_WRITE_ADDR;
        CH_AL1_TRANS_COUNT_TRIG_t     CH14_AL1_TRANS_COUNT_TRIG;
        CH_AL2_CTRL_t                 CH14_AL2_CTRL;
        CH_AL2_TRANS_COUNT_t          CH14_AL2_TRANS_COUNT;
        CH_AL2_READ_ADDR_t            CH14_AL2_READ_ADDR;
        CH_AL2_WRITE_ADDR_TRIG_t      CH14_AL2_WRITE_ADDR_TRIG;
        CH_AL3_CTRL_t                 CH14_AL3_CTRL;
        CH_AL3_WRITE_ADDR_t           CH14_AL3_WRITE_ADDR;
        CH_AL3_TRANS_COUNT_t          CH14_AL3_TRANS_COUNT;
        CH_AL3_READ_ADDR_TRIG_t       CH14_AL3_READ_ADDR_TRIG;
        CH_READ_ADDR_t                CH15_READ_ADDR;
        CH_WRITE_ADDR_t               CH15_WRITE_ADDR;
        CH_TRANS_COUNT_t              CH15_TRANS_COUNT;
        CH_CTRL_TRIG_t                CH15_CTRL_TRIG;
        CH_AL1_CTRL_t                 CH15_AL1_CTRL;
        CH_AL1_READ_ADDR_t            CH15_AL1_READ_ADDR;
        CH_AL1_WRITE_ADDR_t           CH15_AL1_WRITE_ADDR;
        CH_AL1_TRANS_COUNT_TRIG_t     CH15_AL1_TRANS_COUNT_TRIG;
        CH_AL2_CTRL_t                 CH15_AL2_CTRL;
        CH_AL2_TRANS_COUNT_t          CH15_AL2_TRANS_COUNT;
        CH_AL2_READ_ADDR_t            CH15_AL2_READ_ADDR;
        CH_AL2_WRITE_ADDR_TRIG_t      CH15_AL2_WRITE_ADDR_TRIG;
        CH_AL3_CTRL_t                 CH15_AL3_CTRL;
        CH_AL3_WRITE_ADDR_t           CH15_AL3_WRITE_ADDR;
        CH_AL3_TRANS_COUNT_t          CH15_AL3_TRANS_COUNT;
        CH_AL3_READ_ADDR_TRIG_t       CH15_AL3_READ_ADDR_TRIG;
        INTR_t                        INTR;
        INTE0_t                       INTE0;
        INTF0_t                       INTF0;
        INTS0_t                       INTS0;
        INTR1_t                       INTR1;
        INTE1_t                       INTE1;
        INTF1_t                       INTF1;
        INTS1_t                       INTS1;
        INTR2_t                       INTR2;
        INTE2_t                       INTE2;
        INTF2_t                       INTF2;
        INTS2_t                       INTS2;
        INTR3_t                       INTR3;
        INTE3_t                       INTE3;
        INTF3_t                       INTF3;
        INTS3_t                       INTS3;
        TIMER_t                       TIMER[4];
        MULTI_CHAN_TRIGGER_t          MULTI_CHAN_TRIGGER;
        SNIFF_CTRL_t                  SNIFF_CTRL;
        SNIFF_DATA_t                  SNIFF_DATA;
        uint32_t                      reserved0;
        FIFO_LEVELS_t                 FIFO_LEVELS;
        CHAN_ABORT_t                  CHAN_ABORT;
        N_CHANNELS_t                  N_CHANNELS;
        uint32_t                      reserved1[5];
        SECCFG_CH_t                   SECCFG_CH[16];
        SECCFG_IRQ_t                  SECCFG_IRQ[4];
        SECCFG_MISC_t                 SECCFG_MISC;
        uint32_t                      reserved2[11];
        MPU_CTRL_t                    MPU_CTRL;
        MPU_BAR_t                     MPU_BAR0;
        MPU_LAR_t                     MPU_LAR0;
        MPU_BAR_t                     MPU_BAR1;
        MPU_LAR_t                     MPU_LAR1;
        MPU_BAR_t                     MPU_BAR2;
        MPU_LAR_t                     MPU_LAR2;
        MPU_BAR_t                     MPU_BAR3;
        MPU_LAR_t                     MPU_LAR3;
        MPU_BAR_t                     MPU_BAR4;
        MPU_LAR_t                     MPU_LAR4;
        MPU_BAR_t                     MPU_BAR5;
        MPU_LAR_t                     MPU_LAR5;
        MPU_BAR_t                     MPU_BAR6;
        MPU_LAR_t                     MPU_LAR6;
        MPU_BAR_t                     MPU_BAR7;
        MPU_LAR_t                     MPU_LAR7;
        uint32_t                      reserved3[175];
        CH_DBG_CTDREQ_t               CH0_DBG_CTDREQ;
        CH_DBG_TCR_t                  CH0_DBG_TCR;
        uint32_t                      reserved4[14];
        CH_DBG_CTDREQ_t               CH1_DBG_CTDREQ;
        CH_DBG_TCR_t                  CH1_DBG_TCR;
        uint32_t                      reserved5[14];
        CH_DBG_CTDREQ_t               CH2_DBG_CTDREQ;
        CH_DBG_TCR_t                  CH2_DBG_TCR;
        uint32_t                      reserved6[14];
        CH_DBG_CTDREQ_t               CH3_DBG_CTDREQ;
        CH_DBG_TCR_t                  CH3_DBG_TCR;
        uint32_t                      reserved7[14];
        CH_DBG_CTDREQ_t               CH4_DBG_CTDREQ;
        CH_DBG_TCR_t                  CH4_DBG_TCR;
        uint32_t                      reserved8[14];
        CH_DBG_CTDREQ_t               CH5_DBG_CTDREQ;
        CH_DBG_TCR_t                  CH5_DBG_TCR;
        uint32_t                      reserved9[14];
        CH_DBG_CTDREQ_t               CH6_DBG_CTDREQ;
        CH_DBG_TCR_t                  CH6_DBG_TCR;
        uint32_t                      reserved10[14];
        CH_DBG_CTDREQ_t               CH7_DBG_CTDREQ;
        CH_DBG_TCR_t                  CH7_DBG_TCR;
        uint32_t                      reserved11[14];
        CH_DBG_CTDREQ_t               CH8_DBG_CTDREQ;
        CH_DBG_TCR_t                  CH8_DBG_TCR;
        uint32_t                      reserved12[14];
        CH_DBG_CTDREQ_t               CH9_DBG_CTDREQ;
        CH_DBG_TCR_t                  CH9_DBG_TCR;
        uint32_t                      reserved13[14];
        CH_DBG_CTDREQ_t               CH10_DBG_CTDREQ;
        CH_DBG_TCR_t                  CH10_DBG_TCR;
        uint32_t                      reserved14[14];
        CH_DBG_CTDREQ_t               CH11_DBG_CTDREQ;
        CH_DBG_TCR_t                  CH11_DBG_TCR;
        uint32_t                      reserved15[14];
        CH_DBG_CTDREQ_t               CH12_DBG_CTDREQ;
        CH_DBG_TCR_t                  CH12_DBG_TCR;
        uint32_t                      reserved16[14];
        CH_DBG_CTDREQ_t               CH13_DBG_CTDREQ;
        CH_DBG_TCR_t                  CH13_DBG_TCR;
        uint32_t                      reserved17[14];
        CH_DBG_CTDREQ_t               CH14_DBG_CTDREQ;
        CH_DBG_TCR_t                  CH14_DBG_TCR;
        uint32_t                      reserved18[14];
        CH_DBG_CTDREQ_t               CH15_DBG_CTDREQ;
        CH_DBG_TCR_t                  CH15_DBG_TCR;
    };

    static DMA_t & DMA = (*(DMA_t *)0x50000000);
    static DMA_t & DMA_XOR = (*(DMA_t *)0x50001000);
    static DMA_t & DMA_SET = (*(DMA_t *)0x50002000);
    static DMA_t & DMA_CLR = (*(DMA_t *)0x50003000);

} // _DMA_

// Controls time and alarms time is a 64 bit value indicating the time since power-on timeh is the top 32 bits of time & timel is the bottom 32 bits to change time write to timelw before timehw to read time read from timelr before timehr An alarm is set by setting alarm_enable and writing to the corresponding alarm register When an alarm is pending, the corresponding alarm_running signal will be high An alarm can be cancelled before it has finished by clearing the alarm_enable When an alarm fires, the corresponding alarm_irq is set and alarm_running is cleared To clear the interrupt write a 1 to the corresponding alarm_irq The timer can be locked to prevent writing
namespace _TIMER0_  {

    // Write to bits 63:32 of time always write timelw before timehw
    // Reset value: 0x00000000
    BEGIN_TYPE(TIMEHW_t, uint32_t)
        ADD_BITFIELD_WO(TIMEHW, 0, 32)
    END_TYPE()

    // Write to bits 31:0 of time writes do not get copied to time until timehw is written
    // Reset value: 0x00000000
    BEGIN_TYPE(TIMELW_t, uint32_t)
        ADD_BITFIELD_WO(TIMELW, 0, 32)
    END_TYPE()

    // Read from bits 63:32 of time always read timelr before timehr
    // Reset value: 0x00000000
    BEGIN_TYPE(TIMEHR_t, uint32_t)
        ADD_BITFIELD_RO(TIMEHR, 0, 32)
    END_TYPE()

    // Read from bits 31:0 of time
    // Reset value: 0x00000000
    BEGIN_TYPE(TIMELR_t, uint32_t)
        ADD_BITFIELD_RO(TIMELR, 0, 32)
    END_TYPE()

    // Arm alarm 0-3, and configure the time it will fire. Once armed, the alarm fires when TIMER_ALARM[0-3] == TIMELR. The alarm will disarm itself once it fires, and can be disarmed early using the ARMED status register.
    // Reset value: 0x00000000
    typedef uint32_t ALARM_t;

    // Indicates the armed/disarmed status of each alarm. A write to the corresponding ALARMx register arms the alarm. Alarms automatically disarm upon firing, but writing ones here will disarm immediately without waiting to fire.
    // Reset value: 0x00000000
    BEGIN_TYPE(ARMED_t, uint32_t)
        ADD_BITFIELD_RW(ARMED, 0, 4)
    END_TYPE()

    // Raw read from bits 63:32 of time (no side effects)
    // Reset value: 0x00000000
    BEGIN_TYPE(TIMERAWH_t, uint32_t)
        ADD_BITFIELD_RO(TIMERAWH, 0, 32)
    END_TYPE()

    // Raw read from bits 31:0 of time (no side effects)
    // Reset value: 0x00000000
    BEGIN_TYPE(TIMERAWL_t, uint32_t)
        ADD_BITFIELD_RO(TIMERAWL, 0, 32)
    END_TYPE()

    // Set bits high to enable pause when the corresponding debug ports are active
    // Reset value: 0x00000007
    BEGIN_TYPE(DBGPAUSE_t, uint32_t)
        // Pause when processor 1 is in debug mode
        ADD_BITFIELD_RW(DBG1, 2, 1)
        // Pause when processor 0 is in debug mode
        ADD_BITFIELD_RW(DBG0, 1, 1)
    END_TYPE()

    // Set high to pause the timer
    // Reset value: 0x00000000
    BEGIN_TYPE(PAUSE_t, uint32_t)
        ADD_BITFIELD_RW(PAUSE, 0, 1)
    END_TYPE()

    // Set locked bit to disable write access to timer Once set, cannot be cleared (without a reset)
    // Reset value: 0x00000000
    BEGIN_TYPE(LOCKED_t, uint32_t)
        ADD_BITFIELD_RW(LOCKED, 0, 1)
    END_TYPE()

    // Selects the source for the timer. Defaults to the normal tick configured in the ticks block (typically configured to 1 microsecond). Writing to 1 will ignore the tick and count clk_sys cycles instead.
    // Reset value: 0x00000000
    BEGIN_TYPE(SOURCE_t, uint32_t)
        ADD_BITFIELD_RW(CLK_SYS, 0, 1)
    END_TYPE()

    static const uint32_t SOURCE_CLK_SYS__TICK = 0;
    static const uint32_t SOURCE_CLK_SYS__CLK_SYS = 1;

    // Raw Interrupts
    // Reset value: 0x00000000
    BEGIN_TYPE(INTR_t, uint32_t)
        ADD_BITFIELD_RW(ALARM_3, 3, 1)
        ADD_BITFIELD_RW(ALARM_2, 2, 1)
        ADD_BITFIELD_RW(ALARM_1, 1, 1)
        ADD_BITFIELD_RW(ALARM_0, 0, 1)
    END_TYPE()

    // Interrupt Enable
    // Reset value: 0x00000000
    BEGIN_TYPE(INTE_t, uint32_t)
        ADD_BITFIELD_RW(ALARM_3, 3, 1)
        ADD_BITFIELD_RW(ALARM_2, 2, 1)
        ADD_BITFIELD_RW(ALARM_1, 1, 1)
        ADD_BITFIELD_RW(ALARM_0, 0, 1)
    END_TYPE()

    // Interrupt Force
    // Reset value: 0x00000000
    BEGIN_TYPE(INTF_t, uint32_t)
        ADD_BITFIELD_RW(ALARM_3, 3, 1)
        ADD_BITFIELD_RW(ALARM_2, 2, 1)
        ADD_BITFIELD_RW(ALARM_1, 1, 1)
        ADD_BITFIELD_RW(ALARM_0, 0, 1)
    END_TYPE()

    // Interrupt status after masking & forcing
    // Reset value: 0x00000000
    BEGIN_TYPE(INTS_t, uint32_t)
        ADD_BITFIELD_RO(ALARM_3, 3, 1)
        ADD_BITFIELD_RO(ALARM_2, 2, 1)
        ADD_BITFIELD_RO(ALARM_1, 1, 1)
        ADD_BITFIELD_RO(ALARM_0, 0, 1)
    END_TYPE()

    struct TIMER0_t {
        TIMEHW_t                      TIMEHW;
        TIMELW_t                      TIMELW;
        TIMEHR_t                      TIMEHR;
        TIMELR_t                      TIMELR;
        ALARM_t                       ALARM[4];
        ARMED_t                       ARMED;
        TIMERAWH_t                    TIMERAWH;
        TIMERAWL_t                    TIMERAWL;
        DBGPAUSE_t                    DBGPAUSE;
        PAUSE_t                       PAUSE;
        LOCKED_t                      LOCKED;
        SOURCE_t                      SOURCE;
        INTR_t                        INTR;
        INTE_t                        INTE;
        INTF_t                        INTF;
        INTS_t                        INTS;
    };

    static TIMER0_t & TIMER0 = (*(TIMER0_t *)0x400b0000);
    static TIMER0_t & TIMER0_XOR = (*(TIMER0_t *)0x400b1000);
    static TIMER0_t & TIMER0_SET = (*(TIMER0_t *)0x400b2000);
    static TIMER0_t & TIMER0_CLR = (*(TIMER0_t *)0x400b3000);

} // _TIMER0_

namespace _TIMER1_  {

    static _TIMER0_::TIMER0_t & TIMER1 = (*(_TIMER0_::TIMER0_t *)0x400b8000);
    static _TIMER0_::TIMER0_t & TIMER1_XOR = (*(_TIMER0_::TIMER0_t *)0x400b9000);
    static _TIMER0_::TIMER0_t & TIMER1_SET = (*(_TIMER0_::TIMER0_t *)0x400ba000);
    static _TIMER0_::TIMER0_t & TIMER1_CLR = (*(_TIMER0_::TIMER0_t *)0x400bb000);

} // _TIMER1_

// Simple PWM
namespace _PWM_  {

    // Control and status register
    // Reset value: 0x00000000
    BEGIN_TYPE(CH_CSR_t, uint32_t)
        // Advance the phase of the counter by 1 count, while it is running.  Self-clearing. Write a 1, and poll until low. Counter must be running  at less than full speed (div_int + div_frac / 16 > 1)
        ADD_BITFIELD_WO(PH_ADV, 7, 1)
        // Retard the phase of the counter by 1 count, while it is running.  Self-clearing. Write a 1, and poll until low. Counter must be running.
        ADD_BITFIELD_WO(PH_RET, 6, 1)
        ADD_BITFIELD_RW(DIVMODE, 4, 2)
        // Invert output B
        ADD_BITFIELD_RW(B_INV, 3, 1)
        // Invert output A
        ADD_BITFIELD_RW(A_INV, 2, 1)
        // 1: Enable phase-correct modulation. 0: Trailing-edge
        ADD_BITFIELD_RW(PH_CORRECT, 1, 1)
        // Enable the PWM channel.
        ADD_BITFIELD_RW(EN, 0, 1)
    END_TYPE()

    // Free-running counting at rate dictated by fractional divider
    static const uint32_t CH_CSR_DIVMODE__div = 0;
    // Fractional divider operation is gated by the PWM B pin.
    static const uint32_t CH_CSR_DIVMODE__level = 1;
    // Counter advances with each rising edge of the PWM B pin.
    static const uint32_t CH_CSR_DIVMODE__rise = 2;
    // Counter advances with each falling edge of the PWM B pin.
    static const uint32_t CH_CSR_DIVMODE__fall = 3;

    // INT and FRAC form a fixed-point fractional number.  Counting rate is system clock frequency divided by this number.  Fractional division uses simple 1st-order sigma-delta.
    // Reset value: 0x00000010
    BEGIN_TYPE(CH_DIV_t, uint32_t)
        ADD_BITFIELD_RW(INT, 4, 8)
        ADD_BITFIELD_RW(FRAC, 0, 4)
    END_TYPE()

    // Direct access to the PWM counter
    // Reset value: 0x00000000
    BEGIN_TYPE(CH_CTR_t, uint32_t)
        ADD_BITFIELD_RW(CTR, 0, 16)
    END_TYPE()

    // Counter compare values
    // Reset value: 0x00000000
    BEGIN_TYPE(CH_CC_t, uint32_t)
        ADD_BITFIELD_RW(B, 16, 16)
        ADD_BITFIELD_RW(A, 0, 16)
    END_TYPE()

    // Counter wrap value
    // Reset value: 0x0000ffff
    BEGIN_TYPE(CH_TOP_t, uint32_t)
        ADD_BITFIELD_RW(TOP, 0, 16)
    END_TYPE()

    // This register aliases the CSR_EN bits for all channels.  Writing to this register allows multiple channels to be enabled  or disabled simultaneously, so they can run in perfect sync.  For each channel, there is only one physical EN register bit,  which can be accessed through here or CHx_CSR.
    // Reset value: 0x00000000
    BEGIN_TYPE(EN_t, uint32_t)
        ADD_BITFIELD_RW(CH11, 11, 1)
        ADD_BITFIELD_RW(CH10, 10, 1)
        ADD_BITFIELD_RW(CH9, 9, 1)
        ADD_BITFIELD_RW(CH8, 8, 1)
        ADD_BITFIELD_RW(CH7, 7, 1)
        ADD_BITFIELD_RW(CH6, 6, 1)
        ADD_BITFIELD_RW(CH5, 5, 1)
        ADD_BITFIELD_RW(CH4, 4, 1)
        ADD_BITFIELD_RW(CH3, 3, 1)
        ADD_BITFIELD_RW(CH2, 2, 1)
        ADD_BITFIELD_RW(CH1, 1, 1)
        ADD_BITFIELD_RW(CH0, 0, 1)
    END_TYPE()

    // Raw Interrupts
    // Reset value: 0x00000000
    BEGIN_TYPE(INTR_t, uint32_t)
        ADD_BITFIELD_RW(CH11, 11, 1)
        ADD_BITFIELD_RW(CH10, 10, 1)
        ADD_BITFIELD_RW(CH9, 9, 1)
        ADD_BITFIELD_RW(CH8, 8, 1)
        ADD_BITFIELD_RW(CH7, 7, 1)
        ADD_BITFIELD_RW(CH6, 6, 1)
        ADD_BITFIELD_RW(CH5, 5, 1)
        ADD_BITFIELD_RW(CH4, 4, 1)
        ADD_BITFIELD_RW(CH3, 3, 1)
        ADD_BITFIELD_RW(CH2, 2, 1)
        ADD_BITFIELD_RW(CH1, 1, 1)
        ADD_BITFIELD_RW(CH0, 0, 1)
    END_TYPE()

    // Interrupt Enable for irq0
    // Reset value: 0x00000000
    BEGIN_TYPE(IRQ0_INTE_t, uint32_t)
        ADD_BITFIELD_RW(CH11, 11, 1)
        ADD_BITFIELD_RW(CH10, 10, 1)
        ADD_BITFIELD_RW(CH9, 9, 1)
        ADD_BITFIELD_RW(CH8, 8, 1)
        ADD_BITFIELD_RW(CH7, 7, 1)
        ADD_BITFIELD_RW(CH6, 6, 1)
        ADD_BITFIELD_RW(CH5, 5, 1)
        ADD_BITFIELD_RW(CH4, 4, 1)
        ADD_BITFIELD_RW(CH3, 3, 1)
        ADD_BITFIELD_RW(CH2, 2, 1)
        ADD_BITFIELD_RW(CH1, 1, 1)
        ADD_BITFIELD_RW(CH0, 0, 1)
    END_TYPE()

    // Interrupt Force for irq0
    // Reset value: 0x00000000
    BEGIN_TYPE(IRQ0_INTF_t, uint32_t)
        ADD_BITFIELD_RW(CH11, 11, 1)
        ADD_BITFIELD_RW(CH10, 10, 1)
        ADD_BITFIELD_RW(CH9, 9, 1)
        ADD_BITFIELD_RW(CH8, 8, 1)
        ADD_BITFIELD_RW(CH7, 7, 1)
        ADD_BITFIELD_RW(CH6, 6, 1)
        ADD_BITFIELD_RW(CH5, 5, 1)
        ADD_BITFIELD_RW(CH4, 4, 1)
        ADD_BITFIELD_RW(CH3, 3, 1)
        ADD_BITFIELD_RW(CH2, 2, 1)
        ADD_BITFIELD_RW(CH1, 1, 1)
        ADD_BITFIELD_RW(CH0, 0, 1)
    END_TYPE()

    // Interrupt status after masking & forcing for irq0
    // Reset value: 0x00000000
    BEGIN_TYPE(IRQ0_INTS_t, uint32_t)
        ADD_BITFIELD_RO(CH11, 11, 1)
        ADD_BITFIELD_RO(CH10, 10, 1)
        ADD_BITFIELD_RO(CH9, 9, 1)
        ADD_BITFIELD_RO(CH8, 8, 1)
        ADD_BITFIELD_RO(CH7, 7, 1)
        ADD_BITFIELD_RO(CH6, 6, 1)
        ADD_BITFIELD_RO(CH5, 5, 1)
        ADD_BITFIELD_RO(CH4, 4, 1)
        ADD_BITFIELD_RO(CH3, 3, 1)
        ADD_BITFIELD_RO(CH2, 2, 1)
        ADD_BITFIELD_RO(CH1, 1, 1)
        ADD_BITFIELD_RO(CH0, 0, 1)
    END_TYPE()

    // Interrupt Enable for irq1
    // Reset value: 0x00000000
    BEGIN_TYPE(IRQ1_INTE_t, uint32_t)
        ADD_BITFIELD_RW(CH11, 11, 1)
        ADD_BITFIELD_RW(CH10, 10, 1)
        ADD_BITFIELD_RW(CH9, 9, 1)
        ADD_BITFIELD_RW(CH8, 8, 1)
        ADD_BITFIELD_RW(CH7, 7, 1)
        ADD_BITFIELD_RW(CH6, 6, 1)
        ADD_BITFIELD_RW(CH5, 5, 1)
        ADD_BITFIELD_RW(CH4, 4, 1)
        ADD_BITFIELD_RW(CH3, 3, 1)
        ADD_BITFIELD_RW(CH2, 2, 1)
        ADD_BITFIELD_RW(CH1, 1, 1)
        ADD_BITFIELD_RW(CH0, 0, 1)
    END_TYPE()

    // Interrupt Force for irq1
    // Reset value: 0x00000000
    BEGIN_TYPE(IRQ1_INTF_t, uint32_t)
        ADD_BITFIELD_RW(CH11, 11, 1)
        ADD_BITFIELD_RW(CH10, 10, 1)
        ADD_BITFIELD_RW(CH9, 9, 1)
        ADD_BITFIELD_RW(CH8, 8, 1)
        ADD_BITFIELD_RW(CH7, 7, 1)
        ADD_BITFIELD_RW(CH6, 6, 1)
        ADD_BITFIELD_RW(CH5, 5, 1)
        ADD_BITFIELD_RW(CH4, 4, 1)
        ADD_BITFIELD_RW(CH3, 3, 1)
        ADD_BITFIELD_RW(CH2, 2, 1)
        ADD_BITFIELD_RW(CH1, 1, 1)
        ADD_BITFIELD_RW(CH0, 0, 1)
    END_TYPE()

    // Interrupt status after masking & forcing for irq1
    // Reset value: 0x00000000
    BEGIN_TYPE(IRQ1_INTS_t, uint32_t)
        ADD_BITFIELD_RO(CH11, 11, 1)
        ADD_BITFIELD_RO(CH10, 10, 1)
        ADD_BITFIELD_RO(CH9, 9, 1)
        ADD_BITFIELD_RO(CH8, 8, 1)
        ADD_BITFIELD_RO(CH7, 7, 1)
        ADD_BITFIELD_RO(CH6, 6, 1)
        ADD_BITFIELD_RO(CH5, 5, 1)
        ADD_BITFIELD_RO(CH4, 4, 1)
        ADD_BITFIELD_RO(CH3, 3, 1)
        ADD_BITFIELD_RO(CH2, 2, 1)
        ADD_BITFIELD_RO(CH1, 1, 1)
        ADD_BITFIELD_RO(CH0, 0, 1)
    END_TYPE()

    struct PWM_t {
        CH_CSR_t                      CH0_CSR;
        CH_DIV_t                      CH0_DIV;
        CH_CTR_t                      CH0_CTR;
        CH_CC_t                       CH0_CC;
        CH_TOP_t                      CH0_TOP;
        CH_CSR_t                      CH1_CSR;
        CH_DIV_t                      CH1_DIV;
        CH_CTR_t                      CH1_CTR;
        CH_CC_t                       CH1_CC;
        CH_TOP_t                      CH1_TOP;
        CH_CSR_t                      CH2_CSR;
        CH_DIV_t                      CH2_DIV;
        CH_CTR_t                      CH2_CTR;
        CH_CC_t                       CH2_CC;
        CH_TOP_t                      CH2_TOP;
        CH_CSR_t                      CH3_CSR;
        CH_DIV_t                      CH3_DIV;
        CH_CTR_t                      CH3_CTR;
        CH_CC_t                       CH3_CC;
        CH_TOP_t                      CH3_TOP;
        CH_CSR_t                      CH4_CSR;
        CH_DIV_t                      CH4_DIV;
        CH_CTR_t                      CH4_CTR;
        CH_CC_t                       CH4_CC;
        CH_TOP_t                      CH4_TOP;
        CH_CSR_t                      CH5_CSR;
        CH_DIV_t                      CH5_DIV;
        CH_CTR_t                      CH5_CTR;
        CH_CC_t                       CH5_CC;
        CH_TOP_t                      CH5_TOP;
        CH_CSR_t                      CH6_CSR;
        CH_DIV_t                      CH6_DIV;
        CH_CTR_t                      CH6_CTR;
        CH_CC_t                       CH6_CC;
        CH_TOP_t                      CH6_TOP;
        CH_CSR_t                      CH7_CSR;
        CH_DIV_t                      CH7_DIV;
        CH_CTR_t                      CH7_CTR;
        CH_CC_t                       CH7_CC;
        CH_TOP_t                      CH7_TOP;
        CH_CSR_t                      CH8_CSR;
        CH_DIV_t                      CH8_DIV;
        CH_CTR_t                      CH8_CTR;
        CH_CC_t                       CH8_CC;
        CH_TOP_t                      CH8_TOP;
        CH_CSR_t                      CH9_CSR;
        CH_DIV_t                      CH9_DIV;
        CH_CTR_t                      CH9_CTR;
        CH_CC_t                       CH9_CC;
        CH_TOP_t                      CH9_TOP;
        CH_CSR_t                      CH10_CSR;
        CH_DIV_t                      CH10_DIV;
        CH_CTR_t                      CH10_CTR;
        CH_CC_t                       CH10_CC;
        CH_TOP_t                      CH10_TOP;
        CH_CSR_t                      CH11_CSR;
        CH_DIV_t                      CH11_DIV;
        CH_CTR_t                      CH11_CTR;
        CH_CC_t                       CH11_CC;
        CH_TOP_t                      CH11_TOP;
        EN_t                          EN;
        INTR_t                        INTR;
        IRQ0_INTE_t                   IRQ0_INTE;
        IRQ0_INTF_t                   IRQ0_INTF;
        IRQ0_INTS_t                   IRQ0_INTS;
        IRQ1_INTE_t                   IRQ1_INTE;
        IRQ1_INTF_t                   IRQ1_INTF;
        IRQ1_INTS_t                   IRQ1_INTS;
    };

    static PWM_t & PWM = (*(PWM_t *)0x400a8000);
    static PWM_t & PWM_XOR = (*(PWM_t *)0x400a9000);
    static PWM_t & PWM_SET = (*(PWM_t *)0x400aa000);
    static PWM_t & PWM_CLR = (*(PWM_t *)0x400ab000);

} // _PWM_

// Control and data interface to SAR ADC
namespace _ADC_  {

    // ADC Control and Status
    // Reset value: 0x00000000
    BEGIN_TYPE(CS_t, uint32_t)
        // Round-robin sampling. 1 bit per channel. Set all bits to 0 to disable.  Otherwise, the ADC will cycle through each enabled channel in a round-robin fashion.  The first channel to be sampled will be the one currently indicated by AINSEL.  AINSEL will be updated after each conversion with the newly-selected channel.
        ADD_BITFIELD_RW(RROBIN, 16, 9)
        // Select analog mux input. Updated automatically in round-robin mode.  This is corrected for the package option so only ADC channels which are bonded are available, and in the correct order
        ADD_BITFIELD_RW(AINSEL, 12, 4)
        // Some past ADC conversion encountered an error. Write 1 to clear.
        ADD_BITFIELD_RW(ERR_STICKY, 10, 1)
        // The most recent ADC conversion encountered an error; result is undefined or noisy.
        ADD_BITFIELD_RO(ERR, 9, 1)
        // 1 if the ADC is ready to start a new conversion. Implies any previous conversion has completed.  0 whilst conversion in progress.
        ADD_BITFIELD_RO(READY, 8, 1)
        // Continuously perform conversions whilst this bit is 1. A new conversion will start immediately after the previous finishes.
        ADD_BITFIELD_RW(START_MANY, 3, 1)
        // Start a single conversion. Self-clearing. Ignored if start_many is asserted.
        ADD_BITFIELD_WO(START_ONCE, 2, 1)
        // Power on temperature sensor. 1 - enabled. 0 - disabled.
        ADD_BITFIELD_RW(TS_EN, 1, 1)
        // Power on ADC and enable its clock.  1 - enabled. 0 - disabled.
        ADD_BITFIELD_RW(EN, 0, 1)
    END_TYPE()

    // Result of most recent ADC conversion
    // Reset value: 0x00000000
    BEGIN_TYPE(RESULT_t, uint32_t)
        ADD_BITFIELD_RO(RESULT, 0, 12)
    END_TYPE()

    // FIFO control and status
    // Reset value: 0x00000000
    BEGIN_TYPE(FCS_t, uint32_t)
        // DREQ/IRQ asserted when level >= threshold
        ADD_BITFIELD_RW(THRESH, 24, 4)
        // The number of conversion results currently waiting in the FIFO
        ADD_BITFIELD_RO(LEVEL, 16, 4)
        // 1 if the FIFO has been overflowed. Write 1 to clear.
        ADD_BITFIELD_RW(OVER, 11, 1)
        // 1 if the FIFO has been underflowed. Write 1 to clear.
        ADD_BITFIELD_RW(UNDER, 10, 1)
        ADD_BITFIELD_RO(FULL, 9, 1)
        ADD_BITFIELD_RO(EMPTY, 8, 1)
        // If 1: assert DMA requests when FIFO contains data
        ADD_BITFIELD_RW(DREQ_EN, 3, 1)
        // If 1: conversion error bit appears in the FIFO alongside the result
        ADD_BITFIELD_RW(ERR, 2, 1)
        // If 1: FIFO results are right-shifted to be one byte in size. Enables DMA to byte buffers.
        ADD_BITFIELD_RW(SHIFT, 1, 1)
        // If 1: write result to the FIFO after each conversion.
        ADD_BITFIELD_RW(EN, 0, 1)
    END_TYPE()

    // Conversion result FIFO
    // Reset value: 0x00000000
    BEGIN_TYPE(FIFO_t, uint32_t)
        // 1 if this particular sample experienced a conversion error. Remains in the same location if the sample is shifted.
        ADD_BITFIELD_RO(ERR, 15, 1)
        ADD_BITFIELD_RO(VAL, 0, 12)
    END_TYPE()

    // Clock divider. If non-zero, CS_START_MANY will start conversions  at regular intervals rather than back-to-back.  The divider is reset when either of these fields are written.  Total period is 1 + INT + FRAC / 256
    // Reset value: 0x00000000
    BEGIN_TYPE(DIV_t, uint32_t)
        // Integer part of clock divisor.
        ADD_BITFIELD_RW(INT, 8, 16)
        // Fractional part of clock divisor. First-order delta-sigma.
        ADD_BITFIELD_RW(FRAC, 0, 8)
    END_TYPE()

    // Raw Interrupts
    // Reset value: 0x00000000
    BEGIN_TYPE(INTR_t, uint32_t)
        // Triggered when the sample FIFO reaches a certain level.  This level can be programmed via the FCS_THRESH field.
        ADD_BITFIELD_RO(FIFO, 0, 1)
    END_TYPE()

    // Interrupt Enable
    // Reset value: 0x00000000
    BEGIN_TYPE(INTE_t, uint32_t)
        // Triggered when the sample FIFO reaches a certain level.  This level can be programmed via the FCS_THRESH field.
        ADD_BITFIELD_RW(FIFO, 0, 1)
    END_TYPE()

    // Interrupt Force
    // Reset value: 0x00000000
    BEGIN_TYPE(INTF_t, uint32_t)
        // Triggered when the sample FIFO reaches a certain level.  This level can be programmed via the FCS_THRESH field.
        ADD_BITFIELD_RW(FIFO, 0, 1)
    END_TYPE()

    // Interrupt status after masking & forcing
    // Reset value: 0x00000000
    BEGIN_TYPE(INTS_t, uint32_t)
        // Triggered when the sample FIFO reaches a certain level.  This level can be programmed via the FCS_THRESH field.
        ADD_BITFIELD_RO(FIFO, 0, 1)
    END_TYPE()

    struct ADC_t {
        CS_t                          CS;
        RESULT_t                      RESULT;
        FCS_t                         FCS;
        FIFO_t                        FIFO;
        DIV_t                         DIV;
        INTR_t                        INTR;
        INTE_t                        INTE;
        INTF_t                        INTF;
        INTS_t                        INTS;
    };

    static ADC_t & ADC = (*(ADC_t *)0x400a0000);
    static ADC_t & ADC_XOR = (*(ADC_t *)0x400a1000);
    static ADC_t & ADC_SET = (*(ADC_t *)0x400a2000);
    static ADC_t & ADC_CLR = (*(ADC_t *)0x400a3000);

} // _ADC_

// DW_apb_i2c address block List of configuration constants for the Synopsys I2C hardware (you may see references to these in I2C register header; these are *fixed* values, set at hardware design time): IC_ULTRA_FAST_MODE ................ 0x0  IC_UFM_TBUF_CNT_DEFAULT ........... 0x8  IC_UFM_SCL_LOW_COUNT .............. 0x0008  IC_UFM_SCL_HIGH_COUNT ............. 0x0006  IC_TX_TL .......................... 0x0  IC_TX_CMD_BLOCK ................... 0x1  IC_HAS_DMA ........................ 0x1  IC_HAS_ASYNC_FIFO ................. 0x0  IC_SMBUS_ARP ...................... 0x0  IC_FIRST_DATA_BYTE_STATUS ......... 0x1  IC_INTR_IO ........................ 0x1  IC_MASTER_MODE .................... 0x1  IC_DEFAULT_ACK_GENERAL_CALL ....... 0x1  IC_INTR_POL ....................... 0x1  IC_OPTIONAL_SAR ................... 0x0  IC_DEFAULT_TAR_SLAVE_ADDR ......... 0x055  IC_DEFAULT_SLAVE_ADDR ............. 0x055  IC_DEFAULT_HS_SPKLEN .............. 0x1  IC_FS_SCL_HIGH_COUNT .............. 0x0006  IC_HS_SCL_LOW_COUNT ............... 0x0008  IC_DEVICE_ID_VALUE ................ 0x0  IC_10BITADDR_MASTER ............... 0x0  IC_CLK_FREQ_OPTIMIZATION .......... 0x0  IC_DEFAULT_FS_SPKLEN .............. 0x7  IC_ADD_ENCODED_PARAMS ............. 0x0  IC_DEFAULT_SDA_HOLD ............... 0x000001  IC_DEFAULT_SDA_SETUP .............. 0x64  IC_AVOID_RX_FIFO_FLUSH_ON_TX_ABRT . 0x0  IC_CLOCK_PERIOD ................... 100  IC_EMPTYFIFO_HOLD_MASTER_EN ....... 1  IC_RESTART_EN ..................... 0x1  IC_TX_CMD_BLOCK_DEFAULT ........... 0x0  IC_BUS_CLEAR_FEATURE .............. 0x0  IC_CAP_LOADING .................... 100  IC_FS_SCL_LOW_COUNT ............... 0x000d  APB_DATA_WIDTH .................... 32  IC_SDA_STUCK_TIMEOUT_DEFAULT ...... 0xffffffff  IC_SLV_DATA_NACK_ONLY ............. 0x1  IC_10BITADDR_SLAVE ................ 0x0  IC_CLK_TYPE ....................... 0x0  IC_SMBUS_UDID_MSB ................. 0x0  IC_SMBUS_SUSPEND_ALERT ............ 0x0  IC_HS_SCL_HIGH_COUNT .............. 0x0006  IC_SLV_RESTART_DET_EN ............. 0x1  IC_SMBUS .......................... 0x0  IC_OPTIONAL_SAR_DEFAULT ........... 0x0  IC_PERSISTANT_SLV_ADDR_DEFAULT .... 0x0  IC_USE_COUNTS ..................... 0x0  IC_RX_BUFFER_DEPTH ................ 16  IC_SCL_STUCK_TIMEOUT_DEFAULT ...... 0xffffffff  IC_RX_FULL_HLD_BUS_EN ............. 0x1  IC_SLAVE_DISABLE .................. 0x1  IC_RX_TL .......................... 0x0  IC_DEVICE_ID ...................... 0x0  IC_HC_COUNT_VALUES ................ 0x0  I2C_DYNAMIC_TAR_UPDATE ............ 0  IC_SMBUS_CLK_LOW_MEXT_DEFAULT ..... 0xffffffff  IC_SMBUS_CLK_LOW_SEXT_DEFAULT ..... 0xffffffff  IC_HS_MASTER_CODE ................. 0x1  IC_SMBUS_RST_IDLE_CNT_DEFAULT ..... 0xffff  IC_SMBUS_UDID_LSB_DEFAULT ......... 0xffffffff  IC_SS_SCL_HIGH_COUNT .............. 0x0028  IC_SS_SCL_LOW_COUNT ............... 0x002f  IC_MAX_SPEED_MODE ................. 0x2  IC_STAT_FOR_CLK_STRETCH ........... 0x0  IC_STOP_DET_IF_MASTER_ACTIVE ...... 0x0  IC_DEFAULT_UFM_SPKLEN ............. 0x1  IC_TX_BUFFER_DEPTH ................ 16
namespace _I2C0_  {

    // I2C Control Register. This register can be written only when the DW_apb_i2c is disabled, which corresponds to the IC_ENABLE[0] register being set to 0. Writes at other times have no effect. Read/Write Access: - bit 10 is read only. - bit 11 is read only - bit 16 is read only - bit 17 is read only - bits 18 and 19 are read only.
    // Reset value: 0x00000065
    BEGIN_TYPE(IC_CON_t, uint32_t)
        // Master issues the STOP_DET interrupt irrespective of whether master is active or not
        ADD_BITFIELD_RO(STOP_DET_IF_MASTER_ACTIVE, 10, 1)
        // This bit controls whether DW_apb_i2c should hold the bus when the Rx FIFO is physically full to its RX_BUFFER_DEPTH, as described in the IC_RX_FULL_HLD_BUS_EN parameter. Reset value: 0x0.
        ADD_BITFIELD_RW(RX_FIFO_FULL_HLD_CTRL, 9, 1)
        // This bit controls the generation of the TX_EMPTY interrupt, as described in the IC_RAW_INTR_STAT register. Reset value: 0x0.
        ADD_BITFIELD_RW(TX_EMPTY_CTRL, 8, 1)
        // In slave mode: - 1'b1:  issues the STOP_DET interrupt only when it is addressed. - 1'b0:  issues the STOP_DET irrespective of whether it's addressed or not. Reset value: 0x0 NOTE: During a general call address, this slave does not issue the STOP_DET interrupt if STOP_DET_IF_ADDRESSED = 1'b1, even if the slave responds to the general call address by generating ACK. The STOP_DET interrupt is generated only when the transmitted address matches the slave address (SAR).
        ADD_BITFIELD_RW(STOP_DET_IFADDRESSED, 7, 1)
        // This bit controls whether I2C has its slave disabled, which means once the presetn signal is applied, then this bit is set and the slave is disabled. If this bit is set (slave is disabled), DW_apb_i2c functions only as a master and does not perform any action that requires a slave. NOTE: Software should ensure that if this bit is written with 0, then bit 0 should also be written with a 0.
        ADD_BITFIELD_RW(IC_SLAVE_DISABLE, 6, 1)
        // Determines whether RESTART conditions may be sent when acting as a master. Some older slaves do not support handling RESTART conditions; however, RESTART conditions are used in several DW_apb_i2c operations. When RESTART is disabled, the master is prohibited from performing the following functions: - Sending a START BYTE - Performing any high-speed mode operation - High-speed mode operation - Performing direction changes in combined format mode - Performing a read operation with a 10-bit address By replacing RESTART condition followed by a STOP and a subsequent START condition, split operations are broken down into multiple DW_apb_i2c transfers. If the above operations are performed, it will result in setting bit 6 (TX_ABRT) of the IC_RAW_INTR_STAT register. Reset value: ENABLED
        ADD_BITFIELD_RW(IC_RESTART_EN, 5, 1)
        // Controls whether the DW_apb_i2c starts its transfers in 7- or 10-bit addressing mode when acting as a master. - 0: 7-bit addressing - 1: 10-bit addressing
        ADD_BITFIELD_RW(IC_10BITADDR_MASTER, 4, 1)
        // When acting as a slave, this bit controls whether the DW_apb_i2c responds to 7- or 10-bit addresses. - 0: 7-bit addressing. The DW_apb_i2c ignores transactions that involve 10-bit addressing; for 7-bit addressing, only the lower 7 bits of the IC_SAR register are compared. - 1: 10-bit addressing. The DW_apb_i2c responds to only 10-bit addressing transfers that match the full 10 bits of the IC_SAR register.
        ADD_BITFIELD_RW(IC_10BITADDR_SLAVE, 3, 1)
        // These bits control at which speed the DW_apb_i2c operates; its setting is relevant only if one is operating the DW_apb_i2c in master mode. Hardware protects against illegal values being programmed by software. These bits must be programmed appropriately for slave mode also, as it is used to capture correct value of spike filter as per the speed mode. This register should be programmed only with a value in the range of 1 to IC_MAX_SPEED_MODE; otherwise, hardware updates this register with the value of IC_MAX_SPEED_MODE. 1: standard mode (100 kbit/s) 2: fast mode (<=400 kbit/s) or fast mode plus (<=1000Kbit/s) 3: high speed mode (3.4 Mbit/s) Note: This field is not applicable when IC_ULTRA_FAST_MODE=1
        ADD_BITFIELD_RW(SPEED, 1, 2)
        // This bit controls whether the DW_apb_i2c master is enabled. NOTE: Software should ensure that if this bit is written with '1' then bit 6 should also be written with a '1'.
        ADD_BITFIELD_RW(MASTER_MODE, 0, 1)
    END_TYPE()

    // Overflow when RX_FIFO is full
    static const uint32_t IC_CON_RX_FIFO_FULL_HLD_CTRL__DISABLED = 0;
    // Hold bus when RX_FIFO is full
    static const uint32_t IC_CON_RX_FIFO_FULL_HLD_CTRL__ENABLED = 1;
    // Default behaviour of TX_EMPTY interrupt
    static const uint32_t IC_CON_TX_EMPTY_CTRL__DISABLED = 0;
    // Controlled generation of TX_EMPTY interrupt
    static const uint32_t IC_CON_TX_EMPTY_CTRL__ENABLED = 1;
    // slave issues STOP_DET intr always
    static const uint32_t IC_CON_STOP_DET_IFADDRESSED__DISABLED = 0;
    // slave issues STOP_DET intr only if addressed
    static const uint32_t IC_CON_STOP_DET_IFADDRESSED__ENABLED = 1;
    // Slave mode is enabled
    static const uint32_t IC_CON_IC_SLAVE_DISABLE__SLAVE_ENABLED = 0;
    // Slave mode is disabled
    static const uint32_t IC_CON_IC_SLAVE_DISABLE__SLAVE_DISABLED = 1;
    // Master restart disabled
    static const uint32_t IC_CON_IC_RESTART_EN__DISABLED = 0;
    // Master restart enabled
    static const uint32_t IC_CON_IC_RESTART_EN__ENABLED = 1;
    // Master 7Bit addressing mode
    static const uint32_t IC_CON_IC_10BITADDR_MASTER__ADDR_7BITS = 0;
    // Master 10Bit addressing mode
    static const uint32_t IC_CON_IC_10BITADDR_MASTER__ADDR_10BITS = 1;
    // Slave 7Bit addressing
    static const uint32_t IC_CON_IC_10BITADDR_SLAVE__ADDR_7BITS = 0;
    // Slave 10Bit addressing
    static const uint32_t IC_CON_IC_10BITADDR_SLAVE__ADDR_10BITS = 1;
    // Standard Speed mode of operation
    static const uint32_t IC_CON_SPEED__STANDARD = 1;
    // Fast or Fast Plus mode of operation
    static const uint32_t IC_CON_SPEED__FAST = 2;
    // High Speed mode of operation
    static const uint32_t IC_CON_SPEED__HIGH = 3;
    // Master mode is disabled
    static const uint32_t IC_CON_MASTER_MODE__DISABLED = 0;
    // Master mode is enabled
    static const uint32_t IC_CON_MASTER_MODE__ENABLED = 1;

    // I2C Target Address Register This register is 12 bits wide, and bits 31:12 are reserved. This register can be written to only when IC_ENABLE[0] is set to 0. Note: If the software or application is aware that the DW_apb_i2c is not using the TAR address for the pending commands in the Tx FIFO, then it is possible to update the TAR address even while the Tx FIFO has entries (IC_STATUS[2]= 0). - It is not necessary to perform any write to this register if DW_apb_i2c is enabled as an I2C slave only.
    // Reset value: 0x00000055
    BEGIN_TYPE(IC_TAR_t, uint32_t)
        // This bit indicates whether software performs a Device-ID or General Call or START BYTE command. - 0: ignore bit 10 GC_OR_START and use IC_TAR normally - 1: perform special I2C command as specified in Device_ID or GC_OR_START bit Reset value: 0x0
        ADD_BITFIELD_RW(SPECIAL, 11, 1)
        // If bit 11 (SPECIAL) is set to 1 and bit 13(Device-ID) is set to 0, then this bit indicates whether a General Call or START byte command is to be performed by the DW_apb_i2c. - 0: General Call Address - after issuing a General Call, only writes may be performed. Attempting to issue a read command results in setting bit 6 (TX_ABRT) of the IC_RAW_INTR_STAT register. The DW_apb_i2c remains in General Call mode until the SPECIAL bit value (bit 11) is cleared. - 1: START BYTE Reset value: 0x0
        ADD_BITFIELD_RW(GC_OR_START, 10, 1)
        // This is the target address for any master transaction. When transmitting a General Call, these bits are ignored. To generate a START BYTE, the CPU needs to write only once into these bits. If the IC_TAR and IC_SAR are the same, loopback exists but the FIFOs are shared between master and slave, so full loopback is not feasible. Only one direction loopback mode is supported (simplex), not duplex. A master cannot transmit to itself; it can transmit to only a slave.
        ADD_BITFIELD_RW(IC_TAR, 0, 10)
    END_TYPE()

    // Disables programming of GENERAL_CALL or START_BYTE transmission
    static const uint32_t IC_TAR_SPECIAL__DISABLED = 0;
    // Enables programming of GENERAL_CALL or START_BYTE transmission
    static const uint32_t IC_TAR_SPECIAL__ENABLED = 1;
    // GENERAL_CALL byte transmission
    static const uint32_t IC_TAR_GC_OR_START__GENERAL_CALL = 0;
    // START byte transmission
    static const uint32_t IC_TAR_GC_OR_START__START_BYTE = 1;

    // I2C Slave Address Register
    // Reset value: 0x00000055
    BEGIN_TYPE(IC_SAR_t, uint32_t)
        // The IC_SAR holds the slave address when the I2C is operating as a slave. For 7-bit addressing, only IC_SAR[6:0] is used. This register can be written only when the I2C interface is disabled, which corresponds to the IC_ENABLE[0] register being set to 0. Writes at other times have no effect. Note: The default values cannot be any of the reserved address locations: that is, 0x00 to 0x07, or 0x78 to 0x7f. The correct operation of the device is not guaranteed if you program the IC_SAR or IC_TAR to a reserved value. Refer to <<table_I2C_firstbyte_bit_defs>> for a complete list of these reserved values.
        ADD_BITFIELD_RW(IC_SAR, 0, 10)
    END_TYPE()

    // I2C Rx/Tx Data Buffer and Command Register; this is the register the CPU writes to when filling the TX FIFO and the CPU reads from when retrieving bytes from RX FIFO. The size of the register changes as follows: Write: - 11 bits when IC_EMPTYFIFO_HOLD_MASTER_EN=1 - 9 bits when IC_EMPTYFIFO_HOLD_MASTER_EN=0 Read: - 12 bits when IC_FIRST_DATA_BYTE_STATUS = 1 - 8 bits when IC_FIRST_DATA_BYTE_STATUS = 0 Note: In order for the DW_apb_i2c to continue acknowledging reads, a read command should be written for every byte that is to be received; otherwise the DW_apb_i2c will stop acknowledging.
    // Reset value: 0x00000000
    BEGIN_TYPE(IC_DATA_CMD_t, uint32_t)
        // Indicates the first data byte received after the address phase for receive transfer in Master receiver or Slave receiver mode. Reset value : 0x0 NOTE:  In case of APB_DATA_WIDTH=8, 1. The user has to perform two APB Reads to IC_DATA_CMD in order to get status on 11 bit. 2. In order to read the 11 bit, the user has to perform the first data byte read [7:0] (offset 0x10) and then perform the second read [15:8] (offset 0x11) in order to know the status of 11 bit (whether the data received in previous read is a first data byte or not). 3. The 11th bit is an optional read field, user can ignore 2nd byte read [15:8] (offset 0x11) if not interested in FIRST_DATA_BYTE status.
        ADD_BITFIELD_RO(FIRST_DATA_BYTE, 11, 1)
        // This bit controls whether a RESTART is issued before the byte is sent or received. 1 - If IC_RESTART_EN is 1, a RESTART is issued before the data is sent/received (according to the value of CMD), regardless of whether or not the transfer direction is changing from the previous command; if IC_RESTART_EN is 0, a STOP followed by a START is issued instead. 0 - If IC_RESTART_EN is 1, a RESTART is issued only if the transfer direction is changing from the previous command; if IC_RESTART_EN is 0, a STOP followed by a START is issued instead. Reset value: 0x0
        ADD_BITFIELD_WO(RESTART, 10, 1)
        // This bit controls whether a STOP is issued after the byte is sent or received. - 1 - STOP is issued after this byte, regardless of whether or not the Tx FIFO is empty. If the Tx FIFO is not empty, the master immediately tries to start a new transfer by issuing a START and arbitrating for the bus. - 0 - STOP is not issued after this byte, regardless of whether or not the Tx FIFO is empty. If the Tx FIFO is not empty, the master continues the current transfer by sending/receiving data bytes according to the value of the CMD bit. If the Tx FIFO is empty, the master holds the SCL line low and stalls the bus until a new command is available in the Tx FIFO. Reset value: 0x0
        ADD_BITFIELD_WO(STOP, 9, 1)
        // This bit controls whether a read or a write is performed. This bit does not control the direction when the DW_apb_i2con acts as a slave. It controls only the direction when it acts as a master. When a command is entered in the TX FIFO, this bit distinguishes the write and read commands. In slave-receiver mode, this bit is a 'don't care' because writes to this register are not required. In slave-transmitter mode, a '0' indicates that the data in IC_DATA_CMD is to be transmitted. When programming this bit, you should remember the following: attempting to perform a read operation after a General Call command has been sent results in a TX_ABRT interrupt (bit 6 of the IC_RAW_INTR_STAT register), unless bit 11 (SPECIAL) in the IC_TAR register has been cleared. If a '1' is written to this bit after receiving a RD_REQ interrupt, then a TX_ABRT interrupt occurs. Reset value: 0x0
        ADD_BITFIELD_WO(CMD, 8, 1)
        // This register contains the data to be transmitted or received on the I2C bus. If you are writing to this register and want to perform a read, bits 7:0 (DAT) are ignored by the DW_apb_i2c. However, when you read this register, these bits return the value of data received on the DW_apb_i2c interface. Reset value: 0x0
        ADD_BITFIELD_RW(DAT, 0, 8)
    END_TYPE()

    // Sequential data byte received
    static const uint32_t IC_DATA_CMD_FIRST_DATA_BYTE__INACTIVE = 0;
    // Non sequential data byte received
    static const uint32_t IC_DATA_CMD_FIRST_DATA_BYTE__ACTIVE = 1;
    // Don't Issue RESTART before this command
    static const uint32_t IC_DATA_CMD_RESTART__DISABLE = 0;
    // Issue RESTART before this command
    static const uint32_t IC_DATA_CMD_RESTART__ENABLE = 1;
    // Don't Issue STOP after this command
    static const uint32_t IC_DATA_CMD_STOP__DISABLE = 0;
    // Issue STOP after this command
    static const uint32_t IC_DATA_CMD_STOP__ENABLE = 1;
    // Master Write Command
    static const uint32_t IC_DATA_CMD_CMD__WRITE = 0;
    // Master Read Command
    static const uint32_t IC_DATA_CMD_CMD__READ = 1;

    // Standard Speed I2C Clock SCL High Count Register
    // Reset value: 0x00000028
    BEGIN_TYPE(IC_SS_SCL_HCNT_t, uint32_t)
        // This register must be set before any I2C bus transaction can take place to ensure proper I/O timing. This register sets the SCL clock high-period count for standard speed. For more information, refer to 'IC_CLK Frequency Configuration'. This register can be written only when the I2C interface is disabled which corresponds to the IC_ENABLE[0] register being set to 0. Writes at other times have no effect. The minimum valid value is 6; hardware prevents values less than this being written, and if attempted results in 6 being set. For designs with APB_DATA_WIDTH = 8, the order of programming is important to ensure the correct operation of the DW_apb_i2c. The lower byte must be programmed first. Then the upper byte is programmed. NOTE: This register must not be programmed to a value higher than 65525, because DW_apb_i2c uses a 16-bit counter to flag an I2C bus idle condition when this counter reaches a value of IC_SS_SCL_HCNT + 10.
        ADD_BITFIELD_RW(IC_SS_SCL_HCNT, 0, 16)
    END_TYPE()

    // Standard Speed I2C Clock SCL Low Count Register
    // Reset value: 0x0000002f
    BEGIN_TYPE(IC_SS_SCL_LCNT_t, uint32_t)
        // This register must be set before any I2C bus transaction can take place to ensure proper I/O timing. This register sets the SCL clock low period count for standard speed. For more information, refer to 'IC_CLK Frequency Configuration' This register can be written only when the I2C interface is disabled which corresponds to the IC_ENABLE[0] register being set to 0. Writes at other times have no effect. The minimum valid value is 8; hardware prevents values less than this being written, and if attempted, results in 8 being set. For designs with APB_DATA_WIDTH = 8, the order of programming is important to ensure the correct operation of DW_apb_i2c. The lower byte must be programmed first, and then the upper byte is programmed.
        ADD_BITFIELD_RW(IC_SS_SCL_LCNT, 0, 16)
    END_TYPE()

    // Fast Mode or Fast Mode Plus I2C Clock SCL High Count Register
    // Reset value: 0x00000006
    BEGIN_TYPE(IC_FS_SCL_HCNT_t, uint32_t)
        // This register must be set before any I2C bus transaction can take place to ensure proper I/O timing. This register sets the SCL clock high-period count for fast mode or fast mode plus. It is used in high-speed mode to send the Master Code and START BYTE or General CALL. For more information, refer to 'IC_CLK Frequency Configuration'. This register goes away and becomes read-only returning 0s if IC_MAX_SPEED_MODE = standard. This register can be written only when the I2C interface is disabled, which corresponds to the IC_ENABLE[0] register being set to 0. Writes at other times have no effect. The minimum valid value is 6; hardware prevents values less than this being written, and if attempted results in 6 being set. For designs with APB_DATA_WIDTH == 8 the order of programming is important to ensure the correct operation of the DW_apb_i2c. The lower byte must be programmed first. Then the upper byte is programmed.
        ADD_BITFIELD_RW(IC_FS_SCL_HCNT, 0, 16)
    END_TYPE()

    // Fast Mode or Fast Mode Plus I2C Clock SCL Low Count Register
    // Reset value: 0x0000000d
    BEGIN_TYPE(IC_FS_SCL_LCNT_t, uint32_t)
        // This register must be set before any I2C bus transaction can take place to ensure proper I/O timing. This register sets the SCL clock low period count for fast speed. It is used in high-speed mode to send the Master Code and START BYTE or General CALL. For more information, refer to 'IC_CLK Frequency Configuration'. This register goes away and becomes read-only returning 0s if IC_MAX_SPEED_MODE = standard. This register can be written only when the I2C interface is disabled, which corresponds to the IC_ENABLE[0] register being set to 0. Writes at other times have no effect. The minimum valid value is 8; hardware prevents values less than this being written, and if attempted results in 8 being set. For designs with APB_DATA_WIDTH = 8 the order of programming is important to ensure the correct operation of the DW_apb_i2c. The lower byte must be programmed first. Then the upper byte is programmed. If the value is less than 8 then the count value gets changed to 8.
        ADD_BITFIELD_RW(IC_FS_SCL_LCNT, 0, 16)
    END_TYPE()

    // I2C Interrupt Status Register Each bit in this register has a corresponding mask bit in the IC_INTR_MASK register. These bits are cleared by reading the matching interrupt clear register. The unmasked raw versions of these bits are available in the IC_RAW_INTR_STAT register.
    // Reset value: 0x00000000
    BEGIN_TYPE(IC_INTR_STAT_t, uint32_t)
        // See IC_RAW_INTR_STAT for a detailed description of R_RESTART_DET bit. Reset value: 0x0
        ADD_BITFIELD_RO(R_RESTART_DET, 12, 1)
        // See IC_RAW_INTR_STAT for a detailed description of R_GEN_CALL bit. Reset value: 0x0
        ADD_BITFIELD_RO(R_GEN_CALL, 11, 1)
        // See IC_RAW_INTR_STAT for a detailed description of R_START_DET bit. Reset value: 0x0
        ADD_BITFIELD_RO(R_START_DET, 10, 1)
        // See IC_RAW_INTR_STAT for a detailed description of R_STOP_DET bit. Reset value: 0x0
        ADD_BITFIELD_RO(R_STOP_DET, 9, 1)
        // See IC_RAW_INTR_STAT for a detailed description of R_ACTIVITY bit. Reset value: 0x0
        ADD_BITFIELD_RO(R_ACTIVITY, 8, 1)
        // See IC_RAW_INTR_STAT for a detailed description of R_RX_DONE bit. Reset value: 0x0
        ADD_BITFIELD_RO(R_RX_DONE, 7, 1)
        // See IC_RAW_INTR_STAT for a detailed description of R_TX_ABRT bit. Reset value: 0x0
        ADD_BITFIELD_RO(R_TX_ABRT, 6, 1)
        // See IC_RAW_INTR_STAT for a detailed description of R_RD_REQ bit. Reset value: 0x0
        ADD_BITFIELD_RO(R_RD_REQ, 5, 1)
        // See IC_RAW_INTR_STAT for a detailed description of R_TX_EMPTY bit. Reset value: 0x0
        ADD_BITFIELD_RO(R_TX_EMPTY, 4, 1)
        // See IC_RAW_INTR_STAT for a detailed description of R_TX_OVER bit. Reset value: 0x0
        ADD_BITFIELD_RO(R_TX_OVER, 3, 1)
        // See IC_RAW_INTR_STAT for a detailed description of R_RX_FULL bit. Reset value: 0x0
        ADD_BITFIELD_RO(R_RX_FULL, 2, 1)
        // See IC_RAW_INTR_STAT for a detailed description of R_RX_OVER bit. Reset value: 0x0
        ADD_BITFIELD_RO(R_RX_OVER, 1, 1)
        // See IC_RAW_INTR_STAT for a detailed description of R_RX_UNDER bit. Reset value: 0x0
        ADD_BITFIELD_RO(R_RX_UNDER, 0, 1)
    END_TYPE()

    // R_RESTART_DET interrupt is inactive
    static const uint32_t IC_INTR_STAT_R_RESTART_DET__INACTIVE = 0;
    // R_RESTART_DET interrupt is active
    static const uint32_t IC_INTR_STAT_R_RESTART_DET__ACTIVE = 1;
    // R_GEN_CALL interrupt is inactive
    static const uint32_t IC_INTR_STAT_R_GEN_CALL__INACTIVE = 0;
    // R_GEN_CALL interrupt is active
    static const uint32_t IC_INTR_STAT_R_GEN_CALL__ACTIVE = 1;
    // R_START_DET interrupt is inactive
    static const uint32_t IC_INTR_STAT_R_START_DET__INACTIVE = 0;
    // R_START_DET interrupt is active
    static const uint32_t IC_INTR_STAT_R_START_DET__ACTIVE = 1;
    // R_STOP_DET interrupt is inactive
    static const uint32_t IC_INTR_STAT_R_STOP_DET__INACTIVE = 0;
    // R_STOP_DET interrupt is active
    static const uint32_t IC_INTR_STAT_R_STOP_DET__ACTIVE = 1;
    // R_ACTIVITY interrupt is inactive
    static const uint32_t IC_INTR_STAT_R_ACTIVITY__INACTIVE = 0;
    // R_ACTIVITY interrupt is active
    static const uint32_t IC_INTR_STAT_R_ACTIVITY__ACTIVE = 1;
    // R_RX_DONE interrupt is inactive
    static const uint32_t IC_INTR_STAT_R_RX_DONE__INACTIVE = 0;
    // R_RX_DONE interrupt is active
    static const uint32_t IC_INTR_STAT_R_RX_DONE__ACTIVE = 1;
    // R_TX_ABRT interrupt is inactive
    static const uint32_t IC_INTR_STAT_R_TX_ABRT__INACTIVE = 0;
    // R_TX_ABRT interrupt is active
    static const uint32_t IC_INTR_STAT_R_TX_ABRT__ACTIVE = 1;
    // R_RD_REQ interrupt is inactive
    static const uint32_t IC_INTR_STAT_R_RD_REQ__INACTIVE = 0;
    // R_RD_REQ interrupt is active
    static const uint32_t IC_INTR_STAT_R_RD_REQ__ACTIVE = 1;
    // R_TX_EMPTY interrupt is inactive
    static const uint32_t IC_INTR_STAT_R_TX_EMPTY__INACTIVE = 0;
    // R_TX_EMPTY interrupt is active
    static const uint32_t IC_INTR_STAT_R_TX_EMPTY__ACTIVE = 1;
    // R_TX_OVER interrupt is inactive
    static const uint32_t IC_INTR_STAT_R_TX_OVER__INACTIVE = 0;
    // R_TX_OVER interrupt is active
    static const uint32_t IC_INTR_STAT_R_TX_OVER__ACTIVE = 1;
    // R_RX_FULL interrupt is inactive
    static const uint32_t IC_INTR_STAT_R_RX_FULL__INACTIVE = 0;
    // R_RX_FULL interrupt is active
    static const uint32_t IC_INTR_STAT_R_RX_FULL__ACTIVE = 1;
    // R_RX_OVER interrupt is inactive
    static const uint32_t IC_INTR_STAT_R_RX_OVER__INACTIVE = 0;
    // R_RX_OVER interrupt is active
    static const uint32_t IC_INTR_STAT_R_RX_OVER__ACTIVE = 1;
    // RX_UNDER interrupt is inactive
    static const uint32_t IC_INTR_STAT_R_RX_UNDER__INACTIVE = 0;
    // RX_UNDER interrupt is active
    static const uint32_t IC_INTR_STAT_R_RX_UNDER__ACTIVE = 1;

    // I2C Interrupt Mask Register. These bits mask their corresponding interrupt status bits. This register is active low; a value of 0 masks the interrupt, whereas a value of 1 unmasks the interrupt.
    // Reset value: 0x000008ff
    BEGIN_TYPE(IC_INTR_MASK_t, uint32_t)
        // This bit masks the R_RESTART_DET interrupt in IC_INTR_STAT register. Reset value: 0x0
        ADD_BITFIELD_RW(M_RESTART_DET, 12, 1)
        // This bit masks the R_GEN_CALL interrupt in IC_INTR_STAT register. Reset value: 0x1
        ADD_BITFIELD_RW(M_GEN_CALL, 11, 1)
        // This bit masks the R_START_DET interrupt in IC_INTR_STAT register. Reset value: 0x0
        ADD_BITFIELD_RW(M_START_DET, 10, 1)
        // This bit masks the R_STOP_DET interrupt in IC_INTR_STAT register. Reset value: 0x0
        ADD_BITFIELD_RW(M_STOP_DET, 9, 1)
        // This bit masks the R_ACTIVITY interrupt in IC_INTR_STAT register. Reset value: 0x0
        ADD_BITFIELD_RW(M_ACTIVITY, 8, 1)
        // This bit masks the R_RX_DONE interrupt in IC_INTR_STAT register. Reset value: 0x1
        ADD_BITFIELD_RW(M_RX_DONE, 7, 1)
        // This bit masks the R_TX_ABRT interrupt in IC_INTR_STAT register. Reset value: 0x1
        ADD_BITFIELD_RW(M_TX_ABRT, 6, 1)
        // This bit masks the R_RD_REQ interrupt in IC_INTR_STAT register. Reset value: 0x1
        ADD_BITFIELD_RW(M_RD_REQ, 5, 1)
        // This bit masks the R_TX_EMPTY interrupt in IC_INTR_STAT register. Reset value: 0x1
        ADD_BITFIELD_RW(M_TX_EMPTY, 4, 1)
        // This bit masks the R_TX_OVER interrupt in IC_INTR_STAT register. Reset value: 0x1
        ADD_BITFIELD_RW(M_TX_OVER, 3, 1)
        // This bit masks the R_RX_FULL interrupt in IC_INTR_STAT register. Reset value: 0x1
        ADD_BITFIELD_RW(M_RX_FULL, 2, 1)
        // This bit masks the R_RX_OVER interrupt in IC_INTR_STAT register. Reset value: 0x1
        ADD_BITFIELD_RW(M_RX_OVER, 1, 1)
        // This bit masks the R_RX_UNDER interrupt in IC_INTR_STAT register. Reset value: 0x1
        ADD_BITFIELD_RW(M_RX_UNDER, 0, 1)
    END_TYPE()

    // RESTART_DET interrupt is masked
    static const uint32_t IC_INTR_MASK_M_RESTART_DET__ENABLED = 0;
    // RESTART_DET interrupt is unmasked
    static const uint32_t IC_INTR_MASK_M_RESTART_DET__DISABLED = 1;
    // GEN_CALL interrupt is masked
    static const uint32_t IC_INTR_MASK_M_GEN_CALL__ENABLED = 0;
    // GEN_CALL interrupt is unmasked
    static const uint32_t IC_INTR_MASK_M_GEN_CALL__DISABLED = 1;
    // START_DET interrupt is masked
    static const uint32_t IC_INTR_MASK_M_START_DET__ENABLED = 0;
    // START_DET interrupt is unmasked
    static const uint32_t IC_INTR_MASK_M_START_DET__DISABLED = 1;
    // STOP_DET interrupt is masked
    static const uint32_t IC_INTR_MASK_M_STOP_DET__ENABLED = 0;
    // STOP_DET interrupt is unmasked
    static const uint32_t IC_INTR_MASK_M_STOP_DET__DISABLED = 1;
    // ACTIVITY interrupt is masked
    static const uint32_t IC_INTR_MASK_M_ACTIVITY__ENABLED = 0;
    // ACTIVITY interrupt is unmasked
    static const uint32_t IC_INTR_MASK_M_ACTIVITY__DISABLED = 1;
    // RX_DONE interrupt is masked
    static const uint32_t IC_INTR_MASK_M_RX_DONE__ENABLED = 0;
    // RX_DONE interrupt is unmasked
    static const uint32_t IC_INTR_MASK_M_RX_DONE__DISABLED = 1;
    // TX_ABORT interrupt is masked
    static const uint32_t IC_INTR_MASK_M_TX_ABRT__ENABLED = 0;
    // TX_ABORT interrupt is unmasked
    static const uint32_t IC_INTR_MASK_M_TX_ABRT__DISABLED = 1;
    // RD_REQ interrupt is masked
    static const uint32_t IC_INTR_MASK_M_RD_REQ__ENABLED = 0;
    // RD_REQ interrupt is unmasked
    static const uint32_t IC_INTR_MASK_M_RD_REQ__DISABLED = 1;
    // TX_EMPTY interrupt is masked
    static const uint32_t IC_INTR_MASK_M_TX_EMPTY__ENABLED = 0;
    // TX_EMPTY interrupt is unmasked
    static const uint32_t IC_INTR_MASK_M_TX_EMPTY__DISABLED = 1;
    // TX_OVER interrupt is masked
    static const uint32_t IC_INTR_MASK_M_TX_OVER__ENABLED = 0;
    // TX_OVER interrupt is unmasked
    static const uint32_t IC_INTR_MASK_M_TX_OVER__DISABLED = 1;
    // RX_FULL interrupt is masked
    static const uint32_t IC_INTR_MASK_M_RX_FULL__ENABLED = 0;
    // RX_FULL interrupt is unmasked
    static const uint32_t IC_INTR_MASK_M_RX_FULL__DISABLED = 1;
    // RX_OVER interrupt is masked
    static const uint32_t IC_INTR_MASK_M_RX_OVER__ENABLED = 0;
    // RX_OVER interrupt is unmasked
    static const uint32_t IC_INTR_MASK_M_RX_OVER__DISABLED = 1;
    // RX_UNDER interrupt is masked
    static const uint32_t IC_INTR_MASK_M_RX_UNDER__ENABLED = 0;
    // RX_UNDER interrupt is unmasked
    static const uint32_t IC_INTR_MASK_M_RX_UNDER__DISABLED = 1;

    // I2C Raw Interrupt Status Register Unlike the IC_INTR_STAT register, these bits are not masked so they always show the true status of the DW_apb_i2c.
    // Reset value: 0x00000000
    BEGIN_TYPE(IC_RAW_INTR_STAT_t, uint32_t)
        // Indicates whether a RESTART condition has occurred on the I2C interface when DW_apb_i2c is operating in Slave mode and the slave is being addressed. Enabled only when IC_SLV_RESTART_DET_EN=1. Note: However, in high-speed mode or during a START BYTE transfer, the RESTART comes before the address field as per the I2C protocol. In this case, the slave is not the addressed slave when the RESTART is issued, therefore DW_apb_i2c does not generate the RESTART_DET interrupt. Reset value: 0x0
        ADD_BITFIELD_RO(RESTART_DET, 12, 1)
        // Set only when a General Call address is received and it is acknowledged. It stays set until it is cleared either by disabling DW_apb_i2c or when the CPU reads bit 0 of the IC_CLR_GEN_CALL register. DW_apb_i2c stores the received data in the Rx buffer. Reset value: 0x0
        ADD_BITFIELD_RO(GEN_CALL, 11, 1)
        // Indicates whether a START or RESTART condition has occurred on the I2C interface regardless of whether DW_apb_i2c is operating in slave or master mode. Reset value: 0x0
        ADD_BITFIELD_RO(START_DET, 10, 1)
        // Indicates whether a STOP condition has occurred on the I2C interface regardless of whether DW_apb_i2c is operating in slave or master mode. In Slave Mode: - If IC_CON[7]=1'b1  (STOP_DET_IFADDRESSED), the STOP_DET interrupt will be issued only if slave is addressed. Note: During a general call address, this slave does not issue a STOP_DET interrupt if STOP_DET_IF_ADDRESSED=1'b1, even if the slave responds to the general call address by generating ACK. The STOP_DET interrupt is generated only when the transmitted address matches the slave address (SAR). - If IC_CON[7]=1'b0 (STOP_DET_IFADDRESSED), the STOP_DET interrupt is issued irrespective of whether it is being addressed. In Master Mode: - If IC_CON[10]=1'b1  (STOP_DET_IF_MASTER_ACTIVE),the STOP_DET interrupt will be issued only if Master is active. - If IC_CON[10]=1'b0  (STOP_DET_IFADDRESSED),the STOP_DET interrupt will be issued irrespective of whether master is active or not. Reset value: 0x0
        ADD_BITFIELD_RO(STOP_DET, 9, 1)
        // This bit captures DW_apb_i2c activity and stays set until it is cleared. There are four ways to clear it: - Disabling the DW_apb_i2c - Reading the IC_CLR_ACTIVITY register - Reading the IC_CLR_INTR register - System reset Once this bit is set, it stays set unless one of the four methods is used to clear it. Even if the DW_apb_i2c module is idle, this bit remains set until cleared, indicating that there was activity on the bus. Reset value: 0x0
        ADD_BITFIELD_RO(ACTIVITY, 8, 1)
        // When the DW_apb_i2c is acting as a slave-transmitter, this bit is set to 1 if the master does not acknowledge a transmitted byte. This occurs on the last byte of the transmission, indicating that the transmission is done. Reset value: 0x0
        ADD_BITFIELD_RO(RX_DONE, 7, 1)
        // This bit indicates if DW_apb_i2c, as an I2C transmitter, is unable to complete the intended actions on the contents of the transmit FIFO. This situation can occur both as an I2C master or an I2C slave, and is referred to as a 'transmit abort'. When this bit is set to 1, the IC_TX_ABRT_SOURCE register indicates the reason why the transmit abort takes places. Note:  The DW_apb_i2c flushes/resets/empties the TX_FIFO and RX_FIFO whenever there is a transmit abort caused by any of the events tracked by the IC_TX_ABRT_SOURCE register. The FIFOs remains in this flushed state until the register IC_CLR_TX_ABRT is read. Once this read is performed, the Tx FIFO is then ready to accept more data bytes from the APB interface. Reset value: 0x0
        ADD_BITFIELD_RO(TX_ABRT, 6, 1)
        // This bit is set to 1 when DW_apb_i2c is acting as a slave and another I2C master is attempting to read data from DW_apb_i2c. The DW_apb_i2c holds the I2C bus in a wait state (SCL=0) until this interrupt is serviced, which means that the slave has been addressed by a remote master that is asking for data to be transferred. The processor must respond to this interrupt and then write the requested data to the IC_DATA_CMD register. This bit is set to 0 just after the processor reads the IC_CLR_RD_REQ register. Reset value: 0x0
        ADD_BITFIELD_RO(RD_REQ, 5, 1)
        // The behavior of the TX_EMPTY interrupt status differs based on the TX_EMPTY_CTRL selection in the IC_CON register. - When TX_EMPTY_CTRL = 0: This bit is set to 1 when the transmit buffer is at or below the threshold value set in the IC_TX_TL register. - When TX_EMPTY_CTRL = 1: This bit is set to 1 when the transmit buffer is at or below the threshold value set in the IC_TX_TL register and the transmission of the address/data from the internal shift register for the most recently popped command is completed. It is automatically cleared by hardware when the buffer level goes above the threshold. When IC_ENABLE[0] is set to 0, the TX FIFO is flushed and held in reset. There the TX FIFO looks like it has no data within it, so this bit is set to 1, provided there is activity in the master or slave state machines. When there is no longer any activity, then with ic_en=0, this bit is set to 0. Reset value: 0x0.
        ADD_BITFIELD_RO(TX_EMPTY, 4, 1)
        // Set during transmit if the transmit buffer is filled to IC_TX_BUFFER_DEPTH and the processor attempts to issue another I2C command by writing to the IC_DATA_CMD register. When the module is disabled, this bit keeps its level until the master or slave state machines go into idle, and when ic_en goes to 0, this interrupt is cleared. Reset value: 0x0
        ADD_BITFIELD_RO(TX_OVER, 3, 1)
        // Set when the receive buffer reaches or goes above the RX_TL threshold in the IC_RX_TL register. It is automatically cleared by hardware when buffer level goes below the threshold. If the module is disabled (IC_ENABLE[0]=0), the RX FIFO is flushed and held in reset; therefore the RX FIFO is not full. So this bit is cleared once the IC_ENABLE bit 0 is programmed with a 0, regardless of the activity that continues. Reset value: 0x0
        ADD_BITFIELD_RO(RX_FULL, 2, 1)
        // Set if the receive buffer is completely filled to IC_RX_BUFFER_DEPTH and an additional byte is received from an external I2C device. The DW_apb_i2c acknowledges this, but any data bytes received after the FIFO is full are lost. If the module is disabled (IC_ENABLE[0]=0), this bit keeps its level until the master or slave state machines go into idle, and when ic_en goes to 0, this interrupt is cleared. Note:  If bit 9 of the IC_CON register (RX_FIFO_FULL_HLD_CTRL) is programmed to HIGH, then the RX_OVER interrupt never occurs, because the Rx FIFO never overflows. Reset value: 0x0
        ADD_BITFIELD_RO(RX_OVER, 1, 1)
        // Set if the processor attempts to read the receive buffer when it is empty by reading from the IC_DATA_CMD register. If the module is disabled (IC_ENABLE[0]=0), this bit keeps its level until the master or slave state machines go into idle, and when ic_en goes to 0, this interrupt is cleared. Reset value: 0x0
        ADD_BITFIELD_RO(RX_UNDER, 0, 1)
    END_TYPE()

    // RESTART_DET interrupt is inactive
    static const uint32_t IC_RAW_INTR_STAT_RESTART_DET__INACTIVE = 0;
    // RESTART_DET interrupt is active
    static const uint32_t IC_RAW_INTR_STAT_RESTART_DET__ACTIVE = 1;
    // GEN_CALL interrupt is inactive
    static const uint32_t IC_RAW_INTR_STAT_GEN_CALL__INACTIVE = 0;
    // GEN_CALL interrupt is active
    static const uint32_t IC_RAW_INTR_STAT_GEN_CALL__ACTIVE = 1;
    // START_DET interrupt is inactive
    static const uint32_t IC_RAW_INTR_STAT_START_DET__INACTIVE = 0;
    // START_DET interrupt is active
    static const uint32_t IC_RAW_INTR_STAT_START_DET__ACTIVE = 1;
    // STOP_DET interrupt is inactive
    static const uint32_t IC_RAW_INTR_STAT_STOP_DET__INACTIVE = 0;
    // STOP_DET interrupt is active
    static const uint32_t IC_RAW_INTR_STAT_STOP_DET__ACTIVE = 1;
    // RAW_INTR_ACTIVITY interrupt is inactive
    static const uint32_t IC_RAW_INTR_STAT_ACTIVITY__INACTIVE = 0;
    // RAW_INTR_ACTIVITY interrupt is active
    static const uint32_t IC_RAW_INTR_STAT_ACTIVITY__ACTIVE = 1;
    // RX_DONE interrupt is inactive
    static const uint32_t IC_RAW_INTR_STAT_RX_DONE__INACTIVE = 0;
    // RX_DONE interrupt is active
    static const uint32_t IC_RAW_INTR_STAT_RX_DONE__ACTIVE = 1;
    // TX_ABRT interrupt is inactive
    static const uint32_t IC_RAW_INTR_STAT_TX_ABRT__INACTIVE = 0;
    // TX_ABRT interrupt is active
    static const uint32_t IC_RAW_INTR_STAT_TX_ABRT__ACTIVE = 1;
    // RD_REQ interrupt is inactive
    static const uint32_t IC_RAW_INTR_STAT_RD_REQ__INACTIVE = 0;
    // RD_REQ interrupt is active
    static const uint32_t IC_RAW_INTR_STAT_RD_REQ__ACTIVE = 1;
    // TX_EMPTY interrupt is inactive
    static const uint32_t IC_RAW_INTR_STAT_TX_EMPTY__INACTIVE = 0;
    // TX_EMPTY interrupt is active
    static const uint32_t IC_RAW_INTR_STAT_TX_EMPTY__ACTIVE = 1;
    // TX_OVER interrupt is inactive
    static const uint32_t IC_RAW_INTR_STAT_TX_OVER__INACTIVE = 0;
    // TX_OVER interrupt is active
    static const uint32_t IC_RAW_INTR_STAT_TX_OVER__ACTIVE = 1;
    // RX_FULL interrupt is inactive
    static const uint32_t IC_RAW_INTR_STAT_RX_FULL__INACTIVE = 0;
    // RX_FULL interrupt is active
    static const uint32_t IC_RAW_INTR_STAT_RX_FULL__ACTIVE = 1;
    // RX_OVER interrupt is inactive
    static const uint32_t IC_RAW_INTR_STAT_RX_OVER__INACTIVE = 0;
    // RX_OVER interrupt is active
    static const uint32_t IC_RAW_INTR_STAT_RX_OVER__ACTIVE = 1;
    // RX_UNDER interrupt is inactive
    static const uint32_t IC_RAW_INTR_STAT_RX_UNDER__INACTIVE = 0;
    // RX_UNDER interrupt is active
    static const uint32_t IC_RAW_INTR_STAT_RX_UNDER__ACTIVE = 1;

    // I2C Receive FIFO Threshold Register
    // Reset value: 0x00000000
    BEGIN_TYPE(IC_RX_TL_t, uint32_t)
        // Receive FIFO Threshold Level. Controls the level of entries (or above) that triggers the RX_FULL interrupt (bit 2 in IC_RAW_INTR_STAT register). The valid range is 0-255, with the additional restriction that hardware does not allow this value to be set to a value larger than the depth of the buffer. If an attempt is made to do that, the actual value set will be the maximum depth of the buffer. A value of 0 sets the threshold for 1 entry, and a value of 255 sets the threshold for 256 entries.
        ADD_BITFIELD_RW(RX_TL, 0, 8)
    END_TYPE()

    // I2C Transmit FIFO Threshold Register
    // Reset value: 0x00000000
    BEGIN_TYPE(IC_TX_TL_t, uint32_t)
        // Transmit FIFO Threshold Level. Controls the level of entries (or below) that trigger the TX_EMPTY interrupt (bit 4 in IC_RAW_INTR_STAT register). The valid range is 0-255, with the additional restriction that it may not be set to value larger than the depth of the buffer. If an attempt is made to do that, the actual value set will be the maximum depth of the buffer. A value of 0 sets the threshold for 0 entries, and a value of 255 sets the threshold for 255 entries.
        ADD_BITFIELD_RW(TX_TL, 0, 8)
    END_TYPE()

    // Clear Combined and Individual Interrupt Register
    // Reset value: 0x00000000
    BEGIN_TYPE(IC_CLR_INTR_t, uint32_t)
        // Read this register to clear the combined interrupt, all individual interrupts, and the IC_TX_ABRT_SOURCE register. This bit does not clear hardware clearable interrupts but software clearable interrupts. Refer to Bit 9 of the IC_TX_ABRT_SOURCE register for an exception to clearing IC_TX_ABRT_SOURCE. Reset value: 0x0
        ADD_BITFIELD_RO(CLR_INTR, 0, 1)
    END_TYPE()

    // Clear RX_UNDER Interrupt Register
    // Reset value: 0x00000000
    BEGIN_TYPE(IC_CLR_RX_UNDER_t, uint32_t)
        // Read this register to clear the RX_UNDER interrupt (bit 0) of the IC_RAW_INTR_STAT register. Reset value: 0x0
        ADD_BITFIELD_RO(CLR_RX_UNDER, 0, 1)
    END_TYPE()

    // Clear RX_OVER Interrupt Register
    // Reset value: 0x00000000
    BEGIN_TYPE(IC_CLR_RX_OVER_t, uint32_t)
        // Read this register to clear the RX_OVER interrupt (bit 1) of the IC_RAW_INTR_STAT register. Reset value: 0x0
        ADD_BITFIELD_RO(CLR_RX_OVER, 0, 1)
    END_TYPE()

    // Clear TX_OVER Interrupt Register
    // Reset value: 0x00000000
    BEGIN_TYPE(IC_CLR_TX_OVER_t, uint32_t)
        // Read this register to clear the TX_OVER interrupt (bit 3) of the IC_RAW_INTR_STAT register. Reset value: 0x0
        ADD_BITFIELD_RO(CLR_TX_OVER, 0, 1)
    END_TYPE()

    // Clear RD_REQ Interrupt Register
    // Reset value: 0x00000000
    BEGIN_TYPE(IC_CLR_RD_REQ_t, uint32_t)
        // Read this register to clear the RD_REQ interrupt (bit 5) of the IC_RAW_INTR_STAT register. Reset value: 0x0
        ADD_BITFIELD_RO(CLR_RD_REQ, 0, 1)
    END_TYPE()

    // Clear TX_ABRT Interrupt Register
    // Reset value: 0x00000000
    BEGIN_TYPE(IC_CLR_TX_ABRT_t, uint32_t)
        // Read this register to clear the TX_ABRT interrupt (bit 6) of the IC_RAW_INTR_STAT register, and the IC_TX_ABRT_SOURCE register. This also releases the TX FIFO from the flushed/reset state, allowing more writes to the TX FIFO. Refer to Bit 9 of the IC_TX_ABRT_SOURCE register for an exception to clearing IC_TX_ABRT_SOURCE. Reset value: 0x0
        ADD_BITFIELD_RO(CLR_TX_ABRT, 0, 1)
    END_TYPE()

    // Clear RX_DONE Interrupt Register
    // Reset value: 0x00000000
    BEGIN_TYPE(IC_CLR_RX_DONE_t, uint32_t)
        // Read this register to clear the RX_DONE interrupt (bit 7) of the IC_RAW_INTR_STAT register. Reset value: 0x0
        ADD_BITFIELD_RO(CLR_RX_DONE, 0, 1)
    END_TYPE()

    // Clear ACTIVITY Interrupt Register
    // Reset value: 0x00000000
    BEGIN_TYPE(IC_CLR_ACTIVITY_t, uint32_t)
        // Reading this register clears the ACTIVITY interrupt if the I2C is not active anymore. If the I2C module is still active on the bus, the ACTIVITY interrupt bit continues to be set. It is automatically cleared by hardware if the module is disabled and if there is no further activity on the bus. The value read from this register to get status of the ACTIVITY interrupt (bit 8) of the IC_RAW_INTR_STAT register. Reset value: 0x0
        ADD_BITFIELD_RO(CLR_ACTIVITY, 0, 1)
    END_TYPE()

    // Clear STOP_DET Interrupt Register
    // Reset value: 0x00000000
    BEGIN_TYPE(IC_CLR_STOP_DET_t, uint32_t)
        // Read this register to clear the STOP_DET interrupt (bit 9) of the IC_RAW_INTR_STAT register. Reset value: 0x0
        ADD_BITFIELD_RO(CLR_STOP_DET, 0, 1)
    END_TYPE()

    // Clear START_DET Interrupt Register
    // Reset value: 0x00000000
    BEGIN_TYPE(IC_CLR_START_DET_t, uint32_t)
        // Read this register to clear the START_DET interrupt (bit 10) of the IC_RAW_INTR_STAT register. Reset value: 0x0
        ADD_BITFIELD_RO(CLR_START_DET, 0, 1)
    END_TYPE()

    // Clear GEN_CALL Interrupt Register
    // Reset value: 0x00000000
    BEGIN_TYPE(IC_CLR_GEN_CALL_t, uint32_t)
        // Read this register to clear the GEN_CALL interrupt (bit 11) of IC_RAW_INTR_STAT register. Reset value: 0x0
        ADD_BITFIELD_RO(CLR_GEN_CALL, 0, 1)
    END_TYPE()

    // I2C Enable Register
    // Reset value: 0x00000000
    BEGIN_TYPE(IC_ENABLE_t, uint32_t)
        // In Master mode: - 1'b1: Blocks the transmission of data on I2C bus even if Tx FIFO has data to transmit. - 1'b0: The transmission of data starts on I2C bus automatically, as soon as the first data is available in the Tx FIFO. Note: To block the execution of Master commands, set the TX_CMD_BLOCK bit only when Tx FIFO is empty (IC_STATUS[2]==1) and Master is in Idle state (IC_STATUS[5] == 0). Any further commands put in the Tx FIFO are not executed until TX_CMD_BLOCK bit is unset. Reset value:  IC_TX_CMD_BLOCK_DEFAULT
        ADD_BITFIELD_RW(TX_CMD_BLOCK, 2, 1)
        // When set, the controller initiates the transfer abort. - 0: ABORT not initiated or ABORT done - 1: ABORT operation in progress The software can abort the I2C transfer in master mode by setting this bit. The software can set this bit only when ENABLE is already set; otherwise, the controller ignores any write to ABORT bit. The software cannot clear the ABORT bit once set. In response to an ABORT, the controller issues a STOP and flushes the Tx FIFO after completing the current transfer, then sets the TX_ABORT interrupt after the abort operation. The ABORT bit is cleared automatically after the abort operation. For a detailed description on how to abort I2C transfers, refer to 'Aborting I2C Transfers'. Reset value: 0x0
        ADD_BITFIELD_RW(ABORT, 1, 1)
        // Controls whether the DW_apb_i2c is enabled. - 0: Disables DW_apb_i2c (TX and RX FIFOs are held in an erased state) - 1: Enables DW_apb_i2c Software can disable DW_apb_i2c while it is active. However, it is important that care be taken to ensure that DW_apb_i2c is disabled properly. A recommended procedure is described in 'Disabling DW_apb_i2c'. When DW_apb_i2c is disabled, the following occurs: - The TX FIFO and RX FIFO get flushed. - Status bits in the IC_INTR_STAT register are still active until DW_apb_i2c goes into IDLE state. If the module is transmitting, it stops as well as deletes the contents of the transmit buffer after the current transfer is complete. If the module is receiving, the DW_apb_i2c stops the current transfer at the end of the current byte and does not acknowledge the transfer. In systems with asynchronous pclk and ic_clk when IC_CLK_TYPE parameter set to asynchronous (1), there is a two ic_clk delay when enabling or disabling the DW_apb_i2c. For a detailed description on how to disable DW_apb_i2c, refer to 'Disabling DW_apb_i2c' Reset value: 0x0
        ADD_BITFIELD_RW(ENABLE, 0, 1)
    END_TYPE()

    // Tx Command execution not blocked
    static const uint32_t IC_ENABLE_TX_CMD_BLOCK__NOT_BLOCKED = 0;
    // Tx Command execution blocked
    static const uint32_t IC_ENABLE_TX_CMD_BLOCK__BLOCKED = 1;
    // ABORT operation not in progress
    static const uint32_t IC_ENABLE_ABORT__DISABLE = 0;
    // ABORT operation in progress
    static const uint32_t IC_ENABLE_ABORT__ENABLED = 1;
    // I2C is disabled
    static const uint32_t IC_ENABLE_ENABLE__DISABLED = 0;
    // I2C is enabled
    static const uint32_t IC_ENABLE_ENABLE__ENABLED = 1;

    // I2C Status Register This is a read-only register used to indicate the current transfer status and FIFO status. The status register may be read at any time. None of the bits in this register request an interrupt. When the I2C is disabled by writing 0 in bit 0 of the IC_ENABLE register: - Bits 1 and 2 are set to 1 - Bits 3 and 10 are set to 0 When the master or slave state machines goes to idle and ic_en=0: - Bits 5 and 6 are set to 0
    // Reset value: 0x00000006
    BEGIN_TYPE(IC_STATUS_t, uint32_t)
        // Slave FSM Activity Status. When the Slave Finite State Machine (FSM) is not in the IDLE state, this bit is set. - 0: Slave FSM is in IDLE state so the Slave part of DW_apb_i2c is not Active - 1: Slave FSM is not in IDLE state so the Slave part of DW_apb_i2c is Active Reset value: 0x0
        ADD_BITFIELD_RO(SLV_ACTIVITY, 6, 1)
        // Master FSM Activity Status. When the Master Finite State Machine (FSM) is not in the IDLE state, this bit is set. - 0: Master FSM is in IDLE state so the Master part of DW_apb_i2c is not Active - 1: Master FSM is not in IDLE state so the Master part of DW_apb_i2c is Active Note: IC_STATUS[0]-that is, ACTIVITY bit-is the OR of SLV_ACTIVITY and MST_ACTIVITY bits. Reset value: 0x0
        ADD_BITFIELD_RO(MST_ACTIVITY, 5, 1)
        // Receive FIFO Completely Full. When the receive FIFO is completely full, this bit is set. When the receive FIFO contains one or more empty location, this bit is cleared. - 0: Receive FIFO is not full - 1: Receive FIFO is full Reset value: 0x0
        ADD_BITFIELD_RO(RFF, 4, 1)
        // Receive FIFO Not Empty. This bit is set when the receive FIFO contains one or more entries; it is cleared when the receive FIFO is empty. - 0: Receive FIFO is empty - 1: Receive FIFO is not empty Reset value: 0x0
        ADD_BITFIELD_RO(RFNE, 3, 1)
        // Transmit FIFO Completely Empty. When the transmit FIFO is completely empty, this bit is set. When it contains one or more valid entries, this bit is cleared. This bit field does not request an interrupt. - 0: Transmit FIFO is not empty - 1: Transmit FIFO is empty Reset value: 0x1
        ADD_BITFIELD_RO(TFE, 2, 1)
        // Transmit FIFO Not Full. Set when the transmit FIFO contains one or more empty locations, and is cleared when the FIFO is full. - 0: Transmit FIFO is full - 1: Transmit FIFO is not full Reset value: 0x1
        ADD_BITFIELD_RO(TFNF, 1, 1)
        // I2C Activity Status. Reset value: 0x0
        ADD_BITFIELD_RO(ACTIVITY, 0, 1)
    END_TYPE()

    // Slave is idle
    static const uint32_t IC_STATUS_SLV_ACTIVITY__IDLE = 0;
    // Slave not idle
    static const uint32_t IC_STATUS_SLV_ACTIVITY__ACTIVE = 1;
    // Master is idle
    static const uint32_t IC_STATUS_MST_ACTIVITY__IDLE = 0;
    // Master not idle
    static const uint32_t IC_STATUS_MST_ACTIVITY__ACTIVE = 1;
    // Rx FIFO not full
    static const uint32_t IC_STATUS_RFF__NOT_FULL = 0;
    // Rx FIFO is full
    static const uint32_t IC_STATUS_RFF__FULL = 1;
    // Rx FIFO is empty
    static const uint32_t IC_STATUS_RFNE__EMPTY = 0;
    // Rx FIFO not empty
    static const uint32_t IC_STATUS_RFNE__NOT_EMPTY = 1;
    // Tx FIFO not empty
    static const uint32_t IC_STATUS_TFE__NON_EMPTY = 0;
    // Tx FIFO is empty
    static const uint32_t IC_STATUS_TFE__EMPTY = 1;
    // Tx FIFO is full
    static const uint32_t IC_STATUS_TFNF__FULL = 0;
    // Tx FIFO not full
    static const uint32_t IC_STATUS_TFNF__NOT_FULL = 1;
    // I2C is idle
    static const uint32_t IC_STATUS_ACTIVITY__INACTIVE = 0;
    // I2C is active
    static const uint32_t IC_STATUS_ACTIVITY__ACTIVE = 1;

    // I2C Transmit FIFO Level Register This register contains the number of valid data entries in the transmit FIFO buffer. It is cleared whenever: - The I2C is disabled - There is a transmit abort - that is, TX_ABRT bit is set in the IC_RAW_INTR_STAT register - The slave bulk transmit mode is aborted The register increments whenever data is placed into the transmit FIFO and decrements when data is taken from the transmit FIFO.
    // Reset value: 0x00000000
    BEGIN_TYPE(IC_TXFLR_t, uint32_t)
        // Transmit FIFO Level. Contains the number of valid data entries in the transmit FIFO. Reset value: 0x0
        ADD_BITFIELD_RO(TXFLR, 0, 5)
    END_TYPE()

    // I2C Receive FIFO Level Register This register contains the number of valid data entries in the receive FIFO buffer. It is cleared whenever: - The I2C is disabled - Whenever there is a transmit abort caused by any of the events tracked in IC_TX_ABRT_SOURCE The register increments whenever data is placed into the receive FIFO and decrements when data is taken from the receive FIFO.
    // Reset value: 0x00000000
    BEGIN_TYPE(IC_RXFLR_t, uint32_t)
        // Receive FIFO Level. Contains the number of valid data entries in the receive FIFO. Reset value: 0x0
        ADD_BITFIELD_RO(RXFLR, 0, 5)
    END_TYPE()

    // I2C SDA Hold Time Length Register The bits [15:0] of this register are used to control the hold time of SDA during transmit in both slave and master mode (after SCL goes from HIGH to LOW). The bits [23:16] of this register are used to extend the SDA transition (if any) whenever SCL is HIGH in the receiver in either master or slave mode. Writes to this register succeed only when IC_ENABLE[0]=0. The values in this register are in units of ic_clk period. The value programmed in IC_SDA_TX_HOLD must be greater than the minimum hold time in each mode (one cycle in master mode, seven cycles in slave mode) for the value to be implemented. The programmed SDA hold time during transmit (IC_SDA_TX_HOLD) cannot exceed at any time the duration of the low part of scl. Therefore the programmed value cannot be larger than N_SCL_LOW-2, where N_SCL_LOW is the duration of the low part of the scl period measured in ic_clk cycles.
    // Reset value: 0x00000001
    BEGIN_TYPE(IC_SDA_HOLD_t, uint32_t)
        // Sets the required SDA hold time in units of ic_clk period, when DW_apb_i2c acts as a receiver. Reset value: IC_DEFAULT_SDA_HOLD[23:16].
        ADD_BITFIELD_RW(IC_SDA_RX_HOLD, 16, 8)
        // Sets the required SDA hold time in units of ic_clk period, when DW_apb_i2c acts as a transmitter. Reset value: IC_DEFAULT_SDA_HOLD[15:0].
        ADD_BITFIELD_RW(IC_SDA_TX_HOLD, 0, 16)
    END_TYPE()

    // I2C Transmit Abort Source Register This register has 32 bits that indicate the source of the TX_ABRT bit. Except for Bit 9, this register is cleared whenever the IC_CLR_TX_ABRT register or the IC_CLR_INTR register is read. To clear Bit 9, the source of the ABRT_SBYTE_NORSTRT must be fixed first; RESTART must be enabled (IC_CON[5]=1), the SPECIAL bit must be cleared (IC_TAR[11]), or the GC_OR_START bit must be cleared (IC_TAR[10]). Once the source of the ABRT_SBYTE_NORSTRT is fixed, then this bit can be cleared in the same manner as other bits in this register. If the source of the ABRT_SBYTE_NORSTRT is not fixed before attempting to clear this bit, Bit 9 clears for one cycle and is then re-asserted.
    // Reset value: 0x00000000
    BEGIN_TYPE(IC_TX_ABRT_SOURCE_t, uint32_t)
        // This field indicates the number of Tx FIFO Data Commands which are flushed due to TX_ABRT interrupt. It is cleared whenever I2C is disabled. Reset value: 0x0 Role of DW_apb_i2c:  Master-Transmitter or Slave-Transmitter
        ADD_BITFIELD_RO(TX_FLUSH_CNT, 23, 9)
        // This is a master-mode-only bit. Master has detected the transfer abort (IC_ENABLE[1]) Reset value: 0x0 Role of DW_apb_i2c:  Master-Transmitter
        ADD_BITFIELD_RO(ABRT_USER_ABRT, 16, 1)
        // 1: When the processor side responds to a slave mode request for data to be transmitted to a remote master and user writes a 1 in CMD (bit 8) of IC_DATA_CMD register. Reset value: 0x0 Role of DW_apb_i2c:  Slave-Transmitter
        ADD_BITFIELD_RO(ABRT_SLVRD_INTX, 15, 1)
        // This field indicates that a Slave has lost the bus while transmitting data to a remote master. IC_TX_ABRT_SOURCE[12] is set at the same time. Note:  Even though the slave never 'owns' the bus, something could go wrong on the bus. This is a fail safe check. For instance, during a data transmission at the low-to-high transition of SCL, if what is on the data bus is not what is supposed to be transmitted, then DW_apb_i2c no longer own the bus. Reset value: 0x0 Role of DW_apb_i2c:  Slave-Transmitter
        ADD_BITFIELD_RO(ABRT_SLV_ARBLOST, 14, 1)
        // This field specifies that the Slave has received a read command and some data exists in the TX FIFO, so the slave issues a TX_ABRT interrupt to flush old data in TX FIFO. Reset value: 0x0 Role of DW_apb_i2c:  Slave-Transmitter
        ADD_BITFIELD_RO(ABRT_SLVFLUSH_TXFIFO, 13, 1)
        // This field specifies that the Master has lost arbitration, or if IC_TX_ABRT_SOURCE[14] is also set, then the slave transmitter has lost arbitration. Reset value: 0x0 Role of DW_apb_i2c:  Master-Transmitter or Slave-Transmitter
        ADD_BITFIELD_RO(ARB_LOST, 12, 1)
        // This field indicates that the User tries to initiate a Master operation with the Master mode disabled. Reset value: 0x0 Role of DW_apb_i2c:  Master-Transmitter or Master-Receiver
        ADD_BITFIELD_RO(ABRT_MASTER_DIS, 11, 1)
        // This field indicates that the restart is disabled (IC_RESTART_EN bit (IC_CON[5]) =0) and the master sends a read command in 10-bit addressing mode. Reset value: 0x0 Role of DW_apb_i2c:  Master-Receiver
        ADD_BITFIELD_RO(ABRT_10B_RD_NORSTRT, 10, 1)
        // To clear Bit 9, the source of the ABRT_SBYTE_NORSTRT must be fixed first; restart must be enabled (IC_CON[5]=1), the SPECIAL bit must be cleared (IC_TAR[11]), or the GC_OR_START bit must be cleared (IC_TAR[10]). Once the source of the ABRT_SBYTE_NORSTRT is fixed, then this bit can be cleared in the same manner as other bits in this register. If the source of the ABRT_SBYTE_NORSTRT is not fixed before attempting to clear this bit, bit 9 clears for one cycle and then gets reasserted. When this field is set to 1, the restart is disabled (IC_RESTART_EN bit (IC_CON[5]) =0) and the user is trying to send a START Byte. Reset value: 0x0 Role of DW_apb_i2c:  Master
        ADD_BITFIELD_RO(ABRT_SBYTE_NORSTRT, 9, 1)
        // This field indicates that the restart is disabled (IC_RESTART_EN bit (IC_CON[5]) =0) and the user is trying to use the master to transfer data in High Speed mode. Reset value: 0x0 Role of DW_apb_i2c:  Master-Transmitter or Master-Receiver
        ADD_BITFIELD_RO(ABRT_HS_NORSTRT, 8, 1)
        // This field indicates that the Master has sent a START Byte and the START Byte was acknowledged (wrong behavior). Reset value: 0x0 Role of DW_apb_i2c:  Master
        ADD_BITFIELD_RO(ABRT_SBYTE_ACKDET, 7, 1)
        // This field indicates that the Master is in High Speed mode and the High Speed Master code was acknowledged (wrong behavior). Reset value: 0x0 Role of DW_apb_i2c:  Master
        ADD_BITFIELD_RO(ABRT_HS_ACKDET, 6, 1)
        // This field indicates that DW_apb_i2c in the master mode has sent a General Call but the user programmed the byte following the General Call to be a read from the bus (IC_DATA_CMD[9] is set to 1). Reset value: 0x0 Role of DW_apb_i2c:  Master-Transmitter
        ADD_BITFIELD_RO(ABRT_GCALL_READ, 5, 1)
        // This field indicates that DW_apb_i2c in master mode has sent a General Call and no slave on the bus acknowledged the General Call. Reset value: 0x0 Role of DW_apb_i2c:  Master-Transmitter
        ADD_BITFIELD_RO(ABRT_GCALL_NOACK, 4, 1)
        // This field indicates the master-mode only bit. When the master receives an acknowledgement for the address, but when it sends data byte(s) following the address, it did not receive an acknowledge from the remote slave(s). Reset value: 0x0 Role of DW_apb_i2c:  Master-Transmitter
        ADD_BITFIELD_RO(ABRT_TXDATA_NOACK, 3, 1)
        // This field indicates that the Master is in 10-bit address mode and that the second address byte of the 10-bit address was not acknowledged by any slave. Reset value: 0x0 Role of DW_apb_i2c:  Master-Transmitter or Master-Receiver
        ADD_BITFIELD_RO(ABRT_10ADDR2_NOACK, 2, 1)
        // This field indicates that the Master is in 10-bit address mode and the first 10-bit address byte was not acknowledged by any slave. Reset value: 0x0 Role of DW_apb_i2c:  Master-Transmitter or Master-Receiver
        ADD_BITFIELD_RO(ABRT_10ADDR1_NOACK, 1, 1)
        // This field indicates that the Master is in 7-bit addressing mode and the address sent was not acknowledged by any slave. Reset value: 0x0 Role of DW_apb_i2c:  Master-Transmitter or Master-Receiver
        ADD_BITFIELD_RO(ABRT_7B_ADDR_NOACK, 0, 1)
    END_TYPE()

    // Transfer abort detected by master- scenario not present
    static const uint32_t IC_TX_ABRT_SOURCE_ABRT_USER_ABRT__ABRT_USER_ABRT_VOID = 0;
    // Transfer abort detected by master
    static const uint32_t IC_TX_ABRT_SOURCE_ABRT_USER_ABRT__ABRT_USER_ABRT_GENERATED = 1;
    // Slave trying to transmit to remote master in read mode- scenario not present
    static const uint32_t IC_TX_ABRT_SOURCE_ABRT_SLVRD_INTX__ABRT_SLVRD_INTX_VOID = 0;
    // Slave trying to transmit to remote master in read mode
    static const uint32_t IC_TX_ABRT_SOURCE_ABRT_SLVRD_INTX__ABRT_SLVRD_INTX_GENERATED = 1;
    // Slave lost arbitration to remote master- scenario not present
    static const uint32_t IC_TX_ABRT_SOURCE_ABRT_SLV_ARBLOST__ABRT_SLV_ARBLOST_VOID = 0;
    // Slave lost arbitration to remote master
    static const uint32_t IC_TX_ABRT_SOURCE_ABRT_SLV_ARBLOST__ABRT_SLV_ARBLOST_GENERATED = 1;
    // Slave flushes existing data in TX-FIFO upon getting read command- scenario not present
    static const uint32_t IC_TX_ABRT_SOURCE_ABRT_SLVFLUSH_TXFIFO__ABRT_SLVFLUSH_TXFIFO_VOID = 0;
    // Slave flushes existing data in TX-FIFO upon getting read command
    static const uint32_t IC_TX_ABRT_SOURCE_ABRT_SLVFLUSH_TXFIFO__ABRT_SLVFLUSH_TXFIFO_GENERATED = 1;
    // Master or Slave-Transmitter lost arbitration- scenario not present
    static const uint32_t IC_TX_ABRT_SOURCE_ARB_LOST__ABRT_LOST_VOID = 0;
    // Master or Slave-Transmitter lost arbitration
    static const uint32_t IC_TX_ABRT_SOURCE_ARB_LOST__ABRT_LOST_GENERATED = 1;
    // User initiating master operation when MASTER disabled- scenario not present
    static const uint32_t IC_TX_ABRT_SOURCE_ABRT_MASTER_DIS__ABRT_MASTER_DIS_VOID = 0;
    // User initiating master operation when MASTER disabled
    static const uint32_t IC_TX_ABRT_SOURCE_ABRT_MASTER_DIS__ABRT_MASTER_DIS_GENERATED = 1;
    // Master not trying to read in 10Bit addressing mode when RESTART disabled
    static const uint32_t IC_TX_ABRT_SOURCE_ABRT_10B_RD_NORSTRT__ABRT_10B_RD_VOID = 0;
    // Master trying to read in 10Bit addressing mode when RESTART disabled
    static const uint32_t IC_TX_ABRT_SOURCE_ABRT_10B_RD_NORSTRT__ABRT_10B_RD_GENERATED = 1;
    // User trying to send START byte when RESTART disabled- scenario not present
    static const uint32_t IC_TX_ABRT_SOURCE_ABRT_SBYTE_NORSTRT__ABRT_SBYTE_NORSTRT_VOID = 0;
    // User trying to send START byte when RESTART disabled
    static const uint32_t IC_TX_ABRT_SOURCE_ABRT_SBYTE_NORSTRT__ABRT_SBYTE_NORSTRT_GENERATED = 1;
    // User trying to switch Master to HS mode when RESTART disabled- scenario not present
    static const uint32_t IC_TX_ABRT_SOURCE_ABRT_HS_NORSTRT__ABRT_HS_NORSTRT_VOID = 0;
    // User trying to switch Master to HS mode when RESTART disabled
    static const uint32_t IC_TX_ABRT_SOURCE_ABRT_HS_NORSTRT__ABRT_HS_NORSTRT_GENERATED = 1;
    // ACK detected for START byte- scenario not present
    static const uint32_t IC_TX_ABRT_SOURCE_ABRT_SBYTE_ACKDET__ABRT_SBYTE_ACKDET_VOID = 0;
    // ACK detected for START byte
    static const uint32_t IC_TX_ABRT_SOURCE_ABRT_SBYTE_ACKDET__ABRT_SBYTE_ACKDET_GENERATED = 1;
    // HS Master code ACKed in HS Mode- scenario not present
    static const uint32_t IC_TX_ABRT_SOURCE_ABRT_HS_ACKDET__ABRT_HS_ACK_VOID = 0;
    // HS Master code ACKed in HS Mode
    static const uint32_t IC_TX_ABRT_SOURCE_ABRT_HS_ACKDET__ABRT_HS_ACK_GENERATED = 1;
    // GCALL is followed by read from bus-scenario not present
    static const uint32_t IC_TX_ABRT_SOURCE_ABRT_GCALL_READ__ABRT_GCALL_READ_VOID = 0;
    // GCALL is followed by read from bus
    static const uint32_t IC_TX_ABRT_SOURCE_ABRT_GCALL_READ__ABRT_GCALL_READ_GENERATED = 1;
    // GCALL not ACKed by any slave-scenario not present
    static const uint32_t IC_TX_ABRT_SOURCE_ABRT_GCALL_NOACK__ABRT_GCALL_NOACK_VOID = 0;
    // GCALL not ACKed by any slave
    static const uint32_t IC_TX_ABRT_SOURCE_ABRT_GCALL_NOACK__ABRT_GCALL_NOACK_GENERATED = 1;
    // Transmitted data non-ACKed by addressed slave-scenario not present
    static const uint32_t IC_TX_ABRT_SOURCE_ABRT_TXDATA_NOACK__ABRT_TXDATA_NOACK_VOID = 0;
    // Transmitted data not ACKed by addressed slave
    static const uint32_t IC_TX_ABRT_SOURCE_ABRT_TXDATA_NOACK__ABRT_TXDATA_NOACK_GENERATED = 1;
    // This abort is not generated
    static const uint32_t IC_TX_ABRT_SOURCE_ABRT_10ADDR2_NOACK__INACTIVE = 0;
    // Byte 2 of 10Bit Address not ACKed by any slave
    static const uint32_t IC_TX_ABRT_SOURCE_ABRT_10ADDR2_NOACK__ACTIVE = 1;
    // This abort is not generated
    static const uint32_t IC_TX_ABRT_SOURCE_ABRT_10ADDR1_NOACK__INACTIVE = 0;
    // Byte 1 of 10Bit Address not ACKed by any slave
    static const uint32_t IC_TX_ABRT_SOURCE_ABRT_10ADDR1_NOACK__ACTIVE = 1;
    // This abort is not generated
    static const uint32_t IC_TX_ABRT_SOURCE_ABRT_7B_ADDR_NOACK__INACTIVE = 0;
    // This abort is generated because of NOACK for 7-bit address
    static const uint32_t IC_TX_ABRT_SOURCE_ABRT_7B_ADDR_NOACK__ACTIVE = 1;

    // Generate Slave Data NACK Register The register is used to generate a NACK for the data part of a transfer when DW_apb_i2c is acting as a slave-receiver. This register only exists when the IC_SLV_DATA_NACK_ONLY parameter is set to 1. When this parameter disabled, this register does not exist and writing to the register's address has no effect. A write can occur on this register if both of the following conditions are met: - DW_apb_i2c is disabled (IC_ENABLE[0] = 0) - Slave part is inactive (IC_STATUS[6] = 0) Note: The IC_STATUS[6] is a register read-back location for the internal slv_activity signal; the user should poll this before writing the ic_slv_data_nack_only bit.
    // Reset value: 0x00000000
    BEGIN_TYPE(IC_SLV_DATA_NACK_ONLY_t, uint32_t)
        // Generate NACK. This NACK generation only occurs when DW_apb_i2c is a slave-receiver. If this register is set to a value of 1, it can only generate a NACK after a data byte is received; hence, the data transfer is aborted and the data received is not pushed to the receive buffer. When the register is set to a value of 0, it generates NACK/ACK, depending on normal criteria. - 1: generate NACK after data byte received - 0: generate NACK/ACK normally Reset value: 0x0
        ADD_BITFIELD_RW(NACK, 0, 1)
    END_TYPE()

    // Slave receiver generates NACK normally
    static const uint32_t IC_SLV_DATA_NACK_ONLY_NACK__DISABLED = 0;
    // Slave receiver generates NACK upon data reception only
    static const uint32_t IC_SLV_DATA_NACK_ONLY_NACK__ENABLED = 1;

    // DMA Control Register The register is used to enable the DMA Controller interface operation. There is a separate bit for transmit and receive. This can be programmed regardless of the state of IC_ENABLE.
    // Reset value: 0x00000000
    BEGIN_TYPE(IC_DMA_CR_t, uint32_t)
        // Transmit DMA Enable. This bit enables/disables the transmit FIFO DMA channel. Reset value: 0x0
        ADD_BITFIELD_RW(TDMAE, 1, 1)
        // Receive DMA Enable. This bit enables/disables the receive FIFO DMA channel. Reset value: 0x0
        ADD_BITFIELD_RW(RDMAE, 0, 1)
    END_TYPE()

    // transmit FIFO DMA channel disabled
    static const uint32_t IC_DMA_CR_TDMAE__DISABLED = 0;
    // Transmit FIFO DMA channel enabled
    static const uint32_t IC_DMA_CR_TDMAE__ENABLED = 1;
    // Receive FIFO DMA channel disabled
    static const uint32_t IC_DMA_CR_RDMAE__DISABLED = 0;
    // Receive FIFO DMA channel enabled
    static const uint32_t IC_DMA_CR_RDMAE__ENABLED = 1;

    // DMA Transmit Data Level Register
    // Reset value: 0x00000000
    BEGIN_TYPE(IC_DMA_TDLR_t, uint32_t)
        // Transmit Data Level. This bit field controls the level at which a DMA request is made by the transmit logic. It is equal to the watermark level; that is, the dma_tx_req signal is generated when the number of valid data entries in the transmit FIFO is equal to or below this field value, and TDMAE = 1. Reset value: 0x0
        ADD_BITFIELD_RW(DMATDL, 0, 4)
    END_TYPE()

    // I2C Receive Data Level Register
    // Reset value: 0x00000000
    BEGIN_TYPE(IC_DMA_RDLR_t, uint32_t)
        // Receive Data Level. This bit field controls the level at which a DMA request is made by the receive logic. The watermark level = DMARDL+1; that is, dma_rx_req is generated when the number of valid data entries in the receive FIFO is equal to or more than this field value + 1, and RDMAE =1. For instance, when DMARDL is 0, then dma_rx_req is asserted when 1 or more data entries are present in the receive FIFO. Reset value: 0x0
        ADD_BITFIELD_RW(DMARDL, 0, 4)
    END_TYPE()

    // I2C SDA Setup Register This register controls the amount of time delay (in terms of number of ic_clk clock periods) introduced in the rising edge of SCL - relative to SDA changing - when DW_apb_i2c services a read request in a slave-transmitter operation. The relevant I2C requirement is tSU:DAT (note 4) as detailed in the I2C Bus Specification. This register must be programmed with a value equal to or greater than 2. Writes to this register succeed only when IC_ENABLE[0] = 0. Note: The length of setup time is calculated using [(IC_SDA_SETUP - 1) * (ic_clk_period)], so if the user requires 10 ic_clk periods of setup time, they should program a value of 11. The IC_SDA_SETUP register is only used by the DW_apb_i2c when operating as a slave transmitter.
    // Reset value: 0x00000064
    BEGIN_TYPE(IC_SDA_SETUP_t, uint32_t)
        // SDA Setup. It is recommended that if the required delay is 1000ns, then for an ic_clk frequency of 10 MHz, IC_SDA_SETUP should be programmed to a value of 11. IC_SDA_SETUP must be programmed with a minimum value of 2.
        ADD_BITFIELD_RW(SDA_SETUP, 0, 8)
    END_TYPE()

    // I2C ACK General Call Register The register controls whether DW_apb_i2c responds with a ACK or NACK when it receives an I2C General Call address. This register is applicable only when the DW_apb_i2c is in slave mode.
    // Reset value: 0x00000001
    BEGIN_TYPE(IC_ACK_GENERAL_CALL_t, uint32_t)
        // ACK General Call. When set to 1, DW_apb_i2c responds with a ACK (by asserting ic_data_oe) when it receives a General Call. Otherwise, DW_apb_i2c responds with a NACK (by negating ic_data_oe).
        ADD_BITFIELD_RW(ACK_GEN_CALL, 0, 1)
    END_TYPE()

    // Generate NACK for a General Call
    static const uint32_t IC_ACK_GENERAL_CALL_ACK_GEN_CALL__DISABLED = 0;
    // Generate ACK for a General Call
    static const uint32_t IC_ACK_GENERAL_CALL_ACK_GEN_CALL__ENABLED = 1;

    // I2C Enable Status Register The register is used to report the DW_apb_i2c hardware status when the IC_ENABLE[0] register is set from 1 to 0; that is, when DW_apb_i2c is disabled. If IC_ENABLE[0] has been set to 1, bits 2:1 are forced to 0, and bit 0 is forced to 1. If IC_ENABLE[0] has been set to 0, bits 2:1 is only be valid as soon as bit 0 is read as '0'. Note: When IC_ENABLE[0] has been set to 0, a delay occurs for bit 0 to be read as 0 because disabling the DW_apb_i2c depends on I2C bus activities.
    // Reset value: 0x00000000
    BEGIN_TYPE(IC_ENABLE_STATUS_t, uint32_t)
        // Slave Received Data Lost. This bit indicates if a Slave-Receiver operation has been aborted with at least one data byte received from an I2C transfer due to the setting bit 0 of IC_ENABLE from 1 to 0. When read as 1, DW_apb_i2c is deemed to have been actively engaged in an aborted I2C transfer (with matching address) and the data phase of the I2C transfer has been entered, even though a data byte has been responded with a NACK. Note:  If the remote I2C master terminates the transfer with a STOP condition before the DW_apb_i2c has a chance to NACK a transfer, and IC_ENABLE[0] has been set to 0, then this bit is also set to 1. When read as 0, DW_apb_i2c is deemed to have been disabled without being actively involved in the data phase of a Slave-Receiver transfer. Note:  The CPU can safely read this bit when IC_EN (bit 0) is read as 0. Reset value: 0x0
        ADD_BITFIELD_RO(SLV_RX_DATA_LOST, 2, 1)
        // Slave Disabled While Busy (Transmit, Receive). This bit indicates if a potential or active Slave operation has been aborted due to the setting bit 0 of the IC_ENABLE register from 1 to 0. This bit is set when the CPU writes a 0 to the IC_ENABLE register while: (a) DW_apb_i2c is receiving the address byte of the Slave-Transmitter operation from a remote master; OR, (b) address and data bytes of the Slave-Receiver operation from a remote master. When read as 1, DW_apb_i2c is deemed to have forced a NACK during any part of an I2C transfer, irrespective of whether the I2C address matches the slave address set in DW_apb_i2c (IC_SAR register) OR if the transfer is completed before IC_ENABLE is set to 0 but has not taken effect. Note:  If the remote I2C master terminates the transfer with a STOP condition before the DW_apb_i2c has a chance to NACK a transfer, and IC_ENABLE[0] has been set to 0, then this bit will also be set to 1. When read as 0, DW_apb_i2c is deemed to have been disabled when there is master activity, or when the I2C bus is idle. Note:  The CPU can safely read this bit when IC_EN (bit 0) is read as 0. Reset value: 0x0
        ADD_BITFIELD_RO(SLV_DISABLED_WHILE_BUSY, 1, 1)
        // ic_en Status. This bit always reflects the value driven on the output port ic_en. - When read as 1, DW_apb_i2c is deemed to be in an enabled state. - When read as 0, DW_apb_i2c is deemed completely inactive. Note:  The CPU can safely read this bit anytime. When this bit is read as 0, the CPU can safely read SLV_RX_DATA_LOST (bit 2) and SLV_DISABLED_WHILE_BUSY (bit 1). Reset value: 0x0
        ADD_BITFIELD_RO(IC_EN, 0, 1)
    END_TYPE()

    // Slave RX Data is not lost
    static const uint32_t IC_ENABLE_STATUS_SLV_RX_DATA_LOST__INACTIVE = 0;
    // Slave RX Data is lost
    static const uint32_t IC_ENABLE_STATUS_SLV_RX_DATA_LOST__ACTIVE = 1;
    // Slave is disabled when it is idle
    static const uint32_t IC_ENABLE_STATUS_SLV_DISABLED_WHILE_BUSY__INACTIVE = 0;
    // Slave is disabled when it is active
    static const uint32_t IC_ENABLE_STATUS_SLV_DISABLED_WHILE_BUSY__ACTIVE = 1;
    // I2C disabled
    static const uint32_t IC_ENABLE_STATUS_IC_EN__DISABLED = 0;
    // I2C enabled
    static const uint32_t IC_ENABLE_STATUS_IC_EN__ENABLED = 1;

    // I2C SS, FS or FM+ spike suppression limit This register is used to store the duration, measured in ic_clk cycles, of the longest spike that is filtered out by the spike suppression logic when the component is operating in SS, FS or FM+ modes. The relevant I2C requirement is tSP (table 4) as detailed in the I2C Bus Specification. This register must be programmed with a minimum value of 1.
    // Reset value: 0x00000007
    BEGIN_TYPE(IC_FS_SPKLEN_t, uint32_t)
        // This register must be set before any I2C bus transaction can take place to ensure stable operation. This register sets the duration, measured in ic_clk cycles, of the longest spike in the SCL or SDA lines that will be filtered out by the spike suppression logic. This register can be written only when the I2C interface is disabled which corresponds to the IC_ENABLE[0] register being set to 0. Writes at other times have no effect. The minimum valid value is 1; hardware prevents values less than this being written, and if attempted results in 1 being set. or more information, refer to 'Spike Suppression'.
        ADD_BITFIELD_RW(IC_FS_SPKLEN, 0, 8)
    END_TYPE()

    // Clear RESTART_DET Interrupt Register
    // Reset value: 0x00000000
    BEGIN_TYPE(IC_CLR_RESTART_DET_t, uint32_t)
        // Read this register to clear the RESTART_DET interrupt (bit 12) of IC_RAW_INTR_STAT register. Reset value: 0x0
        ADD_BITFIELD_RO(CLR_RESTART_DET, 0, 1)
    END_TYPE()

    // Component Parameter Register 1 Note This register is not implemented and therefore reads as 0. If it was implemented it would be a constant read-only register that contains encoded information about the component's parameter settings. Fields shown below are the settings for those parameters
    // Reset value: 0x00000000
    BEGIN_TYPE(IC_COMP_PARAM_1_t, uint32_t)
        // TX Buffer Depth = 16
        ADD_BITFIELD_RO(TX_BUFFER_DEPTH, 16, 8)
        // RX Buffer Depth = 16
        ADD_BITFIELD_RO(RX_BUFFER_DEPTH, 8, 8)
        // Encoded parameters not visible
        ADD_BITFIELD_RO(ADD_ENCODED_PARAMS, 7, 1)
        // DMA handshaking signals are enabled
        ADD_BITFIELD_RO(HAS_DMA, 6, 1)
        // COMBINED Interrupt outputs
        ADD_BITFIELD_RO(INTR_IO, 5, 1)
        // Programmable count values for each mode.
        ADD_BITFIELD_RO(HC_COUNT_VALUES, 4, 1)
        // MAX SPEED MODE = FAST MODE
        ADD_BITFIELD_RO(MAX_SPEED_MODE, 2, 2)
        // APB data bus width is 32 bits
        ADD_BITFIELD_RO(APB_DATA_WIDTH, 0, 2)
    END_TYPE()

    // I2C Component Version Register
    // Reset value: 0x3230312a
    BEGIN_TYPE(IC_COMP_VERSION_t, uint32_t)
        ADD_BITFIELD_RO(IC_COMP_VERSION, 0, 32)
    END_TYPE()

    // I2C Component Type Register
    // Reset value: 0x44570140
    BEGIN_TYPE(IC_COMP_TYPE_t, uint32_t)
        // Designware Component Type number = 0x44_57_01_40. This assigned unique hex value is constant and is derived from the two ASCII letters 'DW' followed by a 16-bit unsigned number.
        ADD_BITFIELD_RO(IC_COMP_TYPE, 0, 32)
    END_TYPE()

    struct I2C0_t {
        IC_CON_t                      IC_CON;
        IC_TAR_t                      IC_TAR;
        IC_SAR_t                      IC_SAR;
        uint32_t                      reserved0;
        IC_DATA_CMD_t                 IC_DATA_CMD;
        IC_SS_SCL_HCNT_t              IC_SS_SCL_HCNT;
        IC_SS_SCL_LCNT_t              IC_SS_SCL_LCNT;
        IC_FS_SCL_HCNT_t              IC_FS_SCL_HCNT;
        IC_FS_SCL_LCNT_t              IC_FS_SCL_LCNT;
        uint32_t                      reserved1[2];
        IC_INTR_STAT_t                IC_INTR_STAT;
        IC_INTR_MASK_t                IC_INTR_MASK;
        IC_RAW_INTR_STAT_t            IC_RAW_INTR_STAT;
        IC_RX_TL_t                    IC_RX_TL;
        IC_TX_TL_t                    IC_TX_TL;
        IC_CLR_INTR_t                 IC_CLR_INTR;
        IC_CLR_RX_UNDER_t             IC_CLR_RX_UNDER;
        IC_CLR_RX_OVER_t              IC_CLR_RX_OVER;
        IC_CLR_TX_OVER_t              IC_CLR_TX_OVER;
        IC_CLR_RD_REQ_t               IC_CLR_RD_REQ;
        IC_CLR_TX_ABRT_t              IC_CLR_TX_ABRT;
        IC_CLR_RX_DONE_t              IC_CLR_RX_DONE;
        IC_CLR_ACTIVITY_t             IC_CLR_ACTIVITY;
        IC_CLR_STOP_DET_t             IC_CLR_STOP_DET;
        IC_CLR_START_DET_t            IC_CLR_START_DET;
        IC_CLR_GEN_CALL_t             IC_CLR_GEN_CALL;
        IC_ENABLE_t                   IC_ENABLE;
        IC_STATUS_t                   IC_STATUS;
        IC_TXFLR_t                    IC_TXFLR;
        IC_RXFLR_t                    IC_RXFLR;
        IC_SDA_HOLD_t                 IC_SDA_HOLD;
        IC_TX_ABRT_SOURCE_t           IC_TX_ABRT_SOURCE;
        IC_SLV_DATA_NACK_ONLY_t       IC_SLV_DATA_NACK_ONLY;
        IC_DMA_CR_t                   IC_DMA_CR;
        IC_DMA_TDLR_t                 IC_DMA_TDLR;
        IC_DMA_RDLR_t                 IC_DMA_RDLR;
        IC_SDA_SETUP_t                IC_SDA_SETUP;
        IC_ACK_GENERAL_CALL_t         IC_ACK_GENERAL_CALL;
        IC_ENABLE_STATUS_t            IC_ENABLE_STATUS;
        IC_FS_SPKLEN_t                IC_FS_SPKLEN;
        uint32_t                      reserved2;
        IC_CLR_RESTART_DET_t          IC_CLR_RESTART_DET;
        uint32_t                      reserved3[18];
        IC_COMP_PARAM_1_t             IC_COMP_PARAM_1;
        IC_COMP_VERSION_t             IC_COMP_VERSION;
        IC_COMP_TYPE_t                IC_COMP_TYPE;
    };

    static I2C0_t & I2C0 = (*(I2C0_t *)0x40090000);
    static I2C0_t & I2C0_XOR = (*(I2C0_t *)0x40091000);
    static I2C0_t & I2C0_SET = (*(I2C0_t *)0x40092000);
    static I2C0_t & I2C0_CLR = (*(I2C0_t *)0x40093000);

} // _I2C0_

namespace _I2C1_  {

    static _I2C0_::I2C0_t & I2C1 = (*(_I2C0_::I2C0_t *)0x40098000);
    static _I2C0_::I2C0_t & I2C1_XOR = (*(_I2C0_::I2C0_t *)0x40099000);
    static _I2C0_::I2C0_t & I2C1_SET = (*(_I2C0_::I2C0_t *)0x4009a000);
    static _I2C0_::I2C0_t & I2C1_CLR = (*(_I2C0_::I2C0_t *)0x4009b000);

} // _I2C1_

namespace _SPI0_  {

    // Control register 0, SSPCR0 on page 3-4
    // Reset value: 0x00000000
    BEGIN_TYPE(SSPCR0_t, uint32_t)
        // Serial clock rate. The value SCR is used to generate the transmit and receive bit rate of the PrimeCell SSP. The bit rate is: F SSPCLK CPSDVSR x (1+SCR) where CPSDVSR is an even value from 2-254, programmed through the SSPCPSR register and SCR is a value from 0-255.
        ADD_BITFIELD_RW(SCR, 8, 8)
        // SSPCLKOUT phase, applicable to Motorola SPI frame format only. See Motorola SPI frame format on page 2-10.
        ADD_BITFIELD_RW(SPH, 7, 1)
        // SSPCLKOUT polarity, applicable to Motorola SPI frame format only. See Motorola SPI frame format on page 2-10.
        ADD_BITFIELD_RW(SPO, 6, 1)
        // Frame format: 00 Motorola SPI frame format. 01 TI synchronous serial frame format. 10 National Microwire frame format. 11 Reserved, undefined operation.
        ADD_BITFIELD_RW(FRF, 4, 2)
        // Data Size Select: 0000 Reserved, undefined operation. 0001 Reserved, undefined operation. 0010 Reserved, undefined operation. 0011 4-bit data. 0100 5-bit data. 0101 6-bit data. 0110 7-bit data. 0111 8-bit data. 1000 9-bit data. 1001 10-bit data. 1010 11-bit data. 1011 12-bit data. 1100 13-bit data. 1101 14-bit data. 1110 15-bit data. 1111 16-bit data.
        ADD_BITFIELD_RW(DSS, 0, 4)
    END_TYPE()

    // Control register 1, SSPCR1 on page 3-5
    // Reset value: 0x00000000
    BEGIN_TYPE(SSPCR1_t, uint32_t)
        // Slave-mode output disable. This bit is relevant only in the slave mode, MS=1. In multiple-slave systems, it is possible for an PrimeCell SSP master to broadcast a message to all slaves in the system while ensuring that only one slave drives data onto its serial output line. In such systems the RXD lines from multiple slaves could be tied together. To operate in such systems, the SOD bit can be set if the PrimeCell SSP slave is not supposed to drive the SSPTXD line: 0 SSP can drive the SSPTXD output in slave mode. 1 SSP must not drive the SSPTXD output in slave mode.
        ADD_BITFIELD_RW(SOD, 3, 1)
        // Master or slave mode select. This bit can be modified only when the PrimeCell SSP is disabled, SSE=0: 0 Device configured as master, default. 1 Device configured as slave.
        ADD_BITFIELD_RW(MS, 2, 1)
        // Synchronous serial port enable: 0 SSP operation disabled. 1 SSP operation enabled.
        ADD_BITFIELD_RW(SSE, 1, 1)
        // Loop back mode: 0 Normal serial port operation enabled. 1 Output of transmit serial shifter is connected to input of receive serial shifter internally.
        ADD_BITFIELD_RW(LBM, 0, 1)
    END_TYPE()

    // Data register, SSPDR on page 3-6
    BEGIN_TYPE(SSPDR_t, uint32_t)
        // Transmit/Receive FIFO: Read Receive FIFO. Write Transmit FIFO. You must right-justify data when the PrimeCell SSP is programmed for a data size that is less than 16 bits. Unused bits at the top are ignored by transmit logic. The receive logic automatically right-justifies.
        ADD_BITFIELD_RW(DATA, 0, 16)
    END_TYPE()

    // Status register, SSPSR on page 3-7
    // Reset value: 0x00000003
    BEGIN_TYPE(SSPSR_t, uint32_t)
        // PrimeCell SSP busy flag, RO: 0 SSP is idle. 1 SSP is currently transmitting and/or receiving a frame or the transmit FIFO is not empty.
        ADD_BITFIELD_RO(BSY, 4, 1)
        // Receive FIFO full, RO: 0 Receive FIFO is not full. 1 Receive FIFO is full.
        ADD_BITFIELD_RO(RFF, 3, 1)
        // Receive FIFO not empty, RO: 0 Receive FIFO is empty. 1 Receive FIFO is not empty.
        ADD_BITFIELD_RO(RNE, 2, 1)
        // Transmit FIFO not full, RO: 0 Transmit FIFO is full. 1 Transmit FIFO is not full.
        ADD_BITFIELD_RO(TNF, 1, 1)
        // Transmit FIFO empty, RO: 0 Transmit FIFO is not empty. 1 Transmit FIFO is empty.
        ADD_BITFIELD_RO(TFE, 0, 1)
    END_TYPE()

    // Clock prescale register, SSPCPSR on page 3-8
    // Reset value: 0x00000000
    BEGIN_TYPE(SSPCPSR_t, uint32_t)
        // Clock prescale divisor. Must be an even number from 2-254, depending on the frequency of SSPCLK. The least significant bit always returns zero on reads.
        ADD_BITFIELD_RW(CPSDVSR, 0, 8)
    END_TYPE()

    // Interrupt mask set or clear register, SSPIMSC on page 3-9
    // Reset value: 0x00000000
    BEGIN_TYPE(SSPIMSC_t, uint32_t)
        // Transmit FIFO interrupt mask: 0 Transmit FIFO half empty or less condition interrupt is masked. 1 Transmit FIFO half empty or less condition interrupt is not masked.
        ADD_BITFIELD_RW(TXIM, 3, 1)
        // Receive FIFO interrupt mask: 0 Receive FIFO half full or less condition interrupt is masked. 1 Receive FIFO half full or less condition interrupt is not masked.
        ADD_BITFIELD_RW(RXIM, 2, 1)
        // Receive timeout interrupt mask: 0 Receive FIFO not empty and no read prior to timeout period interrupt is masked. 1 Receive FIFO not empty and no read prior to timeout period interrupt is not masked.
        ADD_BITFIELD_RW(RTIM, 1, 1)
        // Receive overrun interrupt mask: 0 Receive FIFO written to while full condition interrupt is masked. 1 Receive FIFO written to while full condition interrupt is not masked.
        ADD_BITFIELD_RW(RORIM, 0, 1)
    END_TYPE()

    // Raw interrupt status register, SSPRIS on page 3-10
    // Reset value: 0x00000008
    BEGIN_TYPE(SSPRIS_t, uint32_t)
        // Gives the raw interrupt state, prior to masking, of the SSPTXINTR interrupt
        ADD_BITFIELD_RO(TXRIS, 3, 1)
        // Gives the raw interrupt state, prior to masking, of the SSPRXINTR interrupt
        ADD_BITFIELD_RO(RXRIS, 2, 1)
        // Gives the raw interrupt state, prior to masking, of the SSPRTINTR interrupt
        ADD_BITFIELD_RO(RTRIS, 1, 1)
        // Gives the raw interrupt state, prior to masking, of the SSPRORINTR interrupt
        ADD_BITFIELD_RO(RORRIS, 0, 1)
    END_TYPE()

    // Masked interrupt status register, SSPMIS on page 3-11
    // Reset value: 0x00000000
    BEGIN_TYPE(SSPMIS_t, uint32_t)
        // Gives the transmit FIFO masked interrupt state, after masking, of the SSPTXINTR interrupt
        ADD_BITFIELD_RO(TXMIS, 3, 1)
        // Gives the receive FIFO masked interrupt state, after masking, of the SSPRXINTR interrupt
        ADD_BITFIELD_RO(RXMIS, 2, 1)
        // Gives the receive timeout masked interrupt state, after masking, of the SSPRTINTR interrupt
        ADD_BITFIELD_RO(RTMIS, 1, 1)
        // Gives the receive over run masked interrupt status, after masking, of the SSPRORINTR interrupt
        ADD_BITFIELD_RO(RORMIS, 0, 1)
    END_TYPE()

    // Interrupt clear register, SSPICR on page 3-11
    // Reset value: 0x00000000
    BEGIN_TYPE(SSPICR_t, uint32_t)
        // Clears the SSPRTINTR interrupt
        ADD_BITFIELD_RW(RTIC, 1, 1)
        // Clears the SSPRORINTR interrupt
        ADD_BITFIELD_RW(RORIC, 0, 1)
    END_TYPE()

    // DMA control register, SSPDMACR on page 3-12
    // Reset value: 0x00000000
    BEGIN_TYPE(SSPDMACR_t, uint32_t)
        // Transmit DMA Enable. If this bit is set to 1, DMA for the transmit FIFO is enabled.
        ADD_BITFIELD_RW(TXDMAE, 1, 1)
        // Receive DMA Enable. If this bit is set to 1, DMA for the receive FIFO is enabled.
        ADD_BITFIELD_RW(RXDMAE, 0, 1)
    END_TYPE()

    // Peripheral identification registers, SSPPeriphID0-3 on page 3-13
    // Reset value: 0x00000022
    BEGIN_TYPE(SSPPERIPHID0_t, uint32_t)
        // These bits read back as 0x22
        ADD_BITFIELD_RO(PARTNUMBER0, 0, 8)
    END_TYPE()

    // Peripheral identification registers, SSPPeriphID0-3 on page 3-13
    // Reset value: 0x00000010
    BEGIN_TYPE(SSPPERIPHID1_t, uint32_t)
        // These bits read back as 0x1
        ADD_BITFIELD_RO(DESIGNER0, 4, 4)
        // These bits read back as 0x0
        ADD_BITFIELD_RO(PARTNUMBER1, 0, 4)
    END_TYPE()

    // Peripheral identification registers, SSPPeriphID0-3 on page 3-13
    // Reset value: 0x00000034
    BEGIN_TYPE(SSPPERIPHID2_t, uint32_t)
        // These bits return the peripheral revision
        ADD_BITFIELD_RO(REVISION, 4, 4)
        // These bits read back as 0x4
        ADD_BITFIELD_RO(DESIGNER1, 0, 4)
    END_TYPE()

    // Peripheral identification registers, SSPPeriphID0-3 on page 3-13
    // Reset value: 0x00000000
    BEGIN_TYPE(SSPPERIPHID3_t, uint32_t)
        // These bits read back as 0x00
        ADD_BITFIELD_RO(CONFIGURATION, 0, 8)
    END_TYPE()

    // PrimeCell identification registers, SSPPCellID0-3 on page 3-16
    // Reset value: 0x0000000d
    BEGIN_TYPE(SSPPCELLID0_t, uint32_t)
        // These bits read back as 0x0D
        ADD_BITFIELD_RO(SSPPCELLID0, 0, 8)
    END_TYPE()

    // PrimeCell identification registers, SSPPCellID0-3 on page 3-16
    // Reset value: 0x000000f0
    BEGIN_TYPE(SSPPCELLID1_t, uint32_t)
        // These bits read back as 0xF0
        ADD_BITFIELD_RO(SSPPCELLID1, 0, 8)
    END_TYPE()

    // PrimeCell identification registers, SSPPCellID0-3 on page 3-16
    // Reset value: 0x00000005
    BEGIN_TYPE(SSPPCELLID2_t, uint32_t)
        // These bits read back as 0x05
        ADD_BITFIELD_RO(SSPPCELLID2, 0, 8)
    END_TYPE()

    // PrimeCell identification registers, SSPPCellID0-3 on page 3-16
    // Reset value: 0x000000b1
    BEGIN_TYPE(SSPPCELLID3_t, uint32_t)
        // These bits read back as 0xB1
        ADD_BITFIELD_RO(SSPPCELLID3, 0, 8)
    END_TYPE()

    struct SPI0_t {
        SSPCR0_t                      SSPCR0;
        SSPCR1_t                      SSPCR1;
        SSPDR_t                       SSPDR;
        SSPSR_t                       SSPSR;
        SSPCPSR_t                     SSPCPSR;
        SSPIMSC_t                     SSPIMSC;
        SSPRIS_t                      SSPRIS;
        SSPMIS_t                      SSPMIS;
        SSPICR_t                      SSPICR;
        SSPDMACR_t                    SSPDMACR;
        uint32_t                      reserved0[1006];
        SSPPERIPHID0_t                SSPPERIPHID0;
        SSPPERIPHID1_t                SSPPERIPHID1;
        SSPPERIPHID2_t                SSPPERIPHID2;
        SSPPERIPHID3_t                SSPPERIPHID3;
        SSPPCELLID0_t                 SSPPCELLID0;
        SSPPCELLID1_t                 SSPPCELLID1;
        SSPPCELLID2_t                 SSPPCELLID2;
        SSPPCELLID3_t                 SSPPCELLID3;
    };

    static SPI0_t & SPI0 = (*(SPI0_t *)0x40080000);
    static SPI0_t & SPI0_XOR = (*(SPI0_t *)0x40081000);
    static SPI0_t & SPI0_SET = (*(SPI0_t *)0x40082000);
    static SPI0_t & SPI0_CLR = (*(SPI0_t *)0x40083000);

} // _SPI0_

namespace _SPI1_  {

    static _SPI0_::SPI0_t & SPI1 = (*(_SPI0_::SPI0_t *)0x40088000);
    static _SPI0_::SPI0_t & SPI1_XOR = (*(_SPI0_::SPI0_t *)0x40089000);
    static _SPI0_::SPI0_t & SPI1_SET = (*(_SPI0_::SPI0_t *)0x4008a000);
    static _SPI0_::SPI0_t & SPI1_CLR = (*(_SPI0_::SPI0_t *)0x4008b000);

} // _SPI1_

// Programmable IO block
namespace _PIO0_  {

    // PIO control register
    // Reset value: 0x00000000
    BEGIN_TYPE(CTRL_t, uint32_t)
        // Write 1 to restart the clock dividers of state machines in neighbouring PIO blocks, as specified by NEXT_PIO_MASK and PREV_PIO_MASK in the same write. This is equivalent to writing 1 to the corresponding CLKDIV_RESTART bits in those PIOs' CTRL registers.
        ADD_BITFIELD_WO(NEXTPREV_CLKDIV_RESTART, 26, 1)
        // Write 1 to disable state machines in neighbouring PIO blocks, as specified by NEXT_PIO_MASK and PREV_PIO_MASK in the same write. This is equivalent to clearing the corresponding SM_ENABLE bits in those PIOs' CTRL registers.
        ADD_BITFIELD_WO(NEXTPREV_SM_DISABLE, 25, 1)
        // Write 1 to enable state machines in neighbouring PIO blocks, as specified by NEXT_PIO_MASK and PREV_PIO_MASK in the same write. This is equivalent to setting the corresponding SM_ENABLE bits in those PIOs' CTRL registers. If both OTHERS_SM_ENABLE and OTHERS_SM_DISABLE are set, the disable takes precedence.
        ADD_BITFIELD_WO(NEXTPREV_SM_ENABLE, 24, 1)
        // A mask of state machines in the neighbouring higher-numbered PIO block in the system (or PIO block 0 if this is the highest-numbered PIO block) to which to apply the operations specified by NEXTPREV_CLKDIV_RESTART, NEXTPREV_SM_ENABLE, and NEXTPREV_SM_DISABLE in the same write. This allows state machines in a neighbouring PIO block to be started/stopped/clock-synced exactly simultaneously with a write to this PIO block's CTRL register. Note that in a system with two PIOs, NEXT_PIO_MASK and PREV_PIO_MASK actually indicate the same PIO block. In this case the effects are applied cumulatively (as though the masks were OR'd together). Neighbouring PIO blocks are disconnected (status signals tied to 0 and control signals ignored) if one block is accessible to NonSecure code, and one is not.
        ADD_BITFIELD_WO(NEXT_PIO_MASK, 20, 4)
        // A mask of state machines in the neighbouring lower-numbered PIO block in the system (or the highest-numbered PIO block if this is PIO block 0) to which to apply the operations specified by OP_CLKDIV_RESTART, OP_ENABLE, OP_DISABLE in the same write. This allows state machines in a neighbouring PIO block to be started/stopped/clock-synced exactly simultaneously with a write to this PIO block's CTRL register. Neighbouring PIO blocks are disconnected (status signals tied to 0 and control signals ignored) if one block is accessible to NonSecure code, and one is not.
        ADD_BITFIELD_WO(PREV_PIO_MASK, 16, 4)
        // Restart a state machine's clock divider from an initial phase of 0. Clock dividers are free-running, so once started, their output (including fractional jitter) is completely determined by the integer/fractional divisor configured in SMx_CLKDIV. This means that, if multiple clock dividers with the same divisor are restarted simultaneously, by writing multiple 1 bits to this field, the execution clocks of those state machines will run in precise lockstep. Note that setting/clearing SM_ENABLE does not stop the clock divider from running, so once multiple state machines' clocks are synchronised, it is safe to disable/reenable a state machine, whilst keeping the clock dividers in sync. Note also that CLKDIV_RESTART can be written to whilst the state machine is running, and this is useful to resynchronise clock dividers after the divisors (SMx_CLKDIV) have been changed on-the-fly.
        ADD_BITFIELD_WO(CLKDIV_RESTART, 8, 4)
        // Write 1 to instantly clear internal SM state which may be otherwise difficult to access and will affect future execution. Specifically, the following are cleared: input and output shift counters; the contents of the input shift register; the delay counter; the waiting-on-IRQ state; any stalled instruction written to SMx_INSTR or run by OUT/MOV EXEC; any pin write left asserted due to OUT_STICKY. The contents of the output shift register and the X/Y scratch registers are not affected.
        ADD_BITFIELD_WO(SM_RESTART, 4, 4)
        // Enable/disable each of the four state machines by writing 1/0 to each of these four bits. When disabled, a state machine will cease executing instructions, except those written directly to SMx_INSTR by the system. Multiple bits can be set/cleared at once to run/halt multiple state machines simultaneously.
        ADD_BITFIELD_RW(SM_ENABLE, 0, 4)
    END_TYPE()

    // FIFO status register
    // Reset value: 0x0f000f00
    BEGIN_TYPE(FSTAT_t, uint32_t)
        // State machine TX FIFO is empty
        ADD_BITFIELD_RO(TXEMPTY, 24, 4)
        // State machine TX FIFO is full
        ADD_BITFIELD_RO(TXFULL, 16, 4)
        // State machine RX FIFO is empty
        ADD_BITFIELD_RO(RXEMPTY, 8, 4)
        // State machine RX FIFO is full
        ADD_BITFIELD_RO(RXFULL, 0, 4)
    END_TYPE()

    // FIFO debug register
    // Reset value: 0x00000000
    BEGIN_TYPE(FDEBUG_t, uint32_t)
        // State machine has stalled on empty TX FIFO during a blocking PULL, or an OUT with autopull enabled. Write 1 to clear.
        ADD_BITFIELD_RW(TXSTALL, 24, 4)
        // TX FIFO overflow (i.e. write-on-full by the system) has occurred. Write 1 to clear. Note that write-on-full does not alter the state or contents of the FIFO in any way, but the data that the system attempted to write is dropped, so if this flag is set, your software has quite likely dropped some data on the floor.
        ADD_BITFIELD_RW(TXOVER, 16, 4)
        // RX FIFO underflow (i.e. read-on-empty by the system) has occurred. Write 1 to clear. Note that read-on-empty does not perturb the state of the FIFO in any way, but the data returned by reading from an empty FIFO is undefined, so this flag generally only becomes set due to some kind of software error.
        ADD_BITFIELD_RW(RXUNDER, 8, 4)
        // State machine has stalled on full RX FIFO during a blocking PUSH, or an IN with autopush enabled. This flag is also set when a nonblocking PUSH to a full FIFO took place, in which case the state machine has dropped data. Write 1 to clear.
        ADD_BITFIELD_RW(RXSTALL, 0, 4)
    END_TYPE()

    // FIFO levels
    // Reset value: 0x00000000
    BEGIN_TYPE(FLEVEL_t, uint32_t)
        ADD_BITFIELD_RO(RX3, 28, 4)
        ADD_BITFIELD_RO(TX3, 24, 4)
        ADD_BITFIELD_RO(RX2, 20, 4)
        ADD_BITFIELD_RO(TX2, 16, 4)
        ADD_BITFIELD_RO(RX1, 12, 4)
        ADD_BITFIELD_RO(TX1, 8, 4)
        ADD_BITFIELD_RO(RX0, 4, 4)
        ADD_BITFIELD_RO(TX0, 0, 4)
    END_TYPE()

    // Direct write access to the TX FIFO for this state machine. Each write pushes one word to the FIFO. Attempting to write to a full FIFO has no effect on the FIFO state or contents, and sets the sticky FDEBUG_TXOVER error flag for this FIFO.
    // Reset value: 0x00000000
    BEGIN_TYPE(TXF0_t, uint32_t)
        ADD_BITFIELD_WO(TXF0, 0, 32)
    END_TYPE()

    // Direct write access to the TX FIFO for this state machine. Each write pushes one word to the FIFO. Attempting to write to a full FIFO has no effect on the FIFO state or contents, and sets the sticky FDEBUG_TXOVER error flag for this FIFO.
    // Reset value: 0x00000000
    BEGIN_TYPE(TXF1_t, uint32_t)
        ADD_BITFIELD_WO(TXF1, 0, 32)
    END_TYPE()

    // Direct write access to the TX FIFO for this state machine. Each write pushes one word to the FIFO. Attempting to write to a full FIFO has no effect on the FIFO state or contents, and sets the sticky FDEBUG_TXOVER error flag for this FIFO.
    // Reset value: 0x00000000
    BEGIN_TYPE(TXF2_t, uint32_t)
        ADD_BITFIELD_WO(TXF2, 0, 32)
    END_TYPE()

    // Direct write access to the TX FIFO for this state machine. Each write pushes one word to the FIFO. Attempting to write to a full FIFO has no effect on the FIFO state or contents, and sets the sticky FDEBUG_TXOVER error flag for this FIFO.
    // Reset value: 0x00000000
    BEGIN_TYPE(TXF3_t, uint32_t)
        ADD_BITFIELD_WO(TXF3, 0, 32)
    END_TYPE()

    // Direct read access to the RX FIFO for this state machine. Each read pops one word from the FIFO. Attempting to read from an empty FIFO has no effect on the FIFO state, and sets the sticky FDEBUG_RXUNDER error flag for this FIFO. The data returned to the system on a read from an empty FIFO is undefined.
    BEGIN_TYPE(RXF0_t, uint32_t)
        ADD_BITFIELD_RO(RXF0, 0, 32)
    END_TYPE()

    // Direct read access to the RX FIFO for this state machine. Each read pops one word from the FIFO. Attempting to read from an empty FIFO has no effect on the FIFO state, and sets the sticky FDEBUG_RXUNDER error flag for this FIFO. The data returned to the system on a read from an empty FIFO is undefined.
    BEGIN_TYPE(RXF1_t, uint32_t)
        ADD_BITFIELD_RO(RXF1, 0, 32)
    END_TYPE()

    // Direct read access to the RX FIFO for this state machine. Each read pops one word from the FIFO. Attempting to read from an empty FIFO has no effect on the FIFO state, and sets the sticky FDEBUG_RXUNDER error flag for this FIFO. The data returned to the system on a read from an empty FIFO is undefined.
    BEGIN_TYPE(RXF2_t, uint32_t)
        ADD_BITFIELD_RO(RXF2, 0, 32)
    END_TYPE()

    // Direct read access to the RX FIFO for this state machine. Each read pops one word from the FIFO. Attempting to read from an empty FIFO has no effect on the FIFO state, and sets the sticky FDEBUG_RXUNDER error flag for this FIFO. The data returned to the system on a read from an empty FIFO is undefined.
    BEGIN_TYPE(RXF3_t, uint32_t)
        ADD_BITFIELD_RO(RXF3, 0, 32)
    END_TYPE()

    // State machine IRQ flags register. Write 1 to clear. There are eight state machine IRQ flags, which can be set, cleared, and waited on by the state machines. There's no fixed association between flags and state machines -- any state machine can use any flag. Any of the eight flags can be used for timing synchronisation between state machines, using IRQ and WAIT instructions. Any combination of the eight flags can also routed out to either of the two system-level interrupt requests, alongside FIFO status interrupts -- see e.g. IRQ0_INTE.
    // Reset value: 0x00000000
    BEGIN_TYPE(IRQ_t, uint32_t)
        ADD_BITFIELD_RW(IRQ, 0, 8)
    END_TYPE()

    // Writing a 1 to each of these bits will forcibly assert the corresponding IRQ. Note this is different to the INTF register: writing here affects PIO internal state. INTF just asserts the processor-facing IRQ signal for testing ISRs, and is not visible to the state machines.
    // Reset value: 0x00000000
    BEGIN_TYPE(IRQ_FORCE_t, uint32_t)
        ADD_BITFIELD_WO(IRQ_FORCE, 0, 8)
    END_TYPE()

    // There is a 2-flipflop synchronizer on each GPIO input, which protects PIO logic from metastabilities. This increases input delay, and for fast synchronous IO (e.g. SPI) these synchronizers may need to be bypassed. Each bit in this register corresponds to one GPIO.  0 -> input is synchronized (default)  1 -> synchronizer is bypassed  If in doubt, leave this register as all zeroes.
    // Reset value: 0x00000000
    BEGIN_TYPE(INPUT_SYNC_BYPASS_t, uint32_t)
        ADD_BITFIELD_RW(INPUT_SYNC_BYPASS, 0, 32)
    END_TYPE()

    // Read to sample the pad output values PIO is currently driving to the GPIOs. On RP2040 there are 30 GPIOs, so the two most significant bits are hardwired to 0.
    // Reset value: 0x00000000
    BEGIN_TYPE(DBG_PADOUT_t, uint32_t)
        ADD_BITFIELD_RO(DBG_PADOUT, 0, 32)
    END_TYPE()

    // Read to sample the pad output enables (direction) PIO is currently driving to the GPIOs. On RP2040 there are 30 GPIOs, so the two most significant bits are hardwired to 0.
    // Reset value: 0x00000000
    BEGIN_TYPE(DBG_PADOE_t, uint32_t)
        ADD_BITFIELD_RO(DBG_PADOE, 0, 32)
    END_TYPE()

    // The PIO hardware has some free parameters that may vary between chip products.  These should be provided in the chip datasheet, but are also exposed here.
    // Reset value: 0x10000000
    BEGIN_TYPE(DBG_CFGINFO_t, uint32_t)
        // Version of the core PIO hardware.
        ADD_BITFIELD_RO(VERSION, 28, 4)
        // The size of the instruction memory, measured in units of one instruction
        ADD_BITFIELD_RO(IMEM_SIZE, 16, 6)
        // The number of state machines this PIO instance is equipped with.
        ADD_BITFIELD_RO(SM_COUNT, 8, 4)
        // The depth of the state machine TX/RX FIFOs, measured in words.  Joining fifos via SHIFTCTRL_FJOIN gives one FIFO with double  this depth.
        ADD_BITFIELD_RO(FIFO_DEPTH, 0, 6)
    END_TYPE()

    // Version 0 (RP2040)
    static const uint32_t DBG_CFGINFO_VERSION__v0 = 0;
    // Version 1 (RP2350)
    static const uint32_t DBG_CFGINFO_VERSION__v1 = 1;

    // Write-only access to instruction memory location 0
    // Reset value: 0x00000000
    BEGIN_TYPE(INSTR_MEM0_t, uint32_t)
        ADD_BITFIELD_WO(INSTR_MEM0, 0, 16)
    END_TYPE()

    // Write-only access to instruction memory location 1
    // Reset value: 0x00000000
    BEGIN_TYPE(INSTR_MEM1_t, uint32_t)
        ADD_BITFIELD_WO(INSTR_MEM1, 0, 16)
    END_TYPE()

    // Write-only access to instruction memory location 2
    // Reset value: 0x00000000
    BEGIN_TYPE(INSTR_MEM2_t, uint32_t)
        ADD_BITFIELD_WO(INSTR_MEM2, 0, 16)
    END_TYPE()

    // Write-only access to instruction memory location 3
    // Reset value: 0x00000000
    BEGIN_TYPE(INSTR_MEM3_t, uint32_t)
        ADD_BITFIELD_WO(INSTR_MEM3, 0, 16)
    END_TYPE()

    // Write-only access to instruction memory location 4
    // Reset value: 0x00000000
    BEGIN_TYPE(INSTR_MEM4_t, uint32_t)
        ADD_BITFIELD_WO(INSTR_MEM4, 0, 16)
    END_TYPE()

    // Write-only access to instruction memory location 5
    // Reset value: 0x00000000
    BEGIN_TYPE(INSTR_MEM5_t, uint32_t)
        ADD_BITFIELD_WO(INSTR_MEM5, 0, 16)
    END_TYPE()

    // Write-only access to instruction memory location 6
    // Reset value: 0x00000000
    BEGIN_TYPE(INSTR_MEM6_t, uint32_t)
        ADD_BITFIELD_WO(INSTR_MEM6, 0, 16)
    END_TYPE()

    // Write-only access to instruction memory location 7
    // Reset value: 0x00000000
    BEGIN_TYPE(INSTR_MEM7_t, uint32_t)
        ADD_BITFIELD_WO(INSTR_MEM7, 0, 16)
    END_TYPE()

    // Write-only access to instruction memory location 8
    // Reset value: 0x00000000
    BEGIN_TYPE(INSTR_MEM8_t, uint32_t)
        ADD_BITFIELD_WO(INSTR_MEM8, 0, 16)
    END_TYPE()

    // Write-only access to instruction memory location 9
    // Reset value: 0x00000000
    BEGIN_TYPE(INSTR_MEM9_t, uint32_t)
        ADD_BITFIELD_WO(INSTR_MEM9, 0, 16)
    END_TYPE()

    // Write-only access to instruction memory location 10
    // Reset value: 0x00000000
    BEGIN_TYPE(INSTR_MEM10_t, uint32_t)
        ADD_BITFIELD_WO(INSTR_MEM10, 0, 16)
    END_TYPE()

    // Write-only access to instruction memory location 11
    // Reset value: 0x00000000
    BEGIN_TYPE(INSTR_MEM11_t, uint32_t)
        ADD_BITFIELD_WO(INSTR_MEM11, 0, 16)
    END_TYPE()

    // Write-only access to instruction memory location 12
    // Reset value: 0x00000000
    BEGIN_TYPE(INSTR_MEM12_t, uint32_t)
        ADD_BITFIELD_WO(INSTR_MEM12, 0, 16)
    END_TYPE()

    // Write-only access to instruction memory location 13
    // Reset value: 0x00000000
    BEGIN_TYPE(INSTR_MEM13_t, uint32_t)
        ADD_BITFIELD_WO(INSTR_MEM13, 0, 16)
    END_TYPE()

    // Write-only access to instruction memory location 14
    // Reset value: 0x00000000
    BEGIN_TYPE(INSTR_MEM14_t, uint32_t)
        ADD_BITFIELD_WO(INSTR_MEM14, 0, 16)
    END_TYPE()

    // Write-only access to instruction memory location 15
    // Reset value: 0x00000000
    BEGIN_TYPE(INSTR_MEM15_t, uint32_t)
        ADD_BITFIELD_WO(INSTR_MEM15, 0, 16)
    END_TYPE()

    // Write-only access to instruction memory location 16
    // Reset value: 0x00000000
    BEGIN_TYPE(INSTR_MEM16_t, uint32_t)
        ADD_BITFIELD_WO(INSTR_MEM16, 0, 16)
    END_TYPE()

    // Write-only access to instruction memory location 17
    // Reset value: 0x00000000
    BEGIN_TYPE(INSTR_MEM17_t, uint32_t)
        ADD_BITFIELD_WO(INSTR_MEM17, 0, 16)
    END_TYPE()

    // Write-only access to instruction memory location 18
    // Reset value: 0x00000000
    BEGIN_TYPE(INSTR_MEM18_t, uint32_t)
        ADD_BITFIELD_WO(INSTR_MEM18, 0, 16)
    END_TYPE()

    // Write-only access to instruction memory location 19
    // Reset value: 0x00000000
    BEGIN_TYPE(INSTR_MEM19_t, uint32_t)
        ADD_BITFIELD_WO(INSTR_MEM19, 0, 16)
    END_TYPE()

    // Write-only access to instruction memory location 20
    // Reset value: 0x00000000
    BEGIN_TYPE(INSTR_MEM20_t, uint32_t)
        ADD_BITFIELD_WO(INSTR_MEM20, 0, 16)
    END_TYPE()

    // Write-only access to instruction memory location 21
    // Reset value: 0x00000000
    BEGIN_TYPE(INSTR_MEM21_t, uint32_t)
        ADD_BITFIELD_WO(INSTR_MEM21, 0, 16)
    END_TYPE()

    // Write-only access to instruction memory location 22
    // Reset value: 0x00000000
    BEGIN_TYPE(INSTR_MEM22_t, uint32_t)
        ADD_BITFIELD_WO(INSTR_MEM22, 0, 16)
    END_TYPE()

    // Write-only access to instruction memory location 23
    // Reset value: 0x00000000
    BEGIN_TYPE(INSTR_MEM23_t, uint32_t)
        ADD_BITFIELD_WO(INSTR_MEM23, 0, 16)
    END_TYPE()

    // Write-only access to instruction memory location 24
    // Reset value: 0x00000000
    BEGIN_TYPE(INSTR_MEM24_t, uint32_t)
        ADD_BITFIELD_WO(INSTR_MEM24, 0, 16)
    END_TYPE()

    // Write-only access to instruction memory location 25
    // Reset value: 0x00000000
    BEGIN_TYPE(INSTR_MEM25_t, uint32_t)
        ADD_BITFIELD_WO(INSTR_MEM25, 0, 16)
    END_TYPE()

    // Write-only access to instruction memory location 26
    // Reset value: 0x00000000
    BEGIN_TYPE(INSTR_MEM26_t, uint32_t)
        ADD_BITFIELD_WO(INSTR_MEM26, 0, 16)
    END_TYPE()

    // Write-only access to instruction memory location 27
    // Reset value: 0x00000000
    BEGIN_TYPE(INSTR_MEM27_t, uint32_t)
        ADD_BITFIELD_WO(INSTR_MEM27, 0, 16)
    END_TYPE()

    // Write-only access to instruction memory location 28
    // Reset value: 0x00000000
    BEGIN_TYPE(INSTR_MEM28_t, uint32_t)
        ADD_BITFIELD_WO(INSTR_MEM28, 0, 16)
    END_TYPE()

    // Write-only access to instruction memory location 29
    // Reset value: 0x00000000
    BEGIN_TYPE(INSTR_MEM29_t, uint32_t)
        ADD_BITFIELD_WO(INSTR_MEM29, 0, 16)
    END_TYPE()

    // Write-only access to instruction memory location 30
    // Reset value: 0x00000000
    BEGIN_TYPE(INSTR_MEM30_t, uint32_t)
        ADD_BITFIELD_WO(INSTR_MEM30, 0, 16)
    END_TYPE()

    // Write-only access to instruction memory location 31
    // Reset value: 0x00000000
    BEGIN_TYPE(INSTR_MEM31_t, uint32_t)
        ADD_BITFIELD_WO(INSTR_MEM31, 0, 16)
    END_TYPE()

    // Clock divisor register for state machine 0  Frequency = clock freq / (CLKDIV_INT + CLKDIV_FRAC / 256)
    // Reset value: 0x00010000
    BEGIN_TYPE(SM0_CLKDIV_t, uint32_t)
        // Effective frequency is sysclk/(int + frac/256).  Value of 0 is interpreted as 65536. If INT is 0, FRAC must also be 0.
        ADD_BITFIELD_RW(INT, 16, 16)
        // Fractional part of clock divisor
        ADD_BITFIELD_RW(FRAC, 8, 8)
    END_TYPE()

    // Execution/behavioural settings for state machine 0
    // Reset value: 0x0001f000
    BEGIN_TYPE(SM0_EXECCTRL_t, uint32_t)
        // If 1, an instruction written to SMx_INSTR is stalled, and latched by the state machine. Will clear to 0 once this instruction completes.
        ADD_BITFIELD_RO(EXEC_STALLED, 31, 1)
        // If 1, the MSB of the Delay/Side-set instruction field is used as side-set enable, rather than a side-set data bit. This allows instructions to perform side-set optionally, rather than on every instruction, but the maximum possible side-set width is reduced from 5 to 4. Note that the value of PINCTRL_SIDESET_COUNT is inclusive of this enable bit.
        ADD_BITFIELD_RW(SIDE_EN, 30, 1)
        // If 1, side-set data is asserted to pin directions, instead of pin values
        ADD_BITFIELD_RW(SIDE_PINDIR, 29, 1)
        // The GPIO number to use as condition for JMP PIN. Unaffected by input mapping.
        ADD_BITFIELD_RW(JMP_PIN, 24, 5)
        // Which data bit to use for inline OUT enable
        ADD_BITFIELD_RW(OUT_EN_SEL, 19, 5)
        // If 1, use a bit of OUT data as an auxiliary write enable  When used in conjunction with OUT_STICKY, writes with an enable of 0 will  deassert the latest pin write. This can create useful masking/override behaviour  due to the priority ordering of state machine pin writes (SM0 < SM1 < ...)
        ADD_BITFIELD_RW(INLINE_OUT_EN, 18, 1)
        // Continuously assert the most recent OUT/SET to the pins
        ADD_BITFIELD_RW(OUT_STICKY, 17, 1)
        // After reaching this address, execution is wrapped to wrap_bottom.  If the instruction is a jump, and the jump condition is true, the jump takes priority.
        ADD_BITFIELD_RW(WRAP_TOP, 12, 5)
        // After reaching wrap_top, execution is wrapped to this address.
        ADD_BITFIELD_RW(WRAP_BOTTOM, 7, 5)
        // Comparison used for the MOV x, STATUS instruction.
        ADD_BITFIELD_RW(STATUS_SEL, 5, 2)
        // Comparison level or IRQ index for the MOV x, STATUS instruction. If STATUS_SEL is TXLEVEL or RXLEVEL, then values of STATUS_N greater than the current FIFO depth are reserved, and have undefined behaviour.
        ADD_BITFIELD_RW(STATUS_N, 0, 5)
    END_TYPE()

    // All-ones if TX FIFO level < N, otherwise all-zeroes
    static const uint32_t SM0_EXECCTRL_STATUS_SEL__TXLEVEL = 0;
    // All-ones if RX FIFO level < N, otherwise all-zeroes
    static const uint32_t SM0_EXECCTRL_STATUS_SEL__RXLEVEL = 1;
    // All-ones if the indexed IRQ flag is raised, otherwise all-zeroes
    static const uint32_t SM0_EXECCTRL_STATUS_SEL__IRQ = 2;
    // Index 0-7 of an IRQ flag in this PIO block
    static const uint32_t SM0_EXECCTRL_STATUS_N__IRQ = 0;
    // Index 0-7 of an IRQ flag in the next lower-numbered PIO block
    static const uint32_t SM0_EXECCTRL_STATUS_N__IRQ_PREVPIO = 8;
    // Index 0-7 of an IRQ flag in the next higher-numbered PIO block
    static const uint32_t SM0_EXECCTRL_STATUS_N__IRQ_NEXTPIO = 16;

    // Control behaviour of the input/output shift registers for state machine 0
    // Reset value: 0x000c0000
    BEGIN_TYPE(SM0_SHIFTCTRL_t, uint32_t)
        // When 1, RX FIFO steals the TX FIFO's storage, and becomes twice as deep.  TX FIFO is disabled as a result (always reads as both full and empty).  FIFOs are flushed when this bit is changed.
        ADD_BITFIELD_RW(FJOIN_RX, 31, 1)
        // When 1, TX FIFO steals the RX FIFO's storage, and becomes twice as deep.  RX FIFO is disabled as a result (always reads as both full and empty).  FIFOs are flushed when this bit is changed.
        ADD_BITFIELD_RW(FJOIN_TX, 30, 1)
        // Number of bits shifted out of OSR before autopull, or conditional pull (PULL IFEMPTY), will take place.  Write 0 for value of 32.
        ADD_BITFIELD_RW(PULL_THRESH, 25, 5)
        // Number of bits shifted into ISR before autopush, or conditional push (PUSH IFFULL), will take place.  Write 0 for value of 32.
        ADD_BITFIELD_RW(PUSH_THRESH, 20, 5)
        // 1 = shift out of output shift register to right. 0 = to left.
        ADD_BITFIELD_RW(OUT_SHIFTDIR, 19, 1)
        // 1 = shift input shift register to right (data enters from left). 0 = to left.
        ADD_BITFIELD_RW(IN_SHIFTDIR, 18, 1)
        // Pull automatically when the output shift register is emptied, i.e. on or following an OUT instruction which causes the output shift counter to reach or exceed PULL_THRESH.
        ADD_BITFIELD_RW(AUTOPULL, 17, 1)
        // Push automatically when the input shift register is filled, i.e. on an IN instruction which causes the input shift counter to reach or exceed PUSH_THRESH.
        ADD_BITFIELD_RW(AUTOPUSH, 16, 1)
        // If 1, disable this state machine's RX FIFO, make its storage available for random write access by the state machine (using the `put` instruction) and, unless FJOIN_RX_GET is also set, random read access by the processor (through the RXFx_PUTGETy registers). If FJOIN_RX_PUT and FJOIN_RX_GET are both set, then the RX FIFO's registers can be randomly read/written by the state machine, but are completely inaccessible to the processor. Setting this bit will clear the FJOIN_TX and FJOIN_RX bits.
        ADD_BITFIELD_RW(FJOIN_RX_PUT, 15, 1)
        // If 1, disable this state machine's RX FIFO, make its storage available for random read access by the state machine (using the `get` instruction) and, unless FJOIN_RX_PUT is also set, random write access by the processor (through the RXFx_PUTGETy registers). If FJOIN_RX_PUT and FJOIN_RX_GET are both set, then the RX FIFO's registers can be randomly read/written by the state machine, but are completely inaccessible to the processor. Setting this bit will clear the FJOIN_TX and FJOIN_RX bits.
        ADD_BITFIELD_RW(FJOIN_RX_GET, 14, 1)
        // Set the number of pins which are not masked to 0 when read by an IN PINS, WAIT PIN or MOV x, PINS instruction. For example, an IN_COUNT of 5 means that the 5 LSBs of the IN pin group are visible (bits 4:0), but the remaining 27 MSBs are masked to 0. A count of 32 is encoded with a field value of 0, so the default behaviour is to not perform any masking. Note this masking is applied in addition to the masking usually performed by the IN instruction. This is mainly useful for the MOV x, PINS instruction, which otherwise has no way of masking pins.
        ADD_BITFIELD_RW(IN_COUNT, 0, 5)
    END_TYPE()

    // Current instruction address of state machine 0
    // Reset value: 0x00000000
    BEGIN_TYPE(SM0_ADDR_t, uint32_t)
        ADD_BITFIELD_RO(SM0_ADDR, 0, 5)
    END_TYPE()

    // Read to see the instruction currently addressed by state machine 0's program counter  Write to execute an instruction immediately (including jumps) and then resume execution.
    BEGIN_TYPE(SM0_INSTR_t, uint32_t)
        ADD_BITFIELD_RW(SM0_INSTR, 0, 16)
    END_TYPE()

    // State machine pin control
    // Reset value: 0x14000000
    BEGIN_TYPE(SM0_PINCTRL_t, uint32_t)
        // The number of MSBs of the Delay/Side-set instruction field which are used for side-set. Inclusive of the enable bit, if present. Minimum of 0 (all delay bits, no side-set) and maximum of 5 (all side-set, no delay).
        ADD_BITFIELD_RW(SIDESET_COUNT, 29, 3)
        // The number of pins asserted by a SET. In the range 0 to 5 inclusive.
        ADD_BITFIELD_RW(SET_COUNT, 26, 3)
        // The number of pins asserted by an OUT PINS, OUT PINDIRS or MOV PINS instruction. In the range 0 to 32 inclusive.
        ADD_BITFIELD_RW(OUT_COUNT, 20, 6)
        // The pin which is mapped to the least-significant bit of a state machine's IN data bus. Higher-numbered pins are mapped to consecutively more-significant data bits, with a modulo of 32 applied to pin number.
        ADD_BITFIELD_RW(IN_BASE, 15, 5)
        // The lowest-numbered pin that will be affected by a side-set operation. The MSBs of an instruction's side-set/delay field (up to 5, determined by SIDESET_COUNT) are used for side-set data, with the remaining LSBs used for delay. The least-significant bit of the side-set portion is the bit written to this pin, with more-significant bits written to higher-numbered pins.
        ADD_BITFIELD_RW(SIDESET_BASE, 10, 5)
        // The lowest-numbered pin that will be affected by a SET PINS or SET PINDIRS instruction. The data written to this pin is the least-significant bit of the SET data.
        ADD_BITFIELD_RW(SET_BASE, 5, 5)
        // The lowest-numbered pin that will be affected by an OUT PINS, OUT PINDIRS or MOV PINS instruction. The data written to this pin will always be the least-significant bit of the OUT or MOV data.
        ADD_BITFIELD_RW(OUT_BASE, 0, 5)
    END_TYPE()

    // Clock divisor register for state machine 1  Frequency = clock freq / (CLKDIV_INT + CLKDIV_FRAC / 256)
    // Reset value: 0x00010000
    BEGIN_TYPE(SM1_CLKDIV_t, uint32_t)
        // Effective frequency is sysclk/(int + frac/256).  Value of 0 is interpreted as 65536. If INT is 0, FRAC must also be 0.
        ADD_BITFIELD_RW(INT, 16, 16)
        // Fractional part of clock divisor
        ADD_BITFIELD_RW(FRAC, 8, 8)
    END_TYPE()

    // Execution/behavioural settings for state machine 1
    // Reset value: 0x0001f000
    BEGIN_TYPE(SM1_EXECCTRL_t, uint32_t)
        // If 1, an instruction written to SMx_INSTR is stalled, and latched by the state machine. Will clear to 0 once this instruction completes.
        ADD_BITFIELD_RO(EXEC_STALLED, 31, 1)
        // If 1, the MSB of the Delay/Side-set instruction field is used as side-set enable, rather than a side-set data bit. This allows instructions to perform side-set optionally, rather than on every instruction, but the maximum possible side-set width is reduced from 5 to 4. Note that the value of PINCTRL_SIDESET_COUNT is inclusive of this enable bit.
        ADD_BITFIELD_RW(SIDE_EN, 30, 1)
        // If 1, side-set data is asserted to pin directions, instead of pin values
        ADD_BITFIELD_RW(SIDE_PINDIR, 29, 1)
        // The GPIO number to use as condition for JMP PIN. Unaffected by input mapping.
        ADD_BITFIELD_RW(JMP_PIN, 24, 5)
        // Which data bit to use for inline OUT enable
        ADD_BITFIELD_RW(OUT_EN_SEL, 19, 5)
        // If 1, use a bit of OUT data as an auxiliary write enable  When used in conjunction with OUT_STICKY, writes with an enable of 0 will  deassert the latest pin write. This can create useful masking/override behaviour  due to the priority ordering of state machine pin writes (SM0 < SM1 < ...)
        ADD_BITFIELD_RW(INLINE_OUT_EN, 18, 1)
        // Continuously assert the most recent OUT/SET to the pins
        ADD_BITFIELD_RW(OUT_STICKY, 17, 1)
        // After reaching this address, execution is wrapped to wrap_bottom.  If the instruction is a jump, and the jump condition is true, the jump takes priority.
        ADD_BITFIELD_RW(WRAP_TOP, 12, 5)
        // After reaching wrap_top, execution is wrapped to this address.
        ADD_BITFIELD_RW(WRAP_BOTTOM, 7, 5)
        // Comparison used for the MOV x, STATUS instruction.
        ADD_BITFIELD_RW(STATUS_SEL, 5, 2)
        // Comparison level or IRQ index for the MOV x, STATUS instruction. If STATUS_SEL is TXLEVEL or RXLEVEL, then values of STATUS_N greater than the current FIFO depth are reserved, and have undefined behaviour.
        ADD_BITFIELD_RW(STATUS_N, 0, 5)
    END_TYPE()

    // All-ones if TX FIFO level < N, otherwise all-zeroes
    static const uint32_t SM1_EXECCTRL_STATUS_SEL__TXLEVEL = 0;
    // All-ones if RX FIFO level < N, otherwise all-zeroes
    static const uint32_t SM1_EXECCTRL_STATUS_SEL__RXLEVEL = 1;
    // All-ones if the indexed IRQ flag is raised, otherwise all-zeroes
    static const uint32_t SM1_EXECCTRL_STATUS_SEL__IRQ = 2;
    // Index 0-7 of an IRQ flag in this PIO block
    static const uint32_t SM1_EXECCTRL_STATUS_N__IRQ = 0;
    // Index 0-7 of an IRQ flag in the next lower-numbered PIO block
    static const uint32_t SM1_EXECCTRL_STATUS_N__IRQ_PREVPIO = 8;
    // Index 0-7 of an IRQ flag in the next higher-numbered PIO block
    static const uint32_t SM1_EXECCTRL_STATUS_N__IRQ_NEXTPIO = 16;

    // Control behaviour of the input/output shift registers for state machine 1
    // Reset value: 0x000c0000
    BEGIN_TYPE(SM1_SHIFTCTRL_t, uint32_t)
        // When 1, RX FIFO steals the TX FIFO's storage, and becomes twice as deep.  TX FIFO is disabled as a result (always reads as both full and empty).  FIFOs are flushed when this bit is changed.
        ADD_BITFIELD_RW(FJOIN_RX, 31, 1)
        // When 1, TX FIFO steals the RX FIFO's storage, and becomes twice as deep.  RX FIFO is disabled as a result (always reads as both full and empty).  FIFOs are flushed when this bit is changed.
        ADD_BITFIELD_RW(FJOIN_TX, 30, 1)
        // Number of bits shifted out of OSR before autopull, or conditional pull (PULL IFEMPTY), will take place.  Write 0 for value of 32.
        ADD_BITFIELD_RW(PULL_THRESH, 25, 5)
        // Number of bits shifted into ISR before autopush, or conditional push (PUSH IFFULL), will take place.  Write 0 for value of 32.
        ADD_BITFIELD_RW(PUSH_THRESH, 20, 5)
        // 1 = shift out of output shift register to right. 0 = to left.
        ADD_BITFIELD_RW(OUT_SHIFTDIR, 19, 1)
        // 1 = shift input shift register to right (data enters from left). 0 = to left.
        ADD_BITFIELD_RW(IN_SHIFTDIR, 18, 1)
        // Pull automatically when the output shift register is emptied, i.e. on or following an OUT instruction which causes the output shift counter to reach or exceed PULL_THRESH.
        ADD_BITFIELD_RW(AUTOPULL, 17, 1)
        // Push automatically when the input shift register is filled, i.e. on an IN instruction which causes the input shift counter to reach or exceed PUSH_THRESH.
        ADD_BITFIELD_RW(AUTOPUSH, 16, 1)
        // If 1, disable this state machine's RX FIFO, make its storage available for random write access by the state machine (using the `put` instruction) and, unless FJOIN_RX_GET is also set, random read access by the processor (through the RXFx_PUTGETy registers). If FJOIN_RX_PUT and FJOIN_RX_GET are both set, then the RX FIFO's registers can be randomly read/written by the state machine, but are completely inaccessible to the processor. Setting this bit will clear the FJOIN_TX and FJOIN_RX bits.
        ADD_BITFIELD_RW(FJOIN_RX_PUT, 15, 1)
        // If 1, disable this state machine's RX FIFO, make its storage available for random read access by the state machine (using the `get` instruction) and, unless FJOIN_RX_PUT is also set, random write access by the processor (through the RXFx_PUTGETy registers). If FJOIN_RX_PUT and FJOIN_RX_GET are both set, then the RX FIFO's registers can be randomly read/written by the state machine, but are completely inaccessible to the processor. Setting this bit will clear the FJOIN_TX and FJOIN_RX bits.
        ADD_BITFIELD_RW(FJOIN_RX_GET, 14, 1)
        // Set the number of pins which are not masked to 0 when read by an IN PINS, WAIT PIN or MOV x, PINS instruction. For example, an IN_COUNT of 5 means that the 5 LSBs of the IN pin group are visible (bits 4:0), but the remaining 27 MSBs are masked to 0. A count of 32 is encoded with a field value of 0, so the default behaviour is to not perform any masking. Note this masking is applied in addition to the masking usually performed by the IN instruction. This is mainly useful for the MOV x, PINS instruction, which otherwise has no way of masking pins.
        ADD_BITFIELD_RW(IN_COUNT, 0, 5)
    END_TYPE()

    // Current instruction address of state machine 1
    // Reset value: 0x00000000
    BEGIN_TYPE(SM1_ADDR_t, uint32_t)
        ADD_BITFIELD_RO(SM1_ADDR, 0, 5)
    END_TYPE()

    // Read to see the instruction currently addressed by state machine 1's program counter  Write to execute an instruction immediately (including jumps) and then resume execution.
    BEGIN_TYPE(SM1_INSTR_t, uint32_t)
        ADD_BITFIELD_RW(SM1_INSTR, 0, 16)
    END_TYPE()

    // State machine pin control
    // Reset value: 0x14000000
    BEGIN_TYPE(SM1_PINCTRL_t, uint32_t)
        // The number of MSBs of the Delay/Side-set instruction field which are used for side-set. Inclusive of the enable bit, if present. Minimum of 0 (all delay bits, no side-set) and maximum of 5 (all side-set, no delay).
        ADD_BITFIELD_RW(SIDESET_COUNT, 29, 3)
        // The number of pins asserted by a SET. In the range 0 to 5 inclusive.
        ADD_BITFIELD_RW(SET_COUNT, 26, 3)
        // The number of pins asserted by an OUT PINS, OUT PINDIRS or MOV PINS instruction. In the range 0 to 32 inclusive.
        ADD_BITFIELD_RW(OUT_COUNT, 20, 6)
        // The pin which is mapped to the least-significant bit of a state machine's IN data bus. Higher-numbered pins are mapped to consecutively more-significant data bits, with a modulo of 32 applied to pin number.
        ADD_BITFIELD_RW(IN_BASE, 15, 5)
        // The lowest-numbered pin that will be affected by a side-set operation. The MSBs of an instruction's side-set/delay field (up to 5, determined by SIDESET_COUNT) are used for side-set data, with the remaining LSBs used for delay. The least-significant bit of the side-set portion is the bit written to this pin, with more-significant bits written to higher-numbered pins.
        ADD_BITFIELD_RW(SIDESET_BASE, 10, 5)
        // The lowest-numbered pin that will be affected by a SET PINS or SET PINDIRS instruction. The data written to this pin is the least-significant bit of the SET data.
        ADD_BITFIELD_RW(SET_BASE, 5, 5)
        // The lowest-numbered pin that will be affected by an OUT PINS, OUT PINDIRS or MOV PINS instruction. The data written to this pin will always be the least-significant bit of the OUT or MOV data.
        ADD_BITFIELD_RW(OUT_BASE, 0, 5)
    END_TYPE()

    // Clock divisor register for state machine 2  Frequency = clock freq / (CLKDIV_INT + CLKDIV_FRAC / 256)
    // Reset value: 0x00010000
    BEGIN_TYPE(SM2_CLKDIV_t, uint32_t)
        // Effective frequency is sysclk/(int + frac/256).  Value of 0 is interpreted as 65536. If INT is 0, FRAC must also be 0.
        ADD_BITFIELD_RW(INT, 16, 16)
        // Fractional part of clock divisor
        ADD_BITFIELD_RW(FRAC, 8, 8)
    END_TYPE()

    // Execution/behavioural settings for state machine 2
    // Reset value: 0x0001f000
    BEGIN_TYPE(SM2_EXECCTRL_t, uint32_t)
        // If 1, an instruction written to SMx_INSTR is stalled, and latched by the state machine. Will clear to 0 once this instruction completes.
        ADD_BITFIELD_RO(EXEC_STALLED, 31, 1)
        // If 1, the MSB of the Delay/Side-set instruction field is used as side-set enable, rather than a side-set data bit. This allows instructions to perform side-set optionally, rather than on every instruction, but the maximum possible side-set width is reduced from 5 to 4. Note that the value of PINCTRL_SIDESET_COUNT is inclusive of this enable bit.
        ADD_BITFIELD_RW(SIDE_EN, 30, 1)
        // If 1, side-set data is asserted to pin directions, instead of pin values
        ADD_BITFIELD_RW(SIDE_PINDIR, 29, 1)
        // The GPIO number to use as condition for JMP PIN. Unaffected by input mapping.
        ADD_BITFIELD_RW(JMP_PIN, 24, 5)
        // Which data bit to use for inline OUT enable
        ADD_BITFIELD_RW(OUT_EN_SEL, 19, 5)
        // If 1, use a bit of OUT data as an auxiliary write enable  When used in conjunction with OUT_STICKY, writes with an enable of 0 will  deassert the latest pin write. This can create useful masking/override behaviour  due to the priority ordering of state machine pin writes (SM0 < SM1 < ...)
        ADD_BITFIELD_RW(INLINE_OUT_EN, 18, 1)
        // Continuously assert the most recent OUT/SET to the pins
        ADD_BITFIELD_RW(OUT_STICKY, 17, 1)
        // After reaching this address, execution is wrapped to wrap_bottom.  If the instruction is a jump, and the jump condition is true, the jump takes priority.
        ADD_BITFIELD_RW(WRAP_TOP, 12, 5)
        // After reaching wrap_top, execution is wrapped to this address.
        ADD_BITFIELD_RW(WRAP_BOTTOM, 7, 5)
        // Comparison used for the MOV x, STATUS instruction.
        ADD_BITFIELD_RW(STATUS_SEL, 5, 2)
        // Comparison level or IRQ index for the MOV x, STATUS instruction. If STATUS_SEL is TXLEVEL or RXLEVEL, then values of STATUS_N greater than the current FIFO depth are reserved, and have undefined behaviour.
        ADD_BITFIELD_RW(STATUS_N, 0, 5)
    END_TYPE()

    // All-ones if TX FIFO level < N, otherwise all-zeroes
    static const uint32_t SM2_EXECCTRL_STATUS_SEL__TXLEVEL = 0;
    // All-ones if RX FIFO level < N, otherwise all-zeroes
    static const uint32_t SM2_EXECCTRL_STATUS_SEL__RXLEVEL = 1;
    // All-ones if the indexed IRQ flag is raised, otherwise all-zeroes
    static const uint32_t SM2_EXECCTRL_STATUS_SEL__IRQ = 2;
    // Index 0-7 of an IRQ flag in this PIO block
    static const uint32_t SM2_EXECCTRL_STATUS_N__IRQ = 0;
    // Index 0-7 of an IRQ flag in the next lower-numbered PIO block
    static const uint32_t SM2_EXECCTRL_STATUS_N__IRQ_PREVPIO = 8;
    // Index 0-7 of an IRQ flag in the next higher-numbered PIO block
    static const uint32_t SM2_EXECCTRL_STATUS_N__IRQ_NEXTPIO = 16;

    // Control behaviour of the input/output shift registers for state machine 2
    // Reset value: 0x000c0000
    BEGIN_TYPE(SM2_SHIFTCTRL_t, uint32_t)
        // When 1, RX FIFO steals the TX FIFO's storage, and becomes twice as deep.  TX FIFO is disabled as a result (always reads as both full and empty).  FIFOs are flushed when this bit is changed.
        ADD_BITFIELD_RW(FJOIN_RX, 31, 1)
        // When 1, TX FIFO steals the RX FIFO's storage, and becomes twice as deep.  RX FIFO is disabled as a result (always reads as both full and empty).  FIFOs are flushed when this bit is changed.
        ADD_BITFIELD_RW(FJOIN_TX, 30, 1)
        // Number of bits shifted out of OSR before autopull, or conditional pull (PULL IFEMPTY), will take place.  Write 0 for value of 32.
        ADD_BITFIELD_RW(PULL_THRESH, 25, 5)
        // Number of bits shifted into ISR before autopush, or conditional push (PUSH IFFULL), will take place.  Write 0 for value of 32.
        ADD_BITFIELD_RW(PUSH_THRESH, 20, 5)
        // 1 = shift out of output shift register to right. 0 = to left.
        ADD_BITFIELD_RW(OUT_SHIFTDIR, 19, 1)
        // 1 = shift input shift register to right (data enters from left). 0 = to left.
        ADD_BITFIELD_RW(IN_SHIFTDIR, 18, 1)
        // Pull automatically when the output shift register is emptied, i.e. on or following an OUT instruction which causes the output shift counter to reach or exceed PULL_THRESH.
        ADD_BITFIELD_RW(AUTOPULL, 17, 1)
        // Push automatically when the input shift register is filled, i.e. on an IN instruction which causes the input shift counter to reach or exceed PUSH_THRESH.
        ADD_BITFIELD_RW(AUTOPUSH, 16, 1)
        // If 1, disable this state machine's RX FIFO, make its storage available for random write access by the state machine (using the `put` instruction) and, unless FJOIN_RX_GET is also set, random read access by the processor (through the RXFx_PUTGETy registers). If FJOIN_RX_PUT and FJOIN_RX_GET are both set, then the RX FIFO's registers can be randomly read/written by the state machine, but are completely inaccessible to the processor. Setting this bit will clear the FJOIN_TX and FJOIN_RX bits.
        ADD_BITFIELD_RW(FJOIN_RX_PUT, 15, 1)
        // If 1, disable this state machine's RX FIFO, make its storage available for random read access by the state machine (using the `get` instruction) and, unless FJOIN_RX_PUT is also set, random write access by the processor (through the RXFx_PUTGETy registers). If FJOIN_RX_PUT and FJOIN_RX_GET are both set, then the RX FIFO's registers can be randomly read/written by the state machine, but are completely inaccessible to the processor. Setting this bit will clear the FJOIN_TX and FJOIN_RX bits.
        ADD_BITFIELD_RW(FJOIN_RX_GET, 14, 1)
        // Set the number of pins which are not masked to 0 when read by an IN PINS, WAIT PIN or MOV x, PINS instruction. For example, an IN_COUNT of 5 means that the 5 LSBs of the IN pin group are visible (bits 4:0), but the remaining 27 MSBs are masked to 0. A count of 32 is encoded with a field value of 0, so the default behaviour is to not perform any masking. Note this masking is applied in addition to the masking usually performed by the IN instruction. This is mainly useful for the MOV x, PINS instruction, which otherwise has no way of masking pins.
        ADD_BITFIELD_RW(IN_COUNT, 0, 5)
    END_TYPE()

    // Current instruction address of state machine 2
    // Reset value: 0x00000000
    BEGIN_TYPE(SM2_ADDR_t, uint32_t)
        ADD_BITFIELD_RO(SM2_ADDR, 0, 5)
    END_TYPE()

    // Read to see the instruction currently addressed by state machine 2's program counter  Write to execute an instruction immediately (including jumps) and then resume execution.
    BEGIN_TYPE(SM2_INSTR_t, uint32_t)
        ADD_BITFIELD_RW(SM2_INSTR, 0, 16)
    END_TYPE()

    // State machine pin control
    // Reset value: 0x14000000
    BEGIN_TYPE(SM2_PINCTRL_t, uint32_t)
        // The number of MSBs of the Delay/Side-set instruction field which are used for side-set. Inclusive of the enable bit, if present. Minimum of 0 (all delay bits, no side-set) and maximum of 5 (all side-set, no delay).
        ADD_BITFIELD_RW(SIDESET_COUNT, 29, 3)
        // The number of pins asserted by a SET. In the range 0 to 5 inclusive.
        ADD_BITFIELD_RW(SET_COUNT, 26, 3)
        // The number of pins asserted by an OUT PINS, OUT PINDIRS or MOV PINS instruction. In the range 0 to 32 inclusive.
        ADD_BITFIELD_RW(OUT_COUNT, 20, 6)
        // The pin which is mapped to the least-significant bit of a state machine's IN data bus. Higher-numbered pins are mapped to consecutively more-significant data bits, with a modulo of 32 applied to pin number.
        ADD_BITFIELD_RW(IN_BASE, 15, 5)
        // The lowest-numbered pin that will be affected by a side-set operation. The MSBs of an instruction's side-set/delay field (up to 5, determined by SIDESET_COUNT) are used for side-set data, with the remaining LSBs used for delay. The least-significant bit of the side-set portion is the bit written to this pin, with more-significant bits written to higher-numbered pins.
        ADD_BITFIELD_RW(SIDESET_BASE, 10, 5)
        // The lowest-numbered pin that will be affected by a SET PINS or SET PINDIRS instruction. The data written to this pin is the least-significant bit of the SET data.
        ADD_BITFIELD_RW(SET_BASE, 5, 5)
        // The lowest-numbered pin that will be affected by an OUT PINS, OUT PINDIRS or MOV PINS instruction. The data written to this pin will always be the least-significant bit of the OUT or MOV data.
        ADD_BITFIELD_RW(OUT_BASE, 0, 5)
    END_TYPE()

    // Clock divisor register for state machine 3  Frequency = clock freq / (CLKDIV_INT + CLKDIV_FRAC / 256)
    // Reset value: 0x00010000
    BEGIN_TYPE(SM3_CLKDIV_t, uint32_t)
        // Effective frequency is sysclk/(int + frac/256).  Value of 0 is interpreted as 65536. If INT is 0, FRAC must also be 0.
        ADD_BITFIELD_RW(INT, 16, 16)
        // Fractional part of clock divisor
        ADD_BITFIELD_RW(FRAC, 8, 8)
    END_TYPE()

    // Execution/behavioural settings for state machine 3
    // Reset value: 0x0001f000
    BEGIN_TYPE(SM3_EXECCTRL_t, uint32_t)
        // If 1, an instruction written to SMx_INSTR is stalled, and latched by the state machine. Will clear to 0 once this instruction completes.
        ADD_BITFIELD_RO(EXEC_STALLED, 31, 1)
        // If 1, the MSB of the Delay/Side-set instruction field is used as side-set enable, rather than a side-set data bit. This allows instructions to perform side-set optionally, rather than on every instruction, but the maximum possible side-set width is reduced from 5 to 4. Note that the value of PINCTRL_SIDESET_COUNT is inclusive of this enable bit.
        ADD_BITFIELD_RW(SIDE_EN, 30, 1)
        // If 1, side-set data is asserted to pin directions, instead of pin values
        ADD_BITFIELD_RW(SIDE_PINDIR, 29, 1)
        // The GPIO number to use as condition for JMP PIN. Unaffected by input mapping.
        ADD_BITFIELD_RW(JMP_PIN, 24, 5)
        // Which data bit to use for inline OUT enable
        ADD_BITFIELD_RW(OUT_EN_SEL, 19, 5)
        // If 1, use a bit of OUT data as an auxiliary write enable  When used in conjunction with OUT_STICKY, writes with an enable of 0 will  deassert the latest pin write. This can create useful masking/override behaviour  due to the priority ordering of state machine pin writes (SM0 < SM1 < ...)
        ADD_BITFIELD_RW(INLINE_OUT_EN, 18, 1)
        // Continuously assert the most recent OUT/SET to the pins
        ADD_BITFIELD_RW(OUT_STICKY, 17, 1)
        // After reaching this address, execution is wrapped to wrap_bottom.  If the instruction is a jump, and the jump condition is true, the jump takes priority.
        ADD_BITFIELD_RW(WRAP_TOP, 12, 5)
        // After reaching wrap_top, execution is wrapped to this address.
        ADD_BITFIELD_RW(WRAP_BOTTOM, 7, 5)
        // Comparison used for the MOV x, STATUS instruction.
        ADD_BITFIELD_RW(STATUS_SEL, 5, 2)
        // Comparison level or IRQ index for the MOV x, STATUS instruction. If STATUS_SEL is TXLEVEL or RXLEVEL, then values of STATUS_N greater than the current FIFO depth are reserved, and have undefined behaviour.
        ADD_BITFIELD_RW(STATUS_N, 0, 5)
    END_TYPE()

    // All-ones if TX FIFO level < N, otherwise all-zeroes
    static const uint32_t SM3_EXECCTRL_STATUS_SEL__TXLEVEL = 0;
    // All-ones if RX FIFO level < N, otherwise all-zeroes
    static const uint32_t SM3_EXECCTRL_STATUS_SEL__RXLEVEL = 1;
    // All-ones if the indexed IRQ flag is raised, otherwise all-zeroes
    static const uint32_t SM3_EXECCTRL_STATUS_SEL__IRQ = 2;
    // Index 0-7 of an IRQ flag in this PIO block
    static const uint32_t SM3_EXECCTRL_STATUS_N__IRQ = 0;
    // Index 0-7 of an IRQ flag in the next lower-numbered PIO block
    static const uint32_t SM3_EXECCTRL_STATUS_N__IRQ_PREVPIO = 8;
    // Index 0-7 of an IRQ flag in the next higher-numbered PIO block
    static const uint32_t SM3_EXECCTRL_STATUS_N__IRQ_NEXTPIO = 16;

    // Control behaviour of the input/output shift registers for state machine 3
    // Reset value: 0x000c0000
    BEGIN_TYPE(SM3_SHIFTCTRL_t, uint32_t)
        // When 1, RX FIFO steals the TX FIFO's storage, and becomes twice as deep.  TX FIFO is disabled as a result (always reads as both full and empty).  FIFOs are flushed when this bit is changed.
        ADD_BITFIELD_RW(FJOIN_RX, 31, 1)
        // When 1, TX FIFO steals the RX FIFO's storage, and becomes twice as deep.  RX FIFO is disabled as a result (always reads as both full and empty).  FIFOs are flushed when this bit is changed.
        ADD_BITFIELD_RW(FJOIN_TX, 30, 1)
        // Number of bits shifted out of OSR before autopull, or conditional pull (PULL IFEMPTY), will take place.  Write 0 for value of 32.
        ADD_BITFIELD_RW(PULL_THRESH, 25, 5)
        // Number of bits shifted into ISR before autopush, or conditional push (PUSH IFFULL), will take place.  Write 0 for value of 32.
        ADD_BITFIELD_RW(PUSH_THRESH, 20, 5)
        // 1 = shift out of output shift register to right. 0 = to left.
        ADD_BITFIELD_RW(OUT_SHIFTDIR, 19, 1)
        // 1 = shift input shift register to right (data enters from left). 0 = to left.
        ADD_BITFIELD_RW(IN_SHIFTDIR, 18, 1)
        // Pull automatically when the output shift register is emptied, i.e. on or following an OUT instruction which causes the output shift counter to reach or exceed PULL_THRESH.
        ADD_BITFIELD_RW(AUTOPULL, 17, 1)
        // Push automatically when the input shift register is filled, i.e. on an IN instruction which causes the input shift counter to reach or exceed PUSH_THRESH.
        ADD_BITFIELD_RW(AUTOPUSH, 16, 1)
        // If 1, disable this state machine's RX FIFO, make its storage available for random write access by the state machine (using the `put` instruction) and, unless FJOIN_RX_GET is also set, random read access by the processor (through the RXFx_PUTGETy registers). If FJOIN_RX_PUT and FJOIN_RX_GET are both set, then the RX FIFO's registers can be randomly read/written by the state machine, but are completely inaccessible to the processor. Setting this bit will clear the FJOIN_TX and FJOIN_RX bits.
        ADD_BITFIELD_RW(FJOIN_RX_PUT, 15, 1)
        // If 1, disable this state machine's RX FIFO, make its storage available for random read access by the state machine (using the `get` instruction) and, unless FJOIN_RX_PUT is also set, random write access by the processor (through the RXFx_PUTGETy registers). If FJOIN_RX_PUT and FJOIN_RX_GET are both set, then the RX FIFO's registers can be randomly read/written by the state machine, but are completely inaccessible to the processor. Setting this bit will clear the FJOIN_TX and FJOIN_RX bits.
        ADD_BITFIELD_RW(FJOIN_RX_GET, 14, 1)
        // Set the number of pins which are not masked to 0 when read by an IN PINS, WAIT PIN or MOV x, PINS instruction. For example, an IN_COUNT of 5 means that the 5 LSBs of the IN pin group are visible (bits 4:0), but the remaining 27 MSBs are masked to 0. A count of 32 is encoded with a field value of 0, so the default behaviour is to not perform any masking. Note this masking is applied in addition to the masking usually performed by the IN instruction. This is mainly useful for the MOV x, PINS instruction, which otherwise has no way of masking pins.
        ADD_BITFIELD_RW(IN_COUNT, 0, 5)
    END_TYPE()

    // Current instruction address of state machine 3
    // Reset value: 0x00000000
    BEGIN_TYPE(SM3_ADDR_t, uint32_t)
        ADD_BITFIELD_RO(SM3_ADDR, 0, 5)
    END_TYPE()

    // Read to see the instruction currently addressed by state machine 3's program counter  Write to execute an instruction immediately (including jumps) and then resume execution.
    BEGIN_TYPE(SM3_INSTR_t, uint32_t)
        ADD_BITFIELD_RW(SM3_INSTR, 0, 16)
    END_TYPE()

    // State machine pin control
    // Reset value: 0x14000000
    BEGIN_TYPE(SM3_PINCTRL_t, uint32_t)
        // The number of MSBs of the Delay/Side-set instruction field which are used for side-set. Inclusive of the enable bit, if present. Minimum of 0 (all delay bits, no side-set) and maximum of 5 (all side-set, no delay).
        ADD_BITFIELD_RW(SIDESET_COUNT, 29, 3)
        // The number of pins asserted by a SET. In the range 0 to 5 inclusive.
        ADD_BITFIELD_RW(SET_COUNT, 26, 3)
        // The number of pins asserted by an OUT PINS, OUT PINDIRS or MOV PINS instruction. In the range 0 to 32 inclusive.
        ADD_BITFIELD_RW(OUT_COUNT, 20, 6)
        // The pin which is mapped to the least-significant bit of a state machine's IN data bus. Higher-numbered pins are mapped to consecutively more-significant data bits, with a modulo of 32 applied to pin number.
        ADD_BITFIELD_RW(IN_BASE, 15, 5)
        // The lowest-numbered pin that will be affected by a side-set operation. The MSBs of an instruction's side-set/delay field (up to 5, determined by SIDESET_COUNT) are used for side-set data, with the remaining LSBs used for delay. The least-significant bit of the side-set portion is the bit written to this pin, with more-significant bits written to higher-numbered pins.
        ADD_BITFIELD_RW(SIDESET_BASE, 10, 5)
        // The lowest-numbered pin that will be affected by a SET PINS or SET PINDIRS instruction. The data written to this pin is the least-significant bit of the SET data.
        ADD_BITFIELD_RW(SET_BASE, 5, 5)
        // The lowest-numbered pin that will be affected by an OUT PINS, OUT PINDIRS or MOV PINS instruction. The data written to this pin will always be the least-significant bit of the OUT or MOV data.
        ADD_BITFIELD_RW(OUT_BASE, 0, 5)
    END_TYPE()

    // Direct read/write access to entry 0 of SM0's RX FIFO, if SHIFTCTRL_FJOIN_RX_PUT xor SHIFTCTRL_FJOIN_RX_GET is set.
    // Reset value: 0x00000000
    BEGIN_TYPE(RXF0_PUTGET0_t, uint32_t)
        ADD_BITFIELD_RW(RXF0_PUTGET0, 0, 32)
    END_TYPE()

    // Direct read/write access to entry 1 of SM0's RX FIFO, if SHIFTCTRL_FJOIN_RX_PUT xor SHIFTCTRL_FJOIN_RX_GET is set.
    // Reset value: 0x00000000
    BEGIN_TYPE(RXF0_PUTGET1_t, uint32_t)
        ADD_BITFIELD_RW(RXF0_PUTGET1, 0, 32)
    END_TYPE()

    // Direct read/write access to entry 2 of SM0's RX FIFO, if SHIFTCTRL_FJOIN_RX_PUT xor SHIFTCTRL_FJOIN_RX_GET is set.
    // Reset value: 0x00000000
    BEGIN_TYPE(RXF0_PUTGET2_t, uint32_t)
        ADD_BITFIELD_RW(RXF0_PUTGET2, 0, 32)
    END_TYPE()

    // Direct read/write access to entry 3 of SM0's RX FIFO, if SHIFTCTRL_FJOIN_RX_PUT xor SHIFTCTRL_FJOIN_RX_GET is set.
    // Reset value: 0x00000000
    BEGIN_TYPE(RXF0_PUTGET3_t, uint32_t)
        ADD_BITFIELD_RW(RXF0_PUTGET3, 0, 32)
    END_TYPE()

    // Direct read/write access to entry 0 of SM1's RX FIFO, if SHIFTCTRL_FJOIN_RX_PUT xor SHIFTCTRL_FJOIN_RX_GET is set.
    // Reset value: 0x00000000
    BEGIN_TYPE(RXF1_PUTGET0_t, uint32_t)
        ADD_BITFIELD_RW(RXF1_PUTGET0, 0, 32)
    END_TYPE()

    // Direct read/write access to entry 1 of SM1's RX FIFO, if SHIFTCTRL_FJOIN_RX_PUT xor SHIFTCTRL_FJOIN_RX_GET is set.
    // Reset value: 0x00000000
    BEGIN_TYPE(RXF1_PUTGET1_t, uint32_t)
        ADD_BITFIELD_RW(RXF1_PUTGET1, 0, 32)
    END_TYPE()

    // Direct read/write access to entry 2 of SM1's RX FIFO, if SHIFTCTRL_FJOIN_RX_PUT xor SHIFTCTRL_FJOIN_RX_GET is set.
    // Reset value: 0x00000000
    BEGIN_TYPE(RXF1_PUTGET2_t, uint32_t)
        ADD_BITFIELD_RW(RXF1_PUTGET2, 0, 32)
    END_TYPE()

    // Direct read/write access to entry 3 of SM1's RX FIFO, if SHIFTCTRL_FJOIN_RX_PUT xor SHIFTCTRL_FJOIN_RX_GET is set.
    // Reset value: 0x00000000
    BEGIN_TYPE(RXF1_PUTGET3_t, uint32_t)
        ADD_BITFIELD_RW(RXF1_PUTGET3, 0, 32)
    END_TYPE()

    // Direct read/write access to entry 0 of SM2's RX FIFO, if SHIFTCTRL_FJOIN_RX_PUT xor SHIFTCTRL_FJOIN_RX_GET is set.
    // Reset value: 0x00000000
    BEGIN_TYPE(RXF2_PUTGET0_t, uint32_t)
        ADD_BITFIELD_RW(RXF2_PUTGET0, 0, 32)
    END_TYPE()

    // Direct read/write access to entry 1 of SM2's RX FIFO, if SHIFTCTRL_FJOIN_RX_PUT xor SHIFTCTRL_FJOIN_RX_GET is set.
    // Reset value: 0x00000000
    BEGIN_TYPE(RXF2_PUTGET1_t, uint32_t)
        ADD_BITFIELD_RW(RXF2_PUTGET1, 0, 32)
    END_TYPE()

    // Direct read/write access to entry 2 of SM2's RX FIFO, if SHIFTCTRL_FJOIN_RX_PUT xor SHIFTCTRL_FJOIN_RX_GET is set.
    // Reset value: 0x00000000
    BEGIN_TYPE(RXF2_PUTGET2_t, uint32_t)
        ADD_BITFIELD_RW(RXF2_PUTGET2, 0, 32)
    END_TYPE()

    // Direct read/write access to entry 3 of SM2's RX FIFO, if SHIFTCTRL_FJOIN_RX_PUT xor SHIFTCTRL_FJOIN_RX_GET is set.
    // Reset value: 0x00000000
    BEGIN_TYPE(RXF2_PUTGET3_t, uint32_t)
        ADD_BITFIELD_RW(RXF2_PUTGET3, 0, 32)
    END_TYPE()

    // Direct read/write access to entry 0 of SM3's RX FIFO, if SHIFTCTRL_FJOIN_RX_PUT xor SHIFTCTRL_FJOIN_RX_GET is set.
    // Reset value: 0x00000000
    BEGIN_TYPE(RXF3_PUTGET0_t, uint32_t)
        ADD_BITFIELD_RW(RXF3_PUTGET0, 0, 32)
    END_TYPE()

    // Direct read/write access to entry 1 of SM3's RX FIFO, if SHIFTCTRL_FJOIN_RX_PUT xor SHIFTCTRL_FJOIN_RX_GET is set.
    // Reset value: 0x00000000
    BEGIN_TYPE(RXF3_PUTGET1_t, uint32_t)
        ADD_BITFIELD_RW(RXF3_PUTGET1, 0, 32)
    END_TYPE()

    // Direct read/write access to entry 2 of SM3's RX FIFO, if SHIFTCTRL_FJOIN_RX_PUT xor SHIFTCTRL_FJOIN_RX_GET is set.
    // Reset value: 0x00000000
    BEGIN_TYPE(RXF3_PUTGET2_t, uint32_t)
        ADD_BITFIELD_RW(RXF3_PUTGET2, 0, 32)
    END_TYPE()

    // Direct read/write access to entry 3 of SM3's RX FIFO, if SHIFTCTRL_FJOIN_RX_PUT xor SHIFTCTRL_FJOIN_RX_GET is set.
    // Reset value: 0x00000000
    BEGIN_TYPE(RXF3_PUTGET3_t, uint32_t)
        ADD_BITFIELD_RW(RXF3_PUTGET3, 0, 32)
    END_TYPE()

    // Relocate GPIO 0 (from PIO's point of view) in the system GPIO numbering, to access more than 32 GPIOs from PIO. Only the values 0 and 16 are supported (only bit 4 is writable).
    // Reset value: 0x00000000
    BEGIN_TYPE(GPIOBASE_t, uint32_t)
        ADD_BITFIELD_RW(GPIOBASE, 4, 1)
    END_TYPE()

    // Raw Interrupts
    // Reset value: 0x00000000
    BEGIN_TYPE(INTR_t, uint32_t)
        ADD_BITFIELD_RO(SM7, 15, 1)
        ADD_BITFIELD_RO(SM6, 14, 1)
        ADD_BITFIELD_RO(SM5, 13, 1)
        ADD_BITFIELD_RO(SM4, 12, 1)
        ADD_BITFIELD_RO(SM3, 11, 1)
        ADD_BITFIELD_RO(SM2, 10, 1)
        ADD_BITFIELD_RO(SM1, 9, 1)
        ADD_BITFIELD_RO(SM0, 8, 1)
        ADD_BITFIELD_RO(SM3_TXNFULL, 7, 1)
        ADD_BITFIELD_RO(SM2_TXNFULL, 6, 1)
        ADD_BITFIELD_RO(SM1_TXNFULL, 5, 1)
        ADD_BITFIELD_RO(SM0_TXNFULL, 4, 1)
        ADD_BITFIELD_RO(SM3_RXNEMPTY, 3, 1)
        ADD_BITFIELD_RO(SM2_RXNEMPTY, 2, 1)
        ADD_BITFIELD_RO(SM1_RXNEMPTY, 1, 1)
        ADD_BITFIELD_RO(SM0_RXNEMPTY, 0, 1)
    END_TYPE()

    // Interrupt Enable for irq0
    // Reset value: 0x00000000
    BEGIN_TYPE(IRQ0_INTE_t, uint32_t)
        ADD_BITFIELD_RW(SM7, 15, 1)
        ADD_BITFIELD_RW(SM6, 14, 1)
        ADD_BITFIELD_RW(SM5, 13, 1)
        ADD_BITFIELD_RW(SM4, 12, 1)
        ADD_BITFIELD_RW(SM3, 11, 1)
        ADD_BITFIELD_RW(SM2, 10, 1)
        ADD_BITFIELD_RW(SM1, 9, 1)
        ADD_BITFIELD_RW(SM0, 8, 1)
        ADD_BITFIELD_RW(SM3_TXNFULL, 7, 1)
        ADD_BITFIELD_RW(SM2_TXNFULL, 6, 1)
        ADD_BITFIELD_RW(SM1_TXNFULL, 5, 1)
        ADD_BITFIELD_RW(SM0_TXNFULL, 4, 1)
        ADD_BITFIELD_RW(SM3_RXNEMPTY, 3, 1)
        ADD_BITFIELD_RW(SM2_RXNEMPTY, 2, 1)
        ADD_BITFIELD_RW(SM1_RXNEMPTY, 1, 1)
        ADD_BITFIELD_RW(SM0_RXNEMPTY, 0, 1)
    END_TYPE()

    // Interrupt Force for irq0
    // Reset value: 0x00000000
    BEGIN_TYPE(IRQ0_INTF_t, uint32_t)
        ADD_BITFIELD_RW(SM7, 15, 1)
        ADD_BITFIELD_RW(SM6, 14, 1)
        ADD_BITFIELD_RW(SM5, 13, 1)
        ADD_BITFIELD_RW(SM4, 12, 1)
        ADD_BITFIELD_RW(SM3, 11, 1)
        ADD_BITFIELD_RW(SM2, 10, 1)
        ADD_BITFIELD_RW(SM1, 9, 1)
        ADD_BITFIELD_RW(SM0, 8, 1)
        ADD_BITFIELD_RW(SM3_TXNFULL, 7, 1)
        ADD_BITFIELD_RW(SM2_TXNFULL, 6, 1)
        ADD_BITFIELD_RW(SM1_TXNFULL, 5, 1)
        ADD_BITFIELD_RW(SM0_TXNFULL, 4, 1)
        ADD_BITFIELD_RW(SM3_RXNEMPTY, 3, 1)
        ADD_BITFIELD_RW(SM2_RXNEMPTY, 2, 1)
        ADD_BITFIELD_RW(SM1_RXNEMPTY, 1, 1)
        ADD_BITFIELD_RW(SM0_RXNEMPTY, 0, 1)
    END_TYPE()

    // Interrupt status after masking & forcing for irq0
    // Reset value: 0x00000000
    BEGIN_TYPE(IRQ0_INTS_t, uint32_t)
        ADD_BITFIELD_RO(SM7, 15, 1)
        ADD_BITFIELD_RO(SM6, 14, 1)
        ADD_BITFIELD_RO(SM5, 13, 1)
        ADD_BITFIELD_RO(SM4, 12, 1)
        ADD_BITFIELD_RO(SM3, 11, 1)
        ADD_BITFIELD_RO(SM2, 10, 1)
        ADD_BITFIELD_RO(SM1, 9, 1)
        ADD_BITFIELD_RO(SM0, 8, 1)
        ADD_BITFIELD_RO(SM3_TXNFULL, 7, 1)
        ADD_BITFIELD_RO(SM2_TXNFULL, 6, 1)
        ADD_BITFIELD_RO(SM1_TXNFULL, 5, 1)
        ADD_BITFIELD_RO(SM0_TXNFULL, 4, 1)
        ADD_BITFIELD_RO(SM3_RXNEMPTY, 3, 1)
        ADD_BITFIELD_RO(SM2_RXNEMPTY, 2, 1)
        ADD_BITFIELD_RO(SM1_RXNEMPTY, 1, 1)
        ADD_BITFIELD_RO(SM0_RXNEMPTY, 0, 1)
    END_TYPE()

    // Interrupt Enable for irq1
    // Reset value: 0x00000000
    BEGIN_TYPE(IRQ1_INTE_t, uint32_t)
        ADD_BITFIELD_RW(SM7, 15, 1)
        ADD_BITFIELD_RW(SM6, 14, 1)
        ADD_BITFIELD_RW(SM5, 13, 1)
        ADD_BITFIELD_RW(SM4, 12, 1)
        ADD_BITFIELD_RW(SM3, 11, 1)
        ADD_BITFIELD_RW(SM2, 10, 1)
        ADD_BITFIELD_RW(SM1, 9, 1)
        ADD_BITFIELD_RW(SM0, 8, 1)
        ADD_BITFIELD_RW(SM3_TXNFULL, 7, 1)
        ADD_BITFIELD_RW(SM2_TXNFULL, 6, 1)
        ADD_BITFIELD_RW(SM1_TXNFULL, 5, 1)
        ADD_BITFIELD_RW(SM0_TXNFULL, 4, 1)
        ADD_BITFIELD_RW(SM3_RXNEMPTY, 3, 1)
        ADD_BITFIELD_RW(SM2_RXNEMPTY, 2, 1)
        ADD_BITFIELD_RW(SM1_RXNEMPTY, 1, 1)
        ADD_BITFIELD_RW(SM0_RXNEMPTY, 0, 1)
    END_TYPE()

    // Interrupt Force for irq1
    // Reset value: 0x00000000
    BEGIN_TYPE(IRQ1_INTF_t, uint32_t)
        ADD_BITFIELD_RW(SM7, 15, 1)
        ADD_BITFIELD_RW(SM6, 14, 1)
        ADD_BITFIELD_RW(SM5, 13, 1)
        ADD_BITFIELD_RW(SM4, 12, 1)
        ADD_BITFIELD_RW(SM3, 11, 1)
        ADD_BITFIELD_RW(SM2, 10, 1)
        ADD_BITFIELD_RW(SM1, 9, 1)
        ADD_BITFIELD_RW(SM0, 8, 1)
        ADD_BITFIELD_RW(SM3_TXNFULL, 7, 1)
        ADD_BITFIELD_RW(SM2_TXNFULL, 6, 1)
        ADD_BITFIELD_RW(SM1_TXNFULL, 5, 1)
        ADD_BITFIELD_RW(SM0_TXNFULL, 4, 1)
        ADD_BITFIELD_RW(SM3_RXNEMPTY, 3, 1)
        ADD_BITFIELD_RW(SM2_RXNEMPTY, 2, 1)
        ADD_BITFIELD_RW(SM1_RXNEMPTY, 1, 1)
        ADD_BITFIELD_RW(SM0_RXNEMPTY, 0, 1)
    END_TYPE()

    // Interrupt status after masking & forcing for irq1
    // Reset value: 0x00000000
    BEGIN_TYPE(IRQ1_INTS_t, uint32_t)
        ADD_BITFIELD_RO(SM7, 15, 1)
        ADD_BITFIELD_RO(SM6, 14, 1)
        ADD_BITFIELD_RO(SM5, 13, 1)
        ADD_BITFIELD_RO(SM4, 12, 1)
        ADD_BITFIELD_RO(SM3, 11, 1)
        ADD_BITFIELD_RO(SM2, 10, 1)
        ADD_BITFIELD_RO(SM1, 9, 1)
        ADD_BITFIELD_RO(SM0, 8, 1)
        ADD_BITFIELD_RO(SM3_TXNFULL, 7, 1)
        ADD_BITFIELD_RO(SM2_TXNFULL, 6, 1)
        ADD_BITFIELD_RO(SM1_TXNFULL, 5, 1)
        ADD_BITFIELD_RO(SM0_TXNFULL, 4, 1)
        ADD_BITFIELD_RO(SM3_RXNEMPTY, 3, 1)
        ADD_BITFIELD_RO(SM2_RXNEMPTY, 2, 1)
        ADD_BITFIELD_RO(SM1_RXNEMPTY, 1, 1)
        ADD_BITFIELD_RO(SM0_RXNEMPTY, 0, 1)
    END_TYPE()

    struct PIO0_t {
        CTRL_t                        CTRL;
        FSTAT_t                       FSTAT;
        FDEBUG_t                      FDEBUG;
        FLEVEL_t                      FLEVEL;
        TXF0_t                        TXF0;
        TXF1_t                        TXF1;
        TXF2_t                        TXF2;
        TXF3_t                        TXF3;
        RXF0_t                        RXF0;
        RXF1_t                        RXF1;
        RXF2_t                        RXF2;
        RXF3_t                        RXF3;
        IRQ_t                         IRQ;
        IRQ_FORCE_t                   IRQ_FORCE;
        INPUT_SYNC_BYPASS_t           INPUT_SYNC_BYPASS;
        DBG_PADOUT_t                  DBG_PADOUT;
        DBG_PADOE_t                   DBG_PADOE;
        DBG_CFGINFO_t                 DBG_CFGINFO;
        INSTR_MEM0_t                  INSTR_MEM0;
        INSTR_MEM1_t                  INSTR_MEM1;
        INSTR_MEM2_t                  INSTR_MEM2;
        INSTR_MEM3_t                  INSTR_MEM3;
        INSTR_MEM4_t                  INSTR_MEM4;
        INSTR_MEM5_t                  INSTR_MEM5;
        INSTR_MEM6_t                  INSTR_MEM6;
        INSTR_MEM7_t                  INSTR_MEM7;
        INSTR_MEM8_t                  INSTR_MEM8;
        INSTR_MEM9_t                  INSTR_MEM9;
        INSTR_MEM10_t                 INSTR_MEM10;
        INSTR_MEM11_t                 INSTR_MEM11;
        INSTR_MEM12_t                 INSTR_MEM12;
        INSTR_MEM13_t                 INSTR_MEM13;
        INSTR_MEM14_t                 INSTR_MEM14;
        INSTR_MEM15_t                 INSTR_MEM15;
        INSTR_MEM16_t                 INSTR_MEM16;
        INSTR_MEM17_t                 INSTR_MEM17;
        INSTR_MEM18_t                 INSTR_MEM18;
        INSTR_MEM19_t                 INSTR_MEM19;
        INSTR_MEM20_t                 INSTR_MEM20;
        INSTR_MEM21_t                 INSTR_MEM21;
        INSTR_MEM22_t                 INSTR_MEM22;
        INSTR_MEM23_t                 INSTR_MEM23;
        INSTR_MEM24_t                 INSTR_MEM24;
        INSTR_MEM25_t                 INSTR_MEM25;
        INSTR_MEM26_t                 INSTR_MEM26;
        INSTR_MEM27_t                 INSTR_MEM27;
        INSTR_MEM28_t                 INSTR_MEM28;
        INSTR_MEM29_t                 INSTR_MEM29;
        INSTR_MEM30_t                 INSTR_MEM30;
        INSTR_MEM31_t                 INSTR_MEM31;
        SM0_CLKDIV_t                  SM0_CLKDIV;
        SM0_EXECCTRL_t                SM0_EXECCTRL;
        SM0_SHIFTCTRL_t               SM0_SHIFTCTRL;
        SM0_ADDR_t                    SM0_ADDR;
        SM0_INSTR_t                   SM0_INSTR;
        SM0_PINCTRL_t                 SM0_PINCTRL;
        SM1_CLKDIV_t                  SM1_CLKDIV;
        SM1_EXECCTRL_t                SM1_EXECCTRL;
        SM1_SHIFTCTRL_t               SM1_SHIFTCTRL;
        SM1_ADDR_t                    SM1_ADDR;
        SM1_INSTR_t                   SM1_INSTR;
        SM1_PINCTRL_t                 SM1_PINCTRL;
        SM2_CLKDIV_t                  SM2_CLKDIV;
        SM2_EXECCTRL_t                SM2_EXECCTRL;
        SM2_SHIFTCTRL_t               SM2_SHIFTCTRL;
        SM2_ADDR_t                    SM2_ADDR;
        SM2_INSTR_t                   SM2_INSTR;
        SM2_PINCTRL_t                 SM2_PINCTRL;
        SM3_CLKDIV_t                  SM3_CLKDIV;
        SM3_EXECCTRL_t                SM3_EXECCTRL;
        SM3_SHIFTCTRL_t               SM3_SHIFTCTRL;
        SM3_ADDR_t                    SM3_ADDR;
        SM3_INSTR_t                   SM3_INSTR;
        SM3_PINCTRL_t                 SM3_PINCTRL;
        RXF0_PUTGET0_t                RXF0_PUTGET0;
        RXF0_PUTGET1_t                RXF0_PUTGET1;
        RXF0_PUTGET2_t                RXF0_PUTGET2;
        RXF0_PUTGET3_t                RXF0_PUTGET3;
        RXF1_PUTGET0_t                RXF1_PUTGET0;
        RXF1_PUTGET1_t                RXF1_PUTGET1;
        RXF1_PUTGET2_t                RXF1_PUTGET2;
        RXF1_PUTGET3_t                RXF1_PUTGET3;
        RXF2_PUTGET0_t                RXF2_PUTGET0;
        RXF2_PUTGET1_t                RXF2_PUTGET1;
        RXF2_PUTGET2_t                RXF2_PUTGET2;
        RXF2_PUTGET3_t                RXF2_PUTGET3;
        RXF3_PUTGET0_t                RXF3_PUTGET0;
        RXF3_PUTGET1_t                RXF3_PUTGET1;
        RXF3_PUTGET2_t                RXF3_PUTGET2;
        RXF3_PUTGET3_t                RXF3_PUTGET3;
        GPIOBASE_t                    GPIOBASE;
        INTR_t                        INTR;
        IRQ0_INTE_t                   IRQ0_INTE;
        IRQ0_INTF_t                   IRQ0_INTF;
        IRQ0_INTS_t                   IRQ0_INTS;
        IRQ1_INTE_t                   IRQ1_INTE;
        IRQ1_INTF_t                   IRQ1_INTF;
        IRQ1_INTS_t                   IRQ1_INTS;
    };

    static PIO0_t & PIO0 = (*(PIO0_t *)0x50200000);
    static PIO0_t & PIO0_XOR = (*(PIO0_t *)0x50201000);
    static PIO0_t & PIO0_SET = (*(PIO0_t *)0x50202000);
    static PIO0_t & PIO0_CLR = (*(PIO0_t *)0x50203000);

} // _PIO0_

namespace _PIO1_  {

    static _PIO0_::PIO0_t & PIO1 = (*(_PIO0_::PIO0_t *)0x50300000);
    static _PIO0_::PIO0_t & PIO1_XOR = (*(_PIO0_::PIO0_t *)0x50301000);
    static _PIO0_::PIO0_t & PIO1_SET = (*(_PIO0_::PIO0_t *)0x50302000);
    static _PIO0_::PIO0_t & PIO1_CLR = (*(_PIO0_::PIO0_t *)0x50303000);

} // _PIO1_

namespace _PIO2_  {

    static _PIO0_::PIO0_t & PIO2 = (*(_PIO0_::PIO0_t *)0x50400000);
    static _PIO0_::PIO0_t & PIO2_XOR = (*(_PIO0_::PIO0_t *)0x50401000);
    static _PIO0_::PIO0_t & PIO2_SET = (*(_PIO0_::PIO0_t *)0x50402000);
    static _PIO0_::PIO0_t & PIO2_CLR = (*(_PIO0_::PIO0_t *)0x50403000);

} // _PIO2_

// Register block for busfabric control signals and performance counters
namespace _BUSCTRL_  {

    // Set the priority of each master for bus arbitration.
    // Reset value: 0x00000000
    BEGIN_TYPE(BUS_PRIORITY_t, uint32_t)
        // 0 - low priority, 1 - high priority
        ADD_BITFIELD_RW(DMA_W, 12, 1)
        // 0 - low priority, 1 - high priority
        ADD_BITFIELD_RW(DMA_R, 8, 1)
        // 0 - low priority, 1 - high priority
        ADD_BITFIELD_RW(PROC1, 4, 1)
        // 0 - low priority, 1 - high priority
        ADD_BITFIELD_RW(PROC0, 0, 1)
    END_TYPE()

    // Bus priority acknowledge
    // Reset value: 0x00000000
    BEGIN_TYPE(BUS_PRIORITY_ACK_t, uint32_t)
        // Goes to 1 once all arbiters have registered the new global priority levels.  Arbiters update their local priority when servicing a new nonsequential access.  In normal circumstances this will happen almost immediately.
        ADD_BITFIELD_RO(BUS_PRIORITY_ACK, 0, 1)
    END_TYPE()

    // Enable the performance counters. If 0, the performance counters do not increment. This can be used to precisely start/stop event sampling around the profiled section of code. The performance counters are initially disabled, to save energy.
    // Reset value: 0x00000000
    BEGIN_TYPE(PERFCTR_EN_t, uint32_t)
        ADD_BITFIELD_RW(PERFCTR_EN, 0, 1)
    END_TYPE()

    // Bus fabric performance counter 0
    // Reset value: 0x00000000
    BEGIN_TYPE(PERFCTR0_t, uint32_t)
        // Busfabric saturating performance counter 0  Count some event signal from the busfabric arbiters, if PERFCTR_EN is set.  Write any value to clear. Select an event to count using PERFSEL0
        ADD_BITFIELD_RW(PERFCTR0, 0, 24)
    END_TYPE()

    // Bus fabric performance event select for PERFCTR0
    // Reset value: 0x0000001f
    BEGIN_TYPE(PERFSEL0_t, uint32_t)
        // Select an event for PERFCTR0. For each downstream port of the main crossbar, four events are available: ACCESS, an access took place; ACCESS_CONTESTED, an access took place that previously stalled due to contention from other masters; STALL_DOWNSTREAM, count cycles where any master stalled due to a stall on the downstream bus; STALL_UPSTREAM, count cycles where any master stalled for any reason, including contention from other masters.
        ADD_BITFIELD_RW(PERFSEL0, 0, 7)
    END_TYPE()

    static const uint32_t PERFSEL0_PERFSEL0__siob_proc1_stall_upstream = 0;
    static const uint32_t PERFSEL0_PERFSEL0__siob_proc1_stall_downstream = 1;
    static const uint32_t PERFSEL0_PERFSEL0__siob_proc1_access_contested = 2;
    static const uint32_t PERFSEL0_PERFSEL0__siob_proc1_access = 3;
    static const uint32_t PERFSEL0_PERFSEL0__siob_proc0_stall_upstream = 4;
    static const uint32_t PERFSEL0_PERFSEL0__siob_proc0_stall_downstream = 5;
    static const uint32_t PERFSEL0_PERFSEL0__siob_proc0_access_contested = 6;
    static const uint32_t PERFSEL0_PERFSEL0__siob_proc0_access = 7;
    static const uint32_t PERFSEL0_PERFSEL0__apb_stall_upstream = 8;
    static const uint32_t PERFSEL0_PERFSEL0__apb_stall_downstream = 9;
    static const uint32_t PERFSEL0_PERFSEL0__apb_access_contested = 10;
    static const uint32_t PERFSEL0_PERFSEL0__apb_access = 11;
    static const uint32_t PERFSEL0_PERFSEL0__fastperi_stall_upstream = 12;
    static const uint32_t PERFSEL0_PERFSEL0__fastperi_stall_downstream = 13;
    static const uint32_t PERFSEL0_PERFSEL0__fastperi_access_contested = 14;
    static const uint32_t PERFSEL0_PERFSEL0__fastperi_access = 15;
    static const uint32_t PERFSEL0_PERFSEL0__sram9_stall_upstream = 16;
    static const uint32_t PERFSEL0_PERFSEL0__sram9_stall_downstream = 17;
    static const uint32_t PERFSEL0_PERFSEL0__sram9_access_contested = 18;
    static const uint32_t PERFSEL0_PERFSEL0__sram9_access = 19;
    static const uint32_t PERFSEL0_PERFSEL0__sram8_stall_upstream = 20;
    static const uint32_t PERFSEL0_PERFSEL0__sram8_stall_downstream = 21;
    static const uint32_t PERFSEL0_PERFSEL0__sram8_access_contested = 22;
    static const uint32_t PERFSEL0_PERFSEL0__sram8_access = 23;
    static const uint32_t PERFSEL0_PERFSEL0__sram7_stall_upstream = 24;
    static const uint32_t PERFSEL0_PERFSEL0__sram7_stall_downstream = 25;
    static const uint32_t PERFSEL0_PERFSEL0__sram7_access_contested = 26;
    static const uint32_t PERFSEL0_PERFSEL0__sram7_access = 27;
    static const uint32_t PERFSEL0_PERFSEL0__sram6_stall_upstream = 28;
    static const uint32_t PERFSEL0_PERFSEL0__sram6_stall_downstream = 29;
    static const uint32_t PERFSEL0_PERFSEL0__sram6_access_contested = 30;
    static const uint32_t PERFSEL0_PERFSEL0__sram6_access = 31;
    static const uint32_t PERFSEL0_PERFSEL0__sram5_stall_upstream = 32;
    static const uint32_t PERFSEL0_PERFSEL0__sram5_stall_downstream = 33;
    static const uint32_t PERFSEL0_PERFSEL0__sram5_access_contested = 34;
    static const uint32_t PERFSEL0_PERFSEL0__sram5_access = 35;
    static const uint32_t PERFSEL0_PERFSEL0__sram4_stall_upstream = 36;
    static const uint32_t PERFSEL0_PERFSEL0__sram4_stall_downstream = 37;
    static const uint32_t PERFSEL0_PERFSEL0__sram4_access_contested = 38;
    static const uint32_t PERFSEL0_PERFSEL0__sram4_access = 39;
    static const uint32_t PERFSEL0_PERFSEL0__sram3_stall_upstream = 40;
    static const uint32_t PERFSEL0_PERFSEL0__sram3_stall_downstream = 41;
    static const uint32_t PERFSEL0_PERFSEL0__sram3_access_contested = 42;
    static const uint32_t PERFSEL0_PERFSEL0__sram3_access = 43;
    static const uint32_t PERFSEL0_PERFSEL0__sram2_stall_upstream = 44;
    static const uint32_t PERFSEL0_PERFSEL0__sram2_stall_downstream = 45;
    static const uint32_t PERFSEL0_PERFSEL0__sram2_access_contested = 46;
    static const uint32_t PERFSEL0_PERFSEL0__sram2_access = 47;
    static const uint32_t PERFSEL0_PERFSEL0__sram1_stall_upstream = 48;
    static const uint32_t PERFSEL0_PERFSEL0__sram1_stall_downstream = 49;
    static const uint32_t PERFSEL0_PERFSEL0__sram1_access_contested = 50;
    static const uint32_t PERFSEL0_PERFSEL0__sram1_access = 51;
    static const uint32_t PERFSEL0_PERFSEL0__sram0_stall_upstream = 52;
    static const uint32_t PERFSEL0_PERFSEL0__sram0_stall_downstream = 53;
    static const uint32_t PERFSEL0_PERFSEL0__sram0_access_contested = 54;
    static const uint32_t PERFSEL0_PERFSEL0__sram0_access = 55;
    static const uint32_t PERFSEL0_PERFSEL0__xip_main1_stall_upstream = 56;
    static const uint32_t PERFSEL0_PERFSEL0__xip_main1_stall_downstream = 57;
    static const uint32_t PERFSEL0_PERFSEL0__xip_main1_access_contested = 58;
    static const uint32_t PERFSEL0_PERFSEL0__xip_main1_access = 59;
    static const uint32_t PERFSEL0_PERFSEL0__xip_main0_stall_upstream = 60;
    static const uint32_t PERFSEL0_PERFSEL0__xip_main0_stall_downstream = 61;
    static const uint32_t PERFSEL0_PERFSEL0__xip_main0_access_contested = 62;
    static const uint32_t PERFSEL0_PERFSEL0__xip_main0_access = 63;
    static const uint32_t PERFSEL0_PERFSEL0__rom_stall_upstream = 64;
    static const uint32_t PERFSEL0_PERFSEL0__rom_stall_downstream = 65;
    static const uint32_t PERFSEL0_PERFSEL0__rom_access_contested = 66;
    static const uint32_t PERFSEL0_PERFSEL0__rom_access = 67;

    // Bus fabric performance counter 1
    // Reset value: 0x00000000
    BEGIN_TYPE(PERFCTR1_t, uint32_t)
        // Busfabric saturating performance counter 1  Count some event signal from the busfabric arbiters, if PERFCTR_EN is set.  Write any value to clear. Select an event to count using PERFSEL1
        ADD_BITFIELD_RW(PERFCTR1, 0, 24)
    END_TYPE()

    // Bus fabric performance event select for PERFCTR1
    // Reset value: 0x0000001f
    BEGIN_TYPE(PERFSEL1_t, uint32_t)
        // Select an event for PERFCTR1. For each downstream port of the main crossbar, four events are available: ACCESS, an access took place; ACCESS_CONTESTED, an access took place that previously stalled due to contention from other masters; STALL_DOWNSTREAM, count cycles where any master stalled due to a stall on the downstream bus; STALL_UPSTREAM, count cycles where any master stalled for any reason, including contention from other masters.
        ADD_BITFIELD_RW(PERFSEL1, 0, 7)
    END_TYPE()

    static const uint32_t PERFSEL1_PERFSEL1__siob_proc1_stall_upstream = 0;
    static const uint32_t PERFSEL1_PERFSEL1__siob_proc1_stall_downstream = 1;
    static const uint32_t PERFSEL1_PERFSEL1__siob_proc1_access_contested = 2;
    static const uint32_t PERFSEL1_PERFSEL1__siob_proc1_access = 3;
    static const uint32_t PERFSEL1_PERFSEL1__siob_proc0_stall_upstream = 4;
    static const uint32_t PERFSEL1_PERFSEL1__siob_proc0_stall_downstream = 5;
    static const uint32_t PERFSEL1_PERFSEL1__siob_proc0_access_contested = 6;
    static const uint32_t PERFSEL1_PERFSEL1__siob_proc0_access = 7;
    static const uint32_t PERFSEL1_PERFSEL1__apb_stall_upstream = 8;
    static const uint32_t PERFSEL1_PERFSEL1__apb_stall_downstream = 9;
    static const uint32_t PERFSEL1_PERFSEL1__apb_access_contested = 10;
    static const uint32_t PERFSEL1_PERFSEL1__apb_access = 11;
    static const uint32_t PERFSEL1_PERFSEL1__fastperi_stall_upstream = 12;
    static const uint32_t PERFSEL1_PERFSEL1__fastperi_stall_downstream = 13;
    static const uint32_t PERFSEL1_PERFSEL1__fastperi_access_contested = 14;
    static const uint32_t PERFSEL1_PERFSEL1__fastperi_access = 15;
    static const uint32_t PERFSEL1_PERFSEL1__sram9_stall_upstream = 16;
    static const uint32_t PERFSEL1_PERFSEL1__sram9_stall_downstream = 17;
    static const uint32_t PERFSEL1_PERFSEL1__sram9_access_contested = 18;
    static const uint32_t PERFSEL1_PERFSEL1__sram9_access = 19;
    static const uint32_t PERFSEL1_PERFSEL1__sram8_stall_upstream = 20;
    static const uint32_t PERFSEL1_PERFSEL1__sram8_stall_downstream = 21;
    static const uint32_t PERFSEL1_PERFSEL1__sram8_access_contested = 22;
    static const uint32_t PERFSEL1_PERFSEL1__sram8_access = 23;
    static const uint32_t PERFSEL1_PERFSEL1__sram7_stall_upstream = 24;
    static const uint32_t PERFSEL1_PERFSEL1__sram7_stall_downstream = 25;
    static const uint32_t PERFSEL1_PERFSEL1__sram7_access_contested = 26;
    static const uint32_t PERFSEL1_PERFSEL1__sram7_access = 27;
    static const uint32_t PERFSEL1_PERFSEL1__sram6_stall_upstream = 28;
    static const uint32_t PERFSEL1_PERFSEL1__sram6_stall_downstream = 29;
    static const uint32_t PERFSEL1_PERFSEL1__sram6_access_contested = 30;
    static const uint32_t PERFSEL1_PERFSEL1__sram6_access = 31;
    static const uint32_t PERFSEL1_PERFSEL1__sram5_stall_upstream = 32;
    static const uint32_t PERFSEL1_PERFSEL1__sram5_stall_downstream = 33;
    static const uint32_t PERFSEL1_PERFSEL1__sram5_access_contested = 34;
    static const uint32_t PERFSEL1_PERFSEL1__sram5_access = 35;
    static const uint32_t PERFSEL1_PERFSEL1__sram4_stall_upstream = 36;
    static const uint32_t PERFSEL1_PERFSEL1__sram4_stall_downstream = 37;
    static const uint32_t PERFSEL1_PERFSEL1__sram4_access_contested = 38;
    static const uint32_t PERFSEL1_PERFSEL1__sram4_access = 39;
    static const uint32_t PERFSEL1_PERFSEL1__sram3_stall_upstream = 40;
    static const uint32_t PERFSEL1_PERFSEL1__sram3_stall_downstream = 41;
    static const uint32_t PERFSEL1_PERFSEL1__sram3_access_contested = 42;
    static const uint32_t PERFSEL1_PERFSEL1__sram3_access = 43;
    static const uint32_t PERFSEL1_PERFSEL1__sram2_stall_upstream = 44;
    static const uint32_t PERFSEL1_PERFSEL1__sram2_stall_downstream = 45;
    static const uint32_t PERFSEL1_PERFSEL1__sram2_access_contested = 46;
    static const uint32_t PERFSEL1_PERFSEL1__sram2_access = 47;
    static const uint32_t PERFSEL1_PERFSEL1__sram1_stall_upstream = 48;
    static const uint32_t PERFSEL1_PERFSEL1__sram1_stall_downstream = 49;
    static const uint32_t PERFSEL1_PERFSEL1__sram1_access_contested = 50;
    static const uint32_t PERFSEL1_PERFSEL1__sram1_access = 51;
    static const uint32_t PERFSEL1_PERFSEL1__sram0_stall_upstream = 52;
    static const uint32_t PERFSEL1_PERFSEL1__sram0_stall_downstream = 53;
    static const uint32_t PERFSEL1_PERFSEL1__sram0_access_contested = 54;
    static const uint32_t PERFSEL1_PERFSEL1__sram0_access = 55;
    static const uint32_t PERFSEL1_PERFSEL1__xip_main1_stall_upstream = 56;
    static const uint32_t PERFSEL1_PERFSEL1__xip_main1_stall_downstream = 57;
    static const uint32_t PERFSEL1_PERFSEL1__xip_main1_access_contested = 58;
    static const uint32_t PERFSEL1_PERFSEL1__xip_main1_access = 59;
    static const uint32_t PERFSEL1_PERFSEL1__xip_main0_stall_upstream = 60;
    static const uint32_t PERFSEL1_PERFSEL1__xip_main0_stall_downstream = 61;
    static const uint32_t PERFSEL1_PERFSEL1__xip_main0_access_contested = 62;
    static const uint32_t PERFSEL1_PERFSEL1__xip_main0_access = 63;
    static const uint32_t PERFSEL1_PERFSEL1__rom_stall_upstream = 64;
    static const uint32_t PERFSEL1_PERFSEL1__rom_stall_downstream = 65;
    static const uint32_t PERFSEL1_PERFSEL1__rom_access_contested = 66;
    static const uint32_t PERFSEL1_PERFSEL1__rom_access = 67;

    // Bus fabric performance counter 2
    // Reset value: 0x00000000
    BEGIN_TYPE(PERFCTR2_t, uint32_t)
        // Busfabric saturating performance counter 2  Count some event signal from the busfabric arbiters, if PERFCTR_EN is set.  Write any value to clear. Select an event to count using PERFSEL2
        ADD_BITFIELD_RW(PERFCTR2, 0, 24)
    END_TYPE()

    // Bus fabric performance event select for PERFCTR2
    // Reset value: 0x0000001f
    BEGIN_TYPE(PERFSEL2_t, uint32_t)
        // Select an event for PERFCTR2. For each downstream port of the main crossbar, four events are available: ACCESS, an access took place; ACCESS_CONTESTED, an access took place that previously stalled due to contention from other masters; STALL_DOWNSTREAM, count cycles where any master stalled due to a stall on the downstream bus; STALL_UPSTREAM, count cycles where any master stalled for any reason, including contention from other masters.
        ADD_BITFIELD_RW(PERFSEL2, 0, 7)
    END_TYPE()

    static const uint32_t PERFSEL2_PERFSEL2__siob_proc1_stall_upstream = 0;
    static const uint32_t PERFSEL2_PERFSEL2__siob_proc1_stall_downstream = 1;
    static const uint32_t PERFSEL2_PERFSEL2__siob_proc1_access_contested = 2;
    static const uint32_t PERFSEL2_PERFSEL2__siob_proc1_access = 3;
    static const uint32_t PERFSEL2_PERFSEL2__siob_proc0_stall_upstream = 4;
    static const uint32_t PERFSEL2_PERFSEL2__siob_proc0_stall_downstream = 5;
    static const uint32_t PERFSEL2_PERFSEL2__siob_proc0_access_contested = 6;
    static const uint32_t PERFSEL2_PERFSEL2__siob_proc0_access = 7;
    static const uint32_t PERFSEL2_PERFSEL2__apb_stall_upstream = 8;
    static const uint32_t PERFSEL2_PERFSEL2__apb_stall_downstream = 9;
    static const uint32_t PERFSEL2_PERFSEL2__apb_access_contested = 10;
    static const uint32_t PERFSEL2_PERFSEL2__apb_access = 11;
    static const uint32_t PERFSEL2_PERFSEL2__fastperi_stall_upstream = 12;
    static const uint32_t PERFSEL2_PERFSEL2__fastperi_stall_downstream = 13;
    static const uint32_t PERFSEL2_PERFSEL2__fastperi_access_contested = 14;
    static const uint32_t PERFSEL2_PERFSEL2__fastperi_access = 15;
    static const uint32_t PERFSEL2_PERFSEL2__sram9_stall_upstream = 16;
    static const uint32_t PERFSEL2_PERFSEL2__sram9_stall_downstream = 17;
    static const uint32_t PERFSEL2_PERFSEL2__sram9_access_contested = 18;
    static const uint32_t PERFSEL2_PERFSEL2__sram9_access = 19;
    static const uint32_t PERFSEL2_PERFSEL2__sram8_stall_upstream = 20;
    static const uint32_t PERFSEL2_PERFSEL2__sram8_stall_downstream = 21;
    static const uint32_t PERFSEL2_PERFSEL2__sram8_access_contested = 22;
    static const uint32_t PERFSEL2_PERFSEL2__sram8_access = 23;
    static const uint32_t PERFSEL2_PERFSEL2__sram7_stall_upstream = 24;
    static const uint32_t PERFSEL2_PERFSEL2__sram7_stall_downstream = 25;
    static const uint32_t PERFSEL2_PERFSEL2__sram7_access_contested = 26;
    static const uint32_t PERFSEL2_PERFSEL2__sram7_access = 27;
    static const uint32_t PERFSEL2_PERFSEL2__sram6_stall_upstream = 28;
    static const uint32_t PERFSEL2_PERFSEL2__sram6_stall_downstream = 29;
    static const uint32_t PERFSEL2_PERFSEL2__sram6_access_contested = 30;
    static const uint32_t PERFSEL2_PERFSEL2__sram6_access = 31;
    static const uint32_t PERFSEL2_PERFSEL2__sram5_stall_upstream = 32;
    static const uint32_t PERFSEL2_PERFSEL2__sram5_stall_downstream = 33;
    static const uint32_t PERFSEL2_PERFSEL2__sram5_access_contested = 34;
    static const uint32_t PERFSEL2_PERFSEL2__sram5_access = 35;
    static const uint32_t PERFSEL2_PERFSEL2__sram4_stall_upstream = 36;
    static const uint32_t PERFSEL2_PERFSEL2__sram4_stall_downstream = 37;
    static const uint32_t PERFSEL2_PERFSEL2__sram4_access_contested = 38;
    static const uint32_t PERFSEL2_PERFSEL2__sram4_access = 39;
    static const uint32_t PERFSEL2_PERFSEL2__sram3_stall_upstream = 40;
    static const uint32_t PERFSEL2_PERFSEL2__sram3_stall_downstream = 41;
    static const uint32_t PERFSEL2_PERFSEL2__sram3_access_contested = 42;
    static const uint32_t PERFSEL2_PERFSEL2__sram3_access = 43;
    static const uint32_t PERFSEL2_PERFSEL2__sram2_stall_upstream = 44;
    static const uint32_t PERFSEL2_PERFSEL2__sram2_stall_downstream = 45;
    static const uint32_t PERFSEL2_PERFSEL2__sram2_access_contested = 46;
    static const uint32_t PERFSEL2_PERFSEL2__sram2_access = 47;
    static const uint32_t PERFSEL2_PERFSEL2__sram1_stall_upstream = 48;
    static const uint32_t PERFSEL2_PERFSEL2__sram1_stall_downstream = 49;
    static const uint32_t PERFSEL2_PERFSEL2__sram1_access_contested = 50;
    static const uint32_t PERFSEL2_PERFSEL2__sram1_access = 51;
    static const uint32_t PERFSEL2_PERFSEL2__sram0_stall_upstream = 52;
    static const uint32_t PERFSEL2_PERFSEL2__sram0_stall_downstream = 53;
    static const uint32_t PERFSEL2_PERFSEL2__sram0_access_contested = 54;
    static const uint32_t PERFSEL2_PERFSEL2__sram0_access = 55;
    static const uint32_t PERFSEL2_PERFSEL2__xip_main1_stall_upstream = 56;
    static const uint32_t PERFSEL2_PERFSEL2__xip_main1_stall_downstream = 57;
    static const uint32_t PERFSEL2_PERFSEL2__xip_main1_access_contested = 58;
    static const uint32_t PERFSEL2_PERFSEL2__xip_main1_access = 59;
    static const uint32_t PERFSEL2_PERFSEL2__xip_main0_stall_upstream = 60;
    static const uint32_t PERFSEL2_PERFSEL2__xip_main0_stall_downstream = 61;
    static const uint32_t PERFSEL2_PERFSEL2__xip_main0_access_contested = 62;
    static const uint32_t PERFSEL2_PERFSEL2__xip_main0_access = 63;
    static const uint32_t PERFSEL2_PERFSEL2__rom_stall_upstream = 64;
    static const uint32_t PERFSEL2_PERFSEL2__rom_stall_downstream = 65;
    static const uint32_t PERFSEL2_PERFSEL2__rom_access_contested = 66;
    static const uint32_t PERFSEL2_PERFSEL2__rom_access = 67;

    // Bus fabric performance counter 3
    // Reset value: 0x00000000
    BEGIN_TYPE(PERFCTR3_t, uint32_t)
        // Busfabric saturating performance counter 3  Count some event signal from the busfabric arbiters, if PERFCTR_EN is set.  Write any value to clear. Select an event to count using PERFSEL3
        ADD_BITFIELD_RW(PERFCTR3, 0, 24)
    END_TYPE()

    // Bus fabric performance event select for PERFCTR3
    // Reset value: 0x0000001f
    BEGIN_TYPE(PERFSEL3_t, uint32_t)
        // Select an event for PERFCTR3. For each downstream port of the main crossbar, four events are available: ACCESS, an access took place; ACCESS_CONTESTED, an access took place that previously stalled due to contention from other masters; STALL_DOWNSTREAM, count cycles where any master stalled due to a stall on the downstream bus; STALL_UPSTREAM, count cycles where any master stalled for any reason, including contention from other masters.
        ADD_BITFIELD_RW(PERFSEL3, 0, 7)
    END_TYPE()

    static const uint32_t PERFSEL3_PERFSEL3__siob_proc1_stall_upstream = 0;
    static const uint32_t PERFSEL3_PERFSEL3__siob_proc1_stall_downstream = 1;
    static const uint32_t PERFSEL3_PERFSEL3__siob_proc1_access_contested = 2;
    static const uint32_t PERFSEL3_PERFSEL3__siob_proc1_access = 3;
    static const uint32_t PERFSEL3_PERFSEL3__siob_proc0_stall_upstream = 4;
    static const uint32_t PERFSEL3_PERFSEL3__siob_proc0_stall_downstream = 5;
    static const uint32_t PERFSEL3_PERFSEL3__siob_proc0_access_contested = 6;
    static const uint32_t PERFSEL3_PERFSEL3__siob_proc0_access = 7;
    static const uint32_t PERFSEL3_PERFSEL3__apb_stall_upstream = 8;
    static const uint32_t PERFSEL3_PERFSEL3__apb_stall_downstream = 9;
    static const uint32_t PERFSEL3_PERFSEL3__apb_access_contested = 10;
    static const uint32_t PERFSEL3_PERFSEL3__apb_access = 11;
    static const uint32_t PERFSEL3_PERFSEL3__fastperi_stall_upstream = 12;
    static const uint32_t PERFSEL3_PERFSEL3__fastperi_stall_downstream = 13;
    static const uint32_t PERFSEL3_PERFSEL3__fastperi_access_contested = 14;
    static const uint32_t PERFSEL3_PERFSEL3__fastperi_access = 15;
    static const uint32_t PERFSEL3_PERFSEL3__sram9_stall_upstream = 16;
    static const uint32_t PERFSEL3_PERFSEL3__sram9_stall_downstream = 17;
    static const uint32_t PERFSEL3_PERFSEL3__sram9_access_contested = 18;
    static const uint32_t PERFSEL3_PERFSEL3__sram9_access = 19;
    static const uint32_t PERFSEL3_PERFSEL3__sram8_stall_upstream = 20;
    static const uint32_t PERFSEL3_PERFSEL3__sram8_stall_downstream = 21;
    static const uint32_t PERFSEL3_PERFSEL3__sram8_access_contested = 22;
    static const uint32_t PERFSEL3_PERFSEL3__sram8_access = 23;
    static const uint32_t PERFSEL3_PERFSEL3__sram7_stall_upstream = 24;
    static const uint32_t PERFSEL3_PERFSEL3__sram7_stall_downstream = 25;
    static const uint32_t PERFSEL3_PERFSEL3__sram7_access_contested = 26;
    static const uint32_t PERFSEL3_PERFSEL3__sram7_access = 27;
    static const uint32_t PERFSEL3_PERFSEL3__sram6_stall_upstream = 28;
    static const uint32_t PERFSEL3_PERFSEL3__sram6_stall_downstream = 29;
    static const uint32_t PERFSEL3_PERFSEL3__sram6_access_contested = 30;
    static const uint32_t PERFSEL3_PERFSEL3__sram6_access = 31;
    static const uint32_t PERFSEL3_PERFSEL3__sram5_stall_upstream = 32;
    static const uint32_t PERFSEL3_PERFSEL3__sram5_stall_downstream = 33;
    static const uint32_t PERFSEL3_PERFSEL3__sram5_access_contested = 34;
    static const uint32_t PERFSEL3_PERFSEL3__sram5_access = 35;
    static const uint32_t PERFSEL3_PERFSEL3__sram4_stall_upstream = 36;
    static const uint32_t PERFSEL3_PERFSEL3__sram4_stall_downstream = 37;
    static const uint32_t PERFSEL3_PERFSEL3__sram4_access_contested = 38;
    static const uint32_t PERFSEL3_PERFSEL3__sram4_access = 39;
    static const uint32_t PERFSEL3_PERFSEL3__sram3_stall_upstream = 40;
    static const uint32_t PERFSEL3_PERFSEL3__sram3_stall_downstream = 41;
    static const uint32_t PERFSEL3_PERFSEL3__sram3_access_contested = 42;
    static const uint32_t PERFSEL3_PERFSEL3__sram3_access = 43;
    static const uint32_t PERFSEL3_PERFSEL3__sram2_stall_upstream = 44;
    static const uint32_t PERFSEL3_PERFSEL3__sram2_stall_downstream = 45;
    static const uint32_t PERFSEL3_PERFSEL3__sram2_access_contested = 46;
    static const uint32_t PERFSEL3_PERFSEL3__sram2_access = 47;
    static const uint32_t PERFSEL3_PERFSEL3__sram1_stall_upstream = 48;
    static const uint32_t PERFSEL3_PERFSEL3__sram1_stall_downstream = 49;
    static const uint32_t PERFSEL3_PERFSEL3__sram1_access_contested = 50;
    static const uint32_t PERFSEL3_PERFSEL3__sram1_access = 51;
    static const uint32_t PERFSEL3_PERFSEL3__sram0_stall_upstream = 52;
    static const uint32_t PERFSEL3_PERFSEL3__sram0_stall_downstream = 53;
    static const uint32_t PERFSEL3_PERFSEL3__sram0_access_contested = 54;
    static const uint32_t PERFSEL3_PERFSEL3__sram0_access = 55;
    static const uint32_t PERFSEL3_PERFSEL3__xip_main1_stall_upstream = 56;
    static const uint32_t PERFSEL3_PERFSEL3__xip_main1_stall_downstream = 57;
    static const uint32_t PERFSEL3_PERFSEL3__xip_main1_access_contested = 58;
    static const uint32_t PERFSEL3_PERFSEL3__xip_main1_access = 59;
    static const uint32_t PERFSEL3_PERFSEL3__xip_main0_stall_upstream = 60;
    static const uint32_t PERFSEL3_PERFSEL3__xip_main0_stall_downstream = 61;
    static const uint32_t PERFSEL3_PERFSEL3__xip_main0_access_contested = 62;
    static const uint32_t PERFSEL3_PERFSEL3__xip_main0_access = 63;
    static const uint32_t PERFSEL3_PERFSEL3__rom_stall_upstream = 64;
    static const uint32_t PERFSEL3_PERFSEL3__rom_stall_downstream = 65;
    static const uint32_t PERFSEL3_PERFSEL3__rom_access_contested = 66;
    static const uint32_t PERFSEL3_PERFSEL3__rom_access = 67;

    struct BUSCTRL_t {
        BUS_PRIORITY_t                BUS_PRIORITY;
        BUS_PRIORITY_ACK_t            BUS_PRIORITY_ACK;
        PERFCTR_EN_t                  PERFCTR_EN;
        PERFCTR0_t                    PERFCTR0;
        PERFSEL0_t                    PERFSEL0;
        PERFCTR1_t                    PERFCTR1;
        PERFSEL1_t                    PERFSEL1;
        PERFCTR2_t                    PERFCTR2;
        PERFSEL2_t                    PERFSEL2;
        PERFCTR3_t                    PERFCTR3;
        PERFSEL3_t                    PERFSEL3;
    };

    static BUSCTRL_t & BUSCTRL = (*(BUSCTRL_t *)0x40068000);
    static BUSCTRL_t & BUSCTRL_XOR = (*(BUSCTRL_t *)0x40069000);
    static BUSCTRL_t & BUSCTRL_SET = (*(BUSCTRL_t *)0x4006a000);
    static BUSCTRL_t & BUSCTRL_CLR = (*(BUSCTRL_t *)0x4006b000);

} // _BUSCTRL_

// Single-cycle IO block  Provides core-local and inter-core hardware for the two processors, with single-cycle access.
namespace _SIO_  {

    // Processor core identifier
    BEGIN_TYPE(CPUID_t, uint32_t)
        // Value is 0 when read from processor core 0, and 1 when read from processor core 1.
        ADD_BITFIELD_RO(CPUID, 0, 32)
    END_TYPE()

    // Input value for GPIO0...31. In the Non-secure SIO, Secure-only GPIOs (as per ACCESSCTRL) appear as zero.
    // Reset value: 0x00000000
    BEGIN_TYPE(GPIO_IN_t, uint32_t)
        ADD_BITFIELD_RO(GPIO_IN, 0, 32)
    END_TYPE()

    // Input value on GPIO32...47, QSPI IOs and USB pins In the Non-secure SIO, Secure-only GPIOs (as per ACCESSCTRL) appear as zero.
    // Reset value: 0x00000000
    BEGIN_TYPE(GPIO_HI_IN_t, uint32_t)
        // Input value on QSPI SD0 (MOSI), SD1 (MISO), SD2 and SD3 pins
        ADD_BITFIELD_RO(QSPI_SD, 28, 4)
        // Input value on QSPI CSn pin
        ADD_BITFIELD_RO(QSPI_CSN, 27, 1)
        // Input value on QSPI SCK pin
        ADD_BITFIELD_RO(QSPI_SCK, 26, 1)
        // Input value on USB D- pin
        ADD_BITFIELD_RO(USB_DM, 25, 1)
        // Input value on USB D+ pin
        ADD_BITFIELD_RO(USB_DP, 24, 1)
        // Input value on GPIO32...47
        ADD_BITFIELD_RO(GPIO, 0, 16)
    END_TYPE()

    // GPIO0...31 output value
    // Reset value: 0x00000000
    BEGIN_TYPE(GPIO_OUT_t, uint32_t)
        // Set output level (1/0 -> high/low) for GPIO0...31. Reading back gives the last value written, NOT the input value from the pins. If core 0 and core 1 both write to GPIO_OUT simultaneously (or to a SET/CLR/XOR alias), the result is as though the write from core 0 took place first, and the write from core 1 was then applied to that intermediate result. In the Non-secure SIO, Secure-only GPIOs (as per ACCESSCTRL) ignore writes, and their output status reads back as zero. This is also true for SET/CLR/XOR aliases of this register.
        ADD_BITFIELD_RW(GPIO_OUT, 0, 32)
    END_TYPE()

    // Output value for GPIO32...47, QSPI IOs and USB pins. Write to set output level (1/0 -> high/low). Reading back gives the last value written, NOT the input value from the pins. If core 0 and core 1 both write to GPIO_HI_OUT simultaneously (or to a SET/CLR/XOR alias), the result is as though the write from core 0 took place first, and the write from core 1 was then applied to that intermediate result. In the Non-secure SIO, Secure-only GPIOs (as per ACCESSCTRL) ignore writes, and their output status reads back as zero. This is also true for SET/CLR/XOR aliases of this register.
    // Reset value: 0x00000000
    BEGIN_TYPE(GPIO_HI_OUT_t, uint32_t)
        // Output value for QSPI SD0 (MOSI), SD1 (MISO), SD2 and SD3 pins
        ADD_BITFIELD_RW(QSPI_SD, 28, 4)
        // Output value for QSPI CSn pin
        ADD_BITFIELD_RW(QSPI_CSN, 27, 1)
        // Output value for QSPI SCK pin
        ADD_BITFIELD_RW(QSPI_SCK, 26, 1)
        // Output value for USB D- pin
        ADD_BITFIELD_RW(USB_DM, 25, 1)
        // Output value for USB D+ pin
        ADD_BITFIELD_RW(USB_DP, 24, 1)
        // Output value for GPIO32...47
        ADD_BITFIELD_RW(GPIO, 0, 16)
    END_TYPE()

    // GPIO0...31 output value set
    // Reset value: 0x00000000
    BEGIN_TYPE(GPIO_OUT_SET_t, uint32_t)
        // Perform an atomic bit-set on GPIO_OUT, i.e. `GPIO_OUT |= wdata`
        ADD_BITFIELD_WO(GPIO_OUT_SET, 0, 32)
    END_TYPE()

    // Output value set for GPIO32..47, QSPI IOs and USB pins.  Perform an atomic bit-set on GPIO_HI_OUT, i.e. `GPIO_HI_OUT |= wdata`
    // Reset value: 0x00000000
    BEGIN_TYPE(GPIO_HI_OUT_SET_t, uint32_t)
        ADD_BITFIELD_WO(QSPI_SD, 28, 4)
        ADD_BITFIELD_WO(QSPI_CSN, 27, 1)
        ADD_BITFIELD_WO(QSPI_SCK, 26, 1)
        ADD_BITFIELD_WO(USB_DM, 25, 1)
        ADD_BITFIELD_WO(USB_DP, 24, 1)
        ADD_BITFIELD_WO(GPIO, 0, 16)
    END_TYPE()

    // GPIO0...31 output value clear
    // Reset value: 0x00000000
    BEGIN_TYPE(GPIO_OUT_CLR_t, uint32_t)
        // Perform an atomic bit-clear on GPIO_OUT, i.e. `GPIO_OUT &= ~wdata`
        ADD_BITFIELD_WO(GPIO_OUT_CLR, 0, 32)
    END_TYPE()

    // Output value clear for GPIO32..47, QSPI IOs and USB pins.  Perform an atomic bit-clear on GPIO_HI_OUT, i.e. `GPIO_HI_OUT &= ~wdata`
    // Reset value: 0x00000000
    BEGIN_TYPE(GPIO_HI_OUT_CLR_t, uint32_t)
        ADD_BITFIELD_WO(QSPI_SD, 28, 4)
        ADD_BITFIELD_WO(QSPI_CSN, 27, 1)
        ADD_BITFIELD_WO(QSPI_SCK, 26, 1)
        ADD_BITFIELD_WO(USB_DM, 25, 1)
        ADD_BITFIELD_WO(USB_DP, 24, 1)
        ADD_BITFIELD_WO(GPIO, 0, 16)
    END_TYPE()

    // GPIO0...31 output value XOR
    // Reset value: 0x00000000
    BEGIN_TYPE(GPIO_OUT_XOR_t, uint32_t)
        // Perform an atomic bitwise XOR on GPIO_OUT, i.e. `GPIO_OUT ^= wdata`
        ADD_BITFIELD_WO(GPIO_OUT_XOR, 0, 32)
    END_TYPE()

    // Output value XOR for GPIO32..47, QSPI IOs and USB pins.  Perform an atomic bitwise XOR on GPIO_HI_OUT, i.e. `GPIO_HI_OUT ^= wdata`
    // Reset value: 0x00000000
    BEGIN_TYPE(GPIO_HI_OUT_XOR_t, uint32_t)
        ADD_BITFIELD_WO(QSPI_SD, 28, 4)
        ADD_BITFIELD_WO(QSPI_CSN, 27, 1)
        ADD_BITFIELD_WO(QSPI_SCK, 26, 1)
        ADD_BITFIELD_WO(USB_DM, 25, 1)
        ADD_BITFIELD_WO(USB_DP, 24, 1)
        ADD_BITFIELD_WO(GPIO, 0, 16)
    END_TYPE()

    // GPIO0...31 output enable
    // Reset value: 0x00000000
    BEGIN_TYPE(GPIO_OE_t, uint32_t)
        // Set output enable (1/0 -> output/input) for GPIO0...31. Reading back gives the last value written. If core 0 and core 1 both write to GPIO_OE simultaneously (or to a SET/CLR/XOR alias), the result is as though the write from core 0 took place first, and the write from core 1 was then applied to that intermediate result. In the Non-secure SIO, Secure-only GPIOs (as per ACCESSCTRL) ignore writes, and their output status reads back as zero. This is also true for SET/CLR/XOR aliases of this register.
        ADD_BITFIELD_RW(GPIO_OE, 0, 32)
    END_TYPE()

    // Output enable value for GPIO32...47, QSPI IOs and USB pins. Write output enable (1/0 -> output/input). Reading back gives the last value written. If core 0 and core 1 both write to GPIO_HI_OE simultaneously (or to a SET/CLR/XOR alias), the result is as though the write from core 0 took place first, and the write from core 1 was then applied to that intermediate result. In the Non-secure SIO, Secure-only GPIOs (as per ACCESSCTRL) ignore writes, and their output status reads back as zero. This is also true for SET/CLR/XOR aliases of this register.
    // Reset value: 0x00000000
    BEGIN_TYPE(GPIO_HI_OE_t, uint32_t)
        // Output enable value for QSPI SD0 (MOSI), SD1 (MISO), SD2 and SD3 pins
        ADD_BITFIELD_RW(QSPI_SD, 28, 4)
        // Output enable value for QSPI CSn pin
        ADD_BITFIELD_RW(QSPI_CSN, 27, 1)
        // Output enable value for QSPI SCK pin
        ADD_BITFIELD_RW(QSPI_SCK, 26, 1)
        // Output enable value for USB D- pin
        ADD_BITFIELD_RW(USB_DM, 25, 1)
        // Output enable value for USB D+ pin
        ADD_BITFIELD_RW(USB_DP, 24, 1)
        // Output enable value for GPIO32...47
        ADD_BITFIELD_RW(GPIO, 0, 16)
    END_TYPE()

    // GPIO0...31 output enable set
    // Reset value: 0x00000000
    BEGIN_TYPE(GPIO_OE_SET_t, uint32_t)
        // Perform an atomic bit-set on GPIO_OE, i.e. `GPIO_OE |= wdata`
        ADD_BITFIELD_WO(GPIO_OE_SET, 0, 32)
    END_TYPE()

    // Output enable set for GPIO32...47, QSPI IOs and USB pins.  Perform an atomic bit-set on GPIO_HI_OE, i.e. `GPIO_HI_OE |= wdata`
    // Reset value: 0x00000000
    BEGIN_TYPE(GPIO_HI_OE_SET_t, uint32_t)
        ADD_BITFIELD_WO(QSPI_SD, 28, 4)
        ADD_BITFIELD_WO(QSPI_CSN, 27, 1)
        ADD_BITFIELD_WO(QSPI_SCK, 26, 1)
        ADD_BITFIELD_WO(USB_DM, 25, 1)
        ADD_BITFIELD_WO(USB_DP, 24, 1)
        ADD_BITFIELD_WO(GPIO, 0, 16)
    END_TYPE()

    // GPIO0...31 output enable clear
    // Reset value: 0x00000000
    BEGIN_TYPE(GPIO_OE_CLR_t, uint32_t)
        // Perform an atomic bit-clear on GPIO_OE, i.e. `GPIO_OE &= ~wdata`
        ADD_BITFIELD_WO(GPIO_OE_CLR, 0, 32)
    END_TYPE()

    // Output enable clear for GPIO32...47, QSPI IOs and USB pins.  Perform an atomic bit-clear on GPIO_HI_OE, i.e. `GPIO_HI_OE &= ~wdata`
    // Reset value: 0x00000000
    BEGIN_TYPE(GPIO_HI_OE_CLR_t, uint32_t)
        ADD_BITFIELD_WO(QSPI_SD, 28, 4)
        ADD_BITFIELD_WO(QSPI_CSN, 27, 1)
        ADD_BITFIELD_WO(QSPI_SCK, 26, 1)
        ADD_BITFIELD_WO(USB_DM, 25, 1)
        ADD_BITFIELD_WO(USB_DP, 24, 1)
        ADD_BITFIELD_WO(GPIO, 0, 16)
    END_TYPE()

    // GPIO0...31 output enable XOR
    // Reset value: 0x00000000
    BEGIN_TYPE(GPIO_OE_XOR_t, uint32_t)
        // Perform an atomic bitwise XOR on GPIO_OE, i.e. `GPIO_OE ^= wdata`
        ADD_BITFIELD_WO(GPIO_OE_XOR, 0, 32)
    END_TYPE()

    // Output enable XOR for GPIO32...47, QSPI IOs and USB pins.  Perform an atomic bitwise XOR on GPIO_HI_OE, i.e. `GPIO_HI_OE ^= wdata`
    // Reset value: 0x00000000
    BEGIN_TYPE(GPIO_HI_OE_XOR_t, uint32_t)
        ADD_BITFIELD_WO(QSPI_SD, 28, 4)
        ADD_BITFIELD_WO(QSPI_CSN, 27, 1)
        ADD_BITFIELD_WO(QSPI_SCK, 26, 1)
        ADD_BITFIELD_WO(USB_DM, 25, 1)
        ADD_BITFIELD_WO(USB_DP, 24, 1)
        ADD_BITFIELD_WO(GPIO, 0, 16)
    END_TYPE()

    // Status register for inter-core FIFOs (mailboxes).  There is one FIFO in the core 0 -> core 1 direction, and one core 1 -> core 0. Both are 32 bits wide and 8 words deep.  Core 0 can see the read side of the 1->0 FIFO (RX), and the write side of 0->1 FIFO (TX).  Core 1 can see the read side of the 0->1 FIFO (RX), and the write side of 1->0 FIFO (TX).  The SIO IRQ for each core is the logical OR of the VLD, WOF and ROE fields of its FIFO_ST register.
    // Reset value: 0x00000002
    BEGIN_TYPE(FIFO_ST_t, uint32_t)
        // Sticky flag indicating the RX FIFO was read when empty. This read was ignored by the FIFO.
        ADD_BITFIELD_RW(ROE, 3, 1)
        // Sticky flag indicating the TX FIFO was written when full. This write was ignored by the FIFO.
        ADD_BITFIELD_RW(WOF, 2, 1)
        // Value is 1 if this core's TX FIFO is not full (i.e. if FIFO_WR is ready for more data)
        ADD_BITFIELD_RO(RDY, 1, 1)
        // Value is 1 if this core's RX FIFO is not empty (i.e. if FIFO_RD is valid)
        ADD_BITFIELD_RO(VLD, 0, 1)
    END_TYPE()

    // Write access to this core's TX FIFO
    // Reset value: 0x00000000
    BEGIN_TYPE(FIFO_WR_t, uint32_t)
        ADD_BITFIELD_WO(FIFO_WR, 0, 32)
    END_TYPE()

    // Read access to this core's RX FIFO
    BEGIN_TYPE(FIFO_RD_t, uint32_t)
        ADD_BITFIELD_RO(FIFO_RD, 0, 32)
    END_TYPE()

    // Spinlock state  A bitmap containing the state of all 32 spinlocks (1=locked).  Mainly intended for debugging.
    // Reset value: 0x00000000
    BEGIN_TYPE(SPINLOCK_ST_t, uint32_t)
        ADD_BITFIELD_RO(SPINLOCK_ST, 0, 32)
    END_TYPE()

    // Read/write access to accumulator 0
    // Reset value: 0x00000000
    BEGIN_TYPE(INTERP0_ACCUM0_t, uint32_t)
        ADD_BITFIELD_RW(INTERP0_ACCUM0, 0, 32)
    END_TYPE()

    // Read/write access to accumulator 1
    // Reset value: 0x00000000
    BEGIN_TYPE(INTERP0_ACCUM1_t, uint32_t)
        ADD_BITFIELD_RW(INTERP0_ACCUM1, 0, 32)
    END_TYPE()

    // Read/write access to BASE0 register.
    // Reset value: 0x00000000
    BEGIN_TYPE(INTERP0_BASE0_t, uint32_t)
        ADD_BITFIELD_RW(INTERP0_BASE0, 0, 32)
    END_TYPE()

    // Read/write access to BASE1 register.
    // Reset value: 0x00000000
    BEGIN_TYPE(INTERP0_BASE1_t, uint32_t)
        ADD_BITFIELD_RW(INTERP0_BASE1, 0, 32)
    END_TYPE()

    // Read/write access to BASE2 register.
    // Reset value: 0x00000000
    BEGIN_TYPE(INTERP0_BASE2_t, uint32_t)
        ADD_BITFIELD_RW(INTERP0_BASE2, 0, 32)
    END_TYPE()

    // Read LANE0 result, and simultaneously write lane results to both accumulators (POP).
    // Reset value: 0x00000000
    BEGIN_TYPE(INTERP0_POP_LANE0_t, uint32_t)
        ADD_BITFIELD_RO(INTERP0_POP_LANE0, 0, 32)
    END_TYPE()

    // Read LANE1 result, and simultaneously write lane results to both accumulators (POP).
    // Reset value: 0x00000000
    BEGIN_TYPE(INTERP0_POP_LANE1_t, uint32_t)
        ADD_BITFIELD_RO(INTERP0_POP_LANE1, 0, 32)
    END_TYPE()

    // Read FULL result, and simultaneously write lane results to both accumulators (POP).
    // Reset value: 0x00000000
    BEGIN_TYPE(INTERP0_POP_FULL_t, uint32_t)
        ADD_BITFIELD_RO(INTERP0_POP_FULL, 0, 32)
    END_TYPE()

    // Read LANE0 result, without altering any internal state (PEEK).
    // Reset value: 0x00000000
    BEGIN_TYPE(INTERP0_PEEK_LANE0_t, uint32_t)
        ADD_BITFIELD_RO(INTERP0_PEEK_LANE0, 0, 32)
    END_TYPE()

    // Read LANE1 result, without altering any internal state (PEEK).
    // Reset value: 0x00000000
    BEGIN_TYPE(INTERP0_PEEK_LANE1_t, uint32_t)
        ADD_BITFIELD_RO(INTERP0_PEEK_LANE1, 0, 32)
    END_TYPE()

    // Read FULL result, without altering any internal state (PEEK).
    // Reset value: 0x00000000
    BEGIN_TYPE(INTERP0_PEEK_FULL_t, uint32_t)
        ADD_BITFIELD_RO(INTERP0_PEEK_FULL, 0, 32)
    END_TYPE()

    // Control register for lane 0
    // Reset value: 0x00000000
    BEGIN_TYPE(INTERP0_CTRL_LANE0_t, uint32_t)
        // Set if either OVERF0 or OVERF1 is set.
        ADD_BITFIELD_RO(OVERF, 25, 1)
        // Indicates if any masked-off MSBs in ACCUM1 are set.
        ADD_BITFIELD_RO(OVERF1, 24, 1)
        // Indicates if any masked-off MSBs in ACCUM0 are set.
        ADD_BITFIELD_RO(OVERF0, 23, 1)
        // Only present on INTERP0 on each core. If BLEND mode is enabled:  - LANE1 result is a linear interpolation between BASE0 and BASE1, controlled  by the 8 LSBs of lane 1 shift and mask value (a fractional number between  0 and 255/256ths)  - LANE0 result does not have BASE0 added (yields only the 8 LSBs of lane 1 shift+mask value)  - FULL result does not have lane 1 shift+mask value added (BASE2 + lane 0 shift+mask)  LANE1 SIGNED flag controls whether the interpolation is signed or unsigned.
        ADD_BITFIELD_RW(BLEND, 21, 1)
        // ORed into bits 29:28 of the lane result presented to the processor on the bus.  No effect on the internal 32-bit datapath. Handy for using a lane to generate sequence  of pointers into flash or SRAM.
        ADD_BITFIELD_RW(FORCE_MSB, 19, 2)
        // If 1, mask + shift is bypassed for LANE0 result. This does not affect FULL result.
        ADD_BITFIELD_RW(ADD_RAW, 18, 1)
        // If 1, feed the opposite lane's result into this lane's accumulator on POP.
        ADD_BITFIELD_RW(CROSS_RESULT, 17, 1)
        // If 1, feed the opposite lane's accumulator into this lane's shift + mask hardware.  Takes effect even if ADD_RAW is set (the CROSS_INPUT mux is before the shift+mask bypass)
        ADD_BITFIELD_RW(CROSS_INPUT, 16, 1)
        // If SIGNED is set, the shifted and masked accumulator value is sign-extended to 32 bits  before adding to BASE0, and LANE0 PEEK/POP appear extended to 32 bits when read by processor.
        ADD_BITFIELD_RW(SIGNED, 15, 1)
        // The most-significant bit allowed to pass by the mask (inclusive)  Setting MSB < LSB may cause chip to turn inside-out
        ADD_BITFIELD_RW(MASK_MSB, 10, 5)
        // The least-significant bit allowed to pass by the mask (inclusive)
        ADD_BITFIELD_RW(MASK_LSB, 5, 5)
        // Right-rotate applied to accumulator before masking. By appropriately configuring the masks, left and right shifts can be synthesised.
        ADD_BITFIELD_RW(SHIFT, 0, 5)
    END_TYPE()

    // Control register for lane 1
    // Reset value: 0x00000000
    BEGIN_TYPE(INTERP0_CTRL_LANE1_t, uint32_t)
        // ORed into bits 29:28 of the lane result presented to the processor on the bus.  No effect on the internal 32-bit datapath. Handy for using a lane to generate sequence  of pointers into flash or SRAM.
        ADD_BITFIELD_RW(FORCE_MSB, 19, 2)
        // If 1, mask + shift is bypassed for LANE1 result. This does not affect FULL result.
        ADD_BITFIELD_RW(ADD_RAW, 18, 1)
        // If 1, feed the opposite lane's result into this lane's accumulator on POP.
        ADD_BITFIELD_RW(CROSS_RESULT, 17, 1)
        // If 1, feed the opposite lane's accumulator into this lane's shift + mask hardware.  Takes effect even if ADD_RAW is set (the CROSS_INPUT mux is before the shift+mask bypass)
        ADD_BITFIELD_RW(CROSS_INPUT, 16, 1)
        // If SIGNED is set, the shifted and masked accumulator value is sign-extended to 32 bits  before adding to BASE1, and LANE1 PEEK/POP appear extended to 32 bits when read by processor.
        ADD_BITFIELD_RW(SIGNED, 15, 1)
        // The most-significant bit allowed to pass by the mask (inclusive)  Setting MSB < LSB may cause chip to turn inside-out
        ADD_BITFIELD_RW(MASK_MSB, 10, 5)
        // The least-significant bit allowed to pass by the mask (inclusive)
        ADD_BITFIELD_RW(MASK_LSB, 5, 5)
        // Right-rotate applied to accumulator before masking. By appropriately configuring the masks, left and right shifts can be synthesised.
        ADD_BITFIELD_RW(SHIFT, 0, 5)
    END_TYPE()

    // Values written here are atomically added to ACCUM0  Reading yields lane 0's raw shift and mask value (BASE0 not added).
    // Reset value: 0x00000000
    BEGIN_TYPE(INTERP0_ACCUM0_ADD_t, uint32_t)
        ADD_BITFIELD_RW(INTERP0_ACCUM0_ADD, 0, 24)
    END_TYPE()

    // Values written here are atomically added to ACCUM1  Reading yields lane 1's raw shift and mask value (BASE1 not added).
    // Reset value: 0x00000000
    BEGIN_TYPE(INTERP0_ACCUM1_ADD_t, uint32_t)
        ADD_BITFIELD_RW(INTERP0_ACCUM1_ADD, 0, 24)
    END_TYPE()

    // On write, the lower 16 bits go to BASE0, upper bits to BASE1 simultaneously.  Each half is sign-extended to 32 bits if that lane's SIGNED flag is set.
    // Reset value: 0x00000000
    BEGIN_TYPE(INTERP0_BASE_1AND0_t, uint32_t)
        ADD_BITFIELD_WO(INTERP0_BASE_1AND0, 0, 32)
    END_TYPE()

    // Read/write access to accumulator 0
    // Reset value: 0x00000000
    BEGIN_TYPE(INTERP1_ACCUM0_t, uint32_t)
        ADD_BITFIELD_RW(INTERP1_ACCUM0, 0, 32)
    END_TYPE()

    // Read/write access to accumulator 1
    // Reset value: 0x00000000
    BEGIN_TYPE(INTERP1_ACCUM1_t, uint32_t)
        ADD_BITFIELD_RW(INTERP1_ACCUM1, 0, 32)
    END_TYPE()

    // Read/write access to BASE0 register.
    // Reset value: 0x00000000
    BEGIN_TYPE(INTERP1_BASE0_t, uint32_t)
        ADD_BITFIELD_RW(INTERP1_BASE0, 0, 32)
    END_TYPE()

    // Read/write access to BASE1 register.
    // Reset value: 0x00000000
    BEGIN_TYPE(INTERP1_BASE1_t, uint32_t)
        ADD_BITFIELD_RW(INTERP1_BASE1, 0, 32)
    END_TYPE()

    // Read/write access to BASE2 register.
    // Reset value: 0x00000000
    BEGIN_TYPE(INTERP1_BASE2_t, uint32_t)
        ADD_BITFIELD_RW(INTERP1_BASE2, 0, 32)
    END_TYPE()

    // Read LANE0 result, and simultaneously write lane results to both accumulators (POP).
    // Reset value: 0x00000000
    BEGIN_TYPE(INTERP1_POP_LANE0_t, uint32_t)
        ADD_BITFIELD_RO(INTERP1_POP_LANE0, 0, 32)
    END_TYPE()

    // Read LANE1 result, and simultaneously write lane results to both accumulators (POP).
    // Reset value: 0x00000000
    BEGIN_TYPE(INTERP1_POP_LANE1_t, uint32_t)
        ADD_BITFIELD_RO(INTERP1_POP_LANE1, 0, 32)
    END_TYPE()

    // Read FULL result, and simultaneously write lane results to both accumulators (POP).
    // Reset value: 0x00000000
    BEGIN_TYPE(INTERP1_POP_FULL_t, uint32_t)
        ADD_BITFIELD_RO(INTERP1_POP_FULL, 0, 32)
    END_TYPE()

    // Read LANE0 result, without altering any internal state (PEEK).
    // Reset value: 0x00000000
    BEGIN_TYPE(INTERP1_PEEK_LANE0_t, uint32_t)
        ADD_BITFIELD_RO(INTERP1_PEEK_LANE0, 0, 32)
    END_TYPE()

    // Read LANE1 result, without altering any internal state (PEEK).
    // Reset value: 0x00000000
    BEGIN_TYPE(INTERP1_PEEK_LANE1_t, uint32_t)
        ADD_BITFIELD_RO(INTERP1_PEEK_LANE1, 0, 32)
    END_TYPE()

    // Read FULL result, without altering any internal state (PEEK).
    // Reset value: 0x00000000
    BEGIN_TYPE(INTERP1_PEEK_FULL_t, uint32_t)
        ADD_BITFIELD_RO(INTERP1_PEEK_FULL, 0, 32)
    END_TYPE()

    // Control register for lane 0
    // Reset value: 0x00000000
    BEGIN_TYPE(INTERP1_CTRL_LANE0_t, uint32_t)
        // Set if either OVERF0 or OVERF1 is set.
        ADD_BITFIELD_RO(OVERF, 25, 1)
        // Indicates if any masked-off MSBs in ACCUM1 are set.
        ADD_BITFIELD_RO(OVERF1, 24, 1)
        // Indicates if any masked-off MSBs in ACCUM0 are set.
        ADD_BITFIELD_RO(OVERF0, 23, 1)
        // Only present on INTERP1 on each core. If CLAMP mode is enabled:  - LANE0 result is shifted and masked ACCUM0, clamped by a lower bound of  BASE0 and an upper bound of BASE1.  - Signedness of these comparisons is determined by LANE0_CTRL_SIGNED
        ADD_BITFIELD_RW(CLAMP, 22, 1)
        // ORed into bits 29:28 of the lane result presented to the processor on the bus.  No effect on the internal 32-bit datapath. Handy for using a lane to generate sequence  of pointers into flash or SRAM.
        ADD_BITFIELD_RW(FORCE_MSB, 19, 2)
        // If 1, mask + shift is bypassed for LANE0 result. This does not affect FULL result.
        ADD_BITFIELD_RW(ADD_RAW, 18, 1)
        // If 1, feed the opposite lane's result into this lane's accumulator on POP.
        ADD_BITFIELD_RW(CROSS_RESULT, 17, 1)
        // If 1, feed the opposite lane's accumulator into this lane's shift + mask hardware.  Takes effect even if ADD_RAW is set (the CROSS_INPUT mux is before the shift+mask bypass)
        ADD_BITFIELD_RW(CROSS_INPUT, 16, 1)
        // If SIGNED is set, the shifted and masked accumulator value is sign-extended to 32 bits  before adding to BASE0, and LANE0 PEEK/POP appear extended to 32 bits when read by processor.
        ADD_BITFIELD_RW(SIGNED, 15, 1)
        // The most-significant bit allowed to pass by the mask (inclusive)  Setting MSB < LSB may cause chip to turn inside-out
        ADD_BITFIELD_RW(MASK_MSB, 10, 5)
        // The least-significant bit allowed to pass by the mask (inclusive)
        ADD_BITFIELD_RW(MASK_LSB, 5, 5)
        // Right-rotate applied to accumulator before masking. By appropriately configuring the masks, left and right shifts can be synthesised.
        ADD_BITFIELD_RW(SHIFT, 0, 5)
    END_TYPE()

    // Control register for lane 1
    // Reset value: 0x00000000
    BEGIN_TYPE(INTERP1_CTRL_LANE1_t, uint32_t)
        // ORed into bits 29:28 of the lane result presented to the processor on the bus.  No effect on the internal 32-bit datapath. Handy for using a lane to generate sequence  of pointers into flash or SRAM.
        ADD_BITFIELD_RW(FORCE_MSB, 19, 2)
        // If 1, mask + shift is bypassed for LANE1 result. This does not affect FULL result.
        ADD_BITFIELD_RW(ADD_RAW, 18, 1)
        // If 1, feed the opposite lane's result into this lane's accumulator on POP.
        ADD_BITFIELD_RW(CROSS_RESULT, 17, 1)
        // If 1, feed the opposite lane's accumulator into this lane's shift + mask hardware.  Takes effect even if ADD_RAW is set (the CROSS_INPUT mux is before the shift+mask bypass)
        ADD_BITFIELD_RW(CROSS_INPUT, 16, 1)
        // If SIGNED is set, the shifted and masked accumulator value is sign-extended to 32 bits  before adding to BASE1, and LANE1 PEEK/POP appear extended to 32 bits when read by processor.
        ADD_BITFIELD_RW(SIGNED, 15, 1)
        // The most-significant bit allowed to pass by the mask (inclusive)  Setting MSB < LSB may cause chip to turn inside-out
        ADD_BITFIELD_RW(MASK_MSB, 10, 5)
        // The least-significant bit allowed to pass by the mask (inclusive)
        ADD_BITFIELD_RW(MASK_LSB, 5, 5)
        // Right-rotate applied to accumulator before masking. By appropriately configuring the masks, left and right shifts can be synthesised.
        ADD_BITFIELD_RW(SHIFT, 0, 5)
    END_TYPE()

    // Values written here are atomically added to ACCUM0  Reading yields lane 0's raw shift and mask value (BASE0 not added).
    // Reset value: 0x00000000
    BEGIN_TYPE(INTERP1_ACCUM0_ADD_t, uint32_t)
        ADD_BITFIELD_RW(INTERP1_ACCUM0_ADD, 0, 24)
    END_TYPE()

    // Values written here are atomically added to ACCUM1  Reading yields lane 1's raw shift and mask value (BASE1 not added).
    // Reset value: 0x00000000
    BEGIN_TYPE(INTERP1_ACCUM1_ADD_t, uint32_t)
        ADD_BITFIELD_RW(INTERP1_ACCUM1_ADD, 0, 24)
    END_TYPE()

    // On write, the lower 16 bits go to BASE0, upper bits to BASE1 simultaneously.  Each half is sign-extended to 32 bits if that lane's SIGNED flag is set.
    // Reset value: 0x00000000
    BEGIN_TYPE(INTERP1_BASE_1AND0_t, uint32_t)
        ADD_BITFIELD_WO(INTERP1_BASE_1AND0, 0, 32)
    END_TYPE()

    // Reading from a spinlock address will:  - Return 0 if lock is already locked  - Otherwise return nonzero, and simultaneously claim the lock Writing (any value) releases the lock.  If core 0 and core 1 attempt to claim the same lock simultaneously, core 0 wins.  The value returned on success is 0x1 << lock number.
    // Reset value: 0x00000000
    typedef uint32_t SPINLOCK_t;

    // Trigger a doorbell interrupt on the opposite core. Write 1 to a bit to set the corresponding bit in DOORBELL_IN on the opposite core. This raises the opposite core's doorbell interrupt. Read to get the status of the doorbells currently asserted on the opposite core. This is equivalent to that core reading its own DOORBELL_IN status.
    // Reset value: 0x00000000
    BEGIN_TYPE(DOORBELL_OUT_SET_t, uint32_t)
        ADD_BITFIELD_RW(DOORBELL_OUT_SET, 0, 8)
    END_TYPE()

    // Clear doorbells which have been posted to the opposite core. This register is intended for debugging and initialisation purposes. Writing 1 to a bit in DOORBELL_OUT_CLR clears the corresponding bit in DOORBELL_IN on the opposite core. Clearing all bits will cause that core's doorbell interrupt to deassert. Since the usual order of events is for software to send events using DOORBELL_OUT_SET, and acknowledge incoming events by writing to DOORBELL_IN_CLR, this register should be used with caution to avoid race conditions. Reading returns the status of the doorbells currently asserted on the other core, i.e. is equivalent to that core reading its own DOORBELL_IN status.
    // Reset value: 0x00000000
    BEGIN_TYPE(DOORBELL_OUT_CLR_t, uint32_t)
        ADD_BITFIELD_RW(DOORBELL_OUT_CLR, 0, 8)
    END_TYPE()

    // Write 1s to trigger doorbell interrupts on this core. Read to get status of doorbells currently asserted on this core.
    // Reset value: 0x00000000
    BEGIN_TYPE(DOORBELL_IN_SET_t, uint32_t)
        ADD_BITFIELD_RW(DOORBELL_IN_SET, 0, 8)
    END_TYPE()

    // Check and acknowledge doorbells posted to this core. This core's doorbell interrupt is asserted when any bit in this register is 1. Write 1 to each bit to clear that bit. The doorbell interrupt deasserts once all bits are cleared. Read to get status of doorbells currently asserted on this core.
    // Reset value: 0x00000000
    BEGIN_TYPE(DOORBELL_IN_CLR_t, uint32_t)
        ADD_BITFIELD_RW(DOORBELL_IN_CLR, 0, 8)
    END_TYPE()

    // Detach certain core-local peripherals from Secure SIO, and attach them to Non-secure SIO, so that Non-secure software can use them. Attempting to access one of these peripherals from the Secure SIO when it is attached to the Non-secure SIO, or vice versa, will generate a bus error. This register is per-core, and is only present on the Secure SIO. Most SIO hardware is duplicated across the Secure and Non-secure SIO, so is not listed in this register.
    // Reset value: 0x00000000
    BEGIN_TYPE(PERI_NONSEC_t, uint32_t)
        // IF 1, detach TMDS encoder (of this core) from the Secure SIO, and attach to the Non-secure SIO.
        ADD_BITFIELD_RW(TMDS, 5, 1)
        // If 1, detach interpolator 1 (of this core) from the Secure SIO, and attach to the Non-secure SIO.
        ADD_BITFIELD_RW(INTERP1, 1, 1)
        // If 1, detach interpolator 0 (of this core) from the Secure SIO, and attach to the Non-secure SIO.
        ADD_BITFIELD_RW(INTERP0, 0, 1)
    END_TYPE()

    // Control the assertion of the standard software interrupt (MIP.MSIP) on the RISC-V cores. Unlike the RISC-V timer, this interrupt is not routed to a normal system-level interrupt line, so can not be used by the Arm cores. It is safe for both cores to write to this register on the same cycle. The set/clear effect is accumulated across both cores, and then applied. If a flag is both set and cleared on the same cycle, only the set takes effect.
    // Reset value: 0x00000000
    BEGIN_TYPE(RISCV_SOFTIRQ_t, uint32_t)
        // Write 1 to atomically clear the core 1 software interrupt flag. Read to get the status of this flag.
        ADD_BITFIELD_RW(CORE1_CLR, 9, 1)
        // Write 1 to atomically clear the core 0 software interrupt flag. Read to get the status of this flag.
        ADD_BITFIELD_RW(CORE0_CLR, 8, 1)
        // Write 1 to atomically set the core 1 software interrupt flag. Read to get the status of this flag.
        ADD_BITFIELD_RW(CORE1_SET, 1, 1)
        // Write 1 to atomically set the core 0 software interrupt flag. Read to get the status of this flag.
        ADD_BITFIELD_RW(CORE0_SET, 0, 1)
    END_TYPE()

    // Control register for the RISC-V 64-bit Machine-mode timer. This timer is only present in the Secure SIO, so is only accessible to an Arm core in Secure mode or a RISC-V core in Machine mode. Note whilst this timer follows the RISC-V privileged specification, it is equally usable by the Arm cores. The interrupts are routed to normal system-level interrupt lines as well as to the MIP.MTIP inputs on the RISC-V cores.
    // Reset value: 0x0000000d
    BEGIN_TYPE(MTIME_CTRL_t, uint32_t)
        // If 1, the timer pauses when core 1 is in the debug halt state.
        ADD_BITFIELD_RW(DBGPAUSE_CORE1, 3, 1)
        // If 1, the timer pauses when core 0 is in the debug halt state.
        ADD_BITFIELD_RW(DBGPAUSE_CORE0, 2, 1)
        // If 1, increment the timer every cycle (i.e. run directly from the system clock), rather than incrementing on the system-level timer tick input.
        ADD_BITFIELD_RW(FULLSPEED, 1, 1)
        // Timer enable bit. When 0, the timer will not increment automatically.
        ADD_BITFIELD_RW(EN, 0, 1)
    END_TYPE()

    // Read/write access to the high half of RISC-V Machine-mode timer. This register is shared between both cores. If both cores write on the same cycle, core 1 takes precedence.
    // Reset value: 0x00000000
    BEGIN_TYPE(MTIME_t, uint32_t)
        ADD_BITFIELD_RW(MTIME, 0, 32)
    END_TYPE()

    // Read/write access to the high half of RISC-V Machine-mode timer. This register is shared between both cores. If both cores write on the same cycle, core 1 takes precedence.
    // Reset value: 0x00000000
    BEGIN_TYPE(MTIMEH_t, uint32_t)
        ADD_BITFIELD_RW(MTIMEH, 0, 32)
    END_TYPE()

    // Low half of RISC-V Machine-mode timer comparator. This register is core-local, i.e., each core gets a copy of this register, with the comparison result routed to its own interrupt line. The timer interrupt is asserted whenever MTIME is greater than or equal to MTIMECMP. This comparison is unsigned, and performed on the full 64-bit values.
    // Reset value: 0xffffffff
    BEGIN_TYPE(MTIMECMP_t, uint32_t)
        ADD_BITFIELD_RW(MTIMECMP, 0, 32)
    END_TYPE()

    // High half of RISC-V Machine-mode timer comparator. This register is core-local. The timer interrupt is asserted whenever MTIME is greater than or equal to MTIMECMP. This comparison is unsigned, and performed on the full 64-bit values.
    // Reset value: 0xffffffff
    BEGIN_TYPE(MTIMECMPH_t, uint32_t)
        ADD_BITFIELD_RW(MTIMECMPH, 0, 32)
    END_TYPE()

    // Control register for TMDS encoder.
    // Reset value: 0x00000000
    BEGIN_TYPE(TMDS_CTRL_t, uint32_t)
        // Clear the running DC balance state of the TMDS encoders. This bit should be written once at the beginning of each scanline.
        ADD_BITFIELD_WO(CLEAR_BALANCE, 28, 1)
        // When encoding two pixels's worth of symbols in one cycle (a read of a PEEK/POP_DOUBLE register), the second encoder sees a shifted version of the colour data register. This control disables that shift, so that both encoder layers see the same pixel data. This is used for pixel doubling.
        ADD_BITFIELD_RW(PIX2_NOSHIFT, 27, 1)
        // Shift applied to the colour data register with each read of a POP alias register. Reading from the POP_SINGLE register, or reading from the POP_DOUBLE register with PIX2_NOSHIFT set (for pixel doubling), shifts by the indicated amount. Reading from a POP_DOUBLE register when PIX2_NOSHIFT is clear will shift by double the indicated amount. (Shift by 32 means no shift.)
        ADD_BITFIELD_RW(PIX_SHIFT, 24, 3)
        // Enable lane interleaving for reads of PEEK_SINGLE/POP_SINGLE. When interleaving is disabled, each of the 3 symbols appears as a contiguous 10-bit field, with lane 0 being the least-significant and starting at bit 0 of the register. When interleaving is enabled, the symbols are packed into 5 chunks of 3 lanes times 2 bits (30 bits total). Each chunk contains two bits of a TMDS symbol per lane, with lane 0 being the least significant.
        ADD_BITFIELD_RW(INTERLEAVE, 23, 1)
        // Number of valid colour MSBs for lane 2 (1-8 bits, encoded as 0 through 7). Remaining LSBs are masked to 0 after the rotate.
        ADD_BITFIELD_RW(L2_NBITS, 18, 3)
        // Number of valid colour MSBs for lane 1 (1-8 bits, encoded as 0 through 7). Remaining LSBs are masked to 0 after the rotate.
        ADD_BITFIELD_RW(L1_NBITS, 15, 3)
        // Number of valid colour MSBs for lane 0 (1-8 bits, encoded as 0 through 7). Remaining LSBs are masked to 0 after the rotate.
        ADD_BITFIELD_RW(L0_NBITS, 12, 3)
        // Right-rotate the 16 LSBs of the colour accumulator by 0-15 bits, in order to get the MSB of the lane 2 (red) colour data aligned with the MSB of the 8-bit encoder input. For example, for RGB565 (red most significant), red is bits 15:11, so should be right-rotated by 8 bits to align with bits 7:3 of the encoder input.
        ADD_BITFIELD_RW(L2_ROT, 8, 4)
        // Right-rotate the 16 LSBs of the colour accumulator by 0-15 bits, in order to get the MSB of the lane 1 (green) colour data aligned with the MSB of the 8-bit encoder input. For example, for RGB565, green is bits 10:5, so should be right-rotated by 3 bits to align with bits 7:2 of the encoder input.
        ADD_BITFIELD_RW(L1_ROT, 4, 4)
        // Right-rotate the 16 LSBs of the colour accumulator by 0-15 bits, in order to get the MSB of the lane 0 (blue) colour data aligned with the MSB of the 8-bit encoder input. For example, for RGB565 (red most significant), blue is bits 4:0, so should be right-rotated by 13 to align with bits 7:3 of the encoder input.
        ADD_BITFIELD_RW(L0_ROT, 0, 4)
    END_TYPE()

    // Do not shift the colour data register.
    static const uint32_t TMDS_CTRL_PIX_SHIFT__0 = 0;
    // Shift the colour data register by 1 bit
    static const uint32_t TMDS_CTRL_PIX_SHIFT__1 = 1;
    // Shift the colour data register by 2 bits
    static const uint32_t TMDS_CTRL_PIX_SHIFT__2 = 2;
    // Shift the colour data register by 4 bits
    static const uint32_t TMDS_CTRL_PIX_SHIFT__4 = 3;
    // Shift the colour data register by 8 bits
    static const uint32_t TMDS_CTRL_PIX_SHIFT__8 = 4;
    // Shift the colour data register by 16 bits
    static const uint32_t TMDS_CTRL_PIX_SHIFT__16 = 5;

    // Write-only access to the TMDS colour data register.
    // Reset value: 0x00000000
    BEGIN_TYPE(TMDS_WDATA_t, uint32_t)
        ADD_BITFIELD_WO(TMDS_WDATA, 0, 32)
    END_TYPE()

    // Get the encoding of one pixel's worth of colour data, packed into a 32-bit value (3x10-bit symbols). The PEEK alias does not shift the colour register when read, but still advances the running DC balance state of each encoder. This is useful for pixel doubling.
    // Reset value: 0x00000000
    BEGIN_TYPE(TMDS_PEEK_SINGLE_t, uint32_t)
        ADD_BITFIELD_RO(TMDS_PEEK_SINGLE, 0, 32)
    END_TYPE()

    // Get the encoding of one pixel's worth of colour data, packed into a 32-bit value. The packing is 5 chunks of 3 lanes times 2 bits (30 bits total). Each chunk contains two bits of a TMDS symbol per lane. This format is intended for shifting out with the HSTX peripheral on RP2350. The POP alias shifts the colour register when read, as well as advancing the running DC balance state of each encoder.
    // Reset value: 0x00000000
    BEGIN_TYPE(TMDS_POP_SINGLE_t, uint32_t)
        ADD_BITFIELD_RO(TMDS_POP_SINGLE, 0, 32)
    END_TYPE()

    // Get lane 0 of the encoding of two pixels' worth of colour data. Two 10-bit TMDS symbols are packed at the bottom of a 32-bit word. The PEEK alias does not shift the colour register when read, but still advances the lane 0 DC balance state. This is useful if all 3 lanes' worth of encode are to be read at once, rather than processing the entire scanline for one lane before moving to the next lane.
    // Reset value: 0x00000000
    BEGIN_TYPE(TMDS_PEEK_DOUBLE_L0_t, uint32_t)
        ADD_BITFIELD_RO(TMDS_PEEK_DOUBLE_L0, 0, 32)
    END_TYPE()

    // Get lane 0 of the encoding of two pixels' worth of colour data. Two 10-bit TMDS symbols are packed at the bottom of a 32-bit word. The POP alias shifts the colour register when read, according to the values of PIX_SHIFT and PIX2_NOSHIFT.
    // Reset value: 0x00000000
    BEGIN_TYPE(TMDS_POP_DOUBLE_L0_t, uint32_t)
        ADD_BITFIELD_RO(TMDS_POP_DOUBLE_L0, 0, 32)
    END_TYPE()

    // Get lane 1 of the encoding of two pixels' worth of colour data. Two 10-bit TMDS symbols are packed at the bottom of a 32-bit word. The PEEK alias does not shift the colour register when read, but still advances the lane 1 DC balance state. This is useful if all 3 lanes' worth of encode are to be read at once, rather than processing the entire scanline for one lane before moving to the next lane.
    // Reset value: 0x00000000
    BEGIN_TYPE(TMDS_PEEK_DOUBLE_L1_t, uint32_t)
        ADD_BITFIELD_RO(TMDS_PEEK_DOUBLE_L1, 0, 32)
    END_TYPE()

    // Get lane 1 of the encoding of two pixels' worth of colour data. Two 10-bit TMDS symbols are packed at the bottom of a 32-bit word. The POP alias shifts the colour register when read, according to the values of PIX_SHIFT and PIX2_NOSHIFT.
    // Reset value: 0x00000000
    BEGIN_TYPE(TMDS_POP_DOUBLE_L1_t, uint32_t)
        ADD_BITFIELD_RO(TMDS_POP_DOUBLE_L1, 0, 32)
    END_TYPE()

    // Get lane 2 of the encoding of two pixels' worth of colour data. Two 10-bit TMDS symbols are packed at the bottom of a 32-bit word. The PEEK alias does not shift the colour register when read, but still advances the lane 2 DC balance state. This is useful if all 3 lanes' worth of encode are to be read at once, rather than processing the entire scanline for one lane before moving to the next lane.
    // Reset value: 0x00000000
    BEGIN_TYPE(TMDS_PEEK_DOUBLE_L2_t, uint32_t)
        ADD_BITFIELD_RO(TMDS_PEEK_DOUBLE_L2, 0, 32)
    END_TYPE()

    // Get lane 2 of the encoding of two pixels' worth of colour data. Two 10-bit TMDS symbols are packed at the bottom of a 32-bit word. The POP alias shifts the colour register when read, according to the values of PIX_SHIFT and PIX2_NOSHIFT.
    // Reset value: 0x00000000
    BEGIN_TYPE(TMDS_POP_DOUBLE_L2_t, uint32_t)
        ADD_BITFIELD_RO(TMDS_POP_DOUBLE_L2, 0, 32)
    END_TYPE()

    struct SIO_t {
        CPUID_t                       CPUID;
        GPIO_IN_t                     GPIO_IN;
        GPIO_HI_IN_t                  GPIO_HI_IN;
        uint32_t                      reserved0;
        GPIO_OUT_t                    GPIO_OUT;
        GPIO_HI_OUT_t                 GPIO_HI_OUT;
        GPIO_OUT_SET_t                GPIO_OUT_SET;
        GPIO_HI_OUT_SET_t             GPIO_HI_OUT_SET;
        GPIO_OUT_CLR_t                GPIO_OUT_CLR;
        GPIO_HI_OUT_CLR_t             GPIO_HI_OUT_CLR;
        GPIO_OUT_XOR_t                GPIO_OUT_XOR;
        GPIO_HI_OUT_XOR_t             GPIO_HI_OUT_XOR;
        GPIO_OE_t                     GPIO_OE;
        GPIO_HI_OE_t                  GPIO_HI_OE;
        GPIO_OE_SET_t                 GPIO_OE_SET;
        GPIO_HI_OE_SET_t              GPIO_HI_OE_SET;
        GPIO_OE_CLR_t                 GPIO_OE_CLR;
        GPIO_HI_OE_CLR_t              GPIO_HI_OE_CLR;
        GPIO_OE_XOR_t                 GPIO_OE_XOR;
        GPIO_HI_OE_XOR_t              GPIO_HI_OE_XOR;
        FIFO_ST_t                     FIFO_ST;
        FIFO_WR_t                     FIFO_WR;
        FIFO_RD_t                     FIFO_RD;
        SPINLOCK_ST_t                 SPINLOCK_ST;
        uint32_t                      reserved1[8];
        INTERP0_ACCUM0_t              INTERP0_ACCUM0;
        INTERP0_ACCUM1_t              INTERP0_ACCUM1;
        INTERP0_BASE0_t               INTERP0_BASE0;
        INTERP0_BASE1_t               INTERP0_BASE1;
        INTERP0_BASE2_t               INTERP0_BASE2;
        INTERP0_POP_LANE0_t           INTERP0_POP_LANE0;
        INTERP0_POP_LANE1_t           INTERP0_POP_LANE1;
        INTERP0_POP_FULL_t            INTERP0_POP_FULL;
        INTERP0_PEEK_LANE0_t          INTERP0_PEEK_LANE0;
        INTERP0_PEEK_LANE1_t          INTERP0_PEEK_LANE1;
        INTERP0_PEEK_FULL_t           INTERP0_PEEK_FULL;
        INTERP0_CTRL_LANE0_t          INTERP0_CTRL_LANE0;
        INTERP0_CTRL_LANE1_t          INTERP0_CTRL_LANE1;
        INTERP0_ACCUM0_ADD_t          INTERP0_ACCUM0_ADD;
        INTERP0_ACCUM1_ADD_t          INTERP0_ACCUM1_ADD;
        INTERP0_BASE_1AND0_t          INTERP0_BASE_1AND0;
        INTERP1_ACCUM0_t              INTERP1_ACCUM0;
        INTERP1_ACCUM1_t              INTERP1_ACCUM1;
        INTERP1_BASE0_t               INTERP1_BASE0;
        INTERP1_BASE1_t               INTERP1_BASE1;
        INTERP1_BASE2_t               INTERP1_BASE2;
        INTERP1_POP_LANE0_t           INTERP1_POP_LANE0;
        INTERP1_POP_LANE1_t           INTERP1_POP_LANE1;
        INTERP1_POP_FULL_t            INTERP1_POP_FULL;
        INTERP1_PEEK_LANE0_t          INTERP1_PEEK_LANE0;
        INTERP1_PEEK_LANE1_t          INTERP1_PEEK_LANE1;
        INTERP1_PEEK_FULL_t           INTERP1_PEEK_FULL;
        INTERP1_CTRL_LANE0_t          INTERP1_CTRL_LANE0;
        INTERP1_CTRL_LANE1_t          INTERP1_CTRL_LANE1;
        INTERP1_ACCUM0_ADD_t          INTERP1_ACCUM0_ADD;
        INTERP1_ACCUM1_ADD_t          INTERP1_ACCUM1_ADD;
        INTERP1_BASE_1AND0_t          INTERP1_BASE_1AND0;
        SPINLOCK_t                    SPINLOCK[32];
        DOORBELL_OUT_SET_t            DOORBELL_OUT_SET;
        DOORBELL_OUT_CLR_t            DOORBELL_OUT_CLR;
        DOORBELL_IN_SET_t             DOORBELL_IN_SET;
        DOORBELL_IN_CLR_t             DOORBELL_IN_CLR;
        PERI_NONSEC_t                 PERI_NONSEC;
        uint32_t                      reserved2[3];
        RISCV_SOFTIRQ_t               RISCV_SOFTIRQ;
        MTIME_CTRL_t                  MTIME_CTRL;
        uint32_t                      reserved3[2];
        MTIME_t                       MTIME;
        MTIMEH_t                      MTIMEH;
        MTIMECMP_t                    MTIMECMP;
        MTIMECMPH_t                   MTIMECMPH;
        TMDS_CTRL_t                   TMDS_CTRL;
        TMDS_WDATA_t                  TMDS_WDATA;
        TMDS_PEEK_SINGLE_t            TMDS_PEEK_SINGLE;
        TMDS_POP_SINGLE_t             TMDS_POP_SINGLE;
        TMDS_PEEK_DOUBLE_L0_t         TMDS_PEEK_DOUBLE_L0;
        TMDS_POP_DOUBLE_L0_t          TMDS_POP_DOUBLE_L0;
        TMDS_PEEK_DOUBLE_L1_t         TMDS_PEEK_DOUBLE_L1;
        TMDS_POP_DOUBLE_L1_t          TMDS_POP_DOUBLE_L1;
        TMDS_PEEK_DOUBLE_L2_t         TMDS_PEEK_DOUBLE_L2;
        TMDS_POP_DOUBLE_L2_t          TMDS_POP_DOUBLE_L2;
    };

    static SIO_t & SIO = (*(SIO_t *)0xd0000000);

} // _SIO_

namespace _SIO_NS_  {

    static _SIO_::SIO_t & SIO_NS = (*(_SIO_::SIO_t *)0xd0020000);

} // _SIO_NS_

// Additional registers mapped adjacent to the bootram, for use by the bootrom.
namespace _BOOTRAM_  {

    // This registers always ORs writes into its current contents. Once a bit is set, it can only be cleared by a reset.
    // Reset value: 0x00000000
    BEGIN_TYPE(WRITE_ONCE0_t, uint32_t)
        ADD_BITFIELD_RW(WRITE_ONCE0, 0, 32)
    END_TYPE()

    // This registers always ORs writes into its current contents. Once a bit is set, it can only be cleared by a reset.
    // Reset value: 0x00000000
    BEGIN_TYPE(WRITE_ONCE1_t, uint32_t)
        ADD_BITFIELD_RW(WRITE_ONCE1, 0, 32)
    END_TYPE()

    // Bootlock status register. 1=unclaimed, 0=claimed. These locks function identically to the SIO spinlocks, but are reserved for bootrom use.
    // Reset value: 0x000000ff
    BEGIN_TYPE(BOOTLOCK_STAT_t, uint32_t)
        ADD_BITFIELD_RW(BOOTLOCK_STAT, 0, 8)
    END_TYPE()

    // Read to claim and check. Write to unclaim. The value returned on successful claim is 1 << n, and on failed claim is zero.
    // Reset value: 0x00000000
    BEGIN_TYPE(BOOTLOCK0_t, uint32_t)
        ADD_BITFIELD_RW(BOOTLOCK0, 0, 32)
    END_TYPE()

    // Read to claim and check. Write to unclaim. The value returned on successful claim is 1 << n, and on failed claim is zero.
    // Reset value: 0x00000000
    BEGIN_TYPE(BOOTLOCK1_t, uint32_t)
        ADD_BITFIELD_RW(BOOTLOCK1, 0, 32)
    END_TYPE()

    // Read to claim and check. Write to unclaim. The value returned on successful claim is 1 << n, and on failed claim is zero.
    // Reset value: 0x00000000
    BEGIN_TYPE(BOOTLOCK2_t, uint32_t)
        ADD_BITFIELD_RW(BOOTLOCK2, 0, 32)
    END_TYPE()

    // Read to claim and check. Write to unclaim. The value returned on successful claim is 1 << n, and on failed claim is zero.
    // Reset value: 0x00000000
    BEGIN_TYPE(BOOTLOCK3_t, uint32_t)
        ADD_BITFIELD_RW(BOOTLOCK3, 0, 32)
    END_TYPE()

    // Read to claim and check. Write to unclaim. The value returned on successful claim is 1 << n, and on failed claim is zero.
    // Reset value: 0x00000000
    BEGIN_TYPE(BOOTLOCK4_t, uint32_t)
        ADD_BITFIELD_RW(BOOTLOCK4, 0, 32)
    END_TYPE()

    // Read to claim and check. Write to unclaim. The value returned on successful claim is 1 << n, and on failed claim is zero.
    // Reset value: 0x00000000
    BEGIN_TYPE(BOOTLOCK5_t, uint32_t)
        ADD_BITFIELD_RW(BOOTLOCK5, 0, 32)
    END_TYPE()

    // Read to claim and check. Write to unclaim. The value returned on successful claim is 1 << n, and on failed claim is zero.
    // Reset value: 0x00000000
    BEGIN_TYPE(BOOTLOCK6_t, uint32_t)
        ADD_BITFIELD_RW(BOOTLOCK6, 0, 32)
    END_TYPE()

    // Read to claim and check. Write to unclaim. The value returned on successful claim is 1 << n, and on failed claim is zero.
    // Reset value: 0x00000000
    BEGIN_TYPE(BOOTLOCK7_t, uint32_t)
        ADD_BITFIELD_RW(BOOTLOCK7, 0, 32)
    END_TYPE()

    struct BOOTRAM_t {
        uint32_t                      reserved0[512];
        WRITE_ONCE0_t                 WRITE_ONCE0;
        WRITE_ONCE1_t                 WRITE_ONCE1;
        BOOTLOCK_STAT_t               BOOTLOCK_STAT;
        BOOTLOCK0_t                   BOOTLOCK0;
        BOOTLOCK1_t                   BOOTLOCK1;
        BOOTLOCK2_t                   BOOTLOCK2;
        BOOTLOCK3_t                   BOOTLOCK3;
        BOOTLOCK4_t                   BOOTLOCK4;
        BOOTLOCK5_t                   BOOTLOCK5;
        BOOTLOCK6_t                   BOOTLOCK6;
        BOOTLOCK7_t                   BOOTLOCK7;
    };

    static BOOTRAM_t & BOOTRAM = (*(BOOTRAM_t *)0x400e0000);
    static BOOTRAM_t & BOOTRAM_XOR = (*(BOOTRAM_t *)0x400e1000);
    static BOOTRAM_t & BOOTRAM_SET = (*(BOOTRAM_t *)0x400e2000);
    static BOOTRAM_t & BOOTRAM_CLR = (*(BOOTRAM_t *)0x400e3000);

} // _BOOTRAM_

// Coresight block - RP specific registers
namespace _CORESIGHT_TRACE_  {

    // Control and status register
    // Reset value: 0x00000001
    BEGIN_TYPE(CTRL_STATUS_t, uint32_t)
        // This status flag is set high when trace data has been dropped due to the FIFO being full at the point trace data was sampled. Write 1 to acknowledge and clear the bit.
        ADD_BITFIELD_RW(TRACE_CAPTURE_FIFO_OVERFLOW, 1, 1)
        // Set to 1 to continuously hold the trace FIFO in a flushed state and prevent overflow. Before clearing this flag, configure and start a DMA channel with the correct DREQ for the TRACE_CAPTURE_FIFO register. Clear this flag to begin sampling trace data, and set once again once the trace capture buffer is full. You must configure the TPIU in order to generate trace packets to be captured, as well as components like the ETM further upstream to generate the event stream propagated to the TPIU.
        ADD_BITFIELD_RW(TRACE_CAPTURE_FIFO_FLUSH, 0, 1)
    END_TYPE()

    // FIFO for trace data captured from the TPIU
    // Reset value: 0x00000000
    BEGIN_TYPE(TRACE_CAPTURE_FIFO_t, uint32_t)
        // Read from an 8 x 32-bit FIFO containing trace data captured from the TPIU. Hardware pushes to the FIFO on rising edges of clk_sys, when either of the following is true: * TPIU TRACECTL output is low (normal trace data) * TPIU TRACETCL output is high, and TPIU TRACEDATA0 and TRACEDATA1 are both low (trigger packet) These conditions are in accordance with Arm Coresight Architecture Spec v3.0 section D3.3.3: Decoding requirements for Trace Capture Devices The data captured into the FIFO is the full 32-bit TRACEDATA bus output by the TPIU. Note that the TPIU is a DDR output at half of clk_sys, therefore this interface can capture the full 32-bit TPIU DDR output bandwidth as it samples once per active edge of the TPIU output clock.
        ADD_BITFIELD_RO(RDATA, 0, 32)
    END_TYPE()

    struct CORESIGHT_TRACE_t {
        CTRL_STATUS_t                 CTRL_STATUS;
        TRACE_CAPTURE_FIFO_t          TRACE_CAPTURE_FIFO;
    };

    static CORESIGHT_TRACE_t & CORESIGHT_TRACE = (*(CORESIGHT_TRACE_t *)0x50700000);
    static CORESIGHT_TRACE_t & CORESIGHT_TRACE_XOR = (*(CORESIGHT_TRACE_t *)0x50701000);
    static CORESIGHT_TRACE_t & CORESIGHT_TRACE_SET = (*(CORESIGHT_TRACE_t *)0x50702000);
    static CORESIGHT_TRACE_t & CORESIGHT_TRACE_CLR = (*(CORESIGHT_TRACE_t *)0x50703000);

} // _CORESIGHT_TRACE_

// USB FS/LS controller device registers
namespace _USB_  {

    // Device address and endpoint control
    // Reset value: 0x00000000
    BEGIN_TYPE(ADDR_ENDP_t, uint32_t)
        // Device endpoint to send data to. Only valid for HOST mode.
        ADD_BITFIELD_RW(ENDPOINT, 16, 4)
        // In device mode, the address that the device should respond to. Set in response to a SET_ADDR setup packet from the host. In host mode set to the address of the device to communicate with.
        ADD_BITFIELD_RW(ADDRESS, 0, 7)
    END_TYPE()

    // Interrupt endpoint 1. Only valid for HOST mode.
    // Reset value: 0x00000000
    BEGIN_TYPE(ADDR_ENDP__t, uint32_t)
        // Interrupt EP requires preamble (is a low speed device on a full speed hub)
        ADD_BITFIELD_RW(INTEP_PREAMBLE, 26, 1)
        // Direction of the interrupt endpoint. In=0, Out=1
        ADD_BITFIELD_RW(INTEP_DIR, 25, 1)
        // Endpoint number of the interrupt endpoint
        ADD_BITFIELD_RW(ENDPOINT, 16, 4)
        // Device address
        ADD_BITFIELD_RW(ADDRESS, 0, 7)
    END_TYPE()

    // Main control register
    // Reset value: 0x00000004
    BEGIN_TYPE(MAIN_CTRL_t, uint32_t)
        // Reduced timings for simulation
        ADD_BITFIELD_RW(SIM_TIMING, 31, 1)
        // Isolates USB phy after controller power-up  Remove isolation once software has configured the controller  Not isolated = 0, Isolated = 1
        ADD_BITFIELD_RW(PHY_ISO, 2, 1)
        // Device mode = 0, Host mode = 1
        ADD_BITFIELD_RW(HOST_NDEVICE, 1, 1)
        // Enable controller
        ADD_BITFIELD_RW(CONTROLLER_EN, 0, 1)
    END_TYPE()

    // Set the SOF (Start of Frame) frame number in the host controller. The SOF packet is sent every 1ms and the host will increment the frame number by 1 each time.
    // Reset value: 0x00000000
    BEGIN_TYPE(SOF_WR_t, uint32_t)
        ADD_BITFIELD_WO(COUNT, 0, 11)
    END_TYPE()

    // Read the last SOF (Start of Frame) frame number seen. In device mode the last SOF received from the host. In host mode the last SOF sent by the host.
    // Reset value: 0x00000000
    BEGIN_TYPE(SOF_RD_t, uint32_t)
        ADD_BITFIELD_RO(COUNT, 0, 11)
    END_TYPE()

    // SIE control register
    // Reset value: 0x00008000
    BEGIN_TYPE(SIE_CTRL_t, uint32_t)
        // Device: Set bit in EP_STATUS_STALL_NAK when EP0 sends a STALL
        ADD_BITFIELD_RW(EP0_INT_STALL, 31, 1)
        // Device: EP0 single buffered = 0, double buffered = 1
        ADD_BITFIELD_RW(EP0_DOUBLE_BUF, 30, 1)
        // Device: Set bit in BUFF_STATUS for every buffer completed on EP0
        ADD_BITFIELD_RW(EP0_INT_1BUF, 29, 1)
        // Device: Set bit in BUFF_STATUS for every 2 buffers completed on EP0
        ADD_BITFIELD_RW(EP0_INT_2BUF, 28, 1)
        // Device: Set bit in EP_STATUS_STALL_NAK when EP0 sends a NAK
        ADD_BITFIELD_RW(EP0_INT_NAK, 27, 1)
        // Direct bus drive enable
        ADD_BITFIELD_RW(DIRECT_EN, 26, 1)
        // Direct control of DP
        ADD_BITFIELD_RW(DIRECT_DP, 25, 1)
        // Direct control of DM
        ADD_BITFIELD_RW(DIRECT_DM, 24, 1)
        // Device: Stop EP0 on a short packet.
        ADD_BITFIELD_RW(EP0_STOP_ON_SHORT_PACKET, 19, 1)
        // Power down bus transceiver
        ADD_BITFIELD_RW(TRANSCEIVER_PD, 18, 1)
        // Device: Pull-up strength (0=1K2, 1=2k3)
        ADD_BITFIELD_RW(RPU_OPT, 17, 1)
        // Device: Enable pull up resistor
        ADD_BITFIELD_RW(PULLUP_EN, 16, 1)
        // Host: Enable pull down resistors
        ADD_BITFIELD_RW(PULLDOWN_EN, 15, 1)
        // Host: Reset bus
        ADD_BITFIELD_WO(RESET_BUS, 13, 1)
        // Device: Remote wakeup. Device can initiate its own resume after suspend.
        ADD_BITFIELD_WO(RESUME, 12, 1)
        // Host: Enable VBUS
        ADD_BITFIELD_RW(VBUS_EN, 11, 1)
        // Host: Enable keep alive packet (for low speed bus)
        ADD_BITFIELD_RW(KEEP_ALIVE_EN, 10, 1)
        // Host: Enable SOF generation (for full speed bus)
        ADD_BITFIELD_RW(SOF_EN, 9, 1)
        // Host: Delay packet(s) until after SOF
        ADD_BITFIELD_RW(SOF_SYNC, 8, 1)
        // Host: Preable enable for LS device on FS hub
        ADD_BITFIELD_RW(PREAMBLE_EN, 6, 1)
        // Host: Stop transaction
        ADD_BITFIELD_WO(STOP_TRANS, 4, 1)
        // Host: Receive transaction (IN to host)
        ADD_BITFIELD_RW(RECEIVE_DATA, 3, 1)
        // Host: Send transaction (OUT from host)
        ADD_BITFIELD_RW(SEND_DATA, 2, 1)
        // Host: Send Setup packet
        ADD_BITFIELD_RW(SEND_SETUP, 1, 1)
        // Host: Start transaction
        ADD_BITFIELD_WO(START_TRANS, 0, 1)
    END_TYPE()

    // SIE status register
    // Reset value: 0x00000000
    BEGIN_TYPE(SIE_STATUS_t, uint32_t)
        // Data Sequence Error. The device can raise a sequence error in the following conditions: * A SETUP packet is received followed by a DATA1 packet (data phase should always be DATA0) * An OUT packet is received from the host but doesn't match the data pid in the buffer control register read from DPSRAM The host can raise a data sequence error in the following conditions: * An IN packet from the device has the wrong data PID
        ADD_BITFIELD_RW(DATA_SEQ_ERROR, 31, 1)
        // ACK received. Raised by both host and device.
        ADD_BITFIELD_RW(ACK_REC, 30, 1)
        // Host: STALL received
        ADD_BITFIELD_RW(STALL_REC, 29, 1)
        // Host: NAK received
        ADD_BITFIELD_RW(NAK_REC, 28, 1)
        // RX timeout is raised by both the host and device if an ACK is not received in the maximum time specified by the USB spec.
        ADD_BITFIELD_RW(RX_TIMEOUT, 27, 1)
        // RX overflow is raised by the Serial RX engine if the incoming data is too fast.
        ADD_BITFIELD_RW(RX_OVERFLOW, 26, 1)
        // Bit Stuff Error. Raised by the Serial RX engine.
        ADD_BITFIELD_RW(BIT_STUFF_ERROR, 25, 1)
        // CRC Error. Raised by the Serial RX engine.
        ADD_BITFIELD_RW(CRC_ERROR, 24, 1)
        // An endpoint has encountered an error. Read the ep_rx_error and ep_tx_error registers to find out which endpoint had an error.
        ADD_BITFIELD_RW(ENDPOINT_ERROR, 23, 1)
        // Device: bus reset received
        ADD_BITFIELD_RW(BUS_RESET, 19, 1)
        // Transaction complete. Raised by device if: * An IN or OUT packet is sent with the `LAST_BUFF` bit set in the buffer control register Raised by host if: * A setup packet is sent when no data in or data out transaction follows * An IN packet is received and the `LAST_BUFF` bit is set in the buffer control register * An IN packet is received with zero length * An OUT packet is sent and the `LAST_BUFF` bit is set
        ADD_BITFIELD_RW(TRANS_COMPLETE, 18, 1)
        // Device: Setup packet received
        ADD_BITFIELD_RW(SETUP_REC, 17, 1)
        // Device: connected
        ADD_BITFIELD_RO(CONNECTED, 16, 1)
        // Device or Host has received a short packet. This is when the data received is less than configured in the buffer control register. Device: If using double buffered mode on device the buffer select will not be toggled after writing status back to the buffer control register. This is to prevent any further transactions on that endpoint until the user has reset the buffer control registers. Host: the current transfer will be stopped early.
        ADD_BITFIELD_RW(RX_SHORT_PACKET, 12, 1)
        // Host: Device has initiated a remote resume. Device: host has initiated a resume.
        ADD_BITFIELD_RW(RESUME, 11, 1)
        // VBUS over current detected
        ADD_BITFIELD_RO(VBUS_OVER_CURR, 10, 1)
        // Host: device speed. Disconnected = 00, LS = 01, FS = 10
        ADD_BITFIELD_RO(SPEED, 8, 2)
        // Bus in suspended state. Valid for device. Device will go into suspend if neither Keep Alive / SOF frames are enabled.
        ADD_BITFIELD_RW(SUSPENDED, 4, 1)
        // USB bus line state
        ADD_BITFIELD_RO(LINE_STATE, 2, 2)
        // Device: VBUS Detected
        ADD_BITFIELD_RO(VBUS_DETECTED, 0, 1)
    END_TYPE()

    // interrupt endpoint control register
    // Reset value: 0x00000000
    BEGIN_TYPE(INT_EP_CTRL_t, uint32_t)
        // Host: Enable interrupt endpoint 1 -> 15
        ADD_BITFIELD_RW(INT_EP_ACTIVE, 1, 15)
    END_TYPE()

    // Buffer status register. A bit set here indicates that a buffer has completed on the endpoint (if the buffer interrupt is enabled). It is possible for 2 buffers to be completed, so clearing the buffer status bit may instantly re set it on the next clock cycle.
    // Reset value: 0x00000000
    BEGIN_TYPE(BUFF_STATUS_t, uint32_t)
        ADD_BITFIELD_RW(EP15_OUT, 31, 1)
        ADD_BITFIELD_RW(EP15_IN, 30, 1)
        ADD_BITFIELD_RW(EP14_OUT, 29, 1)
        ADD_BITFIELD_RW(EP14_IN, 28, 1)
        ADD_BITFIELD_RW(EP13_OUT, 27, 1)
        ADD_BITFIELD_RW(EP13_IN, 26, 1)
        ADD_BITFIELD_RW(EP12_OUT, 25, 1)
        ADD_BITFIELD_RW(EP12_IN, 24, 1)
        ADD_BITFIELD_RW(EP11_OUT, 23, 1)
        ADD_BITFIELD_RW(EP11_IN, 22, 1)
        ADD_BITFIELD_RW(EP10_OUT, 21, 1)
        ADD_BITFIELD_RW(EP10_IN, 20, 1)
        ADD_BITFIELD_RW(EP9_OUT, 19, 1)
        ADD_BITFIELD_RW(EP9_IN, 18, 1)
        ADD_BITFIELD_RW(EP8_OUT, 17, 1)
        ADD_BITFIELD_RW(EP8_IN, 16, 1)
        ADD_BITFIELD_RW(EP7_OUT, 15, 1)
        ADD_BITFIELD_RW(EP7_IN, 14, 1)
        ADD_BITFIELD_RW(EP6_OUT, 13, 1)
        ADD_BITFIELD_RW(EP6_IN, 12, 1)
        ADD_BITFIELD_RW(EP5_OUT, 11, 1)
        ADD_BITFIELD_RW(EP5_IN, 10, 1)
        ADD_BITFIELD_RW(EP4_OUT, 9, 1)
        ADD_BITFIELD_RW(EP4_IN, 8, 1)
        ADD_BITFIELD_RW(EP3_OUT, 7, 1)
        ADD_BITFIELD_RW(EP3_IN, 6, 1)
        ADD_BITFIELD_RW(EP2_OUT, 5, 1)
        ADD_BITFIELD_RW(EP2_IN, 4, 1)
        ADD_BITFIELD_RW(EP1_OUT, 3, 1)
        ADD_BITFIELD_RW(EP1_IN, 2, 1)
        ADD_BITFIELD_RW(EP0_OUT, 1, 1)
        ADD_BITFIELD_RW(EP0_IN, 0, 1)
    END_TYPE()

    // Which of the double buffers should be handled. Only valid if using an interrupt per buffer (i.e. not per 2 buffers). Not valid for host interrupt endpoint polling because they are only single buffered.
    // Reset value: 0x00000000
    BEGIN_TYPE(BUFF_CPU_SHOULD_HANDLE_t, uint32_t)
        ADD_BITFIELD_RO(EP15_OUT, 31, 1)
        ADD_BITFIELD_RO(EP15_IN, 30, 1)
        ADD_BITFIELD_RO(EP14_OUT, 29, 1)
        ADD_BITFIELD_RO(EP14_IN, 28, 1)
        ADD_BITFIELD_RO(EP13_OUT, 27, 1)
        ADD_BITFIELD_RO(EP13_IN, 26, 1)
        ADD_BITFIELD_RO(EP12_OUT, 25, 1)
        ADD_BITFIELD_RO(EP12_IN, 24, 1)
        ADD_BITFIELD_RO(EP11_OUT, 23, 1)
        ADD_BITFIELD_RO(EP11_IN, 22, 1)
        ADD_BITFIELD_RO(EP10_OUT, 21, 1)
        ADD_BITFIELD_RO(EP10_IN, 20, 1)
        ADD_BITFIELD_RO(EP9_OUT, 19, 1)
        ADD_BITFIELD_RO(EP9_IN, 18, 1)
        ADD_BITFIELD_RO(EP8_OUT, 17, 1)
        ADD_BITFIELD_RO(EP8_IN, 16, 1)
        ADD_BITFIELD_RO(EP7_OUT, 15, 1)
        ADD_BITFIELD_RO(EP7_IN, 14, 1)
        ADD_BITFIELD_RO(EP6_OUT, 13, 1)
        ADD_BITFIELD_RO(EP6_IN, 12, 1)
        ADD_BITFIELD_RO(EP5_OUT, 11, 1)
        ADD_BITFIELD_RO(EP5_IN, 10, 1)
        ADD_BITFIELD_RO(EP4_OUT, 9, 1)
        ADD_BITFIELD_RO(EP4_IN, 8, 1)
        ADD_BITFIELD_RO(EP3_OUT, 7, 1)
        ADD_BITFIELD_RO(EP3_IN, 6, 1)
        ADD_BITFIELD_RO(EP2_OUT, 5, 1)
        ADD_BITFIELD_RO(EP2_IN, 4, 1)
        ADD_BITFIELD_RO(EP1_OUT, 3, 1)
        ADD_BITFIELD_RO(EP1_IN, 2, 1)
        ADD_BITFIELD_RO(EP0_OUT, 1, 1)
        ADD_BITFIELD_RO(EP0_IN, 0, 1)
    END_TYPE()

    // Device only: Can be set to ignore the buffer control register for this endpoint in case you would like to revoke a buffer. A NAK will be sent for every access to the endpoint until this bit is cleared. A corresponding bit in `EP_ABORT_DONE` is set when it is safe to modify the buffer control register.
    // Reset value: 0x00000000
    BEGIN_TYPE(EP_ABORT_t, uint32_t)
        ADD_BITFIELD_RW(EP15_OUT, 31, 1)
        ADD_BITFIELD_RW(EP15_IN, 30, 1)
        ADD_BITFIELD_RW(EP14_OUT, 29, 1)
        ADD_BITFIELD_RW(EP14_IN, 28, 1)
        ADD_BITFIELD_RW(EP13_OUT, 27, 1)
        ADD_BITFIELD_RW(EP13_IN, 26, 1)
        ADD_BITFIELD_RW(EP12_OUT, 25, 1)
        ADD_BITFIELD_RW(EP12_IN, 24, 1)
        ADD_BITFIELD_RW(EP11_OUT, 23, 1)
        ADD_BITFIELD_RW(EP11_IN, 22, 1)
        ADD_BITFIELD_RW(EP10_OUT, 21, 1)
        ADD_BITFIELD_RW(EP10_IN, 20, 1)
        ADD_BITFIELD_RW(EP9_OUT, 19, 1)
        ADD_BITFIELD_RW(EP9_IN, 18, 1)
        ADD_BITFIELD_RW(EP8_OUT, 17, 1)
        ADD_BITFIELD_RW(EP8_IN, 16, 1)
        ADD_BITFIELD_RW(EP7_OUT, 15, 1)
        ADD_BITFIELD_RW(EP7_IN, 14, 1)
        ADD_BITFIELD_RW(EP6_OUT, 13, 1)
        ADD_BITFIELD_RW(EP6_IN, 12, 1)
        ADD_BITFIELD_RW(EP5_OUT, 11, 1)
        ADD_BITFIELD_RW(EP5_IN, 10, 1)
        ADD_BITFIELD_RW(EP4_OUT, 9, 1)
        ADD_BITFIELD_RW(EP4_IN, 8, 1)
        ADD_BITFIELD_RW(EP3_OUT, 7, 1)
        ADD_BITFIELD_RW(EP3_IN, 6, 1)
        ADD_BITFIELD_RW(EP2_OUT, 5, 1)
        ADD_BITFIELD_RW(EP2_IN, 4, 1)
        ADD_BITFIELD_RW(EP1_OUT, 3, 1)
        ADD_BITFIELD_RW(EP1_IN, 2, 1)
        ADD_BITFIELD_RW(EP0_OUT, 1, 1)
        ADD_BITFIELD_RW(EP0_IN, 0, 1)
    END_TYPE()

    // Device only: Used in conjunction with `EP_ABORT`. Set once an endpoint is idle so the programmer knows it is safe to modify the buffer control register.
    // Reset value: 0x00000000
    BEGIN_TYPE(EP_ABORT_DONE_t, uint32_t)
        ADD_BITFIELD_RW(EP15_OUT, 31, 1)
        ADD_BITFIELD_RW(EP15_IN, 30, 1)
        ADD_BITFIELD_RW(EP14_OUT, 29, 1)
        ADD_BITFIELD_RW(EP14_IN, 28, 1)
        ADD_BITFIELD_RW(EP13_OUT, 27, 1)
        ADD_BITFIELD_RW(EP13_IN, 26, 1)
        ADD_BITFIELD_RW(EP12_OUT, 25, 1)
        ADD_BITFIELD_RW(EP12_IN, 24, 1)
        ADD_BITFIELD_RW(EP11_OUT, 23, 1)
        ADD_BITFIELD_RW(EP11_IN, 22, 1)
        ADD_BITFIELD_RW(EP10_OUT, 21, 1)
        ADD_BITFIELD_RW(EP10_IN, 20, 1)
        ADD_BITFIELD_RW(EP9_OUT, 19, 1)
        ADD_BITFIELD_RW(EP9_IN, 18, 1)
        ADD_BITFIELD_RW(EP8_OUT, 17, 1)
        ADD_BITFIELD_RW(EP8_IN, 16, 1)
        ADD_BITFIELD_RW(EP7_OUT, 15, 1)
        ADD_BITFIELD_RW(EP7_IN, 14, 1)
        ADD_BITFIELD_RW(EP6_OUT, 13, 1)
        ADD_BITFIELD_RW(EP6_IN, 12, 1)
        ADD_BITFIELD_RW(EP5_OUT, 11, 1)
        ADD_BITFIELD_RW(EP5_IN, 10, 1)
        ADD_BITFIELD_RW(EP4_OUT, 9, 1)
        ADD_BITFIELD_RW(EP4_IN, 8, 1)
        ADD_BITFIELD_RW(EP3_OUT, 7, 1)
        ADD_BITFIELD_RW(EP3_IN, 6, 1)
        ADD_BITFIELD_RW(EP2_OUT, 5, 1)
        ADD_BITFIELD_RW(EP2_IN, 4, 1)
        ADD_BITFIELD_RW(EP1_OUT, 3, 1)
        ADD_BITFIELD_RW(EP1_IN, 2, 1)
        ADD_BITFIELD_RW(EP0_OUT, 1, 1)
        ADD_BITFIELD_RW(EP0_IN, 0, 1)
    END_TYPE()

    // Device: this bit must be set in conjunction with the `STALL` bit in the buffer control register to send a STALL on EP0. The device controller clears these bits when a SETUP packet is received because the USB spec requires that a STALL condition is cleared when a SETUP packet is received.
    // Reset value: 0x00000000
    BEGIN_TYPE(EP_STALL_ARM_t, uint32_t)
        ADD_BITFIELD_RW(EP0_OUT, 1, 1)
        ADD_BITFIELD_RW(EP0_IN, 0, 1)
    END_TYPE()

    // Used by the host controller. Sets the wait time in microseconds before trying again if the device replies with a NAK.
    // Reset value: 0x00100010
    BEGIN_TYPE(NAK_POLL_t, uint32_t)
        // Bits 9:6 of nak_retry count
        ADD_BITFIELD_RO(RETRY_COUNT_HI, 28, 4)
        // EPX polling has stopped because a nak was received
        ADD_BITFIELD_RW(EPX_STOPPED_ON_NAK, 27, 1)
        // Stop polling epx when a nak is received
        ADD_BITFIELD_RW(STOP_EPX_ON_NAK, 26, 1)
        // NAK polling interval for a full speed device
        ADD_BITFIELD_RW(DELAY_FS, 16, 10)
        // Bits 5:0 of nak_retry_count
        ADD_BITFIELD_RO(RETRY_COUNT_LO, 10, 6)
        // NAK polling interval for a low speed device
        ADD_BITFIELD_RW(DELAY_LS, 0, 10)
    END_TYPE()

    // Device: bits are set when the `IRQ_ON_NAK` or `IRQ_ON_STALL` bits are set. For EP0 this comes from `SIE_CTRL`. For all other endpoints it comes from the endpoint control register.
    // Reset value: 0x00000000
    BEGIN_TYPE(EP_STATUS_STALL_NAK_t, uint32_t)
        ADD_BITFIELD_RW(EP15_OUT, 31, 1)
        ADD_BITFIELD_RW(EP15_IN, 30, 1)
        ADD_BITFIELD_RW(EP14_OUT, 29, 1)
        ADD_BITFIELD_RW(EP14_IN, 28, 1)
        ADD_BITFIELD_RW(EP13_OUT, 27, 1)
        ADD_BITFIELD_RW(EP13_IN, 26, 1)
        ADD_BITFIELD_RW(EP12_OUT, 25, 1)
        ADD_BITFIELD_RW(EP12_IN, 24, 1)
        ADD_BITFIELD_RW(EP11_OUT, 23, 1)
        ADD_BITFIELD_RW(EP11_IN, 22, 1)
        ADD_BITFIELD_RW(EP10_OUT, 21, 1)
        ADD_BITFIELD_RW(EP10_IN, 20, 1)
        ADD_BITFIELD_RW(EP9_OUT, 19, 1)
        ADD_BITFIELD_RW(EP9_IN, 18, 1)
        ADD_BITFIELD_RW(EP8_OUT, 17, 1)
        ADD_BITFIELD_RW(EP8_IN, 16, 1)
        ADD_BITFIELD_RW(EP7_OUT, 15, 1)
        ADD_BITFIELD_RW(EP7_IN, 14, 1)
        ADD_BITFIELD_RW(EP6_OUT, 13, 1)
        ADD_BITFIELD_RW(EP6_IN, 12, 1)
        ADD_BITFIELD_RW(EP5_OUT, 11, 1)
        ADD_BITFIELD_RW(EP5_IN, 10, 1)
        ADD_BITFIELD_RW(EP4_OUT, 9, 1)
        ADD_BITFIELD_RW(EP4_IN, 8, 1)
        ADD_BITFIELD_RW(EP3_OUT, 7, 1)
        ADD_BITFIELD_RW(EP3_IN, 6, 1)
        ADD_BITFIELD_RW(EP2_OUT, 5, 1)
        ADD_BITFIELD_RW(EP2_IN, 4, 1)
        ADD_BITFIELD_RW(EP1_OUT, 3, 1)
        ADD_BITFIELD_RW(EP1_IN, 2, 1)
        ADD_BITFIELD_RW(EP0_OUT, 1, 1)
        ADD_BITFIELD_RW(EP0_IN, 0, 1)
    END_TYPE()

    // Where to connect the USB controller. Should be to_phy by default.
    // Reset value: 0x00000001
    BEGIN_TYPE(USB_MUXING_t, uint32_t)
        // Swap the USB PHY DP and DM pins and all related controls and flip receive differential data. Can be used to switch USB DP/DP on the PCB.  This is done at a low level so overrides all other controls.
        ADD_BITFIELD_RW(SWAP_DPDM, 31, 1)
        // Use the usb DP and DM pins as GPIO pins instead of connecting them to the USB controller.
        ADD_BITFIELD_RW(USBPHY_AS_GPIO, 4, 1)
        ADD_BITFIELD_RW(SOFTCON, 3, 1)
        ADD_BITFIELD_RW(TO_DIGITAL_PAD, 2, 1)
        ADD_BITFIELD_RW(TO_EXTPHY, 1, 1)
        ADD_BITFIELD_RW(TO_PHY, 0, 1)
    END_TYPE()

    // Overrides for the power signals in the event that the VBUS signals are not hooked up to GPIO. Set the value of the override and then the override enable to switch over to the override value.
    // Reset value: 0x00000000
    BEGIN_TYPE(USB_PWR_t, uint32_t)
        ADD_BITFIELD_RW(OVERCURR_DETECT_EN, 5, 1)
        ADD_BITFIELD_RW(OVERCURR_DETECT, 4, 1)
        ADD_BITFIELD_RW(VBUS_DETECT_OVERRIDE_EN, 3, 1)
        ADD_BITFIELD_RW(VBUS_DETECT, 2, 1)
        ADD_BITFIELD_RW(VBUS_EN_OVERRIDE_EN, 1, 1)
        ADD_BITFIELD_RW(VBUS_EN, 0, 1)
    END_TYPE()

    // This register allows for direct control of the USB phy. Use in conjunction with usbphy_direct_override register to enable each override bit.
    // Reset value: 0x00000000
    BEGIN_TYPE(USBPHY_DIRECT_t, uint32_t)
        // Override rx_dm value into controller
        ADD_BITFIELD_RW(RX_DM_OVERRIDE, 25, 1)
        // Override rx_dp value into controller
        ADD_BITFIELD_RW(RX_DP_OVERRIDE, 24, 1)
        // Override rx_dd value into controller
        ADD_BITFIELD_RW(RX_DD_OVERRIDE, 23, 1)
        // DM over voltage
        ADD_BITFIELD_RO(DM_OVV, 22, 1)
        // DP over voltage
        ADD_BITFIELD_RO(DP_OVV, 21, 1)
        // DM overcurrent
        ADD_BITFIELD_RO(DM_OVCN, 20, 1)
        // DP overcurrent
        ADD_BITFIELD_RO(DP_OVCN, 19, 1)
        // DPM pin state
        ADD_BITFIELD_RO(RX_DM, 18, 1)
        // DPP pin state
        ADD_BITFIELD_RO(RX_DP, 17, 1)
        // Differential RX
        ADD_BITFIELD_RO(RX_DD, 16, 1)
        // TX_DIFFMODE=0: Single ended mode  TX_DIFFMODE=1: Differential drive mode (TX_DM, TX_DM_OE ignored)
        ADD_BITFIELD_RW(TX_DIFFMODE, 15, 1)
        // TX_FSSLEW=0: Low speed slew rate  TX_FSSLEW=1: Full speed slew rate
        ADD_BITFIELD_RW(TX_FSSLEW, 14, 1)
        // TX power down override (if override enable is set). 1 = powered down.
        ADD_BITFIELD_RW(TX_PD, 13, 1)
        // RX power down override (if override enable is set). 1 = powered down.
        ADD_BITFIELD_RW(RX_PD, 12, 1)
        // Output data. TX_DIFFMODE=1, Ignored  TX_DIFFMODE=0, Drives DPM only. TX_DM_OE=1 to enable drive. DPM=TX_DM
        ADD_BITFIELD_RW(TX_DM, 11, 1)
        // Output data. If TX_DIFFMODE=1, Drives DPP/DPM diff pair. TX_DP_OE=1 to enable drive. DPP=TX_DP, DPM=~TX_DP  If TX_DIFFMODE=0, Drives DPP only. TX_DP_OE=1 to enable drive. DPP=TX_DP
        ADD_BITFIELD_RW(TX_DP, 10, 1)
        // Output enable. If TX_DIFFMODE=1, Ignored.  If TX_DIFFMODE=0, OE for DPM only. 0 - DPM in Hi-Z state; 1 - DPM driving
        ADD_BITFIELD_RW(TX_DM_OE, 9, 1)
        // Output enable. If TX_DIFFMODE=1, OE for DPP/DPM diff pair. 0 - DPP/DPM in Hi-Z state; 1 - DPP/DPM driving  If TX_DIFFMODE=0, OE for DPP only. 0 - DPP in Hi-Z state; 1 - DPP driving
        ADD_BITFIELD_RW(TX_DP_OE, 8, 1)
        // DM pull down enable
        ADD_BITFIELD_RW(DM_PULLDN_EN, 6, 1)
        // DM pull up enable
        ADD_BITFIELD_RW(DM_PULLUP_EN, 5, 1)
        // Enable the second DM pull up resistor. 0 - Pull = Rpu2; 1 - Pull = Rpu1 + Rpu2
        ADD_BITFIELD_RW(DM_PULLUP_HISEL, 4, 1)
        // DP pull down enable
        ADD_BITFIELD_RW(DP_PULLDN_EN, 2, 1)
        // DP pull up enable
        ADD_BITFIELD_RW(DP_PULLUP_EN, 1, 1)
        // Enable the second DP pull up resistor. 0 - Pull = Rpu2; 1 - Pull = Rpu1 + Rpu2
        ADD_BITFIELD_RW(DP_PULLUP_HISEL, 0, 1)
    END_TYPE()

    // Override enable for each control in usbphy_direct
    // Reset value: 0x00000000
    BEGIN_TYPE(USBPHY_DIRECT_OVERRIDE_t, uint32_t)
        ADD_BITFIELD_RW(RX_DM_OVERRIDE_EN, 18, 1)
        ADD_BITFIELD_RW(RX_DP_OVERRIDE_EN, 17, 1)
        ADD_BITFIELD_RW(RX_DD_OVERRIDE_EN, 16, 1)
        ADD_BITFIELD_RW(TX_DIFFMODE_OVERRIDE_EN, 15, 1)
        ADD_BITFIELD_RW(DM_PULLUP_OVERRIDE_EN, 12, 1)
        ADD_BITFIELD_RW(TX_FSSLEW_OVERRIDE_EN, 11, 1)
        ADD_BITFIELD_RW(TX_PD_OVERRIDE_EN, 10, 1)
        ADD_BITFIELD_RW(RX_PD_OVERRIDE_EN, 9, 1)
        ADD_BITFIELD_RW(TX_DM_OVERRIDE_EN, 8, 1)
        ADD_BITFIELD_RW(TX_DP_OVERRIDE_EN, 7, 1)
        ADD_BITFIELD_RW(TX_DM_OE_OVERRIDE_EN, 6, 1)
        ADD_BITFIELD_RW(TX_DP_OE_OVERRIDE_EN, 5, 1)
        ADD_BITFIELD_RW(DM_PULLDN_EN_OVERRIDE_EN, 4, 1)
        ADD_BITFIELD_RW(DP_PULLDN_EN_OVERRIDE_EN, 3, 1)
        ADD_BITFIELD_RW(DP_PULLUP_EN_OVERRIDE_EN, 2, 1)
        ADD_BITFIELD_RW(DM_PULLUP_HISEL_OVERRIDE_EN, 1, 1)
        ADD_BITFIELD_RW(DP_PULLUP_HISEL_OVERRIDE_EN, 0, 1)
    END_TYPE()

    // Used to adjust trim values of USB phy pull down resistors.
    // Reset value: 0x00001f1f
    BEGIN_TYPE(USBPHY_TRIM_t, uint32_t)
        // Value to drive to USB PHY  DM pulldown resistor trim control  Experimental data suggests that the reset value will work, but this register allows adjustment if required
        ADD_BITFIELD_RW(DM_PULLDN_TRIM, 8, 5)
        // Value to drive to USB PHY  DP pulldown resistor trim control  Experimental data suggests that the reset value will work, but this register allows adjustment if required
        ADD_BITFIELD_RW(DP_PULLDN_TRIM, 0, 5)
    END_TYPE()

    // Used for debug only.
    // Reset value: 0x000000f8
    BEGIN_TYPE(LINESTATE_TUNING_t, uint32_t)
        ADD_BITFIELD_RW(SPARE_FIX, 8, 4)
        // Device - exit suspend on any non-idle signalling, not qualified with a 1ms timer
        ADD_BITFIELD_RW(DEV_LS_WAKE_FIX, 7, 1)
        // Device - suppress repeated errors until the device FSM is next in the process of decoding an inbound packet.
        ADD_BITFIELD_RW(DEV_RX_ERR_QUIESCE, 6, 1)
        // RX - when recovering from line chatter or bitstuff errors, treat SE0 as the end of chatter as well as  8 consecutive idle bits.
        ADD_BITFIELD_RW(SIE_RX_CHATTER_SE0_FIX, 5, 1)
        // RX - when a bitstuff error is signalled by rx_dasm, unconditionally terminate RX decode to  avoid a hang during certain packet phases.
        ADD_BITFIELD_RW(SIE_RX_BITSTUFF_FIX, 4, 1)
        // Device - the controller FSM performs two reads of the buffer status memory address to  avoid sampling metastable data. An enabled buffer is only used if both reads match.
        ADD_BITFIELD_RW(DEV_BUFF_CONTROL_DOUBLE_READ_FIX, 3, 1)
        // Host - increase inter-packet and turnaround timeouts to accommodate worst-case hub delays.
        ADD_BITFIELD_RW(MULTI_HUB_FIX, 2, 1)
        // Device/Host - add an extra 1-bit debounce of linestate sampling.
        ADD_BITFIELD_RW(LINESTATE_DELAY, 1, 1)
        // Device - register the received data to account for hub bit dribble before EOP. Only affects certain hubs.
        ADD_BITFIELD_RW(RCV_DELAY, 0, 1)
    END_TYPE()

    // Raw Interrupts
    // Reset value: 0x00000000
    BEGIN_TYPE(INTR_t, uint32_t)
        // Source: NAK_POLL.EPX_STOPPED_ON_NAK
        ADD_BITFIELD_RO(EPX_STOPPED_ON_NAK, 23, 1)
        // Source: DEV_SM_WATCHDOG.FIRED
        ADD_BITFIELD_RO(DEV_SM_WATCHDOG_FIRED, 22, 1)
        // Source: SIE_STATUS.ENDPOINT_ERROR
        ADD_BITFIELD_RO(ENDPOINT_ERROR, 21, 1)
        // Source: SIE_STATUS.RX_SHORT_PACKET
        ADD_BITFIELD_RO(RX_SHORT_PACKET, 20, 1)
        // Raised when any bit in EP_STATUS_STALL_NAK is set. Clear by clearing all bits in EP_STATUS_STALL_NAK.
        ADD_BITFIELD_RO(EP_STALL_NAK, 19, 1)
        // Raised when any bit in ABORT_DONE is set. Clear by clearing all bits in ABORT_DONE.
        ADD_BITFIELD_RO(ABORT_DONE, 18, 1)
        // Set every time the device receives a SOF (Start of Frame) packet. Cleared by reading SOF_RD
        ADD_BITFIELD_RO(DEV_SOF, 17, 1)
        // Device. Source: SIE_STATUS.SETUP_REC
        ADD_BITFIELD_RO(SETUP_REQ, 16, 1)
        // Set when the device receives a resume from the host. Cleared by writing to SIE_STATUS.RESUME
        ADD_BITFIELD_RO(DEV_RESUME_FROM_HOST, 15, 1)
        // Set when the device suspend state changes. Cleared by writing to SIE_STATUS.SUSPENDED
        ADD_BITFIELD_RO(DEV_SUSPEND, 14, 1)
        // Set when the device connection state changes. Cleared by writing to SIE_STATUS.CONNECTED
        ADD_BITFIELD_RO(DEV_CONN_DIS, 13, 1)
        // Source: SIE_STATUS.BUS_RESET
        ADD_BITFIELD_RO(BUS_RESET, 12, 1)
        // Source: SIE_STATUS.VBUS_DETECTED
        ADD_BITFIELD_RO(VBUS_DETECT, 11, 1)
        // Source: SIE_STATUS.STALL_REC
        ADD_BITFIELD_RO(STALL, 10, 1)
        // Source: SIE_STATUS.CRC_ERROR
        ADD_BITFIELD_RO(ERROR_CRC, 9, 1)
        // Source: SIE_STATUS.BIT_STUFF_ERROR
        ADD_BITFIELD_RO(ERROR_BIT_STUFF, 8, 1)
        // Source: SIE_STATUS.RX_OVERFLOW
        ADD_BITFIELD_RO(ERROR_RX_OVERFLOW, 7, 1)
        // Source: SIE_STATUS.RX_TIMEOUT
        ADD_BITFIELD_RO(ERROR_RX_TIMEOUT, 6, 1)
        // Source: SIE_STATUS.DATA_SEQ_ERROR
        ADD_BITFIELD_RO(ERROR_DATA_SEQ, 5, 1)
        // Raised when any bit in BUFF_STATUS is set. Clear by clearing all bits in BUFF_STATUS.
        ADD_BITFIELD_RO(BUFF_STATUS, 4, 1)
        // Raised every time SIE_STATUS.TRANS_COMPLETE is set. Clear by writing to this bit.
        ADD_BITFIELD_RO(TRANS_COMPLETE, 3, 1)
        // Host: raised every time the host sends a SOF (Start of Frame). Cleared by reading SOF_RD
        ADD_BITFIELD_RO(HOST_SOF, 2, 1)
        // Host: raised when a device wakes up the host. Cleared by writing to SIE_STATUS.RESUME
        ADD_BITFIELD_RO(HOST_RESUME, 1, 1)
        // Host: raised when a device is connected or disconnected (i.e. when SIE_STATUS.SPEED changes). Cleared by writing to SIE_STATUS.SPEED
        ADD_BITFIELD_RO(HOST_CONN_DIS, 0, 1)
    END_TYPE()

    // Interrupt Enable
    // Reset value: 0x00000000
    BEGIN_TYPE(INTE_t, uint32_t)
        // Source: NAK_POLL.EPX_STOPPED_ON_NAK
        ADD_BITFIELD_RW(EPX_STOPPED_ON_NAK, 23, 1)
        // Source: DEV_SM_WATCHDOG.FIRED
        ADD_BITFIELD_RW(DEV_SM_WATCHDOG_FIRED, 22, 1)
        // Source: SIE_STATUS.ENDPOINT_ERROR
        ADD_BITFIELD_RW(ENDPOINT_ERROR, 21, 1)
        // Source: SIE_STATUS.RX_SHORT_PACKET
        ADD_BITFIELD_RW(RX_SHORT_PACKET, 20, 1)
        // Raised when any bit in EP_STATUS_STALL_NAK is set. Clear by clearing all bits in EP_STATUS_STALL_NAK.
        ADD_BITFIELD_RW(EP_STALL_NAK, 19, 1)
        // Raised when any bit in ABORT_DONE is set. Clear by clearing all bits in ABORT_DONE.
        ADD_BITFIELD_RW(ABORT_DONE, 18, 1)
        // Set every time the device receives a SOF (Start of Frame) packet. Cleared by reading SOF_RD
        ADD_BITFIELD_RW(DEV_SOF, 17, 1)
        // Device. Source: SIE_STATUS.SETUP_REC
        ADD_BITFIELD_RW(SETUP_REQ, 16, 1)
        // Set when the device receives a resume from the host. Cleared by writing to SIE_STATUS.RESUME
        ADD_BITFIELD_RW(DEV_RESUME_FROM_HOST, 15, 1)
        // Set when the device suspend state changes. Cleared by writing to SIE_STATUS.SUSPENDED
        ADD_BITFIELD_RW(DEV_SUSPEND, 14, 1)
        // Set when the device connection state changes. Cleared by writing to SIE_STATUS.CONNECTED
        ADD_BITFIELD_RW(DEV_CONN_DIS, 13, 1)
        // Source: SIE_STATUS.BUS_RESET
        ADD_BITFIELD_RW(BUS_RESET, 12, 1)
        // Source: SIE_STATUS.VBUS_DETECTED
        ADD_BITFIELD_RW(VBUS_DETECT, 11, 1)
        // Source: SIE_STATUS.STALL_REC
        ADD_BITFIELD_RW(STALL, 10, 1)
        // Source: SIE_STATUS.CRC_ERROR
        ADD_BITFIELD_RW(ERROR_CRC, 9, 1)
        // Source: SIE_STATUS.BIT_STUFF_ERROR
        ADD_BITFIELD_RW(ERROR_BIT_STUFF, 8, 1)
        // Source: SIE_STATUS.RX_OVERFLOW
        ADD_BITFIELD_RW(ERROR_RX_OVERFLOW, 7, 1)
        // Source: SIE_STATUS.RX_TIMEOUT
        ADD_BITFIELD_RW(ERROR_RX_TIMEOUT, 6, 1)
        // Source: SIE_STATUS.DATA_SEQ_ERROR
        ADD_BITFIELD_RW(ERROR_DATA_SEQ, 5, 1)
        // Raised when any bit in BUFF_STATUS is set. Clear by clearing all bits in BUFF_STATUS.
        ADD_BITFIELD_RW(BUFF_STATUS, 4, 1)
        // Raised every time SIE_STATUS.TRANS_COMPLETE is set. Clear by writing to this bit.
        ADD_BITFIELD_RW(TRANS_COMPLETE, 3, 1)
        // Host: raised every time the host sends a SOF (Start of Frame). Cleared by reading SOF_RD
        ADD_BITFIELD_RW(HOST_SOF, 2, 1)
        // Host: raised when a device wakes up the host. Cleared by writing to SIE_STATUS.RESUME
        ADD_BITFIELD_RW(HOST_RESUME, 1, 1)
        // Host: raised when a device is connected or disconnected (i.e. when SIE_STATUS.SPEED changes). Cleared by writing to SIE_STATUS.SPEED
        ADD_BITFIELD_RW(HOST_CONN_DIS, 0, 1)
    END_TYPE()

    // Interrupt Force
    // Reset value: 0x00000000
    BEGIN_TYPE(INTF_t, uint32_t)
        // Source: NAK_POLL.EPX_STOPPED_ON_NAK
        ADD_BITFIELD_RW(EPX_STOPPED_ON_NAK, 23, 1)
        // Source: DEV_SM_WATCHDOG.FIRED
        ADD_BITFIELD_RW(DEV_SM_WATCHDOG_FIRED, 22, 1)
        // Source: SIE_STATUS.ENDPOINT_ERROR
        ADD_BITFIELD_RW(ENDPOINT_ERROR, 21, 1)
        // Source: SIE_STATUS.RX_SHORT_PACKET
        ADD_BITFIELD_RW(RX_SHORT_PACKET, 20, 1)
        // Raised when any bit in EP_STATUS_STALL_NAK is set. Clear by clearing all bits in EP_STATUS_STALL_NAK.
        ADD_BITFIELD_RW(EP_STALL_NAK, 19, 1)
        // Raised when any bit in ABORT_DONE is set. Clear by clearing all bits in ABORT_DONE.
        ADD_BITFIELD_RW(ABORT_DONE, 18, 1)
        // Set every time the device receives a SOF (Start of Frame) packet. Cleared by reading SOF_RD
        ADD_BITFIELD_RW(DEV_SOF, 17, 1)
        // Device. Source: SIE_STATUS.SETUP_REC
        ADD_BITFIELD_RW(SETUP_REQ, 16, 1)
        // Set when the device receives a resume from the host. Cleared by writing to SIE_STATUS.RESUME
        ADD_BITFIELD_RW(DEV_RESUME_FROM_HOST, 15, 1)
        // Set when the device suspend state changes. Cleared by writing to SIE_STATUS.SUSPENDED
        ADD_BITFIELD_RW(DEV_SUSPEND, 14, 1)
        // Set when the device connection state changes. Cleared by writing to SIE_STATUS.CONNECTED
        ADD_BITFIELD_RW(DEV_CONN_DIS, 13, 1)
        // Source: SIE_STATUS.BUS_RESET
        ADD_BITFIELD_RW(BUS_RESET, 12, 1)
        // Source: SIE_STATUS.VBUS_DETECTED
        ADD_BITFIELD_RW(VBUS_DETECT, 11, 1)
        // Source: SIE_STATUS.STALL_REC
        ADD_BITFIELD_RW(STALL, 10, 1)
        // Source: SIE_STATUS.CRC_ERROR
        ADD_BITFIELD_RW(ERROR_CRC, 9, 1)
        // Source: SIE_STATUS.BIT_STUFF_ERROR
        ADD_BITFIELD_RW(ERROR_BIT_STUFF, 8, 1)
        // Source: SIE_STATUS.RX_OVERFLOW
        ADD_BITFIELD_RW(ERROR_RX_OVERFLOW, 7, 1)
        // Source: SIE_STATUS.RX_TIMEOUT
        ADD_BITFIELD_RW(ERROR_RX_TIMEOUT, 6, 1)
        // Source: SIE_STATUS.DATA_SEQ_ERROR
        ADD_BITFIELD_RW(ERROR_DATA_SEQ, 5, 1)
        // Raised when any bit in BUFF_STATUS is set. Clear by clearing all bits in BUFF_STATUS.
        ADD_BITFIELD_RW(BUFF_STATUS, 4, 1)
        // Raised every time SIE_STATUS.TRANS_COMPLETE is set. Clear by writing to this bit.
        ADD_BITFIELD_RW(TRANS_COMPLETE, 3, 1)
        // Host: raised every time the host sends a SOF (Start of Frame). Cleared by reading SOF_RD
        ADD_BITFIELD_RW(HOST_SOF, 2, 1)
        // Host: raised when a device wakes up the host. Cleared by writing to SIE_STATUS.RESUME
        ADD_BITFIELD_RW(HOST_RESUME, 1, 1)
        // Host: raised when a device is connected or disconnected (i.e. when SIE_STATUS.SPEED changes). Cleared by writing to SIE_STATUS.SPEED
        ADD_BITFIELD_RW(HOST_CONN_DIS, 0, 1)
    END_TYPE()

    // Interrupt status after masking & forcing
    // Reset value: 0x00000000
    BEGIN_TYPE(INTS_t, uint32_t)
        // Source: NAK_POLL.EPX_STOPPED_ON_NAK
        ADD_BITFIELD_RO(EPX_STOPPED_ON_NAK, 23, 1)
        // Source: DEV_SM_WATCHDOG.FIRED
        ADD_BITFIELD_RO(DEV_SM_WATCHDOG_FIRED, 22, 1)
        // Source: SIE_STATUS.ENDPOINT_ERROR
        ADD_BITFIELD_RO(ENDPOINT_ERROR, 21, 1)
        // Source: SIE_STATUS.RX_SHORT_PACKET
        ADD_BITFIELD_RO(RX_SHORT_PACKET, 20, 1)
        // Raised when any bit in EP_STATUS_STALL_NAK is set. Clear by clearing all bits in EP_STATUS_STALL_NAK.
        ADD_BITFIELD_RO(EP_STALL_NAK, 19, 1)
        // Raised when any bit in ABORT_DONE is set. Clear by clearing all bits in ABORT_DONE.
        ADD_BITFIELD_RO(ABORT_DONE, 18, 1)
        // Set every time the device receives a SOF (Start of Frame) packet. Cleared by reading SOF_RD
        ADD_BITFIELD_RO(DEV_SOF, 17, 1)
        // Device. Source: SIE_STATUS.SETUP_REC
        ADD_BITFIELD_RO(SETUP_REQ, 16, 1)
        // Set when the device receives a resume from the host. Cleared by writing to SIE_STATUS.RESUME
        ADD_BITFIELD_RO(DEV_RESUME_FROM_HOST, 15, 1)
        // Set when the device suspend state changes. Cleared by writing to SIE_STATUS.SUSPENDED
        ADD_BITFIELD_RO(DEV_SUSPEND, 14, 1)
        // Set when the device connection state changes. Cleared by writing to SIE_STATUS.CONNECTED
        ADD_BITFIELD_RO(DEV_CONN_DIS, 13, 1)
        // Source: SIE_STATUS.BUS_RESET
        ADD_BITFIELD_RO(BUS_RESET, 12, 1)
        // Source: SIE_STATUS.VBUS_DETECTED
        ADD_BITFIELD_RO(VBUS_DETECT, 11, 1)
        // Source: SIE_STATUS.STALL_REC
        ADD_BITFIELD_RO(STALL, 10, 1)
        // Source: SIE_STATUS.CRC_ERROR
        ADD_BITFIELD_RO(ERROR_CRC, 9, 1)
        // Source: SIE_STATUS.BIT_STUFF_ERROR
        ADD_BITFIELD_RO(ERROR_BIT_STUFF, 8, 1)
        // Source: SIE_STATUS.RX_OVERFLOW
        ADD_BITFIELD_RO(ERROR_RX_OVERFLOW, 7, 1)
        // Source: SIE_STATUS.RX_TIMEOUT
        ADD_BITFIELD_RO(ERROR_RX_TIMEOUT, 6, 1)
        // Source: SIE_STATUS.DATA_SEQ_ERROR
        ADD_BITFIELD_RO(ERROR_DATA_SEQ, 5, 1)
        // Raised when any bit in BUFF_STATUS is set. Clear by clearing all bits in BUFF_STATUS.
        ADD_BITFIELD_RO(BUFF_STATUS, 4, 1)
        // Raised every time SIE_STATUS.TRANS_COMPLETE is set. Clear by writing to this bit.
        ADD_BITFIELD_RO(TRANS_COMPLETE, 3, 1)
        // Host: raised every time the host sends a SOF (Start of Frame). Cleared by reading SOF_RD
        ADD_BITFIELD_RO(HOST_SOF, 2, 1)
        // Host: raised when a device wakes up the host. Cleared by writing to SIE_STATUS.RESUME
        ADD_BITFIELD_RO(HOST_RESUME, 1, 1)
        // Host: raised when a device is connected or disconnected (i.e. when SIE_STATUS.SPEED changes). Cleared by writing to SIE_STATUS.SPEED
        ADD_BITFIELD_RO(HOST_CONN_DIS, 0, 1)
    END_TYPE()

    // Device only. Raw value of free-running PHY clock counter @48MHz. Used to calculate time between SOF events.
    // Reset value: 0x00000000
    BEGIN_TYPE(SOF_TIMESTAMP_RAW_t, uint32_t)
        ADD_BITFIELD_RO(SOF_TIMESTAMP_RAW, 0, 21)
    END_TYPE()

    // Device only. Value of free-running PHY clock counter @48MHz when last SOF event occurred.
    // Reset value: 0x00000000
    BEGIN_TYPE(SOF_TIMESTAMP_LAST_t, uint32_t)
        ADD_BITFIELD_RO(SOF_TIMESTAMP_LAST, 0, 21)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(SM_STATE_t, uint32_t)
        ADD_BITFIELD_RO(RX_DASM, 8, 4)
        ADD_BITFIELD_RO(BC_STATE, 5, 3)
        ADD_BITFIELD_RO(STATE, 0, 5)
    END_TYPE()

    // TX error count for each endpoint. Write to each field to reset the counter to 0.
    // Reset value: 0x00000000
    BEGIN_TYPE(EP_TX_ERROR_t, uint32_t)
        ADD_BITFIELD_RW(EP15, 30, 2)
        ADD_BITFIELD_RW(EP14, 28, 2)
        ADD_BITFIELD_RW(EP13, 26, 2)
        ADD_BITFIELD_RW(EP12, 24, 2)
        ADD_BITFIELD_RW(EP11, 22, 2)
        ADD_BITFIELD_RW(EP10, 20, 2)
        ADD_BITFIELD_RW(EP9, 18, 2)
        ADD_BITFIELD_RW(EP8, 16, 2)
        ADD_BITFIELD_RW(EP7, 14, 2)
        ADD_BITFIELD_RW(EP6, 12, 2)
        ADD_BITFIELD_RW(EP5, 10, 2)
        ADD_BITFIELD_RW(EP4, 8, 2)
        ADD_BITFIELD_RW(EP3, 6, 2)
        ADD_BITFIELD_RW(EP2, 4, 2)
        ADD_BITFIELD_RW(EP1, 2, 2)
        ADD_BITFIELD_RW(EP0, 0, 2)
    END_TYPE()

    // RX error count for each endpoint. Write to each field to reset the counter to 0.
    // Reset value: 0x00000000
    BEGIN_TYPE(EP_RX_ERROR_t, uint32_t)
        ADD_BITFIELD_RW(EP15_SEQ, 31, 1)
        ADD_BITFIELD_RW(EP15_TRANSACTION, 30, 1)
        ADD_BITFIELD_RW(EP14_SEQ, 29, 1)
        ADD_BITFIELD_RW(EP14_TRANSACTION, 28, 1)
        ADD_BITFIELD_RW(EP13_SEQ, 27, 1)
        ADD_BITFIELD_RW(EP13_TRANSACTION, 26, 1)
        ADD_BITFIELD_RW(EP12_SEQ, 25, 1)
        ADD_BITFIELD_RW(EP12_TRANSACTION, 24, 1)
        ADD_BITFIELD_RW(EP11_SEQ, 23, 1)
        ADD_BITFIELD_RW(EP11_TRANSACTION, 22, 1)
        ADD_BITFIELD_RW(EP10_SEQ, 21, 1)
        ADD_BITFIELD_RW(EP10_TRANSACTION, 20, 1)
        ADD_BITFIELD_RW(EP9_SEQ, 19, 1)
        ADD_BITFIELD_RW(EP9_TRANSACTION, 18, 1)
        ADD_BITFIELD_RW(EP8_SEQ, 17, 1)
        ADD_BITFIELD_RW(EP8_TRANSACTION, 16, 1)
        ADD_BITFIELD_RW(EP7_SEQ, 15, 1)
        ADD_BITFIELD_RW(EP7_TRANSACTION, 14, 1)
        ADD_BITFIELD_RW(EP6_SEQ, 13, 1)
        ADD_BITFIELD_RW(EP6_TRANSACTION, 12, 1)
        ADD_BITFIELD_RW(EP5_SEQ, 11, 1)
        ADD_BITFIELD_RW(EP5_TRANSACTION, 10, 1)
        ADD_BITFIELD_RW(EP4_SEQ, 9, 1)
        ADD_BITFIELD_RW(EP4_TRANSACTION, 8, 1)
        ADD_BITFIELD_RW(EP3_SEQ, 7, 1)
        ADD_BITFIELD_RW(EP3_TRANSACTION, 6, 1)
        ADD_BITFIELD_RW(EP2_SEQ, 5, 1)
        ADD_BITFIELD_RW(EP2_TRANSACTION, 4, 1)
        ADD_BITFIELD_RW(EP1_SEQ, 3, 1)
        ADD_BITFIELD_RW(EP1_TRANSACTION, 2, 1)
        ADD_BITFIELD_RW(EP0_SEQ, 1, 1)
        ADD_BITFIELD_RW(EP0_TRANSACTION, 0, 1)
    END_TYPE()

    // Watchdog that forces the device state machine to idle and raises an interrupt if the device stays in a state that isn't idle for the configured limit. The counter is reset on every state transition.  Set limit while enable is low and then set the enable.
    // Reset value: 0x00000000
    BEGIN_TYPE(DEV_SM_WATCHDOG_t, uint32_t)
        ADD_BITFIELD_RW(FIRED, 20, 1)
        // Set to 1 to forcibly reset the device state machine on watchdog expiry
        ADD_BITFIELD_RW(RESET, 19, 1)
        ADD_BITFIELD_RW(ENABLE, 18, 1)
        ADD_BITFIELD_RW(LIMIT, 0, 18)
    END_TYPE()

    struct USB_t {
        ADDR_ENDP_t                   ADDR_ENDP;
        ADDR_ENDP__t                  ADDR_ENDP_1;
        ADDR_ENDP__t                  ADDR_ENDP_2;
        ADDR_ENDP__t                  ADDR_ENDP_3;
        ADDR_ENDP__t                  ADDR_ENDP_4;
        ADDR_ENDP__t                  ADDR_ENDP_5;
        ADDR_ENDP__t                  ADDR_ENDP_6;
        ADDR_ENDP__t                  ADDR_ENDP_7;
        ADDR_ENDP__t                  ADDR_ENDP_8;
        ADDR_ENDP__t                  ADDR_ENDP_9;
        ADDR_ENDP__t                  ADDR_ENDP_10;
        ADDR_ENDP__t                  ADDR_ENDP_11;
        ADDR_ENDP__t                  ADDR_ENDP_12;
        ADDR_ENDP__t                  ADDR_ENDP_13;
        ADDR_ENDP__t                  ADDR_ENDP_14;
        ADDR_ENDP__t                  ADDR_ENDP_15;
        MAIN_CTRL_t                   MAIN_CTRL;
        SOF_WR_t                      SOF_WR;
        SOF_RD_t                      SOF_RD;
        SIE_CTRL_t                    SIE_CTRL;
        SIE_STATUS_t                  SIE_STATUS;
        INT_EP_CTRL_t                 INT_EP_CTRL;
        BUFF_STATUS_t                 BUFF_STATUS;
        BUFF_CPU_SHOULD_HANDLE_t      BUFF_CPU_SHOULD_HANDLE;
        EP_ABORT_t                    EP_ABORT;
        EP_ABORT_DONE_t               EP_ABORT_DONE;
        EP_STALL_ARM_t                EP_STALL_ARM;
        NAK_POLL_t                    NAK_POLL;
        EP_STATUS_STALL_NAK_t         EP_STATUS_STALL_NAK;
        USB_MUXING_t                  USB_MUXING;
        USB_PWR_t                     USB_PWR;
        USBPHY_DIRECT_t               USBPHY_DIRECT;
        USBPHY_DIRECT_OVERRIDE_t      USBPHY_DIRECT_OVERRIDE;
        USBPHY_TRIM_t                 USBPHY_TRIM;
        LINESTATE_TUNING_t            LINESTATE_TUNING;
        INTR_t                        INTR;
        INTE_t                        INTE;
        INTF_t                        INTF;
        INTS_t                        INTS;
        uint32_t                      reserved0[25];
        SOF_TIMESTAMP_RAW_t           SOF_TIMESTAMP_RAW;
        SOF_TIMESTAMP_LAST_t          SOF_TIMESTAMP_LAST;
        SM_STATE_t                    SM_STATE;
        EP_TX_ERROR_t                 EP_TX_ERROR;
        EP_RX_ERROR_t                 EP_RX_ERROR;
        DEV_SM_WATCHDOG_t             DEV_SM_WATCHDOG;
    };

    static USB_t & USB = (*(USB_t *)0x50110000);
    static USB_t & USB_XOR = (*(USB_t *)0x50111000);
    static USB_t & USB_SET = (*(USB_t *)0x50112000);
    static USB_t & USB_CLR = (*(USB_t *)0x50113000);

} // _USB_

// ARM TrustZone RNG register block
namespace _TRNG_  {

    // Interrupt masking.
    // Reset value: 0x0000000f
    BEGIN_TYPE(RNG_IMR_t, uint32_t)
        // RESERVED
        ADD_BITFIELD_RO(RESERVED, 4, 28)
        // 1'b1-mask interrupt, no interrupt will be generated. See RNG_ISR for an explanation on this interrupt.
        ADD_BITFIELD_RW(VN_ERR_INT_MASK, 3, 1)
        // 1'b1-mask interrupt, no interrupt will be generated. See RNG_ISR for an explanation on this interrupt.
        ADD_BITFIELD_RW(CRNGT_ERR_INT_MASK, 2, 1)
        // 1'b1-mask interrupt, no interrupt will be generated. See RNG_ISR for an explanation on this interrupt.
        ADD_BITFIELD_RW(AUTOCORR_ERR_INT_MASK, 1, 1)
        // 1'b1-mask interrupt, no interrupt will be generated. See RNG_ISR for an explanation on this interrupt.
        ADD_BITFIELD_RW(EHR_VALID_INT_MASK, 0, 1)
    END_TYPE()

    // RNG status register. If corresponding RNG_IMR bit is unmasked, an interrupt will be generated.
    // Reset value: 0x00000000
    BEGIN_TYPE(RNG_ISR_t, uint32_t)
        // RESERVED
        ADD_BITFIELD_RO(RESERVED, 4, 28)
        // 1'b1 indicates Von Neuman error. Error in von Neuman occurs if 32 consecutive collected bits are identical, ZERO or ONE.
        ADD_BITFIELD_RO(VN_ERR, 3, 1)
        // 1'b1 indicates CRNGT in the RNG test failed. Failure occurs when two consecutive blocks of 16 collected bits are equal.
        ADD_BITFIELD_RO(CRNGT_ERR, 2, 1)
        // 1'b1 indicates Autocorrelation test failed four times in a row. When set, RNG cease from functioning until next reset.
        ADD_BITFIELD_RO(AUTOCORR_ERR, 1, 1)
        // 1'b1 indicates that 192 bits have been collected in the RNG, and are ready to be read.
        ADD_BITFIELD_RO(EHR_VALID, 0, 1)
    END_TYPE()

    // Interrupt/status bit clear Register.
    // Reset value: 0x00000000
    BEGIN_TYPE(RNG_ICR_t, uint32_t)
        // RESERVED
        ADD_BITFIELD_RO(RESERVED, 4, 28)
        // Write 1'b1 - clear corresponding bit in RNG_ISR.
        ADD_BITFIELD_RW(VN_ERR, 3, 1)
        // Write 1'b1 - clear corresponding bit in RNG_ISR.
        ADD_BITFIELD_RW(CRNGT_ERR, 2, 1)
        // Cannot be cleared by SW! Only RNG reset clears this bit.
        ADD_BITFIELD_RW(AUTOCORR_ERR, 1, 1)
        // Write 1'b1 - clear corresponding bit in RNG_ISR.
        ADD_BITFIELD_RW(EHR_VALID, 0, 1)
    END_TYPE()

    // Selecting the inverter-chain length.
    // Reset value: 0x00000000
    BEGIN_TYPE(TRNG_CONFIG_t, uint32_t)
        // RESERVED
        ADD_BITFIELD_RO(RESERVED, 2, 30)
        // Selects the number of inverters (out of four possible selections) in the ring oscillator (the entropy source).
        ADD_BITFIELD_RW(RND_SRC_SEL, 0, 2)
    END_TYPE()

    // 192 bit collection indication.
    // Reset value: 0x00000000
    BEGIN_TYPE(TRNG_VALID_t, uint32_t)
        // RESERVED
        ADD_BITFIELD_RO(RESERVED, 1, 31)
        // 1'b1 indicates that collection of bits in the RNG is completed, and data can be read from EHR_DATA register.
        ADD_BITFIELD_RO(EHR_VALID, 0, 1)
    END_TYPE()

    // RNG collected bits.
    // Reset value: 0x00000000
    BEGIN_TYPE(EHR_DATA0_t, uint32_t)
        // Bits [31:0] of Entropy Holding Register (EHR) - RNG output register
        ADD_BITFIELD_RO(EHR_DATA0, 0, 32)
    END_TYPE()

    // RNG collected bits.
    // Reset value: 0x00000000
    BEGIN_TYPE(EHR_DATA1_t, uint32_t)
        // Bits [63:32] of Entropy Holding Register (EHR) - RNG output register
        ADD_BITFIELD_RO(EHR_DATA1, 0, 32)
    END_TYPE()

    // RNG collected bits.
    // Reset value: 0x00000000
    BEGIN_TYPE(EHR_DATA2_t, uint32_t)
        // Bits [95:64] of Entropy Holding Register (EHR) - RNG output register
        ADD_BITFIELD_RO(EHR_DATA2, 0, 32)
    END_TYPE()

    // RNG collected bits.
    // Reset value: 0x00000000
    BEGIN_TYPE(EHR_DATA3_t, uint32_t)
        // Bits [127:96] of Entropy Holding Register (EHR) - RNG output register
        ADD_BITFIELD_RO(EHR_DATA3, 0, 32)
    END_TYPE()

    // RNG collected bits.
    // Reset value: 0x00000000
    BEGIN_TYPE(EHR_DATA4_t, uint32_t)
        // Bits [159:128] of Entropy Holding Register (EHR) - RNG output register
        ADD_BITFIELD_RO(EHR_DATA4, 0, 32)
    END_TYPE()

    // RNG collected bits.
    // Reset value: 0x00000000
    BEGIN_TYPE(EHR_DATA5_t, uint32_t)
        // Bits [191:160] of Entropy Holding Register (EHR) - RNG output register
        ADD_BITFIELD_RO(EHR_DATA5, 0, 32)
    END_TYPE()

    // Enable signal for the random source.
    // Reset value: 0x00000000
    BEGIN_TYPE(RND_SOURCE_ENABLE_t, uint32_t)
        // RESERVED
        ADD_BITFIELD_RO(RESERVED, 1, 31)
        // * 1'b1 - entropy source is enabled. *1'b0 - entropy source is disabled
        ADD_BITFIELD_RW(RND_SRC_EN, 0, 1)
    END_TYPE()

    // Counts clocks between sampling of random bit.
    // Reset value: 0x0000ffff
    BEGIN_TYPE(SAMPLE_CNT1_t, uint32_t)
        // Sets the number of rng_clk cycles between two consecutive ring oscillator samples. Note! If the Von-Neuman is bypassed, the minimum value for sample counter must not be less then decimal seventeen
        ADD_BITFIELD_RW(SAMPLE_CNTR1, 0, 32)
    END_TYPE()

    // Statistic about Autocorrelation test activations.
    // Reset value: 0x00000000
    BEGIN_TYPE(AUTOCORR_STATISTIC_t, uint32_t)
        // RESERVED
        ADD_BITFIELD_RO(RESERVED, 22, 10)
        // Count each time an autocorrelation test fails. Any write to the register reset the counter. Stop collecting statistic if one of the counters reached the limit.
        ADD_BITFIELD_RW(AUTOCORR_FAILS, 14, 8)
        // Count each time an autocorrelation test starts. Any write to the register reset the counter. Stop collecting statistic if one of the counters reached the limit.
        ADD_BITFIELD_RW(AUTOCORR_TRYS, 0, 14)
    END_TYPE()

    // Debug register.
    // Reset value: 0x00000000
    BEGIN_TYPE(TRNG_DEBUG_CONTROL_t, uint32_t)
        // When set, the autocorrelation test in the TRNG module is bypassed.
        ADD_BITFIELD_RW(AUTO_CORRELATE_BYPASS, 3, 1)
        // When set, the CRNGT test in the RNG is bypassed.
        ADD_BITFIELD_RW(TRNG_CRNGT_BYPASS, 2, 1)
        // When set, the Von-Neuman balancer is bypassed (including the 32 consecutive bits test).
        ADD_BITFIELD_RW(VNC_BYPASS, 1, 1)
        // N/A
        ADD_BITFIELD_RO(RESERVED, 0, 1)
    END_TYPE()

    // Generate internal SW reset within the RNG block.
    // Reset value: 0x00000000
    BEGIN_TYPE(TRNG_SW_RESET_t, uint32_t)
        // RESERVED
        ADD_BITFIELD_RO(RESERVED, 1, 31)
        // Writing 1'b1 to this register causes an internal RNG reset.
        ADD_BITFIELD_RW(TRNG_SW_RESET, 0, 1)
    END_TYPE()

    // Enable the RNG debug mode
    // Reset value: 0x00000000
    BEGIN_TYPE(RNG_DEBUG_EN_INPUT_t, uint32_t)
        // RESERVED
        ADD_BITFIELD_RO(RESERVED, 1, 31)
        // * 1'b1 - debug mode is enabled. *1'b0 - debug mode is disabled
        ADD_BITFIELD_RW(RNG_DEBUG_EN, 0, 1)
    END_TYPE()

    // RNG Busy indication.
    // Reset value: 0x00000000
    BEGIN_TYPE(TRNG_BUSY_t, uint32_t)
        // RESERVED
        ADD_BITFIELD_RO(RESERVED, 1, 31)
        // Reflects rng_busy status.
        ADD_BITFIELD_RO(TRNG_BUSY, 0, 1)
    END_TYPE()

    // Reset the counter of collected bits in the RNG.
    // Reset value: 0x00000000
    BEGIN_TYPE(RST_BITS_COUNTER_t, uint32_t)
        // RESERVED
        ADD_BITFIELD_RO(RESERVED, 1, 31)
        // Writing any value to this address will reset the bits counter and RNG valid registers. RND_SORCE_ENABLE register must be unset in order for the reset to take place.
        ADD_BITFIELD_RW(RST_BITS_COUNTER, 0, 1)
    END_TYPE()

    // Displays the version settings of the TRNG.
    // Reset value: 0x00000000
    BEGIN_TYPE(RNG_VERSION_t, uint32_t)
        // RESERVED
        ADD_BITFIELD_RO(RESERVED, 8, 24)
        // * 1'b1 - 5 SBOX AES. *1'b0 - 20 SBOX AES
        ADD_BITFIELD_RO(RNG_USE_5_SBOXES, 7, 1)
        // * 1'b1 - Exists. *1'b0 - Does not exist
        ADD_BITFIELD_RO(RESEEDING_EXISTS, 6, 1)
        // * 1'b1 - Exists. *1'b0 - Does not exist
        ADD_BITFIELD_RO(KAT_EXISTS, 5, 1)
        // * 1'b1 - Exists. *1'b0 - Does not exist
        ADD_BITFIELD_RO(PRNG_EXISTS, 4, 1)
        // * 1'b1 - Exists. *1'b0 - Does not exist
        ADD_BITFIELD_RO(TRNG_TESTS_BYPASS_EN, 3, 1)
        // * 1'b1 - Exists. *1'b0 - Does not exist
        ADD_BITFIELD_RO(AUTOCORR_EXISTS, 2, 1)
        // * 1'b1 - Exists. *1'b0 - Does not exist
        ADD_BITFIELD_RO(CRNGT_EXISTS, 1, 1)
        // * 1'b1 - 192-bit EHR. *1'b0 - 128-bit EHR
        ADD_BITFIELD_RO(EHR_WIDTH_192, 0, 1)
    END_TYPE()

    // Collected BIST results.
    // Reset value: 0x00000000
    BEGIN_TYPE(RNG_BIST_CNTR_0_t, uint32_t)
        // RESERVED
        ADD_BITFIELD_RO(RESERVED, 22, 10)
        // Reflects the results of RNG BIST counter.
        ADD_BITFIELD_RO(ROSC_CNTR_VAL, 0, 22)
    END_TYPE()

    // Collected BIST results.
    // Reset value: 0x00000000
    BEGIN_TYPE(RNG_BIST_CNTR_1_t, uint32_t)
        // RESERVED
        ADD_BITFIELD_RO(RESERVED, 22, 10)
        // Reflects the results of RNG BIST counter.
        ADD_BITFIELD_RO(ROSC_CNTR_VAL, 0, 22)
    END_TYPE()

    // Collected BIST results.
    // Reset value: 0x00000000
    BEGIN_TYPE(RNG_BIST_CNTR_2_t, uint32_t)
        // RESERVED
        ADD_BITFIELD_RO(RESERVED, 22, 10)
        // Reflects the results of RNG BIST counter.
        ADD_BITFIELD_RO(ROSC_CNTR_VAL, 0, 22)
    END_TYPE()

    struct TRNG_t {
        uint32_t                      reserved0[64];
        RNG_IMR_t                     RNG_IMR;
        RNG_ISR_t                     RNG_ISR;
        RNG_ICR_t                     RNG_ICR;
        TRNG_CONFIG_t                 TRNG_CONFIG;
        TRNG_VALID_t                  TRNG_VALID;
        EHR_DATA0_t                   EHR_DATA0;
        EHR_DATA1_t                   EHR_DATA1;
        EHR_DATA2_t                   EHR_DATA2;
        EHR_DATA3_t                   EHR_DATA3;
        EHR_DATA4_t                   EHR_DATA4;
        EHR_DATA5_t                   EHR_DATA5;
        RND_SOURCE_ENABLE_t           RND_SOURCE_ENABLE;
        SAMPLE_CNT1_t                 SAMPLE_CNT1;
        AUTOCORR_STATISTIC_t          AUTOCORR_STATISTIC;
        TRNG_DEBUG_CONTROL_t          TRNG_DEBUG_CONTROL;
        uint32_t                      reserved1;
        TRNG_SW_RESET_t               TRNG_SW_RESET;
        uint32_t                      reserved2[28];
        RNG_DEBUG_EN_INPUT_t          RNG_DEBUG_EN_INPUT;
        TRNG_BUSY_t                   TRNG_BUSY;
        RST_BITS_COUNTER_t            RST_BITS_COUNTER;
        RNG_VERSION_t                 RNG_VERSION;
        uint32_t                      reserved3[7];
        RNG_BIST_CNTR_0_t             RNG_BIST_CNTR_0;
        RNG_BIST_CNTR_1_t             RNG_BIST_CNTR_1;
        RNG_BIST_CNTR_2_t             RNG_BIST_CNTR_2;
    };

    static TRNG_t & TRNG = (*(TRNG_t *)0x400f0000);
    static TRNG_t & TRNG_XOR = (*(TRNG_t *)0x400f1000);
    static TRNG_t & TRNG_SET = (*(TRNG_t *)0x400f2000);
    static TRNG_t & TRNG_CLR = (*(TRNG_t *)0x400f3000);

} // _TRNG_

// Glitch detector controls
namespace _GLITCH_DETECTOR_  {

    // Forcibly arm the glitch detectors, if they are not already armed by OTP. When armed, any individual detector trigger will cause a restart of the switched core power domain's power-on reset state machine. Glitch detector triggers are recorded accumulatively in TRIG_STATUS. If the system is reset by a glitch detector trigger, this is recorded in POWMAN_CHIP_RESET. This register is Secure read/write only.
    // Reset value: 0x00005bad
    BEGIN_TYPE(ARM_t, uint32_t)
        ADD_BITFIELD_RW(ARM, 0, 16)
    END_TYPE()

    // Do not force the glitch detectors to be armed
    static const uint32_t ARM_ARM__no = 23469;
    // Force the glitch detectors to be armed. (Any value other than ARM_NO counts as YES)
    static const uint32_t ARM_ARM__yes = 0;

    // Reset value: 0x00000000
    BEGIN_TYPE(DISARM_t, uint32_t)
        // Forcibly disarm the glitch detectors, if they are armed by OTP. Ignored if ARM is YES. This register is Secure read/write only.
        ADD_BITFIELD_RW(DISARM, 0, 16)
    END_TYPE()

    // Do not disarm the glitch detectors. (Any value other than DISARM_YES counts as NO)
    static const uint32_t DISARM_DISARM__no = 0;
    // Disarm the glitch detectors
    static const uint32_t DISARM_DISARM__yes = 56495;

    // Adjust the sensitivity of glitch detectors to values other than their OTP-provided defaults. This register is Secure read/write only.
    // Reset value: 0x00000000
    BEGIN_TYPE(SENSITIVITY_t, uint32_t)
        ADD_BITFIELD_RW(DEFAULT, 24, 8)
        // Must be the inverse of DET3, else the default value is used.
        ADD_BITFIELD_RW(DET3_INV, 14, 2)
        // Must be the inverse of DET2, else the default value is used.
        ADD_BITFIELD_RW(DET2_INV, 12, 2)
        // Must be the inverse of DET1, else the default value is used.
        ADD_BITFIELD_RW(DET1_INV, 10, 2)
        // Must be the inverse of DET0, else the default value is used.
        ADD_BITFIELD_RW(DET0_INV, 8, 2)
        // Set sensitivity for detector 3. Higher values are more sensitive.
        ADD_BITFIELD_RW(DET3, 6, 2)
        // Set sensitivity for detector 2. Higher values are more sensitive.
        ADD_BITFIELD_RW(DET2, 4, 2)
        // Set sensitivity for detector 1. Higher values are more sensitive.
        ADD_BITFIELD_RW(DET1, 2, 2)
        // Set sensitivity for detector 0. Higher values are more sensitive.
        ADD_BITFIELD_RW(DET0, 0, 2)
    END_TYPE()

    // Use the default sensitivity configured in OTP for all detectors. (Any value other than DEFAULT_NO counts as YES)
    static const uint32_t SENSITIVITY_DEFAULT__yes = 0;
    // Do not use the default sensitivity configured in OTP. Instead use the value from this register.
    static const uint32_t SENSITIVITY_DEFAULT__no = 222;

    // Reset value: 0x00000000
    BEGIN_TYPE(LOCK_t, uint32_t)
        // Write any nonzero value to disable writes to ARM, DISARM, SENSITIVITY and LOCK. This register is Secure read/write only.
        ADD_BITFIELD_RW(LOCK, 0, 8)
    END_TYPE()

    // Set when a detector output triggers. Write-1-clear. (May immediately return high if the detector remains in a failed state. Detectors can only be cleared by a full reset of the switched core power domain.) This register is Secure read/write only.
    // Reset value: 0x00000000
    BEGIN_TYPE(TRIG_STATUS_t, uint32_t)
        ADD_BITFIELD_RW(DET3, 3, 1)
        ADD_BITFIELD_RW(DET2, 2, 1)
        ADD_BITFIELD_RW(DET1, 1, 1)
        ADD_BITFIELD_RW(DET0, 0, 1)
    END_TYPE()

    // Simulate the firing of one or more detectors. Writing ones to this register will set the matching bits in STATUS_TRIG. If the glitch detectors are currently armed, writing ones will also immediately reset the switched core power domain, and set the reset reason latches in POWMAN_CHIP_RESET to indicate a glitch detector resets. This register is Secure read/write only.
    // Reset value: 0x00000000
    BEGIN_TYPE(TRIG_FORCE_t, uint32_t)
        ADD_BITFIELD_WO(TRIG_FORCE, 0, 4)
    END_TYPE()

    struct GLITCH_DETECTOR_t {
        ARM_t                         ARM;
        DISARM_t                      DISARM;
        SENSITIVITY_t                 SENSITIVITY;
        LOCK_t                        LOCK;
        TRIG_STATUS_t                 TRIG_STATUS;
        TRIG_FORCE_t                  TRIG_FORCE;
    };

    static GLITCH_DETECTOR_t & GLITCH_DETECTOR = (*(GLITCH_DETECTOR_t *)0x40158000);
    static GLITCH_DETECTOR_t & GLITCH_DETECTOR_XOR = (*(GLITCH_DETECTOR_t *)0x40159000);
    static GLITCH_DETECTOR_t & GLITCH_DETECTOR_SET = (*(GLITCH_DETECTOR_t *)0x4015a000);
    static GLITCH_DETECTOR_t & GLITCH_DETECTOR_CLR = (*(GLITCH_DETECTOR_t *)0x4015b000);

} // _GLITCH_DETECTOR_

// SNPS OTP control IF (SBPI and RPi wrapper control)
namespace _OTP_  {

    // Software lock register for page 0. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK0_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK0_NSEC__read_write = 0;
    static const uint32_t SW_LOCK0_NSEC__read_only = 1;
    static const uint32_t SW_LOCK0_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK0_SEC__read_write = 0;
    static const uint32_t SW_LOCK0_SEC__read_only = 1;
    static const uint32_t SW_LOCK0_SEC__inaccessible = 3;

    // Software lock register for page 1. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK1_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK1_NSEC__read_write = 0;
    static const uint32_t SW_LOCK1_NSEC__read_only = 1;
    static const uint32_t SW_LOCK1_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK1_SEC__read_write = 0;
    static const uint32_t SW_LOCK1_SEC__read_only = 1;
    static const uint32_t SW_LOCK1_SEC__inaccessible = 3;

    // Software lock register for page 2. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK2_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK2_NSEC__read_write = 0;
    static const uint32_t SW_LOCK2_NSEC__read_only = 1;
    static const uint32_t SW_LOCK2_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK2_SEC__read_write = 0;
    static const uint32_t SW_LOCK2_SEC__read_only = 1;
    static const uint32_t SW_LOCK2_SEC__inaccessible = 3;

    // Software lock register for page 3. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK3_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK3_NSEC__read_write = 0;
    static const uint32_t SW_LOCK3_NSEC__read_only = 1;
    static const uint32_t SW_LOCK3_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK3_SEC__read_write = 0;
    static const uint32_t SW_LOCK3_SEC__read_only = 1;
    static const uint32_t SW_LOCK3_SEC__inaccessible = 3;

    // Software lock register for page 4. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK4_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK4_NSEC__read_write = 0;
    static const uint32_t SW_LOCK4_NSEC__read_only = 1;
    static const uint32_t SW_LOCK4_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK4_SEC__read_write = 0;
    static const uint32_t SW_LOCK4_SEC__read_only = 1;
    static const uint32_t SW_LOCK4_SEC__inaccessible = 3;

    // Software lock register for page 5. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK5_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK5_NSEC__read_write = 0;
    static const uint32_t SW_LOCK5_NSEC__read_only = 1;
    static const uint32_t SW_LOCK5_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK5_SEC__read_write = 0;
    static const uint32_t SW_LOCK5_SEC__read_only = 1;
    static const uint32_t SW_LOCK5_SEC__inaccessible = 3;

    // Software lock register for page 6. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK6_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK6_NSEC__read_write = 0;
    static const uint32_t SW_LOCK6_NSEC__read_only = 1;
    static const uint32_t SW_LOCK6_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK6_SEC__read_write = 0;
    static const uint32_t SW_LOCK6_SEC__read_only = 1;
    static const uint32_t SW_LOCK6_SEC__inaccessible = 3;

    // Software lock register for page 7. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK7_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK7_NSEC__read_write = 0;
    static const uint32_t SW_LOCK7_NSEC__read_only = 1;
    static const uint32_t SW_LOCK7_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK7_SEC__read_write = 0;
    static const uint32_t SW_LOCK7_SEC__read_only = 1;
    static const uint32_t SW_LOCK7_SEC__inaccessible = 3;

    // Software lock register for page 8. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK8_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK8_NSEC__read_write = 0;
    static const uint32_t SW_LOCK8_NSEC__read_only = 1;
    static const uint32_t SW_LOCK8_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK8_SEC__read_write = 0;
    static const uint32_t SW_LOCK8_SEC__read_only = 1;
    static const uint32_t SW_LOCK8_SEC__inaccessible = 3;

    // Software lock register for page 9. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK9_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK9_NSEC__read_write = 0;
    static const uint32_t SW_LOCK9_NSEC__read_only = 1;
    static const uint32_t SW_LOCK9_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK9_SEC__read_write = 0;
    static const uint32_t SW_LOCK9_SEC__read_only = 1;
    static const uint32_t SW_LOCK9_SEC__inaccessible = 3;

    // Software lock register for page 10. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK10_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK10_NSEC__read_write = 0;
    static const uint32_t SW_LOCK10_NSEC__read_only = 1;
    static const uint32_t SW_LOCK10_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK10_SEC__read_write = 0;
    static const uint32_t SW_LOCK10_SEC__read_only = 1;
    static const uint32_t SW_LOCK10_SEC__inaccessible = 3;

    // Software lock register for page 11. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK11_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK11_NSEC__read_write = 0;
    static const uint32_t SW_LOCK11_NSEC__read_only = 1;
    static const uint32_t SW_LOCK11_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK11_SEC__read_write = 0;
    static const uint32_t SW_LOCK11_SEC__read_only = 1;
    static const uint32_t SW_LOCK11_SEC__inaccessible = 3;

    // Software lock register for page 12. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK12_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK12_NSEC__read_write = 0;
    static const uint32_t SW_LOCK12_NSEC__read_only = 1;
    static const uint32_t SW_LOCK12_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK12_SEC__read_write = 0;
    static const uint32_t SW_LOCK12_SEC__read_only = 1;
    static const uint32_t SW_LOCK12_SEC__inaccessible = 3;

    // Software lock register for page 13. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK13_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK13_NSEC__read_write = 0;
    static const uint32_t SW_LOCK13_NSEC__read_only = 1;
    static const uint32_t SW_LOCK13_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK13_SEC__read_write = 0;
    static const uint32_t SW_LOCK13_SEC__read_only = 1;
    static const uint32_t SW_LOCK13_SEC__inaccessible = 3;

    // Software lock register for page 14. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK14_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK14_NSEC__read_write = 0;
    static const uint32_t SW_LOCK14_NSEC__read_only = 1;
    static const uint32_t SW_LOCK14_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK14_SEC__read_write = 0;
    static const uint32_t SW_LOCK14_SEC__read_only = 1;
    static const uint32_t SW_LOCK14_SEC__inaccessible = 3;

    // Software lock register for page 15. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK15_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK15_NSEC__read_write = 0;
    static const uint32_t SW_LOCK15_NSEC__read_only = 1;
    static const uint32_t SW_LOCK15_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK15_SEC__read_write = 0;
    static const uint32_t SW_LOCK15_SEC__read_only = 1;
    static const uint32_t SW_LOCK15_SEC__inaccessible = 3;

    // Software lock register for page 16. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK16_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK16_NSEC__read_write = 0;
    static const uint32_t SW_LOCK16_NSEC__read_only = 1;
    static const uint32_t SW_LOCK16_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK16_SEC__read_write = 0;
    static const uint32_t SW_LOCK16_SEC__read_only = 1;
    static const uint32_t SW_LOCK16_SEC__inaccessible = 3;

    // Software lock register for page 17. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK17_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK17_NSEC__read_write = 0;
    static const uint32_t SW_LOCK17_NSEC__read_only = 1;
    static const uint32_t SW_LOCK17_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK17_SEC__read_write = 0;
    static const uint32_t SW_LOCK17_SEC__read_only = 1;
    static const uint32_t SW_LOCK17_SEC__inaccessible = 3;

    // Software lock register for page 18. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK18_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK18_NSEC__read_write = 0;
    static const uint32_t SW_LOCK18_NSEC__read_only = 1;
    static const uint32_t SW_LOCK18_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK18_SEC__read_write = 0;
    static const uint32_t SW_LOCK18_SEC__read_only = 1;
    static const uint32_t SW_LOCK18_SEC__inaccessible = 3;

    // Software lock register for page 19. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK19_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK19_NSEC__read_write = 0;
    static const uint32_t SW_LOCK19_NSEC__read_only = 1;
    static const uint32_t SW_LOCK19_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK19_SEC__read_write = 0;
    static const uint32_t SW_LOCK19_SEC__read_only = 1;
    static const uint32_t SW_LOCK19_SEC__inaccessible = 3;

    // Software lock register for page 20. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK20_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK20_NSEC__read_write = 0;
    static const uint32_t SW_LOCK20_NSEC__read_only = 1;
    static const uint32_t SW_LOCK20_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK20_SEC__read_write = 0;
    static const uint32_t SW_LOCK20_SEC__read_only = 1;
    static const uint32_t SW_LOCK20_SEC__inaccessible = 3;

    // Software lock register for page 21. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK21_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK21_NSEC__read_write = 0;
    static const uint32_t SW_LOCK21_NSEC__read_only = 1;
    static const uint32_t SW_LOCK21_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK21_SEC__read_write = 0;
    static const uint32_t SW_LOCK21_SEC__read_only = 1;
    static const uint32_t SW_LOCK21_SEC__inaccessible = 3;

    // Software lock register for page 22. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK22_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK22_NSEC__read_write = 0;
    static const uint32_t SW_LOCK22_NSEC__read_only = 1;
    static const uint32_t SW_LOCK22_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK22_SEC__read_write = 0;
    static const uint32_t SW_LOCK22_SEC__read_only = 1;
    static const uint32_t SW_LOCK22_SEC__inaccessible = 3;

    // Software lock register for page 23. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK23_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK23_NSEC__read_write = 0;
    static const uint32_t SW_LOCK23_NSEC__read_only = 1;
    static const uint32_t SW_LOCK23_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK23_SEC__read_write = 0;
    static const uint32_t SW_LOCK23_SEC__read_only = 1;
    static const uint32_t SW_LOCK23_SEC__inaccessible = 3;

    // Software lock register for page 24. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK24_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK24_NSEC__read_write = 0;
    static const uint32_t SW_LOCK24_NSEC__read_only = 1;
    static const uint32_t SW_LOCK24_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK24_SEC__read_write = 0;
    static const uint32_t SW_LOCK24_SEC__read_only = 1;
    static const uint32_t SW_LOCK24_SEC__inaccessible = 3;

    // Software lock register for page 25. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK25_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK25_NSEC__read_write = 0;
    static const uint32_t SW_LOCK25_NSEC__read_only = 1;
    static const uint32_t SW_LOCK25_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK25_SEC__read_write = 0;
    static const uint32_t SW_LOCK25_SEC__read_only = 1;
    static const uint32_t SW_LOCK25_SEC__inaccessible = 3;

    // Software lock register for page 26. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK26_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK26_NSEC__read_write = 0;
    static const uint32_t SW_LOCK26_NSEC__read_only = 1;
    static const uint32_t SW_LOCK26_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK26_SEC__read_write = 0;
    static const uint32_t SW_LOCK26_SEC__read_only = 1;
    static const uint32_t SW_LOCK26_SEC__inaccessible = 3;

    // Software lock register for page 27. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK27_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK27_NSEC__read_write = 0;
    static const uint32_t SW_LOCK27_NSEC__read_only = 1;
    static const uint32_t SW_LOCK27_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK27_SEC__read_write = 0;
    static const uint32_t SW_LOCK27_SEC__read_only = 1;
    static const uint32_t SW_LOCK27_SEC__inaccessible = 3;

    // Software lock register for page 28. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK28_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK28_NSEC__read_write = 0;
    static const uint32_t SW_LOCK28_NSEC__read_only = 1;
    static const uint32_t SW_LOCK28_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK28_SEC__read_write = 0;
    static const uint32_t SW_LOCK28_SEC__read_only = 1;
    static const uint32_t SW_LOCK28_SEC__inaccessible = 3;

    // Software lock register for page 29. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK29_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK29_NSEC__read_write = 0;
    static const uint32_t SW_LOCK29_NSEC__read_only = 1;
    static const uint32_t SW_LOCK29_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK29_SEC__read_write = 0;
    static const uint32_t SW_LOCK29_SEC__read_only = 1;
    static const uint32_t SW_LOCK29_SEC__inaccessible = 3;

    // Software lock register for page 30. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK30_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK30_NSEC__read_write = 0;
    static const uint32_t SW_LOCK30_NSEC__read_only = 1;
    static const uint32_t SW_LOCK30_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK30_SEC__read_write = 0;
    static const uint32_t SW_LOCK30_SEC__read_only = 1;
    static const uint32_t SW_LOCK30_SEC__inaccessible = 3;

    // Software lock register for page 31. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK31_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK31_NSEC__read_write = 0;
    static const uint32_t SW_LOCK31_NSEC__read_only = 1;
    static const uint32_t SW_LOCK31_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK31_SEC__read_write = 0;
    static const uint32_t SW_LOCK31_SEC__read_only = 1;
    static const uint32_t SW_LOCK31_SEC__inaccessible = 3;

    // Software lock register for page 32. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK32_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK32_NSEC__read_write = 0;
    static const uint32_t SW_LOCK32_NSEC__read_only = 1;
    static const uint32_t SW_LOCK32_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK32_SEC__read_write = 0;
    static const uint32_t SW_LOCK32_SEC__read_only = 1;
    static const uint32_t SW_LOCK32_SEC__inaccessible = 3;

    // Software lock register for page 33. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK33_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK33_NSEC__read_write = 0;
    static const uint32_t SW_LOCK33_NSEC__read_only = 1;
    static const uint32_t SW_LOCK33_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK33_SEC__read_write = 0;
    static const uint32_t SW_LOCK33_SEC__read_only = 1;
    static const uint32_t SW_LOCK33_SEC__inaccessible = 3;

    // Software lock register for page 34. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK34_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK34_NSEC__read_write = 0;
    static const uint32_t SW_LOCK34_NSEC__read_only = 1;
    static const uint32_t SW_LOCK34_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK34_SEC__read_write = 0;
    static const uint32_t SW_LOCK34_SEC__read_only = 1;
    static const uint32_t SW_LOCK34_SEC__inaccessible = 3;

    // Software lock register for page 35. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK35_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK35_NSEC__read_write = 0;
    static const uint32_t SW_LOCK35_NSEC__read_only = 1;
    static const uint32_t SW_LOCK35_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK35_SEC__read_write = 0;
    static const uint32_t SW_LOCK35_SEC__read_only = 1;
    static const uint32_t SW_LOCK35_SEC__inaccessible = 3;

    // Software lock register for page 36. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK36_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK36_NSEC__read_write = 0;
    static const uint32_t SW_LOCK36_NSEC__read_only = 1;
    static const uint32_t SW_LOCK36_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK36_SEC__read_write = 0;
    static const uint32_t SW_LOCK36_SEC__read_only = 1;
    static const uint32_t SW_LOCK36_SEC__inaccessible = 3;

    // Software lock register for page 37. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK37_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK37_NSEC__read_write = 0;
    static const uint32_t SW_LOCK37_NSEC__read_only = 1;
    static const uint32_t SW_LOCK37_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK37_SEC__read_write = 0;
    static const uint32_t SW_LOCK37_SEC__read_only = 1;
    static const uint32_t SW_LOCK37_SEC__inaccessible = 3;

    // Software lock register for page 38. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK38_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK38_NSEC__read_write = 0;
    static const uint32_t SW_LOCK38_NSEC__read_only = 1;
    static const uint32_t SW_LOCK38_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK38_SEC__read_write = 0;
    static const uint32_t SW_LOCK38_SEC__read_only = 1;
    static const uint32_t SW_LOCK38_SEC__inaccessible = 3;

    // Software lock register for page 39. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK39_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK39_NSEC__read_write = 0;
    static const uint32_t SW_LOCK39_NSEC__read_only = 1;
    static const uint32_t SW_LOCK39_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK39_SEC__read_write = 0;
    static const uint32_t SW_LOCK39_SEC__read_only = 1;
    static const uint32_t SW_LOCK39_SEC__inaccessible = 3;

    // Software lock register for page 40. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK40_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK40_NSEC__read_write = 0;
    static const uint32_t SW_LOCK40_NSEC__read_only = 1;
    static const uint32_t SW_LOCK40_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK40_SEC__read_write = 0;
    static const uint32_t SW_LOCK40_SEC__read_only = 1;
    static const uint32_t SW_LOCK40_SEC__inaccessible = 3;

    // Software lock register for page 41. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK41_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK41_NSEC__read_write = 0;
    static const uint32_t SW_LOCK41_NSEC__read_only = 1;
    static const uint32_t SW_LOCK41_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK41_SEC__read_write = 0;
    static const uint32_t SW_LOCK41_SEC__read_only = 1;
    static const uint32_t SW_LOCK41_SEC__inaccessible = 3;

    // Software lock register for page 42. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK42_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK42_NSEC__read_write = 0;
    static const uint32_t SW_LOCK42_NSEC__read_only = 1;
    static const uint32_t SW_LOCK42_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK42_SEC__read_write = 0;
    static const uint32_t SW_LOCK42_SEC__read_only = 1;
    static const uint32_t SW_LOCK42_SEC__inaccessible = 3;

    // Software lock register for page 43. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK43_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK43_NSEC__read_write = 0;
    static const uint32_t SW_LOCK43_NSEC__read_only = 1;
    static const uint32_t SW_LOCK43_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK43_SEC__read_write = 0;
    static const uint32_t SW_LOCK43_SEC__read_only = 1;
    static const uint32_t SW_LOCK43_SEC__inaccessible = 3;

    // Software lock register for page 44. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK44_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK44_NSEC__read_write = 0;
    static const uint32_t SW_LOCK44_NSEC__read_only = 1;
    static const uint32_t SW_LOCK44_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK44_SEC__read_write = 0;
    static const uint32_t SW_LOCK44_SEC__read_only = 1;
    static const uint32_t SW_LOCK44_SEC__inaccessible = 3;

    // Software lock register for page 45. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK45_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK45_NSEC__read_write = 0;
    static const uint32_t SW_LOCK45_NSEC__read_only = 1;
    static const uint32_t SW_LOCK45_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK45_SEC__read_write = 0;
    static const uint32_t SW_LOCK45_SEC__read_only = 1;
    static const uint32_t SW_LOCK45_SEC__inaccessible = 3;

    // Software lock register for page 46. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK46_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK46_NSEC__read_write = 0;
    static const uint32_t SW_LOCK46_NSEC__read_only = 1;
    static const uint32_t SW_LOCK46_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK46_SEC__read_write = 0;
    static const uint32_t SW_LOCK46_SEC__read_only = 1;
    static const uint32_t SW_LOCK46_SEC__inaccessible = 3;

    // Software lock register for page 47. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK47_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK47_NSEC__read_write = 0;
    static const uint32_t SW_LOCK47_NSEC__read_only = 1;
    static const uint32_t SW_LOCK47_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK47_SEC__read_write = 0;
    static const uint32_t SW_LOCK47_SEC__read_only = 1;
    static const uint32_t SW_LOCK47_SEC__inaccessible = 3;

    // Software lock register for page 48. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK48_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK48_NSEC__read_write = 0;
    static const uint32_t SW_LOCK48_NSEC__read_only = 1;
    static const uint32_t SW_LOCK48_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK48_SEC__read_write = 0;
    static const uint32_t SW_LOCK48_SEC__read_only = 1;
    static const uint32_t SW_LOCK48_SEC__inaccessible = 3;

    // Software lock register for page 49. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK49_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK49_NSEC__read_write = 0;
    static const uint32_t SW_LOCK49_NSEC__read_only = 1;
    static const uint32_t SW_LOCK49_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK49_SEC__read_write = 0;
    static const uint32_t SW_LOCK49_SEC__read_only = 1;
    static const uint32_t SW_LOCK49_SEC__inaccessible = 3;

    // Software lock register for page 50. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK50_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK50_NSEC__read_write = 0;
    static const uint32_t SW_LOCK50_NSEC__read_only = 1;
    static const uint32_t SW_LOCK50_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK50_SEC__read_write = 0;
    static const uint32_t SW_LOCK50_SEC__read_only = 1;
    static const uint32_t SW_LOCK50_SEC__inaccessible = 3;

    // Software lock register for page 51. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK51_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK51_NSEC__read_write = 0;
    static const uint32_t SW_LOCK51_NSEC__read_only = 1;
    static const uint32_t SW_LOCK51_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK51_SEC__read_write = 0;
    static const uint32_t SW_LOCK51_SEC__read_only = 1;
    static const uint32_t SW_LOCK51_SEC__inaccessible = 3;

    // Software lock register for page 52. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK52_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK52_NSEC__read_write = 0;
    static const uint32_t SW_LOCK52_NSEC__read_only = 1;
    static const uint32_t SW_LOCK52_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK52_SEC__read_write = 0;
    static const uint32_t SW_LOCK52_SEC__read_only = 1;
    static const uint32_t SW_LOCK52_SEC__inaccessible = 3;

    // Software lock register for page 53. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK53_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK53_NSEC__read_write = 0;
    static const uint32_t SW_LOCK53_NSEC__read_only = 1;
    static const uint32_t SW_LOCK53_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK53_SEC__read_write = 0;
    static const uint32_t SW_LOCK53_SEC__read_only = 1;
    static const uint32_t SW_LOCK53_SEC__inaccessible = 3;

    // Software lock register for page 54. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK54_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK54_NSEC__read_write = 0;
    static const uint32_t SW_LOCK54_NSEC__read_only = 1;
    static const uint32_t SW_LOCK54_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK54_SEC__read_write = 0;
    static const uint32_t SW_LOCK54_SEC__read_only = 1;
    static const uint32_t SW_LOCK54_SEC__inaccessible = 3;

    // Software lock register for page 55. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK55_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK55_NSEC__read_write = 0;
    static const uint32_t SW_LOCK55_NSEC__read_only = 1;
    static const uint32_t SW_LOCK55_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK55_SEC__read_write = 0;
    static const uint32_t SW_LOCK55_SEC__read_only = 1;
    static const uint32_t SW_LOCK55_SEC__inaccessible = 3;

    // Software lock register for page 56. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK56_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK56_NSEC__read_write = 0;
    static const uint32_t SW_LOCK56_NSEC__read_only = 1;
    static const uint32_t SW_LOCK56_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK56_SEC__read_write = 0;
    static const uint32_t SW_LOCK56_SEC__read_only = 1;
    static const uint32_t SW_LOCK56_SEC__inaccessible = 3;

    // Software lock register for page 57. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK57_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK57_NSEC__read_write = 0;
    static const uint32_t SW_LOCK57_NSEC__read_only = 1;
    static const uint32_t SW_LOCK57_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK57_SEC__read_write = 0;
    static const uint32_t SW_LOCK57_SEC__read_only = 1;
    static const uint32_t SW_LOCK57_SEC__inaccessible = 3;

    // Software lock register for page 58. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK58_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK58_NSEC__read_write = 0;
    static const uint32_t SW_LOCK58_NSEC__read_only = 1;
    static const uint32_t SW_LOCK58_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK58_SEC__read_write = 0;
    static const uint32_t SW_LOCK58_SEC__read_only = 1;
    static const uint32_t SW_LOCK58_SEC__inaccessible = 3;

    // Software lock register for page 59. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK59_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK59_NSEC__read_write = 0;
    static const uint32_t SW_LOCK59_NSEC__read_only = 1;
    static const uint32_t SW_LOCK59_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK59_SEC__read_write = 0;
    static const uint32_t SW_LOCK59_SEC__read_only = 1;
    static const uint32_t SW_LOCK59_SEC__inaccessible = 3;

    // Software lock register for page 60. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK60_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK60_NSEC__read_write = 0;
    static const uint32_t SW_LOCK60_NSEC__read_only = 1;
    static const uint32_t SW_LOCK60_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK60_SEC__read_write = 0;
    static const uint32_t SW_LOCK60_SEC__read_only = 1;
    static const uint32_t SW_LOCK60_SEC__inaccessible = 3;

    // Software lock register for page 61. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK61_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK61_NSEC__read_write = 0;
    static const uint32_t SW_LOCK61_NSEC__read_only = 1;
    static const uint32_t SW_LOCK61_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK61_SEC__read_write = 0;
    static const uint32_t SW_LOCK61_SEC__read_only = 1;
    static const uint32_t SW_LOCK61_SEC__inaccessible = 3;

    // Software lock register for page 62. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK62_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK62_NSEC__read_write = 0;
    static const uint32_t SW_LOCK62_NSEC__read_only = 1;
    static const uint32_t SW_LOCK62_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK62_SEC__read_write = 0;
    static const uint32_t SW_LOCK62_SEC__read_only = 1;
    static const uint32_t SW_LOCK62_SEC__inaccessible = 3;

    // Software lock register for page 63. Locks are initialised from the OTP lock pages at reset. This register can be written to further advance the lock state of each page (until next reset), and read to check the current lock state of a page.
    // Reset value: 0x00000000
    BEGIN_TYPE(SW_LOCK63_t, uint32_t)
        // Non-secure lock status. Writes are OR'd with the current value.
        ADD_BITFIELD_RW(NSEC, 2, 2)
        // Secure lock status. Writes are OR'd with the current value. This field is read-only to Non-secure code.
        ADD_BITFIELD_RW(SEC, 0, 2)
    END_TYPE()

    static const uint32_t SW_LOCK63_NSEC__read_write = 0;
    static const uint32_t SW_LOCK63_NSEC__read_only = 1;
    static const uint32_t SW_LOCK63_NSEC__inaccessible = 3;
    static const uint32_t SW_LOCK63_SEC__read_write = 0;
    static const uint32_t SW_LOCK63_SEC__read_only = 1;
    static const uint32_t SW_LOCK63_SEC__inaccessible = 3;

    // Dispatch instructions to the SBPI interface, used for programming the OTP fuses.
    // Reset value: 0x00000000
    BEGIN_TYPE(SBPI_INSTR_t, uint32_t)
        // Execute instruction
        ADD_BITFIELD_WO(EXEC, 30, 1)
        // Payload type is write
        ADD_BITFIELD_RW(IS_WR, 29, 1)
        // Instruction has payload (data to be written or to be read)
        ADD_BITFIELD_RW(HAS_PAYLOAD, 28, 1)
        // Instruction payload size in bytes minus 1
        ADD_BITFIELD_RW(PAYLOAD_SIZE_M1, 24, 4)
        // Instruction target, it can be PMC (0x3a) or DAP (0x02)
        ADD_BITFIELD_RW(TARGET, 16, 8)
        ADD_BITFIELD_RW(CMD, 8, 8)
        // wdata to be used only when payload_size_m1=0
        ADD_BITFIELD_RW(SHORT_WDATA, 0, 8)
    END_TYPE()

    // SBPI write payload bytes 3..0
    // Reset value: 0x00000000
    BEGIN_TYPE(SBPI_WDATA_0_t, uint32_t)
        ADD_BITFIELD_RW(SBPI_WDATA_0, 0, 32)
    END_TYPE()

    // SBPI write payload bytes 7..4
    // Reset value: 0x00000000
    BEGIN_TYPE(SBPI_WDATA_1_t, uint32_t)
        ADD_BITFIELD_RW(SBPI_WDATA_1, 0, 32)
    END_TYPE()

    // SBPI write payload bytes 11..8
    // Reset value: 0x00000000
    BEGIN_TYPE(SBPI_WDATA_2_t, uint32_t)
        ADD_BITFIELD_RW(SBPI_WDATA_2, 0, 32)
    END_TYPE()

    // SBPI write payload bytes 15..12
    // Reset value: 0x00000000
    BEGIN_TYPE(SBPI_WDATA_3_t, uint32_t)
        ADD_BITFIELD_RW(SBPI_WDATA_3, 0, 32)
    END_TYPE()

    // Read payload bytes 3..0. Once read, the data in the register will automatically clear to 0.
    // Reset value: 0x00000000
    BEGIN_TYPE(SBPI_RDATA_0_t, uint32_t)
        ADD_BITFIELD_RO(SBPI_RDATA_0, 0, 32)
    END_TYPE()

    // Read payload bytes 7..4. Once read, the data in the register will automatically clear to 0.
    // Reset value: 0x00000000
    BEGIN_TYPE(SBPI_RDATA_1_t, uint32_t)
        ADD_BITFIELD_RO(SBPI_RDATA_1, 0, 32)
    END_TYPE()

    // Read payload bytes 11..8. Once read, the data in the register will automatically clear to 0.
    // Reset value: 0x00000000
    BEGIN_TYPE(SBPI_RDATA_2_t, uint32_t)
        ADD_BITFIELD_RO(SBPI_RDATA_2, 0, 32)
    END_TYPE()

    // Read payload bytes 15..12. Once read, the data in the register will automatically clear to 0.
    // Reset value: 0x00000000
    BEGIN_TYPE(SBPI_RDATA_3_t, uint32_t)
        ADD_BITFIELD_RO(SBPI_RDATA_3, 0, 32)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(SBPI_STATUS_t, uint32_t)
        // SBPI MISO (master in - slave out): response from SBPI
        ADD_BITFIELD_RO(MISO, 16, 8)
        // SBPI flag
        ADD_BITFIELD_RO(FLAG, 12, 1)
        // Last instruction missed (dropped), as the previous has not finished running
        ADD_BITFIELD_RW(INSTR_MISS, 8, 1)
        // Last instruction done
        ADD_BITFIELD_RW(INSTR_DONE, 4, 1)
        // Read command has returned data
        ADD_BITFIELD_RW(RDATA_VLD, 0, 1)
    END_TYPE()

    // Controls for APB data read interface (USER interface)
    // Reset value: 0x00000001
    BEGIN_TYPE(USR_t, uint32_t)
        // Power-down; 1 disables current reference. Must be 0 to read data from the OTP.
        ADD_BITFIELD_RW(PD, 4, 1)
        // 1 enables USER interface; 0 disables USER interface (enables SBPI). This bit must be cleared before performing any SBPI access, such as when programming the OTP. The APB data read interface (USER interface) will be inaccessible during this time, and will return a bus error if any read is attempted.
        ADD_BITFIELD_RW(DCTRL, 0, 1)
    END_TYPE()

    // Debug for OTP power-on state machine
    // Reset value: 0x00000000
    BEGIN_TYPE(DBG_t, uint32_t)
        // The chip is in RMA mode
        ADD_BITFIELD_RO(CUSTOMER_RMA_FLAG, 12, 1)
        // Monitor the PSM FSM's state
        ADD_BITFIELD_RO(PSM_STATE, 4, 4)
        // Ring oscillator is up and running
        ADD_BITFIELD_RO(ROSC_UP, 3, 1)
        // Ring oscillator was seen up and running
        ADD_BITFIELD_RW(ROSC_UP_SEEN, 2, 1)
        // PSM boot done status flag
        ADD_BITFIELD_RO(BOOT_DONE, 1, 1)
        // PSM done status flag
        ADD_BITFIELD_RO(PSM_DONE, 0, 1)
    END_TYPE()

    // During BIST, count address locations that have at least one leaky bit
    // Reset value: 0x0fff0000
    BEGIN_TYPE(BIST_t, uint32_t)
        // Flag if the count of address locations with at least one leaky bit exceeds cnt_max
        ADD_BITFIELD_RO(CNT_FAIL, 30, 1)
        // Clear counter before use
        ADD_BITFIELD_WO(CNT_CLR, 29, 1)
        // Enable the counter before the BIST function is initiated
        ADD_BITFIELD_RW(CNT_ENA, 28, 1)
        // The cnt_fail flag will be set if the number of leaky locations exceeds this number
        ADD_BITFIELD_RW(CNT_MAX, 16, 12)
        // Number of locations that have at least one leaky bit. Note: This count is true only if the BIST was initiated without the fix option.
        ADD_BITFIELD_RO(CNT, 0, 13)
    END_TYPE()

    // Word 0 (bits 31..0) of the key. Write only, read returns 0x0
    // Reset value: 0x00000000
    BEGIN_TYPE(CRT_KEY_W0_t, uint32_t)
        ADD_BITFIELD_WO(CRT_KEY_W0, 0, 32)
    END_TYPE()

    // Word 1 (bits 63..32) of the key. Write only, read returns 0x0
    // Reset value: 0x00000000
    BEGIN_TYPE(CRT_KEY_W1_t, uint32_t)
        ADD_BITFIELD_WO(CRT_KEY_W1, 0, 32)
    END_TYPE()

    // Word 2 (bits 95..64) of the key. Write only, read returns 0x0
    // Reset value: 0x00000000
    BEGIN_TYPE(CRT_KEY_W2_t, uint32_t)
        ADD_BITFIELD_WO(CRT_KEY_W2, 0, 32)
    END_TYPE()

    // Word 3 (bits 127..96) of the key. Write only, read returns 0x0
    // Reset value: 0x00000000
    BEGIN_TYPE(CRT_KEY_W3_t, uint32_t)
        ADD_BITFIELD_WO(CRT_KEY_W3, 0, 32)
    END_TYPE()

    // Quickly check values of critical flags read during boot up
    // Reset value: 0x00000000
    BEGIN_TYPE(CRITICAL_t, uint32_t)
        ADD_BITFIELD_RO(RISCV_DISABLE, 17, 1)
        ADD_BITFIELD_RO(ARM_DISABLE, 16, 1)
        ADD_BITFIELD_RO(GLITCH_DETECTOR_SENS, 5, 2)
        ADD_BITFIELD_RO(GLITCH_DETECTOR_ENABLE, 4, 1)
        ADD_BITFIELD_RO(DEFAULT_ARCHSEL, 3, 1)
        ADD_BITFIELD_RO(DEBUG_DISABLE, 2, 1)
        ADD_BITFIELD_RO(SECURE_DEBUG_DISABLE, 1, 1)
        ADD_BITFIELD_RO(SECURE_BOOT_ENABLE, 0, 1)
    END_TYPE()

    // Which keys were valid (enrolled) at boot time
    // Reset value: 0x00000000
    BEGIN_TYPE(KEY_VALID_t, uint32_t)
        ADD_BITFIELD_RO(KEY_VALID, 0, 8)
    END_TYPE()

    // Enable a debug feature that has been disabled. Debug features are disabled if one of the relevant critical boot flags is set in OTP (DEBUG_DISABLE or SECURE_DEBUG_DISABLE), OR if a debug key is marked valid in OTP, and the matching key value has not been supplied over SWD. Specifically: - The DEBUG_DISABLE flag disables all debug features. This can be fully overridden by setting all bits of this register. - The SECURE_DEBUG_DISABLE flag disables secure processor debug. This can be fully overridden by setting the PROC0_SECURE and PROC1_SECURE bits of this register. - If a single debug key has been registered, and no matching key value has been supplied over SWD, then all debug features are disabled. This can be fully overridden by setting all bits of this register. - If both debug keys have been registered, and the Non-secure key's value (key 6) has been supplied over SWD, secure processor debug is disabled. This can be fully overridden by setting the PROC0_SECURE and PROC1_SECURE bits of this register. - If both debug keys have been registered, and the Secure key's value (key 5) has been supplied over SWD, then no debug features are disabled by the key mechanism. However, note that in this case debug features may still be disabled by the critical boot flags.
    // Reset value: 0x00000000
    BEGIN_TYPE(DEBUGEN_t, uint32_t)
        // Enable other debug components. Specifically, the CTI, and the APB-AP used to access the RISC-V Debug Module. These components are disabled by default if either of the debug disable critical flags is set, or if at least one debug key has been enrolled and the least secure of these enrolled key values has not been provided over SWD.
        ADD_BITFIELD_RW(MISC, 8, 1)
        // Permit core 1's Mem-AP to generate Secure accesses, assuming it is enabled at all. Also enable secure debug of core 1 (SPIDEN and SPNIDEN). Secure debug of core 1 is disabled by default if the secure debug disable critical flag is set, or if at least one debug key has been enrolled and the most secure of these enrolled key values not yet provided over SWD.
        ADD_BITFIELD_RW(PROC1_SECURE, 3, 1)
        // Enable core 1's Mem-AP if it is currently disabled. The Mem-AP is disabled by default if either of the debug disable critical flags is set, or if at least one debug key has been enrolled and the least secure of these enrolled key values has not been provided over SWD.
        ADD_BITFIELD_RW(PROC1, 2, 1)
        // Permit core 0's Mem-AP to generate Secure accesses, assuming it is enabled at all. Also enable secure debug of core 0 (SPIDEN and SPNIDEN). Secure debug of core 0 is disabled by default if the secure debug disable critical flag is set, or if at least one debug key has been enrolled and the most secure of these enrolled key values not yet provided over SWD. Note also that core Mem-APs are unconditionally disabled when a core is switched to RISC-V mode (by setting the ARCHSEL bit and performing a warm reset of the core).
        ADD_BITFIELD_RW(PROC0_SECURE, 1, 1)
        // Enable core 0's Mem-AP if it is currently disabled. The Mem-AP is disabled by default if either of the debug disable critical flags is set, or if at least one debug key has been enrolled and the least secure of these enrolled key values has not been provided over SWD. Note also that core Mem-APs are unconditionally disabled when a core is switched to RISC-V mode (by setting the ARCHSEL bit and performing a warm reset of the core).
        ADD_BITFIELD_RW(PROC0, 0, 1)
    END_TYPE()

    // Write 1s to lock corresponding bits in DEBUGEN. This register is reset by the processor cold reset.
    // Reset value: 0x00000000
    BEGIN_TYPE(DEBUGEN_LOCK_t, uint32_t)
        // Write 1 to lock the MISC bit of DEBUGEN. Can't be cleared once set.
        ADD_BITFIELD_RW(MISC, 8, 1)
        // Write 1 to lock the PROC1_SECURE bit of DEBUGEN. Can't be cleared once set.
        ADD_BITFIELD_RW(PROC1_SECURE, 3, 1)
        // Write 1 to lock the PROC1 bit of DEBUGEN. Can't be cleared once set.
        ADD_BITFIELD_RW(PROC1, 2, 1)
        // Write 1 to lock the PROC0_SECURE bit of DEBUGEN. Can't be cleared once set.
        ADD_BITFIELD_RW(PROC0_SECURE, 1, 1)
        // Write 1 to lock the PROC0 bit of DEBUGEN. Can't be cleared once set.
        ADD_BITFIELD_RW(PROC0, 0, 1)
    END_TYPE()

    // Architecture select (Arm/RISC-V). The default and allowable values of this register are constrained by the critical boot flags. This register is reset by the earliest reset in the switched core power domain (before a processor cold reset). Cores sample their architecture select signal on a warm reset. The source of the warm reset could be the system power-up state machine, the watchdog timer, Arm SYSRESETREQ or from RISC-V hartresetreq. Note that when an Arm core is deselected, its cold reset domain is also held in reset, since in particular the SYSRESETREQ bit becomes inaccessible once the core is deselected. Note also the RISC-V cores do not have a cold reset domain, since their corresponding controls are located in the Debug Module.
    // Reset value: 0x00000000
    BEGIN_TYPE(ARCHSEL_t, uint32_t)
        // Select architecture for core 1.
        ADD_BITFIELD_RW(CORE1, 1, 1)
        // Select architecture for core 0.
        ADD_BITFIELD_RW(CORE0, 0, 1)
    END_TYPE()

    // Switch core 1 to Arm (Cortex-M33)
    static const uint32_t ARCHSEL_CORE1__arm = 0;
    // Switch core 1 to RISC-V (Hazard3)
    static const uint32_t ARCHSEL_CORE1__riscv = 1;
    // Switch core 0 to Arm (Cortex-M33)
    static const uint32_t ARCHSEL_CORE0__arm = 0;
    // Switch core 0 to RISC-V (Hazard3)
    static const uint32_t ARCHSEL_CORE0__riscv = 1;

    // Get the current architecture select state of each core. Cores sample the current value of the ARCHSEL register when their warm reset is released, at which point the corresponding bit in this register will also update.
    // Reset value: 0x00000000
    BEGIN_TYPE(ARCHSEL_STATUS_t, uint32_t)
        // Current architecture for core 0. Updated on processor warm reset.
        ADD_BITFIELD_RO(CORE1, 1, 1)
        // Current architecture for core 0. Updated on processor warm reset.
        ADD_BITFIELD_RO(CORE0, 0, 1)
    END_TYPE()

    // Core 1 is currently Arm (Cortex-M33)
    static const uint32_t ARCHSEL_STATUS_CORE1__arm = 0;
    // Core 1 is currently RISC-V (Hazard3)
    static const uint32_t ARCHSEL_STATUS_CORE1__riscv = 1;
    // Core 0 is currently Arm (Cortex-M33)
    static const uint32_t ARCHSEL_STATUS_CORE0__arm = 0;
    // Core 0 is currently RISC-V (Hazard3)
    static const uint32_t ARCHSEL_STATUS_CORE0__riscv = 1;

    // Tell the bootrom to ignore scratch register boot vectors (both power manager and watchdog) on the next power up. If an early boot stage has soft-locked some OTP pages in order to protect their contents from later stages, there is a risk that Secure code running at a later stage can unlock the pages by performing a watchdog reset that resets the OTP. This register can be used to ensure that the bootloader runs as normal on the next power up, preventing Secure code at a later stage from accessing OTP in its unlocked state. Should be used in conjunction with the power manager BOOTDIS register.
    // Reset value: 0x00000000
    BEGIN_TYPE(BOOTDIS_t, uint32_t)
        // This flag always ORs writes into its current contents. It can be set but not cleared by software. The BOOTDIS_NEXT bit is OR'd into the BOOTDIS_NOW bit when the core is powered down. Simultaneously, the BOOTDIS_NEXT bit is cleared. Setting this bit means that the boot scratch registers will be ignored following the next core power down. This flag should be set by an early boot stage that has soft-locked OTP pages, to prevent later stages from unlocking it via watchdog reset.
        ADD_BITFIELD_RW(NEXT, 1, 1)
        // When the core is powered down, the current value of BOOTDIS_NEXT is OR'd into BOOTDIS_NOW, and BOOTDIS_NEXT is cleared. The bootrom checks this flag before reading the boot scratch registers. If it is set, the bootrom clears it, and ignores the BOOT registers. This prevents Secure software from diverting the boot path before a bootloader has had the chance to soft lock OTP pages containing sensitive data.
        ADD_BITFIELD_RW(NOW, 0, 1)
    END_TYPE()

    // Raw Interrupts
    // Reset value: 0x00000000
    BEGIN_TYPE(INTR_t, uint32_t)
        ADD_BITFIELD_RW(APB_RD_NSEC_FAIL, 4, 1)
        ADD_BITFIELD_RW(APB_RD_SEC_FAIL, 3, 1)
        ADD_BITFIELD_RW(APB_DCTRL_FAIL, 2, 1)
        ADD_BITFIELD_RW(SBPI_WR_FAIL, 1, 1)
        ADD_BITFIELD_RO(SBPI_FLAG_N, 0, 1)
    END_TYPE()

    // Interrupt Enable
    // Reset value: 0x00000000
    BEGIN_TYPE(INTE_t, uint32_t)
        ADD_BITFIELD_RW(APB_RD_NSEC_FAIL, 4, 1)
        ADD_BITFIELD_RW(APB_RD_SEC_FAIL, 3, 1)
        ADD_BITFIELD_RW(APB_DCTRL_FAIL, 2, 1)
        ADD_BITFIELD_RW(SBPI_WR_FAIL, 1, 1)
        ADD_BITFIELD_RW(SBPI_FLAG_N, 0, 1)
    END_TYPE()

    // Interrupt Force
    // Reset value: 0x00000000
    BEGIN_TYPE(INTF_t, uint32_t)
        ADD_BITFIELD_RW(APB_RD_NSEC_FAIL, 4, 1)
        ADD_BITFIELD_RW(APB_RD_SEC_FAIL, 3, 1)
        ADD_BITFIELD_RW(APB_DCTRL_FAIL, 2, 1)
        ADD_BITFIELD_RW(SBPI_WR_FAIL, 1, 1)
        ADD_BITFIELD_RW(SBPI_FLAG_N, 0, 1)
    END_TYPE()

    // Interrupt status after masking & forcing
    // Reset value: 0x00000000
    BEGIN_TYPE(INTS_t, uint32_t)
        ADD_BITFIELD_RO(APB_RD_NSEC_FAIL, 4, 1)
        ADD_BITFIELD_RO(APB_RD_SEC_FAIL, 3, 1)
        ADD_BITFIELD_RO(APB_DCTRL_FAIL, 2, 1)
        ADD_BITFIELD_RO(SBPI_WR_FAIL, 1, 1)
        ADD_BITFIELD_RO(SBPI_FLAG_N, 0, 1)
    END_TYPE()

    struct OTP_t {
        SW_LOCK0_t                    SW_LOCK0;
        SW_LOCK1_t                    SW_LOCK1;
        SW_LOCK2_t                    SW_LOCK2;
        SW_LOCK3_t                    SW_LOCK3;
        SW_LOCK4_t                    SW_LOCK4;
        SW_LOCK5_t                    SW_LOCK5;
        SW_LOCK6_t                    SW_LOCK6;
        SW_LOCK7_t                    SW_LOCK7;
        SW_LOCK8_t                    SW_LOCK8;
        SW_LOCK9_t                    SW_LOCK9;
        SW_LOCK10_t                   SW_LOCK10;
        SW_LOCK11_t                   SW_LOCK11;
        SW_LOCK12_t                   SW_LOCK12;
        SW_LOCK13_t                   SW_LOCK13;
        SW_LOCK14_t                   SW_LOCK14;
        SW_LOCK15_t                   SW_LOCK15;
        SW_LOCK16_t                   SW_LOCK16;
        SW_LOCK17_t                   SW_LOCK17;
        SW_LOCK18_t                   SW_LOCK18;
        SW_LOCK19_t                   SW_LOCK19;
        SW_LOCK20_t                   SW_LOCK20;
        SW_LOCK21_t                   SW_LOCK21;
        SW_LOCK22_t                   SW_LOCK22;
        SW_LOCK23_t                   SW_LOCK23;
        SW_LOCK24_t                   SW_LOCK24;
        SW_LOCK25_t                   SW_LOCK25;
        SW_LOCK26_t                   SW_LOCK26;
        SW_LOCK27_t                   SW_LOCK27;
        SW_LOCK28_t                   SW_LOCK28;
        SW_LOCK29_t                   SW_LOCK29;
        SW_LOCK30_t                   SW_LOCK30;
        SW_LOCK31_t                   SW_LOCK31;
        SW_LOCK32_t                   SW_LOCK32;
        SW_LOCK33_t                   SW_LOCK33;
        SW_LOCK34_t                   SW_LOCK34;
        SW_LOCK35_t                   SW_LOCK35;
        SW_LOCK36_t                   SW_LOCK36;
        SW_LOCK37_t                   SW_LOCK37;
        SW_LOCK38_t                   SW_LOCK38;
        SW_LOCK39_t                   SW_LOCK39;
        SW_LOCK40_t                   SW_LOCK40;
        SW_LOCK41_t                   SW_LOCK41;
        SW_LOCK42_t                   SW_LOCK42;
        SW_LOCK43_t                   SW_LOCK43;
        SW_LOCK44_t                   SW_LOCK44;
        SW_LOCK45_t                   SW_LOCK45;
        SW_LOCK46_t                   SW_LOCK46;
        SW_LOCK47_t                   SW_LOCK47;
        SW_LOCK48_t                   SW_LOCK48;
        SW_LOCK49_t                   SW_LOCK49;
        SW_LOCK50_t                   SW_LOCK50;
        SW_LOCK51_t                   SW_LOCK51;
        SW_LOCK52_t                   SW_LOCK52;
        SW_LOCK53_t                   SW_LOCK53;
        SW_LOCK54_t                   SW_LOCK54;
        SW_LOCK55_t                   SW_LOCK55;
        SW_LOCK56_t                   SW_LOCK56;
        SW_LOCK57_t                   SW_LOCK57;
        SW_LOCK58_t                   SW_LOCK58;
        SW_LOCK59_t                   SW_LOCK59;
        SW_LOCK60_t                   SW_LOCK60;
        SW_LOCK61_t                   SW_LOCK61;
        SW_LOCK62_t                   SW_LOCK62;
        SW_LOCK63_t                   SW_LOCK63;
        SBPI_INSTR_t                  SBPI_INSTR;
        SBPI_WDATA_0_t                SBPI_WDATA_0;
        SBPI_WDATA_1_t                SBPI_WDATA_1;
        SBPI_WDATA_2_t                SBPI_WDATA_2;
        SBPI_WDATA_3_t                SBPI_WDATA_3;
        SBPI_RDATA_0_t                SBPI_RDATA_0;
        SBPI_RDATA_1_t                SBPI_RDATA_1;
        SBPI_RDATA_2_t                SBPI_RDATA_2;
        SBPI_RDATA_3_t                SBPI_RDATA_3;
        SBPI_STATUS_t                 SBPI_STATUS;
        USR_t                         USR;
        DBG_t                         DBG;
        uint32_t                      reserved0;
        BIST_t                        BIST;
        CRT_KEY_W0_t                  CRT_KEY_W0;
        CRT_KEY_W1_t                  CRT_KEY_W1;
        CRT_KEY_W2_t                  CRT_KEY_W2;
        CRT_KEY_W3_t                  CRT_KEY_W3;
        CRITICAL_t                    CRITICAL;
        KEY_VALID_t                   KEY_VALID;
        DEBUGEN_t                     DEBUGEN;
        DEBUGEN_LOCK_t                DEBUGEN_LOCK;
        ARCHSEL_t                     ARCHSEL;
        ARCHSEL_STATUS_t              ARCHSEL_STATUS;
        BOOTDIS_t                     BOOTDIS;
        INTR_t                        INTR;
        INTE_t                        INTE;
        INTF_t                        INTF;
        INTS_t                        INTS;
    };

    static OTP_t & OTP = (*(OTP_t *)0x40120000);
    static OTP_t & OTP_XOR = (*(OTP_t *)0x40121000);
    static OTP_t & OTP_SET = (*(OTP_t *)0x40122000);
    static OTP_t & OTP_CLR = (*(OTP_t *)0x40123000);

} // _OTP_

// Predefined OTP data layout for RP2350
namespace _OTP_DATA_  {

    // Bits 15:0 of public device ID. (ECC) The CHIPID0..3 rows contain a 64-bit random identifier for this chip, which can be read from the USB bootloader PICOBOOT interface or from the get_sys_info ROM API. The number of random bits makes the occurrence of twins exceedingly unlikely: for example, a fleet of a hundred million devices has a 99.97% probability of no twinned IDs. This is estimated to be lower than the occurrence of process errors in the assignment of sequential random IDs, and for practical purposes CHIPID may be treated as unique.
    BEGIN_TYPE(CHIPID0_t, uint16_t)
        ADD_BITFIELD_RO(CHIPID0, 0, 16)
    END_TYPE()

    // Bits 31:16 of public device ID (ECC)
    BEGIN_TYPE(CHIPID1_t, uint16_t)
        ADD_BITFIELD_RO(CHIPID1, 0, 16)
    END_TYPE()

    // Bits 47:32 of public device ID (ECC)
    BEGIN_TYPE(CHIPID2_t, uint16_t)
        ADD_BITFIELD_RO(CHIPID2, 0, 16)
    END_TYPE()

    // Bits 63:48 of public device ID (ECC)
    BEGIN_TYPE(CHIPID3_t, uint16_t)
        ADD_BITFIELD_RO(CHIPID3, 0, 16)
    END_TYPE()

    // Bits 15:0 of private per-device random number (ECC) The RANDID0..7 rows form a 128-bit random number generated during device test. This ID is not exposed through the USB PICOBOOT GET_INFO command or the ROM `get_sys_info()` API. However note that the USB PICOBOOT OTP access point can read the entirety of page 0, so this value is not meaningfully private unless the USB PICOBOOT interface is disabled via the DISABLE_BOOTSEL_USB_PICOBOOT_IFC flag in BOOT_FLAGS0.
    BEGIN_TYPE(RANDID0_t, uint16_t)
        ADD_BITFIELD_RO(RANDID0, 0, 16)
    END_TYPE()

    // Bits 31:16 of private per-device random number (ECC)
    BEGIN_TYPE(RANDID1_t, uint16_t)
        ADD_BITFIELD_RO(RANDID1, 0, 16)
    END_TYPE()

    // Bits 47:32 of private per-device random number (ECC)
    BEGIN_TYPE(RANDID2_t, uint16_t)
        ADD_BITFIELD_RO(RANDID2, 0, 16)
    END_TYPE()

    // Bits 63:48 of private per-device random number (ECC)
    BEGIN_TYPE(RANDID3_t, uint16_t)
        ADD_BITFIELD_RO(RANDID3, 0, 16)
    END_TYPE()

    // Bits 79:64 of private per-device random number (ECC)
    BEGIN_TYPE(RANDID4_t, uint16_t)
        ADD_BITFIELD_RO(RANDID4, 0, 16)
    END_TYPE()

    // Bits 95:80 of private per-device random number (ECC)
    BEGIN_TYPE(RANDID5_t, uint16_t)
        ADD_BITFIELD_RO(RANDID5, 0, 16)
    END_TYPE()

    // Bits 111:96 of private per-device random number (ECC)
    BEGIN_TYPE(RANDID6_t, uint16_t)
        ADD_BITFIELD_RO(RANDID6, 0, 16)
    END_TYPE()

    // Bits 127:112 of private per-device random number (ECC)
    BEGIN_TYPE(RANDID7_t, uint16_t)
        ADD_BITFIELD_RO(RANDID7, 0, 16)
    END_TYPE()

    // Ring oscillator frequency in kHz, measured during manufacturing (ECC) This is measured at 1.1 V, at room temperature, with the ROSC configuration registers in their reset state.
    BEGIN_TYPE(ROSC_CALIB_t, uint16_t)
        ADD_BITFIELD_RO(ROSC_CALIB, 0, 16)
    END_TYPE()

    // Low-power oscillator frequency in Hz, measured during manufacturing (ECC) This is measured at 1.1V, at room temperature, with the LPOSC trim register in its reset state.
    BEGIN_TYPE(LPOSC_CALIB_t, uint16_t)
        ADD_BITFIELD_RO(LPOSC_CALIB, 0, 16)
    END_TYPE()

    // The number of main user GPIOs (bank 0). Should read 48 in the QFN80 package, and 30 in the QFN60 package. (ECC)
    BEGIN_TYPE(NUM_GPIOS_t, uint16_t)
        ADD_BITFIELD_RO(NUM_GPIOS, 0, 8)
    END_TYPE()

    // Lower 16 bits of CRC32 of OTP addresses 0x00 through 0x6b (polynomial 0x4c11db7, input reflected, output reflected, seed all-ones, final XOR all-ones) (ECC)
    BEGIN_TYPE(INFO_CRC0_t, uint16_t)
        ADD_BITFIELD_RO(INFO_CRC0, 0, 16)
    END_TYPE()

    // Upper 16 bits of CRC32 of OTP addresses 0x00 through 0x6b (ECC)
    BEGIN_TYPE(INFO_CRC1_t, uint16_t)
        ADD_BITFIELD_RO(INFO_CRC1, 0, 16)
    END_TYPE()

    // Stores information about external flash device(s). (ECC) Assumed to be valid if BOOT_FLAGS0_FLASH_DEVINFO_ENABLE is set.
    // Reset value: 0x0000
    BEGIN_TYPE(FLASH_DEVINFO_t, uint16_t)
        // The size of the flash/PSRAM device on chip select 1 (addressable at 0x11000000 through 0x11ffffff). A value of zero is decoded as a size of zero (no device). Nonzero values are decoded as 4kiB << CS1_SIZE. For example, four megabytes is encoded with a CS1_SIZE value of 10, and 16 megabytes is encoded with a CS1_SIZE value of 12. When BOOT_FLAGS0_FLASH_DEVINFO_ENABLE is not set, a default of zero is used.
        ADD_BITFIELD_RO(CS1_SIZE, 12, 4)
        // The size of the flash/PSRAM device on chip select 0 (addressable at 0x10000000 through 0x10ffffff). A value of zero is decoded as a size of zero (no device). Nonzero values are decoded as 4kiB << CS0_SIZE. For example, four megabytes is encoded with a CS0_SIZE value of 10, and 16 megabytes is encoded with a CS0_SIZE value of 12. When BOOT_FLAGS0_FLASH_DEVINFO_ENABLE is not set, a default of 12 (16 MiB) is used.
        ADD_BITFIELD_RO(CS0_SIZE, 8, 4)
        // If true, all attached devices are assumed to support (or ignore, in the case of PSRAM) a block erase command with a command prefix of D8h, an erase size of 64 kiB, and a 24-bit address. Almost all 25-series flash devices support this command. If set, the bootrom will use the D8h erase command where it is able, to accelerate bulk erase operations. This makes flash programming faster. When BOOT_FLAGS0_FLASH_DEVINFO_ENABLE is not set, this field defaults to false.
        ADD_BITFIELD_RO(D8H_ERASE_SUPPORTED, 7, 1)
        // Indicate a GPIO number to be used for the secondary flash chip select (CS1), which selects the external QSPI device mapped at system addresses 0x11000000 through 0x11ffffff. There is no such configuration for CS0, as the primary chip select has a dedicated pin. On RP2350 the permissible GPIO numbers are 0, 8, 19 and 47. Ignored if CS1_size is zero. If CS1_SIZE is nonzero, the bootrom will automatically configure this GPIO as a second chip select upon entering the flash boot path, or entering any other path that may use the QSPI flash interface, such as BOOTSEL mode (nsboot).
        ADD_BITFIELD_RO(CS1_GPIO, 0, 6)
    END_TYPE()

    static const uint32_t FLASH_DEVINFO_CS1_SIZE__NONE = 0;
    static const uint32_t FLASH_DEVINFO_CS1_SIZE__8K = 1;
    static const uint32_t FLASH_DEVINFO_CS1_SIZE__16K = 2;
    static const uint32_t FLASH_DEVINFO_CS1_SIZE__32K = 3;
    static const uint32_t FLASH_DEVINFO_CS1_SIZE__64k = 4;
    static const uint32_t FLASH_DEVINFO_CS1_SIZE__128K = 5;
    static const uint32_t FLASH_DEVINFO_CS1_SIZE__256K = 6;
    static const uint32_t FLASH_DEVINFO_CS1_SIZE__512K = 7;
    static const uint32_t FLASH_DEVINFO_CS1_SIZE__1M = 8;
    static const uint32_t FLASH_DEVINFO_CS1_SIZE__2M = 9;
    static const uint32_t FLASH_DEVINFO_CS1_SIZE__4M = 10;
    static const uint32_t FLASH_DEVINFO_CS1_SIZE__8M = 11;
    static const uint32_t FLASH_DEVINFO_CS1_SIZE__16M = 12;
    static const uint32_t FLASH_DEVINFO_CS0_SIZE__NONE = 0;
    static const uint32_t FLASH_DEVINFO_CS0_SIZE__8K = 1;
    static const uint32_t FLASH_DEVINFO_CS0_SIZE__16K = 2;
    static const uint32_t FLASH_DEVINFO_CS0_SIZE__32K = 3;
    static const uint32_t FLASH_DEVINFO_CS0_SIZE__64k = 4;
    static const uint32_t FLASH_DEVINFO_CS0_SIZE__128K = 5;
    static const uint32_t FLASH_DEVINFO_CS0_SIZE__256K = 6;
    static const uint32_t FLASH_DEVINFO_CS0_SIZE__512K = 7;
    static const uint32_t FLASH_DEVINFO_CS0_SIZE__1M = 8;
    static const uint32_t FLASH_DEVINFO_CS0_SIZE__2M = 9;
    static const uint32_t FLASH_DEVINFO_CS0_SIZE__4M = 10;
    static const uint32_t FLASH_DEVINFO_CS0_SIZE__8M = 11;
    static const uint32_t FLASH_DEVINFO_CS0_SIZE__16M = 12;

    // Gap between partition table slot 0 and slot 1 at the start of flash (the default size is 4096 bytes) (ECC) Enabled by the OVERRIDE_FLASH_PARTITION_SLOT_SIZE bit in BOOT_FLAGS, the size is 4096 * (value + 1)
    BEGIN_TYPE(FLASH_PARTITION_SLOT_SIZE_t, uint16_t)
        ADD_BITFIELD_RO(FLASH_PARTITION_SLOT_SIZE, 0, 16)
    END_TYPE()

    // Pin configuration for LED status, used by USB bootloader. (ECC)  Must be valid if BOOT_FLAGS0_ENABLE_BOOTSEL_LED is set.
    // Reset value: 0x0000
    BEGIN_TYPE(BOOTSEL_LED_CFG_t, uint16_t)
        // LED is active-low. (Default: active-high.)
        ADD_BITFIELD_RO(ACTIVELOW, 8, 1)
        // GPIO index to use for bootloader activity LED.
        ADD_BITFIELD_RO(PIN, 0, 6)
    END_TYPE()

    // Optional PLL configuration for BOOTSEL mode. (ECC) This should be configured to produce an exact 48 MHz based on the crystal oscillator frequency. User mode software may also use this value to calculate the expected crystal frequency based on an assumed 48 MHz PLL output. If no configuration is given, the crystal is assumed to be 12 MHz. The PLL frequency can be calculated as: PLL out = (XOSC frequency / (REFDIV+1)) x FBDIV / (POSTDIV1 x POSTDIV2) Conversely the crystal frequency can be calculated as: XOSC frequency = 48 MHz x (REFDIV+1) x (POSTDIV1 x POSTDIV2) / FBDIV (Note the  +1 on REFDIV is because the value stored in this OTP location is the actual divisor value minus one.) Used if and only if ENABLE_BOOTSEL_NON_DEFAULT_PLL_XOSC_CFG is set in BOOT_FLAGS0. That bit should be set only after this row and BOOTSEL_XOSC_CFG are both correctly programmed.
    // Reset value: 0x0000
    BEGIN_TYPE(BOOTSEL_PLL_CFG_t, uint16_t)
        // PLL reference divisor, minus one. Programming a value of 0 means a reference divisor of 1. Programming a value of 1 means a reference divisor of 2 (for exceptionally fast XIN inputs)
        ADD_BITFIELD_RO(REFDIV, 15, 1)
        // PLL post-divide 2 divisor, in the range 1..7 inclusive.
        ADD_BITFIELD_RO(POSTDIV2, 12, 3)
        // PLL post-divide 1 divisor, in the range 1..7 inclusive.
        ADD_BITFIELD_RO(POSTDIV1, 9, 3)
        // PLL feedback divisor, in the range 16..320 inclusive.
        ADD_BITFIELD_RO(FBDIV, 0, 9)
    END_TYPE()

    // Non-default crystal oscillator configuration for the USB bootloader. (ECC) These values may also be used by user code configuring the crystal oscillator. Used if and only if ENABLE_BOOTSEL_NON_DEFAULT_PLL_XOSC_CFG is set in BOOT_FLAGS0. That bit should be set only after this row and BOOTSEL_PLL_CFG are both correctly programmed.
    // Reset value: 0x0000
    BEGIN_TYPE(BOOTSEL_XOSC_CFG_t, uint16_t)
        // Value of the XOSC_CTRL_FREQ_RANGE register.
        ADD_BITFIELD_RO(RANGE, 14, 2)
        // Value of the XOSC_STARTUP register
        ADD_BITFIELD_RO(STARTUP, 0, 14)
    END_TYPE()

    static const uint32_t BOOTSEL_XOSC_CFG_RANGE__1_15MHZ = 0;
    static const uint32_t BOOTSEL_XOSC_CFG_RANGE__10_30MHZ = 1;
    static const uint32_t BOOTSEL_XOSC_CFG_RANGE__25_60MHZ = 2;
    static const uint32_t BOOTSEL_XOSC_CFG_RANGE__40_100MHZ = 3;

    // Row index of the USB_WHITE_LABEL structure within OTP (ECC) The table has 16 rows, each of which are also ECC and marked valid by the corresponding valid bit in USB_BOOT_FLAGS (ECC). The entries are either _VALUEs where the 16 bit value is used as is, or _STRDEFs which acts as a pointers to a string value. The value stored in a _STRDEF is two separate bytes: The low seven bits of the first (LSB) byte indicates the number of characters in the string, and the top bit of the first (LSB) byte if set to indicate that each character in the string is two bytes (Unicode) versus one byte if unset. The second (MSB) byte represents the location of the string data, and is encoded as the number of rows from this USB_WHITE_LABEL_ADDR; i.e. the row of the start of the string is USB_WHITE_LABEL_ADDR value + msb_byte. In each case, the corresponding valid bit enables replacing the default value for the corresponding item provided by the boot rom. Note that Unicode _STRDEFs are only supported for USB_DEVICE_PRODUCT_STRDEF, USB_DEVICE_SERIAL_NUMBER_STRDEF and USB_DEVICE_MANUFACTURER_STRDEF. Unicode values will be ignored if specified for other fields, and non-unicode values for these three items will be converted to Unicode characters by setting the upper 8 bits to zero. Note that if the USB_WHITE_LABEL structure or the corresponding strings are not readable by BOOTSEL mode based on OTP permissions, or if alignment requirements are not met, then the corresponding default values are used. The index values indicate where each field is located (row USB_WHITE_LABEL_ADDR value + index):
    BEGIN_TYPE(USB_WHITE_LABEL_ADDR_t, uint16_t)
        ADD_BITFIELD_RO(USB_WHITE_LABEL_ADDR, 0, 16)
    END_TYPE()

    static const uint32_t USB_WHITE_LABEL_ADDR_USB_WHITE_LABEL_ADDR__INDEX_USB_DEVICE_VID_VALUE = 0;
    static const uint32_t USB_WHITE_LABEL_ADDR_USB_WHITE_LABEL_ADDR__INDEX_USB_DEVICE_PID_VALUE = 1;
    static const uint32_t USB_WHITE_LABEL_ADDR_USB_WHITE_LABEL_ADDR__INDEX_USB_DEVICE_BCD_DEVICE_VALUE = 2;
    static const uint32_t USB_WHITE_LABEL_ADDR_USB_WHITE_LABEL_ADDR__INDEX_USB_DEVICE_LANG_ID_VALUE = 3;
    static const uint32_t USB_WHITE_LABEL_ADDR_USB_WHITE_LABEL_ADDR__INDEX_USB_DEVICE_MANUFACTURER_STRDEF = 4;
    static const uint32_t USB_WHITE_LABEL_ADDR_USB_WHITE_LABEL_ADDR__INDEX_USB_DEVICE_PRODUCT_STRDEF = 5;
    static const uint32_t USB_WHITE_LABEL_ADDR_USB_WHITE_LABEL_ADDR__INDEX_USB_DEVICE_SERIAL_NUMBER_STRDEF = 6;
    static const uint32_t USB_WHITE_LABEL_ADDR_USB_WHITE_LABEL_ADDR__INDEX_USB_CONFIG_ATTRIBUTES_MAX_POWER_VALUES = 7;
    static const uint32_t USB_WHITE_LABEL_ADDR_USB_WHITE_LABEL_ADDR__INDEX_VOLUME_LABEL_STRDEF = 8;
    static const uint32_t USB_WHITE_LABEL_ADDR_USB_WHITE_LABEL_ADDR__INDEX_SCSI_INQUIRY_VENDOR_STRDEF = 9;
    static const uint32_t USB_WHITE_LABEL_ADDR_USB_WHITE_LABEL_ADDR__INDEX_SCSI_INQUIRY_PRODUCT_STRDEF = 10;
    static const uint32_t USB_WHITE_LABEL_ADDR_USB_WHITE_LABEL_ADDR__INDEX_SCSI_INQUIRY_VERSION_STRDEF = 11;
    static const uint32_t USB_WHITE_LABEL_ADDR_USB_WHITE_LABEL_ADDR__INDEX_INDEX_HTM_REDIRECT_URL_STRDEF = 12;
    static const uint32_t USB_WHITE_LABEL_ADDR_USB_WHITE_LABEL_ADDR__INDEX_INDEX_HTM_REDIRECT_NAME_STRDEF = 13;
    static const uint32_t USB_WHITE_LABEL_ADDR_USB_WHITE_LABEL_ADDR__INDEX_INFO_UF2_TXT_MODEL_STRDEF = 14;
    static const uint32_t USB_WHITE_LABEL_ADDR_USB_WHITE_LABEL_ADDR__INDEX_INFO_UF2_TXT_BOARD_ID_STRDEF = 15;

    // OTP start row for the OTP boot image. (ECC) If OTP boot is enabled, the bootrom will load from this location into SRAM and then directly enter the loaded image. Note that the image must be signed if SECURE_BOOT_ENABLE is set. The image itself is assumed to be ECC-protected. This must be an even number. Equivalently, the OTP boot image must start at a word-aligned location in the ECC read data address window.
    BEGIN_TYPE(OTPBOOT_SRC_t, uint16_t)
        ADD_BITFIELD_RO(OTPBOOT_SRC, 0, 16)
    END_TYPE()

    // Length in rows of the OTP boot image. (ECC) OTPBOOT_LEN must be even. The total image size must be a multiple of 4 bytes (32 bits).
    BEGIN_TYPE(OTPBOOT_LEN_t, uint16_t)
        ADD_BITFIELD_RO(OTPBOOT_LEN, 0, 16)
    END_TYPE()

    // Bits 15:0 of the OTP boot image load destination (and entry point). (ECC) This must be a location in main SRAM (main SRAM is addresses 0x20000000 through 0x20082000) and must be word-aligned.
    BEGIN_TYPE(OTPBOOT_DST0_t, uint16_t)
        ADD_BITFIELD_RO(OTPBOOT_DST0, 0, 16)
    END_TYPE()

    // Bits 31:16 of the OTP boot image load destination (and entry point). (ECC) This must be a location in main SRAM (main SRAM is addresses 0x20000000 through 0x20082000) and must be word-aligned.
    BEGIN_TYPE(OTPBOOT_DST1_t, uint16_t)
        ADD_BITFIELD_RO(OTPBOOT_DST1, 0, 16)
    END_TYPE()

    // Bits 15:0 of SHA-256 hash of boot key 0 (ECC)
    BEGIN_TYPE(BOOTKEY_0_t, uint16_t)
        ADD_BITFIELD_RO(BOOTKEY_0, 0, 16)
    END_TYPE()

    // Bits 31:16 of SHA-256 hash of boot key 0 (ECC)
    BEGIN_TYPE(BOOTKEY_1_t, uint16_t)
        ADD_BITFIELD_RO(BOOTKEY_1, 0, 16)
    END_TYPE()

    // Bits 47:32 of SHA-256 hash of boot key 0 (ECC)
    BEGIN_TYPE(BOOTKEY_2_t, uint16_t)
        ADD_BITFIELD_RO(BOOTKEY_2, 0, 16)
    END_TYPE()

    // Bits 63:48 of SHA-256 hash of boot key 0 (ECC)
    BEGIN_TYPE(BOOTKEY_3_t, uint16_t)
        ADD_BITFIELD_RO(BOOTKEY_3, 0, 16)
    END_TYPE()

    // Bits 79:64 of SHA-256 hash of boot key 0 (ECC)
    BEGIN_TYPE(BOOTKEY_4_t, uint16_t)
        ADD_BITFIELD_RO(BOOTKEY_4, 0, 16)
    END_TYPE()

    // Bits 95:80 of SHA-256 hash of boot key 0 (ECC)
    BEGIN_TYPE(BOOTKEY_5_t, uint16_t)
        ADD_BITFIELD_RO(BOOTKEY_5, 0, 16)
    END_TYPE()

    // Bits 111:96 of SHA-256 hash of boot key 0 (ECC)
    BEGIN_TYPE(BOOTKEY_6_t, uint16_t)
        ADD_BITFIELD_RO(BOOTKEY_6, 0, 16)
    END_TYPE()

    // Bits 127:112 of SHA-256 hash of boot key 0 (ECC)
    BEGIN_TYPE(BOOTKEY_7_t, uint16_t)
        ADD_BITFIELD_RO(BOOTKEY_7, 0, 16)
    END_TYPE()

    // Bits 143:128 of SHA-256 hash of boot key 0 (ECC)
    BEGIN_TYPE(BOOTKEY_8_t, uint16_t)
        ADD_BITFIELD_RO(BOOTKEY_8, 0, 16)
    END_TYPE()

    // Bits 159:144 of SHA-256 hash of boot key 0 (ECC)
    BEGIN_TYPE(BOOTKEY_9_t, uint16_t)
        ADD_BITFIELD_RO(BOOTKEY_9, 0, 16)
    END_TYPE()

    // Bits 175:160 of SHA-256 hash of boot key 0 (ECC)
    BEGIN_TYPE(BOOTKEY_10_t, uint16_t)
        ADD_BITFIELD_RO(BOOTKEY_10, 0, 16)
    END_TYPE()

    // Bits 191:176 of SHA-256 hash of boot key 0 (ECC)
    BEGIN_TYPE(BOOTKEY_11_t, uint16_t)
        ADD_BITFIELD_RO(BOOTKEY_11, 0, 16)
    END_TYPE()

    // Bits 207:192 of SHA-256 hash of boot key 0 (ECC)
    BEGIN_TYPE(BOOTKEY_12_t, uint16_t)
        ADD_BITFIELD_RO(BOOTKEY_12, 0, 16)
    END_TYPE()

    // Bits 223:208 of SHA-256 hash of boot key 0 (ECC)
    BEGIN_TYPE(BOOTKEY_13_t, uint16_t)
        ADD_BITFIELD_RO(BOOTKEY_13, 0, 16)
    END_TYPE()

    // Bits 239:224 of SHA-256 hash of boot key 0 (ECC)
    BEGIN_TYPE(BOOTKEY_14_t, uint16_t)
        ADD_BITFIELD_RO(BOOTKEY_14, 0, 16)
    END_TYPE()

    // Bits 255:240 of SHA-256 hash of boot key 0 (ECC)
    BEGIN_TYPE(BOOTKEY_15_t, uint16_t)
        ADD_BITFIELD_RO(BOOTKEY_15, 0, 16)
    END_TYPE()

    // Bits 15:0 of OTP access key 1 (ECC)
    BEGIN_TYPE(KEY_0_t, uint16_t)
        ADD_BITFIELD_RO(KEY_0, 0, 16)
    END_TYPE()

    // Bits 31:16 of OTP access key 1 (ECC)
    BEGIN_TYPE(KEY_1_t, uint16_t)
        ADD_BITFIELD_RO(KEY_1, 0, 16)
    END_TYPE()

    // Bits 47:32 of OTP access key 1 (ECC)
    BEGIN_TYPE(KEY_2_t, uint16_t)
        ADD_BITFIELD_RO(KEY_2, 0, 16)
    END_TYPE()

    // Bits 63:48 of OTP access key 1 (ECC)
    BEGIN_TYPE(KEY_3_t, uint16_t)
        ADD_BITFIELD_RO(KEY_3, 0, 16)
    END_TYPE()

    // Bits 79:64 of OTP access key 1 (ECC)
    BEGIN_TYPE(KEY_4_t, uint16_t)
        ADD_BITFIELD_RO(KEY_4, 0, 16)
    END_TYPE()

    // Bits 95:80 of OTP access key 1 (ECC)
    BEGIN_TYPE(KEY_5_t, uint16_t)
        ADD_BITFIELD_RO(KEY_5, 0, 16)
    END_TYPE()

    // Bits 111:96 of OTP access key 1 (ECC)
    BEGIN_TYPE(KEY_6_t, uint16_t)
        ADD_BITFIELD_RO(KEY_6, 0, 16)
    END_TYPE()

    // Bits 127:112 of OTP access key 1 (ECC)
    BEGIN_TYPE(KEY_7_t, uint16_t)
        ADD_BITFIELD_RO(KEY_7, 0, 16)
    END_TYPE()

    struct OTP_DATA_t {
        CHIPID0_t                     CHIPID0;
        CHIPID1_t                     CHIPID1;
        CHIPID2_t                     CHIPID2;
        CHIPID3_t                     CHIPID3;
        RANDID0_t                     RANDID0;
        RANDID1_t                     RANDID1;
        RANDID2_t                     RANDID2;
        RANDID3_t                     RANDID3;
        RANDID4_t                     RANDID4;
        RANDID5_t                     RANDID5;
        RANDID6_t                     RANDID6;
        RANDID7_t                     RANDID7;
        uint16_t                      reserved0[4];
        ROSC_CALIB_t                  ROSC_CALIB;
        LPOSC_CALIB_t                 LPOSC_CALIB;
        uint16_t                      reserved1[6];
        NUM_GPIOS_t                   NUM_GPIOS;
        uint16_t                      reserved2[29];
        INFO_CRC0_t                   INFO_CRC0;
        INFO_CRC1_t                   INFO_CRC1;
        uint16_t                      reserved3[28];
        FLASH_DEVINFO_t               FLASH_DEVINFO;
        FLASH_PARTITION_SLOT_SIZE_t   FLASH_PARTITION_SLOT_SIZE;
        BOOTSEL_LED_CFG_t             BOOTSEL_LED_CFG;
        BOOTSEL_PLL_CFG_t             BOOTSEL_PLL_CFG;
        BOOTSEL_XOSC_CFG_t            BOOTSEL_XOSC_CFG;
        uint16_t                      reserved4[3];
        USB_WHITE_LABEL_ADDR_t        USB_WHITE_LABEL_ADDR;
        uint16_t                      reserved5;
        OTPBOOT_SRC_t                 OTPBOOT_SRC;
        OTPBOOT_LEN_t                 OTPBOOT_LEN;
        OTPBOOT_DST0_t                OTPBOOT_DST0;
        OTPBOOT_DST1_t                OTPBOOT_DST1;
        uint16_t                      reserved6[30];
        BOOTKEY_0_t                   BOOTKEY0_0;
        BOOTKEY_1_t                   BOOTKEY0_1;
        BOOTKEY_2_t                   BOOTKEY0_2;
        BOOTKEY_3_t                   BOOTKEY0_3;
        BOOTKEY_4_t                   BOOTKEY0_4;
        BOOTKEY_5_t                   BOOTKEY0_5;
        BOOTKEY_6_t                   BOOTKEY0_6;
        BOOTKEY_7_t                   BOOTKEY0_7;
        BOOTKEY_8_t                   BOOTKEY0_8;
        BOOTKEY_9_t                   BOOTKEY0_9;
        BOOTKEY_10_t                  BOOTKEY0_10;
        BOOTKEY_11_t                  BOOTKEY0_11;
        BOOTKEY_12_t                  BOOTKEY0_12;
        BOOTKEY_13_t                  BOOTKEY0_13;
        BOOTKEY_14_t                  BOOTKEY0_14;
        BOOTKEY_15_t                  BOOTKEY0_15;
        uint16_t                      reserved7[16];
        BOOTKEY_0_t                   BOOTKEY1_0;
        BOOTKEY_1_t                   BOOTKEY1_1;
        BOOTKEY_2_t                   BOOTKEY1_2;
        BOOTKEY_3_t                   BOOTKEY1_3;
        BOOTKEY_4_t                   BOOTKEY1_4;
        BOOTKEY_5_t                   BOOTKEY1_5;
        BOOTKEY_6_t                   BOOTKEY1_6;
        BOOTKEY_7_t                   BOOTKEY1_7;
        BOOTKEY_8_t                   BOOTKEY1_8;
        BOOTKEY_9_t                   BOOTKEY1_9;
        BOOTKEY_10_t                  BOOTKEY1_10;
        BOOTKEY_11_t                  BOOTKEY1_11;
        BOOTKEY_12_t                  BOOTKEY1_12;
        BOOTKEY_13_t                  BOOTKEY1_13;
        BOOTKEY_14_t                  BOOTKEY1_14;
        BOOTKEY_15_t                  BOOTKEY1_15;
        uint16_t                      reserved8[16];
        BOOTKEY_0_t                   BOOTKEY2_0;
        BOOTKEY_1_t                   BOOTKEY2_1;
        BOOTKEY_2_t                   BOOTKEY2_2;
        BOOTKEY_3_t                   BOOTKEY2_3;
        BOOTKEY_4_t                   BOOTKEY2_4;
        BOOTKEY_5_t                   BOOTKEY2_5;
        BOOTKEY_6_t                   BOOTKEY2_6;
        BOOTKEY_7_t                   BOOTKEY2_7;
        BOOTKEY_8_t                   BOOTKEY2_8;
        BOOTKEY_9_t                   BOOTKEY2_9;
        BOOTKEY_10_t                  BOOTKEY2_10;
        BOOTKEY_11_t                  BOOTKEY2_11;
        BOOTKEY_12_t                  BOOTKEY2_12;
        BOOTKEY_13_t                  BOOTKEY2_13;
        BOOTKEY_14_t                  BOOTKEY2_14;
        BOOTKEY_15_t                  BOOTKEY2_15;
        uint16_t                      reserved9[16];
        BOOTKEY_0_t                   BOOTKEY3_0;
        BOOTKEY_1_t                   BOOTKEY3_1;
        BOOTKEY_2_t                   BOOTKEY3_2;
        BOOTKEY_3_t                   BOOTKEY3_3;
        BOOTKEY_4_t                   BOOTKEY3_4;
        BOOTKEY_5_t                   BOOTKEY3_5;
        BOOTKEY_6_t                   BOOTKEY3_6;
        BOOTKEY_7_t                   BOOTKEY3_7;
        BOOTKEY_8_t                   BOOTKEY3_8;
        BOOTKEY_9_t                   BOOTKEY3_9;
        BOOTKEY_10_t                  BOOTKEY3_10;
        BOOTKEY_11_t                  BOOTKEY3_11;
        BOOTKEY_12_t                  BOOTKEY3_12;
        BOOTKEY_13_t                  BOOTKEY3_13;
        BOOTKEY_14_t                  BOOTKEY3_14;
        BOOTKEY_15_t                  BOOTKEY3_15;
        uint16_t                      reserved10[3672];
        KEY_0_t                       KEY1_0;
        KEY_1_t                       KEY1_1;
        KEY_2_t                       KEY1_2;
        KEY_3_t                       KEY1_3;
        KEY_4_t                       KEY1_4;
        KEY_5_t                       KEY1_5;
        KEY_6_t                       KEY1_6;
        KEY_7_t                       KEY1_7;
        uint16_t                      reserved11[8];
        KEY_0_t                       KEY2_0;
        KEY_1_t                       KEY2_1;
        KEY_2_t                       KEY2_2;
        KEY_3_t                       KEY2_3;
        KEY_4_t                       KEY2_4;
        KEY_5_t                       KEY2_5;
        KEY_6_t                       KEY2_6;
        KEY_7_t                       KEY2_7;
        uint16_t                      reserved12[8];
        KEY_0_t                       KEY3_0;
        KEY_1_t                       KEY3_1;
        KEY_2_t                       KEY3_2;
        KEY_3_t                       KEY3_3;
        KEY_4_t                       KEY3_4;
        KEY_5_t                       KEY3_5;
        KEY_6_t                       KEY3_6;
        KEY_7_t                       KEY3_7;
        uint16_t                      reserved13[8];
        KEY_0_t                       KEY4_0;
        KEY_1_t                       KEY4_1;
        KEY_2_t                       KEY4_2;
        KEY_3_t                       KEY4_3;
        KEY_4_t                       KEY4_4;
        KEY_5_t                       KEY4_5;
        KEY_6_t                       KEY4_6;
        KEY_7_t                       KEY4_7;
        uint16_t                      reserved14[8];
        KEY_0_t                       KEY5_0;
        KEY_1_t                       KEY5_1;
        KEY_2_t                       KEY5_2;
        KEY_3_t                       KEY5_3;
        KEY_4_t                       KEY5_4;
        KEY_5_t                       KEY5_5;
        KEY_6_t                       KEY5_6;
        KEY_7_t                       KEY5_7;
        uint16_t                      reserved15[8];
        KEY_0_t                       KEY6_0;
        KEY_1_t                       KEY6_1;
        KEY_2_t                       KEY6_2;
        KEY_3_t                       KEY6_3;
        KEY_4_t                       KEY6_4;
        KEY_5_t                       KEY6_5;
        KEY_6_t                       KEY6_6;
        KEY_7_t                       KEY6_7;
    };

    static OTP_DATA_t & OTP_DATA = (*(OTP_DATA_t *)0x40130000);
    static OTP_DATA_t & OTP_DATA_XOR = (*(OTP_DATA_t *)0x40131000);
    static OTP_DATA_t & OTP_DATA_SET = (*(OTP_DATA_t *)0x40132000);
    static OTP_DATA_t & OTP_DATA_CLR = (*(OTP_DATA_t *)0x40133000);

} // _OTP_DATA_

// Predefined OTP data layout for RP2350
namespace _OTP_DATA_RAW_  {

    // Bits 15:0 of public device ID. (ECC) The CHIPID0..3 rows contain a 64-bit random identifier for this chip, which can be read from the USB bootloader PICOBOOT interface or from the get_sys_info ROM API. The number of random bits makes the occurrence of twins exceedingly unlikely: for example, a fleet of a hundred million devices has a 99.97% probability of no twinned IDs. This is estimated to be lower than the occurrence of process errors in the assignment of sequential random IDs, and for practical purposes CHIPID may be treated as unique.
    BEGIN_TYPE(CHIPID0_t, uint32_t)
        ADD_BITFIELD_RO(CHIPID0, 0, 16)
    END_TYPE()

    // Bits 31:16 of public device ID (ECC)
    BEGIN_TYPE(CHIPID1_t, uint32_t)
        ADD_BITFIELD_RO(CHIPID1, 0, 16)
    END_TYPE()

    // Bits 47:32 of public device ID (ECC)
    BEGIN_TYPE(CHIPID2_t, uint32_t)
        ADD_BITFIELD_RO(CHIPID2, 0, 16)
    END_TYPE()

    // Bits 63:48 of public device ID (ECC)
    BEGIN_TYPE(CHIPID3_t, uint32_t)
        ADD_BITFIELD_RO(CHIPID3, 0, 16)
    END_TYPE()

    // Bits 15:0 of private per-device random number (ECC) The RANDID0..7 rows form a 128-bit random number generated during device test. This ID is not exposed through the USB PICOBOOT GET_INFO command or the ROM `get_sys_info()` API. However note that the USB PICOBOOT OTP access point can read the entirety of page 0, so this value is not meaningfully private unless the USB PICOBOOT interface is disabled via the DISABLE_BOOTSEL_USB_PICOBOOT_IFC flag in BOOT_FLAGS0.
    BEGIN_TYPE(RANDID0_t, uint32_t)
        ADD_BITFIELD_RO(RANDID0, 0, 16)
    END_TYPE()

    // Bits 31:16 of private per-device random number (ECC)
    BEGIN_TYPE(RANDID1_t, uint32_t)
        ADD_BITFIELD_RO(RANDID1, 0, 16)
    END_TYPE()

    // Bits 47:32 of private per-device random number (ECC)
    BEGIN_TYPE(RANDID2_t, uint32_t)
        ADD_BITFIELD_RO(RANDID2, 0, 16)
    END_TYPE()

    // Bits 63:48 of private per-device random number (ECC)
    BEGIN_TYPE(RANDID3_t, uint32_t)
        ADD_BITFIELD_RO(RANDID3, 0, 16)
    END_TYPE()

    // Bits 79:64 of private per-device random number (ECC)
    BEGIN_TYPE(RANDID4_t, uint32_t)
        ADD_BITFIELD_RO(RANDID4, 0, 16)
    END_TYPE()

    // Bits 95:80 of private per-device random number (ECC)
    BEGIN_TYPE(RANDID5_t, uint32_t)
        ADD_BITFIELD_RO(RANDID5, 0, 16)
    END_TYPE()

    // Bits 111:96 of private per-device random number (ECC)
    BEGIN_TYPE(RANDID6_t, uint32_t)
        ADD_BITFIELD_RO(RANDID6, 0, 16)
    END_TYPE()

    // Bits 127:112 of private per-device random number (ECC)
    BEGIN_TYPE(RANDID7_t, uint32_t)
        ADD_BITFIELD_RO(RANDID7, 0, 16)
    END_TYPE()

    // Ring oscillator frequency in kHz, measured during manufacturing (ECC) This is measured at 1.1 V, at room temperature, with the ROSC configuration registers in their reset state.
    BEGIN_TYPE(ROSC_CALIB_t, uint32_t)
        ADD_BITFIELD_RO(ROSC_CALIB, 0, 16)
    END_TYPE()

    // Low-power oscillator frequency in Hz, measured during manufacturing (ECC) This is measured at 1.1V, at room temperature, with the LPOSC trim register in its reset state.
    BEGIN_TYPE(LPOSC_CALIB_t, uint32_t)
        ADD_BITFIELD_RO(LPOSC_CALIB, 0, 16)
    END_TYPE()

    // The number of main user GPIOs (bank 0). Should read 48 in the QFN80 package, and 30 in the QFN60 package. (ECC)
    BEGIN_TYPE(NUM_GPIOS_t, uint32_t)
        ADD_BITFIELD_RO(NUM_GPIOS, 0, 8)
    END_TYPE()

    // Lower 16 bits of CRC32 of OTP addresses 0x00 through 0x6b (polynomial 0x4c11db7, input reflected, output reflected, seed all-ones, final XOR all-ones) (ECC)
    BEGIN_TYPE(INFO_CRC0_t, uint32_t)
        ADD_BITFIELD_RO(INFO_CRC0, 0, 16)
    END_TYPE()

    // Upper 16 bits of CRC32 of OTP addresses 0x00 through 0x6b (ECC)
    BEGIN_TYPE(INFO_CRC1_t, uint32_t)
        ADD_BITFIELD_RO(INFO_CRC1, 0, 16)
    END_TYPE()

    // Page 0 critical boot flags (RBIT-8)
    // Reset value: 0x00000000
    BEGIN_TYPE(CRIT0_t, uint32_t)
        // Permanently disable RISC-V processors (Hazard3)
        ADD_BITFIELD_RO(RISCV_DISABLE, 1, 1)
        // Permanently disable ARM processors (Cortex-M33)
        ADD_BITFIELD_RO(ARM_DISABLE, 0, 1)
    END_TYPE()

    // Redundant copy of CRIT0
    BEGIN_TYPE(CRIT0_R1_t, uint32_t)
        ADD_BITFIELD_RO(CRIT0_R1, 0, 24)
    END_TYPE()

    // Redundant copy of CRIT0
    BEGIN_TYPE(CRIT0_R2_t, uint32_t)
        ADD_BITFIELD_RO(CRIT0_R2, 0, 24)
    END_TYPE()

    // Redundant copy of CRIT0
    BEGIN_TYPE(CRIT0_R3_t, uint32_t)
        ADD_BITFIELD_RO(CRIT0_R3, 0, 24)
    END_TYPE()

    // Redundant copy of CRIT0
    BEGIN_TYPE(CRIT0_R4_t, uint32_t)
        ADD_BITFIELD_RO(CRIT0_R4, 0, 24)
    END_TYPE()

    // Redundant copy of CRIT0
    BEGIN_TYPE(CRIT0_R5_t, uint32_t)
        ADD_BITFIELD_RO(CRIT0_R5, 0, 24)
    END_TYPE()

    // Redundant copy of CRIT0
    BEGIN_TYPE(CRIT0_R6_t, uint32_t)
        ADD_BITFIELD_RO(CRIT0_R6, 0, 24)
    END_TYPE()

    // Redundant copy of CRIT0
    BEGIN_TYPE(CRIT0_R7_t, uint32_t)
        ADD_BITFIELD_RO(CRIT0_R7, 0, 24)
    END_TYPE()

    // Page 1 critical boot flags (RBIT-8)
    // Reset value: 0x00000000
    BEGIN_TYPE(CRIT1_t, uint32_t)
        // Increase the sensitivity of the glitch detectors from their default.
        ADD_BITFIELD_RO(GLITCH_DETECTOR_SENS, 5, 2)
        // Arm the glitch detectors to reset the system if an abnormal clock/power event is observed.
        ADD_BITFIELD_RO(GLITCH_DETECTOR_ENABLE, 4, 1)
        // Set the default boot architecture, 0=ARM 1=RISC-V. Ignored if ARM_DISABLE, RISCV_DISABLE or SECURE_BOOT_ENABLE is set.
        ADD_BITFIELD_RO(BOOT_ARCH, 3, 1)
        // Disable all debug access
        ADD_BITFIELD_RO(DEBUG_DISABLE, 2, 1)
        // Disable Secure debug access
        ADD_BITFIELD_RO(SECURE_DEBUG_DISABLE, 1, 1)
        // Enable boot signature enforcement, and permanently disable the RISC-V cores.
        ADD_BITFIELD_RO(SECURE_BOOT_ENABLE, 0, 1)
    END_TYPE()

    // Redundant copy of CRIT1
    BEGIN_TYPE(CRIT1_R1_t, uint32_t)
        ADD_BITFIELD_RO(CRIT1_R1, 0, 24)
    END_TYPE()

    // Redundant copy of CRIT1
    BEGIN_TYPE(CRIT1_R2_t, uint32_t)
        ADD_BITFIELD_RO(CRIT1_R2, 0, 24)
    END_TYPE()

    // Redundant copy of CRIT1
    BEGIN_TYPE(CRIT1_R3_t, uint32_t)
        ADD_BITFIELD_RO(CRIT1_R3, 0, 24)
    END_TYPE()

    // Redundant copy of CRIT1
    BEGIN_TYPE(CRIT1_R4_t, uint32_t)
        ADD_BITFIELD_RO(CRIT1_R4, 0, 24)
    END_TYPE()

    // Redundant copy of CRIT1
    BEGIN_TYPE(CRIT1_R5_t, uint32_t)
        ADD_BITFIELD_RO(CRIT1_R5, 0, 24)
    END_TYPE()

    // Redundant copy of CRIT1
    BEGIN_TYPE(CRIT1_R6_t, uint32_t)
        ADD_BITFIELD_RO(CRIT1_R6, 0, 24)
    END_TYPE()

    // Redundant copy of CRIT1
    BEGIN_TYPE(CRIT1_R7_t, uint32_t)
        ADD_BITFIELD_RO(CRIT1_R7, 0, 24)
    END_TYPE()

    // Disable/Enable boot paths/features in the RP2350 mask ROM. Disables always supersede enables. Enables are provided where there are other configurations in OTP that must be valid. (RBIT-3)
    // Reset value: 0x00000000
    BEGIN_TYPE(BOOT_FLAGS0_t, uint32_t)
        ADD_BITFIELD_RO(DISABLE_SRAM_WINDOW_BOOT, 21, 1)
        // Disable all access to XIP after entering an SRAM binary. Note that this will cause bootrom APIs that access XIP to fail, including APIs that interact with the partition table.
        ADD_BITFIELD_RO(DISABLE_XIP_ACCESS_ON_SRAM_ENTRY, 20, 1)
        ADD_BITFIELD_RO(DISABLE_BOOTSEL_UART_BOOT, 19, 1)
        ADD_BITFIELD_RO(DISABLE_BOOTSEL_USB_PICOBOOT_IFC, 18, 1)
        ADD_BITFIELD_RO(DISABLE_BOOTSEL_USB_MSD_IFC, 17, 1)
        ADD_BITFIELD_RO(DISABLE_WATCHDOG_SCRATCH, 16, 1)
        ADD_BITFIELD_RO(DISABLE_POWER_SCRATCH, 15, 1)
        // Enable OTP boot. A number of OTP rows specified by OTPBOOT_LEN will be loaded, starting from OTPBOOT_SRC, into the SRAM location specified by OTPBOOT_DST1 and OTPBOOT_DST0. The loaded program image is stored with ECC, 16 bits per row, and must contain a valid IMAGE_DEF. Do not set this bit without first programming an image into OTP and configuring OTPBOOT_LEN, OTPBOOT_SRC, OTPBOOT_DST0 and OTPBOOT_DST1. Note that OTPBOOT_LEN and OTPBOOT_SRC must be even numbers of OTP rows. Equivalently, the image must be a multiple of 32 bits in size, and must start at a 32-bit-aligned address in the ECC read data address window.
        ADD_BITFIELD_RO(ENABLE_OTP_BOOT, 14, 1)
        // Takes precedence over ENABLE_OTP_BOOT.
        ADD_BITFIELD_RO(DISABLE_OTP_BOOT, 13, 1)
        ADD_BITFIELD_RO(DISABLE_FLASH_BOOT, 12, 1)
        // Require binaries to have a rollback version. Set automatically the first time a binary with a rollback version is booted.
        ADD_BITFIELD_RO(ROLLBACK_REQUIRED, 11, 1)
        // Require a partition table to be hashed (if not signed)
        ADD_BITFIELD_RO(HASHED_PARTITION_TABLE, 10, 1)
        // Require a partition table to be signed
        ADD_BITFIELD_RO(SECURE_PARTITION_TABLE, 9, 1)
        // Disable auto-switch of CPU architecture on boot when the (only) binary to be booted is for the other Arm/RISC-V architecture and both architectures are enabled
        ADD_BITFIELD_RO(DISABLE_AUTO_SWITCH_ARCH, 8, 1)
        // Restrict flash boot path to use of a single binary at the start of flash
        ADD_BITFIELD_RO(SINGLE_FLASH_BINARY, 7, 1)
        // Override the limit for default flash metadata scanning. The value is specified in FLASH_PARTITION_SLOT_SIZE. Make sure FLASH_PARTITION_SLOT_SIZE is valid before setting this bit
        ADD_BITFIELD_RO(OVERRIDE_FLASH_PARTITION_SLOT_SIZE, 6, 1)
        // Mark FLASH_DEVINFO as containing valid, ECC'd data which describes external flash devices.
        ADD_BITFIELD_RO(FLASH_DEVINFO_ENABLE, 5, 1)
        // Enable quartering of ROSC divisor during signature check, to reduce secure boot time
        ADD_BITFIELD_RO(FAST_SIGCHECK_ROSC_DIV, 4, 1)
        // If 1, configure the QSPI pads for 1.8 V operation when accessing flash for the first time from the bootrom, using the VOLTAGE_SELECT register for the QSPI pads bank. This slightly improves the input timing of the pads at low voltages, but does not affect their output characteristics. If 0, leave VOLTAGE_SELECT in its reset state (suitable for operation at and above 2.5 V)
        ADD_BITFIELD_RO(FLASH_IO_VOLTAGE_1V8, 3, 1)
        // Enable loading of the non-default XOSC and PLL configuration before entering BOOTSEL mode. Ensure that BOOTSEL_XOSC_CFG and BOOTSEL_PLL_CFG are correctly programmed before setting this bit. If this bit is set, user software may use the contents of BOOTSEL_PLL_CFG to calculated the expected XOSC frequency based on the fixed USB boot frequency of 48 MHz.
        ADD_BITFIELD_RO(ENABLE_BOOTSEL_NON_DEFAULT_PLL_XOSC_CFG, 2, 1)
        // Enable bootloader activity LED. If set, bootsel_led_cfg is assumed to be valid
        ADD_BITFIELD_RO(ENABLE_BOOTSEL_LED, 1, 1)
        ADD_BITFIELD_RO(DISABLE_BOOTSEL_EXEC2, 0, 1)
    END_TYPE()

    // Redundant copy of BOOT_FLAGS0
    BEGIN_TYPE(BOOT_FLAGS0_R1_t, uint32_t)
        ADD_BITFIELD_RO(BOOT_FLAGS0_R1, 0, 24)
    END_TYPE()

    // Redundant copy of BOOT_FLAGS0
    BEGIN_TYPE(BOOT_FLAGS0_R2_t, uint32_t)
        ADD_BITFIELD_RO(BOOT_FLAGS0_R2, 0, 24)
    END_TYPE()

    // Disable/Enable boot paths/features in the RP2350 mask ROM. Disables always supersede enables. Enables are provided where there are other configurations in OTP that must be valid. (RBIT-3)
    // Reset value: 0x00000000
    BEGIN_TYPE(BOOT_FLAGS1_t, uint32_t)
        // Enable entering BOOTSEL mode via double-tap of the RUN/RSTn pin. Adds a significant delay to boot time, as configured by DOUBLE_TAP_DELAY. This functions by waiting at startup (i.e. following a reset) to see if a second reset is applied soon afterward. The second reset is detected by the bootrom with help of the POWMAN_CHIP_RESET_DOUBLE_TAP flag, which is not reset by the external reset pin, and the bootrom enters BOOTSEL mode (NSBOOT) to await further instruction over USB or UART.
        ADD_BITFIELD_RO(DOUBLE_TAP, 19, 1)
        // Adjust how long to wait for a second reset when double tap BOOTSEL mode is enabled via DOUBLE_TAP. The minimum is 50 milliseconds, and each unit of this field adds an additional 50 milliseconds. For example, settings this field to its maximum value of 7 will cause the chip to wait for 400 milliseconds at boot to check for a second reset which requests entry to BOOTSEL mode. 200 milliseconds (DOUBLE_TAP_DELAY=3) is a good intermediate value.
        ADD_BITFIELD_RO(DOUBLE_TAP_DELAY, 16, 3)
        // Mark a boot key as invalid, or prevent it from ever becoming valid. The bootrom will ignore any boot key marked as invalid during secure boot signature checks. Each bit in this field corresponds to one of the four 256-bit boot key hashes that may be stored in page 2 of the OTP. When provisioning boot keys, it's recommended to mark any boot key slots you don't intend to use as KEY_INVALID, so that spurious keys can not be installed at a later time.
        ADD_BITFIELD_RO(KEY_INVALID, 8, 4)
        // Mark each of the possible boot keys as valid. The bootrom will check signatures against all valid boot keys, and ignore invalid boot keys. Each bit in this field corresponds to one of the four 256-bit boot key hashes that may be stored in page 2 of the OTP. A KEY_VALID bit is ignored if the corresponding KEY_INVALID bit is set. Boot keys are considered valid only when KEY_VALID is set and KEY_INVALID is clear. Do not mark a boot key as KEY_VALID if it does not contain a valid SHA-256 hash of your secp256k1 public key. Verify keys after programming, before setting the KEY_VALID bits -- a boot key with uncorrectable ECC faults will render your device unbootable if secure boot is enabled. Do not enable secure boot without first installing a valid key. This will render your device unbootable.
        ADD_BITFIELD_RO(KEY_VALID, 0, 4)
    END_TYPE()

    // Redundant copy of BOOT_FLAGS1
    BEGIN_TYPE(BOOT_FLAGS1_R1_t, uint32_t)
        ADD_BITFIELD_RO(BOOT_FLAGS1_R1, 0, 24)
    END_TYPE()

    // Redundant copy of BOOT_FLAGS1
    BEGIN_TYPE(BOOT_FLAGS1_R2_t, uint32_t)
        ADD_BITFIELD_RO(BOOT_FLAGS1_R2, 0, 24)
    END_TYPE()

    // Default boot version thermometer counter, bits 23:0 (RBIT-3)
    BEGIN_TYPE(DEFAULT_BOOT_VERSION0_t, uint32_t)
        ADD_BITFIELD_RO(DEFAULT_BOOT_VERSION0, 0, 24)
    END_TYPE()

    // Redundant copy of DEFAULT_BOOT_VERSION0
    BEGIN_TYPE(DEFAULT_BOOT_VERSION0_R1_t, uint32_t)
        ADD_BITFIELD_RO(DEFAULT_BOOT_VERSION0_R1, 0, 24)
    END_TYPE()

    // Redundant copy of DEFAULT_BOOT_VERSION0
    BEGIN_TYPE(DEFAULT_BOOT_VERSION0_R2_t, uint32_t)
        ADD_BITFIELD_RO(DEFAULT_BOOT_VERSION0_R2, 0, 24)
    END_TYPE()

    // Default boot version thermometer counter, bits 47:24 (RBIT-3)
    BEGIN_TYPE(DEFAULT_BOOT_VERSION1_t, uint32_t)
        ADD_BITFIELD_RO(DEFAULT_BOOT_VERSION1, 0, 24)
    END_TYPE()

    // Redundant copy of DEFAULT_BOOT_VERSION1
    BEGIN_TYPE(DEFAULT_BOOT_VERSION1_R1_t, uint32_t)
        ADD_BITFIELD_RO(DEFAULT_BOOT_VERSION1_R1, 0, 24)
    END_TYPE()

    // Redundant copy of DEFAULT_BOOT_VERSION1
    BEGIN_TYPE(DEFAULT_BOOT_VERSION1_R2_t, uint32_t)
        ADD_BITFIELD_RO(DEFAULT_BOOT_VERSION1_R2, 0, 24)
    END_TYPE()

    // Stores information about external flash device(s). (ECC) Assumed to be valid if BOOT_FLAGS0_FLASH_DEVINFO_ENABLE is set.
    // Reset value: 0x00000000
    BEGIN_TYPE(FLASH_DEVINFO_t, uint32_t)
        // The size of the flash/PSRAM device on chip select 1 (addressable at 0x11000000 through 0x11ffffff). A value of zero is decoded as a size of zero (no device). Nonzero values are decoded as 4kiB << CS1_SIZE. For example, four megabytes is encoded with a CS1_SIZE value of 10, and 16 megabytes is encoded with a CS1_SIZE value of 12. When BOOT_FLAGS0_FLASH_DEVINFO_ENABLE is not set, a default of zero is used.
        ADD_BITFIELD_RO(CS1_SIZE, 12, 4)
        // The size of the flash/PSRAM device on chip select 0 (addressable at 0x10000000 through 0x10ffffff). A value of zero is decoded as a size of zero (no device). Nonzero values are decoded as 4kiB << CS0_SIZE. For example, four megabytes is encoded with a CS0_SIZE value of 10, and 16 megabytes is encoded with a CS0_SIZE value of 12. When BOOT_FLAGS0_FLASH_DEVINFO_ENABLE is not set, a default of 12 (16 MiB) is used.
        ADD_BITFIELD_RO(CS0_SIZE, 8, 4)
        // If true, all attached devices are assumed to support (or ignore, in the case of PSRAM) a block erase command with a command prefix of D8h, an erase size of 64 kiB, and a 24-bit address. Almost all 25-series flash devices support this command. If set, the bootrom will use the D8h erase command where it is able, to accelerate bulk erase operations. This makes flash programming faster. When BOOT_FLAGS0_FLASH_DEVINFO_ENABLE is not set, this field defaults to false.
        ADD_BITFIELD_RO(D8H_ERASE_SUPPORTED, 7, 1)
        // Indicate a GPIO number to be used for the secondary flash chip select (CS1), which selects the external QSPI device mapped at system addresses 0x11000000 through 0x11ffffff. There is no such configuration for CS0, as the primary chip select has a dedicated pin. On RP2350 the permissible GPIO numbers are 0, 8, 19 and 47. Ignored if CS1_size is zero. If CS1_SIZE is nonzero, the bootrom will automatically configure this GPIO as a second chip select upon entering the flash boot path, or entering any other path that may use the QSPI flash interface, such as BOOTSEL mode (nsboot).
        ADD_BITFIELD_RO(CS1_GPIO, 0, 6)
    END_TYPE()

    static const uint32_t FLASH_DEVINFO_CS1_SIZE__NONE = 0;
    static const uint32_t FLASH_DEVINFO_CS1_SIZE__8K = 1;
    static const uint32_t FLASH_DEVINFO_CS1_SIZE__16K = 2;
    static const uint32_t FLASH_DEVINFO_CS1_SIZE__32K = 3;
    static const uint32_t FLASH_DEVINFO_CS1_SIZE__64k = 4;
    static const uint32_t FLASH_DEVINFO_CS1_SIZE__128K = 5;
    static const uint32_t FLASH_DEVINFO_CS1_SIZE__256K = 6;
    static const uint32_t FLASH_DEVINFO_CS1_SIZE__512K = 7;
    static const uint32_t FLASH_DEVINFO_CS1_SIZE__1M = 8;
    static const uint32_t FLASH_DEVINFO_CS1_SIZE__2M = 9;
    static const uint32_t FLASH_DEVINFO_CS1_SIZE__4M = 10;
    static const uint32_t FLASH_DEVINFO_CS1_SIZE__8M = 11;
    static const uint32_t FLASH_DEVINFO_CS1_SIZE__16M = 12;
    static const uint32_t FLASH_DEVINFO_CS0_SIZE__NONE = 0;
    static const uint32_t FLASH_DEVINFO_CS0_SIZE__8K = 1;
    static const uint32_t FLASH_DEVINFO_CS0_SIZE__16K = 2;
    static const uint32_t FLASH_DEVINFO_CS0_SIZE__32K = 3;
    static const uint32_t FLASH_DEVINFO_CS0_SIZE__64k = 4;
    static const uint32_t FLASH_DEVINFO_CS0_SIZE__128K = 5;
    static const uint32_t FLASH_DEVINFO_CS0_SIZE__256K = 6;
    static const uint32_t FLASH_DEVINFO_CS0_SIZE__512K = 7;
    static const uint32_t FLASH_DEVINFO_CS0_SIZE__1M = 8;
    static const uint32_t FLASH_DEVINFO_CS0_SIZE__2M = 9;
    static const uint32_t FLASH_DEVINFO_CS0_SIZE__4M = 10;
    static const uint32_t FLASH_DEVINFO_CS0_SIZE__8M = 11;
    static const uint32_t FLASH_DEVINFO_CS0_SIZE__16M = 12;

    // Gap between partition table slot 0 and slot 1 at the start of flash (the default size is 4096 bytes) (ECC) Enabled by the OVERRIDE_FLASH_PARTITION_SLOT_SIZE bit in BOOT_FLAGS, the size is 4096 * (value + 1)
    BEGIN_TYPE(FLASH_PARTITION_SLOT_SIZE_t, uint32_t)
        ADD_BITFIELD_RO(FLASH_PARTITION_SLOT_SIZE, 0, 16)
    END_TYPE()

    // Pin configuration for LED status, used by USB bootloader. (ECC)  Must be valid if BOOT_FLAGS0_ENABLE_BOOTSEL_LED is set.
    // Reset value: 0x00000000
    BEGIN_TYPE(BOOTSEL_LED_CFG_t, uint32_t)
        // LED is active-low. (Default: active-high.)
        ADD_BITFIELD_RO(ACTIVELOW, 8, 1)
        // GPIO index to use for bootloader activity LED.
        ADD_BITFIELD_RO(PIN, 0, 6)
    END_TYPE()

    // Optional PLL configuration for BOOTSEL mode. (ECC) This should be configured to produce an exact 48 MHz based on the crystal oscillator frequency. User mode software may also use this value to calculate the expected crystal frequency based on an assumed 48 MHz PLL output. If no configuration is given, the crystal is assumed to be 12 MHz. The PLL frequency can be calculated as: PLL out = (XOSC frequency / (REFDIV+1)) x FBDIV / (POSTDIV1 x POSTDIV2) Conversely the crystal frequency can be calculated as: XOSC frequency = 48 MHz x (REFDIV+1) x (POSTDIV1 x POSTDIV2) / FBDIV (Note the  +1 on REFDIV is because the value stored in this OTP location is the actual divisor value minus one.) Used if and only if ENABLE_BOOTSEL_NON_DEFAULT_PLL_XOSC_CFG is set in BOOT_FLAGS0. That bit should be set only after this row and BOOTSEL_XOSC_CFG are both correctly programmed.
    // Reset value: 0x00000000
    BEGIN_TYPE(BOOTSEL_PLL_CFG_t, uint32_t)
        // PLL reference divisor, minus one. Programming a value of 0 means a reference divisor of 1. Programming a value of 1 means a reference divisor of 2 (for exceptionally fast XIN inputs)
        ADD_BITFIELD_RO(REFDIV, 15, 1)
        // PLL post-divide 2 divisor, in the range 1..7 inclusive.
        ADD_BITFIELD_RO(POSTDIV2, 12, 3)
        // PLL post-divide 1 divisor, in the range 1..7 inclusive.
        ADD_BITFIELD_RO(POSTDIV1, 9, 3)
        // PLL feedback divisor, in the range 16..320 inclusive.
        ADD_BITFIELD_RO(FBDIV, 0, 9)
    END_TYPE()

    // Non-default crystal oscillator configuration for the USB bootloader. (ECC) These values may also be used by user code configuring the crystal oscillator. Used if and only if ENABLE_BOOTSEL_NON_DEFAULT_PLL_XOSC_CFG is set in BOOT_FLAGS0. That bit should be set only after this row and BOOTSEL_PLL_CFG are both correctly programmed.
    // Reset value: 0x00000000
    BEGIN_TYPE(BOOTSEL_XOSC_CFG_t, uint32_t)
        // Value of the XOSC_CTRL_FREQ_RANGE register.
        ADD_BITFIELD_RO(RANGE, 14, 2)
        // Value of the XOSC_STARTUP register
        ADD_BITFIELD_RO(STARTUP, 0, 14)
    END_TYPE()

    static const uint32_t BOOTSEL_XOSC_CFG_RANGE__1_15MHZ = 0;
    static const uint32_t BOOTSEL_XOSC_CFG_RANGE__10_30MHZ = 1;
    static const uint32_t BOOTSEL_XOSC_CFG_RANGE__25_60MHZ = 2;
    static const uint32_t BOOTSEL_XOSC_CFG_RANGE__40_100MHZ = 3;

    // USB boot specific feature flags (RBIT-3)
    // Reset value: 0x00000000
    BEGIN_TYPE(USB_BOOT_FLAGS_t, uint32_t)
        // Swap DM/DP during USB boot, to support board layouts with mirrored USB routing (deliberate or accidental).
        ADD_BITFIELD_RO(DP_DM_SWAP, 23, 1)
        // valid flag for INFO_UF2_TXT_BOARD_ID_STRDEF entry of the USB_WHITE_LABEL struct (index 15)
        ADD_BITFIELD_RO(WHITE_LABEL_ADDR_VALID, 22, 1)
        // valid flag for the USB_WHITE_LABEL_ADDR field
        ADD_BITFIELD_RO(WL_INFO_UF2_TXT_BOARD_ID_STRDEF_VALID, 15, 1)
        // valid flag for INFO_UF2_TXT_MODEL_STRDEF entry of the USB_WHITE_LABEL struct (index 14)
        ADD_BITFIELD_RO(WL_INFO_UF2_TXT_MODEL_STRDEF_VALID, 14, 1)
        // valid flag for INDEX_HTM_REDIRECT_NAME_STRDEF entry of the USB_WHITE_LABEL struct (index 13)
        ADD_BITFIELD_RO(WL_INDEX_HTM_REDIRECT_NAME_STRDEF_VALID, 13, 1)
        // valid flag for INDEX_HTM_REDIRECT_URL_STRDEF entry of the USB_WHITE_LABEL struct (index 12)
        ADD_BITFIELD_RO(WL_INDEX_HTM_REDIRECT_URL_STRDEF_VALID, 12, 1)
        // valid flag for SCSI_INQUIRY_VERSION_STRDEF entry of the USB_WHITE_LABEL struct (index 11)
        ADD_BITFIELD_RO(WL_SCSI_INQUIRY_VERSION_STRDEF_VALID, 11, 1)
        // valid flag for SCSI_INQUIRY_PRODUCT_STRDEF entry of the USB_WHITE_LABEL struct (index 10)
        ADD_BITFIELD_RO(WL_SCSI_INQUIRY_PRODUCT_STRDEF_VALID, 10, 1)
        // valid flag for SCSI_INQUIRY_VENDOR_STRDEF entry of the USB_WHITE_LABEL struct (index 9)
        ADD_BITFIELD_RO(WL_SCSI_INQUIRY_VENDOR_STRDEF_VALID, 9, 1)
        // valid flag for VOLUME_LABEL_STRDEF entry of the USB_WHITE_LABEL struct (index 8)
        ADD_BITFIELD_RO(WL_VOLUME_LABEL_STRDEF_VALID, 8, 1)
        // valid flag for USB_CONFIG_ATTRIBUTES_MAX_POWER_VALUES entry of the USB_WHITE_LABEL struct (index 7)
        ADD_BITFIELD_RO(WL_USB_CONFIG_ATTRIBUTES_MAX_POWER_VALUES_VALID, 7, 1)
        // valid flag for USB_DEVICE_SERIAL_NUMBER_STRDEF entry of the USB_WHITE_LABEL struct (index 6)
        ADD_BITFIELD_RO(WL_USB_DEVICE_SERIAL_NUMBER_STRDEF_VALID, 6, 1)
        // valid flag for USB_DEVICE_PRODUCT_STRDEF entry of the USB_WHITE_LABEL struct (index 5)
        ADD_BITFIELD_RO(WL_USB_DEVICE_PRODUCT_STRDEF_VALID, 5, 1)
        // valid flag for USB_DEVICE_MANUFACTURER_STRDEF entry of the USB_WHITE_LABEL struct (index 4)
        ADD_BITFIELD_RO(WL_USB_DEVICE_MANUFACTURER_STRDEF_VALID, 4, 1)
        // valid flag for USB_DEVICE_LANG_ID_VALUE entry of the USB_WHITE_LABEL struct (index 3)
        ADD_BITFIELD_RO(WL_USB_DEVICE_LANG_ID_VALUE_VALID, 3, 1)
        // valid flag for USB_DEVICE_BCD_DEVICEVALUE entry of the USB_WHITE_LABEL struct (index 2)
        ADD_BITFIELD_RO(WL_USB_DEVICE_SERIAL_NUMBER_VALUE_VALID, 2, 1)
        // valid flag for USB_DEVICE_PID_VALUE entry of the USB_WHITE_LABEL struct (index 1)
        ADD_BITFIELD_RO(WL_USB_DEVICE_PID_VALUE_VALID, 1, 1)
        // valid flag for USB_DEVICE_VID_VALUE entry of the USB_WHITE_LABEL struct (index 0)
        ADD_BITFIELD_RO(WL_USB_DEVICE_VID_VALUE_VALID, 0, 1)
    END_TYPE()

    // Redundant copy of USB_BOOT_FLAGS
    BEGIN_TYPE(USB_BOOT_FLAGS_R1_t, uint32_t)
        ADD_BITFIELD_RO(USB_BOOT_FLAGS_R1, 0, 24)
    END_TYPE()

    // Redundant copy of USB_BOOT_FLAGS
    BEGIN_TYPE(USB_BOOT_FLAGS_R2_t, uint32_t)
        ADD_BITFIELD_RO(USB_BOOT_FLAGS_R2, 0, 24)
    END_TYPE()

    // Row index of the USB_WHITE_LABEL structure within OTP (ECC) The table has 16 rows, each of which are also ECC and marked valid by the corresponding valid bit in USB_BOOT_FLAGS (ECC). The entries are either _VALUEs where the 16 bit value is used as is, or _STRDEFs which acts as a pointers to a string value. The value stored in a _STRDEF is two separate bytes: The low seven bits of the first (LSB) byte indicates the number of characters in the string, and the top bit of the first (LSB) byte if set to indicate that each character in the string is two bytes (Unicode) versus one byte if unset. The second (MSB) byte represents the location of the string data, and is encoded as the number of rows from this USB_WHITE_LABEL_ADDR; i.e. the row of the start of the string is USB_WHITE_LABEL_ADDR value + msb_byte. In each case, the corresponding valid bit enables replacing the default value for the corresponding item provided by the boot rom. Note that Unicode _STRDEFs are only supported for USB_DEVICE_PRODUCT_STRDEF, USB_DEVICE_SERIAL_NUMBER_STRDEF and USB_DEVICE_MANUFACTURER_STRDEF. Unicode values will be ignored if specified for other fields, and non-unicode values for these three items will be converted to Unicode characters by setting the upper 8 bits to zero. Note that if the USB_WHITE_LABEL structure or the corresponding strings are not readable by BOOTSEL mode based on OTP permissions, or if alignment requirements are not met, then the corresponding default values are used. The index values indicate where each field is located (row USB_WHITE_LABEL_ADDR value + index):
    BEGIN_TYPE(USB_WHITE_LABEL_ADDR_t, uint32_t)
        ADD_BITFIELD_RO(USB_WHITE_LABEL_ADDR, 0, 16)
    END_TYPE()

    static const uint32_t USB_WHITE_LABEL_ADDR_USB_WHITE_LABEL_ADDR__INDEX_USB_DEVICE_VID_VALUE = 0;
    static const uint32_t USB_WHITE_LABEL_ADDR_USB_WHITE_LABEL_ADDR__INDEX_USB_DEVICE_PID_VALUE = 1;
    static const uint32_t USB_WHITE_LABEL_ADDR_USB_WHITE_LABEL_ADDR__INDEX_USB_DEVICE_BCD_DEVICE_VALUE = 2;
    static const uint32_t USB_WHITE_LABEL_ADDR_USB_WHITE_LABEL_ADDR__INDEX_USB_DEVICE_LANG_ID_VALUE = 3;
    static const uint32_t USB_WHITE_LABEL_ADDR_USB_WHITE_LABEL_ADDR__INDEX_USB_DEVICE_MANUFACTURER_STRDEF = 4;
    static const uint32_t USB_WHITE_LABEL_ADDR_USB_WHITE_LABEL_ADDR__INDEX_USB_DEVICE_PRODUCT_STRDEF = 5;
    static const uint32_t USB_WHITE_LABEL_ADDR_USB_WHITE_LABEL_ADDR__INDEX_USB_DEVICE_SERIAL_NUMBER_STRDEF = 6;
    static const uint32_t USB_WHITE_LABEL_ADDR_USB_WHITE_LABEL_ADDR__INDEX_USB_CONFIG_ATTRIBUTES_MAX_POWER_VALUES = 7;
    static const uint32_t USB_WHITE_LABEL_ADDR_USB_WHITE_LABEL_ADDR__INDEX_VOLUME_LABEL_STRDEF = 8;
    static const uint32_t USB_WHITE_LABEL_ADDR_USB_WHITE_LABEL_ADDR__INDEX_SCSI_INQUIRY_VENDOR_STRDEF = 9;
    static const uint32_t USB_WHITE_LABEL_ADDR_USB_WHITE_LABEL_ADDR__INDEX_SCSI_INQUIRY_PRODUCT_STRDEF = 10;
    static const uint32_t USB_WHITE_LABEL_ADDR_USB_WHITE_LABEL_ADDR__INDEX_SCSI_INQUIRY_VERSION_STRDEF = 11;
    static const uint32_t USB_WHITE_LABEL_ADDR_USB_WHITE_LABEL_ADDR__INDEX_INDEX_HTM_REDIRECT_URL_STRDEF = 12;
    static const uint32_t USB_WHITE_LABEL_ADDR_USB_WHITE_LABEL_ADDR__INDEX_INDEX_HTM_REDIRECT_NAME_STRDEF = 13;
    static const uint32_t USB_WHITE_LABEL_ADDR_USB_WHITE_LABEL_ADDR__INDEX_INFO_UF2_TXT_MODEL_STRDEF = 14;
    static const uint32_t USB_WHITE_LABEL_ADDR_USB_WHITE_LABEL_ADDR__INDEX_INFO_UF2_TXT_BOARD_ID_STRDEF = 15;

    // OTP start row for the OTP boot image. (ECC) If OTP boot is enabled, the bootrom will load from this location into SRAM and then directly enter the loaded image. Note that the image must be signed if SECURE_BOOT_ENABLE is set. The image itself is assumed to be ECC-protected. This must be an even number. Equivalently, the OTP boot image must start at a word-aligned location in the ECC read data address window.
    BEGIN_TYPE(OTPBOOT_SRC_t, uint32_t)
        ADD_BITFIELD_RO(OTPBOOT_SRC, 0, 16)
    END_TYPE()

    // Length in rows of the OTP boot image. (ECC) OTPBOOT_LEN must be even. The total image size must be a multiple of 4 bytes (32 bits).
    BEGIN_TYPE(OTPBOOT_LEN_t, uint32_t)
        ADD_BITFIELD_RO(OTPBOOT_LEN, 0, 16)
    END_TYPE()

    // Bits 15:0 of the OTP boot image load destination (and entry point). (ECC) This must be a location in main SRAM (main SRAM is addresses 0x20000000 through 0x20082000) and must be word-aligned.
    BEGIN_TYPE(OTPBOOT_DST0_t, uint32_t)
        ADD_BITFIELD_RO(OTPBOOT_DST0, 0, 16)
    END_TYPE()

    // Bits 31:16 of the OTP boot image load destination (and entry point). (ECC) This must be a location in main SRAM (main SRAM is addresses 0x20000000 through 0x20082000) and must be word-aligned.
    BEGIN_TYPE(OTPBOOT_DST1_t, uint32_t)
        ADD_BITFIELD_RO(OTPBOOT_DST1, 0, 16)
    END_TYPE()

    // Bits 15:0 of SHA-256 hash of boot key 0 (ECC)
    BEGIN_TYPE(BOOTKEY_0_t, uint32_t)
        ADD_BITFIELD_RO(BOOTKEY_0, 0, 16)
    END_TYPE()

    // Bits 31:16 of SHA-256 hash of boot key 0 (ECC)
    BEGIN_TYPE(BOOTKEY_1_t, uint32_t)
        ADD_BITFIELD_RO(BOOTKEY_1, 0, 16)
    END_TYPE()

    // Bits 47:32 of SHA-256 hash of boot key 0 (ECC)
    BEGIN_TYPE(BOOTKEY_2_t, uint32_t)
        ADD_BITFIELD_RO(BOOTKEY_2, 0, 16)
    END_TYPE()

    // Bits 63:48 of SHA-256 hash of boot key 0 (ECC)
    BEGIN_TYPE(BOOTKEY_3_t, uint32_t)
        ADD_BITFIELD_RO(BOOTKEY_3, 0, 16)
    END_TYPE()

    // Bits 79:64 of SHA-256 hash of boot key 0 (ECC)
    BEGIN_TYPE(BOOTKEY_4_t, uint32_t)
        ADD_BITFIELD_RO(BOOTKEY_4, 0, 16)
    END_TYPE()

    // Bits 95:80 of SHA-256 hash of boot key 0 (ECC)
    BEGIN_TYPE(BOOTKEY_5_t, uint32_t)
        ADD_BITFIELD_RO(BOOTKEY_5, 0, 16)
    END_TYPE()

    // Bits 111:96 of SHA-256 hash of boot key 0 (ECC)
    BEGIN_TYPE(BOOTKEY_6_t, uint32_t)
        ADD_BITFIELD_RO(BOOTKEY_6, 0, 16)
    END_TYPE()

    // Bits 127:112 of SHA-256 hash of boot key 0 (ECC)
    BEGIN_TYPE(BOOTKEY_7_t, uint32_t)
        ADD_BITFIELD_RO(BOOTKEY_7, 0, 16)
    END_TYPE()

    // Bits 143:128 of SHA-256 hash of boot key 0 (ECC)
    BEGIN_TYPE(BOOTKEY_8_t, uint32_t)
        ADD_BITFIELD_RO(BOOTKEY_8, 0, 16)
    END_TYPE()

    // Bits 159:144 of SHA-256 hash of boot key 0 (ECC)
    BEGIN_TYPE(BOOTKEY_9_t, uint32_t)
        ADD_BITFIELD_RO(BOOTKEY_9, 0, 16)
    END_TYPE()

    // Bits 175:160 of SHA-256 hash of boot key 0 (ECC)
    BEGIN_TYPE(BOOTKEY_10_t, uint32_t)
        ADD_BITFIELD_RO(BOOTKEY_10, 0, 16)
    END_TYPE()

    // Bits 191:176 of SHA-256 hash of boot key 0 (ECC)
    BEGIN_TYPE(BOOTKEY_11_t, uint32_t)
        ADD_BITFIELD_RO(BOOTKEY_11, 0, 16)
    END_TYPE()

    // Bits 207:192 of SHA-256 hash of boot key 0 (ECC)
    BEGIN_TYPE(BOOTKEY_12_t, uint32_t)
        ADD_BITFIELD_RO(BOOTKEY_12, 0, 16)
    END_TYPE()

    // Bits 223:208 of SHA-256 hash of boot key 0 (ECC)
    BEGIN_TYPE(BOOTKEY_13_t, uint32_t)
        ADD_BITFIELD_RO(BOOTKEY_13, 0, 16)
    END_TYPE()

    // Bits 239:224 of SHA-256 hash of boot key 0 (ECC)
    BEGIN_TYPE(BOOTKEY_14_t, uint32_t)
        ADD_BITFIELD_RO(BOOTKEY_14, 0, 16)
    END_TYPE()

    // Bits 255:240 of SHA-256 hash of boot key 0 (ECC)
    BEGIN_TYPE(BOOTKEY_15_t, uint32_t)
        ADD_BITFIELD_RO(BOOTKEY_15, 0, 16)
    END_TYPE()

    // Bits 15:0 of OTP access key 1 (ECC)
    BEGIN_TYPE(KEY_0_t, uint32_t)
        ADD_BITFIELD_RO(KEY_0, 0, 16)
    END_TYPE()

    // Bits 31:16 of OTP access key 1 (ECC)
    BEGIN_TYPE(KEY_1_t, uint32_t)
        ADD_BITFIELD_RO(KEY_1, 0, 16)
    END_TYPE()

    // Bits 47:32 of OTP access key 1 (ECC)
    BEGIN_TYPE(KEY_2_t, uint32_t)
        ADD_BITFIELD_RO(KEY_2, 0, 16)
    END_TYPE()

    // Bits 63:48 of OTP access key 1 (ECC)
    BEGIN_TYPE(KEY_3_t, uint32_t)
        ADD_BITFIELD_RO(KEY_3, 0, 16)
    END_TYPE()

    // Bits 79:64 of OTP access key 1 (ECC)
    BEGIN_TYPE(KEY_4_t, uint32_t)
        ADD_BITFIELD_RO(KEY_4, 0, 16)
    END_TYPE()

    // Bits 95:80 of OTP access key 1 (ECC)
    BEGIN_TYPE(KEY_5_t, uint32_t)
        ADD_BITFIELD_RO(KEY_5, 0, 16)
    END_TYPE()

    // Bits 111:96 of OTP access key 1 (ECC)
    BEGIN_TYPE(KEY_6_t, uint32_t)
        ADD_BITFIELD_RO(KEY_6, 0, 16)
    END_TYPE()

    // Bits 127:112 of OTP access key 1 (ECC)
    BEGIN_TYPE(KEY_7_t, uint32_t)
        ADD_BITFIELD_RO(KEY_7, 0, 16)
    END_TYPE()

    // Valid flag for key 1. Once the valid flag is set, the key can no longer be read or written, and becomes a valid fixed key for protecting OTP pages.
    // Reset value: 0x00000000
    BEGIN_TYPE(KEY1_VALID_t, uint32_t)
        // Redundant copy of VALID, with 3-way majority vote
        ADD_BITFIELD_RO(VALID_R2, 16, 1)
        // Redundant copy of VALID, with 3-way majority vote
        ADD_BITFIELD_RO(VALID_R1, 8, 1)
        ADD_BITFIELD_RO(VALID, 0, 1)
    END_TYPE()

    // Valid flag for key 2. Once the valid flag is set, the key can no longer be read or written, and becomes a valid fixed key for protecting OTP pages.
    // Reset value: 0x00000000
    BEGIN_TYPE(KEY2_VALID_t, uint32_t)
        // Redundant copy of VALID, with 3-way majority vote
        ADD_BITFIELD_RO(VALID_R2, 16, 1)
        // Redundant copy of VALID, with 3-way majority vote
        ADD_BITFIELD_RO(VALID_R1, 8, 1)
        ADD_BITFIELD_RO(VALID, 0, 1)
    END_TYPE()

    // Valid flag for key 3. Once the valid flag is set, the key can no longer be read or written, and becomes a valid fixed key for protecting OTP pages.
    // Reset value: 0x00000000
    BEGIN_TYPE(KEY3_VALID_t, uint32_t)
        // Redundant copy of VALID, with 3-way majority vote
        ADD_BITFIELD_RO(VALID_R2, 16, 1)
        // Redundant copy of VALID, with 3-way majority vote
        ADD_BITFIELD_RO(VALID_R1, 8, 1)
        ADD_BITFIELD_RO(VALID, 0, 1)
    END_TYPE()

    // Valid flag for key 4. Once the valid flag is set, the key can no longer be read or written, and becomes a valid fixed key for protecting OTP pages.
    // Reset value: 0x00000000
    BEGIN_TYPE(KEY4_VALID_t, uint32_t)
        // Redundant copy of VALID, with 3-way majority vote
        ADD_BITFIELD_RO(VALID_R2, 16, 1)
        // Redundant copy of VALID, with 3-way majority vote
        ADD_BITFIELD_RO(VALID_R1, 8, 1)
        ADD_BITFIELD_RO(VALID, 0, 1)
    END_TYPE()

    // Valid flag for key 5. Once the valid flag is set, the key can no longer be read or written, and becomes a valid fixed key for protecting OTP pages.
    // Reset value: 0x00000000
    BEGIN_TYPE(KEY5_VALID_t, uint32_t)
        // Redundant copy of VALID, with 3-way majority vote
        ADD_BITFIELD_RO(VALID_R2, 16, 1)
        // Redundant copy of VALID, with 3-way majority vote
        ADD_BITFIELD_RO(VALID_R1, 8, 1)
        ADD_BITFIELD_RO(VALID, 0, 1)
    END_TYPE()

    // Valid flag for key 6. Once the valid flag is set, the key can no longer be read or written, and becomes a valid fixed key for protecting OTP pages.
    // Reset value: 0x00000000
    BEGIN_TYPE(KEY6_VALID_t, uint32_t)
        // Redundant copy of VALID, with 3-way majority vote
        ADD_BITFIELD_RO(VALID_R2, 16, 1)
        // Redundant copy of VALID, with 3-way majority vote
        ADD_BITFIELD_RO(VALID_R1, 8, 1)
        ADD_BITFIELD_RO(VALID, 0, 1)
    END_TYPE()

    // Lock configuration LSBs for page 0 (rows 0x0 through 0x3f). Locks are stored with 3-way majority vote encoding, so that bits can be set independently. This OTP location is always readable, and is write-protected by its own permissions.
    // Reset value: 0x00000000
    BEGIN_TYPE(PAGE_LOCK0_t, uint32_t)
        // Redundant copy of bits 7:0
        ADD_BITFIELD_RO(R2, 16, 8)
        // Redundant copy of bits 7:0
        ADD_BITFIELD_RO(R1, 8, 8)
        // State when at least one key is registered for this page and no matching key has been entered.
        ADD_BITFIELD_RO(NO_KEY_STATE, 6, 1)
        // Index 1-6 of a hardware key which must be entered to grant read access, or 0 if no such key is required.
        ADD_BITFIELD_RO(KEY_R, 3, 3)
        // Index 1-6 of a hardware key which must be entered to grant write access, or 0 if no such key is required.
        ADD_BITFIELD_RO(KEY_W, 0, 3)
    END_TYPE()

    static const uint32_t PAGE_LOCK0_NO_KEY_STATE__read_only = 0;
    static const uint32_t PAGE_LOCK0_NO_KEY_STATE__inaccessible = 1;

    // Lock configuration MSBs for page 0 (rows 0x0 through 0x3f). Locks are stored with 3-way majority vote encoding, so that bits can be set independently. This OTP location is always readable, and is write-protected by its own permissions.
    // Reset value: 0x00000000
    BEGIN_TYPE(PAGE_LOCK1_t, uint32_t)
        // Redundant copy of bits 7:0
        ADD_BITFIELD_RO(R2, 16, 8)
        // Redundant copy of bits 7:0
        ADD_BITFIELD_RO(R1, 8, 8)
        // Dummy lock bits reserved for bootloaders (including the RP2350 USB bootloader) to store their own OTP access permissions. No hardware effect, and no corresponding SW_LOCKx registers.
        ADD_BITFIELD_RO(LOCK_BL, 4, 2)
        // Lock state for Non-secure accesses to this page. Thermometer-coded, so lock state can be advanced permanently from any state to any less-permissive state by programming OTP. Software can also advance the lock state temporarily (until next OTP reset) using the SW_LOCKx registers. Note that READ_WRITE and READ_ONLY are equivalent in hardware, as the SBPI programming interface is not accessible to Non-secure software. However, Secure software may check these bits to apply write permissions to a Non-secure OTP programming API.
        ADD_BITFIELD_RO(LOCK_NS, 2, 2)
        // Lock state for Secure accesses to this page. Thermometer-coded, so lock state can be advanced permanently from any state to any less-permissive state by programming OTP. Software can also advance the lock state temporarily (until next OTP reset) using the SW_LOCKx registers.
        ADD_BITFIELD_RO(LOCK_S, 0, 2)
    END_TYPE()

    // Bootloader permits user reads and writes to this page
    static const uint32_t PAGE_LOCK1_LOCK_BL__read_write = 0;
    // Bootloader permits user reads of this page
    static const uint32_t PAGE_LOCK1_LOCK_BL__read_only = 1;
    // Do not use. Behaves the same as INACCESSIBLE
    static const uint32_t PAGE_LOCK1_LOCK_BL__reserved = 2;
    // Bootloader does not permit user access to this page
    static const uint32_t PAGE_LOCK1_LOCK_BL__inaccessible = 3;
    // Page can be read by Non-secure software, and Secure software may permit Non-secure writes.
    static const uint32_t PAGE_LOCK1_LOCK_NS__read_write = 0;
    // Page can be read by Non-secure software
    static const uint32_t PAGE_LOCK1_LOCK_NS__read_only = 1;
    // Do not use. Behaves the same as INACCESSIBLE.
    static const uint32_t PAGE_LOCK1_LOCK_NS__reserved = 2;
    // Page can not be accessed by Non-secure software.
    static const uint32_t PAGE_LOCK1_LOCK_NS__inaccessible = 3;
    // Page is fully accessible by Secure software.
    static const uint32_t PAGE_LOCK1_LOCK_S__read_write = 0;
    // Page can be read by Secure software, but can not be written.
    static const uint32_t PAGE_LOCK1_LOCK_S__read_only = 1;
    // Do not use. Behaves the same as INACCESSIBLE.
    static const uint32_t PAGE_LOCK1_LOCK_S__reserved = 2;
    // Page can not be accessed by Secure software.
    static const uint32_t PAGE_LOCK1_LOCK_S__inaccessible = 3;

    // Lock configuration LSBs for page 63 (rows 0xfc0 through 0xfff). Locks are stored with 3-way majority vote encoding, so that bits can be set independently. This OTP location is always readable, and is write-protected by its own permissions.
    // Reset value: 0x00000000
    BEGIN_TYPE(PAGE63_LOCK0_t, uint32_t)
        // Redundant copy of bits 7:0
        ADD_BITFIELD_RO(R2, 16, 8)
        // Redundant copy of bits 7:0
        ADD_BITFIELD_RO(R1, 8, 8)
        // Decommission for RMA of a suspected faulty device. This re-enables the factory test JTAG interface, and makes pages 3 through 61 of the OTP permanently inaccessible.
        ADD_BITFIELD_RO(RMA, 7, 1)
        // State when at least one key is registered for this page and no matching key has been entered.
        ADD_BITFIELD_RO(NO_KEY_STATE, 6, 1)
        // Index 1-6 of a hardware key which must be entered to grant read access, or 0 if no such key is required.
        ADD_BITFIELD_RO(KEY_R, 3, 3)
        // Index 1-6 of a hardware key which must be entered to grant write access, or 0 if no such key is required.
        ADD_BITFIELD_RO(KEY_W, 0, 3)
    END_TYPE()

    static const uint32_t PAGE63_LOCK0_NO_KEY_STATE__read_only = 0;
    static const uint32_t PAGE63_LOCK0_NO_KEY_STATE__inaccessible = 1;

    // Lock configuration MSBs for page 63 (rows 0xfc0 through 0xfff). Locks are stored with 3-way majority vote encoding, so that bits can be set independently. This OTP location is always readable, and is write-protected by its own permissions.
    // Reset value: 0x00000000
    BEGIN_TYPE(PAGE63_LOCK1_t, uint32_t)
        // Redundant copy of bits 7:0
        ADD_BITFIELD_RO(R2, 16, 8)
        // Redundant copy of bits 7:0
        ADD_BITFIELD_RO(R1, 8, 8)
        // Dummy lock bits reserved for bootloaders (including the RP2350 USB bootloader) to store their own OTP access permissions. No hardware effect, and no corresponding SW_LOCKx registers.
        ADD_BITFIELD_RO(LOCK_BL, 4, 2)
        // Lock state for Non-secure accesses to this page. Thermometer-coded, so lock state can be advanced permanently from any state to any less-permissive state by programming OTP. Software can also advance the lock state temporarily (until next OTP reset) using the SW_LOCKx registers. Note that READ_WRITE and READ_ONLY are equivalent in hardware, as the SBPI programming interface is not accessible to Non-secure software. However, Secure software may check these bits to apply write permissions to a Non-secure OTP programming API.
        ADD_BITFIELD_RO(LOCK_NS, 2, 2)
        // Lock state for Secure accesses to this page. Thermometer-coded, so lock state can be advanced permanently from any state to any less-permissive state by programming OTP. Software can also advance the lock state temporarily (until next OTP reset) using the SW_LOCKx registers.
        ADD_BITFIELD_RO(LOCK_S, 0, 2)
    END_TYPE()

    // Bootloader permits user reads and writes to this page
    static const uint32_t PAGE63_LOCK1_LOCK_BL__read_write = 0;
    // Bootloader permits user reads of this page
    static const uint32_t PAGE63_LOCK1_LOCK_BL__read_only = 1;
    // Do not use. Behaves the same as INACCESSIBLE
    static const uint32_t PAGE63_LOCK1_LOCK_BL__reserved = 2;
    // Bootloader does not permit user access to this page
    static const uint32_t PAGE63_LOCK1_LOCK_BL__inaccessible = 3;
    // Page can be read by Non-secure software, and Secure software may permit Non-secure writes.
    static const uint32_t PAGE63_LOCK1_LOCK_NS__read_write = 0;
    // Page can be read by Non-secure software
    static const uint32_t PAGE63_LOCK1_LOCK_NS__read_only = 1;
    // Do not use. Behaves the same as INACCESSIBLE.
    static const uint32_t PAGE63_LOCK1_LOCK_NS__reserved = 2;
    // Page can not be accessed by Non-secure software.
    static const uint32_t PAGE63_LOCK1_LOCK_NS__inaccessible = 3;
    // Page is fully accessible by Secure software.
    static const uint32_t PAGE63_LOCK1_LOCK_S__read_write = 0;
    // Page can be read by Secure software, but can not be written.
    static const uint32_t PAGE63_LOCK1_LOCK_S__read_only = 1;
    // Do not use. Behaves the same as INACCESSIBLE.
    static const uint32_t PAGE63_LOCK1_LOCK_S__reserved = 2;
    // Page can not be accessed by Secure software.
    static const uint32_t PAGE63_LOCK1_LOCK_S__inaccessible = 3;

    struct OTP_DATA_RAW_t {
        CHIPID0_t                     CHIPID0;
        CHIPID1_t                     CHIPID1;
        CHIPID2_t                     CHIPID2;
        CHIPID3_t                     CHIPID3;
        RANDID0_t                     RANDID0;
        RANDID1_t                     RANDID1;
        RANDID2_t                     RANDID2;
        RANDID3_t                     RANDID3;
        RANDID4_t                     RANDID4;
        RANDID5_t                     RANDID5;
        RANDID6_t                     RANDID6;
        RANDID7_t                     RANDID7;
        uint32_t                      reserved0[4];
        ROSC_CALIB_t                  ROSC_CALIB;
        LPOSC_CALIB_t                 LPOSC_CALIB;
        uint32_t                      reserved1[6];
        NUM_GPIOS_t                   NUM_GPIOS;
        uint32_t                      reserved2[29];
        INFO_CRC0_t                   INFO_CRC0;
        INFO_CRC1_t                   INFO_CRC1;
        CRIT0_t                       CRIT0;
        CRIT0_R1_t                    CRIT0_R1;
        CRIT0_R2_t                    CRIT0_R2;
        CRIT0_R3_t                    CRIT0_R3;
        CRIT0_R4_t                    CRIT0_R4;
        CRIT0_R5_t                    CRIT0_R5;
        CRIT0_R6_t                    CRIT0_R6;
        CRIT0_R7_t                    CRIT0_R7;
        CRIT1_t                       CRIT1;
        CRIT1_R1_t                    CRIT1_R1;
        CRIT1_R2_t                    CRIT1_R2;
        CRIT1_R3_t                    CRIT1_R3;
        CRIT1_R4_t                    CRIT1_R4;
        CRIT1_R5_t                    CRIT1_R5;
        CRIT1_R6_t                    CRIT1_R6;
        CRIT1_R7_t                    CRIT1_R7;
        BOOT_FLAGS0_t                 BOOT_FLAGS0;
        BOOT_FLAGS0_R1_t              BOOT_FLAGS0_R1;
        BOOT_FLAGS0_R2_t              BOOT_FLAGS0_R2;
        BOOT_FLAGS1_t                 BOOT_FLAGS1;
        BOOT_FLAGS1_R1_t              BOOT_FLAGS1_R1;
        BOOT_FLAGS1_R2_t              BOOT_FLAGS1_R2;
        DEFAULT_BOOT_VERSION0_t       DEFAULT_BOOT_VERSION0;
        DEFAULT_BOOT_VERSION0_R1_t    DEFAULT_BOOT_VERSION0_R1;
        DEFAULT_BOOT_VERSION0_R2_t    DEFAULT_BOOT_VERSION0_R2;
        DEFAULT_BOOT_VERSION1_t       DEFAULT_BOOT_VERSION1;
        DEFAULT_BOOT_VERSION1_R1_t    DEFAULT_BOOT_VERSION1_R1;
        DEFAULT_BOOT_VERSION1_R2_t    DEFAULT_BOOT_VERSION1_R2;
        FLASH_DEVINFO_t               FLASH_DEVINFO;
        FLASH_PARTITION_SLOT_SIZE_t   FLASH_PARTITION_SLOT_SIZE;
        BOOTSEL_LED_CFG_t             BOOTSEL_LED_CFG;
        BOOTSEL_PLL_CFG_t             BOOTSEL_PLL_CFG;
        BOOTSEL_XOSC_CFG_t            BOOTSEL_XOSC_CFG;
        USB_BOOT_FLAGS_t              USB_BOOT_FLAGS;
        USB_BOOT_FLAGS_R1_t           USB_BOOT_FLAGS_R1;
        USB_BOOT_FLAGS_R2_t           USB_BOOT_FLAGS_R2;
        USB_WHITE_LABEL_ADDR_t        USB_WHITE_LABEL_ADDR;
        uint32_t                      reserved3;
        OTPBOOT_SRC_t                 OTPBOOT_SRC;
        OTPBOOT_LEN_t                 OTPBOOT_LEN;
        OTPBOOT_DST0_t                OTPBOOT_DST0;
        OTPBOOT_DST1_t                OTPBOOT_DST1;
        uint32_t                      reserved4[30];
        BOOTKEY_0_t                   BOOTKEY0_0;
        BOOTKEY_1_t                   BOOTKEY0_1;
        BOOTKEY_2_t                   BOOTKEY0_2;
        BOOTKEY_3_t                   BOOTKEY0_3;
        BOOTKEY_4_t                   BOOTKEY0_4;
        BOOTKEY_5_t                   BOOTKEY0_5;
        BOOTKEY_6_t                   BOOTKEY0_6;
        BOOTKEY_7_t                   BOOTKEY0_7;
        BOOTKEY_8_t                   BOOTKEY0_8;
        BOOTKEY_9_t                   BOOTKEY0_9;
        BOOTKEY_10_t                  BOOTKEY0_10;
        BOOTKEY_11_t                  BOOTKEY0_11;
        BOOTKEY_12_t                  BOOTKEY0_12;
        BOOTKEY_13_t                  BOOTKEY0_13;
        BOOTKEY_14_t                  BOOTKEY0_14;
        BOOTKEY_15_t                  BOOTKEY0_15;
        BOOTKEY_0_t                   BOOTKEY1_0;
        BOOTKEY_1_t                   BOOTKEY1_1;
        BOOTKEY_2_t                   BOOTKEY1_2;
        BOOTKEY_3_t                   BOOTKEY1_3;
        BOOTKEY_4_t                   BOOTKEY1_4;
        BOOTKEY_5_t                   BOOTKEY1_5;
        BOOTKEY_6_t                   BOOTKEY1_6;
        BOOTKEY_7_t                   BOOTKEY1_7;
        BOOTKEY_8_t                   BOOTKEY1_8;
        BOOTKEY_9_t                   BOOTKEY1_9;
        BOOTKEY_10_t                  BOOTKEY1_10;
        BOOTKEY_11_t                  BOOTKEY1_11;
        BOOTKEY_12_t                  BOOTKEY1_12;
        BOOTKEY_13_t                  BOOTKEY1_13;
        BOOTKEY_14_t                  BOOTKEY1_14;
        BOOTKEY_15_t                  BOOTKEY1_15;
        BOOTKEY_0_t                   BOOTKEY2_0;
        BOOTKEY_1_t                   BOOTKEY2_1;
        BOOTKEY_2_t                   BOOTKEY2_2;
        BOOTKEY_3_t                   BOOTKEY2_3;
        BOOTKEY_4_t                   BOOTKEY2_4;
        BOOTKEY_5_t                   BOOTKEY2_5;
        BOOTKEY_6_t                   BOOTKEY2_6;
        BOOTKEY_7_t                   BOOTKEY2_7;
        BOOTKEY_8_t                   BOOTKEY2_8;
        BOOTKEY_9_t                   BOOTKEY2_9;
        BOOTKEY_10_t                  BOOTKEY2_10;
        BOOTKEY_11_t                  BOOTKEY2_11;
        BOOTKEY_12_t                  BOOTKEY2_12;
        BOOTKEY_13_t                  BOOTKEY2_13;
        BOOTKEY_14_t                  BOOTKEY2_14;
        BOOTKEY_15_t                  BOOTKEY2_15;
        BOOTKEY_0_t                   BOOTKEY3_0;
        BOOTKEY_1_t                   BOOTKEY3_1;
        BOOTKEY_2_t                   BOOTKEY3_2;
        BOOTKEY_3_t                   BOOTKEY3_3;
        BOOTKEY_4_t                   BOOTKEY3_4;
        BOOTKEY_5_t                   BOOTKEY3_5;
        BOOTKEY_6_t                   BOOTKEY3_6;
        BOOTKEY_7_t                   BOOTKEY3_7;
        BOOTKEY_8_t                   BOOTKEY3_8;
        BOOTKEY_9_t                   BOOTKEY3_9;
        BOOTKEY_10_t                  BOOTKEY3_10;
        BOOTKEY_11_t                  BOOTKEY3_11;
        BOOTKEY_12_t                  BOOTKEY3_12;
        BOOTKEY_13_t                  BOOTKEY3_13;
        BOOTKEY_14_t                  BOOTKEY3_14;
        BOOTKEY_15_t                  BOOTKEY3_15;
        uint32_t                      reserved5[3720];
        KEY_0_t                       KEY1_0;
        KEY_1_t                       KEY1_1;
        KEY_2_t                       KEY1_2;
        KEY_3_t                       KEY1_3;
        KEY_4_t                       KEY1_4;
        KEY_5_t                       KEY1_5;
        KEY_6_t                       KEY1_6;
        KEY_7_t                       KEY1_7;
        KEY_0_t                       KEY2_0;
        KEY_1_t                       KEY2_1;
        KEY_2_t                       KEY2_2;
        KEY_3_t                       KEY2_3;
        KEY_4_t                       KEY2_4;
        KEY_5_t                       KEY2_5;
        KEY_6_t                       KEY2_6;
        KEY_7_t                       KEY2_7;
        KEY_0_t                       KEY3_0;
        KEY_1_t                       KEY3_1;
        KEY_2_t                       KEY3_2;
        KEY_3_t                       KEY3_3;
        KEY_4_t                       KEY3_4;
        KEY_5_t                       KEY3_5;
        KEY_6_t                       KEY3_6;
        KEY_7_t                       KEY3_7;
        KEY_0_t                       KEY4_0;
        KEY_1_t                       KEY4_1;
        KEY_2_t                       KEY4_2;
        KEY_3_t                       KEY4_3;
        KEY_4_t                       KEY4_4;
        KEY_5_t                       KEY4_5;
        KEY_6_t                       KEY4_6;
        KEY_7_t                       KEY4_7;
        KEY_0_t                       KEY5_0;
        KEY_1_t                       KEY5_1;
        KEY_2_t                       KEY5_2;
        KEY_3_t                       KEY5_3;
        KEY_4_t                       KEY5_4;
        KEY_5_t                       KEY5_5;
        KEY_6_t                       KEY5_6;
        KEY_7_t                       KEY5_7;
        KEY_0_t                       KEY6_0;
        KEY_1_t                       KEY6_1;
        KEY_2_t                       KEY6_2;
        KEY_3_t                       KEY6_3;
        KEY_4_t                       KEY6_4;
        KEY_5_t                       KEY6_5;
        KEY_6_t                       KEY6_6;
        KEY_7_t                       KEY6_7;
        uint32_t                      reserved6;
        KEY1_VALID_t                  KEY1_VALID;
        KEY2_VALID_t                  KEY2_VALID;
        KEY3_VALID_t                  KEY3_VALID;
        KEY4_VALID_t                  KEY4_VALID;
        KEY5_VALID_t                  KEY5_VALID;
        KEY6_VALID_t                  KEY6_VALID;
        uint32_t                      reserved7;
        PAGE_LOCK0_t                  PAGE0_LOCK0;
        PAGE_LOCK1_t                  PAGE0_LOCK1;
        PAGE_LOCK0_t                  PAGE1_LOCK0;
        PAGE_LOCK1_t                  PAGE1_LOCK1;
        PAGE_LOCK0_t                  PAGE2_LOCK0;
        PAGE_LOCK1_t                  PAGE2_LOCK1;
        PAGE_LOCK0_t                  PAGE3_LOCK0;
        PAGE_LOCK1_t                  PAGE3_LOCK1;
        PAGE_LOCK0_t                  PAGE4_LOCK0;
        PAGE_LOCK1_t                  PAGE4_LOCK1;
        PAGE_LOCK0_t                  PAGE5_LOCK0;
        PAGE_LOCK1_t                  PAGE5_LOCK1;
        PAGE_LOCK0_t                  PAGE6_LOCK0;
        PAGE_LOCK1_t                  PAGE6_LOCK1;
        PAGE_LOCK0_t                  PAGE7_LOCK0;
        PAGE_LOCK1_t                  PAGE7_LOCK1;
        PAGE_LOCK0_t                  PAGE8_LOCK0;
        PAGE_LOCK1_t                  PAGE8_LOCK1;
        PAGE_LOCK0_t                  PAGE9_LOCK0;
        PAGE_LOCK1_t                  PAGE9_LOCK1;
        PAGE_LOCK0_t                  PAGE10_LOCK0;
        PAGE_LOCK1_t                  PAGE10_LOCK1;
        PAGE_LOCK0_t                  PAGE11_LOCK0;
        PAGE_LOCK1_t                  PAGE11_LOCK1;
        PAGE_LOCK0_t                  PAGE12_LOCK0;
        PAGE_LOCK1_t                  PAGE12_LOCK1;
        PAGE_LOCK0_t                  PAGE13_LOCK0;
        PAGE_LOCK1_t                  PAGE13_LOCK1;
        PAGE_LOCK0_t                  PAGE14_LOCK0;
        PAGE_LOCK1_t                  PAGE14_LOCK1;
        PAGE_LOCK0_t                  PAGE15_LOCK0;
        PAGE_LOCK1_t                  PAGE15_LOCK1;
        PAGE_LOCK0_t                  PAGE16_LOCK0;
        PAGE_LOCK1_t                  PAGE16_LOCK1;
        PAGE_LOCK0_t                  PAGE17_LOCK0;
        PAGE_LOCK1_t                  PAGE17_LOCK1;
        PAGE_LOCK0_t                  PAGE18_LOCK0;
        PAGE_LOCK1_t                  PAGE18_LOCK1;
        PAGE_LOCK0_t                  PAGE19_LOCK0;
        PAGE_LOCK1_t                  PAGE19_LOCK1;
        PAGE_LOCK0_t                  PAGE20_LOCK0;
        PAGE_LOCK1_t                  PAGE20_LOCK1;
        PAGE_LOCK0_t                  PAGE21_LOCK0;
        PAGE_LOCK1_t                  PAGE21_LOCK1;
        PAGE_LOCK0_t                  PAGE22_LOCK0;
        PAGE_LOCK1_t                  PAGE22_LOCK1;
        PAGE_LOCK0_t                  PAGE23_LOCK0;
        PAGE_LOCK1_t                  PAGE23_LOCK1;
        PAGE_LOCK0_t                  PAGE24_LOCK0;
        PAGE_LOCK1_t                  PAGE24_LOCK1;
        PAGE_LOCK0_t                  PAGE25_LOCK0;
        PAGE_LOCK1_t                  PAGE25_LOCK1;
        PAGE_LOCK0_t                  PAGE26_LOCK0;
        PAGE_LOCK1_t                  PAGE26_LOCK1;
        PAGE_LOCK0_t                  PAGE27_LOCK0;
        PAGE_LOCK1_t                  PAGE27_LOCK1;
        PAGE_LOCK0_t                  PAGE28_LOCK0;
        PAGE_LOCK1_t                  PAGE28_LOCK1;
        PAGE_LOCK0_t                  PAGE29_LOCK0;
        PAGE_LOCK1_t                  PAGE29_LOCK1;
        PAGE_LOCK0_t                  PAGE30_LOCK0;
        PAGE_LOCK1_t                  PAGE30_LOCK1;
        PAGE_LOCK0_t                  PAGE31_LOCK0;
        PAGE_LOCK1_t                  PAGE31_LOCK1;
        PAGE_LOCK0_t                  PAGE32_LOCK0;
        PAGE_LOCK1_t                  PAGE32_LOCK1;
        PAGE_LOCK0_t                  PAGE33_LOCK0;
        PAGE_LOCK1_t                  PAGE33_LOCK1;
        PAGE_LOCK0_t                  PAGE34_LOCK0;
        PAGE_LOCK1_t                  PAGE34_LOCK1;
        PAGE_LOCK0_t                  PAGE35_LOCK0;
        PAGE_LOCK1_t                  PAGE35_LOCK1;
        PAGE_LOCK0_t                  PAGE36_LOCK0;
        PAGE_LOCK1_t                  PAGE36_LOCK1;
        PAGE_LOCK0_t                  PAGE37_LOCK0;
        PAGE_LOCK1_t                  PAGE37_LOCK1;
        PAGE_LOCK0_t                  PAGE38_LOCK0;
        PAGE_LOCK1_t                  PAGE38_LOCK1;
        PAGE_LOCK0_t                  PAGE39_LOCK0;
        PAGE_LOCK1_t                  PAGE39_LOCK1;
        PAGE_LOCK0_t                  PAGE40_LOCK0;
        PAGE_LOCK1_t                  PAGE40_LOCK1;
        PAGE_LOCK0_t                  PAGE41_LOCK0;
        PAGE_LOCK1_t                  PAGE41_LOCK1;
        PAGE_LOCK0_t                  PAGE42_LOCK0;
        PAGE_LOCK1_t                  PAGE42_LOCK1;
        PAGE_LOCK0_t                  PAGE43_LOCK0;
        PAGE_LOCK1_t                  PAGE43_LOCK1;
        PAGE_LOCK0_t                  PAGE44_LOCK0;
        PAGE_LOCK1_t                  PAGE44_LOCK1;
        PAGE_LOCK0_t                  PAGE45_LOCK0;
        PAGE_LOCK1_t                  PAGE45_LOCK1;
        PAGE_LOCK0_t                  PAGE46_LOCK0;
        PAGE_LOCK1_t                  PAGE46_LOCK1;
        PAGE_LOCK0_t                  PAGE47_LOCK0;
        PAGE_LOCK1_t                  PAGE47_LOCK1;
        PAGE_LOCK0_t                  PAGE48_LOCK0;
        PAGE_LOCK1_t                  PAGE48_LOCK1;
        PAGE_LOCK0_t                  PAGE49_LOCK0;
        PAGE_LOCK1_t                  PAGE49_LOCK1;
        PAGE_LOCK0_t                  PAGE50_LOCK0;
        PAGE_LOCK1_t                  PAGE50_LOCK1;
        PAGE_LOCK0_t                  PAGE51_LOCK0;
        PAGE_LOCK1_t                  PAGE51_LOCK1;
        PAGE_LOCK0_t                  PAGE52_LOCK0;
        PAGE_LOCK1_t                  PAGE52_LOCK1;
        PAGE_LOCK0_t                  PAGE53_LOCK0;
        PAGE_LOCK1_t                  PAGE53_LOCK1;
        PAGE_LOCK0_t                  PAGE54_LOCK0;
        PAGE_LOCK1_t                  PAGE54_LOCK1;
        PAGE_LOCK0_t                  PAGE55_LOCK0;
        PAGE_LOCK1_t                  PAGE55_LOCK1;
        PAGE_LOCK0_t                  PAGE56_LOCK0;
        PAGE_LOCK1_t                  PAGE56_LOCK1;
        PAGE_LOCK0_t                  PAGE57_LOCK0;
        PAGE_LOCK1_t                  PAGE57_LOCK1;
        PAGE_LOCK0_t                  PAGE58_LOCK0;
        PAGE_LOCK1_t                  PAGE58_LOCK1;
        PAGE_LOCK0_t                  PAGE59_LOCK0;
        PAGE_LOCK1_t                  PAGE59_LOCK1;
        PAGE_LOCK0_t                  PAGE60_LOCK0;
        PAGE_LOCK1_t                  PAGE60_LOCK1;
        PAGE_LOCK0_t                  PAGE61_LOCK0;
        PAGE_LOCK1_t                  PAGE61_LOCK1;
        PAGE_LOCK0_t                  PAGE62_LOCK0;
        PAGE_LOCK1_t                  PAGE62_LOCK1;
        PAGE63_LOCK0_t                PAGE63_LOCK0;
        PAGE63_LOCK1_t                PAGE63_LOCK1;
    };

    static OTP_DATA_RAW_t & OTP_DATA_RAW = (*(OTP_DATA_RAW_t *)0x40134000);
    static OTP_DATA_RAW_t & OTP_DATA_RAW_XOR = (*(OTP_DATA_RAW_t *)0x40135000);
    static OTP_DATA_RAW_t & OTP_DATA_RAW_SET = (*(OTP_DATA_RAW_t *)0x40136000);
    static OTP_DATA_RAW_t & OTP_DATA_RAW_CLR = (*(OTP_DATA_RAW_t *)0x40137000);

} // _OTP_DATA_RAW_

// For managing simulation testbenches
namespace _TBMAN_  {

    // Indicates the type of platform in use
    // Reset value: 0x00000001
    BEGIN_TYPE(PLATFORM_t, uint32_t)
        // Indicates the platform is a simulation
        ADD_BITFIELD_RO(HDLSIM, 2, 1)
        // Indicates the platform is an FPGA
        ADD_BITFIELD_RO(FPGA, 1, 1)
        // Indicates the platform is an ASIC
        ADD_BITFIELD_RO(ASIC, 0, 1)
    END_TYPE()

    struct TBMAN_t {
        PLATFORM_t                    PLATFORM;
    };

    static TBMAN_t & TBMAN = (*(TBMAN_t *)0x40160000);
    static TBMAN_t & TBMAN_XOR = (*(TBMAN_t *)0x40161000);
    static TBMAN_t & TBMAN_SET = (*(TBMAN_t *)0x40162000);
    static TBMAN_t & TBMAN_CLR = (*(TBMAN_t *)0x40163000);

} // _TBMAN_

// DPRAM layout for USB device.
namespace _USB_DPRAM_  {

    // Bytes 0-3 of the SETUP packet from the host.
    // Reset value: 0x00000000
    BEGIN_TYPE(SETUP_PACKET_LOW_t, uint32_t)
        ADD_BITFIELD_RW(WVALUE, 16, 16)
        ADD_BITFIELD_RW(BREQUEST, 8, 8)
        ADD_BITFIELD_RW(BMREQUESTTYPE, 0, 8)
    END_TYPE()

    // Bytes 4-7 of the setup packet from the host.
    // Reset value: 0x00000000
    BEGIN_TYPE(SETUP_PACKET_HIGH_t, uint32_t)
        ADD_BITFIELD_RW(WLENGTH, 16, 16)
        ADD_BITFIELD_RW(WINDEX, 0, 16)
    END_TYPE()

    // Reset value: 0x00000000
    BEGIN_TYPE(EP_CONTROL_t, uint32_t)
        // Enable this endpoint. The device will not reply to any packets for this endpoint if this bit is not set.
        ADD_BITFIELD_RW(ENABLE, 31, 1)
        // This endpoint is double buffered.
        ADD_BITFIELD_RW(DOUBLE_BUFFERED, 30, 1)
        // Trigger an interrupt each time a buffer is done.
        ADD_BITFIELD_RW(INTERRUPT_PER_BUFF, 29, 1)
        // Trigger an interrupt each time both buffers are done. Only valid in double buffered mode.
        ADD_BITFIELD_RW(INTERRUPT_PER_DOUBLE_BUFF, 28, 1)
        ADD_BITFIELD_RW(ENDPOINT_TYPE, 26, 2)
        // Trigger an interrupt if a STALL is sent. Intended for debug only.
        ADD_BITFIELD_RW(INTERRUPT_ON_STALL, 17, 1)
        // Trigger an interrupt if a NAK is sent. Intended for debug only.
        ADD_BITFIELD_RW(INTERRUPT_ON_NAK, 16, 1)
        // 64 byte aligned buffer address for this EP (bits 0-5 are ignored). Relative to the start of the DPRAM.
        ADD_BITFIELD_RW(BUFFER_ADDRESS, 0, 16)
    END_TYPE()

    static const uint32_t EP_CONTROL_ENDPOINT_TYPE__Control = 0;
    static const uint32_t EP_CONTROL_ENDPOINT_TYPE__Isochronous = 1;
    static const uint32_t EP_CONTROL_ENDPOINT_TYPE__Bulk = 2;
    static const uint32_t EP_CONTROL_ENDPOINT_TYPE__Interrupt = 3;

    // Buffer control for both buffers of an endpoint. Fields ending in a _1 are for buffer 1.  Fields ending in a _0 are for buffer 0. Buffer 1 controls are only valid if the endpoint is in double buffered mode.
    // Reset value: 0x00000000
    BEGIN_TYPE(EP_BUFFER_CONTROL_t, uint32_t)
        // Buffer 1 is full. For an IN transfer (TX to the host) the bit is set to indicate the data is valid. For an OUT transfer (RX from the host) this bit should be left as a 0. The host will set it when it has filled the buffer with data.
        ADD_BITFIELD_RW(FULL_1, 31, 1)
        // Buffer 1 is the last buffer of the transfer.
        ADD_BITFIELD_RW(LAST_1, 30, 1)
        // The data pid of buffer 1.
        ADD_BITFIELD_RW(PID_1, 29, 1)
        // The number of bytes buffer 1 is offset from buffer 0 in Isochronous mode. Only valid in double buffered mode for an Isochronous endpoint.  For a non Isochronous endpoint the offset is always 64 bytes.
        ADD_BITFIELD_RW(DOUBLE_BUFFER_ISO_OFFSET, 27, 2)
        // Buffer 1 is available. This bit is set to indicate the buffer can be used by the controller. The controller clears the available bit when writing the status back.
        ADD_BITFIELD_RW(AVAILABLE_1, 26, 1)
        // The length of the data in buffer 1.
        ADD_BITFIELD_RW(LENGTH_1, 16, 10)
        // Buffer 0 is full. For an IN transfer (TX to the host) the bit is set to indicate the data is valid. For an OUT transfer (RX from the host) this bit should be left as a 0. The host will set it when it has filled the buffer with data.
        ADD_BITFIELD_RW(FULL_0, 15, 1)
        // Buffer 0 is the last buffer of the transfer.
        ADD_BITFIELD_RW(LAST_0, 14, 1)
        // The data pid of buffer 0.
        ADD_BITFIELD_RW(PID_0, 13, 1)
        // Reset the buffer selector to buffer 0.
        ADD_BITFIELD_RW(RESET, 12, 1)
        // Reply with a stall (valid for both buffers).
        ADD_BITFIELD_RW(STALL, 11, 1)
        // Buffer 0 is available. This bit is set to indicate the buffer can be used by the controller. The controller clears the available bit when writing the status back.
        ADD_BITFIELD_RW(AVAILABLE_0, 10, 1)
        // The length of the data in buffer 1.
        ADD_BITFIELD_RW(LENGTH_0, 0, 10)
    END_TYPE()

    static const uint32_t EP_BUFFER_CONTROL_DOUBLE_BUFFER_ISO_OFFSET__128 = 0;
    static const uint32_t EP_BUFFER_CONTROL_DOUBLE_BUFFER_ISO_OFFSET__256 = 1;
    static const uint32_t EP_BUFFER_CONTROL_DOUBLE_BUFFER_ISO_OFFSET__512 = 2;
    static const uint32_t EP_BUFFER_CONTROL_DOUBLE_BUFFER_ISO_OFFSET__1024 = 3;

    struct USB_DPRAM_t {
        SETUP_PACKET_LOW_t            SETUP_PACKET_LOW;
        SETUP_PACKET_HIGH_t           SETUP_PACKET_HIGH;
        EP_CONTROL_t                  EP1_IN_CONTROL;
        EP_CONTROL_t                  EP1_OUT_CONTROL;
        EP_CONTROL_t                  EP2_IN_CONTROL;
        EP_CONTROL_t                  EP2_OUT_CONTROL;
        EP_CONTROL_t                  EP3_IN_CONTROL;
        EP_CONTROL_t                  EP3_OUT_CONTROL;
        EP_CONTROL_t                  EP4_IN_CONTROL;
        EP_CONTROL_t                  EP4_OUT_CONTROL;
        EP_CONTROL_t                  EP5_IN_CONTROL;
        EP_CONTROL_t                  EP5_OUT_CONTROL;
        EP_CONTROL_t                  EP6_IN_CONTROL;
        EP_CONTROL_t                  EP6_OUT_CONTROL;
        EP_CONTROL_t                  EP7_IN_CONTROL;
        EP_CONTROL_t                  EP7_OUT_CONTROL;
        EP_CONTROL_t                  EP8_IN_CONTROL;
        EP_CONTROL_t                  EP8_OUT_CONTROL;
        EP_CONTROL_t                  EP9_IN_CONTROL;
        EP_CONTROL_t                  EP9_OUT_CONTROL;
        EP_CONTROL_t                  EP10_IN_CONTROL;
        EP_CONTROL_t                  EP10_OUT_CONTROL;
        EP_CONTROL_t                  EP11_IN_CONTROL;
        EP_CONTROL_t                  EP11_OUT_CONTROL;
        EP_CONTROL_t                  EP12_IN_CONTROL;
        EP_CONTROL_t                  EP12_OUT_CONTROL;
        EP_CONTROL_t                  EP13_IN_CONTROL;
        EP_CONTROL_t                  EP13_OUT_CONTROL;
        EP_CONTROL_t                  EP14_IN_CONTROL;
        EP_CONTROL_t                  EP14_OUT_CONTROL;
        EP_CONTROL_t                  EP15_IN_CONTROL;
        EP_CONTROL_t                  EP15_OUT_CONTROL;
        EP_BUFFER_CONTROL_t           EP0_IN_BUFFER_CONTROL;
        EP_BUFFER_CONTROL_t           EP0_OUT_BUFFER_CONTROL;
        EP_BUFFER_CONTROL_t           EP1_IN_BUFFER_CONTROL;
        EP_BUFFER_CONTROL_t           EP1_OUT_BUFFER_CONTROL;
        EP_BUFFER_CONTROL_t           EP2_IN_BUFFER_CONTROL;
        EP_BUFFER_CONTROL_t           EP2_OUT_BUFFER_CONTROL;
        EP_BUFFER_CONTROL_t           EP3_IN_BUFFER_CONTROL;
        EP_BUFFER_CONTROL_t           EP3_OUT_BUFFER_CONTROL;
        EP_BUFFER_CONTROL_t           EP4_IN_BUFFER_CONTROL;
        EP_BUFFER_CONTROL_t           EP4_OUT_BUFFER_CONTROL;
        EP_BUFFER_CONTROL_t           EP5_IN_BUFFER_CONTROL;
        EP_BUFFER_CONTROL_t           EP5_OUT_BUFFER_CONTROL;
        EP_BUFFER_CONTROL_t           EP6_IN_BUFFER_CONTROL;
        EP_BUFFER_CONTROL_t           EP6_OUT_BUFFER_CONTROL;
        EP_BUFFER_CONTROL_t           EP7_IN_BUFFER_CONTROL;
        EP_BUFFER_CONTROL_t           EP7_OUT_BUFFER_CONTROL;
        EP_BUFFER_CONTROL_t           EP8_IN_BUFFER_CONTROL;
        EP_BUFFER_CONTROL_t           EP8_OUT_BUFFER_CONTROL;
        EP_BUFFER_CONTROL_t           EP9_IN_BUFFER_CONTROL;
        EP_BUFFER_CONTROL_t           EP9_OUT_BUFFER_CONTROL;
        EP_BUFFER_CONTROL_t           EP10_IN_BUFFER_CONTROL;
        EP_BUFFER_CONTROL_t           EP10_OUT_BUFFER_CONTROL;
        EP_BUFFER_CONTROL_t           EP11_IN_BUFFER_CONTROL;
        EP_BUFFER_CONTROL_t           EP11_OUT_BUFFER_CONTROL;
        EP_BUFFER_CONTROL_t           EP12_IN_BUFFER_CONTROL;
        EP_BUFFER_CONTROL_t           EP12_OUT_BUFFER_CONTROL;
        EP_BUFFER_CONTROL_t           EP13_IN_BUFFER_CONTROL;
        EP_BUFFER_CONTROL_t           EP13_OUT_BUFFER_CONTROL;
        EP_BUFFER_CONTROL_t           EP14_IN_BUFFER_CONTROL;
        EP_BUFFER_CONTROL_t           EP14_OUT_BUFFER_CONTROL;
        EP_BUFFER_CONTROL_t           EP15_IN_BUFFER_CONTROL;
        EP_BUFFER_CONTROL_t           EP15_OUT_BUFFER_CONTROL;
    };

    static USB_DPRAM_t & USB_DPRAM = (*(USB_DPRAM_t *)0x50100000);
    static USB_DPRAM_t & USB_DPRAM_XOR = (*(USB_DPRAM_t *)0x50101000);
    static USB_DPRAM_t & USB_DPRAM_SET = (*(USB_DPRAM_t *)0x50102000);
    static USB_DPRAM_t & USB_DPRAM_CLR = (*(USB_DPRAM_t *)0x50103000);

} // _USB_DPRAM_

